# 1. Spring Boot 구현 

딸~깍의 시대라지만, 그래도 작동구조는 알고 있어야 할 것이 아닌가?

기능을 한 번에 구현하지 않고 작은 단계별로 정상 동작을 검증한다.

다시 한번 프로젝트 구조를 보고 가자 

raspberry-monitor/

├── backend/
│   ├── src/
│   ├── build.gradle
│   ├── Dockerfile
│   └── ...
│
├── frontend/
│   ├── src/
│   ├── package.json
│   ├── Dockerfile
│   ├── nginx.conf
│   └── ...
│
├── raspberry/
│   ├── sender.py
│   └── requirements.txt
│
├── docker-compose.yml
│
├── .gitignore
│
└── README.md

## Step 1. Spring Boot 생성

Spring Initializr를 이용해서 Backend 프로젝트를 만든다.

Dependency:

Spring Web
Spring WebSocket
Spring Data JPA
PostgreSQL Driver
Lombok
Validation

Spring Boot가 정상적으로 실행되는지만 확인

localhost:8080