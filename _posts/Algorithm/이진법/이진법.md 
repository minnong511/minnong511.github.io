# 이진법

실전 코딩에서 이진법을 구현해야 한다...? 
생각이 잘 안날 확률이 높다. (당신이 수학킹이라면 걱정은 없겠지만)

bin(int_num)[2:] = 2진법 숫자 

bin(6)[2:]   # '110' -> 이렇게 나온다. 

다만 

def to_binary(n):
    result = ''

    while n > 0:
        result = str(n % 2) + result
        n //= 2

    return result