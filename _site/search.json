[
  
    {
      "title": "Vue DOM",
      "url": "/frontend/vue/2026/08/03/vue-dom/",
      "date": "2026.08.03",
      "categories": "Frontend, Vue",
      "tags": "Frontend, Vue.js, Composition API, ref, reactive, computed, watch",
      "excerpt": "DOM(Document Object Model)이란?",
      "content": "DOM(Document Object Model)이란?정의  HTML 문서를 JavaScript가 조작할 수 있도록 브라우저가 메모리에 만든 객체 구조(Tree 구조)입니다.쉽게 말하면:HTML 파일 → 브라우저가 읽음 → DOM이라는 객체 형태로 변환 → JavaScript가 조작하는 과정입니다.예를 들어 HTML이 있습니다.&lt;html&gt;  &lt;body&gt;    &lt;h1&gt;Hello&lt;/h1&gt;    &lt;p&gt;Vue 공부&lt;/p&gt;  &lt;/body&gt;&lt;/html&gt;브라우저는 이것을:Document│└── html    │    └── body        │        ├── h1        │    └── \"Hello\"        │        └── p             └── \"Vue 공부\"이런 트리 구조 객체로 만듭니다.이게 DOM입니다.왜 DOM이 필요한가?HTML은 원래 정적인 문서입니다.예:&lt;h1&gt;Hello&lt;/h1&gt;브라우저가 화면에 보여주는 것까지는 가능합니다.하지만:“버튼 클릭하면 글자가 바뀌게 해줘”같은 동적인 동작은 HTML만으로 어렵습니다.그래서 JavaScript가 DOM을 조작합니다.JavaScript로 DOM 조작 예시HTML:&lt;h1 id=\"title\"&gt;Hello&lt;/h1&gt;JavaScript:const title = document.querySelector(\"#title\")title.innerText = \"Hello Vue\"결과:화면:변경 전:Hello변경 후:Hello Vue흐름:HTML ↓브라우저 ↓DOM 생성 ↓JavaScript가 DOM 수정 ↓화면 변경DOM은 그냥 HTML인가?아닙니다.중요한 차이:HTML:&lt;h1&gt;Hello&lt;/h1&gt;문자열(텍스트 문서)DOM:{ tag:\"h1\", children:[   \"Hello\" ]}객체 구조즉:                   HTML      DOM                  형태      문서      객체              목적      내용 표현      프로그램 조작              관리 주체      파일      브라우저 메모리      그런데 DOM 조작이 왜 문제가 됨?여기서 Vue가 등장합니다.예:게시판에 글 10,000개가 있다고 생각해봅시다.사용자가 좋아요 버튼 클릭:좋아요 10↓좋아요 11변경해야 합니다.순수 JavaScript:element.innerText = 11직접 DOM 수정문제:개발자가 직접:  어떤 DOM을 바꿀지  언제 바꿀지  어떤 순서로 바꿀지관리해야 합니다.프로젝트가 커지면:데이터 ↓DOM ↓다른 DOM ↓이벤트 ↓다른 데이터관리가 복잡해집니다.Vue의 방식Vue는:“DOM을 직접 만지지 말고 데이터만 변경해”라는 철학입니다.예:const count = ref(0)count.value++개발자는:데이터 변경만 합니다.Vue가:데이터 변경 ↓반응성 시스템 감지 ↓Virtual DOM 생성 ↓기존 DOM과 비교 ↓필요한 부분만 수정합니다.Real DOM vs Virtual DOMReal DOM실제 브라우저 화면을 구성하는 DOMBrowser Memory&lt;html&gt; └── body      └── div           └── buttonVirtual DOMVue가 메모리에 따로 가지고 있는 가상 DOMJavaScript Memory{ type:\"button\", text:\"Click\"}변경 발생:기존:Virtual DOM A&lt;button&gt;Count:0&lt;/button&gt;변경:Virtual DOM B&lt;button&gt;Count:1&lt;/button&gt;Vue가 비교:A와 B 비교Count 부분만 변경 필요실제 DOM:Count:1Vue 관점에서 DOM 이해 흐름가장 중요한 흐름:사용자 행동      ↓데이터 변경      ↓ref/reactive      ↓Vue 반응성 시스템      ↓Virtual DOM 비교      ↓Real DOM 업데이트      ↓화면 변경지금까지 배운 개념을 연결하면:ref/reactive      ↓상태(state)      ↓변경 감지      ↓Virtual DOM      ↓DOM 업데이트      ↓화면입니다.다음으로 “브라우저 렌더링 과정(Critical Rendering Path) → DOM 생성 → Vue 렌더링 과정”을 공부해야함"
    },
  
    {
      "title": "JavaScript & Vue 기본 단어 정의 ",
      "url": "/frontend/vue/2026/08/03/vue-word/",
      "date": "2026.08.03",
      "categories": "Frontend, Vue",
      "tags": "Frontend, JavaScript, ES6, Vue.js, Data Type, ref",
      "excerpt": "JavaScript 데이터 타입과 Vue 반응형 데이터 구조",
      "content": "JavaScript 데이터 타입과 Vue 반응형 데이터 구조1. 데이터 타입의 큰 분류데이터│├── 원시값(Primitive Type)│└── 참조값(Reference Type)2. 원시값(Primitive Value)정의원시값은 하나의 값 자체를 저장하는 가장 기본적인 데이터 형태다.stringnumberbooleannullundefinedsymbolbigint예시const age = 20const name = 'Kim'const isLogin = true메모리 구조age | ↓20원시값은 값 자체를 저장한다.값 복사원시값을 복사하면 메모리 주소가 아니라 값 자체가 복사된다.let a = 10let b = ab = 20console.log(a)// 10a↓10b↓10처음에는 같은 값이지만, 값을 복사한 뒤에는 서로 독립적으로 동작한다.3. 객체(Object)정의객체는 여러 개의 데이터를 하나로 묶어서 저장하는 자료 구조다.예시const user = {  name: 'Kim',  age: 20,  city: 'Seoul'}구조user | ↓객체 | ├── name: Kim ├── age: 20 └── city: Seoul객체는 속성(property)을 가진다.속성 접근user.name// 'Kim'user.name은 user 객체 안에 있는 name 값을 가져온다.4. 배열(Array)정의배열은 여러 개의 값을 순서대로 저장하는 객체다.배열도 객체의 한 종류이지만, 일반적으로 순서가 있는 여러 데이터를 저장할 때 사용한다.예시const fruits = [  'apple',  'banana',  'orange']구조fruits | ↓Array | ├── 0: apple ├── 1: banana └── 2: orange배열은 index(순서)로 접근한다.fruits[0]// 'apple'5. 객체와 배열의 차이둘 다 여러 데이터를 저장하지만 목적이 다르다.객체객체는 하나의 대상을 표현할 때 사용한다.const user = {  name: 'Kim',  age: 20}위 객체는 사용자 1명의 정보를 표현한다.배열배열은 여러 데이터의 목록을 표현할 때 사용한다.const users = [  {    name: 'Kim'  },  {    name: 'Lee'  }]위 배열은 사용자 목록을 표현한다.            구분      객체      배열                  목적      하나의 대상 표현      여러 데이터 목록              접근      이름(key)      순서(index)              예시      user      users      6. 객체 안에 객체를 넣을 수 있다객체 안에 다른 객체를 넣는 구조는 실무에서 자주 사용된다.const user = {  name: 'Kim',  address: {    city: 'Seoul',    zip: '12345'  }}구조user├── name│└── address    |    ├── city    └── zip접근user.address.city// 'Seoul'7. 배열 안에 객체를 넣을 수 있다Vue에서 자주 볼 수 있는 형태다.const todos = [  {    id: 1,    text: 'Vue 공부'  },  {    id: 2,    text: 'JavaScript 공부'  }]구조todos 배열0 | └── 객체      ├── id      └── text1 | └── 객체      ├── id      └── text접근todos[0].text// 'Vue 공부'8. Vue에서 객체와 배열이 중요한 이유Vue에서는 객체를 ref로 감싸서 반응형 상태로 만들 수 있다.import { ref } from 'vue'const user = ref({  name: 'Kim',  age: 20})데이터 구조ref | ↓객체 | ├── name └── ageref로 만든 객체의 값에 접근할 때는 .value를 사용한다.user.value.age데이터 접근 흐름user ↓ref 객체 ↓value ↓일반 객체 ↓age9. 배열도 같은 원리로 접근한다import { ref } from 'vue'const todos = ref([  {    id: 1,    text: '공부'  }])배열 안의 객체에 접근할 때는 .value와 index를 함께 사용한다.todos.value[0].text// '공부'데이터 접근 흐름todos ↓ref 객체 ↓value ↓배열 ↓0번째 객체 ↓text10. 결론개발할 때는 데이터가 다음과 같은 구조로 감싸져 있다고 생각하면 된다.import { ref } from 'vue'const weatherList = ref([  {    id: 1,    name: '서울',    temp: 30  }])weatherList | ↓ref | ↓value | ↓Array | ↓Object | ├── id ├── name └── temp따라서 다음과 같은 방식으로 데이터를 추출할 수 있다.weatherList.value[0].temp// 3011. Vue 공부에서 꼭 구분해야 하는 용어            용어      의미                  값(value)      20, 'hello', true 같은 단일 데이터              변수(variable)      값을 저장하는 이름              객체(object)      key를 사용해 데이터를 저장하는 자료 구조              속성(property)      객체 내부에 저장된 데이터              배열(array)      데이터를 순서대로 저장하는 자료 구조              원소(element)      배열 안에 있는 하나의 데이터              참조(reference)      객체의 주소를 가리키는 것      1. Vue.js 기본 용어1) component화면을 구성하는 독립적인 UI 부품WeatherParent.vue├── SearchBar.vue├── WeatherCard.vue└── WeatherForecast.vueVue 컴포넌트는 일반적으로 세 부분으로 구성한다.  2) Template브라우저에 표시한 HTML 구조를 작성하는 영역  는 JavaScript 값을 화면에 출력하는 문자열 보간법3) DirectiveHTML 태그에 Vue 기능을 추가하는 문법, 대부분 V- 시작함.보이는 문장&lt;button @click=”increase”&gt;증가&lt;/button&gt;대표적인 Directive            문법      역할                  v-if      조건에 따라 요소 생성·제거              v-show      조건에 따라 CSS로 숨김              v-for      배열을 반복 출력              v-model      입력값과 상태를 양방향 연결              v-bind 또는 :      속성에 JavaScript 값 연결              v-on 또는 @      이벤트 연결      4) Reactive State값이 변경되었을 때, Vue가 이를 감지하고 화면을 자동으로 다시 갱신하는 상태  8count가 변경되면 화면의 숫자도 자동으로 변경된다.5) ref숫자, 문자열, 불리언, 객체, 배열 등을 반응형으로 만드는 함수.const count = ref(0)const name = ref(‘민형’)const user = ref({  age: 25,})JavaScript 영역에서는 .value가 필요하다.count.value++user.value.age++그러나 tempalte에서는 .value를 사용하지 않는다.86) reactive주로 객체를 반응형으로 만드는 함수import { reactive } from ‘vue’const user = reactive({  name: ‘민형’,  age: 25,})user.age++reactive에서는 .value를 사용하지 않습니다.ref와 reactive 비교const count = ref(0)count.value++const user = reactive({ age: 25 })user.age++7. Computed기존 반응형 상태를 이용해 새로운 계산 결과를 만드는 값.const price = ref(10000)const quantity = ref(3)const totalPrice = computed(() =&gt; {  return price.value * quantity.value})총 가격: 원특징 :  의존하는 값이 바뀌면 자동으로 다시 계산  계산결과를 캐싱  기본적으로 읽기 전용  화면에 보여줄 파생 값을 만들 때 사용const filteredUsers = computed(() =&gt; {  return users.value.filter((user) =&gt;    user.name.includes(searchQuery.value)  )})8) Method / Function  버튼 클릭이나 특정 동작이 발생했을 떄 실행하는 함수const count = ref(0)const increase = () =&gt; {  count.value++}&lt;button @click=”increase”&gt;증가&lt;/button&gt;computed는 값을 계산하고, 함수는 행동을 실행한다고 생각하면 쉽다.화면 제어 용어9) v-if조건이 참일 때만 HTML 요소를 생성합니다.로그인되었습니다.로그인이 필요합니다.조건이 거짓이면 실제 DOM에서 요소가 제거됨10) v-show요소는 그대로 두고 CSS의 display: none으로 숨긴다.내용v-if와 v-show            구분      v-if      v-show                  거짓일 때      DOM에서 제거      CSS로 숨김              전환 비용      큼      작음              자주 열고 닫기      부적합      적합              처음부터 조건이 중요      적합      부적합      모달이나 드롭다운처럼 자구 열고 닫는 요소는 v-show가 유리11) v-for배열 데이터를 반복해서 화면에 표시const cities = ref([  { id: 1, name: ‘서울’ },  { id: 2, name: ‘부산’ },])&lt;div  v-for=”city in cities”  :key=”city.id”&lt;/div&gt;12) KeyVue가 반복된 각 요소를 구분하도록 제공하는 고유 식별자&lt;div v-for=”city in cities” :key=”city.id”일반적으로 DB ID처럼 변하지 않는 값을 사용:key=”city.id”13) v-model입력창의 값과 반응형 상태를 양방향으로 연결합니다.const searchQuery = ref(‘’) 14) Event클릭, 입력, 제출처럼 사용자가 발생시키는 동작&lt;button @click=”handleClick”&gt;클릭&lt;/button&gt;&lt;input @input=”handleInput” /&gt;&lt;form @submit.prevent=”handleSubmit”&gt;대표 이벤트 :            이벤트      의미                  click      클릭              input      입력값 변경              change      입력 확정 또는 선택 변경              submit      폼 제출              keydown      키보드 누름      15) Event Modifier이벤트 동작을 간단히 제어하는 수식어&lt;a @click.prevent = “handelLink”&gt; 링크 &lt;/a&gt; &lt;button @click.stop = “handleClick”&gt; 버튼 &lt;/button&gt;            수식어      기능                  .prevent      기본 동작 방지              .stop      이벤트 버블링 방지              .once      한 번만 실행              .self      자신을 직접 클릭한 경우만 실행      3. 컴포넌트 통신 용어16) Props부모 컴포넌트가 자식 컴포넌트에 데이터를 전달하는 방법부모&lt;WeatherCard     :city = “selectedCity”    title = “현재 날씨”/&gt;자식    데이터 흐름:부모  -&gt; props -&gt; 자식Props는 자식에서 직접 수정하지 않는 것이 원칙17) Emit자식 컴포넌트가 부모 컴포넌트에 이벤트나 데이터를 전달하는 방법자식  &lt;button @click=\"handleClick\"&gt;서울 선택&lt;/button&gt;부모&lt;WeatherCard @select-city=”handleSelectCity” /&gt;const handleSelectCity = (cityName) =&gt; {    console.log(cityName)}데이터 흐름:자식 -&gt; emit -&gt; 부모정리하면:부모가 자식에게 데이터 전달: Props자식이 부모에게 알림 전달: Emit18) Slot부모가 자식 컴포넌트의 특정 위치에 HTML 내용을 삽입하는 기능자식        부모     서울 날씨      현재 기온 28도 BaseCard는 공통 틀을 만들고, 내부 콘텐츠는 부모가 결정4.상태 변화 감시19) watch특정 상태의 변경을 감지한 후 부수 효과를 실행한 이유watch(searchQuery, (newValue, oldValue) =&gt; {  console.log(‘새 값:’, newValue)  console.log(‘이전 값:’, oldValue)})다음과 같은 작업에 적합  API 요청  localStroage 저장  로그 기록  특정 값 변경 시 별도 처리객체의 특정 속성을 감시할 수 있다.watch(    () =&gt; user.value.age,     (newAge, oldAge) =&gt; {        console.log(newAge,oldAge)    })"
    },
  
    {
      "title": "Vue Router Navigation Guard 정리",
      "url": "/frontend/vue/2026/08/03/vue-router-navigation-guard/",
      "date": "2026.08.03",
      "categories": "Frontend, Vue",
      "tags": "Frontend, Vue.js, Vue Router, Navigation Guard",
      "excerpt": "Vue Router Navigation Guard 정리",
      "content": "Vue Router Navigation Guard 정리Vue Router의 Navigation Guard"
    },
  
    {
      "title": "Vue 라이브러리 정리",
      "url": "/frontend/vue/2026/08/03/vue-library/",
      "date": "2026.08.03",
      "categories": "Frontend, Vue",
      "tags": "Frontend, Vue.js, Library",
      "excerpt": "Vue 라이브러리 정리",
      "content": "Vue 라이브러리 정리"
    },
  
    {
      "title": "JavaScript ES6 핵심 문법",
      "url": "/frontend/vue/2026/08/03/vue-es6/",
      "date": "2026.08.03",
      "categories": "Frontend, Vue",
      "tags": "Frontend, JavaScript, ES6, Vue.js",
      "excerpt": "JavaScript ES6 핵심 문법",
      "content": "JavaScript ES6 핵심 문법"
    },
  
    {
      "title": "Vue 컴포넌트의 구조와 생명주기",
      "url": "/frontend/vue/2026/08/03/vue-component/",
      "date": "2026.08.03",
      "categories": "Frontend, Vue",
      "tags": "Frontend, Vue.js, Component, SFC, Lifecycle",
      "excerpt": "Vue 컴포넌트의 구조와 생명주기",
      "content": "Vue 컴포넌트의 구조와 생명주기1. Component란?소프트웨어 공학에서 컴포넌트(Component)는 독립적인 기능을 수행하고, 필요할 때 다른 부품으로 교체하거나 다른 프로그램과 연결할 수 있는 표준화된 소프트웨어 모듈을 말한다.컴포넌트의 핵심 특징은 다음과 같다.  독립성(Independence): 기능과 책임이 하나의 단위로 분리된다.  교체 가능성(Replaceability): 같은 역할을 수행하는 다른 컴포넌트로 교체할 수 있다.  재사용성(Reusability): 여러 화면이나 프로젝트에서 반복해서 사용할 수 있다.2. Vue Component란?Vue 컴포넌트는 웹 페이지를 구성하는 독립적이고 재사용 가능한 블록이다.Vue에서는 HTML, CSS, JavaScript를 하나의 .vue 파일에 모아 작성하는 방식을 SFC(Single-File Component)라고 한다.하나의 애플리케이션은 여러 컴포넌트를 조립해 완성하며, 컴포넌트들은 트리 구조로 연결된다.App├── Header├── Main│   ├── UserProfile│   └── ProductList└── Footer3. Component Hierarchy컴포넌트 계층 구조에서 자주 사용하는 관계는 다음과 같다.            관계      의미      데이터 전달 방식                  Parent-Child      부모가 자식을 포함하는 관계      부모에서 자식으로 props, 자식에서 부모로 emit              Sibling      같은 부모 아래에 나란히 있는 형제 관계      부모를 통해 상태를 공유하거나 공통 저장소 사용              Ancestor-Descendant      자식의 자식까지 이어지는 다층 계층 관계      provide와 inject, 공통 저장소 등을 사용      부모와 자식은 서로 독립된 컴포넌트다. 자식은 부모의 상태를 직접 수정하지 않고 props로 전달받으며, 부모에게 필요한 이벤트는 emit으로 알린다.4. Component 지역 등록지역 등록은 특정 부모 컴포넌트에서만 자식 컴포넌트를 사용하는 방식이다.  부모 컴포넌트에서 자식 컴포넌트를 import한다.  등록한 자식 컴포넌트는 &lt;template&gt; 영역에서 컴포넌트 태그처럼 사용할 수 있다.  컴포넌트 이름은 일반적으로 PascalCase를 사용한다.  &lt;script setup&gt;에서는 import한 컴포넌트가 자동으로 등록된다.&lt;script setup&gt;import BaseButton from './components/BaseButton.vue'&lt;/script&gt;&lt;template&gt;  &lt;div class=\"box\"&gt;    &lt;h3&gt;컴포넌트 조립 테스트&lt;/h3&gt;    &lt;hr&gt;    &lt;BaseButton /&gt;  &lt;/div&gt;&lt;/template&gt;5. Component 전역 등록전역 등록한 컴포넌트는 Vue 애플리케이션의 여러 컴포넌트에서 별도의 import 없이 사용할 수 있다.전역 등록은 보통 main.js에서 수행한다.import { createApp } from 'vue'import App from './App.vue'import BaseButton from './components/BaseButton.vue'import BaseInput from './components/BaseInput.vue'const app = createApp(App)app.component('BaseButton', BaseButton)app.component('BaseInput', BaseInput)app.mount('#app')app.component()의 첫 번째 인자는 템플릿에서 사용할 컴포넌트 이름이고, 두 번째 인자는 import한 컴포넌트 변수다.6. Vue 주요 내장 함수Vue는 애플리케이션 생성, 반응형 상태 관리, 계산과 감시, 컴포넌트 조립 등을 위한 함수를 제공한다.            카테고리      주요 함수                  애플리케이션(Application)      createApp, createSSRApp, app.*, app.config.*              반응형 상태(Reactive State)      ref, reactive, readonly, shallowRef, shallowReactive, shallowReadonly, toRef, toRefs, customRef, unref, toRaw, markRaw, isRef, isReactive, isReadonly              계산 및 감시(Computed &amp; Watchers)      computed, watch, watchEffect              라이프사이클 훅(Lifecycle Hooks)      onBeforeMount, onMounted, onBeforeUpdate, onUpdated, onBeforeUnmount, onUnmounted, onActivated, onDeactivated, onErrorCaptured, onRenderTracked, onRenderTriggered              컴포넌트 구성(Component Composition)      defineComponent, defineProps, defineEmits, useAttrs, defineExpose, useSlots, withDefaults, getCurrentInstance              렌더링 제어(Rendering &amp; DOM)      h, resolveComponent, withDirectives, renderList, renderSlot, mergeProps, nextTick, useCssModule, useCssVars              의존성 주입(Dependency Injection)      provide, inject, hasInjectionContext      7. Component Lifecycle컴포넌트 생명주기는 컴포넌트가 생성되고, 화면에 연결되고, 갱신된 뒤 제거되는 흐름이다.            단계      컴포넌트의 상태      주요 작업과 훅                  생성 및 설정(Creation &amp; Setup)      컴포넌트 인스턴스가 만들어지고 JavaScript에서 사용할 준비를 하는 단계      ref, reactive, computed, watch 등을 초기화한다.              부착(Mounting)      가상으로 구성한 화면을 실제 DOM에 연결하는 단계      DOM에 접근하거나 초기 데이터를 요청한다. onMounted를 사용한다.              갱신(Updating)      반응형 데이터가 변경되어 화면을 다시 그리는 단계      업데이트된 DOM을 확인하거나 크기와 스크롤 위치를 다시 계산한다. onUpdated를 사용한다.              소멸(Unmounting)      v-if=\"false\" 등의 조건으로 컴포넌트가 화면에서 제거되는 단계      타이머와 이벤트 리스너를 정리해 메모리 누수를 방지한다. onUnmounted를 사용한다.      라이프사이클 훅을 사용한 간단한 예시는 다음과 같다.&lt;script setup&gt;import { onMounted, onUnmounted } from 'vue'const handleResize = () =&gt; {  console.log(window.innerWidth)}onMounted(() =&gt; {  window.addEventListener('resize', handleResize)})onUnmounted(() =&gt; {  window.removeEventListener('resize', handleResize)})&lt;/script&gt;onMounted에서 등록한 이벤트 리스너는 onUnmounted에서 제거해야 컴포넌트가 사라진 뒤에도 불필요한 동작이 남지 않는다.Props &amp; EmitsComponent 연동  Vue 3의 모든 컴포넌트 연동은”데이터는 위에서 아래로 물려주고, 이벤트는 아래에서 위로 쏘아 올린다”는 구조를 따른다.분류 🔽 Props (하행선) 🔼 Emits (상행선)개념 정의 부모가 자식에게 주는 반응형 데이터 값 자식이 부모에게 보고하는 이벤트흐름 방향 부모 → 자식 (위에서 아래로) 자식 → 부모 (아래에서 위로)데이터 권한 읽기 전용. 자식은 수정 불가 부모에게 변경 요청 및 값 전달 가능Compiler Macro defineProps({ … }) defineEmits([ … ])Parent Binding 자식 태그 속성에 콜론(:)으로 주입 자식 태그 이벤트에 골뱅이(@)로 청취Compiler Macro란 Runtime 시점이 아닌 Build 시점에 Vue Compiler가 코드를 변환하는 특수 예약어로 defineProps(),defineEmits(), defineExpose() 같은 함수들은 defineProps() - 속성 정의자식 컴포넌트 내부에서”부모가 넘겨줄 데이터(속성)의 이름과 규격”을 선언하는 Vue 3 내장 컴파일러 매크로 함수  Compiler Macro이기 때문에 상단에 import할 필요 없이   간단한 배열 표기법과, 강력한 객체 표기법이 있다[배열 형식]const props = defineProps([‘title’, ‘count’])[객체 형식– 기본값 지정]defineProps({// 1. 타입만 간단히 지정하는 경우title: String,// 2. 필수 값과 기본값까지 꼼꼼하게 지정하는 경우likes: {type: Number,required: true // 부모가 이 값을 안 넘기면 에러발생.},status: {type: String,default: ＇대기 중＇ // 부모가 값을 안 주면 이 값이 기본으로 세팅.}})▪  에서 사용하기• 정의된 변수를 그대로 사용하면 된다.좋아요: ▪ Readonly• defineProps로 Parent에서 전달된 값은 읽기 전용(ReadOnly)이다.• Child Component에서 이 값을 직접 바꾸려고 하면 에러가 발생된다.const props = defineProps([‘likes’])const brokenFunction = () =&gt; {// ❌ 절대 금지! 콘솔에 ReadOnly 에러 발생.props.likes = 999}"
    },
  
    {
      "title": "Axios 정리",
      "url": "/frontend/vue/2026/08/03/vue-axios/",
      "date": "2026.08.03",
      "categories": "Frontend, Vue",
      "tags": "Frontend, Vue.js, Axios",
      "excerpt": "Axios 정리",
      "content": "Axios 정리"
    },
  
    {
      "title": "Pinia 상태 관리 정리",
      "url": "/frontend/vue/2026/08/03/pinia/",
      "date": "2026.08.03",
      "categories": "Frontend, Vue",
      "tags": "Frontend, Vue.js, Pinia, State Management",
      "excerpt": "Pinia 상태 관리 정리",
      "content": "Pinia 상태 관리 정리1. Pinia가 필요한 이유App.vue├── Header.vue │      └── 로그인 사용자 이름 표시 │ ├── ProductList.vue │      └── 상품 목록 │ └── Cart.vue        └── 장바구니 개수 표시로그인 정보 :const user = {  name:”Kim”,  role:”admin”}방법.1App.vue | props ↓Header.vueApp.vue | props ↓Cart.vue문제 : 중간 컴포넌트를 계속 거쳐야 한다.-&gt; Props DrillingA |props ↓B |props ↓C |props ↓D깊어질수록 관리가 어렵다.# 2. PiniaPinia는 Vue 어플리케이션에서 전역 상태 (Global State)를 관리하기 위한 공식 상태 라이브러리쉽게…여러 컴포넌트가 공유해야하는 데이터를 한 곳에서 관리하는 저장소구조.Component A      |      props      |Component B      |      props      |Component C-&gt; Pinia를 통해서 한번에 전달하도록 개선할 수 있다.    Pinia Store   user   cart   product  ↑       ↑       ↑Component Component Component# 3. State먼저 State 개념State애플리케이션이 현재 가지고 있는 데이터const count = ref(0)이것이 State쇼핑몰 :state:usercartItemsproductsisLogin# 4. StoreStore 정의State, Getter, Action을 하나로 묶어서 관리하는 저장하는 공간Pinia의 기본 구조 .Store├── State │ ├── Getter │ └── Action# Pinia 사용법npm install piniaimport {createApp} from ‘vue’import {createPinia} from ‘pinia’import APP from ‘./App.vue’const app = create(App)app.use(createPinai())app.mount(‘#app’)의미 :Vue App | Pinia 연결 | 모든 Component에서 사용 가능# 7.Store 만들기stores/user.jsimport { defineStore } from ‘pinia’export const useUserStore = defineStore(‘user’,{state:()=&gt;({name:””,  isLogin:false})})–Component    |    ↓useUserStore()    |    ↓Pinia Store    |    ↓state 사용# 9.State 변경"
    },
  
    {
      "title": "Vue Composition API 정리",
      "url": "/frontend/vue/2026/08/02/vue-composition-api/",
      "date": "2026.08.02",
      "categories": "Frontend, Vue",
      "tags": "Frontend, Vue.js, Composition API, ref, reactive, computed, watch",
      "excerpt": "",
      "content": "Composition API란?자자, 이번에는 Vue의 Composition API를 정리해보자.Vue 컴포넌트에는 보통 다음과 같은 로직이 들어간다.  화면에서 사용하는 상태  상태를 변경하는 함수  상태에서 계산되는 값  서버 요청이나 이벤트 감시  컴포넌트 생명주기에 맞춰 실행할 작업Composition API는 이런 로직을 옵션별로 흩어놓기보다, 기능 단위로 가까이 모아서 작성하는 방식이다. 그래서 컴포넌트가 커질수록 어떤 상태와 함수가 한 기능을 이루는지 파악하기 쉬워진다.Vue 3에서는 &lt;script setup&gt;과 함께 Composition API를 많이 사용한다.&lt;script setup&gt;import { computed, ref } from 'vue'const count = ref(0)const doubleCount = computed(() =&gt; {  return count.value * 2})const increase = () =&gt; {  count.value++}&lt;/script&gt;&lt;template&gt;  &lt;p&gt;현재 값: {{ count }}&lt;/p&gt;  &lt;p&gt;두 배 값: {{ doubleCount }}&lt;/p&gt;  &lt;button @click=\"increase\"&gt;증가&lt;/button&gt;&lt;/template&gt;이 코드에서는 count 상태, doubleCount 계산값, increase 함수가 한곳에 모여 있다. 화면과 연결되는 템플릿도 아래에서 바로 확인할 수 있다.Composition API에서 자주 쓰는 함수처음부터 모든 함수를 외울 필요는 없다. 우선 아래 함수들의 역할을 구분하면 된다.            분류      주요 함수      역할                  반응형 상태      ref, reactive      변경을 추적하는 상태를 만든다.              계산과 감시      computed, watch, watchEffect      상태에서 값을 계산하거나 변경 후 작업을 실행한다.              생명주기      onMounted, onUpdated, onUnmounted      컴포넌트의 생성·수정·제거 시점에 코드를 실행한다.              컴포넌트 통신      defineProps, defineEmits      부모와 자식 컴포넌트 사이의 데이터를 연결한다.              반응성 변환      toRef, toRefs, unref      반응형 객체의 속성을 ref로 연결하거나 값을 꺼낸다.              의존성 주입      provide, inject      깊은 컴포넌트 트리에서 값을 공유한다.              DOM·렌더링      nextTick, h      DOM 반영 시점을 기다리거나 렌더링을 직접 제어한다.      이번 글에서는 그중에서도 ref, reactive, computed, watch, watchEffect를 중심으로 살펴보자.ref()로 반응형 상태 만들기ref()는 원시 값과 객체·배열을 반응형 상태로 감싸는 함수다.&lt;script setup&gt;import { ref } from 'vue'const count = ref(0)const name = ref('홍길동')const isActive = ref(true)const items = ref(['사과', '배'])const user = ref({  name: '이순신',  age: 30})const increase = () =&gt; {  count.value++}const changeUserName = () =&gt; {  user.value.name = '장보고'}&lt;/script&gt;&lt;template&gt;  &lt;p&gt;카운트: {{ count }}&lt;/p&gt;  &lt;p&gt;이름: &lt;input v-model=\"name\" /&gt;&lt;/p&gt;  &lt;p&gt;활성 상태: {{ isActive ? '활성' : '비활성' }}&lt;/p&gt;  &lt;p&gt;과일 목록: {{ items.join(', ') }}&lt;/p&gt;  &lt;p&gt;사용자: {{ user.name }} / {{ user.age }}세&lt;/p&gt;  &lt;button @click=\"increase\"&gt;카운트 증가&lt;/button&gt;  &lt;button @click=\"isActive = !isActive\"&gt;상태 토글&lt;/button&gt;  &lt;button @click=\"items.push('귤')\"&gt;과일 추가&lt;/button&gt;  &lt;button @click=\"changeUserName\"&gt;이름 변경&lt;/button&gt;&lt;/template&gt;&lt;script setup&gt; 안의 JavaScript에서는 .value를 사용하고, 템플릿에서는 Vue가 자동으로 언래핑해주므로 .value를 생략한다.const count = ref(0)// JavaScriptcount.value++console.log(count.value)&lt;!-- template --&gt;&lt;p&gt;{{ count }}&lt;/p&gt;ref를 사용할 때 기억할 점  ref를 사용하려면 vue에서 import해야 한다.  JavaScript에서 값을 읽거나 변경할 때는 .value를 사용한다.  템플릿에서는 .value를 생략한다.  객체나 배열을 ref로 만들면 내부 값에도 반응성이 연결된다.reactive()로 객체와 배열 다루기reactive()는 객체나 배열 자체를 반응형 객체로 만든다. ref와 달리 .value 없이 속성에 접근한다.&lt;script setup&gt;import { reactive } from 'vue'const user = reactive({  name: '이순신',  age: 30})const items = reactive(['사과', '바나나'])const celebrate = () =&gt; {  user.age++}const addItem = () =&gt; {  items.push(`과일 ${items.length + 1}`)}const removeItem = (index) =&gt; {  items.splice(index, 1)}&lt;/script&gt;&lt;template&gt;  &lt;p&gt;{{ user.name }} / {{ user.age }}세&lt;/p&gt;  &lt;button @click=\"celebrate\"&gt;나이 한 살 추가&lt;/button&gt;  &lt;ul&gt;    &lt;li v-for=\"(item, index) in items\" :key=\"item\"&gt;      {{ item }}      &lt;button @click=\"removeItem(index)\"&gt;삭제&lt;/button&gt;    &lt;/li&gt;  &lt;/ul&gt;  &lt;button @click=\"addItem\"&gt;과일 추가&lt;/button&gt;&lt;/template&gt;reactive의 주의점반응형으로 만든 객체를 통째로 다른 객체로 교체하면 기존 반응성 연결이 끊긴다.let state = reactive({ count: 0 })// 잘못된 사용: 반응형 객체 자체를 교체함state = { count: 5 }// 올바른 사용: 기존 객체의 속성을 변경함state.count = 5객체를 통째로 교체해야 하거나 .value를 사용하는 방식이 더 명확하다 싶으면 ref를 선택하면 된다. 무조건 어느 한쪽이 정답이라기보다는 데이터의 형태와 팀의 작성 규칙에 맞춰 선택하는 것이 중요하다.ref와 reactive 비교            구분      ref      reactive                  대상      원시 값, 객체, 배열      객체, 배열, Map, Set              JavaScript 접근      .value 필요      속성에 바로 접근              객체 교체      .value = 새 객체로 가능      연결이 끊길 수 있음              사용 예      단일 값, 교체 가능한 상태      여러 속성을 묶은 객체      반응성 연결을 유지하는 보조 함수reactive 객체를 구조 분해하면 반응성 연결이 끊길 수 있다. 이때 toRef나 toRefs를 사용하면 원본 객체와 연결된 ref를 만들 수 있다.import { reactive, toRefs } from 'vue'const state = reactive({  name: '홍길동',  age: 20})const { name, age } = toRefs(state)age.value++// state.age도 함께 변경됨그 밖의 반응성 보조 함수는 필요할 때 찾아서 사용하면 된다.            함수      역할                  readonly      반응형 값을 읽기 전용으로 만든다.              shallowRef      최상위 ref 값의 변경만 추적한다.              shallowReactive      객체의 최상위 속성만 반응형으로 만든다.              unref      ref라면 .value, 일반 값이면 원래 값을 반환한다.              toRaw      반응형 프록시의 원본 객체를 가져온다.              markRaw      특정 객체가 반응형으로 변환되지 않게 한다.              isRef, isReactive, isReadonly      반응형 여부를 확인한다.      computed()로 계산된 값 만들기computed()는 기존 반응형 상태를 바탕으로 새로운 값을 계산한다. 계산에 사용한 의존성이 변경될 때 다시 계산하고, 같은 값이 필요할 때는 이전 결과를 재사용한다.&lt;script setup&gt;import { computed, ref } from 'vue'const count = ref(0)const dummy = ref(0)const doubleCount = computed(() =&gt; {  return count.value * 2})const increaseCount = () =&gt; {  count.value++}const increaseDummy = () =&gt; {  dummy.value++}&lt;/script&gt;&lt;template&gt;  &lt;p&gt;count: {{ count }}&lt;/p&gt;  &lt;p&gt;dummy: {{ dummy }}&lt;/p&gt;  &lt;p&gt;두 배 값: {{ doubleCount }}&lt;/p&gt;  &lt;button @click=\"increaseCount\"&gt;count 증가&lt;/button&gt;  &lt;button @click=\"increaseDummy\"&gt;dummy 증가&lt;/button&gt;&lt;/template&gt;dummy가 변경되어 컴포넌트가 다시 렌더링되어도 doubleCount의 계산에 dummy가 사용되지 않았다면 다시 계산할 필요가 없다. 이것이 일반 함수와 computed의 중요한 차이다.const totalPrice = computed(() =&gt; {  return products.value.reduce((sum, product) =&gt; {    return sum + product.price  }, 0)})computed는 계산된 값이므로 템플릿에서 함수처럼 호출하지 않는다.&lt;!-- 올바른 사용 --&gt;&lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt;&lt;!-- 잘못된 사용 --&gt;&lt;button @click=\"totalPrice(products)\"&gt;총가격&lt;/button&gt;일반 함수와 computed 구분// 사용자 행동을 처리하는 일반 함수const save = () =&gt; {  alert('저장되었습니다.')}// 상태에서 값을 계산하는 computedconst completedCount = computed(() =&gt; {  return todos.value.filter((todo) =&gt; todo.done).length})사용자가 행동한다 → 일반 함수상태에서 값이 나온다 → computedwatch()로 변경 후 작업 실행하기watch()는 특정 반응형 값의 변경을 감시하고, 값이 바뀐 뒤 후속 작업을 실행한다.  서버 요청  로컬 스토리지 저장  로그 기록  다른 상태 변경  외부 라이브러리와 동기화&lt;script setup&gt;import { ref, watch } from 'vue'const currentCity = ref('서울')const logMessage = ref('아직 감시 시스템이 작동하지 않았습니다.')watch(currentCity, (newValue, oldValue) =&gt; {  logMessage.value = `${oldValue}에서 ${newValue}(으)로 변경됨.`  console.log(`${newValue}의 날씨 API를 다시 조회합니다.`)})&lt;/script&gt;&lt;template&gt;  &lt;p&gt;현재 도시: {{ currentCity }}&lt;/p&gt;  &lt;button @click=\"currentCity = '서울'\"&gt;서울&lt;/button&gt;  &lt;button @click=\"currentCity = '수원'\"&gt;수원&lt;/button&gt;  &lt;button @click=\"currentCity = '부산'\"&gt;부산&lt;/button&gt;  &lt;p&gt;{{ logMessage }}&lt;/p&gt;&lt;/template&gt;콜백에는 새 값과 이전 값이 순서대로 전달된다.여러 값 감시하기첫 번째 인자에 감시할 ref를 배열로 전달하면 여러 값을 한 번에 감시할 수 있다.import { ref, watch } from 'vue'const city = ref('서울')const dateType = ref('오늘')const apiStatus = ref('대기 중...')watch(  [city, dateType],  ([newCity, newDate], [oldCity, oldDate]) =&gt; {    apiStatus.value = `${oldCity}(${oldDate}) → ${newCity}(${newDate})`  })감시 대상 배열의 순서와 콜백의 새 값·이전 값 배열 순서는 서로 대응한다.객체와 배열 깊이 감시하기객체나 배열 내부의 변경까지 감시하려면 deep: true를 사용할 수 있다.import { ref, watch } from 'vue'const user = ref({  name: '홍길동',  age: 20})watch(  user,  (newUser) =&gt; {    console.log(`현재 이름: ${newUser.name}`)  },  { deep: true })다만 깊은 감시는 객체 전체를 재귀적으로 확인하므로 꼭 필요한 경우에 사용하는 것이 좋다. 내부 속성의 변경을 감시하면서 이전 값도 정확하게 비교하고 싶다면 특정 속성만 감시한다.watch(  () =&gt; user.value.age,  (newAge, oldAge) =&gt; {    console.log(`${oldAge}세에서 ${newAge}세로 변경됨.`)  })reactive 객체 자체를 감시하면 내부 변경을 감지할 수 있지만, 중첩 속성 변경에서는 새 값과 이전 값이 같은 객체를 가리킬 수 있다. 이전 값 비교가 필요하면 위처럼 감시할 속성을 함수로 지정하는 편이 명확하다.watchEffect()로 자동 감시하기watchEffect()는 감시할 대상을 따로 적지 않는다. 함수 내부에서 사용하는 반응형 값을 자동으로 추적하고, 값이 변경될 때 다시 실행한다.  컴포넌트가 처음 생성될 때 즉시 한 번 실행된다.  함수 내부에서 읽은 반응형 값만 추적한다.  oldValue를 제공하지 않는다.  이전 값과 비교해야 하면 watch를 사용한다.import { ref, watchEffect } from 'vue'const city = ref('서울')const message = ref('')watchEffect(() =&gt; {  message.value = `${city.value}의 날씨를 조회합니다.`})watch, watchEffect 비교            구분      watch      watchEffect                  감시 대상      직접 지정      함수 안에서 자동 추적              최초 실행      기본적으로 변경 후 실행      생성 직후 즉시 실행              이전 값      제공      제공하지 않음              적합한 상황      특정 값의 변경 전후 비교      여러 의존성을 간단히 동기화      DOM 업데이트가 끝난 뒤 실행해야 하면 watchPostEffect, 값이 바뀌는 즉시 동기적으로 실행해야 하면 watchSyncEffect를 검토할 수 있다. 다만 실행 타이밍을 세밀하게 제어해야 하는 경우에만 사용하자.생명주기 훅 맛보기Composition API에서는 컴포넌트의 생명주기 훅도 함수로 사용한다.&lt;script setup&gt;import { onMounted, onUnmounted } from 'vue'const handleResize = () =&gt; {  console.log(window.innerWidth)}onMounted(() =&gt; {  window.addEventListener('resize', handleResize)})onUnmounted(() =&gt; {  window.removeEventListener('resize', handleResize)})&lt;/script&gt;  onMounted: 컴포넌트가 DOM에 마운트된 뒤 실행한다.  onUpdated: 반응형 상태 변경으로 업데이트된 뒤 실행한다.  onUnmounted: 컴포넌트가 제거된 뒤 실행한다. 이벤트 리스너나 타이머 정리에 사용한다.특히 onMounted에서 등록한 이벤트나 타이머는 onUnmounted에서 정리하는 습관을 들이는 것이 좋다.Composition API를 composable로 확장하기Composition API의 장점은 반복되는 로직을 함수로 분리해 여러 컴포넌트에서 재사용할 수 있다는 점이다. 이런 재사용 함수를 보통 composable이라고 부른다.// useCounter.jsimport { computed, ref } from 'vue'export function useCounter(initialValue = 0) {  const count = ref(initialValue)  const doubleCount = computed(() =&gt; count.value * 2)  const increase = () =&gt; {    count.value++  }  const reset = () =&gt; {    count.value = initialValue  }  return {    count,    doubleCount,    increase,    reset  }}&lt;script setup&gt;import { useCounter } from './useCounter'const { count, doubleCount, increase, reset } = useCounter(10)&lt;/script&gt;&lt;template&gt;  &lt;p&gt;{{ count }} / {{ doubleCount }}&lt;/p&gt;  &lt;button @click=\"increase\"&gt;증가&lt;/button&gt;  &lt;button @click=\"reset\"&gt;초기화&lt;/button&gt;&lt;/template&gt;상태와 함수가 특정 기능을 중심으로 묶여 있으니, 컴포넌트가 커져도 로직을 분리해서 관리할 수 있다. 나중에 useFetch, useTodo, useAuth 같은 형태로 확장하는 흐름이다.최종 정리Composition API는 단순히 함수 몇 개를 새로 사용하는 문법이 아니다. 컴포넌트의 로직을 기능 단위로 구성하고, 필요한 반응형 흐름을 조합하는 방식이다.ref / reactive      ↓반응형 상태 생성      ↓computed로 파생 값 계산      ↓watch 또는 watchEffect로 변경 감지      ↓함수와 템플릿으로 화면 연결      ↓composable로 재사용기억할 것만 다시 적어보자.  ref는 원시 값과 객체·배열을 반응형으로 만든다.  reactive는 객체나 배열 자체를 반응형으로 만든다.  JavaScript에서 ref는 .value, 템플릿에서는 .value를 생략한다.  computed는 상태에서 파생된 값을 계산한다.  watch는 특정 값의 변경 후 작업을 실행한다.  watchEffect는 함수 내부에서 사용하는 반응형 값을 자동으로 추적한다.  onUnmounted에서 이벤트와 타이머를 정리한다.  반복되는 로직은 composable로 분리한다.처음에는 ref, computed, watch만으로도 충분하다. 나머지는 실제로 필요한 상황이 왔을 때 하나씩 살펴보면 된다. 현업에 가서 빨리 써보고 싶다. 일단은 여기까지!"
    },
  
    {
      "title": "Psql JSONB, 행 잠금, 멱등성",
      "url": "/db/postgresql/2026/07/31/psql2/",
      "date": "2026.07.31",
      "categories": "DB, PostgreSQL",
      "tags": "PostgreSQL",
      "excerpt": "JSONBJSONB 객체 생성중첩 JSON 반환jsonb_pretty와 to_jsonb",
      "content": "JSONBJSONB 객체 생성중첩 JSON 반환jsonb_pretty와 to_jsonb행 잠금동시성 문제SELECT FOR UPDATE안전한 재고 처리 순서멱등성(Idempotency)멱등성 개념batch_keyON CONFLICT DO NOTHINGGET DIAGNOSTICS와 ROW_COUNT"
    },
  
    {
      "title": "Psql 함수,프로시져,트리거",
      "url": "/db/postgresql/2026/07/31/psql1/",
      "date": "2026.07.31",
      "categories": "DB, PostgreSQL",
      "tags": "PostgreSQL",
      "excerpt": "함수(Function)",
      "content": "함수(Function)psql에서 함수의 기본 구조는 다음과 같다.함수 기본 구조’'’sqlCREATE OR REPLACE FUNCTION 스키마명.함수명(    매개변수명 데이터타입)RETURNS 반환타입LANGUAGE 언어AS \\(함수 본문\\);‘’’예시 함수 작성예를 들어 고객 등급에 따른 할인율 반환하는 함수는 다음과 같이 작성할 수 있다.’'’sqlCREATE OR REPLACE FUNCTION proc_lab.fn_grade_discount_rate(    p_grade text)RETURNS numericLANGUAGE sqlAS \\(SELECT CASE UPPER(TRIM(p_grade))        WHEN 'BASIC' THEN 0        WHEN 'SILVER' THEN 0.05        WHEN 'GOLD' THEN 0.10        WHEN 'VIP' THEN 0.15        ELSE 0    END;\\);‘’’  함수 호출’'’sql SELECT proc_lab.fn_grade_discount_rate(‘ gold ‘);‘’’SQL 함수와 PL/pgSQL 함수PostgreSQL 함수는 본문을 어떤 언어로 작성하는지에 따라 실행 방식이 달라지는데요, 이번에는 LANGUAGE sql 과 LANGAUGE plpgsql을 사용했습니다.SQL 함수LANGUAGE sql 함수는 일반적으로 하나의 SQL 문장이나 비교적 단순한 조회 로직을 작성할 때 사용합니다’'’sql create or replace function proc_lab.fn_grade_discount_rate(    p_grade text)returns numeric LANGUAGE sql AS \\(SELECT CASE UPPER(TRIM(p_grade))        WHEN 'GOLD' THEN 0.10         ELSE 0     END;\\); ‘’’SQL 함수는 다음과 같은 경우에 적합합니다.  단순 값 계산  조건에 따른 값 반환  테이블 조회 결과 반화  복잡한 데이터 제어가 필요하지 않은 경우PL/pgSQL 함수LANGUAGE plpgsql 함수는 PostgreSQL의 절차형 언어를 사용한다. 변수 선언, 조건문, 예외 처리처럼 일반 SQL으로 표현하기 어려운 로직을 작성할 수 있다.’’’ CREATE OR REPLACE FUNCTION proc_lab.fn_customer_order_summary_json(    p_customer_id bigint)returns jsonb LANGUAGE plpgsql AS $$ DECLARE     v_customer jsonb; Begin     select jsonb_build_object(        ‘customer_id’, c.customer_id,         ‘name’, c.customer_name     )    into v_customer     from proc_lab.customer c    where c.customer_id = p_customer_id;if v_customer is null then    raise exception 'Customer ID % does not exist', p_customer_id; end if; return v_customer;  end;  $$; ’’’PL/pgSQL은 다음과 같은 경우에 적합하다.  DECLARE를 이용한 변수 선언  IF, CASE 를 이용한 조건 처리  여러 SQL 문장 순차 실행  RAISE EXCEPTION을 이용한 예외 처리  조회 결과를 변수에 저장하는 SELECT INTO정리하자면 단순 조회와 계산에는 SQL 함수가 적합하고, 제어 흐름이나 예외 처리가 필요하다면 PL/pgSQL 함수가 적합하다.단일 값·테이블·JSONB 반환함수 속성과 주요 문법프로시저(Procedure)함수와 프로시저의 차이IN, OUT, INOUT 매개변수주문 생성 프로시저RETURNING INTO트랜잭션 기반 테스트트리거(Trigger)트리거의 기본 구조OLD·NEW·TG_OP감사 로그 트리거주문 총액 자동 갱신"
    },
  
    {
      "title": "Vue Intro - ",
      "url": "/frontend/vue/2026/07/30/vue_intro1/",
      "date": "2026.07.30",
      "categories": "Frontend, Vue",
      "tags": "Frontend, Vue.js",
      "excerpt": "Vue.js",
      "content": "Vue.js자자! 오늘은 Vue.js라는 것을 들고 왔다. (프론트엔드 첨해봄 ㅎ)[오늘의 주제]      ↓   [Vue.js]Vue.js는 Front-end JavaScript Framework이다.[Vue.js]   ↓[사용자 인터페이스 구축]즉, 사용자 인터페이스 구축을 위한 프레임워크이다.[Vue.js]   ↓┌──────────────────────┐│ 사용자 인터페이스 구축 │└──────────────────────┘이걸로 무엇을 할 수 있을까?[Vue.js] ├─ 간결한 문법 ├─ 반응형 데이터 바인딩 ├─ 컴포넌트 ├─ 가상 DOM └─ 트랜지션 효과Vue.js를 이용해서 간결한 문법, 반응형 데이터 바인딩, 컴포넌트, 가상 DOM, 트랜지션 효과가 가능하다. (노가다 멈춰!)왜 만들어졌을까?  이유는 간단하다… 그야.. 기존의 것이 불편하니까… (귀찮은 건 질색이야! 딩기리딩딩~~~)[기존 방식의 불편함]          ↓ [더 간결한 개발 방식]Vue.js는 “기존 프레임워크들의 장점만 흡수하여 더 가볍고 쓰기 쉽게 만들자”는 아이디어에서 시작했다.[기존 프레임워크의 장점]            +      [가벼운 사용성]            ↓         [Vue.js]근데 기존의 HTML과 JavaScript와 뭐가 다른 걸까?[HTML + JavaScript]          ↕ 비교       [Vue.js]지금부터 알아보도록 하자.[다음 순서]    ↓[Vue.js의 핵심 특징]Vue.js의 특징 1 - MVVM ArchitectureVue.js의 설계는 MVVM(Model-View-ViewModel) Architecture Pattern에서 영감을 받았지만, MVVM을 엄격하게 따르는 것은 아니다.[Model] ⇄ [ViewModel] ⇄ [View]              ↑       Vue의 반응형 연결이게 뭐냐고요? 잘 따라와 보십쇼.[궁금증]   ↓[MVVM 구조 살펴보기]Then, what is MVVM?MVVM Architecture Pattern은 데이터를 보여주는 View와 데이터를 처리하는 Model 사이에 데이터를 중개하는 ViewModel을 두어 UI를 그리는 부분과 Data 처리 로직을 분리하는 Architecture Pattern이다.[Model: 데이터]       ↕[ViewModel: 연결·상태]       ↕[View: 화면]model  애플리케이션에서 사용하는 데이터와 상태. Vue에서는 보통 &lt;script&gt; 내부에 정의한 반응형 JavaScript 상태가 이에 해당한다.[애플리케이션 데이터]          ↓ [반응형 JavaScript 상태]View  사용자에게 실제로 보여지는 화면 인터페이스와 DOM 구조, 시각적 스타일링을 담당[View]  ├─ 화면 인터페이스  ├─ DOM 구조  └─ 시각적 스타일링ViewModel  View와 Model 사이를 연결해주는 중간 다리. Vue의 반응형 상태·템플릿 연결 로직과 런타임이 ViewModel에 가까운 역할을 수행[View] ⇄ [반응형 상태·템플릿 로직] ⇄ [Model]그러면 어떻게 생긴 걸까?[Model]   ⇅[ViewModel]   ⇅[View]MVVM 구조는 왜 나온 걸까?근데 대충 어떻게 생겼는지 다들 알았을 것이라 생각한다.[Model] → [ViewModel] → [View]그러면 이게 왜 만들어진 것일까?[분리된 역할]      ↓[변경 관리가 쉬운 구조]  기존 브라우저의 Real DOM을 자주 직접 조작하면 렌더링 비용이 커질 수 있다.          DOM: Browser가 HTML을 인식하여 Tree 구조로 만든 Object Model      [HTML]  ↓ 파싱[DOM Tree]  ↓ 렌더링 계산[화면]기존 브라우저에서는 실제 DOM이 수정되면 Browser는 Layout을 다시 계산(Reflow)하고 화면을 다시 색칠하는 Repaint 연산을 수행할 수 있다. 이러면 연속적인 상태 변경으로 인해 노드를 재구성하는 작업이 반복되고, Layout/Paint 연산 부하로 화면이 느려질 수 있다.[DOM 수정]    ↓[Layout/Reflow]    ↓[Paint/Repaint]    ↓[화면 갱신]뭐 예시를 간단하게 들어보자면..[추상적인 설명]        ↓   [생활 속 예시]브라우저의 상태가 변경될 때마다 직접 Real DOM을 연속해서 조작하므로, 만약 10번의 데이터 변화가 일어나면 브라우저가 매번 DOM 변경을 처리하고 그 결과에 따라 여러 번의 스타일 계산·Layout·Paint가 발생할 수 있다.데이터 변화 1 ─┐데이터 변화 2 ─┤데이터 변화 … ─┼→ DOM 처리 → Layout/Paint 가능데이터 변화 10 ─┘실생활에서의 예를 들자면[데이터 변경]      ↓ 비유[마트를 여러 번 방문]감자 10개를 환불하고 고구마 5개를 사야하는데, 감자 환불 하러 10번 마트가고 고구마 사러 마트 5번 가는 느낌이랄까? 그렇게 예를 들 수 있을 거 같다.[감자 환불 10번] + [고구마 구매 5번]                 ↓          [마트 방문 반복]  Vue.js는 Virtual DOM과 렌더러를 통해 필요한 DOM 변경만 적용한다.  Virtual DOM: 실제 브라우저의 DOM과 동기화되는 메모리상의 UI 표현[상태 변화]    ↓[Virtual DOM 비교]    ↓[필요한 Real DOM만 업데이트]아무튼 이런 문제를 줄이기 위해서 Vue.js에서는 Virtual DOM과 렌더러로 변경 사항을 비교한 뒤 필요한 DOM 업데이트를 적용한다.[이전 Virtual DOM] ⇄ [새 Virtual DOM]          ↓ 차이 계산    [필요한 DOM 업데이트]10번 변경할 것을 여러 번의 상태 변경으로 모아서 한 번의 업데이트 사이클에 처리할 수 있게 해준다.[상태 변경 1][상태 변경 2] ─┐[상태 변경 …] ─┼→ [업데이트 사이클 1회][상태 변경 10] ┘개발자가 직접 DOM을 일일이 제어하는 번거로움 없이, 상태 변화만 선언해도 Vue가 반응형 의존성을 추적하고 변경된 Virtual DOM을 이전 것과 비교해 필요한 DOM 업데이트를 계산한다.[상태 선언]    ↓[반응형 의존성 추적]    ↓[Virtual DOM 비교]    ↓[필요한 DOM 업데이트 계산]Batch 처리를 통한 렌더링 횟수 최적화를 통해 Vue는 한 업데이트 사이클 안에 여러 번 데이터가 바뀌어도 변경 사항을 모아서 실제 DOM에 한 번 반영할 수 있다.[여러 상태 변경]        ↓ Batch[한 번의 업데이트 사이클]        ↓   [Real DOM 반영][상태 변경]    ↓[Vue 반응성 + Virtual DOM]    ↓[필요한 화면만 갱신]Vue.js 특징 2 - Two-Way Data BindingVue.js에서 v-model을 사용하면 양방향 데이터 바인딩 (Two-Way Data Binding)을 구현할 수 있다. “Model(JavaScript Data)이 바뀌면 View(화면)가 바뀌고, 반대로 View(화면 입력)가 바뀌면 Model도 동시에 바뀐다”는 것을 의미한다.[Model: JavaScript Data]          ⇅ [v-model 양방향 연결]          ⇅      [View: 입력 화면]Vue.js 특징 3 - Component Based Architecture웹페이지를 통째로 만들지 않는다![큰 웹페이지 하나]        ↓ 분리[여러 개의 컴포넌트]이제는 조각조각 하나씩 만들고 각각을 조립하는 시스템을 채택한다.[Header] + [Content] + [Footer]              ↓ 조립          [웹페이지]  이 방식을 Component(독립 요소) Based Architecture라고 한다.[Component Based Architecture]              ↓ [독립적인 요소를 조립해 화면 구성]세 가지 특징을 가진다.[Component] ├─ Encapsulation ├─ Reusability └─ Tree Structure  Encapsulation : Single-File Component(SFC)로 작성하면 컴포넌트의 HTML(구조), JavaScript(로직), CSS(스타일)를 하나의 파일(.vue) 안에 응집시켜 놓는다.[Component.vue] ├─ &lt;template&gt;  구조 ├─ &lt;script&gt;    로직 └─ &lt;style&gt;     스타일  Reusability: 잘 만든 Component는 여러 곳에서 다시 가져다 쓸 수 있어 중복 코드가 제거된다. (재탕이 최고!)       [재사용]          ↑[Component] ─┬→ [화면 A]             └→ [화면 B]  Tree Structure: Component들은 부모-자식 관계를 가지며 부모는 자식에게 데이터를 내려주고(Props Down), 자식은 부모에게 상태 변경을 알려준다(Event Up = Emit).        [Parent]        ↙      ↘ [Child A]   [Child B]    ↑ Props Down / Event Up ↑Vue.js 특징 4 - Rendering근데 이러면 화면은 어떻게 표시되는 걸까?[Vue 상태·템플릿]        ↓ 렌더링      [화면]HTML은 조상님이 렌더해주시는 걸까?[HTML]  ?[조상님 렌더링?]그럴 리는 없겠지…?[조상님]   ✕[렌더링 담당 아님]아쉽게도 조상님 대신 콤퓨타가 렌더해준다..[Vue + Browser + JavaScript]             ↓          [렌더링]화면을 구성하는 대표적인 방식은 2가지이다.          [렌더링 방식]             ↙   ↘          [CSR] [SSR]  CSR(Client-Side-Rendering)          브라우저(Client)가 JavaScript를 실행하여 화면을 직접 그린다(rendering).        SSR(Server-Side-Rendering)          서버(Server)에서 Vue 컴포넌트를 HTML로 렌더링해 브라우저에 내려주는 방식이다. 데이터는 서버에서 미리 주입할 수도 있고, Vue의 SSR API나 Nuxt.js 같은 프레임워크를 이용해서 구현한다.      [CSR] 브라우저에서 JavaScript 실행 → 화면 렌더링[SSR] 서버에서 HTML 렌더링       → 브라우저 전달Vue.js로 구현하는 SPA (Single Page Application)자자 아직 할 것이 많으니 다음으로 넘어가자.[Vue 특징 1~4]        ↓[SPA 방식 살펴보기]Vue.js로 구현할 수 있는 SPA(Single Page Application)에 대해서 알아보자?[Vue.js]    ↓ 구현[SPA]기존 : MPA기존에는 사용자가 메뉴를 클릭할 때마다 새 HTML을 새로 요청해서 렌더했다.[메뉴 클릭]    ↓[새 HTML 요청]    ↓[전체 페이지 렌더링]그래서 페이지 전환마다 HTML과 리소스를 다시 요청할 수 있어 네트워크 비용과 처리 시간이 늘어날 수 있었다.[페이지 전환]      ↓[HTML·리소스 재요청 가능]      ↓[네트워크·처리 비용 증가 가능]신규 : SPA다행히도! Vue.js로 SPA (Single Page Application)를 구현하면 HTML 문서를 처음에 불러온 뒤, 클라이언트 측 JavaScript가 필요한 데이터와 화면만 업데이트하는 방식으로 변경을 처리할 수 있다.[HTML 문서 최초 로드]          ↓[클라이언트 JavaScript]          ↓ [변경된 화면만 업데이트]어떻게 구현? 그리고 뭐가 장단점일까?구현은 초기 로딩 시 하나의 HTML과 초기 화면에 필요한 JavaScript를 로딩하는 방식으로 이루어진다.[초기 로딩] ├─ HTML └─ 초기 화면에 필요한 JavaScript이제 뭔가 변경이 필요하다면 클라이언트에서 JS로 필요한 부분만 변경(부분 렌더링)하는 방식으로 이루어진다.[변경 요청]    ↓[클라이언트 JS]    ↓[필요한 부분만 변경]데이터 전송도 필요한 데이터만 주고받고, 클라이언트의 JS로 필요한 부분만 변경(부분 렌더링)한다.[서버] ⇄ [필요한 데이터]              ↓        [클라이언트 JS]              ↓       [부분 렌더링]그래서 여러 가지 장점을 제공한다.[SPA] ├─ 빠른 페이지 전환 ├─ 네트워크 효율성 └─ 역할 분리  페이지 전환 시 전체 문서를 다시 로드하지 않아 빠른 사용자 경험을 제공할 수 있다.  네트워크 효율성 : 한 번 로딩된 리소스 (CSS, JS 등)를 재사용하고 필요한 데이터만 요청할 수 있어 전체적인 네트워크 트래픽을 줄일 수 있다. (엄마가 끓여주신 사골국으로 온갖 요리를 해먹는 것을 생각해보자, 본인은 일주일동안 사골로 사골라면, 사골된장찌개…부터 시작해서 약 10가지 요리를 해봤다..OTL)  프론트/백엔드 분리: 프론트엔드는 Vue 화면과 로직을 담당하고, 백엔드는 API로 데이터를 제공하는 식으로 역할을 나눌 수 있어 개발 팀 간 협업과 서버 구성이 명확해질 수 있다.[Vue 프론트엔드] ⇄ [API 서버]   화면·로직          데이터 제공근데 이 방식 장점만 있는 것은 아니다.[SPA 장점]    +[SPA 단점]    ↓[상황에 맞는 선택]여러 가지 문제가 있다.[주의할 점] ├─ 초기 로딩 ├─ 새로고침 └─ SEO  초기 로딩이 느려질 수 있다.          첫 로딩 시 초기 화면에 필요한 JavaScript를 내려받고 실행해야 해서 초기에 느리게 로딩될 수 있다.        새로고침 시 전체 앱이 다시 로딩된다.          이로 인해 상태가 초기화될 수 있다.        JS 기반 렌더링만 사용하는 경우 SEO에 불리할 수 있다.          로봇이 들어와도 JavaScript 실행 전에는 알맹이를 못 볼 수 있다.      그래서 검색 노출에 불리할 수 있다.        별도 기술 조합이 필요할 수 있다. 예를 들어 Vue.js + REST API (ex: Spring Boot)를 함께 구성해야 한다.[SPA 단점] ├─ 초기 JavaScript 로딩·실행 ├─ 새로고침 시 앱 재로딩 └─ CSR 환경에서 SEO 불리할 수 있음Vue.js로 SPA를 만들 때 자주 쓰는 추가 기술 요소  Vite: Frontend Build Tool로 개발자가 작성한 수많은 .vue, .js, .css 파일들을 개발 중에는 빠르게 제공하고, 배포 시에는 브라우저가 읽을 수 있는 최적화된 정적 파일로 변환·번들링하며 개발 서버도 띄워주는 빌드 도구이다.  Vue Router: 브라우저의 URL 주소와 Vue 컴포넌트를 연결해 주는 공식 라우팅 라이브러리  Pinia: 전역 상태 관리 라이브러리로 필요한 컴포넌트가 접근할 수 있는 중앙 집중식 데이터 저장소(Store)를 메모리 상에 개설한다.  Axios: 백엔드 서버(API 서버)와 JSON 등을 주고받는 HTTP 통신 창구.[Vite]   ↓ 빌드[Vue 앱 정적 파일]   ↓ 라우팅·상태·통신[Vue Router] + [Pinia] + [Axios]SPA의 장점  압도적인 사용자 경험(UX)  네트워크 효율성  프론트 / 백엔드 분리[SPA 장점] ├─ 사용자 경험 ├─ 네트워크 효율성 └─ 프론트 / 백엔드 분리SPA 단점  초기 로딩 속도: 초기 화면에 필요한 JavaScript가 한꺼번에 다운로드·실행되면 첫 접속 시 대기 시간이 발생할 수 있다. 코드 분할을 적용하면 필요한 코드만 먼저 불러올 수도 있다.  SEO 취약: 순수 CSR에서는 검색 로봇이 JavaScript를 실행하기 전 알맹이가 적은 index.html을 먼저 보게 될 수 있어 검색 노출에 불리할 수 있다.[초기 JS 다운로드·실행]          ↓   [첫 화면 대기 시간][SSR/SSG 또는 코드 분할]          ↓   [초기 부담 완화 가능]"
    },
  
    {
      "title": "[번외] Vue 개념 점검",
      "url": "/frontend/vue/2026/07/30/vue_intro2/",
      "date": "2026.07.30",
      "categories": "Frontend, Vue",
      "tags": "Vue",
      "excerpt": "Vue에서 반드시 알아야 하는 JavaScriptVue를 시작하기 전에 진짜~ 기본적인 JavaScript를 알아보고 가자.전부 외울 필요는 없고, 아래 문법을 읽고 간단하게 사용할 수 있으면 된다.  객체와 배열  함수와 화살표 함수  구조 분해와 스프레드 문법  map, filter, find, reduce  import와 export  async와 await변수, 객체, 배열const name = 'Lee'let count = 0const user = {  id: 1,  name: 'Lee',  active: true}console.log(user.name)console.log(user.active)const users = [  { id: 1, name: 'Lee', active: true },  { id: 2, name: 'Kim', active: false }]함수와 구조 분해const add = (a, b) =&gt; {  return a + b}const { name, age } = {  name: 'Lee',  age: 25}배열 함수와 자주 쓰는 문법const names = users.map((user) =&gt; user.name)const activeUsers = users.filter((user) =&gt; user.active)const firstUser = users.find((user) =&gt; user.id === 1)const newUser = {  ...users[0],  active: false}const message = isLoggedIn ? '환영합니다' : '로그인하세요'map은 배열을 변환하고, filter는 조건에 맞는 항목만 남기며, find는 조건에 맞는 첫 번째 항목을 찾는다. 상품 총합처럼 여러 값을 하나로 합칠 때는 reduce를 사용한다.비동기 처리const loadUsers = async () =&gt; {  const response = await fetch('/api/users')  const data = await response.json()  return data}Vue 코드를 읽는 방법Vue 코드는 먼저 상태를 찾고, 그 상태를 바꾸는 함수와 템플릿의 연결을 확인하면 된다.&lt;script setup&gt;import { computed, ref } from 'vue'const products = ref([  { id: 1, name: '키보드', price: 50000 },  { id: 2, name: '마우스', price: 30000 }])const selectedProduct = ref(null)const totalPrice = computed(() =&gt; {  return products.value.reduce((sum, product) =&gt; {    return sum + product.price  }, 0)})const selectProduct = (product) =&gt; {  selectedProduct.value = product}&lt;/script&gt;&lt;template&gt;  &lt;div v-for=\"product in products\" :key=\"product.id\"&gt;    &lt;button @click=\"selectProduct(product)\"&gt;      {{ product.name }}    &lt;/button&gt;  &lt;/div&gt;  &lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt;  &lt;p v-if=\"selectedProduct\"&gt;    선택 상품: {{ selectedProduct.name }}  &lt;/p&gt;&lt;/template&gt;읽는 순서는 다음과 같다.  어떤 반응형 상태가 있는가? → products, selectedProduct  어떤 계산된 값이 있는가? → totalPrice  어떤 함수가 상태를 바꾸는가? → selectProduct  템플릿에서 무엇을 반복하는가? → products  어떤 이벤트가 함수를 호출하는가? → @click  어떤 조건에서 화면이 나타나는가? → v-if=\"selectedProduct\"결국 Vue 코드는 상태 → 함수 → 템플릿 연결 순서로 분석하면 된다.기본 개념1. 일반 변수와 ref일반 변수도 값을 변경할 수 있지만, Vue가 그 변경을 반응형 상태로 추적하지는 않는다.let count = 0count++Vue가 값을 추적하고 화면까지 갱신하게 하려면 ref를 사용한다.import { ref } from 'vue'const count = ref(0)count.value++정리하면 다음과 같다.            구분      값 변경      Vue의 변경 추적      화면 갱신                  일반 변수      가능      하지 않음      보장하지 않음              ref      가능      함      반응형으로 갱신      2. JavaScript에서는 .value, 템플릿에서는 생략ref(0)은 내부적으로 값을 감싸는 반응형 객체를 만든다.const count = ref(0)// JavaScript에서는console.log(count.value)count.value++템플릿에서는 Vue가 .value를 자동으로 처리하므로 생략할 수 있다.&lt;p&gt;{{ count }}&lt;/p&gt;이것을 자동 ref 언래핑이라고 한다.3. v-bind:src와 :src둘은 같은 문법이다. :는 v-bind:의 줄임말이다.&lt;img v-bind:src=\"imageUrl\" alt=\"프로필\" /&gt;&lt;img :src=\"imageUrl\" alt=\"프로필\" /&gt;4. @click=\"increase\"와 @click=\"increase()\"인자가 없는 단순한 이벤트라면 함수 이름을 바로 전달하는 방식이 읽기 좋다.&lt;button @click=\"increase\"&gt;증가&lt;/button&gt;const increase = () =&gt; {  count.value++}인자를 전달해야 할 때는 괄호를 사용한다.&lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;함수 이름만 전달하면 Vue가 이벤트 객체를 함수에 전달할 수도 있다.&lt;button @click=\"handleClick\"&gt;클릭&lt;/button&gt;const handleClick = (event) =&gt; {  console.log(event)}5. v-if와 v-show둘 다 조건에 따라 화면을 보이거나 숨기지만 동작이 다르다.&lt;p v-if=\"isVisible\"&gt;내용&lt;/p&gt;&lt;p v-show=\"isVisible\"&gt;내용&lt;/p&gt;  v-if: 조건에 따라 DOM 요소를 생성하거나 제거한다.  v-show: DOM 요소를 유지하고 CSS의 display로 보이거나 숨긴다.v-if는 조건 변경 비용이 있지만 처음부터 렌더링하지 않을 수 있다. v-show는 자주 보였다 숨겨지는 요소에 적합하다.6. v-for와 keyv-for가 만든 반복 변수는 해당 요소와 자식 요소 안에서만 사용할 수 있다.&lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;  &lt;span&gt;{{ city.name }}: {{ city.temp }}℃&lt;/span&gt;  &lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;&lt;/div&gt;city는 반복문 밖에서는 존재하지 않는다.&lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;  {{ city.name }}&lt;/div&gt;&lt;!-- 오류: city는 v-for 바깥에서 사용할 수 없음 --&gt;&lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;key는 각 항목의 고유한 정체성을 Vue에 알려준다. 목록의 순서가 바뀌거나 항목이 삭제될 때 Vue가 어떤 DOM을 재사용하고 제거할지 정확하게 판단할 수 있다.&lt;div v-for=\"user in users\" :key=\"user.id\"&gt;  {{ user.name }}&lt;/div&gt;7. computed와 일반 함수상태에서 새로운 값을 계산하면 computed, 사용자 행동을 처리하면 일반 함수를 사용한다.const totalPrice = computed(() =&gt; {  return products.value.reduce((sum, product) =&gt; {    return sum + product.price  }, 0)})const save = () =&gt; {  alert('저장되었습니다.')}&lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt;&lt;button @click=\"save\"&gt;저장&lt;/button&gt;computed는 계산된 값이므로 템플릿에서 함수처럼 호출하지 않는다.&lt;!-- 잘못된 사용 --&gt;&lt;button @click=\"totalPrice(products)\"&gt;총가격&lt;/button&gt;기억법은 간단하다.  증가, 삭제, 선택, 저장 → 일반 함수  총합, 개수, 평균, 필터 결과 → computed8. v-model은 입력 요소에 사용v-model은 사용자가 값을 입력하거나 선택하는 요소에 연결한다.&lt;input v-model=\"name\" /&gt;&lt;textarea v-model=\"content\"&gt;&lt;/textarea&gt;&lt;select v-model=\"selectedCity\"&gt;  &lt;option value=\"서울\"&gt;서울&lt;/option&gt;&lt;/select&gt;p는 값을 입력받는 요소가 아니라 결과를 보여주는 요소다.&lt;!-- 잘못된 사용 --&gt;&lt;p input v-model=\"name\"&gt;{{ name }}&lt;/p&gt;&lt;!-- 올바른 사용 --&gt;&lt;input v-model=\"name\" /&gt;&lt;p&gt;{{ name }}&lt;/p&gt;입력과 출력의 흐름은 다음과 같다.사용자가 input에 입력        ↓v-model이 name 상태를 변경        ↓실제로는 name.value가 변경        ↓{{ name }}을 사용하는 화면이 갱신9. 부모와 자식 컴포넌트 통신부모에서 자식으로는 props, 자식에서 부모로는 emit을 사용한다.&lt;!-- 부모 컴포넌트 --&gt;&lt;UserCard :name=\"userName\" @select=\"handleSelect\" /&gt;&lt;!-- 자식 컴포넌트 --&gt;&lt;script setup&gt;defineProps({  name: String})const emit = defineEmits(['select'])const handleClick = () =&gt; {  emit('select', 10)}&lt;/script&gt;&lt;template&gt;  &lt;p&gt;{{ name }}&lt;/p&gt;  &lt;button @click=\"handleClick\"&gt;선택&lt;/button&gt;&lt;/template&gt;흐름은 다음과 같다.부모 → 자식: props자식 → 부모: emit10. 버튼 클릭부터 화면 변경까지&lt;script setup&gt;import { ref } from 'vue'const count = ref(0)const increase = () =&gt; {  count.value++}&lt;/script&gt;&lt;template&gt;  &lt;p&gt;{{ count }}&lt;/p&gt;  &lt;button @click=\"increase\"&gt;증가&lt;/button&gt;&lt;/template&gt;사용자가 버튼을 클릭        ↓@click이 increase 실행        ↓count.value 변경        ↓Vue가 변경을 감지        ↓{{ count }}를 사용하는 화면 갱신코드 따라치기: 작은 관리 화면 만들기앞에서 배운 ref, computed, v-model, v-if, v-for, 이벤트를 한 번에 연습해보자.&lt;script setup&gt;import { computed, ref } from 'vue'// 1. 카운터const count = ref(0)const increase = () =&gt; {  count.value++}const decrease = () =&gt; {  count.value--}const reset = () =&gt; {  count.value = 0}// 2. 이름 입력const name = ref('')// 3. 도시 목록const cities = ref([  { id: 1, name: '서울', temp: 28 },  { id: 2, name: '수원', temp: 27 },  { id: 3, name: '부산', temp: 26 }])const selectedCity = ref(null)const selectCity = (city) =&gt; {  selectedCity.value = city}// 4. 상품 목록const products = ref([  { id: 1, name: '키보드', price: 50000 },  { id: 2, name: '마우스', price: 30000 },  { id: 3, name: '모니터', price: 250000 }])const totalPrice = computed(() =&gt; {  return products.value.reduce((sum, product) =&gt; {    return sum + product.price  }, 0)})// 5. 할 일 목록const newTodo = ref('')const todos = ref([  { id: 1, text: 'Vue 공부하기' }])const addTodo = () =&gt; {  const text = newTodo.value.trim()  if (text === '') {    return  }  todos.value.push({    id: Date.now(),    text  })  newTodo.value = ''}const deleteTodo = (todoId) =&gt; {  todos.value = todos.value.filter((todo) =&gt; {    return todo.id !== todoId  })}const todoCount = computed(() =&gt; {  return todos.value.length})&lt;/script&gt;&lt;template&gt;  &lt;main&gt;    &lt;section&gt;      &lt;h2&gt;1. 카운터&lt;/h2&gt;      &lt;p&gt;현재 숫자: {{ count }}&lt;/p&gt;      &lt;button @click=\"increase\"&gt;증가&lt;/button&gt;      &lt;button @click=\"decrease\"&gt;감소&lt;/button&gt;      &lt;button @click=\"reset\"&gt;초기화&lt;/button&gt;      &lt;p v-if=\"count &gt; 0\"&gt;양수입니다.&lt;/p&gt;      &lt;p v-else-if=\"count === 0\"&gt;0입니다.&lt;/p&gt;      &lt;p v-else&gt;음수입니다.&lt;/p&gt;    &lt;/section&gt;    &lt;section&gt;      &lt;h2&gt;2. 이름 입력&lt;/h2&gt;      &lt;input v-model=\"name\" placeholder=\"이름을 입력하세요\" /&gt;      &lt;p v-if=\"name.trim() === ''\"&gt;이름을 입력하세요.&lt;/p&gt;      &lt;p v-else&gt;안녕하세요, {{ name }}님!&lt;/p&gt;    &lt;/section&gt;    &lt;section&gt;      &lt;h2&gt;3. 도시 목록&lt;/h2&gt;      &lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;        &lt;span&gt;{{ city.name }}: {{ city.temp }}℃&lt;/span&gt;        &lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;      &lt;/div&gt;      &lt;p v-if=\"selectedCity\"&gt;        선택된 도시: {{ selectedCity.name }}      &lt;/p&gt;      &lt;p v-else&gt;선택된 도시가 없습니다.&lt;/p&gt;    &lt;/section&gt;    &lt;section&gt;      &lt;h2&gt;4. 상품 목록&lt;/h2&gt;      &lt;div v-for=\"product in products\" :key=\"product.id\"&gt;        {{ product.name }}: {{ product.price }}원      &lt;/div&gt;      &lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt;    &lt;/section&gt;    &lt;section&gt;      &lt;h2&gt;5. 할 일 목록&lt;/h2&gt;      &lt;input        v-model=\"newTodo\"        placeholder=\"할 일을 입력하세요\"        @keyup.enter=\"addTodo\"      /&gt;      &lt;button @click=\"addTodo\"&gt;추가&lt;/button&gt;      &lt;p&gt;현재 할 일: {{ todoCount }}개&lt;/p&gt;      &lt;p v-if=\"todos.length === 0\"&gt;등록된 할 일이 없습니다.&lt;/p&gt;      &lt;ul v-else&gt;        &lt;li v-for=\"todo in todos\" :key=\"todo.id\"&gt;          {{ todo.text }}          &lt;button @click=\"deleteTodo(todo.id)\"&gt;삭제&lt;/button&gt;        &lt;/li&gt;      &lt;/ul&gt;    &lt;/section&gt;  &lt;/main&gt;&lt;/template&gt;자주 발생하는 문법 오류1. ref 값을 .value 없이 변경const count = ref(0)// 잘못된 사용const increase = () =&gt; {  count++}// 올바른 사용const increase = () =&gt; {  count.value++}2. 화살표 함수 문법 오류// 잘못된 사용const handleClick() = () =&gt; {  alert('클릭')}// 올바른 사용 1const handleClick = () =&gt; {  alert('클릭')}// 올바른 사용 2function handleClick() {  alert('클릭')}화살표 함수와 일반 함수 문법을 섞으면 안 된다.3. 배열 객체 사이의 쉼표 누락// 잘못된 사용const cities = ref([  { id: 1, name: '서울' }  { id: 2, name: '부산' }])// 올바른 사용const cities = ref([  { id: 1, name: '서울' },  { id: 2, name: '부산' }])4. v-for 변수의 범위 착각&lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;  {{ city.name }}&lt;/div&gt;&lt;!-- city는 이 위치에 없음 --&gt;&lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;반복 중인 항목을 사용하려면 버튼도 반복 범위 안에 넣어야 한다.&lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;  {{ city.name }}  &lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;&lt;/div&gt;5. ref import와 .value 누락&lt;script setup&gt;import { ref } from 'vue'const selectedCity = ref(null)const selectCity = (city) =&gt; {  selectedCity.value = city}&lt;/script&gt;ref를 사용하면 import가 필요하고, JavaScript에서 값을 변경할 때는 .value를 사용한다.6. HTML 부모·자식 구조 오류목록 전체를 버튼 안에 넣기보다, 입력·버튼·목록을 자연스러운 형제로 구성한다.&lt;!-- 잘못된 구조 --&gt;&lt;button @click=\"addTodo\"&gt;  &lt;ul&gt;    &lt;li&gt;Vue 공부&lt;/li&gt;  &lt;/ul&gt;&lt;/button&gt;&lt;!-- 올바른 구조 --&gt;&lt;section&gt;  &lt;input v-model=\"newTodo\" /&gt;  &lt;button @click=\"addTodo\"&gt;추가&lt;/button&gt;  &lt;ul&gt;    &lt;li v-for=\"todo in todos\" :key=\"todo.id\"&gt;      {{ todo.text }}      &lt;button @click=\"deleteTodo(todo.id)\"&gt;삭제&lt;/button&gt;    &lt;/li&gt;  &lt;/ul&gt;&lt;/section&gt;최종 점검아래 질문에 답할 수 있으면 Vue 기초 흐름은 잘 잡힌 것이다.  ref 값은 JavaScript에서 어떻게 읽고 변경하는가? → .value  템플릿에서 ref의 .value를 생략할 수 있는 이유는? → 자동 ref 언래핑  이벤트가 함수를 실행하고 함수가 상태를 변경하는 흐름을 설명할 수 있는가?  v-for로 만든 변수는 어느 범위에서 사용할 수 있는가? → 반복 요소와 자식 요소 안  computed와 일반 함수의 차이는 무엇인가? → 계산된 값과 사용자 행동 처리  v-model은 어떤 요소에 사용하는가? → 값을 입력하거나 선택하는 요소  부모에서 자식으로, 자식에서 부모로 데이터를 전달할 때 무엇을 사용하는가? → props, emitref 선언  ↓템플릿과 v-model·이벤트 연결  ↓함수가 상태 변경  ↓computed가 필요한 값을 계산  ↓Vue가 화면을 갱신여기까지 이해했으면 이제부터는 문법 자체보다, 어떤 상태를 만들고 어떤 흐름으로 화면에 연결할지 생각하면 된다.",
      "content": "Vue에서 반드시 알아야 하는 JavaScriptVue를 시작하기 전에 진짜~ 기본적인 JavaScript를 알아보고 가자.전부 외울 필요는 없고, 아래 문법을 읽고 간단하게 사용할 수 있으면 된다.  객체와 배열  함수와 화살표 함수  구조 분해와 스프레드 문법  map, filter, find, reduce  import와 export  async와 await변수, 객체, 배열const name = 'Lee'let count = 0const user = {  id: 1,  name: 'Lee',  active: true}console.log(user.name)console.log(user.active)const users = [  { id: 1, name: 'Lee', active: true },  { id: 2, name: 'Kim', active: false }]함수와 구조 분해const add = (a, b) =&gt; {  return a + b}const { name, age } = {  name: 'Lee',  age: 25}배열 함수와 자주 쓰는 문법const names = users.map((user) =&gt; user.name)const activeUsers = users.filter((user) =&gt; user.active)const firstUser = users.find((user) =&gt; user.id === 1)const newUser = {  ...users[0],  active: false}const message = isLoggedIn ? '환영합니다' : '로그인하세요'map은 배열을 변환하고, filter는 조건에 맞는 항목만 남기며, find는 조건에 맞는 첫 번째 항목을 찾는다. 상품 총합처럼 여러 값을 하나로 합칠 때는 reduce를 사용한다.비동기 처리const loadUsers = async () =&gt; {  const response = await fetch('/api/users')  const data = await response.json()  return data}Vue 코드를 읽는 방법Vue 코드는 먼저 상태를 찾고, 그 상태를 바꾸는 함수와 템플릿의 연결을 확인하면 된다.&lt;script setup&gt;import { computed, ref } from 'vue'const products = ref([  { id: 1, name: '키보드', price: 50000 },  { id: 2, name: '마우스', price: 30000 }])const selectedProduct = ref(null)const totalPrice = computed(() =&gt; {  return products.value.reduce((sum, product) =&gt; {    return sum + product.price  }, 0)})const selectProduct = (product) =&gt; {  selectedProduct.value = product}&lt;/script&gt;&lt;template&gt;  &lt;div v-for=\"product in products\" :key=\"product.id\"&gt;    &lt;button @click=\"selectProduct(product)\"&gt;      {{ product.name }}    &lt;/button&gt;  &lt;/div&gt;  &lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt;  &lt;p v-if=\"selectedProduct\"&gt;    선택 상품: {{ selectedProduct.name }}  &lt;/p&gt;&lt;/template&gt;읽는 순서는 다음과 같다.  어떤 반응형 상태가 있는가? → products, selectedProduct  어떤 계산된 값이 있는가? → totalPrice  어떤 함수가 상태를 바꾸는가? → selectProduct  템플릿에서 무엇을 반복하는가? → products  어떤 이벤트가 함수를 호출하는가? → @click  어떤 조건에서 화면이 나타나는가? → v-if=\"selectedProduct\"결국 Vue 코드는 상태 → 함수 → 템플릿 연결 순서로 분석하면 된다.기본 개념1. 일반 변수와 ref일반 변수도 값을 변경할 수 있지만, Vue가 그 변경을 반응형 상태로 추적하지는 않는다.let count = 0count++Vue가 값을 추적하고 화면까지 갱신하게 하려면 ref를 사용한다.import { ref } from 'vue'const count = ref(0)count.value++정리하면 다음과 같다.            구분      값 변경      Vue의 변경 추적      화면 갱신                  일반 변수      가능      하지 않음      보장하지 않음              ref      가능      함      반응형으로 갱신      2. JavaScript에서는 .value, 템플릿에서는 생략ref(0)은 내부적으로 값을 감싸는 반응형 객체를 만든다.const count = ref(0)// JavaScript에서는console.log(count.value)count.value++템플릿에서는 Vue가 .value를 자동으로 처리하므로 생략할 수 있다.&lt;p&gt;{{ count }}&lt;/p&gt;이것을 자동 ref 언래핑이라고 한다.3. v-bind:src와 :src둘은 같은 문법이다. :는 v-bind:의 줄임말이다.&lt;img v-bind:src=\"imageUrl\" alt=\"프로필\" /&gt;&lt;img :src=\"imageUrl\" alt=\"프로필\" /&gt;4. @click=\"increase\"와 @click=\"increase()\"인자가 없는 단순한 이벤트라면 함수 이름을 바로 전달하는 방식이 읽기 좋다.&lt;button @click=\"increase\"&gt;증가&lt;/button&gt;const increase = () =&gt; {  count.value++}인자를 전달해야 할 때는 괄호를 사용한다.&lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;함수 이름만 전달하면 Vue가 이벤트 객체를 함수에 전달할 수도 있다.&lt;button @click=\"handleClick\"&gt;클릭&lt;/button&gt;const handleClick = (event) =&gt; {  console.log(event)}5. v-if와 v-show둘 다 조건에 따라 화면을 보이거나 숨기지만 동작이 다르다.&lt;p v-if=\"isVisible\"&gt;내용&lt;/p&gt;&lt;p v-show=\"isVisible\"&gt;내용&lt;/p&gt;  v-if: 조건에 따라 DOM 요소를 생성하거나 제거한다.  v-show: DOM 요소를 유지하고 CSS의 display로 보이거나 숨긴다.v-if는 조건 변경 비용이 있지만 처음부터 렌더링하지 않을 수 있다. v-show는 자주 보였다 숨겨지는 요소에 적합하다.6. v-for와 keyv-for가 만든 반복 변수는 해당 요소와 자식 요소 안에서만 사용할 수 있다.&lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;  &lt;span&gt;{{ city.name }}: {{ city.temp }}℃&lt;/span&gt;  &lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;&lt;/div&gt;city는 반복문 밖에서는 존재하지 않는다.&lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;  {{ city.name }}&lt;/div&gt;&lt;!-- 오류: city는 v-for 바깥에서 사용할 수 없음 --&gt;&lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;key는 각 항목의 고유한 정체성을 Vue에 알려준다. 목록의 순서가 바뀌거나 항목이 삭제될 때 Vue가 어떤 DOM을 재사용하고 제거할지 정확하게 판단할 수 있다.&lt;div v-for=\"user in users\" :key=\"user.id\"&gt;  {{ user.name }}&lt;/div&gt;7. computed와 일반 함수상태에서 새로운 값을 계산하면 computed, 사용자 행동을 처리하면 일반 함수를 사용한다.const totalPrice = computed(() =&gt; {  return products.value.reduce((sum, product) =&gt; {    return sum + product.price  }, 0)})const save = () =&gt; {  alert('저장되었습니다.')}&lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt;&lt;button @click=\"save\"&gt;저장&lt;/button&gt;computed는 계산된 값이므로 템플릿에서 함수처럼 호출하지 않는다.&lt;!-- 잘못된 사용 --&gt;&lt;button @click=\"totalPrice(products)\"&gt;총가격&lt;/button&gt;기억법은 간단하다.  증가, 삭제, 선택, 저장 → 일반 함수  총합, 개수, 평균, 필터 결과 → computed8. v-model은 입력 요소에 사용v-model은 사용자가 값을 입력하거나 선택하는 요소에 연결한다.&lt;input v-model=\"name\" /&gt;&lt;textarea v-model=\"content\"&gt;&lt;/textarea&gt;&lt;select v-model=\"selectedCity\"&gt;  &lt;option value=\"서울\"&gt;서울&lt;/option&gt;&lt;/select&gt;p는 값을 입력받는 요소가 아니라 결과를 보여주는 요소다.&lt;!-- 잘못된 사용 --&gt;&lt;p input v-model=\"name\"&gt;{{ name }}&lt;/p&gt;&lt;!-- 올바른 사용 --&gt;&lt;input v-model=\"name\" /&gt;&lt;p&gt;{{ name }}&lt;/p&gt;입력과 출력의 흐름은 다음과 같다.사용자가 input에 입력        ↓v-model이 name 상태를 변경        ↓실제로는 name.value가 변경        ↓{{ name }}을 사용하는 화면이 갱신9. 부모와 자식 컴포넌트 통신부모에서 자식으로는 props, 자식에서 부모로는 emit을 사용한다.&lt;!-- 부모 컴포넌트 --&gt;&lt;UserCard :name=\"userName\" @select=\"handleSelect\" /&gt;&lt;!-- 자식 컴포넌트 --&gt;&lt;script setup&gt;defineProps({  name: String})const emit = defineEmits(['select'])const handleClick = () =&gt; {  emit('select', 10)}&lt;/script&gt;&lt;template&gt;  &lt;p&gt;{{ name }}&lt;/p&gt;  &lt;button @click=\"handleClick\"&gt;선택&lt;/button&gt;&lt;/template&gt;흐름은 다음과 같다.부모 → 자식: props자식 → 부모: emit10. 버튼 클릭부터 화면 변경까지&lt;script setup&gt;import { ref } from 'vue'const count = ref(0)const increase = () =&gt; {  count.value++}&lt;/script&gt;&lt;template&gt;  &lt;p&gt;{{ count }}&lt;/p&gt;  &lt;button @click=\"increase\"&gt;증가&lt;/button&gt;&lt;/template&gt;사용자가 버튼을 클릭        ↓@click이 increase 실행        ↓count.value 변경        ↓Vue가 변경을 감지        ↓{{ count }}를 사용하는 화면 갱신코드 따라치기: 작은 관리 화면 만들기앞에서 배운 ref, computed, v-model, v-if, v-for, 이벤트를 한 번에 연습해보자.&lt;script setup&gt;import { computed, ref } from 'vue'// 1. 카운터const count = ref(0)const increase = () =&gt; {  count.value++}const decrease = () =&gt; {  count.value--}const reset = () =&gt; {  count.value = 0}// 2. 이름 입력const name = ref('')// 3. 도시 목록const cities = ref([  { id: 1, name: '서울', temp: 28 },  { id: 2, name: '수원', temp: 27 },  { id: 3, name: '부산', temp: 26 }])const selectedCity = ref(null)const selectCity = (city) =&gt; {  selectedCity.value = city}// 4. 상품 목록const products = ref([  { id: 1, name: '키보드', price: 50000 },  { id: 2, name: '마우스', price: 30000 },  { id: 3, name: '모니터', price: 250000 }])const totalPrice = computed(() =&gt; {  return products.value.reduce((sum, product) =&gt; {    return sum + product.price  }, 0)})// 5. 할 일 목록const newTodo = ref('')const todos = ref([  { id: 1, text: 'Vue 공부하기' }])const addTodo = () =&gt; {  const text = newTodo.value.trim()  if (text === '') {    return  }  todos.value.push({    id: Date.now(),    text  })  newTodo.value = ''}const deleteTodo = (todoId) =&gt; {  todos.value = todos.value.filter((todo) =&gt; {    return todo.id !== todoId  })}const todoCount = computed(() =&gt; {  return todos.value.length})&lt;/script&gt;&lt;template&gt;  &lt;main&gt;    &lt;section&gt;      &lt;h2&gt;1. 카운터&lt;/h2&gt;      &lt;p&gt;현재 숫자: {{ count }}&lt;/p&gt;      &lt;button @click=\"increase\"&gt;증가&lt;/button&gt;      &lt;button @click=\"decrease\"&gt;감소&lt;/button&gt;      &lt;button @click=\"reset\"&gt;초기화&lt;/button&gt;      &lt;p v-if=\"count &gt; 0\"&gt;양수입니다.&lt;/p&gt;      &lt;p v-else-if=\"count === 0\"&gt;0입니다.&lt;/p&gt;      &lt;p v-else&gt;음수입니다.&lt;/p&gt;    &lt;/section&gt;    &lt;section&gt;      &lt;h2&gt;2. 이름 입력&lt;/h2&gt;      &lt;input v-model=\"name\" placeholder=\"이름을 입력하세요\" /&gt;      &lt;p v-if=\"name.trim() === ''\"&gt;이름을 입력하세요.&lt;/p&gt;      &lt;p v-else&gt;안녕하세요, {{ name }}님!&lt;/p&gt;    &lt;/section&gt;    &lt;section&gt;      &lt;h2&gt;3. 도시 목록&lt;/h2&gt;      &lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;        &lt;span&gt;{{ city.name }}: {{ city.temp }}℃&lt;/span&gt;        &lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;      &lt;/div&gt;      &lt;p v-if=\"selectedCity\"&gt;        선택된 도시: {{ selectedCity.name }}      &lt;/p&gt;      &lt;p v-else&gt;선택된 도시가 없습니다.&lt;/p&gt;    &lt;/section&gt;    &lt;section&gt;      &lt;h2&gt;4. 상품 목록&lt;/h2&gt;      &lt;div v-for=\"product in products\" :key=\"product.id\"&gt;        {{ product.name }}: {{ product.price }}원      &lt;/div&gt;      &lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt;    &lt;/section&gt;    &lt;section&gt;      &lt;h2&gt;5. 할 일 목록&lt;/h2&gt;      &lt;input        v-model=\"newTodo\"        placeholder=\"할 일을 입력하세요\"        @keyup.enter=\"addTodo\"      /&gt;      &lt;button @click=\"addTodo\"&gt;추가&lt;/button&gt;      &lt;p&gt;현재 할 일: {{ todoCount }}개&lt;/p&gt;      &lt;p v-if=\"todos.length === 0\"&gt;등록된 할 일이 없습니다.&lt;/p&gt;      &lt;ul v-else&gt;        &lt;li v-for=\"todo in todos\" :key=\"todo.id\"&gt;          {{ todo.text }}          &lt;button @click=\"deleteTodo(todo.id)\"&gt;삭제&lt;/button&gt;        &lt;/li&gt;      &lt;/ul&gt;    &lt;/section&gt;  &lt;/main&gt;&lt;/template&gt;자주 발생하는 문법 오류1. ref 값을 .value 없이 변경const count = ref(0)// 잘못된 사용const increase = () =&gt; {  count++}// 올바른 사용const increase = () =&gt; {  count.value++}2. 화살표 함수 문법 오류// 잘못된 사용const handleClick() = () =&gt; {  alert('클릭')}// 올바른 사용 1const handleClick = () =&gt; {  alert('클릭')}// 올바른 사용 2function handleClick() {  alert('클릭')}화살표 함수와 일반 함수 문법을 섞으면 안 된다.3. 배열 객체 사이의 쉼표 누락// 잘못된 사용const cities = ref([  { id: 1, name: '서울' }  { id: 2, name: '부산' }])// 올바른 사용const cities = ref([  { id: 1, name: '서울' },  { id: 2, name: '부산' }])4. v-for 변수의 범위 착각&lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;  {{ city.name }}&lt;/div&gt;&lt;!-- city는 이 위치에 없음 --&gt;&lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;반복 중인 항목을 사용하려면 버튼도 반복 범위 안에 넣어야 한다.&lt;div v-for=\"city in cities\" :key=\"city.id\"&gt;  {{ city.name }}  &lt;button @click=\"selectCity(city)\"&gt;선택&lt;/button&gt;&lt;/div&gt;5. ref import와 .value 누락&lt;script setup&gt;import { ref } from 'vue'const selectedCity = ref(null)const selectCity = (city) =&gt; {  selectedCity.value = city}&lt;/script&gt;ref를 사용하면 import가 필요하고, JavaScript에서 값을 변경할 때는 .value를 사용한다.6. HTML 부모·자식 구조 오류목록 전체를 버튼 안에 넣기보다, 입력·버튼·목록을 자연스러운 형제로 구성한다.&lt;!-- 잘못된 구조 --&gt;&lt;button @click=\"addTodo\"&gt;  &lt;ul&gt;    &lt;li&gt;Vue 공부&lt;/li&gt;  &lt;/ul&gt;&lt;/button&gt;&lt;!-- 올바른 구조 --&gt;&lt;section&gt;  &lt;input v-model=\"newTodo\" /&gt;  &lt;button @click=\"addTodo\"&gt;추가&lt;/button&gt;  &lt;ul&gt;    &lt;li v-for=\"todo in todos\" :key=\"todo.id\"&gt;      {{ todo.text }}      &lt;button @click=\"deleteTodo(todo.id)\"&gt;삭제&lt;/button&gt;    &lt;/li&gt;  &lt;/ul&gt;&lt;/section&gt;최종 점검아래 질문에 답할 수 있으면 Vue 기초 흐름은 잘 잡힌 것이다.  ref 값은 JavaScript에서 어떻게 읽고 변경하는가? → .value  템플릿에서 ref의 .value를 생략할 수 있는 이유는? → 자동 ref 언래핑  이벤트가 함수를 실행하고 함수가 상태를 변경하는 흐름을 설명할 수 있는가?  v-for로 만든 변수는 어느 범위에서 사용할 수 있는가? → 반복 요소와 자식 요소 안  computed와 일반 함수의 차이는 무엇인가? → 계산된 값과 사용자 행동 처리  v-model은 어떤 요소에 사용하는가? → 값을 입력하거나 선택하는 요소  부모에서 자식으로, 자식에서 부모로 데이터를 전달할 때 무엇을 사용하는가? → props, emitref 선언  ↓템플릿과 v-model·이벤트 연결  ↓함수가 상태 변경  ↓computed가 필요한 값을 계산  ↓Vue가 화면을 갱신여기까지 이해했으면 이제부터는 문법 자체보다, 어떤 상태를 만들고 어떤 흐름으로 화면에 연결할지 생각하면 된다."
    },
  
    {
      "title": "Deep Learning 테스트",
      "url": "/deep_learning/2026/07/30/deep-learning-test/",
      "date": "2026.07.30",
      "categories": "Deep_Learning",
      "tags": "",
      "excerpt": "가",
      "content": "가"
    },
  
    {
      "title": "CS 테스트",
      "url": "/cs/2026/07/30/cs-test/",
      "date": "2026.07.30",
      "categories": "CS",
      "tags": "",
      "excerpt": "가",
      "content": "가"
    }
  
]
