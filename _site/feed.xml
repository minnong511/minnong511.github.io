<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-08-05T17:49:13+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Minnong’s Study Log</title><subtitle>개발과 AI를 배우고, 실험하고, 이해한 내용을 연결하는 공부 기록</subtitle><author><name>Min Hyeong Lee</name></author><entry><title type="html">Vue DOM</title><link href="http://localhost:4000/frontend/vue/2026/08/03/vue-dom/" rel="alternate" type="text/html" title="Vue DOM" /><published>2026-08-03T09:00:00+09:00</published><updated>2026-08-03T09:00:00+09:00</updated><id>http://localhost:4000/frontend/vue/2026/08/03/vue-dom</id><content type="html" xml:base="http://localhost:4000/frontend/vue/2026/08/03/vue-dom/"><![CDATA[<h1 id="domdocument-object-model이란">DOM(Document Object Model)이란?</h1>

<h2 id="정의">정의</h2>

<blockquote>
  <p><strong>HTML 문서를 JavaScript가 조작할 수 있도록 브라우저가 메모리에 만든 객체 구조(Tree 구조)</strong></p>
</blockquote>

<p>입니다.</p>

<p>쉽게 말하면:</p>

<p><strong>HTML 파일 → 브라우저가 읽음 → DOM이라는 객체 형태로 변환 → JavaScript가 조작</strong></p>

<p>하는 과정입니다.</p>

<hr />

<p>예를 들어 HTML이 있습니다.</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;html&gt;</span>
  <span class="nt">&lt;body&gt;</span>
    <span class="nt">&lt;h1&gt;</span>Hello<span class="nt">&lt;/h1&gt;</span>
    <span class="nt">&lt;p&gt;</span>Vue 공부<span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<p>브라우저는 이것을:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Document
│
└── html
    │
    └── body
        │
        ├── h1
        │    └── "Hello"
        │
        └── p
             └── "Vue 공부"
</code></pre></div></div>

<p>이런 <strong>트리 구조 객체</strong>로 만듭니다.</p>

<p>이게 DOM입니다.</p>

<hr />

<h1 id="왜-dom이-필요한가">왜 DOM이 필요한가?</h1>

<p>HTML은 원래 정적인 문서입니다.</p>

<p>예:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;h1&gt;</span>Hello<span class="nt">&lt;/h1&gt;</span>
</code></pre></div></div>

<p>브라우저가 화면에 보여주는 것까지는 가능합니다.</p>

<p>하지만:</p>

<p>“버튼 클릭하면 글자가 바뀌게 해줘”</p>

<p>같은 동적인 동작은 HTML만으로 어렵습니다.</p>

<p>그래서 JavaScript가 DOM을 조작합니다.</p>

<hr />

<h1 id="javascript로-dom-조작-예시">JavaScript로 DOM 조작 예시</h1>

<p>HTML:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;h1</span> <span class="na">id=</span><span class="s">"title"</span><span class="nt">&gt;</span>
Hello
<span class="nt">&lt;/h1&gt;</span>
</code></pre></div></div>

<p>JavaScript:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">title</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nx">querySelector</span><span class="p">(</span><span class="dl">"</span><span class="s2">#title</span><span class="dl">"</span><span class="p">)</span>

<span class="nx">title</span><span class="p">.</span><span class="nx">innerText</span> <span class="o">=</span> <span class="dl">"</span><span class="s2">Hello Vue</span><span class="dl">"</span>
</code></pre></div></div>

<p>결과:</p>

<p>화면:</p>

<p>변경 전:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hello
</code></pre></div></div>

<p>변경 후:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hello Vue
</code></pre></div></div>

<hr />

<p>흐름:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTML
 ↓
브라우저
 ↓
DOM 생성
 ↓
JavaScript가 DOM 수정
 ↓
화면 변경
</code></pre></div></div>

<hr />

<h1 id="dom은-그냥-html인가">DOM은 그냥 HTML인가?</h1>

<p>아닙니다.</p>

<p>중요한 차이:</p>

<p>HTML:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;h1&gt;</span>Hello<span class="nt">&lt;/h1&gt;</span>
</code></pre></div></div>

<p>문자열(텍스트 문서)</p>

<hr />

<p>DOM:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
 <span class="nl">tag</span><span class="p">:</span><span class="dl">"</span><span class="s2">h1</span><span class="dl">"</span><span class="p">,</span>
 <span class="nx">children</span><span class="p">:[</span>
   <span class="dl">"</span><span class="s2">Hello</span><span class="dl">"</span>
 <span class="p">]</span>
<span class="p">}</span>
</code></pre></div></div>

<p>객체 구조</p>

<hr />

<p>즉:</p>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>HTML</th>
      <th>DOM</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>형태</td>
      <td>문서</td>
      <td>객체</td>
    </tr>
    <tr>
      <td>목적</td>
      <td>내용 표현</td>
      <td>프로그램 조작</td>
    </tr>
    <tr>
      <td>관리 주체</td>
      <td>파일</td>
      <td>브라우저 메모리</td>
    </tr>
  </tbody>
</table>

<hr />

<h1 id="그런데-dom-조작이-왜-문제가-됨">그런데 DOM 조작이 왜 문제가 됨?</h1>

<p>여기서 Vue가 등장합니다.</p>

<p>예:</p>

<p>게시판에 글 10,000개가 있다고 생각해봅시다.</p>

<p>사용자가 좋아요 버튼 클릭:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>좋아요 10
↓
좋아요 11
</code></pre></div></div>

<p>변경해야 합니다.</p>

<p>순수 JavaScript:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">element</span><span class="p">.</span><span class="nx">innerText</span> <span class="o">=</span> <span class="mi">11</span>
</code></pre></div></div>

<p>직접 DOM 수정</p>

<hr />

<p>문제:</p>

<p>개발자가 직접:</p>

<ul>
  <li>어떤 DOM을 바꿀지</li>
  <li>언제 바꿀지</li>
  <li>어떤 순서로 바꿀지</li>
</ul>

<p>관리해야 합니다.</p>

<p>프로젝트가 커지면:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>데이터
 ↓
DOM
 ↓
다른 DOM
 ↓
이벤트
 ↓
다른 데이터
</code></pre></div></div>

<p>관리가 복잡해집니다.</p>

<hr />

<h1 id="vue의-방식">Vue의 방식</h1>

<p>Vue는:</p>

<p>“DOM을 직접 만지지 말고 데이터만 변경해”</p>

<p>라는 철학입니다.</p>

<p>예:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
</code></pre></div></div>

<p>개발자는:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>데이터 변경
</code></pre></div></div>

<p>만 합니다.</p>

<p>Vue가:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>데이터 변경
 ↓
반응성 시스템 감지
 ↓
Virtual DOM 생성
 ↓
기존 DOM과 비교
 ↓
필요한 부분만 수정
</code></pre></div></div>

<p>합니다.</p>

<hr />

<h1 id="real-dom-vs-virtual-dom">Real DOM vs Virtual DOM</h1>

<h2 id="real-dom">Real DOM</h2>

<p>실제 브라우저 화면을 구성하는 DOM</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Browser Memory

&lt;html&gt;
 └── body
      └── div
           └── button
</code></pre></div></div>

<hr />

<h2 id="virtual-dom">Virtual DOM</h2>

<p>Vue가 메모리에 따로 가지고 있는 가상 DOM</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JavaScript Memory

{
 type:"button",
 text:"Click"
}
</code></pre></div></div>

<hr />

<p>변경 발생:</p>

<p>기존:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Virtual DOM A

&lt;button&gt;
Count:0
&lt;/button&gt;
</code></pre></div></div>

<p>변경:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Virtual DOM B

&lt;button&gt;
Count:1
&lt;/button&gt;
</code></pre></div></div>

<p>Vue가 비교:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A와 B 비교

Count 부분만 변경 필요
</code></pre></div></div>

<p>실제 DOM:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Count:1
</code></pre></div></div>

<hr />

<h1 id="vue-관점에서-dom-이해-흐름">Vue 관점에서 DOM 이해 흐름</h1>

<p>가장 중요한 흐름:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자 행동
      ↓
데이터 변경
      ↓
ref/reactive
      ↓
Vue 반응성 시스템
      ↓
Virtual DOM 비교
      ↓
Real DOM 업데이트
      ↓
화면 변경
</code></pre></div></div>

<hr />

<p>지금까지 배운 개념을 연결하면:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ref/reactive
      ↓
상태(state)
      ↓
변경 감지
      ↓
Virtual DOM
      ↓
DOM 업데이트
      ↓
화면
</code></pre></div></div>

<p>입니다.</p>

<p>다음으로 <strong>“브라우저 렌더링 과정(Critical Rendering Path) → DOM 생성 → Vue 렌더링 과정”</strong>을 공부해야함</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="Vue.js" /><category term="Composition API" /><category term="ref" /><category term="reactive" /><category term="computed" /><category term="watch" /><summary type="html"><![CDATA[DOM(Document Object Model)이란?]]></summary></entry><entry><title type="html">Axios 정리</title><link href="http://localhost:4000/frontend/vue/2026/08/03/vue-axios/" rel="alternate" type="text/html" title="Axios 정리" /><published>2026-08-03T00:00:00+09:00</published><updated>2026-08-03T00:00:00+09:00</updated><id>http://localhost:4000/frontend/vue/2026/08/03/vue-axios</id><content type="html" xml:base="http://localhost:4000/frontend/vue/2026/08/03/vue-axios/"><![CDATA[<h1 id="axios-정리">Axios 정리</h1>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="Vue.js" /><category term="Axios" /><summary type="html"><![CDATA[Axios 정리]]></summary></entry><entry><title type="html">Vue 컴포넌트의 구조와 생명주기</title><link href="http://localhost:4000/frontend/vue/2026/08/03/vue-component/" rel="alternate" type="text/html" title="Vue 컴포넌트의 구조와 생명주기" /><published>2026-08-03T00:00:00+09:00</published><updated>2026-08-03T00:00:00+09:00</updated><id>http://localhost:4000/frontend/vue/2026/08/03/vue-component</id><content type="html" xml:base="http://localhost:4000/frontend/vue/2026/08/03/vue-component/"><![CDATA[<h1 id="vue-컴포넌트의-구조와-생명주기">Vue 컴포넌트의 구조와 생명주기</h1>

<h2 id="1-component란">1. Component란?</h2>

<p>소프트웨어 공학에서 컴포넌트(Component)는 독립적인 기능을 수행하고, 필요할 때 다른 부품으로 교체하거나 다른 프로그램과 연결할 수 있는 표준화된 소프트웨어 모듈을 말한다.</p>

<p>컴포넌트의 핵심 특징은 다음과 같다.</p>

<ul>
  <li><strong>독립성(Independence)</strong>: 기능과 책임이 하나의 단위로 분리된다.</li>
  <li><strong>교체 가능성(Replaceability)</strong>: 같은 역할을 수행하는 다른 컴포넌트로 교체할 수 있다.</li>
  <li><strong>재사용성(Reusability)</strong>: 여러 화면이나 프로젝트에서 반복해서 사용할 수 있다.</li>
</ul>

<h2 id="2-vue-component란">2. Vue Component란?</h2>

<p>Vue 컴포넌트는 웹 페이지를 구성하는 독립적이고 재사용 가능한 블록이다.</p>

<p>Vue에서는 HTML, CSS, JavaScript를 하나의 <code class="language-plaintext highlighter-rouge">.vue</code> 파일에 모아 작성하는 방식을 SFC(Single-File Component)라고 한다.</p>

<p>하나의 애플리케이션은 여러 컴포넌트를 조립해 완성하며, 컴포넌트들은 트리 구조로 연결된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>App
├── Header
├── Main
│   ├── UserProfile
│   └── ProductList
└── Footer
</code></pre></div></div>

<h2 id="3-component-hierarchy">3. Component Hierarchy</h2>

<p>컴포넌트 계층 구조에서 자주 사용하는 관계는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>관계</th>
      <th>의미</th>
      <th>데이터 전달 방식</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Parent-Child</td>
      <td>부모가 자식을 포함하는 관계</td>
      <td>부모에서 자식으로 <code class="language-plaintext highlighter-rouge">props</code>, 자식에서 부모로 <code class="language-plaintext highlighter-rouge">emit</code></td>
    </tr>
    <tr>
      <td>Sibling</td>
      <td>같은 부모 아래에 나란히 있는 형제 관계</td>
      <td>부모를 통해 상태를 공유하거나 공통 저장소 사용</td>
    </tr>
    <tr>
      <td>Ancestor-Descendant</td>
      <td>자식의 자식까지 이어지는 다층 계층 관계</td>
      <td><code class="language-plaintext highlighter-rouge">provide</code>와 <code class="language-plaintext highlighter-rouge">inject</code>, 공통 저장소 등을 사용</td>
    </tr>
  </tbody>
</table>

<p>부모와 자식은 서로 독립된 컴포넌트다. 자식은 부모의 상태를 직접 수정하지 않고 <code class="language-plaintext highlighter-rouge">props</code>로 전달받으며, 부모에게 필요한 이벤트는 <code class="language-plaintext highlighter-rouge">emit</code>으로 알린다.</p>

<h2 id="4-component-지역-등록">4. Component 지역 등록</h2>

<p>지역 등록은 특정 부모 컴포넌트에서만 자식 컴포넌트를 사용하는 방식이다.</p>

<ul>
  <li>부모 컴포넌트에서 자식 컴포넌트를 import한다.</li>
  <li>등록한 자식 컴포넌트는 <code class="language-plaintext highlighter-rouge">&lt;template&gt;</code> 영역에서 컴포넌트 태그처럼 사용할 수 있다.</li>
  <li>컴포넌트 이름은 일반적으로 PascalCase를 사용한다.</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;script setup&gt;</code>에서는 import한 컴포넌트가 자동으로 등록된다.</li>
</ul>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="nx">BaseButton</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./components/BaseButton.vue</span><span class="dl">'</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;div</span> <span class="na">class=</span><span class="s">"box"</span><span class="nt">&gt;</span>
    <span class="nt">&lt;h3&gt;</span>컴포넌트 조립 테스트<span class="nt">&lt;/h3&gt;</span>
    <span class="nt">&lt;hr&gt;</span>
    <span class="nt">&lt;BaseButton</span> <span class="nt">/&gt;</span>
  <span class="nt">&lt;/div&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<h2 id="5-component-전역-등록">5. Component 전역 등록</h2>

<p>전역 등록한 컴포넌트는 Vue 애플리케이션의 여러 컴포넌트에서 별도의 import 없이 사용할 수 있다.</p>

<p>전역 등록은 보통 <code class="language-plaintext highlighter-rouge">main.js</code>에서 수행한다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">createApp</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>
<span class="k">import</span> <span class="nx">App</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./App.vue</span><span class="dl">'</span>
<span class="k">import</span> <span class="nx">BaseButton</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./components/BaseButton.vue</span><span class="dl">'</span>
<span class="k">import</span> <span class="nx">BaseInput</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./components/BaseInput.vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">app</span> <span class="o">=</span> <span class="nx">createApp</span><span class="p">(</span><span class="nx">App</span><span class="p">)</span>

<span class="nx">app</span><span class="p">.</span><span class="nx">component</span><span class="p">(</span><span class="dl">'</span><span class="s1">BaseButton</span><span class="dl">'</span><span class="p">,</span> <span class="nx">BaseButton</span><span class="p">)</span>
<span class="nx">app</span><span class="p">.</span><span class="nx">component</span><span class="p">(</span><span class="dl">'</span><span class="s1">BaseInput</span><span class="dl">'</span><span class="p">,</span> <span class="nx">BaseInput</span><span class="p">)</span>

<span class="nx">app</span><span class="p">.</span><span class="nx">mount</span><span class="p">(</span><span class="dl">'</span><span class="s1">#app</span><span class="dl">'</span><span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">app.component()</code>의 첫 번째 인자는 템플릿에서 사용할 컴포넌트 이름이고, 두 번째 인자는 import한 컴포넌트 변수다.</p>

<h2 id="6-vue-주요-내장-함수">6. Vue 주요 내장 함수</h2>

<p>Vue는 애플리케이션 생성, 반응형 상태 관리, 계산과 감시, 컴포넌트 조립 등을 위한 함수를 제공한다.</p>

<table>
  <thead>
    <tr>
      <th>카테고리</th>
      <th>주요 함수</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>애플리케이션(Application)</td>
      <td><code class="language-plaintext highlighter-rouge">createApp</code>, <code class="language-plaintext highlighter-rouge">createSSRApp</code>, <code class="language-plaintext highlighter-rouge">app.*</code>, <code class="language-plaintext highlighter-rouge">app.config.*</code></td>
    </tr>
    <tr>
      <td>반응형 상태(Reactive State)</td>
      <td><code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">reactive</code>, <code class="language-plaintext highlighter-rouge">readonly</code>, <code class="language-plaintext highlighter-rouge">shallowRef</code>, <code class="language-plaintext highlighter-rouge">shallowReactive</code>, <code class="language-plaintext highlighter-rouge">shallowReadonly</code>, <code class="language-plaintext highlighter-rouge">toRef</code>, <code class="language-plaintext highlighter-rouge">toRefs</code>, <code class="language-plaintext highlighter-rouge">customRef</code>, <code class="language-plaintext highlighter-rouge">unref</code>, <code class="language-plaintext highlighter-rouge">toRaw</code>, <code class="language-plaintext highlighter-rouge">markRaw</code>, <code class="language-plaintext highlighter-rouge">isRef</code>, <code class="language-plaintext highlighter-rouge">isReactive</code>, <code class="language-plaintext highlighter-rouge">isReadonly</code></td>
    </tr>
    <tr>
      <td>계산 및 감시(Computed &amp; Watchers)</td>
      <td><code class="language-plaintext highlighter-rouge">computed</code>, <code class="language-plaintext highlighter-rouge">watch</code>, <code class="language-plaintext highlighter-rouge">watchEffect</code></td>
    </tr>
    <tr>
      <td>라이프사이클 훅(Lifecycle Hooks)</td>
      <td><code class="language-plaintext highlighter-rouge">onBeforeMount</code>, <code class="language-plaintext highlighter-rouge">onMounted</code>, <code class="language-plaintext highlighter-rouge">onBeforeUpdate</code>, <code class="language-plaintext highlighter-rouge">onUpdated</code>, <code class="language-plaintext highlighter-rouge">onBeforeUnmount</code>, <code class="language-plaintext highlighter-rouge">onUnmounted</code>, <code class="language-plaintext highlighter-rouge">onActivated</code>, <code class="language-plaintext highlighter-rouge">onDeactivated</code>, <code class="language-plaintext highlighter-rouge">onErrorCaptured</code>, <code class="language-plaintext highlighter-rouge">onRenderTracked</code>, <code class="language-plaintext highlighter-rouge">onRenderTriggered</code></td>
    </tr>
    <tr>
      <td>컴포넌트 구성(Component Composition)</td>
      <td><code class="language-plaintext highlighter-rouge">defineComponent</code>, <code class="language-plaintext highlighter-rouge">defineProps</code>, <code class="language-plaintext highlighter-rouge">defineEmits</code>, <code class="language-plaintext highlighter-rouge">useAttrs</code>, <code class="language-plaintext highlighter-rouge">defineExpose</code>, <code class="language-plaintext highlighter-rouge">useSlots</code>, <code class="language-plaintext highlighter-rouge">withDefaults</code>, <code class="language-plaintext highlighter-rouge">getCurrentInstance</code></td>
    </tr>
    <tr>
      <td>렌더링 제어(Rendering &amp; DOM)</td>
      <td><code class="language-plaintext highlighter-rouge">h</code>, <code class="language-plaintext highlighter-rouge">resolveComponent</code>, <code class="language-plaintext highlighter-rouge">withDirectives</code>, <code class="language-plaintext highlighter-rouge">renderList</code>, <code class="language-plaintext highlighter-rouge">renderSlot</code>, <code class="language-plaintext highlighter-rouge">mergeProps</code>, <code class="language-plaintext highlighter-rouge">nextTick</code>, <code class="language-plaintext highlighter-rouge">useCssModule</code>, <code class="language-plaintext highlighter-rouge">useCssVars</code></td>
    </tr>
    <tr>
      <td>의존성 주입(Dependency Injection)</td>
      <td><code class="language-plaintext highlighter-rouge">provide</code>, <code class="language-plaintext highlighter-rouge">inject</code>, <code class="language-plaintext highlighter-rouge">hasInjectionContext</code></td>
    </tr>
  </tbody>
</table>

<h2 id="7-component-lifecycle">7. Component Lifecycle</h2>

<p>컴포넌트 생명주기는 컴포넌트가 생성되고, 화면에 연결되고, 갱신된 뒤 제거되는 흐름이다.</p>

<table>
  <thead>
    <tr>
      <th>단계</th>
      <th>컴포넌트의 상태</th>
      <th>주요 작업과 훅</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>생성 및 설정(Creation &amp; Setup)</td>
      <td>컴포넌트 인스턴스가 만들어지고 JavaScript에서 사용할 준비를 하는 단계</td>
      <td><code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">reactive</code>, <code class="language-plaintext highlighter-rouge">computed</code>, <code class="language-plaintext highlighter-rouge">watch</code> 등을 초기화한다.</td>
    </tr>
    <tr>
      <td>부착(Mounting)</td>
      <td>가상으로 구성한 화면을 실제 DOM에 연결하는 단계</td>
      <td>DOM에 접근하거나 초기 데이터를 요청한다. <code class="language-plaintext highlighter-rouge">onMounted</code>를 사용한다.</td>
    </tr>
    <tr>
      <td>갱신(Updating)</td>
      <td>반응형 데이터가 변경되어 화면을 다시 그리는 단계</td>
      <td>업데이트된 DOM을 확인하거나 크기와 스크롤 위치를 다시 계산한다. <code class="language-plaintext highlighter-rouge">onUpdated</code>를 사용한다.</td>
    </tr>
    <tr>
      <td>소멸(Unmounting)</td>
      <td><code class="language-plaintext highlighter-rouge">v-if="false"</code> 등의 조건으로 컴포넌트가 화면에서 제거되는 단계</td>
      <td>타이머와 이벤트 리스너를 정리해 메모리 누수를 방지한다. <code class="language-plaintext highlighter-rouge">onUnmounted</code>를 사용한다.</td>
    </tr>
  </tbody>
</table>

<p>라이프사이클 훅을 사용한 간단한 예시는 다음과 같다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">onMounted</span><span class="p">,</span> <span class="nx">onUnmounted</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">handleResize</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nb">window</span><span class="p">.</span><span class="nx">innerWidth</span><span class="p">)</span>
<span class="p">}</span>

<span class="nx">onMounted</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nb">window</span><span class="p">.</span><span class="nx">addEventListener</span><span class="p">(</span><span class="dl">'</span><span class="s1">resize</span><span class="dl">'</span><span class="p">,</span> <span class="nx">handleResize</span><span class="p">)</span>
<span class="p">})</span>

<span class="nx">onUnmounted</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nb">window</span><span class="p">.</span><span class="nx">removeEventListener</span><span class="p">(</span><span class="dl">'</span><span class="s1">resize</span><span class="dl">'</span><span class="p">,</span> <span class="nx">handleResize</span><span class="p">)</span>
<span class="p">})</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">onMounted</code>에서 등록한 이벤트 리스너는 <code class="language-plaintext highlighter-rouge">onUnmounted</code>에서 제거해야 컴포넌트가 사라진 뒤에도 불필요한 동작이 남지 않는다.</p>

<h1 id="props--emits">Props &amp; Emits</h1>

<p>Component 연동</p>
<ul>
  <li>Vue 3의 모든 컴포넌트 연동은”데이터는 위에서 아래로 물려주고, 이벤트는 아래에서 위로 쏘아 올린다”는 구조를 따른다.</li>
</ul>

<p>분류 🔽 Props (하행선) 🔼 Emits (상행선)
개념 정의 부모가 자식에게 주는 반응형 데이터 값 자식이 부모에게 보고하는 이벤트
흐름 방향 부모 → 자식 (위에서 아래로) 자식 → 부모 (아래에서 위로)
데이터 권한 읽기 전용. 자식은 수정 불가 부모에게 변경 요청 및 값 전달 가능
Compiler Macro defineProps({ … }) defineEmits([ … ])
Parent Binding 자식 태그 속성에 콜론(:)으로 주입 자식 태그 이벤트에 골뱅이(@)로 청취</p>

<p>Compiler Macro란 Runtime 시점이 아닌 Build 시점에 Vue Compiler가 코드를 변환하는 특수 예약어로 defineProps(),
defineEmits(), defineExpose() 같은 함수들은 <script setup="">에서만 사용이 가능</script></p>

<p>defineProps() - 속성 정의</p>

<p>자식 컴포넌트 내부에서”부모가 넘겨줄 데이터(속성)의 이름과 규격”을 선언하는 Vue 3 내장 컴파일러 매크로 함수</p>
<ul>
  <li>Compiler Macro이기 때문에 상단에 import할 필요 없이 <script setup=""> 안에서 즉시 호출할 수 있다</script></li>
  <li>간단한 배열 표기법과, 강력한 객체 표기법이 있다</li>
</ul>

<p>[배열 형식]
const props = defineProps([‘title’, ‘count’])</p>

<p>[객체 형식– 기본값 지정]
defineProps({
// 1. 타입만 간단히 지정하는 경우
title: String,
// 2. 필수 값과 기본값까지 꼼꼼하게 지정하는 경우
likes: {
type: Number,
required: true // 부모가 이 값을 안 넘기면 에러발생.
},
status: {
type: String,
default: ＇대기 중＇ // 부모가 값을 안 주면 이 값이 기본으로 세팅.
}
})</p>

<p>▪ <template> 에서 사용하기
• 정의된 변수를 그대로 사용하면 된다.</template></p>
<template>
<h1></h1>
<p>좋아요: </p>
</template>
<p>▪ <script setup=""> 에서 사용하기
• defineProps가 반환하는 객체를 변수(보통 props라는 이름)에 받아서.점 문법으로 접근해야 한다.
<script setup="">
// 변수에 결과를 할당합니다.
const props = defineProps({
title: String,
likes: Number
})
// 내부 함수에서 쓸 때는 props.을 앞에 꼭 붙여야 합니다.
const checkPopularity = () =&gt; {
if (props.likes &gt; 100) {
console.log(`${props.title}은 인기 게시글입니다.`)
}
}
</script></script></p>

<p>Readonly
• defineProps로 Parent에서 전달된 값은 읽기 전용(ReadOnly)이다.
• Child Component에서 이 값을 직접 바꾸려고 하면 에러가 발생된다.</p>

<p>const props = defineProps([‘likes’])
const brokenFunction = () =&gt; {
// ❌ 절대 금지! 콘솔에 ReadOnly 에러 발생.
props.likes = 999
}</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="Vue.js" /><category term="Component" /><category term="SFC" /><category term="Lifecycle" /><summary type="html"><![CDATA[소프트웨어 컴포넌트의 개념부터 Vue 컴포넌트의 계층 구조, 지역·전역 등록, 내장 함수, 생명주기까지 정리한다.]]></summary></entry><entry><title type="html">JavaScript ES6 핵심 문법</title><link href="http://localhost:4000/frontend/vue/2026/08/03/vue-es6/" rel="alternate" type="text/html" title="JavaScript ES6 핵심 문법" /><published>2026-08-03T00:00:00+09:00</published><updated>2026-08-03T00:00:00+09:00</updated><id>http://localhost:4000/frontend/vue/2026/08/03/vue-es6</id><content type="html" xml:base="http://localhost:4000/frontend/vue/2026/08/03/vue-es6/"><![CDATA[<h1 id="javascript-es6-핵심-문법">JavaScript ES6 핵심 문법</h1>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="JavaScript" /><category term="ES6" /><category term="Vue.js" /><summary type="html"><![CDATA[JavaScript ES6 핵심 문법]]></summary></entry><entry><title type="html">Vue 라이브러리 정리</title><link href="http://localhost:4000/frontend/vue/2026/08/03/vue-library/" rel="alternate" type="text/html" title="Vue 라이브러리 정리" /><published>2026-08-03T00:00:00+09:00</published><updated>2026-08-03T00:00:00+09:00</updated><id>http://localhost:4000/frontend/vue/2026/08/03/vue-library</id><content type="html" xml:base="http://localhost:4000/frontend/vue/2026/08/03/vue-library/"><![CDATA[<h1 id="vue-라이브러리-정리">Vue 라이브러리 정리</h1>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="Vue.js" /><category term="Library" /><summary type="html"><![CDATA[Vue 라이브러리 정리]]></summary></entry><entry><title type="html">Vue Router Navigation Guard 정리</title><link href="http://localhost:4000/frontend/vue/2026/08/03/vue-router-navigation-guard/" rel="alternate" type="text/html" title="Vue Router Navigation Guard 정리" /><published>2026-08-03T00:00:00+09:00</published><updated>2026-08-03T00:00:00+09:00</updated><id>http://localhost:4000/frontend/vue/2026/08/03/vue-router-navigation-guard</id><content type="html" xml:base="http://localhost:4000/frontend/vue/2026/08/03/vue-router-navigation-guard/"><![CDATA[<h1 id="vue-router-navigation-guard-정리">Vue Router Navigation Guard 정리</h1>

<p>Vue Router의 Navigation Guard</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="Vue.js" /><category term="Vue Router" /><category term="Navigation Guard" /><summary type="html"><![CDATA[Vue Router Navigation Guard 정리]]></summary></entry><entry><title type="html">JavaScript &amp;amp; Vue 기본 단어 정의</title><link href="http://localhost:4000/frontend/vue/2026/08/03/vue-word/" rel="alternate" type="text/html" title="JavaScript &amp;amp; Vue 기본 단어 정의" /><published>2026-08-03T00:00:00+09:00</published><updated>2026-08-03T00:00:00+09:00</updated><id>http://localhost:4000/frontend/vue/2026/08/03/vue-word</id><content type="html" xml:base="http://localhost:4000/frontend/vue/2026/08/03/vue-word/"><![CDATA[<h1 id="javascript-데이터-타입과-vue-반응형-데이터-구조">JavaScript 데이터 타입과 Vue 반응형 데이터 구조</h1>

<h2 id="1-데이터-타입의-큰-분류">1. 데이터 타입의 큰 분류</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>데이터
│
├── 원시값(Primitive Type)
│
└── 참조값(Reference Type)
</code></pre></div></div>

<h2 id="2-원시값primitive-value">2. 원시값(Primitive Value)</h2>

<h3 id="정의">정의</h3>

<p>원시값은 하나의 값 자체를 저장하는 가장 기본적인 데이터 형태다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>string
number
boolean
null
undefined
symbol
bigint
</code></pre></div></div>

<h3 id="예시">예시</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">age</span> <span class="o">=</span> <span class="mi">20</span>
<span class="kd">const</span> <span class="nx">name</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">Kim</span><span class="dl">'</span>
<span class="kd">const</span> <span class="nx">isLogin</span> <span class="o">=</span> <span class="kc">true</span>
</code></pre></div></div>

<h3 id="메모리-구조">메모리 구조</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>age
 |
 ↓
20
</code></pre></div></div>

<p>원시값은 값 자체를 저장한다.</p>

<h3 id="값-복사">값 복사</h3>

<p>원시값을 복사하면 메모리 주소가 아니라 값 자체가 복사된다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">let</span> <span class="nx">a</span> <span class="o">=</span> <span class="mi">10</span>
<span class="kd">let</span> <span class="nx">b</span> <span class="o">=</span> <span class="nx">a</span>

<span class="nx">b</span> <span class="o">=</span> <span class="mi">20</span>

<span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nx">a</span><span class="p">)</span>
<span class="c1">// 10</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a
↓
10

b
↓
10
</code></pre></div></div>

<p>처음에는 같은 값이지만, 값을 복사한 뒤에는 서로 독립적으로 동작한다.</p>

<h2 id="3-객체object">3. 객체(Object)</h2>

<h3 id="정의-1">정의</h3>

<p>객체는 여러 개의 데이터를 하나로 묶어서 저장하는 자료 구조다.</p>

<h3 id="예시-1">예시</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Kim</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">20</span><span class="p">,</span>
  <span class="na">city</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Seoul</span><span class="dl">'</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="구조">구조</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>user
 |
 ↓
객체
 |
 ├── name: Kim
 ├── age: 20
 └── city: Seoul
</code></pre></div></div>

<p>객체는 속성(property)을 가진다.</p>

<h3 id="속성-접근">속성 접근</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">user</span><span class="p">.</span><span class="nx">name</span>
<span class="c1">// 'Kim'</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">user.name</code>은 <code class="language-plaintext highlighter-rouge">user</code> 객체 안에 있는 <code class="language-plaintext highlighter-rouge">name</code> 값을 가져온다.</p>

<h2 id="4-배열array">4. 배열(Array)</h2>

<h3 id="정의-2">정의</h3>

<p>배열은 여러 개의 값을 순서대로 저장하는 객체다.</p>

<p>배열도 객체의 한 종류이지만, 일반적으로 순서가 있는 여러 데이터를 저장할 때 사용한다.</p>

<h3 id="예시-2">예시</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">fruits</span> <span class="o">=</span> <span class="p">[</span>
  <span class="dl">'</span><span class="s1">apple</span><span class="dl">'</span><span class="p">,</span>
  <span class="dl">'</span><span class="s1">banana</span><span class="dl">'</span><span class="p">,</span>
  <span class="dl">'</span><span class="s1">orange</span><span class="dl">'</span>
<span class="p">]</span>
</code></pre></div></div>

<h3 id="구조-1">구조</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fruits
 |
 ↓
Array
 |
 ├── 0: apple
 ├── 1: banana
 └── 2: orange
</code></pre></div></div>

<p>배열은 index(순서)로 접근한다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">fruits</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
<span class="c1">// 'apple'</span>
</code></pre></div></div>

<h2 id="5-객체와-배열의-차이">5. 객체와 배열의 차이</h2>

<p>둘 다 여러 데이터를 저장하지만 목적이 다르다.</p>

<h3 id="객체">객체</h3>

<p>객체는 하나의 대상을 표현할 때 사용한다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Kim</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">20</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 객체는 사용자 1명의 정보를 표현한다.</p>

<h3 id="배열">배열</h3>

<p>배열은 여러 데이터의 목록을 표현할 때 사용한다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">users</span> <span class="o">=</span> <span class="p">[</span>
  <span class="p">{</span>
    <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Kim</span><span class="dl">'</span>
  <span class="p">},</span>
  <span class="p">{</span>
    <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Lee</span><span class="dl">'</span>
  <span class="p">}</span>
<span class="p">]</span>
</code></pre></div></div>

<p>위 배열은 사용자 목록을 표현한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>객체</th>
      <th>배열</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>목적</td>
      <td>하나의 대상 표현</td>
      <td>여러 데이터 목록</td>
    </tr>
    <tr>
      <td>접근</td>
      <td>이름(key)</td>
      <td>순서(index)</td>
    </tr>
    <tr>
      <td>예시</td>
      <td><code class="language-plaintext highlighter-rouge">user</code></td>
      <td><code class="language-plaintext highlighter-rouge">users</code></td>
    </tr>
  </tbody>
</table>

<h2 id="6-객체-안에-객체를-넣을-수-있다">6. 객체 안에 객체를 넣을 수 있다</h2>

<p>객체 안에 다른 객체를 넣는 구조는 실무에서 자주 사용된다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Kim</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">address</span><span class="p">:</span> <span class="p">{</span>
    <span class="na">city</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Seoul</span><span class="dl">'</span><span class="p">,</span>
    <span class="na">zip</span><span class="p">:</span> <span class="dl">'</span><span class="s1">12345</span><span class="dl">'</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="구조-2">구조</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>user
├── name
│
└── address
    |
    ├── city
    └── zip
</code></pre></div></div>

<h3 id="접근">접근</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">user</span><span class="p">.</span><span class="nx">address</span><span class="p">.</span><span class="nx">city</span>
<span class="c1">// 'Seoul'</span>
</code></pre></div></div>

<h2 id="7-배열-안에-객체를-넣을-수-있다">7. 배열 안에 객체를 넣을 수 있다</h2>

<p>Vue에서 자주 볼 수 있는 형태다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">todos</span> <span class="o">=</span> <span class="p">[</span>
  <span class="p">{</span>
    <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span>
    <span class="na">text</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Vue 공부</span><span class="dl">'</span>
  <span class="p">},</span>
  <span class="p">{</span>
    <span class="na">id</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span>
    <span class="na">text</span><span class="p">:</span> <span class="dl">'</span><span class="s1">JavaScript 공부</span><span class="dl">'</span>
  <span class="p">}</span>
<span class="p">]</span>
</code></pre></div></div>

<h3 id="구조-3">구조</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>todos 배열

0
 |
 └── 객체
      ├── id
      └── text

1
 |
 └── 객체
      ├── id
      └── text
</code></pre></div></div>

<h3 id="접근-1">접근</h3>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">todos</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="nx">text</span>
<span class="c1">// 'Vue 공부'</span>
</code></pre></div></div>

<h2 id="8-vue에서-객체와-배열이-중요한-이유">8. Vue에서 객체와 배열이 중요한 이유</h2>

<p>Vue에서는 객체를 <code class="language-plaintext highlighter-rouge">ref</code>로 감싸서 반응형 상태로 만들 수 있다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Kim</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">20</span>
<span class="p">})</span>
</code></pre></div></div>

<h3 id="데이터-구조">데이터 구조</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ref
 |
 ↓
객체
 |
 ├── name
 └── age
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ref</code>로 만든 객체의 값에 접근할 때는 <code class="language-plaintext highlighter-rouge">.value</code>를 사용한다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">user</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">age</span>
</code></pre></div></div>

<h3 id="데이터-접근-흐름">데이터 접근 흐름</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>user
 ↓
ref 객체
 ↓
value
 ↓
일반 객체
 ↓
age
</code></pre></div></div>

<h2 id="9-배열도-같은-원리로-접근한다">9. 배열도 같은 원리로 접근한다</h2>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">todos</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span>
  <span class="p">{</span>
    <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span>
    <span class="na">text</span><span class="p">:</span> <span class="dl">'</span><span class="s1">공부</span><span class="dl">'</span>
  <span class="p">}</span>
<span class="p">])</span>
</code></pre></div></div>

<p>배열 안의 객체에 접근할 때는 <code class="language-plaintext highlighter-rouge">.value</code>와 index를 함께 사용한다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">todos</span><span class="p">.</span><span class="nx">value</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="nx">text</span>
<span class="c1">// '공부'</span>
</code></pre></div></div>

<h3 id="데이터-접근-흐름-1">데이터 접근 흐름</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>todos
 ↓
ref 객체
 ↓
value
 ↓
배열
 ↓
0번째 객체
 ↓
text
</code></pre></div></div>

<h2 id="10-결론">10. 결론</h2>

<p>개발할 때는 데이터가 다음과 같은 구조로 감싸져 있다고 생각하면 된다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">weatherList</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span>
  <span class="p">{</span>
    <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span>
    <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span><span class="p">,</span>
    <span class="na">temp</span><span class="p">:</span> <span class="mi">30</span>
  <span class="p">}</span>
<span class="p">])</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>weatherList
 |
 ↓
ref
 |
 ↓
value
 |
 ↓
Array
 |
 ↓
Object
 |
 ├── id
 ├── name
 └── temp
</code></pre></div></div>

<p>따라서 다음과 같은 방식으로 데이터를 추출할 수 있다.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">weatherList</span><span class="p">.</span><span class="nx">value</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="nx">temp</span>
<span class="c1">// 30</span>
</code></pre></div></div>

<h2 id="11-vue-공부에서-꼭-구분해야-하는-용어">11. Vue 공부에서 꼭 구분해야 하는 용어</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>값(value)</td>
      <td><code class="language-plaintext highlighter-rouge">20</code>, <code class="language-plaintext highlighter-rouge">'hello'</code>, <code class="language-plaintext highlighter-rouge">true</code> 같은 단일 데이터</td>
    </tr>
    <tr>
      <td>변수(variable)</td>
      <td>값을 저장하는 이름</td>
    </tr>
    <tr>
      <td>객체(object)</td>
      <td>key를 사용해 데이터를 저장하는 자료 구조</td>
    </tr>
    <tr>
      <td>속성(property)</td>
      <td>객체 내부에 저장된 데이터</td>
    </tr>
    <tr>
      <td>배열(array)</td>
      <td>데이터를 순서대로 저장하는 자료 구조</td>
    </tr>
    <tr>
      <td>원소(element)</td>
      <td>배열 안에 있는 하나의 데이터</td>
    </tr>
    <tr>
      <td>참조(reference)</td>
      <td>객체의 주소를 가리키는 것</td>
    </tr>
  </tbody>
</table>

<h1 id="1-vuejs-기본-용어">1. Vue.js 기본 용어</h1>

<h2 id="1-component">1) component</h2>

<p>화면을 구성하는 독립적인 UI 부품</p>

<p>WeatherParent.vue
├── SearchBar.vue
├── WeatherCard.vue
└── WeatherForecast.vue</p>

<p>Vue 컴포넌트는 일반적으로 세 부분으로 구성한다.</p>

<script setup="">
// JavaScript 로직
</script>

<template>
  <!-- 화면 구조 -->
</template>

<style scoped="">
/* CSS 디자인 */
</style>

<h2 id="2-template">2) Template</h2>

<p>브라우저에 표시한 HTML 구조를 작성하는 영역</p>

<template>
  <h1></h1>
</template>

<p>는 JavaScript 값을 화면에 출력하는 문자열 보간법</p>

<h1 id="3-directive">3) Directive</h1>

<p>HTML 태그에 Vue 기능을 추가하는 문법, 대부분 V- 시작함.</p>

<p v-if="isVisible">보이는 문장</p>
<p>&lt;button @click=”increase”&gt;증가&lt;/button&gt;
<input v-model="name" /></p>

<p>대표적인 Directive</p>

<table>
  <thead>
    <tr>
      <th>문법</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">v-if</code></td>
      <td>조건에 따라 요소 생성·제거</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">v-show</code></td>
      <td>조건에 따라 CSS로 숨김</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">v-for</code></td>
      <td>배열을 반복 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">v-model</code></td>
      <td>입력값과 상태를 양방향 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">v-bind</code> 또는 <code class="language-plaintext highlighter-rouge">:</code></td>
      <td>속성에 JavaScript 값 연결</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">v-on</code> 또는 <code class="language-plaintext highlighter-rouge">@</code></td>
      <td>이벤트 연결</td>
    </tr>
  </tbody>
</table>

<h1 id="4-reactive-state">4) Reactive State</h1>

<p>값이 변경되었을 때, Vue가 이를 감지하고 화면을 자동으로 다시 갱신하는 상태</p>

<script setup="">
import { ref } from 'vue'

const count = ref(0)
</script>

<template>
  <p>8</p>
</template>

<p>count가 변경되면 화면의 숫자도 자동으로 변경된다.</p>

<h1 id="5-ref">5) ref</h1>

<p>숫자, 문자열, 불리언, 객체, 배열 등을 반응형으로 만드는 함수.</p>

<p>const count = ref(0)
const name = ref(‘민형’)
const user = ref({
  age: 25,
})</p>

<p>JavaScript 영역에서는 .value가 필요하다.</p>

<p>count.value++
user.value.age++</p>

<p>그러나 tempalte에서는 .value를 사용하지 않는다.</p>

<p>8</p>
<p></p>

<h1 id="6-reactive">6) reactive</h1>

<p>주로 객체를 반응형으로 만드는 함수</p>

<p>import { reactive } from ‘vue’</p>

<p>const user = reactive({
  name: ‘민형’,
  age: 25,
})</p>

<p>user.age++</p>

<p>reactive에서는 .value를 사용하지 않습니다.</p>

<p>ref와 reactive 비교</p>

<p>const count = ref(0)
count.value++</p>

<p>const user = reactive({ age: 25 })
user.age++</p>
<h1 id="7-computed">7. Computed</h1>

<p>기존 반응형 상태를 이용해 새로운 계산 결과를 만드는 값.</p>

<p>const price = ref(10000)
const quantity = ref(3)</p>

<p>const totalPrice = computed(() =&gt; {
  return price.value * quantity.value
})</p>

<p>총 가격: 원</p>

<p>특징 :</p>
<ul>
  <li>의존하는 값이 바뀌면 자동으로 다시 계산</li>
  <li>계산결과를 캐싱</li>
  <li>기본적으로 읽기 전용</li>
  <li>화면에 보여줄 파생 값을 만들 때 사용</li>
</ul>

<p>const filteredUsers = computed(() =&gt; {
  return users.value.filter((user) =&gt;
    user.name.includes(searchQuery.value)
  )
})</p>

<h1 id="8-method--function">8) Method / Function</h1>

<ul>
  <li>버튼 클릭이나 특정 동작이 발생했을 떄 실행하는 함수</li>
</ul>

<p>const count = ref(0)</p>

<p>const increase = () =&gt; {
  count.value++
}</p>

<p>&lt;button @click=”increase”&gt;증가&lt;/button&gt;</p>

<p>computed는 값을 계산하고, 함수는 행동을 실행한다고 생각하면 쉽다.</p>

<h1 id="화면-제어-용어">화면 제어 용어</h1>

<h2 id="9-v-if">9) v-if</h2>

<p>조건이 참일 때만 HTML 요소를 생성합니다.</p>
<p v-if="isLoggedIn">로그인되었습니다.</p>
<p v-else="">로그인이 필요합니다.</p>

<p>조건이 거짓이면 실제 DOM에서 요소가 제거됨</p>

<h2 id="10-v-show">10) v-show</h2>

<p>요소는 그대로 두고 CSS의 display: none으로 숨긴다.</p>

<p v-show="isVisible">내용</p>

<p>v-if와 v-show</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th><code class="language-plaintext highlighter-rouge">v-if</code></th>
      <th><code class="language-plaintext highlighter-rouge">v-show</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>거짓일 때</td>
      <td>DOM에서 제거</td>
      <td>CSS로 숨김</td>
    </tr>
    <tr>
      <td>전환 비용</td>
      <td>큼</td>
      <td>작음</td>
    </tr>
    <tr>
      <td>자주 열고 닫기</td>
      <td>부적합</td>
      <td>적합</td>
    </tr>
    <tr>
      <td>처음부터 조건이 중요</td>
      <td>적합</td>
      <td>부적합</td>
    </tr>
  </tbody>
</table>

<p>모달이나 드롭다운처럼 자구 열고 닫는 요소는 v-show가 유리</p>

<h2 id="11-v-for">11) v-for</h2>

<p>배열 데이터를 반복해서 화면에 표시</p>

<p>const cities = ref([
  { id: 1, name: ‘서울’ },
  { id: 2, name: ‘부산’ },
])</p>

<p>&lt;div
  v-for=”city in cities”
  :key=”city.id”</p>
<blockquote>

</blockquote>

<p>&lt;/div&gt;</p>

<h2 id="12-key">12) Key</h2>

<p>Vue가 반복된 각 요소를 구분하도록 제공하는 고유 식별자</p>

<p>&lt;div v-for=”city in cities” :key=”city.id”</p>

<p>일반적으로 DB ID처럼 변하지 않는 값을 사용</p>

<p>:key=”city.id”</p>

<h2 id="13-v-model">13) v-model</h2>

<p>입력창의 값과 반응형 상태를 양방향으로 연결합니다.</p>

<p>const searchQuery = ref(‘’)</p>

<p><input v-model="searchQuery" /></p>
<p> </p>

<h2 id="14-event">14) Event</h2>

<p>클릭, 입력, 제출처럼 사용자가 발생시키는 동작</p>

<p>&lt;button @click=”handleClick”&gt;클릭&lt;/button&gt;</p>

<p>&lt;input @input=”handleInput” /&gt;</p>

<p>&lt;form @submit.prevent=”handleSubmit”&gt;</p>

<p>대표 이벤트 :</p>

<table>
  <thead>
    <tr>
      <th>이벤트</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">click</code></td>
      <td>클릭</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">input</code></td>
      <td>입력값 변경</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">change</code></td>
      <td>입력 확정 또는 선택 변경</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">submit</code></td>
      <td>폼 제출</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">keydown</code></td>
      <td>키보드 누름</td>
    </tr>
  </tbody>
</table>

<h2 id="15-event-modifier">15) Event Modifier</h2>

<p>이벤트 동작을 간단히 제어하는 수식어</p>

<p>&lt;a @click.prevent = “handelLink”&gt; 링크 &lt;/a&gt; 
&lt;button @click.stop = “handleClick”&gt; 버튼 &lt;/button&gt;</p>

<table>
  <thead>
    <tr>
      <th>수식어</th>
      <th>기능</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.prevent</code></td>
      <td>기본 동작 방지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.stop</code></td>
      <td>이벤트 버블링 방지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.once</code></td>
      <td>한 번만 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.self</code></td>
      <td>자신을 직접 클릭한 경우만 실행</td>
    </tr>
  </tbody>
</table>

<h1 id="3-컴포넌트-통신-용어">3. 컴포넌트 통신 용어</h1>

<h2 id="16-props">16) Props</h2>

<p>부모 컴포넌트가 자식 컴포넌트에 데이터를 전달하는 방법</p>

<p>부모</p>

<p>&lt;WeatherCard 
    :city = “selectedCity”
    title = “현재 날씨”
/&gt;</p>

<p>자식</p>

<script setup="">
const props = defineProps({
  city: {
    type: Object,
    required: true,
  },
  title: {
    type: String,
    default: '',
  },
})
</script>

<template>
  <h2></h2>
  <p></p>
</template>

<p>데이터 흐름:</p>

<p>부모  -&gt; props -&gt; 자식</p>

<p>Props는 자식에서 직접 수정하지 않는 것이 원칙</p>

<h2 id="17-emit">17) Emit</h2>

<p>자식 컴포넌트가 부모 컴포넌트에 이벤트나 데이터를 전달하는 방법</p>

<p>자식</p>

<script setup="">
const emit = defineEmits(['select-city'])

const handleClick = () => {
  emit('select-city', '서울')
}
</script>

<template>
  &lt;button @click="handleClick"&gt;서울 선택&lt;/button&gt;
</template>

<p>부모</p>

<p>&lt;WeatherCard @select-city=”handleSelectCity” /&gt;
const handleSelectCity = (cityName) =&gt; {
    console.log(cityName)
}</p>

<p>데이터 흐름:</p>

<p>자식 -&gt; emit -&gt; 부모</p>

<p>정리하면:</p>

<p>부모가 자식에게 데이터 전달: Props
자식이 부모에게 알림 전달: Emit</p>

<h2 id="18-slot">18) Slot</h2>

<p>부모가 자식 컴포넌트의 특정 위치에 HTML 내용을 삽입하는 기능</p>

<p>자식</p>

<template>
  <div class="card">
    <slot />
  </div>
</template>

<p>부모</p>

<BaseCard>
    <h2> 서울 날씨 </h2>
    <p> 현재 기온 28도 </p>
</BaseCard>

<p>BaseCard는 공통 틀을 만들고, 내부 콘텐츠는 부모가 결정</p>

<h1 id="4상태-변화-감시">4.상태 변화 감시</h1>

<h2 id="19-watch">19) watch</h2>

<p>특정 상태의 변경을 감지한 후 부수 효과를 실행한 이유</p>

<p>watch(searchQuery, (newValue, oldValue) =&gt; {
  console.log(‘새 값:’, newValue)
  console.log(‘이전 값:’, oldValue)
})</p>

<p>다음과 같은 작업에 적합</p>

<ul>
  <li>API 요청</li>
  <li>localStroage 저장</li>
  <li>로그 기록</li>
  <li>특정 값 변경 시 별도 처리</li>
</ul>

<p>객체의 특정 속성을 감시할 수 있다.</p>

<p>watch(
    () =&gt; user.value.age, 
    (newAge, oldAge) =&gt; {
        console.log(newAge,oldAge)
    }
)</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="JavaScript" /><category term="ES6" /><category term="Vue.js" /><category term="Data Type" /><category term="ref" /><summary type="html"><![CDATA[JavaScript의 원시값, 객체, 배열을 비교하고 Vue의 ref 안에서 데이터가 어떻게 구성되는지 정리한다.]]></summary></entry><entry><title type="html">Pinia 상태 관리 정리</title><link href="http://localhost:4000/frontend/vue/2026/08/03/pinia/" rel="alternate" type="text/html" title="Pinia 상태 관리 정리" /><published>2026-08-03T00:00:00+09:00</published><updated>2026-08-03T00:00:00+09:00</updated><id>http://localhost:4000/frontend/vue/2026/08/03/pinia</id><content type="html" xml:base="http://localhost:4000/frontend/vue/2026/08/03/pinia/"><![CDATA[<h1 id="pinia-상태-관리-정리">Pinia 상태 관리 정리</h1>

<h2 id="1-pinia가-필요한-이유">1. Pinia가 필요한 이유</h2>

<p>App.vue</p>

<p>├── Header.vue
 │      └── 로그인 사용자 이름 표시
 │
 ├── ProductList.vue
 │      └── 상품 목록
 │
 └── Cart.vue
        └── 장바구니 개수 표시</p>

<p>로그인 정보 :</p>

<p>const user = {
  name:”Kim”,
  role:”admin”
}</p>

<p>방법.1</p>

<p>App.vue
 |
 props
 ↓
Header.vue</p>

<p>App.vue
 |
 props
 ↓
Cart.vue</p>

<p>문제 : 중간 컴포넌트를 계속 거쳐야 한다.</p>

<p>-&gt; Props Drilling</p>

<p>A
 |
props
 ↓
B
 |
props
 ↓
C
 |
props
 ↓
D</p>

<p>깊어질수록 관리가 어렵다.</p>

<p># 2. Pinia</p>

<p>Pinia는 Vue 어플리케이션에서 전역 상태 (Global State)를 관리하기 위한 공식 상태 라이브러리</p>

<p>쉽게…</p>

<p>여러 컴포넌트가 공유해야하는 데이터를 한 곳에서 관리하는 저장소</p>

<p>구조.</p>

<p>Component A
      |
      props
      |
Component B
      |
      props
      |
Component C</p>

<p>-&gt; Pinia를 통해서 한번에 전달하도록 개선할 수 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    Pinia Store

   user
   cart
   product


  ↑       ↑       ↑
</code></pre></div></div>

<p>Component Component Component</p>

<p># 3. State</p>

<p>먼저 State 개념</p>

<h3 id="state">State</h3>

<p>애플리케이션이 현재 가지고 있는 데이터</p>

<p>const count = ref(0)</p>

<p>이것이 State</p>

<p>쇼핑몰 :</p>

<p>state:</p>

<p>user
cartItems
products
isLogin</p>

<p># 4. Store</p>

<h3 id="store-정의">Store 정의</h3>

<p>State, Getter, Action을 하나로 묶어서 관리하는 저장하는 공간</p>

<p>Pinia의 기본 구조 .</p>

<p>Store</p>

<p>├── State
 │
 ├── Getter
 │
 └── Action</p>

<p># Pinia 사용법</p>

<p>npm install pinia</p>

<p>import {createApp} from ‘vue’
import {createPinia} from ‘pinia’</p>

<p>import APP from ‘./App.vue’</p>

<p>const app = create(App)</p>

<p>app.use(createPinai())</p>

<p>app.mount(‘#app’)</p>

<p>의미 :</p>

<p>Vue App
 |
 Pinia 연결
 |
 모든 Component에서 사용 가능</p>

<p># 7.Store 만들기</p>

<p>stores/user.js</p>

<p>import { defineStore } from ‘pinia’</p>

<p>export const useUserStore = defineStore(
‘user’,
{</p>

<p>state:()=&gt;({</p>

<p>name:””,
  isLogin:false</p>

<p>})</p>

<p>})</p>

<p>–</p>

<p>Component
    |
    ↓
useUserStore()
    |
    ↓
Pinia Store
    |
    ↓
state 사용</p>

<p># 9.State 변경</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="Vue.js" /><category term="Pinia" /><category term="State Management" /><summary type="html"><![CDATA[Pinia 상태 관리 정리]]></summary></entry><entry><title type="html">Vue Composition API 정리</title><link href="http://localhost:4000/frontend/vue/2026/08/02/vue-composition-api/" rel="alternate" type="text/html" title="Vue Composition API 정리" /><published>2026-08-02T09:00:00+09:00</published><updated>2026-08-02T09:00:00+09:00</updated><id>http://localhost:4000/frontend/vue/2026/08/02/vue-composition-api</id><content type="html" xml:base="http://localhost:4000/frontend/vue/2026/08/02/vue-composition-api/"><![CDATA[<h2 id="composition-api란">Composition API란?</h2>

<p>자자, 이번에는 Vue의 <strong>Composition API</strong>를 정리해보자.</p>

<p>Vue 컴포넌트에는 보통 다음과 같은 로직이 들어간다.</p>

<ul>
  <li>화면에서 사용하는 상태</li>
  <li>상태를 변경하는 함수</li>
  <li>상태에서 계산되는 값</li>
  <li>서버 요청이나 이벤트 감시</li>
  <li>컴포넌트 생명주기에 맞춰 실행할 작업</li>
</ul>

<p>Composition API는 이런 로직을 옵션별로 흩어놓기보다, 기능 단위로 가까이 모아서 작성하는 방식이다. 그래서 컴포넌트가 커질수록 어떤 상태와 함수가 한 기능을 이루는지 파악하기 쉬워진다.</p>

<!--more-->

<p>Vue 3에서는 <code class="language-plaintext highlighter-rouge">&lt;script setup&gt;</code>과 함께 Composition API를 많이 사용한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">computed</span><span class="p">,</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">doubleCount</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">count</span><span class="p">.</span><span class="nx">value</span> <span class="o">*</span> <span class="mi">2</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span>현재 값: <span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>두 배 값: <span class="si">{{</span> <span class="nx">doubleCount</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increase"</span><span class="nt">&gt;</span>증가<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>이 코드에서는 <code class="language-plaintext highlighter-rouge">count</code> 상태, <code class="language-plaintext highlighter-rouge">doubleCount</code> 계산값, <code class="language-plaintext highlighter-rouge">increase</code> 함수가 한곳에 모여 있다. 화면과 연결되는 템플릿도 아래에서 바로 확인할 수 있다.</p>

<h2 id="composition-api에서-자주-쓰는-함수">Composition API에서 자주 쓰는 함수</h2>

<p>처음부터 모든 함수를 외울 필요는 없다. 우선 아래 함수들의 역할을 구분하면 된다.</p>

<table>
  <thead>
    <tr>
      <th>분류</th>
      <th>주요 함수</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>반응형 상태</td>
      <td><code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">reactive</code></td>
      <td>변경을 추적하는 상태를 만든다.</td>
    </tr>
    <tr>
      <td>계산과 감시</td>
      <td><code class="language-plaintext highlighter-rouge">computed</code>, <code class="language-plaintext highlighter-rouge">watch</code>, <code class="language-plaintext highlighter-rouge">watchEffect</code></td>
      <td>상태에서 값을 계산하거나 변경 후 작업을 실행한다.</td>
    </tr>
    <tr>
      <td>생명주기</td>
      <td><code class="language-plaintext highlighter-rouge">onMounted</code>, <code class="language-plaintext highlighter-rouge">onUpdated</code>, <code class="language-plaintext highlighter-rouge">onUnmounted</code></td>
      <td>컴포넌트의 생성·수정·제거 시점에 코드를 실행한다.</td>
    </tr>
    <tr>
      <td>컴포넌트 통신</td>
      <td><code class="language-plaintext highlighter-rouge">defineProps</code>, <code class="language-plaintext highlighter-rouge">defineEmits</code></td>
      <td>부모와 자식 컴포넌트 사이의 데이터를 연결한다.</td>
    </tr>
    <tr>
      <td>반응성 변환</td>
      <td><code class="language-plaintext highlighter-rouge">toRef</code>, <code class="language-plaintext highlighter-rouge">toRefs</code>, <code class="language-plaintext highlighter-rouge">unref</code></td>
      <td>반응형 객체의 속성을 ref로 연결하거나 값을 꺼낸다.</td>
    </tr>
    <tr>
      <td>의존성 주입</td>
      <td><code class="language-plaintext highlighter-rouge">provide</code>, <code class="language-plaintext highlighter-rouge">inject</code></td>
      <td>깊은 컴포넌트 트리에서 값을 공유한다.</td>
    </tr>
    <tr>
      <td>DOM·렌더링</td>
      <td><code class="language-plaintext highlighter-rouge">nextTick</code>, <code class="language-plaintext highlighter-rouge">h</code></td>
      <td>DOM 반영 시점을 기다리거나 렌더링을 직접 제어한다.</td>
    </tr>
  </tbody>
</table>

<p>이번 글에서는 그중에서도 <code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">reactive</code>, <code class="language-plaintext highlighter-rouge">computed</code>, <code class="language-plaintext highlighter-rouge">watch</code>, <code class="language-plaintext highlighter-rouge">watchEffect</code>를 중심으로 살펴보자.</p>

<h2 id="ref로-반응형-상태-만들기"><code class="language-plaintext highlighter-rouge">ref()</code>로 반응형 상태 만들기</h2>

<p><code class="language-plaintext highlighter-rouge">ref()</code>는 원시 값과 객체·배열을 반응형 상태로 감싸는 함수다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">name</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">홍길동</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">isActive</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="kc">true</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">items</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span><span class="dl">'</span><span class="s1">사과</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">배</span><span class="dl">'</span><span class="p">])</span>
<span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">이순신</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">30</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">changeUserName</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">user</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">name</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">장보고</span><span class="dl">'</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span>카운트: <span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>이름: <span class="nt">&lt;input</span> <span class="na">v-model=</span><span class="s">"name"</span> <span class="nt">/&gt;&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>활성 상태: <span class="si">{{</span> <span class="nx">isActive</span> <span class="p">?</span> <span class="dl">'</span><span class="s1">활성</span><span class="dl">'</span> <span class="p">:</span> <span class="dl">'</span><span class="s1">비활성</span><span class="dl">'</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>과일 목록: <span class="si">{{</span> <span class="nx">items</span><span class="p">.</span><span class="nx">join</span><span class="p">(</span><span class="dl">'</span><span class="s1">, </span><span class="dl">'</span><span class="p">)</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>사용자: <span class="si">{{</span> <span class="nx">user</span><span class="p">.</span><span class="nx">name</span> <span class="si">}}</span> / <span class="si">{{</span> <span class="nx">user</span><span class="p">.</span><span class="nx">age</span> <span class="si">}}</span>세<span class="nt">&lt;/p&gt;</span>

  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increase"</span><span class="nt">&gt;</span>카운트 증가<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"isActive = !isActive"</span><span class="nt">&gt;</span>상태 토글<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"items.push('귤')"</span><span class="nt">&gt;</span>과일 추가<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"changeUserName"</span><span class="nt">&gt;</span>이름 변경<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">&lt;script setup&gt;</code> 안의 JavaScript에서는 <code class="language-plaintext highlighter-rouge">.value</code>를 사용하고, 템플릿에서는 Vue가 자동으로 언래핑해주므로 <code class="language-plaintext highlighter-rouge">.value</code>를 생략한다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="c1">// JavaScript</span>
<span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="p">)</span>
</code></pre></div></div>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- template --&gt;</span>
<span class="nt">&lt;p&gt;</span>{{ count }}<span class="nt">&lt;/p&gt;</span>
</code></pre></div></div>

<h3 id="ref를-사용할-때-기억할-점"><code class="language-plaintext highlighter-rouge">ref</code>를 사용할 때 기억할 점</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">ref</code>를 사용하려면 <code class="language-plaintext highlighter-rouge">vue</code>에서 import해야 한다.</li>
  <li>JavaScript에서 값을 읽거나 변경할 때는 <code class="language-plaintext highlighter-rouge">.value</code>를 사용한다.</li>
  <li>템플릿에서는 <code class="language-plaintext highlighter-rouge">.value</code>를 생략한다.</li>
  <li>객체나 배열을 <code class="language-plaintext highlighter-rouge">ref</code>로 만들면 내부 값에도 반응성이 연결된다.</li>
</ul>

<h2 id="reactive로-객체와-배열-다루기"><code class="language-plaintext highlighter-rouge">reactive()</code>로 객체와 배열 다루기</h2>

<p><code class="language-plaintext highlighter-rouge">reactive()</code>는 객체나 배열 자체를 반응형 객체로 만든다. <code class="language-plaintext highlighter-rouge">ref</code>와 달리 <code class="language-plaintext highlighter-rouge">.value</code> 없이 속성에 접근한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">reactive</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="nx">reactive</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">이순신</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">30</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">items</span> <span class="o">=</span> <span class="nx">reactive</span><span class="p">([</span><span class="dl">'</span><span class="s1">사과</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">바나나</span><span class="dl">'</span><span class="p">])</span>

<span class="kd">const</span> <span class="nx">celebrate</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">user</span><span class="p">.</span><span class="nx">age</span><span class="o">++</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">addItem</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">items</span><span class="p">.</span><span class="nx">push</span><span class="p">(</span><span class="s2">`과일 </span><span class="p">${</span><span class="nx">items</span><span class="p">.</span><span class="nx">length</span> <span class="o">+</span> <span class="mi">1</span><span class="p">}</span><span class="s2">`</span><span class="p">)</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">removeItem</span> <span class="o">=</span> <span class="p">(</span><span class="nx">index</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">items</span><span class="p">.</span><span class="nx">splice</span><span class="p">(</span><span class="nx">index</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span><span class="si">{{</span> <span class="nx">user</span><span class="p">.</span><span class="nx">name</span> <span class="si">}}</span> / <span class="si">{{</span> <span class="nx">user</span><span class="p">.</span><span class="nx">age</span> <span class="si">}}</span>세<span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"celebrate"</span><span class="nt">&gt;</span>나이 한 살 추가<span class="nt">&lt;/button&gt;</span>

  <span class="nt">&lt;ul&gt;</span>
    <span class="nt">&lt;li</span> <span class="na">v-for=</span><span class="s">"(item, index) in items"</span> <span class="na">:key=</span><span class="s">"item"</span><span class="nt">&gt;</span>
      <span class="si">{{</span> <span class="nx">item</span> <span class="si">}}</span>
      <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"removeItem(index)"</span><span class="nt">&gt;</span>삭제<span class="nt">&lt;/button&gt;</span>
    <span class="nt">&lt;/li&gt;</span>
  <span class="nt">&lt;/ul&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"addItem"</span><span class="nt">&gt;</span>과일 추가<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<h3 id="reactive의-주의점"><code class="language-plaintext highlighter-rouge">reactive</code>의 주의점</h3>

<p>반응형으로 만든 객체를 통째로 다른 객체로 교체하면 기존 반응성 연결이 끊긴다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">let</span> <span class="nx">state</span> <span class="o">=</span> <span class="nx">reactive</span><span class="p">({</span> <span class="na">count</span><span class="p">:</span> <span class="mi">0</span> <span class="p">})</span>

<span class="c1">// 잘못된 사용: 반응형 객체 자체를 교체함</span>
<span class="nx">state</span> <span class="o">=</span> <span class="p">{</span> <span class="na">count</span><span class="p">:</span> <span class="mi">5</span> <span class="p">}</span>

<span class="c1">// 올바른 사용: 기존 객체의 속성을 변경함</span>
<span class="nx">state</span><span class="p">.</span><span class="nx">count</span> <span class="o">=</span> <span class="mi">5</span>
</code></pre></div></div>

<p>객체를 통째로 교체해야 하거나 <code class="language-plaintext highlighter-rouge">.value</code>를 사용하는 방식이 더 명확하다 싶으면 <code class="language-plaintext highlighter-rouge">ref</code>를 선택하면 된다. 무조건 어느 한쪽이 정답이라기보다는 데이터의 형태와 팀의 작성 규칙에 맞춰 선택하는 것이 중요하다.</p>

<h3 id="ref와-reactive-비교"><code class="language-plaintext highlighter-rouge">ref</code>와 <code class="language-plaintext highlighter-rouge">reactive</code> 비교</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th><code class="language-plaintext highlighter-rouge">ref</code></th>
      <th><code class="language-plaintext highlighter-rouge">reactive</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>대상</td>
      <td>원시 값, 객체, 배열</td>
      <td>객체, 배열, Map, Set</td>
    </tr>
    <tr>
      <td>JavaScript 접근</td>
      <td><code class="language-plaintext highlighter-rouge">.value</code> 필요</td>
      <td>속성에 바로 접근</td>
    </tr>
    <tr>
      <td>객체 교체</td>
      <td><code class="language-plaintext highlighter-rouge">.value = 새 객체</code>로 가능</td>
      <td>연결이 끊길 수 있음</td>
    </tr>
    <tr>
      <td>사용 예</td>
      <td>단일 값, 교체 가능한 상태</td>
      <td>여러 속성을 묶은 객체</td>
    </tr>
  </tbody>
</table>

<h3 id="반응성-연결을-유지하는-보조-함수">반응성 연결을 유지하는 보조 함수</h3>

<p><code class="language-plaintext highlighter-rouge">reactive</code> 객체를 구조 분해하면 반응성 연결이 끊길 수 있다. 이때 <code class="language-plaintext highlighter-rouge">toRef</code>나 <code class="language-plaintext highlighter-rouge">toRefs</code>를 사용하면 원본 객체와 연결된 ref를 만들 수 있다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">reactive</span><span class="p">,</span> <span class="nx">toRefs</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">state</span> <span class="o">=</span> <span class="nx">reactive</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">홍길동</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">20</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="p">{</span> <span class="nx">name</span><span class="p">,</span> <span class="nx">age</span> <span class="p">}</span> <span class="o">=</span> <span class="nx">toRefs</span><span class="p">(</span><span class="nx">state</span><span class="p">)</span>

<span class="nx">age</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="c1">// state.age도 함께 변경됨</span>
</code></pre></div></div>

<p>그 밖의 반응성 보조 함수는 필요할 때 찾아서 사용하면 된다.</p>

<table>
  <thead>
    <tr>
      <th>함수</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">readonly</code></td>
      <td>반응형 값을 읽기 전용으로 만든다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">shallowRef</code></td>
      <td>최상위 ref 값의 변경만 추적한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">shallowReactive</code></td>
      <td>객체의 최상위 속성만 반응형으로 만든다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">unref</code></td>
      <td>ref라면 <code class="language-plaintext highlighter-rouge">.value</code>, 일반 값이면 원래 값을 반환한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">toRaw</code></td>
      <td>반응형 프록시의 원본 객체를 가져온다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">markRaw</code></td>
      <td>특정 객체가 반응형으로 변환되지 않게 한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">isRef</code>, <code class="language-plaintext highlighter-rouge">isReactive</code>, <code class="language-plaintext highlighter-rouge">isReadonly</code></td>
      <td>반응형 여부를 확인한다.</td>
    </tr>
  </tbody>
</table>

<h2 id="computed로-계산된-값-만들기"><code class="language-plaintext highlighter-rouge">computed()</code>로 계산된 값 만들기</h2>

<p><code class="language-plaintext highlighter-rouge">computed()</code>는 기존 반응형 상태를 바탕으로 새로운 값을 계산한다. 계산에 사용한 의존성이 변경될 때 다시 계산하고, 같은 값이 필요할 때는 이전 결과를 재사용한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">computed</span><span class="p">,</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">dummy</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">doubleCount</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">count</span><span class="p">.</span><span class="nx">value</span> <span class="o">*</span> <span class="mi">2</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">increaseCount</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">increaseDummy</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">dummy</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span>count: <span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>dummy: <span class="si">{{</span> <span class="nx">dummy</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>두 배 값: <span class="si">{{</span> <span class="nx">doubleCount</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>

  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increaseCount"</span><span class="nt">&gt;</span>count 증가<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increaseDummy"</span><span class="nt">&gt;</span>dummy 증가<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">dummy</code>가 변경되어 컴포넌트가 다시 렌더링되어도 <code class="language-plaintext highlighter-rouge">doubleCount</code>의 계산에 <code class="language-plaintext highlighter-rouge">dummy</code>가 사용되지 않았다면 다시 계산할 필요가 없다. 이것이 일반 함수와 <code class="language-plaintext highlighter-rouge">computed</code>의 중요한 차이다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">totalPrice</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">products</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">reduce</span><span class="p">((</span><span class="nx">sum</span><span class="p">,</span> <span class="nx">product</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="k">return</span> <span class="nx">sum</span> <span class="o">+</span> <span class="nx">product</span><span class="p">.</span><span class="nx">price</span>
  <span class="p">},</span> <span class="mi">0</span><span class="p">)</span>
<span class="p">})</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">computed</code>는 계산된 값이므로 템플릿에서 함수처럼 호출하지 않는다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- 올바른 사용 --&gt;</span>
<span class="nt">&lt;p&gt;</span>총가격: {{ totalPrice }}원<span class="nt">&lt;/p&gt;</span>

<span class="c">&lt;!-- 잘못된 사용 --&gt;</span>
<span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"totalPrice(products)"</span><span class="nt">&gt;</span>총가격<span class="nt">&lt;/button&gt;</span>
</code></pre></div></div>

<h3 id="일반-함수와-computed-구분">일반 함수와 <code class="language-plaintext highlighter-rouge">computed</code> 구분</h3>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 사용자 행동을 처리하는 일반 함수</span>
<span class="kd">const</span> <span class="nx">save</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">alert</span><span class="p">(</span><span class="dl">'</span><span class="s1">저장되었습니다.</span><span class="dl">'</span><span class="p">)</span>
<span class="p">}</span>

<span class="c1">// 상태에서 값을 계산하는 computed</span>
<span class="kd">const</span> <span class="nx">completedCount</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">todos</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">filter</span><span class="p">((</span><span class="nx">todo</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">todo</span><span class="p">.</span><span class="nx">done</span><span class="p">).</span><span class="nx">length</span>
<span class="p">})</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자가 행동한다 → 일반 함수
상태에서 값이 나온다 → computed
</code></pre></div></div>

<h2 id="watch로-변경-후-작업-실행하기"><code class="language-plaintext highlighter-rouge">watch()</code>로 변경 후 작업 실행하기</h2>

<p><code class="language-plaintext highlighter-rouge">watch()</code>는 특정 반응형 값의 변경을 감시하고, 값이 바뀐 뒤 후속 작업을 실행한다.</p>

<ul>
  <li>서버 요청</li>
  <li>로컬 스토리지 저장</li>
  <li>로그 기록</li>
  <li>다른 상태 변경</li>
  <li>외부 라이브러리와 동기화</li>
</ul>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span><span class="p">,</span> <span class="nx">watch</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">currentCity</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">logMessage</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">아직 감시 시스템이 작동하지 않았습니다.</span><span class="dl">'</span><span class="p">)</span>

<span class="nx">watch</span><span class="p">(</span><span class="nx">currentCity</span><span class="p">,</span> <span class="p">(</span><span class="nx">newValue</span><span class="p">,</span> <span class="nx">oldValue</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">logMessage</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="s2">`</span><span class="p">${</span><span class="nx">oldValue</span><span class="p">}</span><span class="s2">에서 </span><span class="p">${</span><span class="nx">newValue</span><span class="p">}</span><span class="s2">(으)로 변경됨.`</span>
  <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">newValue</span><span class="p">}</span><span class="s2">의 날씨 API를 다시 조회합니다.`</span><span class="p">)</span>
<span class="p">})</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span>현재 도시: <span class="si">{{</span> <span class="nx">currentCity</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"currentCity = '서울'"</span><span class="nt">&gt;</span>서울<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"currentCity = '수원'"</span><span class="nt">&gt;</span>수원<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"currentCity = '부산'"</span><span class="nt">&gt;</span>부산<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;p&gt;</span><span class="si">{{</span> <span class="nx">logMessage</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>콜백에는 새 값과 이전 값이 순서대로 전달된다.</p>

<h3 id="여러-값-감시하기">여러 값 감시하기</h3>

<p>첫 번째 인자에 감시할 ref를 배열로 전달하면 여러 값을 한 번에 감시할 수 있다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span><span class="p">,</span> <span class="nx">watch</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">city</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">dateType</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">오늘</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">apiStatus</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">대기 중...</span><span class="dl">'</span><span class="p">)</span>

<span class="nx">watch</span><span class="p">(</span>
  <span class="p">[</span><span class="nx">city</span><span class="p">,</span> <span class="nx">dateType</span><span class="p">],</span>
  <span class="p">([</span><span class="nx">newCity</span><span class="p">,</span> <span class="nx">newDate</span><span class="p">],</span> <span class="p">[</span><span class="nx">oldCity</span><span class="p">,</span> <span class="nx">oldDate</span><span class="p">])</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">apiStatus</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="s2">`</span><span class="p">${</span><span class="nx">oldCity</span><span class="p">}</span><span class="s2">(</span><span class="p">${</span><span class="nx">oldDate</span><span class="p">}</span><span class="s2">) → </span><span class="p">${</span><span class="nx">newCity</span><span class="p">}</span><span class="s2">(</span><span class="p">${</span><span class="nx">newDate</span><span class="p">}</span><span class="s2">)`</span>
  <span class="p">}</span>
<span class="p">)</span>
</code></pre></div></div>

<p>감시 대상 배열의 순서와 콜백의 새 값·이전 값 배열 순서는 서로 대응한다.</p>

<h3 id="객체와-배열-깊이-감시하기">객체와 배열 깊이 감시하기</h3>

<p>객체나 배열 내부의 변경까지 감시하려면 <code class="language-plaintext highlighter-rouge">deep: true</code>를 사용할 수 있다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span><span class="p">,</span> <span class="nx">watch</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">홍길동</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">20</span>
<span class="p">})</span>

<span class="nx">watch</span><span class="p">(</span>
  <span class="nx">user</span><span class="p">,</span>
  <span class="p">(</span><span class="nx">newUser</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`현재 이름: </span><span class="p">${</span><span class="nx">newUser</span><span class="p">.</span><span class="nx">name</span><span class="p">}</span><span class="s2">`</span><span class="p">)</span>
  <span class="p">},</span>
  <span class="p">{</span> <span class="na">deep</span><span class="p">:</span> <span class="kc">true</span> <span class="p">}</span>
<span class="p">)</span>
</code></pre></div></div>

<p>다만 깊은 감시는 객체 전체를 재귀적으로 확인하므로 꼭 필요한 경우에 사용하는 것이 좋다. 내부 속성의 변경을 감시하면서 이전 값도 정확하게 비교하고 싶다면 특정 속성만 감시한다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">watch</span><span class="p">(</span>
  <span class="p">()</span> <span class="o">=&gt;</span> <span class="nx">user</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">age</span><span class="p">,</span>
  <span class="p">(</span><span class="nx">newAge</span><span class="p">,</span> <span class="nx">oldAge</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">oldAge</span><span class="p">}</span><span class="s2">세에서 </span><span class="p">${</span><span class="nx">newAge</span><span class="p">}</span><span class="s2">세로 변경됨.`</span><span class="p">)</span>
  <span class="p">}</span>
<span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">reactive</code> 객체 자체를 감시하면 내부 변경을 감지할 수 있지만, 중첩 속성 변경에서는 새 값과 이전 값이 같은 객체를 가리킬 수 있다. 이전 값 비교가 필요하면 위처럼 감시할 속성을 함수로 지정하는 편이 명확하다.</p>

<h2 id="watcheffect로-자동-감시하기"><code class="language-plaintext highlighter-rouge">watchEffect()</code>로 자동 감시하기</h2>

<p><code class="language-plaintext highlighter-rouge">watchEffect()</code>는 감시할 대상을 따로 적지 않는다. 함수 내부에서 사용하는 반응형 값을 자동으로 추적하고, 값이 변경될 때 다시 실행한다.</p>

<ul>
  <li>컴포넌트가 처음 생성될 때 즉시 한 번 실행된다.</li>
  <li>함수 내부에서 읽은 반응형 값만 추적한다.</li>
  <li><code class="language-plaintext highlighter-rouge">oldValue</code>를 제공하지 않는다.</li>
  <li>이전 값과 비교해야 하면 <code class="language-plaintext highlighter-rouge">watch</code>를 사용한다.</li>
</ul>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span><span class="p">,</span> <span class="nx">watchEffect</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">city</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">message</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">''</span><span class="p">)</span>

<span class="nx">watchEffect</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">message</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="s2">`</span><span class="p">${</span><span class="nx">city</span><span class="p">.</span><span class="nx">value</span><span class="p">}</span><span class="s2">의 날씨를 조회합니다.`</span>
<span class="p">})</span>
</code></pre></div></div>

<h3 id="watch-watcheffect-비교"><code class="language-plaintext highlighter-rouge">watch</code>, <code class="language-plaintext highlighter-rouge">watchEffect</code> 비교</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th><code class="language-plaintext highlighter-rouge">watch</code></th>
      <th><code class="language-plaintext highlighter-rouge">watchEffect</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>감시 대상</td>
      <td>직접 지정</td>
      <td>함수 안에서 자동 추적</td>
    </tr>
    <tr>
      <td>최초 실행</td>
      <td>기본적으로 변경 후 실행</td>
      <td>생성 직후 즉시 실행</td>
    </tr>
    <tr>
      <td>이전 값</td>
      <td>제공</td>
      <td>제공하지 않음</td>
    </tr>
    <tr>
      <td>적합한 상황</td>
      <td>특정 값의 변경 전후 비교</td>
      <td>여러 의존성을 간단히 동기화</td>
    </tr>
  </tbody>
</table>

<p>DOM 업데이트가 끝난 뒤 실행해야 하면 <code class="language-plaintext highlighter-rouge">watchPostEffect</code>, 값이 바뀌는 즉시 동기적으로 실행해야 하면 <code class="language-plaintext highlighter-rouge">watchSyncEffect</code>를 검토할 수 있다. 다만 실행 타이밍을 세밀하게 제어해야 하는 경우에만 사용하자.</p>

<h2 id="생명주기-훅-맛보기">생명주기 훅 맛보기</h2>

<p>Composition API에서는 컴포넌트의 생명주기 훅도 함수로 사용한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">onMounted</span><span class="p">,</span> <span class="nx">onUnmounted</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">handleResize</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nb">window</span><span class="p">.</span><span class="nx">innerWidth</span><span class="p">)</span>
<span class="p">}</span>

<span class="nx">onMounted</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nb">window</span><span class="p">.</span><span class="nx">addEventListener</span><span class="p">(</span><span class="dl">'</span><span class="s1">resize</span><span class="dl">'</span><span class="p">,</span> <span class="nx">handleResize</span><span class="p">)</span>
<span class="p">})</span>

<span class="nx">onUnmounted</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nb">window</span><span class="p">.</span><span class="nx">removeEventListener</span><span class="p">(</span><span class="dl">'</span><span class="s1">resize</span><span class="dl">'</span><span class="p">,</span> <span class="nx">handleResize</span><span class="p">)</span>
<span class="p">})</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">onMounted</code>: 컴포넌트가 DOM에 마운트된 뒤 실행한다.</li>
  <li><code class="language-plaintext highlighter-rouge">onUpdated</code>: 반응형 상태 변경으로 업데이트된 뒤 실행한다.</li>
  <li><code class="language-plaintext highlighter-rouge">onUnmounted</code>: 컴포넌트가 제거된 뒤 실행한다. 이벤트 리스너나 타이머 정리에 사용한다.</li>
</ul>

<p>특히 <code class="language-plaintext highlighter-rouge">onMounted</code>에서 등록한 이벤트나 타이머는 <code class="language-plaintext highlighter-rouge">onUnmounted</code>에서 정리하는 습관을 들이는 것이 좋다.</p>

<h2 id="composition-api를-composable로-확장하기">Composition API를 composable로 확장하기</h2>

<p>Composition API의 장점은 반복되는 로직을 함수로 분리해 여러 컴포넌트에서 재사용할 수 있다는 점이다. 이런 재사용 함수를 보통 <strong>composable</strong>이라고 부른다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// useCounter.js</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">computed</span><span class="p">,</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="k">export</span> <span class="kd">function</span> <span class="nx">useCounter</span><span class="p">(</span><span class="nx">initialValue</span> <span class="o">=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="nx">initialValue</span><span class="p">)</span>
  <span class="kd">const</span> <span class="nx">doubleCount</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="nx">count</span><span class="p">.</span><span class="nx">value</span> <span class="o">*</span> <span class="mi">2</span><span class="p">)</span>

  <span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
  <span class="p">}</span>

  <span class="kd">const</span> <span class="nx">reset</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">count</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="nx">initialValue</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="p">{</span>
    <span class="nx">count</span><span class="p">,</span>
    <span class="nx">doubleCount</span><span class="p">,</span>
    <span class="nx">increase</span><span class="p">,</span>
    <span class="nx">reset</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">useCounter</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./useCounter</span><span class="dl">'</span>

<span class="kd">const</span> <span class="p">{</span> <span class="nx">count</span><span class="p">,</span> <span class="nx">doubleCount</span><span class="p">,</span> <span class="nx">increase</span><span class="p">,</span> <span class="nx">reset</span> <span class="p">}</span> <span class="o">=</span> <span class="nx">useCounter</span><span class="p">(</span><span class="mi">10</span><span class="p">)</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span><span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span> / <span class="si">{{</span> <span class="nx">doubleCount</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increase"</span><span class="nt">&gt;</span>증가<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"reset"</span><span class="nt">&gt;</span>초기화<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>상태와 함수가 특정 기능을 중심으로 묶여 있으니, 컴포넌트가 커져도 로직을 분리해서 관리할 수 있다. 나중에 <code class="language-plaintext highlighter-rouge">useFetch</code>, <code class="language-plaintext highlighter-rouge">useTodo</code>, <code class="language-plaintext highlighter-rouge">useAuth</code> 같은 형태로 확장하는 흐름이다.</p>

<h2 id="최종-정리">최종 정리</h2>

<p>Composition API는 단순히 함수 몇 개를 새로 사용하는 문법이 아니다. 컴포넌트의 로직을 기능 단위로 구성하고, 필요한 반응형 흐름을 조합하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ref / reactive
      ↓
반응형 상태 생성
      ↓
computed로 파생 값 계산
      ↓
watch 또는 watchEffect로 변경 감지
      ↓
함수와 템플릿으로 화면 연결
      ↓
composable로 재사용
</code></pre></div></div>

<p>기억할 것만 다시 적어보자.</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">ref</code>는 원시 값과 객체·배열을 반응형으로 만든다.</li>
  <li><code class="language-plaintext highlighter-rouge">reactive</code>는 객체나 배열 자체를 반응형으로 만든다.</li>
  <li>JavaScript에서 <code class="language-plaintext highlighter-rouge">ref</code>는 <code class="language-plaintext highlighter-rouge">.value</code>, 템플릿에서는 <code class="language-plaintext highlighter-rouge">.value</code>를 생략한다.</li>
  <li><code class="language-plaintext highlighter-rouge">computed</code>는 상태에서 파생된 값을 계산한다.</li>
  <li><code class="language-plaintext highlighter-rouge">watch</code>는 특정 값의 변경 후 작업을 실행한다.</li>
  <li><code class="language-plaintext highlighter-rouge">watchEffect</code>는 함수 내부에서 사용하는 반응형 값을 자동으로 추적한다.</li>
  <li><code class="language-plaintext highlighter-rouge">onUnmounted</code>에서 이벤트와 타이머를 정리한다.</li>
  <li>반복되는 로직은 composable로 분리한다.</li>
</ol>

<p>처음에는 <code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">computed</code>, <code class="language-plaintext highlighter-rouge">watch</code>만으로도 충분하다. 나머지는 실제로 필요한 상황이 왔을 때 하나씩 살펴보면 된다. 현업에 가서 빨리 써보고 싶다. 일단은 여기까지!</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="Vue.js" /><category term="Composition API" /><category term="ref" /><category term="reactive" /><category term="computed" /><category term="watch" /><summary type="html"><![CDATA[Vue Composition API의 기본 개념과 ref, reactive, computed, watch, watchEffect의 사용 방법을 정리한다.]]></summary></entry><entry><title type="html">Psql JSONB, 행 잠금, 멱등성</title><link href="http://localhost:4000/db/postgresql/2026/07/31/psql2/" rel="alternate" type="text/html" title="Psql JSONB, 행 잠금, 멱등성" /><published>2026-07-31T09:00:00+09:00</published><updated>2026-07-31T09:00:00+09:00</updated><id>http://localhost:4000/db/postgresql/2026/07/31/psql2</id><content type="html" xml:base="http://localhost:4000/db/postgresql/2026/07/31/psql2/"><![CDATA[<h1 id="jsonb">JSONB</h1>
<h2 id="jsonb-객체-생성">JSONB 객체 생성</h2>
<h2 id="중첩-json-반환">중첩 JSON 반환</h2>
<h2 id="jsonb_pretty와-to_jsonb">jsonb_pretty와 to_jsonb</h2>

<h1 id="행-잠금">행 잠금</h1>
<h2 id="동시성-문제">동시성 문제</h2>
<h2 id="select-for-update">SELECT FOR UPDATE</h2>
<h2 id="안전한-재고-처리-순서">안전한 재고 처리 순서</h2>

<h1 id="멱등성idempotency">멱등성(Idempotency)</h1>
<h2 id="멱등성-개념">멱등성 개념</h2>
<h2 id="batch_key">batch_key</h2>
<h2 id="on-conflict-do-nothing">ON CONFLICT DO NOTHING</h2>
<h2 id="get-diagnostics와-row_count">GET DIAGNOSTICS와 ROW_COUNT</h2>]]></content><author><name>Min Hyeong Lee</name></author><category term="DB" /><category term="PostgreSQL" /><category term="PostgreSQL" /><summary type="html"><![CDATA[JSONB JSONB 객체 생성 중첩 JSON 반환 jsonb_pretty와 to_jsonb]]></summary></entry></feed>