<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-08-19T19:13:35+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Minnong’s Study Log</title><subtitle>개발과 AI를 배우고, 실험하고, 이해한 내용을 연결하는 공부 기록</subtitle><author><name>Min Hyeong Lee</name></author><entry><title type="html">Spring 기초 Part 7: Proxy 패턴과 Spring Proxy</title><link href="http://localhost:4000/java/spring/2026/08/19/spring-proxy/" rel="alternate" type="text/html" title="Spring 기초 Part 7: Proxy 패턴과 Spring Proxy" /><published>2026-08-19T09:00:00+09:00</published><updated>2026-08-19T09:00:00+09:00</updated><id>http://localhost:4000/java/spring/2026/08/19/spring-proxy</id><content type="html" xml:base="http://localhost:4000/java/spring/2026/08/19/spring-proxy/"><![CDATA[<h2 id="spring-기초-part-7-proxy-패턴과-spring-proxy">Spring 기초 Part 7: Proxy 패턴과 Spring Proxy</h2>

<blockquote>
  <p>Proxy는 Client와 실제 객체 사이에서 요청을 먼저 받고, 실제 메서드 호출 전후에 부가 기능을 수행하는 대리 객체다.</p>
</blockquote>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  → Proxy
  → 실제 객체
</code></pre></div></div>

<p>Proxy를 이해하면 <code class="language-plaintext highlighter-rouge">@Transactional</code>, <code class="language-plaintext highlighter-rouge">@Async</code>, <code class="language-plaintext highlighter-rouge">@Cacheable</code>, Spring AOP가 왜 같은 클래스의 내부 호출에서 동작하지 않을 수 있는지도 이해할 수 있다.</p>

<hr />

<h3 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Client</td>
      <td>기능을 요청하는 객체</td>
      <td>Service를 호출하는 Controller</td>
    </tr>
    <tr>
      <td>Subject</td>
      <td>Proxy와 실제 객체가 함께 따르는 인터페이스</td>
      <td>두 객체의 공통 사용법</td>
    </tr>
    <tr>
      <td>RealSubject</td>
      <td>핵심 기능을 실제로 처리하는 객체</td>
      <td>실제 Service</td>
    </tr>
    <tr>
      <td>Proxy</td>
      <td>실제 객체 대신 요청을 먼저 받는 객체</td>
      <td>대리인</td>
    </tr>
    <tr>
      <td>Target</td>
      <td>Proxy가 감싸고 있는 실제 대상 객체</td>
      <td>Proxy 내부의 Service</td>
    </tr>
    <tr>
      <td>Wrapping</td>
      <td>한 객체가 다른 객체를 내부에 보관하는 구조</td>
      <td>Proxy가 Target을 감쌈</td>
    </tr>
    <tr>
      <td>Delegation</td>
      <td>받은 작업을 다른 객체에 넘기는 것</td>
      <td>Proxy가 실제 작업을 Target에 전달</td>
    </tr>
    <tr>
      <td>전처리</td>
      <td>실제 메서드 실행 전에 수행하는 작업</td>
      <td>트랜잭션 시작, 권한 확인</td>
    </tr>
    <tr>
      <td>후처리</td>
      <td>실제 메서드 실행 후 수행하는 작업</td>
      <td>커밋, 로그 기록</td>
    </tr>
    <tr>
      <td>동적 Proxy</td>
      <td>실행 시점에 자동으로 생성되는 Proxy</td>
      <td>개발자가 Proxy 클래스를 직접 작성하지 않음</td>
    </tr>
    <tr>
      <td>Bean</td>
      <td>Spring Container가 생성하고 관리하는 객체</td>
      <td>Spring이 보관하고 주입하는 객체</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="2-proxy-패턴의-기본-구조">2. Proxy 패턴의 기본 구조</h3>

<p>Proxy와 실제 객체는 같은 인터페이스를 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">UserService</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="nf">findUserName</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="실제-객체">실제 객체</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserServiceImpl</span> <span class="kd">implements</span> <span class="nc">UserService</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">findUserName</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"실제 사용자 조회"</span><span class="o">);</span>
        <span class="k">return</span> <span class="s">"민형"</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="proxy-객체">Proxy 객체</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserServiceProxy</span> <span class="kd">implements</span> <span class="nc">UserService</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserService</span> <span class="n">target</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserServiceProxy</span><span class="o">(</span><span class="nc">UserService</span> <span class="n">target</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">target</span> <span class="o">=</span> <span class="n">target</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">findUserName</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"조회 시작"</span><span class="o">);</span>

        <span class="nc">String</span> <span class="n">result</span> <span class="o">=</span> <span class="n">target</span><span class="o">.</span><span class="na">findUserName</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>

        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"조회 종료"</span><span class="o">);</span>
        <span class="k">return</span> <span class="n">result</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="client">Client</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ProxyExample</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">UserService</span> <span class="n">target</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">UserServiceImpl</span><span class="o">();</span>
        <span class="nc">UserService</span> <span class="n">proxy</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">UserServiceProxy</span><span class="o">(</span><span class="n">target</span><span class="o">);</span>

        <span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="n">proxy</span><span class="o">.</span><span class="na">findUserName</span><span class="o">(</span><span class="mi">1L</span><span class="o">);</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">name</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client가 proxy.findUserName() 호출
          ↓
Proxy가 "조회 시작" 출력
          ↓
target.findUserName()에 실제 작업 위임
          ↓
Proxy가 "조회 종료" 출력
          ↓
결과 반환
</code></pre></div></div>

<p>Proxy 안의 다음 코드가 위임이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">String</span> <span class="n">result</span> <span class="o">=</span> <span class="n">target</span><span class="o">.</span><span class="na">findUserName</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>
</code></pre></div></div>

<hr />

<h3 id="3-proxy를-사용하는-이유">3. Proxy를 사용하는 이유</h3>

<p>회원 조회 Service의 핵심 목적은 회원을 조회하는 것이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="nc">User</span> <span class="nf">findUser</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<p>여기에 로그, 실행 시간, 권한 검사, 트랜잭션 코드를 모두 넣으면 핵심 로직이 흐려진다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="nc">User</span> <span class="nf">findUser</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="kt">long</span> <span class="n">startTime</span> <span class="o">=</span> <span class="nc">System</span><span class="o">.</span><span class="na">currentTimeMillis</span><span class="o">();</span>
    <span class="n">checkPermission</span><span class="o">();</span>
    <span class="n">beginTransaction</span><span class="o">();</span>

    <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>

    <span class="n">commitTransaction</span><span class="o">();</span>
    <span class="n">logExecutionTime</span><span class="o">(</span><span class="n">startTime</span><span class="o">);</span>
    <span class="k">return</span> <span class="n">user</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Proxy가 공통 기능을 맡으면 실제 Service는 업무 로직에 집중할 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>실제 Service</th>
      <th>Proxy</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>회원 조회, 주문 처리, 재고 감소</td>
      <td>로그, 권한, 트랜잭션, 캐시</td>
    </tr>
  </tbody>
</table>

<p>Proxy가 처리할 수 있는 기능은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>기능</th>
      <th>Proxy가 하는 일</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>트랜잭션</td>
      <td>메서드 전후에 시작, 커밋, 롤백 처리</td>
    </tr>
    <tr>
      <td>로그</td>
      <td>메서드 실행 정보 기록</td>
    </tr>
    <tr>
      <td>권한 검사</td>
      <td>실제 메서드 실행 전에 접근 권한 확인</td>
    </tr>
    <tr>
      <td>캐시</td>
      <td>저장된 결과가 있으면 실제 메서드 호출 생략</td>
    </tr>
    <tr>
      <td>실행 시간 측정</td>
      <td>시작과 종료 시각 비교</td>
    </tr>
    <tr>
      <td>비동기 실행</td>
      <td>작업을 Executor에 제출</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="4-bean과-proxy의-관계">4. Bean과 Proxy의 관계</h3>

<p>Bean은 Spring Container가 관리하는 객체다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">TransferService</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Service</code>가 붙었다고 항상 Proxy가 생성되는 것은 아니다. Spring이 적용해야 할 부가 기능을 발견했을 때 Proxy로 감쌀 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">TransferService</span> <span class="o">{</span>

    <span class="nd">@Transactional</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">transfer</span><span class="o">()</span> <span class="o">{</span>
        <span class="c1">// 출금과 입금</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Component Scan
   ↓
TransferService 객체 생성
   ↓
@Transactional 적용 대상 확인
   ↓
Proxy가 Target을 감쌈
   ↓
다른 Bean에는 Proxy가 주입됨
</code></pre></div></div>

<p>Controller가 <code class="language-plaintext highlighter-rouge">TransferService</code>를 호출한다고 생각하지만, 실제 참조가 Proxy일 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Controller
   ↓
TransferService Proxy
   ↓ 트랜잭션 시작
실제 TransferService
   ↓
Proxy가 커밋 또는 롤백
</code></pre></div></div>

<hr />

<h3 id="5-spring의-동적-proxy">5. Spring의 동적 Proxy</h3>

<p>Spring은 실행 시점에 Proxy 객체를 자동으로 만들 수 있다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>JDK Dynamic Proxy</th>
      <th>CGLIB Proxy</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>기준</td>
      <td>인터페이스</td>
      <td>클래스</td>
    </tr>
    <tr>
      <td>인터페이스 필요 여부</td>
      <td>필요</td>
      <td>없어도 됨</td>
    </tr>
    <tr>
      <td>생성 방식</td>
      <td>Reflection을 이용한 Proxy 생성</td>
      <td>대상 클래스를 상속한 하위 클래스 생성</td>
    </tr>
    <tr>
      <td>주요 제약</td>
      <td>인터페이스에 선언된 호출 중심</td>
      <td><code class="language-plaintext highlighter-rouge">final</code> 클래스와 <code class="language-plaintext highlighter-rouge">final</code> 메서드 재정의 불가</td>
    </tr>
  </tbody>
</table>

<p>Spring Boot에서는 클래스 기반 Proxy가 널리 사용되지만, 실제 방식은 설정과 대상 구조에 따라 달라질 수 있다. 코드를 작성할 때 특정 Proxy 방식에 과도하게 의존하지 않는 것이 좋다.</p>

<hr />

<h3 id="6-transactional-호출-흐름">6. <code class="language-plaintext highlighter-rouge">@Transactional</code> 호출 흐름</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">updateUser</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">,</span> <span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>
    <span class="n">user</span><span class="o">.</span><span class="na">changeName</span><span class="o">(</span><span class="n">name</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>개발자가 작성한 메서드에는 회원 수정 로직만 있다. 트랜잭션 처리는 Proxy가 앞뒤에서 수행한다.</p>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>Transactional Proxy 호출 흐름</span>
  </header>
  <div class="mermaid js-mermaid">
flowchart TD
    C[Controller] --&gt; P[Transactional Proxy]
    P --&gt; B[트랜잭션 시작]
    B --&gt; T[실제 updateUser 실행]
    T --&gt; Q{정상 종료?}
    Q --&gt;|예| COMMIT[Commit]
    Q --&gt;|아니오| ROLLBACK[Rollback]
    COMMIT --&gt; C
    ROLLBACK --&gt; C
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<hr />

<h3 id="7-자기-호출-문제">7. 자기 호출 문제</h3>

<p>Spring Proxy는 <strong>Proxy를 거쳐 들어오는 호출</strong>을 가로챈다.</p>

<p>같은 객체 안에서 자신의 메서드를 직접 호출하면 Proxy를 거치지 않는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">PaymentService</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">order</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">pay</span><span class="o">();</span> <span class="c1">// 같은 객체의 내부 호출</span>
    <span class="o">}</span>

    <span class="nd">@Transactional</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">pay</span><span class="o">()</span> <span class="o">{</span>
        <span class="c1">// 결제 처리</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>외부 호출
→ Proxy
→ @Transactional 적용 가능

같은 객체의 pay() 내부 호출
→ Proxy를 다시 거치지 않음
→ 새 트랜잭션 설정이 적용되지 않을 수 있음
</code></pre></div></div>

<p>이것을 Self-invocation, 자기 호출 문제라고 한다.</p>

<p>트랜잭션 경계가 분명히 달라야 한다면 별도 Bean으로 분리하는 방법을 사용할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">PaymentProcessor</span> <span class="o">{</span>

    <span class="nd">@Transactional</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">pay</span><span class="o">()</span> <span class="o">{</span>
        <span class="c1">// 결제 처리</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">OrderService</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">PaymentProcessor</span> <span class="n">paymentProcessor</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">OrderService</span><span class="o">(</span><span class="nc">PaymentProcessor</span> <span class="n">paymentProcessor</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">paymentProcessor</span> <span class="o">=</span> <span class="n">paymentProcessor</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">order</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">paymentProcessor</span><span class="o">.</span><span class="na">pay</span><span class="o">();</span> <span class="c1">// 다른 Bean의 Proxy를 통해 호출</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<hr />

<h3 id="8-proxy와-aop의-차이">8. Proxy와 AOP의 차이</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>AOP</td>
      <td>어떤 공통 기능을 어디에 적용할지 정의하는 설계 방식</td>
    </tr>
    <tr>
      <td>Proxy</td>
      <td>실제 메서드 호출을 가로채 공통 기능을 실행하는 객체</td>
    </tr>
    <tr>
      <td>Spring</td>
      <td>AOP 설정을 읽고 Proxy를 자동 생성하고 관리</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>AOP가 곧 Proxy인 것은 아니다. Spring AOP가 공통 기능을 적용하는 핵심 구현 방식으로 Proxy를 사용하는 것이다.</p>
</blockquote>

<hr />

<h3 id="핵심-정리">핵심 정리</h3>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 줄 정리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Proxy</td>
      <td>실제 객체 대신 요청을 먼저 받는 대리 객체</td>
    </tr>
    <tr>
      <td>Target</td>
      <td>Proxy가 감싸고 있는 실제 객체</td>
    </tr>
    <tr>
      <td>Wrapping</td>
      <td>Proxy가 Target을 내부에 보관하는 구조</td>
    </tr>
    <tr>
      <td>Delegation</td>
      <td>Proxy가 실제 작업을 Target에 전달하는 것</td>
    </tr>
    <tr>
      <td>동적 Proxy</td>
      <td>실행 시점에 자동으로 생성되는 Proxy</td>
    </tr>
    <tr>
      <td>자기 호출</td>
      <td>같은 객체 내부 호출이 Proxy를 거치지 않는 문제</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Proxy의 핵심은 실제 업무 코드를 크게 바꾸지 않고 메서드 호출 앞뒤에 공통 기능을 추가하는 것이다.</p>
</blockquote>

<h3 id="참고-자료">참고 자료</h3>

<ul>
  <li><a href="https://docs.spring.io/spring-framework/reference/core/aop/proxying.html">Spring Framework AOP Proxying Mechanisms</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="Proxy" /><category term="Dynamic Proxy" /><category term="JDK Proxy" /><category term="CGLIB" /><category term="Bean" /><summary type="html"><![CDATA[대리 객체인 Proxy가 실제 객체의 호출을 감싸는 원리와 Spring의 JDK Dynamic Proxy, CGLIB, 자기 호출 문제를 정리한다.]]></summary></entry><entry><title type="html">현대 LLM 워크플로의 패턴</title><link href="http://localhost:4000/deep_learning/llm/2026/08/19/llm-workflow-patterns/" rel="alternate" type="text/html" title="현대 LLM 워크플로의 패턴" /><published>2026-08-19T00:00:00+09:00</published><updated>2026-08-19T00:00:00+09:00</updated><id>http://localhost:4000/deep_learning/llm/2026/08/19/llm-workflow-patterns</id><content type="html" xml:base="http://localhost:4000/deep_learning/llm/2026/08/19/llm-workflow-patterns/"><![CDATA[<h1 id="현대-llm-워크플로의-패턴">현대 LLM 워크플로의 패턴</h1>

<blockquote>
  <p>LLM 서비스는 도대체 어떻게 작동하는 걸까? 궁금해서 참을 수가 없단 말이지!</p>
</blockquote>

<p>LLM 애플리케이션을 만들 때는 요청을 한 번 받고 바로 답을 내는 경우도 있지만, 실제로는 입력을 분류하고, 작업을 나누고, 결과를 평가하는 흐름으로 구성하는 경우가 많다.</p>

<h2 id="자주-나오는-workflow-패턴">자주 나오는 Workflow 패턴</h2>

<table>
  <thead>
    <tr>
      <th>패턴</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Routing</td>
      <td>입력을 분류해서 적절한 처리 경로 하나를 선택한다.</td>
    </tr>
    <tr>
      <td>Parallelization</td>
      <td>서로 의존하지 않는 AI 작업을 동시에 실행한다.</td>
    </tr>
    <tr>
      <td>Evaluator-Optimizer</td>
      <td>결과를 평가하고 피드백을 기반으로 반복 개선한다.</td>
    </tr>
    <tr>
      <td>Orchestrator-Workers</td>
      <td>큰 작업을 여러 하위 작업으로 분해하고 각각 처리한 후 합친다.</td>
    </tr>
    <tr>
      <td>Chaining</td>
      <td>앞 단계의 출력을 다음 단계의 입력으로 연결한다.</td>
    </tr>
  </tbody>
</table>

<h2 id="개념적으로-이런-구조">개념적으로 이런 구조</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자 요청
  ↓
Routing
  ↓
계획 / 작업 분해
  ↓
┌───────────────┐
│ Worker A      │
│ Worker B      │
│ Worker C      │
└───────────────┘
  ↓
결과 통합
  ↓
Evaluator
  ↓
필요하면 재시도
  ↓
최종 응답
</code></pre></div></div>

<p>이건 지금도 LLM 애플리케이션에서 아주 전형적인 구조다.</p>

<p>다만, 실제 현대 시스템에서는 여기에 보통 다음 요소가 추가된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LLM Workflow
  + Tool Calling
  + RAG
  + Memory / State
  + Retry / Timeout
  + Observability
  + Guardrails
  + Model Selection
  + Caching
</code></pre></div></div>

<p>현대적인 구조에서는 Spring Boot에서 했던 기능에 더해 다음과 같은 Tool을 사용할 수 있다.</p>

<ul>
  <li>웹 검색</li>
  <li>DB 조회</li>
  <li>사내 문서 검색</li>
  <li>Python 실행</li>
  <li>API 호출</li>
  <li>파일 읽기</li>
</ul>

<h2 id="요즘은-이런-것도-만들-수-있다">요즘은 이런 것도 만들 수 있다</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자
  ↓
Router
  ↓
Planner
  ↓
Workers
  ├─ Search Agent
  ├─ SQL Agent
  ├─ Code Agent
  └─ Document Agent
  ↓
Aggregator
  ↓
Evaluator
  ↓
Final Answer
</code></pre></div></div>

<p>이런 형태가 된다.</p>

<h2 id="정해진-로직으로-처리하는-workflow">정해진 로직으로 처리하는 Workflow</h2>

<p>다만 나는 로직을 정해놓았는데,</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>분류
  → switch
  → 특정 처리
</code></pre></div></div>

<p>또는</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>계획
  → worker 실행
  → 통합
</code></pre></div></div>

<p>이런 방식으로 처리한다면 <code class="language-plaintext highlighter-rouge">deterministic workflow</code> 또는 <code class="language-plaintext highlighter-rouge">LLM workflow</code>라고 생각하면 된다.</p>

<h2 id="llm이-다음-행동을-직접-선택하는-agent">LLM이 다음 행동을 직접 선택하는 Agent</h2>

<p>다만 모던한 Agentic 시스템은 조금 다르게 동작한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LLM: "검색이 필요하겠군"
  ↓
검색 Tool 호출
  ↓
LLM: "정보가 부족하군"
  ↓
다른 Tool 호출
  ↓
LLM: "이제 답할 수 있겠다"
</code></pre></div></div>

<p>이렇게 LLM이 알아서 다음 행동을 스스로 선택한다.</p>

<p>요즘은 이런 식으로 가는 중이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>고정 Workflow                                      자율 Agent
──────────────────────────────────────────────────────→
Chaining
Routing
Parallel
Evaluator
Orchestrator
                      Tool Calling
                              Planner
                                      Agent Loop
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">WorkflowPatterns.java</code>에서 생각해볼 수 있는 위치는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Chaining
Routing
Parallel
Evaluator
Orchestrator
        ↑
     현재 위치
</code></pre></div></div>

<p>자율 Agent를 만드는 것보다 가능하면 Workflow로 통제하고, 필요한 부분만 Agent화하는 방식이 안정성, 비용, 디버깅 측면에서 훨씬 유리한 경우가 있다.</p>

<blockquote>
  <p>항상 자유가 좋은 결과를 내주는 것은 아니다.</p>
</blockquote>

<p>가끔은 행동에 제약을 걸어서 예측 가능한 결과를 낼 수 있어야 한다.</p>

<p>예를 들어 다음처럼 반복 횟수를 Java 코드에서 제한하는 것은 상당히 프로덕션 지향적인 사고다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="o">(</span><span class="kt">int</span> <span class="n">round</span> <span class="o">=</span> <span class="mi">1</span><span class="o">;</span> <span class="n">round</span> <span class="o">&lt;=</span> <span class="n">maxRounds</span><span class="o">;</span> <span class="n">round</span><span class="o">++)</span> <span class="o">{</span>
    <span class="c1">// 작업 실행과 평가</span>
<span class="o">}</span>
</code></pre></div></div>

<p>LLM에게 그냥 “좋아질 때까지 반복해”라고 하는 것은 결과를 예측하기도 어렵고, 무한 루프 문제가 생길 수도 있다.</p>

<h2 id="학습-순서">학습 순서</h2>

<ol>
  <li>Tool Calling</li>
  <li>RAG</li>
  <li>Memory / State</li>
  <li>Agent Loop</li>
  <li>Observability</li>
  <li>Guardrails</li>
  <li>Model Routing</li>
</ol>]]></content><author><name>Min Hyeong Lee</name></author><category term="Deep_Learning" /><category term="LLM" /><category term="Deep Learning" /><category term="LLM" /><category term="Workflow" /><category term="Agent" /><summary type="html"><![CDATA[Routing, Chaining, Parallelization, Evaluator-Optimizer, Orchestrator-Workers와 Agent Loop의 차이를 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 6: JPA 연관관계 매핑</title><link href="http://localhost:4000/java/spring/2026/08/18/jpa-relationship-mapping/" rel="alternate" type="text/html" title="Spring 기초 Part 6: JPA 연관관계 매핑" /><published>2026-08-18T09:00:00+09:00</published><updated>2026-08-18T09:00:00+09:00</updated><id>http://localhost:4000/java/spring/2026/08/18/jpa-relationship-mapping</id><content type="html" xml:base="http://localhost:4000/java/spring/2026/08/18/jpa-relationship-mapping/"><![CDATA[<h2 id="spring-기초-part-6-jpa-연관관계-매핑">Spring 기초 Part 6: JPA 연관관계 매핑</h2>

<blockquote>
  <p>연관관계 매핑은 자바 객체의 참조와 데이터베이스 테이블의 외래키를 연결하는 작업이다.</p>
</blockquote>

<p>자바 객체는 다른 객체를 필드로 참조한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">member</span><span class="o">.</span><span class="na">getTeam</span><span class="o">();</span>
</code></pre></div></div>

<p>관계형 데이터베이스는 외래키로 다른 테이블을 참조한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>members.team_id → teams.id
</code></pre></div></div>

<p>JPA는 이 두 관계를 어노테이션으로 연결한다.</p>

<hr />

<h3 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉬운 설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>기본키(PK)</td>
      <td>테이블의 각 행을 유일하게 구분하는 값</td>
      <td><code class="language-plaintext highlighter-rouge">teams.id</code></td>
    </tr>
    <tr>
      <td>외래키(FK)</td>
      <td>다른 테이블의 기본키를 참조하는 컬럼</td>
      <td><code class="language-plaintext highlighter-rouge">members.team_id</code></td>
    </tr>
    <tr>
      <td>연관관계</td>
      <td>한 객체나 테이블이 다른 대상과 연결된 관계</td>
      <td>회원이 팀에 소속됨</td>
    </tr>
    <tr>
      <td>다중성</td>
      <td>관계에 참여하는 대상의 수</td>
      <td>일대일, 일대다, 다대일, 다대다</td>
    </tr>
    <tr>
      <td>방향성</td>
      <td>객체에서 어느 방향으로 참조할 수 있는지 나타내는 것</td>
      <td><code class="language-plaintext highlighter-rouge">Member → Team</code>, 또는 양쪽 모두</td>
    </tr>
    <tr>
      <td>연관관계의 주인</td>
      <td>외래키 값을 실제로 저장하고 변경하는 쪽</td>
      <td><code class="language-plaintext highlighter-rouge">Member.team</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mappedBy</code></td>
      <td>주인이 아닌 쪽에서 주인 필드의 이름을 지정하는 속성</td>
      <td><code class="language-plaintext highlighter-rouge">mappedBy = "team"</code></td>
    </tr>
    <tr>
      <td>Join</td>
      <td>외래키를 기준으로 여러 테이블의 데이터를 연결하는 것</td>
      <td>회원과 팀을 함께 조회</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="2-연관관계의-종류">2. 연관관계의 종류</h3>

<table>
  <thead>
    <tr>
      <th>어노테이션</th>
      <th>관계</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@OneToOne</code></td>
      <td>일대일(1:1)</td>
      <td>사용자와 프로필</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@OneToMany</code></td>
      <td>일대다(1:N)</td>
      <td>하나의 팀과 여러 회원</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@ManyToOne</code></td>
      <td>다대일(N:1)</td>
      <td>여러 회원과 하나의 팀</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@ManyToMany</code></td>
      <td>다대다(N:M)</td>
      <td>여러 사용자와 여러 관심 종목</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@JoinColumn</code></td>
      <td>외래키 컬럼 지정</td>
      <td><code class="language-plaintext highlighter-rouge">team_id</code>, <code class="language-plaintext highlighter-rouge">user_id</code></td>
    </tr>
  </tbody>
</table>

<p>실무에서는 다대다 관계를 바로 사용하기보다 중간 Entity로 분리하는 경우가 많다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User 1 ─── N Watchlist N ─── 1 Stock
</code></pre></div></div>

<p>중간 Entity를 만들면 등록 날짜, 알림 설정 같은 추가 정보도 저장할 수 있다.</p>

<hr />

<h3 id="3-다대일-단방향-매핑">3. 다대일 단방향 매핑</h3>

<p>여러 회원이 하나의 팀에 소속된다고 해보자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Team 1  ←────  N Member
</code></pre></div></div>

<p>외래키는 보통 다수 쪽 테이블인 <code class="language-plaintext highlighter-rouge">members</code>에 둔다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>teams                   members
-------------           ----------------
id (PK)       ←──────── team_id (FK)
name                    id (PK)
                        name
</code></pre></div></div>

<h4 id="team-entity">Team Entity</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Entity</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Team</span> <span class="o">{</span>

    <span class="nd">@Id</span>
    <span class="nd">@GeneratedValue</span><span class="o">(</span><span class="n">strategy</span> <span class="o">=</span> <span class="nc">GenerationType</span><span class="o">.</span><span class="na">IDENTITY</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">;</span>

    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="kd">protected</span> <span class="nf">Team</span><span class="o">()</span> <span class="o">{</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="member-entity">Member Entity</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Entity</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Member</span> <span class="o">{</span>

    <span class="nd">@Id</span>
    <span class="nd">@GeneratedValue</span><span class="o">(</span><span class="n">strategy</span> <span class="o">=</span> <span class="nc">GenerationType</span><span class="o">.</span><span class="na">IDENTITY</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">;</span>

    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="nd">@ManyToOne</span><span class="o">(</span><span class="n">fetch</span> <span class="o">=</span> <span class="nc">FetchType</span><span class="o">.</span><span class="na">LAZY</span><span class="o">)</span>
    <span class="nd">@JoinColumn</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"team_id"</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">Team</span> <span class="n">team</span><span class="o">;</span>

    <span class="kd">protected</span> <span class="nf">Member</span><span class="o">()</span> <span class="o">{</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">changeTeam</span><span class="o">(</span><span class="nc">Team</span> <span class="n">team</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">team</span> <span class="o">=</span> <span class="n">team</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@ManyToOne</code>: 여러 <code class="language-plaintext highlighter-rouge">Member</code>가 하나의 <code class="language-plaintext highlighter-rouge">Team</code>과 연결된다.</li>
  <li><code class="language-plaintext highlighter-rouge">@JoinColumn(name = "team_id")</code>: <code class="language-plaintext highlighter-rouge">members.team_id</code>를 외래키로 사용한다.</li>
  <li><code class="language-plaintext highlighter-rouge">FetchType.LAZY</code>: <code class="language-plaintext highlighter-rouge">team</code>이 실제로 필요할 때 조회하도록 설정한다.</li>
</ul>

<p>현재는 Member만 Team을 참조하므로 단방향 관계다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">member</span><span class="o">.</span><span class="na">getTeam</span><span class="o">();</span>  <span class="c1">// 가능</span>
<span class="n">team</span><span class="o">.</span><span class="na">getMembers</span><span class="o">();</span> <span class="c1">// Team에 members 필드가 없으므로 불가능</span>
</code></pre></div></div>

<hr />

<h3 id="4-양방향-연관관계">4. 양방향 연관관계</h3>

<p>Team에서도 소속 회원 목록을 조회하고 싶다면 <code class="language-plaintext highlighter-rouge">@OneToMany</code>를 추가한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Entity</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Team</span> <span class="o">{</span>

    <span class="nd">@Id</span>
    <span class="nd">@GeneratedValue</span><span class="o">(</span><span class="n">strategy</span> <span class="o">=</span> <span class="nc">GenerationType</span><span class="o">.</span><span class="na">IDENTITY</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">;</span>

    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="nd">@OneToMany</span><span class="o">(</span><span class="n">mappedBy</span> <span class="o">=</span> <span class="s">"team"</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Member</span><span class="o">&gt;</span> <span class="n">members</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>

    <span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Member</span><span class="o">&gt;</span> <span class="nf">getMembers</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">members</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이제 객체에서 양쪽 방향으로 이동할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">member</span><span class="o">.</span><span class="na">getTeam</span><span class="o">();</span>  <span class="c1">// Member → Team</span>
<span class="n">team</span><span class="o">.</span><span class="na">getMembers</span><span class="o">();</span> <span class="c1">// Team → Member</span>
</code></pre></div></div>

<p>양방향 객체 관계라고 해서 DB 외래키가 두 개 생기는 것은 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>객체: Member ↔ Team
DB:   members.team_id → teams.id
</code></pre></div></div>

<hr />

<h3 id="5-연관관계의-주인">5. 연관관계의 주인</h3>

<p>객체에는 <code class="language-plaintext highlighter-rouge">Member.team</code>과 <code class="language-plaintext highlighter-rouge">Team.members</code>라는 참조가 두 개지만, DB 외래키는 <code class="language-plaintext highlighter-rouge">members.team_id</code> 하나다.</p>

<p>따라서 JPA는 어느 필드의 값을 기준으로 외래키를 변경할지 결정해야 한다. 외래키를 관리하는 쪽을 <strong>연관관계의 주인</strong>이라고 한다.</p>

<p>이 예제에서는 <code class="language-plaintext highlighter-rouge">Member.team</code>이 주인이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@ManyToOne</span><span class="o">(</span><span class="n">fetch</span> <span class="o">=</span> <span class="nc">FetchType</span><span class="o">.</span><span class="na">LAZY</span><span class="o">)</span>
<span class="nd">@JoinColumn</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"team_id"</span><span class="o">)</span>
<span class="kd">private</span> <span class="nc">Team</span> <span class="n">team</span><span class="o">;</span>
</code></pre></div></div>

<p>주인이 아닌 <code class="language-plaintext highlighter-rouge">Team.members</code>에는 <code class="language-plaintext highlighter-rouge">mappedBy</code>를 작성한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@OneToMany</span><span class="o">(</span><span class="n">mappedBy</span> <span class="o">=</span> <span class="s">"team"</span><span class="o">)</span>
<span class="kd">private</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Member</span><span class="o">&gt;</span> <span class="n">members</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">mappedBy = "team"</code>의 <code class="language-plaintext highlighter-rouge">team</code>은 DB 컬럼명이 아니라 <strong>Member 클래스의 필드명</strong>이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="nc">Team</span> <span class="n">team</span><span class="o">;</span>
             <span class="c1">// ↑ 이 필드명</span>
</code></pre></div></div>

<h4 id="외래키를-변경한다는-뜻">외래키를 변경한다는 뜻</h4>

<p>외래키를 변경한다는 것은 객체의 소속 관계를 바꾼다는 뜻이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>변경 전: members.team_id = 1
변경 후: members.team_id = 2
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">member</span><span class="o">.</span><span class="na">changeTeam</span><span class="o">(</span><span class="n">team2</span><span class="o">);</span>
</code></pre></div></div>

<p>JPA는 주인인 <code class="language-plaintext highlighter-rouge">Member.team</code>의 변경을 기준으로 <code class="language-plaintext highlighter-rouge">team_id</code>를 수정한다.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">UPDATE</span> <span class="n">members</span>
<span class="k">SET</span> <span class="n">team_id</span> <span class="o">=</span> <span class="mi">2</span>
<span class="k">WHERE</span> <span class="n">id</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div>

<p>반대편 컬렉션만 변경하면 메모리의 목록은 바뀌지만 외래키 변경이 반영되지 않을 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">team</span><span class="o">.</span><span class="na">getMembers</span><span class="o">().</span><span class="na">add</span><span class="o">(</span><span class="n">member</span><span class="o">);</span> <span class="c1">// 이것만으로는 부족함</span>
</code></pre></div></div>

<hr />

<h3 id="6-양쪽-객체를-함께-변경하기">6. 양쪽 객체를 함께 변경하기</h3>

<p>DB 외래키는 주인 쪽 값으로 변경되지만, 현재 메모리의 두 객체도 일치시키는 것이 안전하다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">changeTeam</span><span class="o">(</span><span class="nc">Team</span> <span class="n">newTeam</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="k">this</span><span class="o">.</span><span class="na">team</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">team</span><span class="o">.</span><span class="na">getMembers</span><span class="o">().</span><span class="na">remove</span><span class="o">(</span><span class="k">this</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="k">this</span><span class="o">.</span><span class="na">team</span> <span class="o">=</span> <span class="n">newTeam</span><span class="o">;</span>

    <span class="k">if</span> <span class="o">(</span><span class="n">newTeam</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">newTeam</span><span class="o">.</span><span class="na">getMembers</span><span class="o">().</span><span class="na">add</span><span class="o">(</span><span class="k">this</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">member</span><span class="o">.</span><span class="na">changeTeam</span><span class="o">(</span><span class="n">team</span><span class="o">);</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Member.team에 Team 저장
          ↓
Team.members에 Member 추가
          ↓
메모리의 양쪽 관계 일치
          ↓
members.team_id 변경
</code></pre></div></div>

<p>이런 메서드를 연관관계 편의 메서드라고 한다. 양쪽 메서드가 서로를 계속 호출하면 무한 호출이 발생할 수 있으므로 한쪽에서 관계를 관리한다.</p>

<hr />

<h3 id="7-지연-로딩">7. 지연 로딩</h3>

<p>지연 로딩(Lazy Loading)은 연관된 Entity를 처음부터 조회하지 않고 실제로 사용할 때 조회하는 방식이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@ManyToOne</span><span class="o">(</span><span class="n">fetch</span> <span class="o">=</span> <span class="nc">FetchType</span><span class="o">.</span><span class="na">LAZY</span><span class="o">)</span>
<span class="kd">private</span> <span class="nc">Team</span> <span class="n">team</span><span class="o">;</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Member 조회
    ↓
Team은 아직 조회하지 않음
    ↓
member.getTeam() 사용
    ↓
필요한 Team 조회
</code></pre></div></div>

<p>즉시 로딩(Eager Loading)은 원래 Entity를 조회할 때 연관된 Entity도 함께 조회한다.</p>

<p>연관관계를 무조건 즉시 로딩하면 예상하지 못한 SQL이 많이 실행될 수 있으므로 일반적으로 지연 로딩을 우선 사용하고, 필요한 조회에서 fetch join이나 EntityGraph 등을 명시적으로 고려한다.</p>

<p>지연 로딩된 Entity를 영속성 컨텍스트가 끝난 뒤 접근하면 예외가 발생할 수 있으므로 Service 안에서 필요한 데이터를 DTO로 변환하는 방식이 안전하다.</p>

<hr />

<h3 id="8-cascade와-orphanremoval">8. <code class="language-plaintext highlighter-rouge">cascade</code>와 <code class="language-plaintext highlighter-rouge">orphanRemoval</code></h3>

<h4 id="cascade"><code class="language-plaintext highlighter-rouge">cascade</code></h4>

<p>영속성 전이(Cascade)는 부모 Entity의 저장이나 삭제 작업을 연관된 Entity에도 전달한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@OneToMany</span><span class="o">(</span><span class="n">mappedBy</span> <span class="o">=</span> <span class="s">"team"</span><span class="o">,</span> <span class="n">cascade</span> <span class="o">=</span> <span class="nc">CascadeType</span><span class="o">.</span><span class="na">ALL</span><span class="o">)</span>
<span class="kd">private</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Member</span><span class="o">&gt;</span> <span class="n">members</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
</code></pre></div></div>

<p>연관되어 있다는 이유만으로 무조건 사용하면 안 된다. 두 Entity의 생명주기를 함께 관리할 때 사용한다.</p>

<h4 id="orphanremoval"><code class="language-plaintext highlighter-rouge">orphanRemoval</code></h4>

<p>고아 객체 제거는 부모 컬렉션에서 제거된 자식 Entity를 DB에서도 삭제하는 기능이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@OneToMany</span><span class="o">(</span><span class="n">mappedBy</span> <span class="o">=</span> <span class="s">"team"</span><span class="o">,</span> <span class="n">orphanRemoval</span> <span class="o">=</span> <span class="kc">true</span><span class="o">)</span>
<span class="kd">private</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Member</span><span class="o">&gt;</span> <span class="n">members</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
</code></pre></div></div>

<p>단순히 관계만 끊으려는 상황에서도 데이터가 삭제될 수 있으므로 주의해야 한다.</p>

<hr />

<h3 id="핵심-정리">핵심 정리</h3>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 줄 정리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>연관관계 매핑</td>
      <td>객체 참조와 테이블 외래키를 연결</td>
    </tr>
    <tr>
      <td>다중성</td>
      <td><code class="language-plaintext highlighter-rouge">1:1</code>, <code class="language-plaintext highlighter-rouge">1:N</code>, <code class="language-plaintext highlighter-rouge">N:1</code>, <code class="language-plaintext highlighter-rouge">N:M</code>처럼 관계의 수를 표현</td>
    </tr>
    <tr>
      <td>방향성</td>
      <td>객체가 한쪽 또는 양쪽으로 참조할 수 있는지 표현</td>
    </tr>
    <tr>
      <td>연관관계의 주인</td>
      <td>외래키 값을 실제로 저장하고 변경하는 쪽</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mappedBy</code></td>
      <td>주인이 아닌 쪽에서 주인 필드명을 지정</td>
    </tr>
    <tr>
      <td>지연 로딩</td>
      <td>연관 Entity가 실제로 필요할 때 조회</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">cascade</code></td>
      <td>영속성 작업을 연관 Entity에 전달</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">orphanRemoval</code></td>
      <td>관계에서 제거된 자식 Entity를 DB에서도 삭제</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Team과 Member의 양방향 관계에서는 외래키 <code class="language-plaintext highlighter-rouge">team_id</code>가 있는 <code class="language-plaintext highlighter-rouge">Member.team</code>이 연관관계의 주인이다.</p>
</blockquote>

<h3 id="참고-자료">참고 자료</h3>

<ul>
  <li><a href="https://jakarta.ee/specifications/persistence/">Jakarta Persistence 공식 명세</a></li>
  <li><a href="https://docs.jboss.org/hibernate/orm/current/userguide/html_single/Hibernate_User_Guide.html">Hibernate ORM User Guide</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="JPA" /><category term="Relationship Mapping" /><category term="ManyToOne" /><category term="OneToMany" /><category term="Foreign Key" /><summary type="html"><![CDATA[객체 참조와 외래키를 연결하고, 다대일, 일대다, 연관관계의 주인, mappedBy와 지연 로딩을 이해한다.]]></summary></entry><entry><title type="html">CI/CD 기초 3편: 배포 전략과 운영</title><link href="http://localhost:4000/devops/2026/08/17/docker-deployment-operations/" rel="alternate" type="text/html" title="CI/CD 기초 3편: 배포 전략과 운영" /><published>2026-08-17T15:00:00+09:00</published><updated>2026-08-17T15:00:00+09:00</updated><id>http://localhost:4000/devops/2026/08/17/docker-deployment-operations</id><content type="html" xml:base="http://localhost:4000/devops/2026/08/17/docker-deployment-operations/"><![CDATA[<h2 id="배포-후에도-끝은-아니다">배포 후에도 끝은 아니다</h2>

<p>CI/CD를 처음 접하면 <code class="language-plaintext highlighter-rouge">docker run</code>이 성공한 것을 배포 성공으로 생각하기 쉽다. 하지만 컨테이너 프로세스가 실행됐더라도 Spring Boot 애플리케이션은 정상적으로 동작하지 않을 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Deploy
→ Container Start
→ Health Check
→ Application 정상 확인
→ Traffic 연결
</code></pre></div></div>

<p>배포 이후에는 위와 같은 확인 과정이 필요하다.</p>

<h3 id="1-health-check">1. Health Check</h3>

<p>Spring Boot Actuator를 사용한다면 <code class="language-plaintext highlighter-rouge">/actuator/health</code> 엔드포인트로 애플리케이션 상태를 확인할 수 있다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"status"</span><span class="p">:</span><span class="w"> </span><span class="s2">"UP"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>컨테이너에는 크게 두 가지 상태가 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>프로세스가 살아 있음
애플리케이션이 정상 작동함
</code></pre></div></div>

<p>이 둘은 항상 같지 않다. Java 프로세스는 살아 있지만 DB 연결에 실패한 상태일 수도 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTTP 요청
→ /actuator/health
→ DB 연결 등 상태 확인
→ HTTP 200 OK
</code></pre></div></div>

<p>따라서 단순히 컨테이너가 실행 중인지 확인하는 것보다 애플리케이션이 실제 요청을 처리할 수 있는지 검사하는 것이 중요하다.</p>

<h3 id="2-rollback">2. Rollback</h3>

<p>Health Check에서 문제가 발견되면 이전 정상 버전으로 되돌려야 한다. 이때 Docker 이미지의 버전 관리가 도움이 된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>기존 버전: myapp:1.4.1  → GOOD
새 버전:   myapp:1.4.2  → ERROR

myapp:1.4.2 중지
→ myapp:1.4.1 실행
→ Health Check
</code></pre></div></div>

<p>Registry에 이전 이미지가 남아 있으면 빠르게 Rollback할 수 있다. 따라서 운영 정책과 보관 기간을 정하지 않은 상태에서 이전 이미지를 무조건 삭제하면 안 된다.</p>

<h3 id="3-컨테이너는-수정하지-않고-교체한다">3. 컨테이너는 수정하지 않고 교체한다</h3>

<p>Docker의 중요한 철학 중 하나는 고장 난 컨테이너 내부를 직접 고치는 대신 새 이미지로 컨테이너를 교체하는 것이다.</p>

<p>컨테이너에 접속해 다음과 같은 작업을 하면 변경 사항을 재현하기 어렵다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vi config
apt <span class="nb">install </span>some-package
</code></pre></div></div>

<p>대신 원인이 되는 설정이나 Dockerfile을 수정하고 새 이미지를 만든다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile 또는 설정 수정
→ 새 Image 생성
→ myapp:1.1
→ 새 Container 실행
→ 검증 후 기존 Container 제거
</code></pre></div></div>

<p>실행 중인 서버를 계속 수정하는 Mutable Server 방식보다, 변경 사항을 이미지에 반영해 통째로 교체하는 <strong>Immutable Infrastructure</strong> 방식에 가깝다.</p>

<h3 id="4-production에서-컨테이너-교체하기">4. Production에서 컨테이너 교체하기</h3>

<p>컨테이너 한 개만 운영한다면 다음 교체 과정에서 잠깐 서비스가 끊길 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Old Container Stop
→ New Container Start
</code></pre></div></div>

<p>서비스 중단을 줄이기 위해 Rolling, Blue-Green, Canary 같은 배포 전략을 사용한다.</p>

<h3 id="5-rolling-deployment">5. Rolling Deployment</h3>

<p>여러 서버 또는 컨테이너를 하나씩 새 버전으로 교체하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1단계: A=v1, B=v1, C=v1
2단계: A=v2, B=v1, C=v1
3단계: A=v2, B=v2, C=v1
4단계: A=v2, B=v2, C=v2
</code></pre></div></div>

<p>기존 인스턴스를 순차적으로 교체하므로 서비스 중단을 줄이고 리소스를 효율적으로 사용할 수 있다. Kubernetes에서 흔히 사용하는 방식이다.</p>

<h3 id="6-blue-green-deployment">6. Blue-Green Deployment</h3>

<p>현재 운영 중인 Blue 환경 옆에 새 버전인 Green 환경을 별도로 만든다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>BLUE                    GREEN
v1                      v2
현재 서비스             새 버전 검증
</code></pre></div></div>

<p>Green 검증이 끝나면 Load Balancer가 보낼 대상을 바꾼다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Users
→ Load Balancer
→ GREEN v2
</code></pre></div></div>

<p>문제가 발생하면 트래픽을 다시 Blue로 돌릴 수 있어 Rollback이 빠르다. 다만 두 환경을 동시에 유지할 리소스가 필요하다.</p>

<h3 id="7-canary-deployment">7. Canary Deployment</h3>

<p>새 버전을 일부 사용자에게 먼저 제공하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>초기: v1 90%, v2 10%
확대: v1 50%, v2 50%
완료:          v2 100%
</code></pre></div></div>

<p>Canary 배포를 사용하는 이유는 모든 서버의 가용성을 높이기 위해서라기보다 <strong>새 버전의 위험을 일부 트래픽으로 제한하기 위해서</strong>다. 오류율이나 지연 시간이 나빠지면 전체 사용자에게 영향을 주기 전에 배포를 중단하거나 되돌릴 수 있다.</p>

<p>대규모 서비스에서는 사용자 그룹, 지역 또는 트래픽 비율을 기준으로 점진적으로 범위를 넓힌다.</p>

<h3 id="8-cicd-pipeline의-stage">8. CI/CD Pipeline의 Stage</h3>

<p>실무에서는 Pipeline을 Stage 단위로 생각하면 이해하기 쉽다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source
  ↓
Test
  ↓
Build
  ↓
Image Build
  ↓
Image Push
  ↓
Deploy
  ↓
Health Check
  ↓
Monitoring
</code></pre></div></div>

<p>이것이 Docker 기반 CI/CD의 전체 뼈대다.</p>

<h3 id="9-배포-시스템에서-흐르는-것">9. 배포 시스템에서 흐르는 것</h3>

<p>CI/CD 시스템에서는 다음 항목들이 각 단계를 거쳐 이동하거나 참조된다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Source Code</td>
      <td>GitHub Repository</td>
    </tr>
    <tr>
      <td>Build Artifact</td>
      <td><code class="language-plaintext highlighter-rouge">app.jar</code></td>
    </tr>
    <tr>
      <td>Container Artifact</td>
      <td>Docker Image</td>
    </tr>
    <tr>
      <td>Configuration</td>
      <td>Environment Variables</td>
    </tr>
    <tr>
      <td>Secrets</td>
      <td>Password, API Key</td>
    </tr>
    <tr>
      <td>Runtime Data</td>
      <td>Database Data</td>
    </tr>
    <tr>
      <td>Logs</td>
      <td>Application Log</td>
    </tr>
    <tr>
      <td>Metrics</td>
      <td>CPU, Memory, Latency, Error Rate</td>
    </tr>
  </tbody>
</table>

<p>이들은 성격에 맞게 분리해서 관리해야 한다. 특히 Docker 이미지와 데이터베이스 데이터는 서로 다르다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container 삭제
      │
      └─ Database Data는 유지
</code></pre></div></div>

<p>컨테이너를 삭제해도 데이터가 사라지지 않도록 DB는 Docker Volume이나 외부 데이터베이스를 사용한다.</p>

<h3 id="10-운영에서는-observability가-중요하다">10. 운영에서는 Observability가 중요하다</h3>

<p>배포 이후에는 서비스가 정상적으로 동작하는지 지속해서 관찰해야 한다. 최소한 다음 정보를 확인할 필요가 있다.</p>

<ul>
  <li>CPU, Memory, Disk, Network</li>
  <li>Request Count, Latency, Error Rate</li>
  <li>Application Log, Database Connection</li>
  <li>Container Restart Count</li>
</ul>

<p>대표적인 도구로 Prometheus, Grafana, ELK, Loki, Datadog, CloudWatch 등이 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CI        → Build
CD        → Deploy
Operation → Observe
Problem   → Fix
git push  → 다시 CI
</code></pre></div></div>

<h3 id="11-devops는-하나의-loop다">11. DevOps는 하나의 Loop다</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PLAN
→ CODE
→ BUILD
→ TEST
→ RELEASE
→ DEPLOY
→ OPERATE
→ MONITOR
→ PLAN
</code></pre></div></div>

<p>Docker는 이 과정에서 Build, Release, Deploy, Operate를 연결해 주는 핵심 기술이다.</p>

<h3 id="12-spring-boot-프로젝트에-적용한다면">12. Spring Boot 프로젝트에 적용한다면</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Boot Source Code
→ git push
→ GitHub
→ GitHub Actions Runner
   ├─ JDK 설치
   ├─ Gradle Test
   ├─ bootJar
   └─ docker build
→ Docker Image: myapp:a8df329
→ docker push
→ Container Registry
→ Production Server에서 docker pull
→ Container 실행
→ Spring Boot
   ├─ Redis
   └─ PostgreSQL
→ Nginx
→ Internet
→ User
</code></pre></div></div>

<p>핵심 개념을 다시 정리하면 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>핵심 의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>CI</td>
      <td>코드를 지속적으로 통합하고 자동 검증</td>
    </tr>
    <tr>
      <td>CD</td>
      <td>검증된 결과물을 서버에 배포</td>
    </tr>
    <tr>
      <td>Artifact</td>
      <td>Build 결과물</td>
    </tr>
    <tr>
      <td>Docker Image</td>
      <td>실행 환경까지 포함한 배포 Artifact</td>
    </tr>
    <tr>
      <td>Container</td>
      <td>Image를 실행한 Runtime Instance</td>
    </tr>
    <tr>
      <td>Registry</td>
      <td>Docker Image 저장소</td>
    </tr>
    <tr>
      <td>Image Tag</td>
      <td>Image 버전 식별자</td>
    </tr>
    <tr>
      <td>Immutable</td>
      <td>실행 중인 서버를 수정하지 않고 교체하는 방식</td>
    </tr>
    <tr>
      <td>Health Check</td>
      <td>실제 서비스가 정상인지 검사</td>
    </tr>
    <tr>
      <td>Rollback</td>
      <td>이전 정상 버전으로 복귀</td>
    </tr>
  </tbody>
</table>

<h3 id="13-cicd를-공부할-때의-질문">13. CI/CD를 공부할 때의 질문</h3>

<p>Workflow 문법만 외우기보다 다음 흐름을 이해하는 것이 중요하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>누가 Trigger하는가?
→ 무엇을 Test하는가?
→ 무엇을 Build하는가?
→ 어떤 Artifact가 만들어지는가?
→ Artifact는 어디에 저장되는가?
→ 서버는 무엇을 가져오는가?
→ 어떻게 실행하는가?
→ 정상인지 어떻게 판단하는가?
→ 실패하면 어떻게 되돌리는가?
</code></pre></div></div>

<p>헷갈린다면 각 도구의 역할을 다음처럼 구분하면 된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GitHub          = 코드를 저장
Docker Registry = 이미지를 저장
Docker          = 이미지를 Container로 실행
CI/CD           = 이 전체 이동과 검증을 자동화
</code></pre></div></div>

<h3 id="14-다음-실습에서-확인할-것">14. 다음 실습에서 확인할 것</h3>

<ol>
  <li>Spring Boot 애플리케이션 작성</li>
  <li><code class="language-plaintext highlighter-rouge">./gradlew bootJar</code> 실행</li>
  <li>Dockerfile 작성</li>
  <li><code class="language-plaintext highlighter-rouge">docker build</code>와 <code class="language-plaintext highlighter-rouge">docker run</code> 실행</li>
  <li>Docker Hub 또는 GHCR에 Push</li>
  <li>GitHub Actions로 Test, Build, Docker Build, Docker Push 자동화</li>
  <li>Linux 서버에서 <code class="language-plaintext highlighter-rouge">docker pull</code> 실행</li>
  <li>컨테이너 실행 후 <code class="language-plaintext highlighter-rouge">/actuator/health</code> 확인</li>
  <li>v2 이미지 배포</li>
  <li>오류를 발생시킨 뒤 v1으로 Rollback</li>
</ol>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Git
→ CI
→ Artifact
→ Docker
→ Registry
→ Server
→ Container
→ Monitoring
→ Rollback
</code></pre></div></div>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="CI/CD" /><category term="Docker" /><category term="Health Check" /><category term="Rollback" /><category term="Deployment" /><category term="Observability" /><summary type="html"><![CDATA[컨테이너 배포 이후 필요한 Health Check, Rollback, 무중단 배포 전략과 모니터링을 정리한다.]]></summary></entry><entry><title type="html">CI/CD 기초 2편: Docker 이미지와 배포 파이프라인</title><link href="http://localhost:4000/devops/2026/08/17/docker-ci-cd-pipeline/" rel="alternate" type="text/html" title="CI/CD 기초 2편: Docker 이미지와 배포 파이프라인" /><published>2026-08-17T14:00:00+09:00</published><updated>2026-08-17T14:00:00+09:00</updated><id>http://localhost:4000/devops/2026/08/17/docker-ci-cd-pipeline</id><content type="html" xml:base="http://localhost:4000/devops/2026/08/17/docker-ci-cd-pipeline/"><![CDATA[<h2 id="docker-기반-cicd">Docker 기반 CI/CD</h2>

<p>먼저 Docker 기반 배포의 전체 흐름을 살펴보자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Developer
   │ git push
   ▼
GitHub Repository
   │
   ▼
CI Pipeline
   ├── Source Checkout
   ├── Dependency Install
   ├── Test
   ├── Build
   └── Docker Image Build
            │
            ▼
      Container Registry
      (Docker Hub, GHCR, ECR)
            │
            ▼
       Production Server
            │ docker pull
            ▼
      Docker Container
            │
            ▼
        Application
            │
            ▼
          Users
</code></pre></div></div>

<p>핵심 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source Code
→ Executable Artifact
→ Docker Image
→ Registry
→ Container
→ Running Service
</code></pre></div></div>

<h3 id="1-ci와-cd-다시-구분하기">1. CI와 CD 다시 구분하기</h3>

<h4 id="cicontinuous-integration">CI(Continuous Integration)</h4>

<p>개발자가 작성한 코드를 지속적으로 통합하고 검증하는 과정이다.</p>

<blockquote>
  <p>이 코드를 배포 가능한 상태로 신뢰해도 되는가?</p>
</blockquote>

<p>Spring Boot 프로젝트라면 다음과 같은 작업을 수행할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./gradlew <span class="nb">test</span>
./gradlew bootJar
docker build <span class="nt">-t</span> myapp:1.0 <span class="nb">.</span>
</code></pre></div></div>

<h4 id="cdcontinuous-delivery-또는-deployment">CD(Continuous Delivery 또는 Deployment)</h4>

<p><strong>Continuous Delivery</strong>는 배포 가능한 상태까지 자동화하고 실제 배포는 사람이 승인하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Code
→ Test
→ Build
→ Image
→ Registry
→ 배포 준비 완료
</code></pre></div></div>

<p><strong>Continuous Deployment</strong>는 Production 배포까지 완전히 자동화하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push
→ CI
→ Docker Image
→ Production
→ Health Check
→ 서비스
</code></pre></div></div>

<p>간단히 정리하면 CI는 배포 가능한 결과물을 만들고, CD는 그 결과물을 실제 환경에 배포한다.</p>

<h3 id="2-docker에서-cicd가-중요한-이유">2. Docker에서 CI/CD가 중요한 이유</h3>

<p>서버에 직접 접속해서 코드를 받고 환경을 구성하면 개발 환경과 운영 환경의 차이로 문제가 생길 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발자 PC
├── Java 21
├── PostgreSQL 16
└── Library A 2.1

Production
├── Java 17
├── PostgreSQL 14
└── Library A 1.9
</code></pre></div></div>

<p>바로 유명한 “내 컴퓨터에서는 되는데?” 문제다.</p>

<p>Docker는 애플리케이션과 실행에 필요한 환경을 하나의 이미지로 묶어 이 차이를 줄인다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
+ Runtime
+ Library
+ Dependency
+ Environment
= Docker Image
</code></pre></div></div>

<p>개발 및 검증 환경에서 확인한 이미지를 Production에서도 동일하게 실행할 수 있다.</p>

<h3 id="3-docker-image와-container">3. Docker Image와 Container</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile
→ docker build
→ Docker Image
→ docker run
→ Container
</code></pre></div></div>

<p><strong>Image</strong>는 실행에 필요한 파일을 담은 불변 배포 패키지다. <strong>Container</strong>는 이미지를 실제로 실행한 인스턴스다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image: myapp:1.0
       │
       ├─ Container A
       ├─ Container B
       └─ Container C
</code></pre></div></div>

<p>하나의 이미지에서 여러 컨테이너를 만들 수 있다. 객체 지향에 비유하면 Class와 Instance의 관계와 비슷하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Class     → Docker Image
Instance  → Docker Container
</code></pre></div></div>

<h3 id="4-dockerfile">4. Dockerfile</h3>

<p>Dockerfile은 Docker 이미지를 어떻게 만들지 선언하는 파일이다. Spring Boot라면 다음과 같이 작성할 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> eclipse-temurin:21-jre</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> build/libs/app.jar app.jar</span>

<span class="k">EXPOSE</span><span class="s"> 8080</span>

<span class="k">ENTRYPOINT</span><span class="s"> ["java", "-jar", "app.jar"]</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>명령</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">FROM</code></td>
      <td>사용할 기본 실행 환경</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">WORKDIR</code></td>
      <td>컨테이너 내부 작업 디렉터리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">COPY</code></td>
      <td>애플리케이션 파일 복사</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">EXPOSE</code></td>
      <td>애플리케이션이 사용하는 포트 명시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ENTRYPOINT</code></td>
      <td>컨테이너가 시작될 때 실행할 명령</td>
    </tr>
  </tbody>
</table>

<p>즉, Dockerfile을 보면 애플리케이션을 실행할 서버 환경이 어떻게 구성되는지 알 수 있다.</p>

<h3 id="5-build-artifact">5. Build Artifact</h3>

<p>Build Artifact는 소스 코드를 컴파일·테스트·패키징하는 과정에서 만들어지는 배포 가능한 결과물이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source Code
→ Compile
→ Test
→ Build
→ app.jar
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">app.jar</code>가 Build Artifact다. Docker를 사용하면 한 단계 더 나아간다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source Code
→ JAR
→ Docker Image
</code></pre></div></div>

<p>Docker 기반 시스템에서는 Docker 이미지를 최종 Release Artifact로 사용할 수 있다.</p>

<h3 id="6-container-registry">6. Container Registry</h3>

<p>Release Artifact인 Docker 이미지는 어딘가에 저장해야 한다. 이때 사용하는 저장소가 Container Registry다.</p>

<ul>
  <li>Docker Hub</li>
  <li>GitHub Container Registry(GHCR)</li>
  <li>AWS ECR</li>
  <li>Google Artifact Registry</li>
  <li>Azure Container Registry</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CI Server
→ docker push
→ Registry
→ docker pull
→ Production Server
</code></pre></div></div>

<p>GitHub가 소스 코드 저장소라면 Registry는 Docker 이미지 저장소다.</p>

<h3 id="7-실제-데이터-흐름">7. 실제 데이터 흐름</h3>

<p>개발자가 Spring Boot 코드를 수정하고 Push하면 GitHub Actions가 CI를 실행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GitHub Actions Runner
→ Checkout
→ JDK 설치
→ ./gradlew test
→ ./gradlew bootJar
→ docker build
</code></pre></div></div>

<p>만들어진 이미지를 Registry에 Push한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker push registry.example.com/myapp:1.4.2
</code></pre></div></div>

<p>Registry에는 여러 버전의 이미지가 존재할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>myapp
├── 1.4.0
├── 1.4.1
├── 1.4.2
└── latest
</code></pre></div></div>

<p>Production Server는 지정한 버전의 이미지를 받아 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker pull registry.example.com/myapp:1.4.2
docker run registry.example.com/myapp:1.4.2
</code></pre></div></div>

<p>최종 요청 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User
→ Internet
→ Load Balancer / Nginx
→ Container
→ Spring Boot
→ Database
</code></pre></div></div>

<h3 id="8-build-once-deploy-many">8. Build Once, Deploy Many</h3>

<p>CI/CD에서 중요한 원칙은 <strong>한 번 빌드한 결과물을 여러 환경에 배포하는 것</strong>이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dev용 Image Build
Stage용 Image Build
Prod용 Image Build
</code></pre></div></div>

<p>환경마다 이미지를 다시 빌드하면 Staging에서 검증한 이미지와 Production에 올린 이미지가 달라질 수 있다. 따라서 한 번 만든 동일한 이미지를 여러 환경에서 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source
  ↓
Build
  ↓
Image: abc123
  ├── Dev
  ├── Staging
  └── Production
</code></pre></div></div>

<h3 id="9-환경-설정-분리">9. 환경 설정 분리</h3>

<p>개발, Staging, Production의 DB 주소와 설정은 서로 다르다. 이미지는 동일하게 유지하고 환경별 값은 컨테이너 실행 시 외부에서 주입한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">SPRING_PROFILES_ACTIVE</span><span class="o">=</span>prod <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">DB_HOST</span><span class="o">=</span>prod-db <span class="se">\</span>
  myapp:1.4.2
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>              Docker Image
              myapp:1.4.2
                   │
       ┌───────────┼───────────┐
       ↓           ↓           ↓
 DEV Container  STAGE Container  PROD Container
 DB=dev         DB=stage         DB=prod
</code></pre></div></div>

<p>이미지와 환경 설정을 분리해서 생각해야 한다.</p>

<h3 id="10-secret은-이미지에-넣지-않기">10. Secret은 이미지에 넣지 않기</h3>

<p>다음처럼 비밀번호나 API Key를 이미지에 넣으면 이미지를 받은 사람이 값을 확인할 수 있다.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">ENV</span><span class="s"> DB_PASSWORD=123456</span>
<span class="k">ENV</span><span class="s"> AWS_SECRET_KEY=example</span>
</code></pre></div></div>

<p>다음 값은 이미지 외부의 Secret 관리 시스템에서 관리하고 배포 시점에 주입해야 한다.</p>

<ul>
  <li>DB Password</li>
  <li>API Key</li>
  <li>AWS Secret Key</li>
  <li>JWT Secret</li>
</ul>

<p>대표적인 관리 방법으로 GitHub Actions Secrets, AWS Secrets Manager, Kubernetes Secrets, Vault 등이 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Secret Storage
→ Deployment
→ Container Environment
</code></pre></div></div>

<h3 id="11-image-tagging">11. Image Tagging</h3>

<p><code class="language-plaintext highlighter-rouge">myapp:latest</code>만 사용하면 현재 실행 중인 이미지가 정확히 어떤 버전인지 추적하기 어렵다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>오늘 latest  → v1.5
내일 latest  → v1.6
</code></pre></div></div>

<p>따라서 명시적인 버전이나 Git commit SHA를 태그로 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>myapp:1.4.2
myapp:a8df329
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Production Error
→ 실행 중인 Image 확인
→ myapp:a8df329
→ Git commit a8df329 확인
→ 정확한 Source Code 추적
</code></pre></div></div>

<p>이처럼 배포 결과물과 소스 코드의 관계를 추적할 수 있는 성질을 <strong>Traceability(추적 가능성)</strong>라고 한다.</p>

<p>다음 글에서는 배포 이후의 Health Check, Rollback과 무중단 배포 전략을 살펴본다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="CI/CD" /><category term="Docker" /><category term="GitHub Actions" /><category term="Container Registry" /><category term="Deployment" /><summary type="html"><![CDATA[Docker 기반 CI/CD의 흐름과 이미지, 컨테이너, Registry, 환경 설정 및 태그 관리 원칙을 정리한다.]]></summary></entry><entry><title type="html">CI/CD 기초 1편: 개념과 GitHub Actions</title><link href="http://localhost:4000/devops/2026/08/17/ci-cd-basics/" rel="alternate" type="text/html" title="CI/CD 기초 1편: 개념과 GitHub Actions" /><published>2026-08-17T13:00:00+09:00</published><updated>2026-08-17T13:00:00+09:00</updated><id>http://localhost:4000/devops/2026/08/17/ci-cd-basics</id><content type="html" xml:base="http://localhost:4000/devops/2026/08/17/ci-cd-basics/"><![CDATA[<h2 id="cicd가-뭔가요">CI/CD가 뭔가요?</h2>

<p>쉽게 말하면 코드를 GitHub에 올린 다음, 사람이 반복하던 검사와 배포를 자동화하는 것이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>코드 작성
  ↓
GitHub에 Push
  ↓
자동 테스트와 빌드                 ← CI
  ↓
통과한 프로그램을 서버에 배포     ← CD
  ↓
실제 서비스 동작 확인
</code></pre></div></div>

<h3 id="1-cicontinuous-integration">1. CI(Continuous Integration)</h3>

<p>한국어로는 <strong>지속적 통합</strong>이다. 여러 사람이 작성한 코드나 새로 작성한 코드가 기존 프로젝트와 잘 합쳐지는지 자동으로 검사한다.</p>

<p>현재 프로젝트인 Pi-monitor를 예로 들면 다음 작업이 CI에 해당한다.</p>

<h4 id="frontend">Frontend</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm ci
npm <span class="nb">test
</span>npm run build
</code></pre></div></div>

<h4 id="backend">Backend</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./mvnw <span class="nb">test</span>
./mvnw package
</code></pre></div></div>

<p>잘못된 코드를 GitHub에 올렸을 때 프론트엔드 테스트, Vue 빌드, Java 컴파일 또는 백엔드 테스트가 실패할 수 있다. CI가 없으면 사람이 매번 직접 명령을 실행해야 한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>frontend
npm <span class="nb">test
</span>npm run build

<span class="nb">cd</span> ../backend
./mvnw <span class="nb">test</span>
</code></pre></div></div>

<p>CI를 구성하면 GitHub가 별도의 실행 환경에서 이 명령들을 자동으로 실행한다.</p>

<h3 id="2-cdcontinuous-delivery-또는-deployment">2. CD(Continuous Delivery 또는 Deployment)</h3>

<p>CD에는 두 가지 의미가 있다.</p>

<ul>
  <li><strong>Continuous Delivery</strong>: 배포 가능한 상태까지 자동으로 준비하고 실제 배포는 사람이 승인한다.</li>
  <li><strong>Continuous Deployment</strong>: 테스트가 통과하면 실제 서버 배포까지 자동으로 진행한다.</li>
</ul>

<p>배포 시스템을 처음 구성한다면 사람이 마지막으로 승인하는 Continuous Delivery 방식이 안전하다.</p>

<h4 id="continuous-delivery-예시">Continuous Delivery 예시</h4>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>main에 Push
→ 테스트 및 빌드
→ 배포 준비 완료
→ 사람이 Deploy 버튼 클릭
</code></pre></div></div>

<h4 id="continuous-deployment-예시">Continuous Deployment 예시</h4>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>main에 Push
→ 테스트 및 빌드 통과
→ 라즈베리파이 자동 업데이트
→ 컨테이너 재시작
</code></pre></div></div>

<h3 id="3-github-actions">3. GitHub Actions</h3>

<p>GitHub Actions는 GitHub에서 CI/CD 작업을 실행해 주는 기능이다. 저장소에 다음과 같은 Workflow 파일을 만든다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.github/
└── workflows/
    └── ci.yml
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ci.yml</code>에는 언제, 어떤 운영체제에서, 어떤 명령을 실행할지 정의한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">CI</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
  <span class="na">pull_request</span><span class="pi">:</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">frontend</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>

    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">소스 코드 가져오기</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v4</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Node.js 설치</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-node@v4</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">node-version</span><span class="pi">:</span> <span class="m">22</span>
          <span class="na">cache</span><span class="pi">:</span> <span class="s">npm</span>
          <span class="na">cache-dependency-path</span><span class="pi">:</span> <span class="s">frontend/package-lock.json</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">패키지 설치</span>
        <span class="na">working-directory</span><span class="pi">:</span> <span class="s">frontend</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">npm ci</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">테스트</span>
        <span class="na">working-directory</span><span class="pi">:</span> <span class="s">frontend</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">npm test</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">빌드</span>
        <span class="na">working-directory</span><span class="pi">:</span> <span class="s">frontend</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">npm run build</span>
</code></pre></div></div>

<p>이 파일을 Push하면 GitHub가 Ubuntu 가상 환경을 만들고 위 명령을 실행한다.</p>

<h3 id="4-알아두면-좋은-용어">4. 알아두면 좋은 용어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Workflow</td>
      <td>CI/CD 전체 작업을 정의한 파일</td>
    </tr>
    <tr>
      <td>Trigger</td>
      <td>작업을 시작시키는 사건. 예: Push, Pull Request</td>
    </tr>
    <tr>
      <td>Job</td>
      <td>테스트, 빌드처럼 하나로 묶인 작업</td>
    </tr>
    <tr>
      <td>Step</td>
      <td>Job 안에서 실행하는 개별 명령</td>
    </tr>
    <tr>
      <td>Runner</td>
      <td>실제 명령을 실행하는 컴퓨터</td>
    </tr>
    <tr>
      <td>Artifact</td>
      <td>빌드해서 만들어진 결과물</td>
    </tr>
    <tr>
      <td>Secret</td>
      <td>비밀번호, 토큰 같은 비공개 값</td>
    </tr>
    <tr>
      <td>Deploy</td>
      <td>완성된 프로그램을 실제 서버에 반영하는 작업</td>
    </tr>
    <tr>
      <td>Rollback</td>
      <td>문제가 생겼을 때 이전 버전으로 복구하는 작업</td>
    </tr>
  </tbody>
</table>

<h3 id="5-내-프로젝트로-보는-cicd">5. 내 프로젝트로 보는 CI/CD</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GitHub Push
  │
  ├─ Frontend CI
  │    ├─ npm ci
  │    ├─ npm test
  │    └─ npm run build
  │
  ├─ Backend CI
  │    ├─ Maven test
  │    └─ Spring Boot package
  │
  └─ 모든 검사 통과
       │
       ├─ Vercel이 프론트엔드 자동 배포
       └─ Raspberry Pi에 백엔드 배포
</code></pre></div></div>

<p>프론트엔드는 Vercel과 GitHub 저장소가 연결되어 있어 기본적인 CD를 사용하고 있다. <code class="language-plaintext highlighter-rouge">main</code>에 Push하면 Vercel이 자동으로 빌드하고 배포한다.</p>

<p>반면 라즈베리파이 백엔드는 다음 명령으로 수동 배포 중이므로 이 부분을 자동화해야 한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git pull
./scripts/pi-compose.sh up <span class="nt">-d</span> <span class="nt">--build</span>
</code></pre></div></div>

<h3 id="6-추천-학습-순서">6. 추천 학습 순서</h3>

<ol>
  <li>GitHub Actions로 프론트엔드 테스트와 빌드 자동화</li>
  <li>백엔드 테스트와 빌드 추가</li>
  <li>Pull Request에서 CI가 통과해야 Merge하도록 설정</li>
  <li>Vercel 자동 배포 과정 이해</li>
  <li>라즈베리파이 자동 배포 테스트</li>
</ol>

<h3 id="7-라즈베리파이-cd-방법">7. 라즈베리파이 CD 방법</h3>

<h4 id="ssh로-배포">SSH로 배포</h4>

<p>GitHub Actions가 라즈베리파이에 SSH로 접속해 배포 명령을 실행한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GitHub Actions
→ SSH 접속
→ git pull
→ Docker 이미지 빌드
→ 컨테이너 교체
→ 상태 확인
</code></pre></div></div>

<p>다만 라즈베리파이를 외부에서 SSH로 접근할 수 있어야 한다.</p>

<h4 id="self-hosted-runner-사용">Self-hosted Runner 사용</h4>

<p>라즈베리파이에 GitHub Actions Runner를 설치하면 GitHub가 보낸 작업을 라즈베리파이가 직접 실행한다. 외부 SSH 포트를 열 필요는 없지만, 공개 저장소나 신뢰할 수 없는 PR의 코드가 실행되지 않도록 주의해야 한다.</p>

<h4 id="이미지-기반-배포">이미지 기반 배포</h4>

<p>GitHub Actions가 Docker 이미지를 빌드해 Container Registry에 올리고, 라즈베리파이는 완성된 이미지만 내려받는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GitHub Actions
→ Docker 이미지 빌드
→ GitHub Container Registry에 Push
→ Raspberry Pi가 이미지 Pull
→ 컨테이너 교체
</code></pre></div></div>

<p>가장 체계적인 방식이며, 다음 글에서 Docker 기반 CI/CD 흐름을 더 자세히 살펴본다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="CI/CD" /><category term="GitHub Actions" /><category term="Docker" /><category term="Deployment" /><summary type="html"><![CDATA[CI와 CD의 차이, GitHub Actions의 기본 구조, 라즈베리파이 배포 방법을 정리한다.]]></summary></entry><entry><title type="html">Docker 기초 1편: Image, Container와 Compose</title><link href="http://localhost:4000/devops/2026/08/17/docker-basics/" rel="alternate" type="text/html" title="Docker 기초 1편: Image, Container와 Compose" /><published>2026-08-17T12:00:00+09:00</published><updated>2026-08-17T12:00:00+09:00</updated><id>http://localhost:4000/devops/2026/08/17/docker-basics</id><content type="html" xml:base="http://localhost:4000/devops/2026/08/17/docker-basics/"><![CDATA[<h2 id="1-docker가-왜-필요할까">1. Docker가 왜 필요할까?</h2>

<p>뭐.. 개발은 내 컴퓨터에서만 하고, 사용도 내 컴퓨터에서만 한다면 사실 이 게시물을 읽을 필요가 없다.</p>

<p>그런데 아시다시피 개발을 하면 배포를 해야한다.</p>

<p>여기서부터 문제는 시작된다.</p>

<p>내가 뭐 SpringBoot 프로젝트를 한다고 치자</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Java 21
MySQL 8
특정 환경변수
특정 라이브러리
</code></pre></div></div>

<p>내 컴퓨터는 세팅이 잘 되어있어서 내가 만들어놓은 프로젝트가 잘 돌아갈 것이다.
(근데 당연한 소리다. 내 컴퓨터에서 돌아가지도 않는데 개발을 어캐함???? ㅋㅋㅋㅋ)</p>

<p>그렇지만 내 개발을 서버에 올릴 떄는 이야기가 달라진다.</p>

<p>내가 올려야 할 서버는 내 컴퓨터와 사양과 환경이 같을까? 그렇리는 없다.</p>

<p>일례로 서버 환경이</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Java 17
MySQL 5
환경변수 없음
라이브러리 버전 다름
</code></pre></div></div>

<p>뭐 이렇다고 치면</p>

<p>실행이 될리가 없다. 애초에 개발에 사용된 환경과 서버에서 사용된 환경이 다르니까.</p>

<blockquote>
  <p>이럴 때 하는 말이 하나있다. “내 컴에서는 되던데?”</p>
</blockquote>

<p>(아무런 도움이 되지 않는 말이다..ㅋㅋ)</p>

<p>아무튼 그렇다면 문제를 어떻게 해결해야겠는가?</p>

<p>애플리케이션과 실행 환경을 같이 묶어서 실행하면 되는 것이다.</p>

<p>Docker가 이 문제를 해결해준다.</p>

<p>일단 실행환경과 코드를 묶어서 이미지로 만들어주고</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Boot Application
        +
Java Runtime
        +
Library
        +
설정
        ↓
Docker Image
</code></pre></div></div>

<p>서버에서는</p>

<p>도커 이미지를 바탕으로</p>

<p>돌려서</p>

<p>Container를 돌리는 것이다.</p>

<h2 id="2-docker의-가장-중요한-개념">2. Docker의 가장 중요한 개념</h2>

<p>Docker를 처음 공부한다면 아래 흐름을 먼저 외우는 게 좋다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile
-&gt;
docker build
-&gt;
Image
-&gt;
docker run
-&gt;
Container
</code></pre></div></div>

<h2 id="3-dockerfile">3. Dockerfile</h2>

<blockquote>
  <p>Dockerfile은 Docker Image를 만드는 설계도</p>
</blockquote>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> eclipse-temurin:21-jre</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> build/libs/app.jar app.jar</span>

<span class="k">EXPOSE</span><span class="s"> 8080</span>

<span class="k">ENTRYPOINT</span><span class="s"> ["java", "-jar", "app.jar"]</span>
</code></pre></div></div>

<p>하나씩 보면:</p>

<p>FROM eclipse-temurin:21-jre</p>

<p>Java 21이 설치된 환경을 기반으로 사용</p>

<p>WORKDIR /app</p>

<p>Container 내부의 작업 위치를 /app으로 저장한다.</p>

<p>COPY build/libs/app.jar app.jar</p>

<p>내 컴퓨통에 있는 jar 파일을 Container 이미지 내부로 복사한다.</p>

<p>EXPOSE 8080</p>

<p>이 애플리케이션이 8080 Port</p>

<p>ENTRYPOINT [“java”, “-jar”, “app.jar”]</p>

<p>Container가 시작할 떄 실행할 명령</p>

<h4 id="요약">요약</h4>

<blockquote>
  <p>이 어플리케이션을 실행하기 위해 어떤 환경을 만들고, 어떤 파일을 넣고, 어떤 명령으로 실행할지 선언하는 파일</p>
</blockquote>

<h2 id="4-docker-image">4. Docker Image</h2>

<p>Dockerfile을 이용해서 Image를 만든다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> myapp:1.0,
</code></pre></div></div>

<p>그러면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile
-&gt;
docker build
-&gt;
myapp:1.0
</code></pre></div></div>

<p>이라는 Image가 만들어진다.</p>

<p>Image는 쉽게 말하면</p>

<blockquote>
  <p>어플리케이션 실행에 필요한 환경을 포함한 환경 패키지</p>
</blockquote>

<p>라고 보면 된다.</p>

<p>여기서 중요한 점은 Image 자체는 실행중인 프로그램이 아니라는 것.</p>

<h2 id="5-docker-container">5. Docker Container</h2>

<p>Image를 실제로 실행하면 Container가 된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run myapp:1.0
</code></pre></div></div>

<p>구조는</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image
myapp:1.0
-&gt; docker run
container
</code></pre></div></div>

<p>이게 비유하면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Java class
-&gt; new
object
</code></pre></div></div>

<p>Docker에서는</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Image
-&gt; docker run
Container
</code></pre></div></div>

<p>(마치 OOP의 느낌, 클래스로 여러 개의 인스턴스를 만드는 것을 생각하면 쉽게 생각될 듯 하다. )</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>             myapp:1.0
                 │
        ┌────────┼────────┐
        ↓        ↓        ↓
Container A Container B Container C
</code></pre></div></div>

<h2 id="6-image와-container의-차이">6. Image와 Container의 차이</h2>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Dockerfile</td>
      <td>Image 만드는 설계도</td>
    </tr>
    <tr>
      <td>Image</td>
      <td>실행 가능한 패키지</td>
    </tr>
    <tr>
      <td>Container</td>
      <td>Image가 실제 실행된 상태</td>
    </tr>
  </tbody>
</table>

<p>비유하자면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile
= 붕어빵 틀을 만드는 설계

Image
= 붕어빵 틀

Container
= 실제 만들어진 붕어빵
</code></pre></div></div>

<h2 id="7-docker-명령어-기본-흐름">7. Docker 명령어 기본 흐름</h2>

<p>Docker를 사용하면 보통 이런 순서</p>

<h3 id="image-만들기">Image 만들기</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> myapp:1.0 <span class="nb">.</span>
</code></pre></div></div>

<h3 id="image-확인">Image 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker images
</code></pre></div></div>

<h3 id="container-확인">Container 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run myapp:1.0
</code></pre></div></div>

<p>-&gt; web_server라면 Port도 연결해야함</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-p</span> 8080:8080 myapp:1.0
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>내 컴퓨터 8080
      ↓
Container 8080
</code></pre></div></div>

<h2 id="8-근데-왜-port-mapping을-해야할까">8. 근데 왜 PORT mapping을 해야할까?</h2>

<p>Container는 외부와 분리된 환경이므로 내 컴퓨터의 내부 포트와 연결해야 한다.</p>

<p>예를 들어 Spring Boot가 Container 안에서: localhost:8080</p>

<p>으로 실행된다. 그렇다고 내 컴퓨터의 8080과 자동으로 연결되는 것은 아님</p>

<p><code class="language-plaintext highlighter-rouge">docker run -p 8080:8080 myapp</code> 처럼 연결</p>

<p>형식은: -p HostPort:ContainerPort</p>

<p>예를 들어:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-p</span> 9000:8080 myapp
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Browser

localhost:9000
-&gt; Host 9000
-&gt; Container 8080
-&gt; Spring Boot
</code></pre></div></div>

<h2 id="9-container는-격리된-환경">9. Container는 격리된 환경</h2>

<p>Docker의 중요한 특징이다. 컨테이너끼리 격리되어있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container A
Java 17

Container B
Java 21

Container C
Python 3.13
</code></pre></div></div>

<p>를 같은 컴퓨터에서 동시에 실행이 가능하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host Operating System
        │
        ├── Container A
        │     Java 17
        │
        ├── Container B
        │     Java 21
        │
        └── Container C
              Python
</code></pre></div></div>

<p>이런 식으로 격리되어 있어서 프로젝트마다 분리해서 사용하기가 쉽다.</p>

<h2 id="10-근데-이러면-vmvirtual-machine하고-뭐가-다른거임">10. 근데 이러면 VM(Virtual Machine)하고 뭐가 다른거임?</h2>

<p>일단 VM부터 살펴보도록하자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hardware
   ↓
Host OS
   ↓
Hypervisor
   ↓
Guest OS
   ↓
Application
</code></pre></div></div>

<p>VM은 기본적으로 기본 OS 위에 가상 OS를 띄우는 방식으로 굴러간다.</p>

<p>그러나 Docker의 경우에는</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hardware
   ↓
Host OS
   ↓
Docker Engine
   ↓
Container
   ↓
Application
</code></pre></div></div>

<p>Docker Container는 VM처럼 OS 전체를 하나 더 띄우지 않는다.</p>

<p>Host OS의 Kernel을 공유</p>

<p>시작이 빠르고
용량이 작고
리소스를 덜 사용하는</p>

<p>장점이 있다.</p>

<h2 id="11-docker-image는-layer-구조">11. Docker Image는 Layer 구조</h2>

<blockquote>
  <p>Docker Image 내부는 하나의 거대한 파일이라기보다 여러 Layer로 구성</p>
</blockquote>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> eclipse-temurin:21-jre</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> app.jar app.jar</span>
</code></pre></div></div>

<p>개념적으로</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Layer 3

app.jar

Layer 2

WORKDIR /app

Layer

JAVA 21 Runtime
</code></pre></div></div>

<p>그래서 변경되지 않은 Layer는 재사용할 수 있다.</p>

<p>Docker Layer Cache</p>

<p>예를 들어</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Java Runtime -&gt; 변경 없음

app.jar -&gt; 변경됨
</code></pre></div></div>

<p>이라면 전체를 처음부터 다시 만들지 않고, 변경된 위주로만 Build하면 된다.</p>

<p>이 특성을 이용해서 CI/CD에서 Build 속도를 높일 수 있다.</p>

<h2 id="12-docker-registry">12. Docker Registry</h2>

<p>Image를 내 컴퓨터에만 가지고 있으면 서버가 재사용할 수가 없다.</p>

<p>따라서 Image를 저장하는 중앙 저장소가 필요하다.</p>

<p>이를 위한 위한 저장소가 다행히도 Docker Registry 라고 존재한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Hub
GitHub Container Registry
AWS ECR
Google Artifact Registry
</code></pre></div></div>

<p>이게 목록이다.</p>

<p>구조는 아래와 같다</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Developer
    ↓
docker build
    ↓
Docker Image
    ↓
docker push
    ↓
Docker Registry
    ↓
docker pull
    ↓
Production Server
</code></pre></div></div>

<h2 id="13-github와-docker-registry-차이">13. GitHub와 Docker Registry 차이</h2>

<p>이 둘을 구분해야할 필요가 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GitHub
    ↓
Source Code 저장

Docker Registry
    ↓
Docker Image 저장
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GitHub
= 코드 저장소

Docker Hub
= 실행 패키지 저장소
</code></pre></div></div>

<p>이렇게 정리해서 생각하면 된다.</p>

<h2 id="14-volume">14. Volume</h2>

<p>근데 저장은 어떻게 하는 걸까?</p>

<p>Container 안에 파일을 저장했다고 하자.</p>

<p>만약에 Container 안에 저장할 경우에는</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container
   ↓
data.txt
</code></pre></div></div>

<p>이런식으로 하면 Container가 작동하는 동안에는 저장이 되겠지만,</p>

<p>Container가 종료되는 경우에는, 내부 데이터가 삭제될 것이다.</p>

<p>근데 여기에 DB 데이터가 담겨있다면?</p>

<p>음</p>

<p>유감이라고 할 수 있다.</p>

<p>하지만 개발자성님들은 최강이기 때문에</p>

<blockquote>
  <p>VOULME</p>
</blockquote>

<p>이라는 기능을 구현해놓았다.</p>

<p>그래서 사용하는 것이 Volume</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container
     │
     ▼
Docker Volume
     │
     ▼
Data
</code></pre></div></div>

<p>이런 식으로 데이터를 유지할 수 있다.</p>

<p>예를 들어 MySQL를 Docker로 실행한다고 하자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MySQL Container
       ↓
Volume
       ↓
Database Files
</code></pre></div></div>

<p>이런식으로 구조를 유지할 수 있다.</p>

<h2 id="15-docker-network">15. Docker Network</h2>

<p>Container 끼리 통신도 된다!</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Boot
     ↓
MySQL
</code></pre></div></div>

<p>이렇게 구현했는데</p>

<p>둘 다 Container라면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Network

Spring Container
        │
        ↓
MySQL Container
</code></pre></div></div>

<p>이런식으롤 연결한다.</p>

<p>다만, 컨테이너끼리 통신에서는 localhost는 사용 불가하다.</p>

<p>각 컨테이너의 localhost는 자기 자신을 가리키기 때문이다.</p>

<p>따라서 네트워크 이름을 Container 또는 Service 이름으로 접근할 수 있다.</p>

<h2 id="16-docker-compose">16. Docker Compose</h2>

<p>Container가 하나뿐이라면 명령어로 실행할 수 있다.</p>

<p>근데 아시다시피 프로젝트에서는 여러가지 기능을 구현해야하고, 그로 인해 컨테이너를 여러개 구동해야 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Boot
MySQL
Redis
Nginx
</code></pre></div></div>

<p>처럼 여러 개가 된다.</p>

<p>매번</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run ...
docker run ...
docker run ...
docker run ...
</code></pre></div></div>

<p>이거를 언제 치고 있겠음?</p>

<p>그래서 Docker Compose를 사용한다 .</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">backend</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">.</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8080:8080"</span>

  <span class="na">mysql</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">mysql:8</span>
    <span class="na">environment</span><span class="pi">:</span>
      <span class="na">MYSQL_ROOT_PASSWORD</span><span class="pi">:</span> <span class="s">password</span>

  <span class="na">redis</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">redis:7</span>
</code></pre></div></div>

<p>이렇게 해놓고</p>

<p><code class="language-plaintext highlighter-rouge">docker compose up</code> 때리면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Boot
MySQL
Redis
</code></pre></div></div>

<p>가 한꺼번에 실행되서 매우 편리하다.</p>

<blockquote>
  <p>Docker Compose를 통해서 여러 Container의 실행 방법과 연결 관계를 하나의 파일로 정의한다.</p>
</blockquote>

<h2 id="17-docker와-cicd가-연결되는-순간">17. Docker와 CI/CD가 연결되는 순간</h2>

<p>소스 코드를 Github에 올리는 순간부터 시작한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push
</code></pre></div></div>

<p>이후 CI가 실행된다</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Developer
-&gt;
Github
-&gt;
Github Actions
-&gt;
Tests
-&gt;
Spring Boot build
-&gt;
app.jar
-&gt;
Docker build
-&gt;
Docker Image
-&gt;
Docker push
-&gt;
Docker Registry
</code></pre></div></div>

<p>그 다음에 CD가 이뤄진다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Production Server
-&gt;
Docker pull
-&gt;
Docker Image
-&gt;
Docker run
-&gt;
Container
-&gt;
Spring Boot 실행
</code></pre></div></div>

<h2 id="18-docker를-배포-단위로-사용">18. Docker를 배포 단위로 사용</h2>

<p>Docker가 없는 경우</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Source Code
-&gt;
Server 접속
-&gt;
Java 설치
-&gt;
Dependency 설치
-&gt;
설정
-&gt;
실행
</code></pre></div></div>

<p>Docker를 사용하면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Image
-&gt;
Server
-&gt;
Docker run
</code></pre></div></div>

<p>으로 단순화</p>

<p>그래서 중요한 개념</p>

<blockquote>
  <p>배포 단위를 Source Code가 아니라 Docker Image로 만든다.</p>
</blockquote>

<h2 id="19-docker의-핵심-철학">19. Docker의 핵심 철학</h2>

<p>Docker를 공부할 때 이 개념도 굉장히 중요하다.</p>

<p>Container가 이상해졌다고 안에 들어가서:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vi ...
apt <span class="nb">install</span> ...
</code></pre></div></div>

<p>하면서 직접 수정하는 방식은 좋지 않다.</p>

<p>대신:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile 수정
      ↓
새 Image Build
      ↓
새 Container 생성
      ↓
기존 Container 제거
</code></pre></div></div>

<p>한다.</p>

<p>즉:</p>

<p>수리해서 계속 사용</p>

<p>하는 것이 아니라:</p>

<p>문제가 있으면 새것으로 교체</p>

<p>하는 방향이다.</p>

<p>이것을 Immutable Infrastructure 사고방식이라고 한다.</p>

<h2 id="20-기억하면-되는-구조">20. 기억하면 되는 구조</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                      Dockerfile
                           │
                           │ docker build
                           ▼
                     Docker Image
                           │
             ┌─────────────┴─────────────┐
             │                           │
         docker run                 docker push
             │                           │
             ▼                           ▼
         Container               Docker Registry
                                         │
                                     docker pull
                                         │
                                         ▼
                                  Production Server
                                         │
                                     docker run
                                         │
                                         ▼
                                     Container
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Docker</td>
      <td>Container를 만들고 실행하는 플랫폼</td>
    </tr>
    <tr>
      <td>Dockerfile</td>
      <td>Image 제작 방법</td>
    </tr>
    <tr>
      <td>Image</td>
      <td>실행 가능한 패키지</td>
    </tr>
    <tr>
      <td>Container</td>
      <td>Image를 실행한 프로세스 환경</td>
    </tr>
    <tr>
      <td>Registry</td>
      <td>Image 저장소</td>
    </tr>
    <tr>
      <td>Volume</td>
      <td>Container 외부 데이터 저장</td>
    </tr>
    <tr>
      <td>Network</td>
      <td>Container 간 통신</td>
    </tr>
    <tr>
      <td>Compose</td>
      <td>여러 Container를 한 번에 관리</td>
    </tr>
  </tbody>
</table>

<h2 id="21-한-문장씩-정리하면-된다">21. 한 문장씩 정리하면 된다.</h2>

<ul>
  <li><strong>Docker:</strong> 애플리케이션과 실행 환경을 Container 라는 격리된 환경 패키징하고 실행하기 위한 플랫폼</li>
  <li><strong>Dockerfile:</strong> Docker Image를 어떤 환경과 파일, 명령으로 구성할지 선언하는 파일</li>
  <li><strong>Image:</strong> 애플리케이션을 실행하기 위한 필요한 환경과 파일을 포함한 불변 패키지</li>
  <li><strong>Container:</strong> Docker Image를 기반으로 실제 실행되고 있는 격리된 Runtime 환경</li>
  <li><strong>Registry:</strong> Docker를 저장하고 배포하기 위한 저장소</li>
  <li><strong>Volume:</strong> Container가 삭제되더라도 유지해야 하는 데이터를 Container 밖에 저장하는 공간</li>
  <li><strong>Docker Compose:</strong> 여러 Docker Container의 실행 환경과 연결 관계를 하나의 설정 파일로 관리하는 도구</li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="Docker" /><category term="Dockerfile" /><category term="Container" /><category term="Docker Compose" /><category term="DevOps" /><summary type="html"><![CDATA[Docker가 필요한 이유부터 Dockerfile, Image, Container, Volume, Network와 Compose까지 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 5: EntityManager와 영속성 컨텍스트</title><link href="http://localhost:4000/java/spring/2026/08/17/jpa-entity-manager-persistence-context/" rel="alternate" type="text/html" title="Spring 기초 Part 5: EntityManager와 영속성 컨텍스트" /><published>2026-08-17T09:00:00+09:00</published><updated>2026-08-17T09:00:00+09:00</updated><id>http://localhost:4000/java/spring/2026/08/17/jpa-entity-manager-persistence-context</id><content type="html" xml:base="http://localhost:4000/java/spring/2026/08/17/jpa-entity-manager-persistence-context/"><![CDATA[<h2 id="spring-기초-part-5-entitymanager와-영속성-컨텍스트">Spring 기초 Part 5: EntityManager와 영속성 컨텍스트</h2>

<blockquote>
  <p>EntityManager는 Entity를 영속성 컨텍스트에서 관리하고, 데이터베이스 작업을 JPA 구현체에 요청하는 핵심 인터페이스다.</p>
</blockquote>

<p>두 개념의 관계를 먼저 기억하자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>EntityManager
→ 영속성 컨텍스트를 사용해 Entity를 관리하는 관리자

영속성 컨텍스트
→ Entity의 상태를 보관하고 추적하는 논리적인 작업 공간
</code></pre></div></div>

<hr />

<h3 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>EntityManager</td>
      <td>Entity의 저장, 조회, 삭제와 상태 관리를 담당하는 JPA 인터페이스</td>
      <td>Entity 관리자</td>
    </tr>
    <tr>
      <td>영속성</td>
      <td>애플리케이션이 종료돼도 데이터가 DB에 남는 성질</td>
      <td>데이터를 계속 보관</td>
    </tr>
    <tr>
      <td>영속성 컨텍스트</td>
      <td>JPA가 Entity를 관리하는 논리적인 공간</td>
      <td>Entity 작업실</td>
    </tr>
    <tr>
      <td>1차 캐시</td>
      <td>영속성 컨텍스트 내부의 Entity 저장 공간</td>
      <td>같은 트랜잭션의 조회 결과 보관</td>
    </tr>
    <tr>
      <td>스냅샷</td>
      <td>Entity를 처음 관리할 때 저장한 상태</td>
      <td>변경 전 값</td>
    </tr>
    <tr>
      <td>변경 감지</td>
      <td>현재 상태와 스냅샷을 비교해 변경을 찾는 기능</td>
      <td>수정된 필드를 자동 확인</td>
    </tr>
    <tr>
      <td>쓰기 지연</td>
      <td>쓰기 SQL을 모아두었다가 flush 시점에 전달하는 방식</td>
      <td>SQL을 바로 보내지 않고 준비</td>
    </tr>
    <tr>
      <td>flush</td>
      <td>영속성 컨텍스트의 변경을 SQL로 DB에 전달</td>
      <td>SQL 전송</td>
    </tr>
    <tr>
      <td>commit</td>
      <td>트랜잭션의 변경 내용을 최종 확정</td>
      <td>저장 확정</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="2-entitymanager가-하는-일">2. EntityManager가 하는 일</h3>

<p>EntityManager의 대표 메서드는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>메서드</th>
      <th>역할</th>
      <th>SQL 관점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">persist(entity)</code></td>
      <td>새 Entity를 영속 상태로 만듦</td>
      <td>보통 <code class="language-plaintext highlighter-rouge">INSERT</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">find(type, id)</code></td>
      <td>기본키로 Entity 조회</td>
      <td><code class="language-plaintext highlighter-rouge">SELECT</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">remove(entity)</code></td>
      <td>Entity를 삭제 상태로 만듦</td>
      <td><code class="language-plaintext highlighter-rouge">DELETE</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">merge(entity)</code></td>
      <td>준영속 Entity의 값을 새 영속 Entity에 복사</td>
      <td><code class="language-plaintext highlighter-rouge">SELECT</code>, <code class="language-plaintext highlighter-rouge">UPDATE</code> 가능</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">flush()</code></td>
      <td>변경 내용을 DB에 전달</td>
      <td>쓰기 SQL 실행</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">detach(entity)</code></td>
      <td>특정 Entity를 관리 대상에서 제외</td>
      <td>자동 변경 감지 중단</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">clear()</code></td>
      <td>영속성 컨텍스트를 비움</td>
      <td>관리 중인 Entity 전체 분리</td>
    </tr>
  </tbody>
</table>

<p>직접 사용하는 Repository를 만들면 다음과 같은 모습이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Repository</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserJpaRepository</span> <span class="o">{</span>

    <span class="nd">@PersistenceContext</span>
    <span class="kd">private</span> <span class="nc">EntityManager</span> <span class="n">entityManager</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">save</span><span class="o">(</span><span class="nc">User</span> <span class="n">user</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">entityManager</span><span class="o">.</span><span class="na">persist</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nc">User</span> <span class="nf">findById</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">find</span><span class="o">(</span><span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">,</span> <span class="n">id</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">delete</span><span class="o">(</span><span class="nc">User</span> <span class="n">user</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">entityManager</span><span class="o">.</span><span class="na">remove</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Spring Data JPA의 <code class="language-plaintext highlighter-rouge">JpaRepository</code>도 내부적으로 EntityManager를 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>내 코드
  → JpaRepository
  → EntityManager
  → Hibernate
  → JDBC
  → Database
</code></pre></div></div>

<hr />

<h3 id="3-entity의-네-가지-상태">3. Entity의 네 가지 상태</h3>

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>비영속(New/Transient)</td>
      <td>객체는 있지만 JPA가 관리하지 않는 상태</td>
    </tr>
    <tr>
      <td>영속(Managed)</td>
      <td>영속성 컨텍스트가 관리하는 상태</td>
    </tr>
    <tr>
      <td>준영속(Detached)</td>
      <td>한때 관리됐지만 현재는 분리된 상태</td>
    </tr>
    <tr>
      <td>삭제(Removed)</td>
      <td>삭제하도록 등록된 상태</td>
    </tr>
  </tbody>
</table>

<h4 id="비영속">비영속</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">(</span><span class="s">"민형"</span><span class="o">,</span> <span class="s">"min@example.com"</span><span class="o">);</span>
</code></pre></div></div>

<p>일반 자바 객체만 생성된 상태다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Java 메모리: User 객체 있음
영속성 컨텍스트: 등록되지 않음
Database: 저장되지 않음
</code></pre></div></div>

<h4 id="영속">영속</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">entityManager</span><span class="o">.</span><span class="na">persist</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
</code></pre></div></div>

<p>또는 EntityManager로 조회한 객체도 영속 상태가 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">find</span><span class="o">(</span><span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">,</span> <span class="mi">1L</span><span class="o">);</span>
</code></pre></div></div>

<p>영속 상태가 되면 JPA가 객체의 변경을 추적한다.</p>

<h4 id="준영속">준영속</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">entityManager</span><span class="o">.</span><span class="na">detach</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
</code></pre></div></div>

<p>영속성 컨텍스트에서 분리된 객체는 값을 바꿔도 변경 감지가 동작하지 않는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">user</span><span class="o">.</span><span class="na">changeName</span><span class="o">(</span><span class="s">"민농"</span><span class="o">);</span> <span class="c1">// 자동 UPDATE 대상이 아님</span>
</code></pre></div></div>

<h4 id="삭제">삭제</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">entityManager</span><span class="o">.</span><span class="na">remove</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
</code></pre></div></div>

<p>Entity가 삭제 대상으로 등록되고 flush 시점에 <code class="language-plaintext highlighter-rouge">DELETE</code> SQL이 전달된다.</p>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>Entity 생명주기</span>
  </header>
  <div class="mermaid js-mermaid">
flowchart LR
    N[비영속] --&gt;|persist| M[영속]
    M --&gt;|detach, clear| D[준영속]
    D --&gt;|merge 후 반환 객체| M
    M --&gt;|remove| R[삭제]
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<hr />

<h3 id="4-1차-캐시와-동일성-보장">4. 1차 캐시와 동일성 보장</h3>

<p>영속성 컨텍스트는 관리 중인 Entity를 기본키 기준으로 1차 캐시에 보관한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">user1</span> <span class="o">=</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">find</span><span class="o">(</span><span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">,</span> <span class="mi">1L</span><span class="o">);</span>
<span class="nc">User</span> <span class="n">user2</span> <span class="o">=</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">find</span><span class="o">(</span><span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">,</span> <span class="mi">1L</span><span class="o">);</span>
</code></pre></div></div>

<p>첫 조회는 DB에서 데이터를 가져와 1차 캐시에 저장한다. 같은 영속성 컨텍스트에서 다시 조회하면 1차 캐시의 객체를 반환할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>첫 번째 find()
→ 1차 캐시에 없음
→ SELECT 실행
→ 조회 결과를 1차 캐시에 저장

두 번째 find()
→ 1차 캐시에서 발견
→ 같은 Entity 객체 반환
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">user1</span> <span class="o">==</span> <span class="n">user2</span><span class="o">);</span> <span class="c1">// true</span>
</code></pre></div></div>

<p>이처럼 같은 영속성 컨텍스트에서 같은 기본키의 Entity에 대해 같은 객체 참조를 제공하는 것을 동일성 보장이라고 한다.</p>

<p>1차 캐시는 보통 하나의 트랜잭션처럼 짧은 범위에서 사용되므로 애플리케이션 전체의 공용 캐시와는 다르다.</p>

<hr />

<h3 id="5-변경-감지">5. 변경 감지</h3>

<p>변경 감지(Dirty Checking)는 영속 상태의 Entity가 변경됐는지 확인하여 <code class="language-plaintext highlighter-rouge">UPDATE</code> SQL을 만드는 기능이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">changeName</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">,</span> <span class="nc">String</span> <span class="n">newName</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">find</span><span class="o">(</span><span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">,</span> <span class="n">id</span><span class="o">);</span>
    <span class="n">user</span><span class="o">.</span><span class="na">changeName</span><span class="o">(</span><span class="n">newName</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">update()</code>나 <code class="language-plaintext highlighter-rouge">save()</code>를 따로 호출하지 않아도 트랜잭션이 끝날 때 변경이 반영될 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. User 조회
2. 조회 당시 상태를 스냅샷으로 보관
3. user.changeName() 호출
4. flush 시 현재 상태와 스냅샷 비교
5. 변경 발견
6. UPDATE SQL 실행
</code></pre></div></div>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">UPDATE</span> <span class="n">users</span>
<span class="k">SET</span> <span class="n">name</span> <span class="o">=</span> <span class="s1">'민농'</span>
<span class="k">WHERE</span> <span class="n">id</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div>

<p>변경 감지는 영속 상태의 Entity에만 적용된다.</p>

<hr />

<h3 id="6-쓰기-지연">6. 쓰기 지연</h3>

<p>JPA는 쓰기 SQL을 준비해두었다가 flush 시점에 DB로 전달할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">entityManager</span><span class="o">.</span><span class="na">persist</span><span class="o">(</span><span class="n">user1</span><span class="o">);</span>
<span class="n">entityManager</span><span class="o">.</span><span class="na">persist</span><span class="o">(</span><span class="n">user2</span><span class="o">);</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>persist(user1) → INSERT 준비
persist(user2) → INSERT 준비
flush          → SQL을 DB에 전달
commit         → 트랜잭션 확정
</code></pre></div></div>

<p>이 방식을 쓰기 지연(Write Behind)이라고 한다.</p>

<p>다만 모든 SQL이 반드시 커밋 직전에만 실행되는 것은 아니다. 예를 들어 <code class="language-plaintext highlighter-rouge">IDENTITY</code> 기본키 전략은 생성된 ID를 얻기 위해 <code class="language-plaintext highlighter-rouge">persist()</code> 시점에 <code class="language-plaintext highlighter-rouge">INSERT</code>가 먼저 실행될 수 있다.</p>

<hr />

<h3 id="7-flush와-commit의-차이">7. <code class="language-plaintext highlighter-rouge">flush()</code>와 <code class="language-plaintext highlighter-rouge">commit()</code>의 차이</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">entityManager</span><span class="o">.</span><span class="na">flush</span><span class="o">();</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">flush()</code>는 변경 내용을 SQL로 DB에 전달하지만 트랜잭션을 최종 확정하지는 않는다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">flush()</code></td>
      <td>변경 내용을 SQL로 DB에 전달</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">commit()</code></td>
      <td>트랜잭션 변경을 최종 확정</td>
    </tr>
  </tbody>
</table>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>flush 실행
  → SQL은 DB에 전달됨
  → 아직 commit 전
  → 오류 발생 시 rollback 가능
</code></pre></div></div>

<p>일반적으로 <code class="language-plaintext highlighter-rouge">@Transactional</code> 메서드가 정상 종료될 때 다음 과정이 진행된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>메서드 정상 종료
   ↓
flush
   ↓
쓰기 SQL 실행
   ↓
commit
</code></pre></div></div>

<p>JPQL 쿼리를 실행하기 전에 조회 결과의 일관성을 맞추기 위해 flush가 먼저 발생할 수도 있다.</p>

<hr />

<h3 id="8-merge-주의점">8. <code class="language-plaintext highlighter-rouge">merge()</code> 주의점</h3>

<p><code class="language-plaintext highlighter-rouge">merge()</code>는 전달받은 준영속 객체 자체를 다시 영속 상태로 바꾸는 메서드가 아니다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">managedUser</span> <span class="o">=</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">merge</span><span class="o">(</span><span class="n">detachedUser</span><span class="o">);</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>detachedUser → 여전히 준영속
managedUser  → merge가 반환한 영속 객체
</code></pre></div></div>

<p>따라서 <code class="language-plaintext highlighter-rouge">merge()</code>를 사용할 때는 반환 객체를 사용해야 한다.</p>

<p>실무에서는 준영속 Entity 전체를 <code class="language-plaintext highlighter-rouge">merge()</code>하기보다 트랜잭션 안에서 Entity를 다시 조회하고 필요한 값만 변경하는 방식을 자주 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">changeName</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">,</span> <span class="nc">String</span> <span class="n">newName</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">find</span><span class="o">(</span><span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">,</span> <span class="n">id</span><span class="o">);</span>
    <span class="n">user</span><span class="o">.</span><span class="na">changeName</span><span class="o">(</span><span class="n">newName</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이 방식은 어떤 필드가 변경되는지 코드에서 명확하게 확인할 수 있다.</p>

<hr />

<h3 id="9-실제-service-실행-흐름">9. 실제 Service 실행 흐름</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">updateUser</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">,</span> <span class="nc">String</span> <span class="n">newName</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>
    <span class="n">user</span><span class="o">.</span><span class="na">changeName</span><span class="o">(</span><span class="n">newName</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. @Transactional로 트랜잭션 시작
2. EntityManager가 영속성 컨텍스트 사용
3. User 조회
4. User가 영속 상태가 됨
5. 이름 변경
6. 트랜잭션 종료 직전 변경 감지
7. UPDATE SQL 실행
8. commit
9. 영속성 컨텍스트 범위 종료
</code></pre></div></div>

<hr />

<h3 id="핵심-정리">핵심 정리</h3>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 줄 정리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>EntityManager</td>
      <td>Entity의 저장, 조회, 삭제와 상태를 관리</td>
    </tr>
    <tr>
      <td>영속성 컨텍스트</td>
      <td>Entity를 보관하고 변경을 추적하는 논리적인 공간</td>
    </tr>
    <tr>
      <td>1차 캐시</td>
      <td>같은 영속성 컨텍스트에서 조회한 Entity 보관</td>
    </tr>
    <tr>
      <td>변경 감지</td>
      <td>영속 Entity의 변경을 찾아 <code class="language-plaintext highlighter-rouge">UPDATE</code> 실행</td>
    </tr>
    <tr>
      <td>쓰기 지연</td>
      <td>쓰기 SQL을 준비했다가 flush 시 전달</td>
    </tr>
    <tr>
      <td>flush</td>
      <td>SQL을 DB에 전달하지만 확정하지는 않음</td>
    </tr>
    <tr>
      <td>commit</td>
      <td>트랜잭션 변경을 최종 확정</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>EntityManager의 핵심은 SQL 메서드를 대신 제공하는 것보다 Entity의 생명주기와 상태를 관리하는 데 있다.</p>
</blockquote>

<h3 id="참고-자료">참고 자료</h3>

<ul>
  <li><a href="https://jakarta.ee/specifications/persistence/">Jakarta Persistence 공식 명세</a></li>
  <li><a href="https://docs.jboss.org/hibernate/orm/current/userguide/html_single/Hibernate_User_Guide.html">Hibernate ORM User Guide</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="JPA" /><category term="EntityManager" /><category term="Persistence Context" /><category term="Dirty Checking" /><summary type="html"><![CDATA[EntityManager의 역할, Entity 생명주기, 1차 캐시, 변경 감지, 쓰기 지연, flush와 commit의 차이를 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 4: JPA, Entity와 Repository</title><link href="http://localhost:4000/java/spring/2026/08/16/spring-data-jpa-basics/" rel="alternate" type="text/html" title="Spring 기초 Part 4: JPA, Entity와 Repository" /><published>2026-08-16T09:00:00+09:00</published><updated>2026-08-16T09:00:00+09:00</updated><id>http://localhost:4000/java/spring/2026/08/16/spring-data-jpa-basics</id><content type="html" xml:base="http://localhost:4000/java/spring/2026/08/16/spring-data-jpa-basics/"><![CDATA[<h2 id="spring-기초-part-4-jpa-entity와-repository">Spring 기초 Part 4: JPA, Entity와 Repository</h2>

<blockquote>
  <p>JPA는 자바 객체와 데이터베이스 테이블을 매핑하여 객체 중심으로 데이터를 다룰 수 있게 해주는 자바 표준이다.</p>
</blockquote>

<p>개발자는 Entity와 Repository를 작성하고, JPA 구현체는 객체 정보를 바탕으로 SQL을 만든다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Java 객체
   ↓
Spring Data JPA
   ↓
JPA
   ↓
Hibernate
   ↓
JDBC
   ↓
Database
</code></pre></div></div>

<hr />

<h3 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>관계형 데이터베이스</td>
      <td>데이터를 테이블 사이의 관계로 저장하는 시스템</td>
      <td>H2, MySQL, PostgreSQL</td>
    </tr>
    <tr>
      <td>테이블</td>
      <td>데이터를 행과 열로 저장하는 구조</td>
      <td>사용자 목록을 저장하는 표</td>
    </tr>
    <tr>
      <td>행(Row)</td>
      <td>테이블에 저장된 데이터 한 건</td>
      <td>사용자 한 명</td>
    </tr>
    <tr>
      <td>컬럼(Column)</td>
      <td>데이터가 가지는 속성</td>
      <td>이름, 이메일, 나이</td>
    </tr>
    <tr>
      <td>기본키(PK)</td>
      <td>각 행을 유일하게 구분하는 값</td>
      <td>사용자 <code class="language-plaintext highlighter-rouge">id</code></td>
    </tr>
    <tr>
      <td>ORM</td>
      <td>객체와 관계형 테이블을 연결하는 기술</td>
      <td><code class="language-plaintext highlighter-rouge">User</code>와 <code class="language-plaintext highlighter-rouge">users</code>를 연결</td>
    </tr>
    <tr>
      <td>JPA</td>
      <td>자바 ORM 사용법을 정의한 표준</td>
      <td>Entity와 관련 어노테이션 규칙</td>
    </tr>
    <tr>
      <td>Hibernate</td>
      <td>JPA 표준을 실제로 구현한 라이브러리</td>
      <td>객체를 보고 SQL을 생성</td>
    </tr>
    <tr>
      <td>JDBC</td>
      <td>자바가 DB에 SQL을 전달하는 API</td>
      <td>Hibernate가 생성한 SQL 전달</td>
    </tr>
    <tr>
      <td>Entity</td>
      <td>JPA가 관리하고 테이블과 매핑하는 객체</td>
      <td><code class="language-plaintext highlighter-rouge">User</code> 클래스</td>
    </tr>
    <tr>
      <td>Repository</td>
      <td>데이터 저장과 조회를 담당하는 계층</td>
      <td><code class="language-plaintext highlighter-rouge">UserRepository</code></td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="2-orm은-왜-필요한가">2. ORM은 왜 필요한가</h3>

<p>자바는 객체를 사용하고 관계형 데이터베이스는 테이블을 사용한다.</p>

<table>
  <thead>
    <tr>
      <th>자바 객체</th>
      <th>데이터베이스</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>클래스</td>
      <td>테이블</td>
    </tr>
    <tr>
      <td>객체</td>
      <td>행</td>
    </tr>
    <tr>
      <td>필드</td>
      <td>컬럼</td>
    </tr>
    <tr>
      <td>객체 참조</td>
      <td>외래키</td>
    </tr>
  </tbody>
</table>

<p>두 구조가 다르기 때문에 JDBC만 사용하면 SQL 작성과 결과 변환을 직접 처리해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">String</span> <span class="n">sql</span> <span class="o">=</span> <span class="s">"SELECT id, name, email FROM users WHERE id = ?"</span><span class="o">;</span>

<span class="c1">// Connection, PreparedStatement, ResultSet 처리</span>
<span class="c1">// ResultSet 값을 꺼내 User 객체로 직접 변환</span>
</code></pre></div></div>

<p>ORM을 사용하면 매핑 정보를 기준으로 반복 코드를 줄일 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>
</code></pre></div></div>

<p>ORM이 SQL 지식을 없애주는 것은 아니다. 생성되는 SQL과 데이터베이스 구조를 이해해야 성능 문제를 찾을 수 있다.</p>

<hr />

<h3 id="3-jpa-hibernate-spring-data-jpa의-차이">3. JPA, Hibernate, Spring Data JPA의 차이</h3>

<p>세 단어는 같은 뜻이 아니다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>JPA</td>
      <td>ORM을 사용하는 인터페이스와 어노테이션을 정의한 표준</td>
    </tr>
    <tr>
      <td>Hibernate</td>
      <td>JPA 표준을 구현하여 실제 SQL을 생성하고 Entity를 관리</td>
    </tr>
    <tr>
      <td>Spring Data JPA</td>
      <td>Repository 인터페이스를 쉽게 작성하도록 도와주는 Spring 프로젝트</td>
    </tr>
  </tbody>
</table>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발자가 UserRepository 호출
          ↓
Spring Data JPA가 Repository 구현 제공
          ↓
JPA 표준 방식으로 Entity 관리 요청
          ↓
Hibernate가 SQL 생성
          ↓
JDBC가 DB에 SQL 전달
</code></pre></div></div>

<blockquote>
  <p>JPA는 규칙이고, Hibernate는 그 규칙의 구현체이며, Spring Data JPA는 JPA를 편하게 사용하도록 도와준다.</p>
</blockquote>

<hr />

<h3 id="4-entity-만들기">4. Entity 만들기</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">jakarta.persistence.Column</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.Entity</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.GeneratedValue</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.GenerationType</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.Id</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.Table</span><span class="o">;</span>

<span class="nd">@Entity</span>
<span class="nd">@Table</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>

    <span class="nd">@Id</span>
    <span class="nd">@GeneratedValue</span><span class="o">(</span><span class="n">strategy</span> <span class="o">=</span> <span class="nc">GenerationType</span><span class="o">.</span><span class="na">IDENTITY</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">;</span>

    <span class="nd">@Column</span><span class="o">(</span><span class="n">nullable</span> <span class="o">=</span> <span class="kc">false</span><span class="o">,</span> <span class="n">length</span> <span class="o">=</span> <span class="mi">50</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="nd">@Column</span><span class="o">(</span><span class="n">nullable</span> <span class="o">=</span> <span class="kc">false</span><span class="o">,</span> <span class="n">unique</span> <span class="o">=</span> <span class="kc">true</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">email</span><span class="o">;</span>

    <span class="kd">protected</span> <span class="nf">User</span><span class="o">()</span> <span class="o">{</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nf">User</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="nc">String</span> <span class="n">email</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">email</span> <span class="o">=</span> <span class="n">email</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Entity 클래스에는 다음 조건이 필요하다.</p>

<ul>
  <li>클래스에 <code class="language-plaintext highlighter-rouge">@Entity</code>를 붙인다.</li>
  <li><code class="language-plaintext highlighter-rouge">@Id</code>로 기본키를 지정한다.</li>
  <li>JPA가 사용할 <code class="language-plaintext highlighter-rouge">public</code> 또는 <code class="language-plaintext highlighter-rouge">protected</code> 기본 생성자가 필요하다.</li>
  <li>Entity 클래스 자체를 <code class="language-plaintext highlighter-rouge">final</code>로 선언하지 않는다.</li>
</ul>

<h4 id="주요-어노테이션">주요 어노테이션</h4>

<table>
  <thead>
    <tr>
      <th>어노테이션</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Entity</code></td>
      <td>클래스를 JPA가 관리할 Entity로 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Table</code></td>
      <td>매핑할 테이블 이름 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Id</code></td>
      <td>기본키 필드 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@GeneratedValue</code></td>
      <td>기본키 자동 생성 전략 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Column</code></td>
      <td>컬럼 이름과 제약조건 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Enumerated</code></td>
      <td>Enum을 저장할 방식 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Transient</code></td>
      <td>DB 컬럼과 매핑하지 않을 필드 지정</td>
    </tr>
  </tbody>
</table>

<h4 id="generatedvalue"><code class="language-plaintext highlighter-rouge">@GeneratedValue</code></h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Id</span>
<span class="nd">@GeneratedValue</span><span class="o">(</span><span class="n">strategy</span> <span class="o">=</span> <span class="nc">GenerationType</span><span class="o">.</span><span class="na">IDENTITY</span><span class="o">)</span>
<span class="kd">private</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">IDENTITY</code> 전략은 기본키 생성을 데이터베이스에 맡긴다. 새로운 사용자를 만들 때 개발자가 <code class="language-plaintext highlighter-rouge">id</code>를 직접 지정하지 않는다.</p>

<h4 id="column"><code class="language-plaintext highlighter-rouge">@Column</code></h4>

<table>
  <thead>
    <tr>
      <th>속성</th>
      <th>의미</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">name</code></td>
      <td>매핑할 컬럼명</td>
      <td><code class="language-plaintext highlighter-rouge">@Column(name = "user_name")</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">nullable</code></td>
      <td><code class="language-plaintext highlighter-rouge">NULL</code> 허용 여부</td>
      <td><code class="language-plaintext highlighter-rouge">@Column(nullable = false)</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">length</code></td>
      <td>문자열 컬럼 길이</td>
      <td><code class="language-plaintext highlighter-rouge">@Column(length = 50)</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">unique</code></td>
      <td>중복 허용 여부</td>
      <td><code class="language-plaintext highlighter-rouge">@Column(unique = true)</code></td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">@Column</code> 설정만 믿기보다 운영 DB에는 마이그레이션 도구 등을 이용해 실제 제약조건도 함께 관리해야 한다.</p>

<hr />

<h3 id="5-repository-만들기">5. Repository 만들기</h3>

<p>Spring Data JPA의 <code class="language-plaintext highlighter-rouge">JpaRepository</code>를 상속하면 기본 CRUD 기능을 사용할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">UserRepository</span> <span class="kd">extends</span> <span class="nc">JpaRepository</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">,</span> <span class="nc">Long</span><span class="o">&gt;</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p>두 타입의 의미는 다음과 같다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">User</code>: Repository가 관리할 Entity 타입</li>
  <li><code class="language-plaintext highlighter-rouge">Long</code>: <code class="language-plaintext highlighter-rouge">User</code>의 기본키 타입</li>
</ul>

<p>기본 제공 메서드는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>메서드</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">save(entity)</code></td>
      <td>새 Entity 저장 또는 기존 Entity 반영</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">findById(id)</code></td>
      <td>기본키로 한 건 조회</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">findAll()</code></td>
      <td>전체 조회</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">existsById(id)</code></td>
      <td>기본키에 해당하는 데이터 존재 여부 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">delete(entity)</code></td>
      <td>Entity 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">deleteById(id)</code></td>
      <td>기본키로 삭제</td>
    </tr>
  </tbody>
</table>

<p>메서드 이름을 분석해 조회 조건을 만들 수도 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Optional</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="nf">findByEmail</span><span class="o">(</span><span class="nc">String</span> <span class="n">email</span><span class="o">);</span>

<span class="kt">boolean</span> <span class="nf">existsByEmail</span><span class="o">(</span><span class="nc">String</span> <span class="n">email</span><span class="o">);</span>

<span class="nc">List</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="nf">findByNameContaining</span><span class="o">(</span><span class="nc">String</span> <span class="n">keyword</span><span class="o">);</span>
</code></pre></div></div>

<hr />

<h3 id="6-사용자-저장-흐름">6. 사용자 저장 흐름</h3>

<h4 id="service">Service</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserRepository</span> <span class="n">userRepository</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserService</span><span class="o">(</span><span class="nc">UserRepository</span> <span class="n">userRepository</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userRepository</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@Transactional</span>
    <span class="kd">public</span> <span class="nc">Long</span> <span class="nf">createUser</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="nc">String</span> <span class="n">email</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">(</span><span class="n">name</span><span class="o">,</span> <span class="n">email</span><span class="o">);</span>
        <span class="nc">User</span> <span class="n">savedUser</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
        <span class="k">return</span> <span class="n">savedUser</span><span class="o">.</span><span class="na">getId</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>실행 흐름은 다음과 같다.</p>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>Spring Data JPA 저장 흐름</span>
  </header>
  <div class="mermaid js-mermaid">
flowchart TD
    S[Service가 User 생성] --&gt; R[UserRepository.save 호출]
    R --&gt; SD[Spring Data JPA]
    SD --&gt; H[Hibernate가 Entity 관리]
    H --&gt; SQL[INSERT SQL 생성]
    SQL --&gt; J[JDBC가 SQL 전달]
    J --&gt; DB[(Database 저장)]
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<p>Hibernate는 Entity의 매핑 정보를 보고 대략 다음과 같은 SQL을 실행한다.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">users</span> <span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="n">email</span><span class="p">)</span>
<span class="k">VALUES</span> <span class="p">(</span><span class="s1">'민형'</span><span class="p">,</span> <span class="s1">'min@example.com'</span><span class="p">);</span>
</code></pre></div></div>

<hr />

<h3 id="7-jpa와-jdbc-비교">7. JPA와 JDBC 비교</h3>

<table>
  <thead>
    <tr>
      <th>JPA</th>
      <th>JDBC</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>객체와 테이블을 매핑한다.</td>
      <td>SQL을 직접 작성한다.</td>
    </tr>
    <tr>
      <td>반복적인 CRUD 코드를 줄인다.</td>
      <td>결과를 객체로 직접 변환한다.</td>
    </tr>
    <tr>
      <td>변경 감지와 연관관계 기능을 제공한다.</td>
      <td>실행 흐름이 SQL 중심으로 명확하다.</td>
    </tr>
    <tr>
      <td>잘못 사용하면 예상하지 못한 SQL이 발생할 수 있다.</td>
      <td>반복 코드가 많아질 수 있다.</td>
    </tr>
  </tbody>
</table>

<p>JPA도 내부적으로 JDBC를 사용해 SQL을 전달한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JPA와 JDBC 중 하나만 선택
</code></pre></div></div>

<p>이 아니라 다음 구조에 가깝다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JPA / Hibernate
      ↓
    JDBC
      ↓
  Database
</code></pre></div></div>

<hr />

<h3 id="8-h2-연결-설정">8. H2 연결 설정</h3>

<p>H2는 학습과 테스트에 많이 사용하는 가벼운 관계형 데이터베이스다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">spring</span><span class="pi">:</span>
  <span class="na">datasource</span><span class="pi">:</span>
    <span class="na">url</span><span class="pi">:</span> <span class="s">jdbc:h2:mem:testdb</span>
    <span class="na">driver-class-name</span><span class="pi">:</span> <span class="s">org.h2.Driver</span>
    <span class="na">username</span><span class="pi">:</span> <span class="s">sa</span>
    <span class="na">password</span><span class="pi">:</span>

  <span class="na">h2</span><span class="pi">:</span>
    <span class="na">console</span><span class="pi">:</span>
      <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>
      <span class="na">path</span><span class="pi">:</span> <span class="s">/h2-console</span>

  <span class="na">jpa</span><span class="pi">:</span>
    <span class="na">hibernate</span><span class="pi">:</span>
      <span class="na">ddl-auto</span><span class="pi">:</span> <span class="s">create-drop</span>
    <span class="na">show-sql</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">datasource</code></td>
      <td>연결할 DB 주소와 계정 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ddl-auto</code></td>
      <td>Entity를 기준으로 테이블을 관리하는 방식 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">create-drop</code></td>
      <td>시작할 때 생성하고 종료할 때 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">show-sql</code></td>
      <td>Hibernate가 실행하는 SQL 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">h2.console.enabled</code></td>
      <td>H2 웹 콘솔 활성화</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">create-drop</code>은 애플리케이션 종료 시 테이블을 삭제하므로 운영 환경에서 사용하면 안 된다.</p>

<hr />

<h3 id="핵심-정리">핵심 정리</h3>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 줄 정리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ORM</td>
      <td>자바 객체와 관계형 테이블을 연결하는 기술</td>
    </tr>
    <tr>
      <td>JPA</td>
      <td>자바 ORM 사용법을 정의한 표준</td>
    </tr>
    <tr>
      <td>Hibernate</td>
      <td>JPA를 실제로 구현한 라이브러리</td>
    </tr>
    <tr>
      <td>Spring Data JPA</td>
      <td>Repository 작성을 편하게 도와주는 Spring 프로젝트</td>
    </tr>
    <tr>
      <td>Entity</td>
      <td>JPA가 관리하며 테이블과 매핑되는 객체</td>
    </tr>
    <tr>
      <td>Repository</td>
      <td>Entity의 저장과 조회를 담당하는 데이터 접근 계층</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>JPA를 사용한다는 것은 SQL을 몰라도 된다는 뜻이 아니라, 객체와 테이블의 매핑을 이용해 반복적인 데이터 접근 코드를 줄인다는 뜻이다.</p>
</blockquote>

<h3 id="참고-자료">참고 자료</h3>

<ul>
  <li><a href="https://docs.spring.io/spring-data/jpa/reference/">Spring Data JPA 공식 문서</a></li>
  <li><a href="https://jakarta.ee/specifications/persistence/">Jakarta Persistence 공식 명세</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="Spring Boot" /><category term="JPA" /><category term="Hibernate" /><category term="Spring Data JPA" /><category term="Entity" /><summary type="html"><![CDATA[ORM, JPA, Hibernate, Entity, Repository의 역할과 Spring Data JPA의 기본 CRUD 흐름을 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 3: REST API 요청값과 입력값 검증</title><link href="http://localhost:4000/java/spring/2026/08/15/spring-rest-api-validation/" rel="alternate" type="text/html" title="Spring 기초 Part 3: REST API 요청값과 입력값 검증" /><published>2026-08-15T09:00:00+09:00</published><updated>2026-08-15T09:00:00+09:00</updated><id>http://localhost:4000/java/spring/2026/08/15/spring-rest-api-validation</id><content type="html" xml:base="http://localhost:4000/java/spring/2026/08/15/spring-rest-api-validation/"><![CDATA[<h2 id="spring-기초-part-3-rest-api-요청값과-입력값-검증">Spring 기초 Part 3: REST API 요청값과 입력값 검증</h2>

<blockquote>
  <p>Controller는 HTTP 요청값을 자바 값이나 객체로 변환하고, 규칙에 맞는지 검증한 뒤 Service에 전달한다.</p>
</blockquote>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTTP Request
    ↓
요청값 바인딩
    ↓
입력값 검증
    ↓
Controller
    ↓
Service
</code></pre></div></div>

<hr />

<h3 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Endpoint</td>
      <td>Client가 호출할 수 있는 API 주소와 HTTP Method의 조합</td>
      <td><code class="language-plaintext highlighter-rouge">POST /api/users</code></td>
    </tr>
    <tr>
      <td>Parameter</td>
      <td>HTTP 요청에 포함된 입력값</td>
      <td>사용자 ID, 검색어, 페이지 번호</td>
    </tr>
    <tr>
      <td>Request Body</td>
      <td>HTTP 요청 본문에 담긴 데이터</td>
      <td>JSON 회원가입 정보</td>
    </tr>
    <tr>
      <td>Binding</td>
      <td>HTTP 요청값을 자바 값이나 객체로 변환하는 과정</td>
      <td>JSON을 <code class="language-plaintext highlighter-rouge">UserCreateRequest</code>로 변환</td>
    </tr>
    <tr>
      <td>DTO</td>
      <td>계층 사이에서 데이터를 전달하는 객체</td>
      <td>API 요청 전용 객체</td>
    </tr>
    <tr>
      <td>Validation</td>
      <td>값이 정해진 규칙을 만족하는지 검사하는 과정</td>
      <td>이메일 형식과 나이 확인</td>
    </tr>
    <tr>
      <td>Constraint</td>
      <td>입력값이 지켜야 하는 검증 규칙</td>
      <td><code class="language-plaintext highlighter-rouge">@NotBlank</code>, <code class="language-plaintext highlighter-rouge">@Email</code></td>
    </tr>
    <tr>
      <td>직렬화</td>
      <td>자바 객체를 JSON 같은 전송 형식으로 변환</td>
      <td><code class="language-plaintext highlighter-rouge">UserResponse</code> → JSON</td>
    </tr>
    <tr>
      <td>역직렬화</td>
      <td>JSON을 자바 객체로 변환</td>
      <td>JSON → <code class="language-plaintext highlighter-rouge">UserCreateRequest</code></td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="2-http-요청값을-받는-세-가지-방법">2. HTTP 요청값을 받는 세 가지 방법</h3>

<table>
  <thead>
    <tr>
      <th>어노테이션</th>
      <th>값을 가져오는 위치</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PathVariable</code></td>
      <td>URL 경로</td>
      <td><code class="language-plaintext highlighter-rouge">/api/users/1</code>의 <code class="language-plaintext highlighter-rouge">1</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestParam</code></td>
      <td>Query String</td>
      <td><code class="language-plaintext highlighter-rouge">/api/users?name=민형</code>의 <code class="language-plaintext highlighter-rouge">name</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestBody</code></td>
      <td>HTTP 요청 본문</td>
      <td>JSON 회원가입 정보</td>
    </tr>
  </tbody>
</table>

<h4 id="pathvariable"><code class="language-plaintext highlighter-rouge">@PathVariable</code></h4>

<p>특정 자원을 식별할 때 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/api/users/{id}"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">getUser</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">getUser</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">GET /api/users/1
</span></code></pre></div></div>

<h4 id="requestparam"><code class="language-plaintext highlighter-rouge">@RequestParam</code></h4>

<p>검색, 필터, 정렬처럼 선택적인 조건을 전달할 때 자주 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/api/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">UserResponse</span><span class="o">&gt;</span> <span class="nf">searchUsers</span><span class="o">(</span>
        <span class="nd">@RequestParam</span><span class="o">(</span><span class="n">required</span> <span class="o">=</span> <span class="kc">false</span><span class="o">)</span> <span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">searchUsers</span><span class="o">(</span><span class="n">name</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">GET /api/users?name=민형
</span></code></pre></div></div>

<h4 id="requestbody"><code class="language-plaintext highlighter-rouge">@RequestBody</code></h4>

<p>JSON 같은 요청 본문을 자바 객체로 변환할 때 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@PostMapping</span><span class="o">(</span><span class="s">"/api/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">createUser</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">UserCreateRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">createUser</span><span class="o">(</span><span class="n">request</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"민형"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"min@example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"age"</span><span class="p">:</span><span class="w"> </span><span class="mi">25</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Spring MVC와 Jackson이 JSON을 <code class="language-plaintext highlighter-rouge">UserCreateRequest</code> 객체로 변환한다.</p>

<hr />

<h3 id="3-entity-대신-요청-dto를-사용한다">3. Entity 대신 요청 DTO를 사용한다</h3>

<p>요청 JSON을 Entity로 바로 받으면 API 입력 구조와 DB 구조가 강하게 연결된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 권장하지 않는 형태</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">createUser</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">User</span> <span class="n">user</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// 요청이 Entity 구조에 직접 의존</span>
<span class="o">}</span>
</code></pre></div></div>

<p>요청 전용 DTO를 만들면 API가 허용할 값과 검증 규칙을 명확하게 표현할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="n">record</span> <span class="nf">UserCreateRequest</span><span class="o">(</span>
        <span class="nc">String</span> <span class="n">name</span><span class="o">,</span>
        <span class="nc">String</span> <span class="n">email</span><span class="o">,</span>
        <span class="kt">int</span> <span class="n">age</span>
<span class="o">)</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>객체</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Request DTO</td>
      <td>Client가 보낸 입력값 표현</td>
    </tr>
    <tr>
      <td>Entity</td>
      <td>데이터베이스에 저장하고 JPA가 관리</td>
    </tr>
    <tr>
      <td>Response DTO</td>
      <td>Client에 반환할 데이터 표현</td>
    </tr>
  </tbody>
</table>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JSON
  → Request DTO
  → Service
  → Entity
  → Repository
  → Database
</code></pre></div></div>

<hr />

<h3 id="4-bean-validation">4. Bean Validation</h3>

<p>Bean Validation은 어노테이션으로 입력값 규칙을 선언하는 표준이다. Spring Boot 3 계열에서는 일반적으로 <code class="language-plaintext highlighter-rouge">jakarta.validation</code> 패키지를 사용한다.</p>

<p>검증 기능을 사용하려면 Validation Starter를 추가한다.</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;dependency&gt;</span>
    <span class="nt">&lt;groupId&gt;</span>org.springframework.boot<span class="nt">&lt;/groupId&gt;</span>
    <span class="nt">&lt;artifactId&gt;</span>spring-boot-starter-validation<span class="nt">&lt;/artifactId&gt;</span>
<span class="nt">&lt;/dependency&gt;</span>
</code></pre></div></div>

<h4 id="자주-사용하는-검증-어노테이션">자주 사용하는 검증 어노테이션</h4>

<table>
  <thead>
    <tr>
      <th>어노테이션</th>
      <th>검증 내용</th>
      <th>적용 예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@NotNull</code></td>
      <td>값이 <code class="language-plaintext highlighter-rouge">null</code>이면 실패</td>
      <td>객체, 숫자 필수값</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@NotBlank</code></td>
      <td><code class="language-plaintext highlighter-rouge">null</code>, 빈 문자열, 공백 문자열이면 실패</td>
      <td>이름, 제목</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@NotEmpty</code></td>
      <td><code class="language-plaintext highlighter-rouge">null</code>이거나 길이가 0이면 실패</td>
      <td>문자열, 컬렉션</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Email</code></td>
      <td>이메일 형식 검사</td>
      <td>이메일 주소</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Positive</code></td>
      <td>0보다 큰 값인지 검사</td>
      <td>가격, 수량</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Min</code></td>
      <td>지정한 최솟값 이상인지 검사</td>
      <td>나이</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Max</code></td>
      <td>지정한 최댓값 이하인지 검사</td>
      <td>점수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Size</code></td>
      <td>문자열이나 컬렉션의 길이 검사</td>
      <td>비밀번호 길이</td>
    </tr>
  </tbody>
</table>

<p>요청 DTO에 규칙을 선언한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">jakarta.validation.constraints.Email</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.validation.constraints.Max</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.validation.constraints.Min</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.validation.constraints.NotBlank</span><span class="o">;</span>

<span class="kd">public</span> <span class="n">record</span> <span class="nf">UserCreateRequest</span><span class="o">(</span>
        <span class="nd">@NotBlank</span><span class="o">(</span><span class="n">message</span> <span class="o">=</span> <span class="s">"이름은 필수입니다."</span><span class="o">)</span>
        <span class="nc">String</span> <span class="n">name</span><span class="o">,</span>

        <span class="nd">@NotBlank</span><span class="o">(</span><span class="n">message</span> <span class="o">=</span> <span class="s">"이메일은 필수입니다."</span><span class="o">)</span>
        <span class="nd">@Email</span><span class="o">(</span><span class="n">message</span> <span class="o">=</span> <span class="s">"이메일 형식이 올바르지 않습니다."</span><span class="o">)</span>
        <span class="nc">String</span> <span class="n">email</span><span class="o">,</span>

        <span class="nd">@Min</span><span class="o">(</span><span class="n">value</span> <span class="o">=</span> <span class="mi">1</span><span class="o">,</span> <span class="n">message</span> <span class="o">=</span> <span class="s">"나이는 1 이상이어야 합니다."</span><span class="o">)</span>
        <span class="nd">@Max</span><span class="o">(</span><span class="n">value</span> <span class="o">=</span> <span class="mi">150</span><span class="o">,</span> <span class="n">message</span> <span class="o">=</span> <span class="s">"나이는 150 이하여야 합니다."</span><span class="o">)</span>
        <span class="kt">int</span> <span class="n">age</span>
<span class="o">)</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<hr />

<h3 id="5-valid로-검증-실행하기">5. <code class="language-plaintext highlighter-rouge">@Valid</code>로 검증 실행하기</h3>

<p>DTO에 검증 어노테이션만 붙이면 끝이 아니다. Controller 매개변수에 <code class="language-plaintext highlighter-rouge">@Valid</code>를 붙여 검증을 실행한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/api/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserController</span><span class="o">(</span><span class="nc">UserService</span> <span class="n">userService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userService</span> <span class="o">=</span> <span class="n">userService</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">UserResponse</span><span class="o">&gt;</span> <span class="nf">createUser</span><span class="o">(</span>
            <span class="nd">@Valid</span> <span class="nd">@RequestBody</span> <span class="nc">UserCreateRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>

        <span class="nc">UserResponse</span> <span class="n">response</span> <span class="o">=</span> <span class="n">userService</span><span class="o">.</span><span class="na">createUser</span><span class="o">(</span><span class="n">request</span><span class="o">);</span>

        <span class="k">return</span> <span class="nc">ResponseEntity</span>
                <span class="o">.</span><span class="na">status</span><span class="o">(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">CREATED</span><span class="o">)</span>
                <span class="o">.</span><span class="na">body</span><span class="o">(</span><span class="n">response</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>검증 흐름은 다음과 같다.</p>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>Request Body 검증 흐름</span>
  </header>
  <div class="mermaid js-mermaid">
flowchart TD
    C[Client JSON 요청] --&gt; J[Jackson이 DTO로 변환]
    J --&gt; V[@Valid 검증]
    V --&gt;|성공| CT[Controller 실행]
    CT --&gt; S[Service 실행]
    V --&gt;|실패| E[MethodArgumentNotValidException]
    E --&gt; R[400 Bad Request 응답]
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<p>검증에 실패하면 보통 <code class="language-plaintext highlighter-rouge">MethodArgumentNotValidException</code>이 발생하고 HTTP 400 응답으로 처리된다.</p>

<hr />

<h3 id="6-검증-오류-응답-통일하기">6. 검증 오류 응답 통일하기</h3>

<p><code class="language-plaintext highlighter-rouge">@RestControllerAdvice</code>를 사용하면 여러 Controller의 예외를 한곳에서 처리할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestControllerAdvice</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GlobalExceptionHandler</span> <span class="o">{</span>

    <span class="nd">@ExceptionHandler</span><span class="o">(</span><span class="nc">MethodArgumentNotValidException</span><span class="o">.</span><span class="na">class</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;&gt;</span> <span class="nf">handleValidation</span><span class="o">(</span>
            <span class="nc">MethodArgumentNotValidException</span> <span class="n">exception</span><span class="o">)</span> <span class="o">{</span>

        <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;</span> <span class="n">errors</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">LinkedHashMap</span><span class="o">&lt;&gt;();</span>

        <span class="n">exception</span><span class="o">.</span><span class="na">getBindingResult</span><span class="o">()</span>
                <span class="o">.</span><span class="na">getFieldErrors</span><span class="o">()</span>
                <span class="o">.</span><span class="na">forEach</span><span class="o">(</span><span class="n">error</span> <span class="o">-&gt;</span> <span class="n">errors</span><span class="o">.</span><span class="na">put</span><span class="o">(</span>
                        <span class="n">error</span><span class="o">.</span><span class="na">getField</span><span class="o">(),</span>
                        <span class="n">error</span><span class="o">.</span><span class="na">getDefaultMessage</span><span class="o">()</span>
                <span class="o">));</span>

        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">.</span><span class="na">badRequest</span><span class="o">().</span><span class="na">body</span><span class="o">(</span><span class="n">errors</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>잘못된 요청을 보내면 다음과 같은 응답을 만들 수 있다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"이름은 필수입니다."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"이메일 형식이 올바르지 않습니다."</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<hr />

<h3 id="7-직접-요청해보기">7. 직접 요청해보기</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-i</span> <span class="nt">-X</span> POST <span class="s1">'http://localhost:8080/api/users'</span> <span class="se">\</span>
  <span class="nt">-H</span> <span class="s1">'Content-Type: application/json'</span> <span class="se">\</span>
  <span class="nt">-d</span> <span class="s1">'{
    "name": "민형",
    "email": "min@example.com",
    "age": 25
  }'</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>부분</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-i</code></td>
      <td>응답 Header까지 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-X POST</code></td>
      <td>POST Method 사용</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Content-Type</code></td>
      <td>요청 본문이 JSON임을 알림</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-d</code></td>
      <td>서버로 보낼 요청 본문</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="8-형식-검증과-비즈니스-검증">8. 형식 검증과 비즈니스 검증</h3>

<p><code class="language-plaintext highlighter-rouge">@Valid</code>는 값의 기본 형식을 검사하는 데 적합하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@NotBlank → 이름이 비어 있는가?
@Email    → 이메일 형태가 맞는가?
@Positive → 수량이 양수인가?
</code></pre></div></div>

<p>하지만 DB 조회가 필요한 업무 규칙은 Service에서 검사하는 것이 자연스럽다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">createUser</span><span class="o">(</span><span class="nc">UserCreateRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">userRepository</span><span class="o">.</span><span class="na">existsByEmail</span><span class="o">(</span><span class="n">request</span><span class="o">.</span><span class="na">email</span><span class="o">()))</span> <span class="o">{</span>
        <span class="k">throw</span> <span class="k">new</span> <span class="nf">DuplicateEmailException</span><span class="o">(</span><span class="n">request</span><span class="o">.</span><span class="na">email</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="nc">User</span><span class="o">.</span><span class="na">create</span><span class="o">(</span>
            <span class="n">request</span><span class="o">.</span><span class="na">name</span><span class="o">(),</span>
            <span class="n">request</span><span class="o">.</span><span class="na">email</span><span class="o">(),</span>
            <span class="n">request</span><span class="o">.</span><span class="na">age</span><span class="o">()</span>
    <span class="o">);</span>

    <span class="k">return</span> <span class="nc">UserResponse</span><span class="o">.</span><span class="na">from</span><span class="o">(</span><span class="n">userRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">user</span><span class="o">));</span>
<span class="o">}</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>검증 위치</th>
      <th>담당할 규칙</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Request DTO</td>
      <td>빈 값, 길이, 숫자 범위, 이메일 형식</td>
    </tr>
    <tr>
      <td>Service</td>
      <td>이메일 중복, 재고 부족, 권한 등 업무 규칙</td>
    </tr>
    <tr>
      <td>Database</td>
      <td><code class="language-plaintext highlighter-rouge">NOT NULL</code>, <code class="language-plaintext highlighter-rouge">UNIQUE</code>, 외래키 등 최종 무결성</td>
    </tr>
  </tbody>
</table>

<p>검증을 한 계층에만 의존하지 않고 각 계층이 책임져야 할 규칙을 나누는 것이 중요하다.</p>

<hr />

<h3 id="핵심-정리">핵심 정리</h3>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>핵심 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PathVariable</code></td>
      <td>URL 경로의 식별자를 받는다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestParam</code></td>
      <td>Query String의 검색 조건을 받는다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestBody</code></td>
      <td>JSON 요청 본문을 자바 객체로 변환한다.</td>
    </tr>
    <tr>
      <td>DTO</td>
      <td>API와 Entity의 구조를 분리한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Valid</code></td>
      <td>DTO에 선언한 Bean Validation을 실행한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RestControllerAdvice</code></td>
      <td>여러 Controller의 오류 응답을 통일한다.</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Controller는 요청값을 받고 검증하는 입구이며, 실제 비즈니스 규칙은 Service에 맡긴다.</p>
</blockquote>

<h3 id="참고-자료">참고 자료</h3>

<ul>
  <li><a href="https://docs.spring.io/spring-framework/reference/core/validation/beanvalidation.html">Spring Framework Validation 공식 문서</a></li>
  <li><a href="https://docs.spring.io/spring-framework/reference/web/webmvc.html">Spring Framework Web MVC 공식 문서</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="Spring Boot" /><category term="REST API" /><category term="Validation" /><category term="DTO" /><category term="RequestBody" /><summary type="html"><![CDATA[PathVariable, RequestParam, RequestBody로 요청값을 받고 DTO와 Bean Validation으로 검증하는 흐름을 정리한다.]]></summary></entry></feed>