<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://minnong511.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://minnong511.github.io/" rel="alternate" type="text/html" /><updated>2026-08-02T11:06:12+09:00</updated><id>https://minnong511.github.io/feed.xml</id><title type="html">Minnong’s Study Log</title><subtitle>개발과 AI를 배우고, 실험하고, 이해한 내용을 연결하는 공부 기록</subtitle><author><name>Min Hyeong Lee</name></author><entry><title type="html">Vue Composition API 정리</title><link href="https://minnong511.github.io/frontend/vue/2026/08/02/vue-composition-api/" rel="alternate" type="text/html" title="Vue Composition API 정리" /><published>2026-08-02T09:00:00+09:00</published><updated>2026-08-02T09:00:00+09:00</updated><id>https://minnong511.github.io/frontend/vue/2026/08/02/vue-composition-api</id><content type="html" xml:base="https://minnong511.github.io/frontend/vue/2026/08/02/vue-composition-api/"><![CDATA[<h2 id="composition-api란">Composition API란?</h2>

<p>자자, 이번에는 Vue의 <strong>Composition API</strong>를 정리해보자.</p>

<p>Vue 컴포넌트에는 보통 다음과 같은 로직이 들어간다.</p>

<ul>
  <li>화면에서 사용하는 상태</li>
  <li>상태를 변경하는 함수</li>
  <li>상태에서 계산되는 값</li>
  <li>서버 요청이나 이벤트 감시</li>
  <li>컴포넌트 생명주기에 맞춰 실행할 작업</li>
</ul>

<p>Composition API는 이런 로직을 옵션별로 흩어놓기보다, 기능 단위로 가까이 모아서 작성하는 방식이다. 그래서 컴포넌트가 커질수록 어떤 상태와 함수가 한 기능을 이루는지 파악하기 쉬워진다.</p>

<!--more-->

<p>Vue 3에서는 <code class="language-plaintext highlighter-rouge">&lt;script setup&gt;</code>과 함께 Composition API를 많이 사용한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">computed</span><span class="p">,</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">doubleCount</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">count</span><span class="p">.</span><span class="nx">value</span> <span class="o">*</span> <span class="mi">2</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span>현재 값: <span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>두 배 값: <span class="si">{{</span> <span class="nx">doubleCount</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increase"</span><span class="nt">&gt;</span>증가<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>이 코드에서는 <code class="language-plaintext highlighter-rouge">count</code> 상태, <code class="language-plaintext highlighter-rouge">doubleCount</code> 계산값, <code class="language-plaintext highlighter-rouge">increase</code> 함수가 한곳에 모여 있다. 화면과 연결되는 템플릿도 아래에서 바로 확인할 수 있다.</p>

<h2 id="composition-api에서-자주-쓰는-함수">Composition API에서 자주 쓰는 함수</h2>

<p>처음부터 모든 함수를 외울 필요는 없다. 우선 아래 함수들의 역할을 구분하면 된다.</p>

<table>
  <thead>
    <tr>
      <th>분류</th>
      <th>주요 함수</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>반응형 상태</td>
      <td><code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">reactive</code></td>
      <td>변경을 추적하는 상태를 만든다.</td>
    </tr>
    <tr>
      <td>계산과 감시</td>
      <td><code class="language-plaintext highlighter-rouge">computed</code>, <code class="language-plaintext highlighter-rouge">watch</code>, <code class="language-plaintext highlighter-rouge">watchEffect</code></td>
      <td>상태에서 값을 계산하거나 변경 후 작업을 실행한다.</td>
    </tr>
    <tr>
      <td>생명주기</td>
      <td><code class="language-plaintext highlighter-rouge">onMounted</code>, <code class="language-plaintext highlighter-rouge">onUpdated</code>, <code class="language-plaintext highlighter-rouge">onUnmounted</code></td>
      <td>컴포넌트의 생성·수정·제거 시점에 코드를 실행한다.</td>
    </tr>
    <tr>
      <td>컴포넌트 통신</td>
      <td><code class="language-plaintext highlighter-rouge">defineProps</code>, <code class="language-plaintext highlighter-rouge">defineEmits</code></td>
      <td>부모와 자식 컴포넌트 사이의 데이터를 연결한다.</td>
    </tr>
    <tr>
      <td>반응성 변환</td>
      <td><code class="language-plaintext highlighter-rouge">toRef</code>, <code class="language-plaintext highlighter-rouge">toRefs</code>, <code class="language-plaintext highlighter-rouge">unref</code></td>
      <td>반응형 객체의 속성을 ref로 연결하거나 값을 꺼낸다.</td>
    </tr>
    <tr>
      <td>의존성 주입</td>
      <td><code class="language-plaintext highlighter-rouge">provide</code>, <code class="language-plaintext highlighter-rouge">inject</code></td>
      <td>깊은 컴포넌트 트리에서 값을 공유한다.</td>
    </tr>
    <tr>
      <td>DOM·렌더링</td>
      <td><code class="language-plaintext highlighter-rouge">nextTick</code>, <code class="language-plaintext highlighter-rouge">h</code></td>
      <td>DOM 반영 시점을 기다리거나 렌더링을 직접 제어한다.</td>
    </tr>
  </tbody>
</table>

<p>이번 글에서는 그중에서도 <code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">reactive</code>, <code class="language-plaintext highlighter-rouge">computed</code>, <code class="language-plaintext highlighter-rouge">watch</code>, <code class="language-plaintext highlighter-rouge">watchEffect</code>를 중심으로 살펴보자.</p>

<h2 id="ref로-반응형-상태-만들기"><code class="language-plaintext highlighter-rouge">ref()</code>로 반응형 상태 만들기</h2>

<p><code class="language-plaintext highlighter-rouge">ref()</code>는 원시 값과 객체·배열을 반응형 상태로 감싸는 함수다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">name</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">홍길동</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">isActive</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="kc">true</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">items</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span><span class="dl">'</span><span class="s1">사과</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">배</span><span class="dl">'</span><span class="p">])</span>
<span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">이순신</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">30</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">changeUserName</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">user</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">name</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">장보고</span><span class="dl">'</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span>카운트: <span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>이름: <span class="nt">&lt;input</span> <span class="na">v-model=</span><span class="s">"name"</span> <span class="nt">/&gt;&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>활성 상태: <span class="si">{{</span> <span class="nx">isActive</span> <span class="p">?</span> <span class="dl">'</span><span class="s1">활성</span><span class="dl">'</span> <span class="p">:</span> <span class="dl">'</span><span class="s1">비활성</span><span class="dl">'</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>과일 목록: <span class="si">{{</span> <span class="nx">items</span><span class="p">.</span><span class="nx">join</span><span class="p">(</span><span class="dl">'</span><span class="s1">, </span><span class="dl">'</span><span class="p">)</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>사용자: <span class="si">{{</span> <span class="nx">user</span><span class="p">.</span><span class="nx">name</span> <span class="si">}}</span> / <span class="si">{{</span> <span class="nx">user</span><span class="p">.</span><span class="nx">age</span> <span class="si">}}</span>세<span class="nt">&lt;/p&gt;</span>

  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increase"</span><span class="nt">&gt;</span>카운트 증가<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"isActive = !isActive"</span><span class="nt">&gt;</span>상태 토글<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"items.push('귤')"</span><span class="nt">&gt;</span>과일 추가<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"changeUserName"</span><span class="nt">&gt;</span>이름 변경<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">&lt;script setup&gt;</code> 안의 JavaScript에서는 <code class="language-plaintext highlighter-rouge">.value</code>를 사용하고, 템플릿에서는 Vue가 자동으로 언래핑해주므로 <code class="language-plaintext highlighter-rouge">.value</code>를 생략한다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="c1">// JavaScript</span>
<span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="p">)</span>
</code></pre></div></div>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- template --&gt;</span>
<span class="nt">&lt;p&gt;</span>{{ count }}<span class="nt">&lt;/p&gt;</span>
</code></pre></div></div>

<h3 id="ref를-사용할-때-기억할-점"><code class="language-plaintext highlighter-rouge">ref</code>를 사용할 때 기억할 점</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">ref</code>를 사용하려면 <code class="language-plaintext highlighter-rouge">vue</code>에서 import해야 한다.</li>
  <li>JavaScript에서 값을 읽거나 변경할 때는 <code class="language-plaintext highlighter-rouge">.value</code>를 사용한다.</li>
  <li>템플릿에서는 <code class="language-plaintext highlighter-rouge">.value</code>를 생략한다.</li>
  <li>객체나 배열을 <code class="language-plaintext highlighter-rouge">ref</code>로 만들면 내부 값에도 반응성이 연결된다.</li>
</ul>

<h2 id="reactive로-객체와-배열-다루기"><code class="language-plaintext highlighter-rouge">reactive()</code>로 객체와 배열 다루기</h2>

<p><code class="language-plaintext highlighter-rouge">reactive()</code>는 객체나 배열 자체를 반응형 객체로 만든다. <code class="language-plaintext highlighter-rouge">ref</code>와 달리 <code class="language-plaintext highlighter-rouge">.value</code> 없이 속성에 접근한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">reactive</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="nx">reactive</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">이순신</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">30</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">items</span> <span class="o">=</span> <span class="nx">reactive</span><span class="p">([</span><span class="dl">'</span><span class="s1">사과</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">바나나</span><span class="dl">'</span><span class="p">])</span>

<span class="kd">const</span> <span class="nx">celebrate</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">user</span><span class="p">.</span><span class="nx">age</span><span class="o">++</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">addItem</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">items</span><span class="p">.</span><span class="nx">push</span><span class="p">(</span><span class="s2">`과일 </span><span class="p">${</span><span class="nx">items</span><span class="p">.</span><span class="nx">length</span> <span class="o">+</span> <span class="mi">1</span><span class="p">}</span><span class="s2">`</span><span class="p">)</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">removeItem</span> <span class="o">=</span> <span class="p">(</span><span class="nx">index</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">items</span><span class="p">.</span><span class="nx">splice</span><span class="p">(</span><span class="nx">index</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span><span class="si">{{</span> <span class="nx">user</span><span class="p">.</span><span class="nx">name</span> <span class="si">}}</span> / <span class="si">{{</span> <span class="nx">user</span><span class="p">.</span><span class="nx">age</span> <span class="si">}}</span>세<span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"celebrate"</span><span class="nt">&gt;</span>나이 한 살 추가<span class="nt">&lt;/button&gt;</span>

  <span class="nt">&lt;ul&gt;</span>
    <span class="nt">&lt;li</span> <span class="na">v-for=</span><span class="s">"(item, index) in items"</span> <span class="na">:key=</span><span class="s">"item"</span><span class="nt">&gt;</span>
      <span class="si">{{</span> <span class="nx">item</span> <span class="si">}}</span>
      <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"removeItem(index)"</span><span class="nt">&gt;</span>삭제<span class="nt">&lt;/button&gt;</span>
    <span class="nt">&lt;/li&gt;</span>
  <span class="nt">&lt;/ul&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"addItem"</span><span class="nt">&gt;</span>과일 추가<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<h3 id="reactive의-주의점"><code class="language-plaintext highlighter-rouge">reactive</code>의 주의점</h3>

<p>반응형으로 만든 객체를 통째로 다른 객체로 교체하면 기존 반응성 연결이 끊긴다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">let</span> <span class="nx">state</span> <span class="o">=</span> <span class="nx">reactive</span><span class="p">({</span> <span class="na">count</span><span class="p">:</span> <span class="mi">0</span> <span class="p">})</span>

<span class="c1">// 잘못된 사용: 반응형 객체 자체를 교체함</span>
<span class="nx">state</span> <span class="o">=</span> <span class="p">{</span> <span class="na">count</span><span class="p">:</span> <span class="mi">5</span> <span class="p">}</span>

<span class="c1">// 올바른 사용: 기존 객체의 속성을 변경함</span>
<span class="nx">state</span><span class="p">.</span><span class="nx">count</span> <span class="o">=</span> <span class="mi">5</span>
</code></pre></div></div>

<p>객체를 통째로 교체해야 하거나 <code class="language-plaintext highlighter-rouge">.value</code>를 사용하는 방식이 더 명확하다 싶으면 <code class="language-plaintext highlighter-rouge">ref</code>를 선택하면 된다. 무조건 어느 한쪽이 정답이라기보다는 데이터의 형태와 팀의 작성 규칙에 맞춰 선택하는 것이 중요하다.</p>

<h3 id="ref와-reactive-비교"><code class="language-plaintext highlighter-rouge">ref</code>와 <code class="language-plaintext highlighter-rouge">reactive</code> 비교</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th><code class="language-plaintext highlighter-rouge">ref</code></th>
      <th><code class="language-plaintext highlighter-rouge">reactive</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>대상</td>
      <td>원시 값, 객체, 배열</td>
      <td>객체, 배열, Map, Set</td>
    </tr>
    <tr>
      <td>JavaScript 접근</td>
      <td><code class="language-plaintext highlighter-rouge">.value</code> 필요</td>
      <td>속성에 바로 접근</td>
    </tr>
    <tr>
      <td>객체 교체</td>
      <td><code class="language-plaintext highlighter-rouge">.value = 새 객체</code>로 가능</td>
      <td>연결이 끊길 수 있음</td>
    </tr>
    <tr>
      <td>사용 예</td>
      <td>단일 값, 교체 가능한 상태</td>
      <td>여러 속성을 묶은 객체</td>
    </tr>
  </tbody>
</table>

<h3 id="반응성-연결을-유지하는-보조-함수">반응성 연결을 유지하는 보조 함수</h3>

<p><code class="language-plaintext highlighter-rouge">reactive</code> 객체를 구조 분해하면 반응성 연결이 끊길 수 있다. 이때 <code class="language-plaintext highlighter-rouge">toRef</code>나 <code class="language-plaintext highlighter-rouge">toRefs</code>를 사용하면 원본 객체와 연결된 ref를 만들 수 있다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">reactive</span><span class="p">,</span> <span class="nx">toRefs</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">state</span> <span class="o">=</span> <span class="nx">reactive</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">홍길동</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">20</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="p">{</span> <span class="nx">name</span><span class="p">,</span> <span class="nx">age</span> <span class="p">}</span> <span class="o">=</span> <span class="nx">toRefs</span><span class="p">(</span><span class="nx">state</span><span class="p">)</span>

<span class="nx">age</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="c1">// state.age도 함께 변경됨</span>
</code></pre></div></div>

<p>그 밖의 반응성 보조 함수는 필요할 때 찾아서 사용하면 된다.</p>

<table>
  <thead>
    <tr>
      <th>함수</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">readonly</code></td>
      <td>반응형 값을 읽기 전용으로 만든다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">shallowRef</code></td>
      <td>최상위 ref 값의 변경만 추적한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">shallowReactive</code></td>
      <td>객체의 최상위 속성만 반응형으로 만든다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">unref</code></td>
      <td>ref라면 <code class="language-plaintext highlighter-rouge">.value</code>, 일반 값이면 원래 값을 반환한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">toRaw</code></td>
      <td>반응형 프록시의 원본 객체를 가져온다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">markRaw</code></td>
      <td>특정 객체가 반응형으로 변환되지 않게 한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">isRef</code>, <code class="language-plaintext highlighter-rouge">isReactive</code>, <code class="language-plaintext highlighter-rouge">isReadonly</code></td>
      <td>반응형 여부를 확인한다.</td>
    </tr>
  </tbody>
</table>

<h2 id="computed로-계산된-값-만들기"><code class="language-plaintext highlighter-rouge">computed()</code>로 계산된 값 만들기</h2>

<p><code class="language-plaintext highlighter-rouge">computed()</code>는 기존 반응형 상태를 바탕으로 새로운 값을 계산한다. 계산에 사용한 의존성이 변경될 때 다시 계산하고, 같은 값이 필요할 때는 이전 결과를 재사용한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">computed</span><span class="p">,</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">dummy</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">doubleCount</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">count</span><span class="p">.</span><span class="nx">value</span> <span class="o">*</span> <span class="mi">2</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">increaseCount</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">increaseDummy</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">dummy</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span>count: <span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>dummy: <span class="si">{{</span> <span class="nx">dummy</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;p&gt;</span>두 배 값: <span class="si">{{</span> <span class="nx">doubleCount</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>

  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increaseCount"</span><span class="nt">&gt;</span>count 증가<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increaseDummy"</span><span class="nt">&gt;</span>dummy 증가<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">dummy</code>가 변경되어 컴포넌트가 다시 렌더링되어도 <code class="language-plaintext highlighter-rouge">doubleCount</code>의 계산에 <code class="language-plaintext highlighter-rouge">dummy</code>가 사용되지 않았다면 다시 계산할 필요가 없다. 이것이 일반 함수와 <code class="language-plaintext highlighter-rouge">computed</code>의 중요한 차이다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">totalPrice</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">products</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">reduce</span><span class="p">((</span><span class="nx">sum</span><span class="p">,</span> <span class="nx">product</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="k">return</span> <span class="nx">sum</span> <span class="o">+</span> <span class="nx">product</span><span class="p">.</span><span class="nx">price</span>
  <span class="p">},</span> <span class="mi">0</span><span class="p">)</span>
<span class="p">})</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">computed</code>는 계산된 값이므로 템플릿에서 함수처럼 호출하지 않는다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- 올바른 사용 --&gt;</span>
<span class="nt">&lt;p&gt;</span>총가격: {{ totalPrice }}원<span class="nt">&lt;/p&gt;</span>

<span class="c">&lt;!-- 잘못된 사용 --&gt;</span>
<span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"totalPrice(products)"</span><span class="nt">&gt;</span>총가격<span class="nt">&lt;/button&gt;</span>
</code></pre></div></div>

<h3 id="일반-함수와-computed-구분">일반 함수와 <code class="language-plaintext highlighter-rouge">computed</code> 구분</h3>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 사용자 행동을 처리하는 일반 함수</span>
<span class="kd">const</span> <span class="nx">save</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">alert</span><span class="p">(</span><span class="dl">'</span><span class="s1">저장되었습니다.</span><span class="dl">'</span><span class="p">)</span>
<span class="p">}</span>

<span class="c1">// 상태에서 값을 계산하는 computed</span>
<span class="kd">const</span> <span class="nx">completedCount</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">todos</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">filter</span><span class="p">((</span><span class="nx">todo</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">todo</span><span class="p">.</span><span class="nx">done</span><span class="p">).</span><span class="nx">length</span>
<span class="p">})</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자가 행동한다 → 일반 함수
상태에서 값이 나온다 → computed
</code></pre></div></div>

<h2 id="watch로-변경-후-작업-실행하기"><code class="language-plaintext highlighter-rouge">watch()</code>로 변경 후 작업 실행하기</h2>

<p><code class="language-plaintext highlighter-rouge">watch()</code>는 특정 반응형 값의 변경을 감시하고, 값이 바뀐 뒤 후속 작업을 실행한다.</p>

<ul>
  <li>서버 요청</li>
  <li>로컬 스토리지 저장</li>
  <li>로그 기록</li>
  <li>다른 상태 변경</li>
  <li>외부 라이브러리와 동기화</li>
</ul>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span><span class="p">,</span> <span class="nx">watch</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">currentCity</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">logMessage</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">아직 감시 시스템이 작동하지 않았습니다.</span><span class="dl">'</span><span class="p">)</span>

<span class="nx">watch</span><span class="p">(</span><span class="nx">currentCity</span><span class="p">,</span> <span class="p">(</span><span class="nx">newValue</span><span class="p">,</span> <span class="nx">oldValue</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">logMessage</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="s2">`</span><span class="p">${</span><span class="nx">oldValue</span><span class="p">}</span><span class="s2">에서 </span><span class="p">${</span><span class="nx">newValue</span><span class="p">}</span><span class="s2">(으)로 변경됨.`</span>
  <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">newValue</span><span class="p">}</span><span class="s2">의 날씨 API를 다시 조회합니다.`</span><span class="p">)</span>
<span class="p">})</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span>현재 도시: <span class="si">{{</span> <span class="nx">currentCity</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"currentCity = '서울'"</span><span class="nt">&gt;</span>서울<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"currentCity = '수원'"</span><span class="nt">&gt;</span>수원<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"currentCity = '부산'"</span><span class="nt">&gt;</span>부산<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;p&gt;</span><span class="si">{{</span> <span class="nx">logMessage</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>콜백에는 새 값과 이전 값이 순서대로 전달된다.</p>

<h3 id="여러-값-감시하기">여러 값 감시하기</h3>

<p>첫 번째 인자에 감시할 ref를 배열로 전달하면 여러 값을 한 번에 감시할 수 있다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span><span class="p">,</span> <span class="nx">watch</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">city</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">dateType</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">오늘</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">apiStatus</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">대기 중...</span><span class="dl">'</span><span class="p">)</span>

<span class="nx">watch</span><span class="p">(</span>
  <span class="p">[</span><span class="nx">city</span><span class="p">,</span> <span class="nx">dateType</span><span class="p">],</span>
  <span class="p">([</span><span class="nx">newCity</span><span class="p">,</span> <span class="nx">newDate</span><span class="p">],</span> <span class="p">[</span><span class="nx">oldCity</span><span class="p">,</span> <span class="nx">oldDate</span><span class="p">])</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">apiStatus</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="s2">`</span><span class="p">${</span><span class="nx">oldCity</span><span class="p">}</span><span class="s2">(</span><span class="p">${</span><span class="nx">oldDate</span><span class="p">}</span><span class="s2">) → </span><span class="p">${</span><span class="nx">newCity</span><span class="p">}</span><span class="s2">(</span><span class="p">${</span><span class="nx">newDate</span><span class="p">}</span><span class="s2">)`</span>
  <span class="p">}</span>
<span class="p">)</span>
</code></pre></div></div>

<p>감시 대상 배열의 순서와 콜백의 새 값·이전 값 배열 순서는 서로 대응한다.</p>

<h3 id="객체와-배열-깊이-감시하기">객체와 배열 깊이 감시하기</h3>

<p>객체나 배열 내부의 변경까지 감시하려면 <code class="language-plaintext highlighter-rouge">deep: true</code>를 사용할 수 있다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span><span class="p">,</span> <span class="nx">watch</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">홍길동</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">20</span>
<span class="p">})</span>

<span class="nx">watch</span><span class="p">(</span>
  <span class="nx">user</span><span class="p">,</span>
  <span class="p">(</span><span class="nx">newUser</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`현재 이름: </span><span class="p">${</span><span class="nx">newUser</span><span class="p">.</span><span class="nx">name</span><span class="p">}</span><span class="s2">`</span><span class="p">)</span>
  <span class="p">},</span>
  <span class="p">{</span> <span class="na">deep</span><span class="p">:</span> <span class="kc">true</span> <span class="p">}</span>
<span class="p">)</span>
</code></pre></div></div>

<p>다만 깊은 감시는 객체 전체를 재귀적으로 확인하므로 꼭 필요한 경우에 사용하는 것이 좋다. 내부 속성의 변경을 감시하면서 이전 값도 정확하게 비교하고 싶다면 특정 속성만 감시한다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">watch</span><span class="p">(</span>
  <span class="p">()</span> <span class="o">=&gt;</span> <span class="nx">user</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">age</span><span class="p">,</span>
  <span class="p">(</span><span class="nx">newAge</span><span class="p">,</span> <span class="nx">oldAge</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">oldAge</span><span class="p">}</span><span class="s2">세에서 </span><span class="p">${</span><span class="nx">newAge</span><span class="p">}</span><span class="s2">세로 변경됨.`</span><span class="p">)</span>
  <span class="p">}</span>
<span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">reactive</code> 객체 자체를 감시하면 내부 변경을 감지할 수 있지만, 중첩 속성 변경에서는 새 값과 이전 값이 같은 객체를 가리킬 수 있다. 이전 값 비교가 필요하면 위처럼 감시할 속성을 함수로 지정하는 편이 명확하다.</p>

<h2 id="watcheffect로-자동-감시하기"><code class="language-plaintext highlighter-rouge">watchEffect()</code>로 자동 감시하기</h2>

<p><code class="language-plaintext highlighter-rouge">watchEffect()</code>는 감시할 대상을 따로 적지 않는다. 함수 내부에서 사용하는 반응형 값을 자동으로 추적하고, 값이 변경될 때 다시 실행한다.</p>

<ul>
  <li>컴포넌트가 처음 생성될 때 즉시 한 번 실행된다.</li>
  <li>함수 내부에서 읽은 반응형 값만 추적한다.</li>
  <li><code class="language-plaintext highlighter-rouge">oldValue</code>를 제공하지 않는다.</li>
  <li>이전 값과 비교해야 하면 <code class="language-plaintext highlighter-rouge">watch</code>를 사용한다.</li>
</ul>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span><span class="p">,</span> <span class="nx">watchEffect</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">city</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">message</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">''</span><span class="p">)</span>

<span class="nx">watchEffect</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">message</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="s2">`</span><span class="p">${</span><span class="nx">city</span><span class="p">.</span><span class="nx">value</span><span class="p">}</span><span class="s2">의 날씨를 조회합니다.`</span>
<span class="p">})</span>
</code></pre></div></div>

<h3 id="watch-watcheffect-비교"><code class="language-plaintext highlighter-rouge">watch</code>, <code class="language-plaintext highlighter-rouge">watchEffect</code> 비교</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th><code class="language-plaintext highlighter-rouge">watch</code></th>
      <th><code class="language-plaintext highlighter-rouge">watchEffect</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>감시 대상</td>
      <td>직접 지정</td>
      <td>함수 안에서 자동 추적</td>
    </tr>
    <tr>
      <td>최초 실행</td>
      <td>기본적으로 변경 후 실행</td>
      <td>생성 직후 즉시 실행</td>
    </tr>
    <tr>
      <td>이전 값</td>
      <td>제공</td>
      <td>제공하지 않음</td>
    </tr>
    <tr>
      <td>적합한 상황</td>
      <td>특정 값의 변경 전후 비교</td>
      <td>여러 의존성을 간단히 동기화</td>
    </tr>
  </tbody>
</table>

<p>DOM 업데이트가 끝난 뒤 실행해야 하면 <code class="language-plaintext highlighter-rouge">watchPostEffect</code>, 값이 바뀌는 즉시 동기적으로 실행해야 하면 <code class="language-plaintext highlighter-rouge">watchSyncEffect</code>를 검토할 수 있다. 다만 실행 타이밍을 세밀하게 제어해야 하는 경우에만 사용하자.</p>

<h2 id="생명주기-훅-맛보기">생명주기 훅 맛보기</h2>

<p>Composition API에서는 컴포넌트의 생명주기 훅도 함수로 사용한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">onMounted</span><span class="p">,</span> <span class="nx">onUnmounted</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">handleResize</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nb">window</span><span class="p">.</span><span class="nx">innerWidth</span><span class="p">)</span>
<span class="p">}</span>

<span class="nx">onMounted</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nb">window</span><span class="p">.</span><span class="nx">addEventListener</span><span class="p">(</span><span class="dl">'</span><span class="s1">resize</span><span class="dl">'</span><span class="p">,</span> <span class="nx">handleResize</span><span class="p">)</span>
<span class="p">})</span>

<span class="nx">onUnmounted</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nb">window</span><span class="p">.</span><span class="nx">removeEventListener</span><span class="p">(</span><span class="dl">'</span><span class="s1">resize</span><span class="dl">'</span><span class="p">,</span> <span class="nx">handleResize</span><span class="p">)</span>
<span class="p">})</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">onMounted</code>: 컴포넌트가 DOM에 마운트된 뒤 실행한다.</li>
  <li><code class="language-plaintext highlighter-rouge">onUpdated</code>: 반응형 상태 변경으로 업데이트된 뒤 실행한다.</li>
  <li><code class="language-plaintext highlighter-rouge">onUnmounted</code>: 컴포넌트가 제거된 뒤 실행한다. 이벤트 리스너나 타이머 정리에 사용한다.</li>
</ul>

<p>특히 <code class="language-plaintext highlighter-rouge">onMounted</code>에서 등록한 이벤트나 타이머는 <code class="language-plaintext highlighter-rouge">onUnmounted</code>에서 정리하는 습관을 들이는 것이 좋다.</p>

<h2 id="composition-api를-composable로-확장하기">Composition API를 composable로 확장하기</h2>

<p>Composition API의 장점은 반복되는 로직을 함수로 분리해 여러 컴포넌트에서 재사용할 수 있다는 점이다. 이런 재사용 함수를 보통 <strong>composable</strong>이라고 부른다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// useCounter.js</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">computed</span><span class="p">,</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="k">export</span> <span class="kd">function</span> <span class="nx">useCounter</span><span class="p">(</span><span class="nx">initialValue</span> <span class="o">=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="nx">initialValue</span><span class="p">)</span>
  <span class="kd">const</span> <span class="nx">doubleCount</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="nx">count</span><span class="p">.</span><span class="nx">value</span> <span class="o">*</span> <span class="mi">2</span><span class="p">)</span>

  <span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
  <span class="p">}</span>

  <span class="kd">const</span> <span class="nx">reset</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">count</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="nx">initialValue</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="p">{</span>
    <span class="nx">count</span><span class="p">,</span>
    <span class="nx">doubleCount</span><span class="p">,</span>
    <span class="nx">increase</span><span class="p">,</span>
    <span class="nx">reset</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">useCounter</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./useCounter</span><span class="dl">'</span>

<span class="kd">const</span> <span class="p">{</span> <span class="nx">count</span><span class="p">,</span> <span class="nx">doubleCount</span><span class="p">,</span> <span class="nx">increase</span><span class="p">,</span> <span class="nx">reset</span> <span class="p">}</span> <span class="o">=</span> <span class="nx">useCounter</span><span class="p">(</span><span class="mi">10</span><span class="p">)</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span><span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span> / <span class="si">{{</span> <span class="nx">doubleCount</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increase"</span><span class="nt">&gt;</span>증가<span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"reset"</span><span class="nt">&gt;</span>초기화<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>상태와 함수가 특정 기능을 중심으로 묶여 있으니, 컴포넌트가 커져도 로직을 분리해서 관리할 수 있다. 나중에 <code class="language-plaintext highlighter-rouge">useFetch</code>, <code class="language-plaintext highlighter-rouge">useTodo</code>, <code class="language-plaintext highlighter-rouge">useAuth</code> 같은 형태로 확장하는 흐름이다.</p>

<h2 id="최종-정리">최종 정리</h2>

<p>Composition API는 단순히 함수 몇 개를 새로 사용하는 문법이 아니다. 컴포넌트의 로직을 기능 단위로 구성하고, 필요한 반응형 흐름을 조합하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ref / reactive
      ↓
반응형 상태 생성
      ↓
computed로 파생 값 계산
      ↓
watch 또는 watchEffect로 변경 감지
      ↓
함수와 템플릿으로 화면 연결
      ↓
composable로 재사용
</code></pre></div></div>

<p>기억할 것만 다시 적어보자.</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">ref</code>는 원시 값과 객체·배열을 반응형으로 만든다.</li>
  <li><code class="language-plaintext highlighter-rouge">reactive</code>는 객체나 배열 자체를 반응형으로 만든다.</li>
  <li>JavaScript에서 <code class="language-plaintext highlighter-rouge">ref</code>는 <code class="language-plaintext highlighter-rouge">.value</code>, 템플릿에서는 <code class="language-plaintext highlighter-rouge">.value</code>를 생략한다.</li>
  <li><code class="language-plaintext highlighter-rouge">computed</code>는 상태에서 파생된 값을 계산한다.</li>
  <li><code class="language-plaintext highlighter-rouge">watch</code>는 특정 값의 변경 후 작업을 실행한다.</li>
  <li><code class="language-plaintext highlighter-rouge">watchEffect</code>는 함수 내부에서 사용하는 반응형 값을 자동으로 추적한다.</li>
  <li><code class="language-plaintext highlighter-rouge">onUnmounted</code>에서 이벤트와 타이머를 정리한다.</li>
  <li>반복되는 로직은 composable로 분리한다.</li>
</ol>

<p>처음에는 <code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">computed</code>, <code class="language-plaintext highlighter-rouge">watch</code>만으로도 충분하다. 나머지는 실제로 필요한 상황이 왔을 때 하나씩 살펴보면 된다. 현업에 가서 빨리 써보고 싶다. 일단은 여기까지!</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="Vue.js" /><category term="Composition API" /><category term="ref" /><category term="reactive" /><category term="computed" /><category term="watch" /><summary type="html"><![CDATA[Vue Composition API의 기본 개념과 ref, reactive, computed, watch, watchEffect의 사용 방법을 정리한다.]]></summary></entry><entry><title type="html">Psql JSONB, 행 잠금, 멱등성</title><link href="https://minnong511.github.io/db/postgresql/2026/07/31/psql2/" rel="alternate" type="text/html" title="Psql JSONB, 행 잠금, 멱등성" /><published>2026-07-31T09:00:00+09:00</published><updated>2026-07-31T09:00:00+09:00</updated><id>https://minnong511.github.io/db/postgresql/2026/07/31/psql2</id><content type="html" xml:base="https://minnong511.github.io/db/postgresql/2026/07/31/psql2/"><![CDATA[<h1 id="jsonb">JSONB</h1>
<h2 id="jsonb-객체-생성">JSONB 객체 생성</h2>
<h2 id="중첩-json-반환">중첩 JSON 반환</h2>
<h2 id="jsonb_pretty와-to_jsonb">jsonb_pretty와 to_jsonb</h2>

<h1 id="행-잠금">행 잠금</h1>
<h2 id="동시성-문제">동시성 문제</h2>
<h2 id="select-for-update">SELECT FOR UPDATE</h2>
<h2 id="안전한-재고-처리-순서">안전한 재고 처리 순서</h2>

<h1 id="멱등성idempotency">멱등성(Idempotency)</h1>
<h2 id="멱등성-개념">멱등성 개념</h2>
<h2 id="batch_key">batch_key</h2>
<h2 id="on-conflict-do-nothing">ON CONFLICT DO NOTHING</h2>
<h2 id="get-diagnostics와-row_count">GET DIAGNOSTICS와 ROW_COUNT</h2>]]></content><author><name>Min Hyeong Lee</name></author><category term="DB" /><category term="PostgreSQL" /><category term="PostgreSQL" /><summary type="html"><![CDATA[JSONB JSONB 객체 생성 중첩 JSON 반환 jsonb_pretty와 to_jsonb]]></summary></entry><entry><title type="html">Psql 함수,프로시져,트리거</title><link href="https://minnong511.github.io/db/postgresql/2026/07/31/psql1/" rel="alternate" type="text/html" title="Psql 함수,프로시져,트리거" /><published>2026-07-31T08:00:00+09:00</published><updated>2026-07-31T08:00:00+09:00</updated><id>https://minnong511.github.io/db/postgresql/2026/07/31/psql1</id><content type="html" xml:base="https://minnong511.github.io/db/postgresql/2026/07/31/psql1/"><![CDATA[<h1 id="함수function">함수(Function)</h1>

<p>psql에서 함수의 기본 구조는 다음과 같다.</p>

<h3 id="함수-기본-구조">함수 기본 구조</h3>

<p>’'’sql
CREATE OR REPLACE FUNCTION 스키마명.함수명(
    매개변수명 데이터타입
)
RETURNS 반환타입
LANGUAGE 언어
AS \(함수 본문\);
‘’’</p>

<h3 id="예시-함수-작성">예시 함수 작성</h3>

<p>예를 들어 고객 등급에 따른 할인율 반환하는 함수는 다음과 같이 작성할 수 있다.</p>

<p>’'’sql
CREATE OR REPLACE FUNCTION proc_lab.fn_grade_discount_rate(
    p_grade text
)
RETURNS numeric
LANGUAGE sql
AS \(SELECT CASE UPPER(TRIM(p_grade))
        WHEN 'BASIC' THEN 0
        WHEN 'SILVER' THEN 0.05
        WHEN 'GOLD' THEN 0.10
        WHEN 'VIP' THEN 0.15
        ELSE 0
    END;\);
‘’’</p>

<ul>
  <li>함수 호출</li>
</ul>

<p>’'’sql 
SELECT proc_lab.fn_grade_discount_rate(‘ gold ‘);
‘’’</p>

<h2 id="sql-함수와-plpgsql-함수">SQL 함수와 PL/pgSQL 함수</h2>

<p>PostgreSQL 함수는 본문을 어떤 언어로 작성하는지에 따라 실행 방식이 달라지는데요, 이번에는 LANGUAGE sql 과 LANGAUGE plpgsql을 사용했습니다.</p>

<h2 id="sql-함수">SQL 함수</h2>

<p>LANGUAGE sql 함수는 일반적으로 하나의 SQL 문장이나 비교적 단순한 조회 로직을 작성할 때 사용합니다</p>

<p>’'’sql 
create or replace function proc_lab.fn_grade_discount_rate(
    p_grade text
)
returns numeric 
LANGUAGE sql 
AS \(SELECT CASE UPPER(TRIM(p_grade))
        WHEN 'GOLD' THEN 0.10 
        ELSE 0 
    END;\); 
‘’’
SQL 함수는 다음과 같은 경우에 적합합니다.</p>
<ul>
  <li>단순 값 계산</li>
  <li>조건에 따른 값 반환</li>
  <li>테이블 조회 결과 반화</li>
  <li>복잡한 데이터 제어가 필요하지 않은 경우</li>
</ul>

<h3 id="plpgsql-함수">PL/pgSQL 함수</h3>
<p>LANGUAGE plpgsql 함수는 PostgreSQL의 절차형 언어를 사용한다. 
변수 선언, 조건문, 예외 처리처럼 일반 SQL으로 표현하기 어려운 로직을 작성할 수 있다.</p>

<p>’’’ 
CREATE OR REPLACE FUNCTION proc_lab.fn_customer_order_summary_json(
    p_customer_id bigint
)
returns jsonb 
LANGUAGE plpgsql 
AS $$ 
DECLARE 
    v_customer jsonb; 
Begin 
    select jsonb_build_object(
        ‘customer_id’, c.customer_id, 
        ‘name’, c.customer_name 
    )
    into v_customer 
    from proc_lab.customer c
    where c.customer_id = p_customer_id;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if v_customer is null then
    raise exception 'Customer ID % does not exist', p_customer_id; 
end if; 

return v_customer;  end;  $$; 
</code></pre></div></div>

<p>’’’</p>

<p>PL/pgSQL은 다음과 같은 경우에 적합하다.</p>
<ul>
  <li>DECLARE를 이용한 변수 선언</li>
  <li>IF, CASE 를 이용한 조건 처리</li>
  <li>여러 SQL 문장 순차 실행</li>
  <li>RAISE EXCEPTION을 이용한 예외 처리</li>
  <li>조회 결과를 변수에 저장하는 SELECT INTO</li>
</ul>

<p>정리하자면 단순 조회와 계산에는 SQL 함수가 적합하고, 제어 흐름이나 예외 처리가 필요하다면 PL/pgSQL 함수가 적합하다.</p>

<h2 id="단일-값테이블jsonb-반환">단일 값·테이블·JSONB 반환</h2>

<h2 id="함수-속성과-주요-문법">함수 속성과 주요 문법</h2>

<h1 id="프로시저procedure">프로시저(Procedure)</h1>

<h2 id="함수와-프로시저의-차이">함수와 프로시저의 차이</h2>

<h2 id="in-out-inout-매개변수">IN, OUT, INOUT 매개변수</h2>

<h2 id="주문-생성-프로시저">주문 생성 프로시저</h2>

<h2 id="returning-into">RETURNING INTO</h2>

<h2 id="트랜잭션-기반-테스트">트랜잭션 기반 테스트</h2>

<h1 id="트리거trigger">트리거(Trigger)</h1>

<h2 id="트리거의-기본-구조">트리거의 기본 구조</h2>

<h2 id="oldnewtg_op">OLD·NEW·TG_OP</h2>

<h2 id="감사-로그-트리거">감사 로그 트리거</h2>

<h2 id="주문-총액-자동-갱신">주문 총액 자동 갱신</h2>]]></content><author><name>Min Hyeong Lee</name></author><category term="DB" /><category term="PostgreSQL" /><category term="PostgreSQL" /><summary type="html"><![CDATA[함수(Function)]]></summary></entry><entry><title type="html">Vue Intro -</title><link href="https://minnong511.github.io/frontend/vue/2026/07/30/vue_intro1/" rel="alternate" type="text/html" title="Vue Intro -" /><published>2026-07-30T20:00:00+09:00</published><updated>2026-07-30T20:00:00+09:00</updated><id>https://minnong511.github.io/frontend/vue/2026/07/30/vue_intro1</id><content type="html" xml:base="https://minnong511.github.io/frontend/vue/2026/07/30/vue_intro1/"><![CDATA[<h2 id="vuejs">Vue.js</h2>

<p>자자! 오늘은 Vue.js라는 것을 들고 왔다. (프론트엔드 첨해봄 ㅎ)</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[오늘의 주제]
      ↓
   [Vue.js]
</code></pre></div></div>

<p>Vue.js는 Front-end JavaScript Framework이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Vue.js]
   ↓
[사용자 인터페이스 구축]
</code></pre></div></div>

<p>즉, 사용자 인터페이스 구축을 위한 프레임워크이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Vue.js]
   ↓
┌──────────────────────┐
│ 사용자 인터페이스 구축 │
└──────────────────────┘
</code></pre></div></div>

<p>이걸로 무엇을 할 수 있을까?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Vue.js]
 ├─ 간결한 문법
 ├─ 반응형 데이터 바인딩
 ├─ 컴포넌트
 ├─ 가상 DOM
 └─ 트랜지션 효과
</code></pre></div></div>

<p>Vue.js를 이용해서 간결한 문법, 반응형 데이터 바인딩, 컴포넌트, 가상 DOM, 트랜지션 효과가 가능하다. (노가다 멈춰!)</p>

<h2 id="왜-만들어졌을까">왜 만들어졌을까?</h2>

<ul>
  <li>이유는 간단하다… 
그야.. 기존의 것이 불편하니까… (귀찮은 건 질색이야! 딩기리딩딩~~~)</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[기존 방식의 불편함]
          ↓
 [더 간결한 개발 방식]
</code></pre></div></div>

<p>Vue.js는 “기존 프레임워크들의 장점만 흡수하여 더 가볍고 쓰기 쉽게 만들자”는 아이디어에서 시작했다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[기존 프레임워크의 장점]
            +
      [가벼운 사용성]
            ↓
         [Vue.js]
</code></pre></div></div>

<p>근데 기존의 HTML과 JavaScript와 뭐가 다른 걸까?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[HTML + JavaScript]
          ↕ 비교
       [Vue.js]
</code></pre></div></div>

<p>지금부터 알아보도록 하자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[다음 순서]
    ↓
[Vue.js의 핵심 특징]
</code></pre></div></div>

<h2 id="vuejs의-특징-1---mvvm-architecture">Vue.js의 특징 1 - MVVM Architecture</h2>

<p>Vue.js의 설계는 MVVM(Model-View-ViewModel) Architecture Pattern에서 영감을 받았지만, MVVM을 엄격하게 따르는 것은 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Model] ⇄ [ViewModel] ⇄ [View]
              ↑
       Vue의 반응형 연결
</code></pre></div></div>

<p>이게 뭐냐고요? 잘 따라와 보십쇼.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[궁금증]
   ↓
[MVVM 구조 살펴보기]
</code></pre></div></div>

<h3 id="then-what-is-mvvm">Then, what is MVVM?</h3>

<p>MVVM Architecture Pattern은 데이터를 보여주는 View와 데이터를 처리하는 Model 사이에 데이터를 중개하는 ViewModel을 두어 UI를 그리는 부분과 Data 처리 로직을 분리하는 Architecture Pattern이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Model: 데이터]
       ↕
[ViewModel: 연결·상태]
       ↕
[View: 화면]
</code></pre></div></div>

<h3 id="model">model</h3>

<ul>
  <li>애플리케이션에서 사용하는 데이터와 상태. Vue에서는 보통 <code class="language-plaintext highlighter-rouge">&lt;script&gt;</code> 내부에 정의한 반응형 JavaScript 상태가 이에 해당한다.</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[애플리케이션 데이터]
          ↓
 [반응형 JavaScript 상태]
</code></pre></div></div>

<h3 id="view">View</h3>

<ul>
  <li>사용자에게 실제로 보여지는 화면 인터페이스와 DOM 구조, 시각적 스타일링을 담당</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[View]
  ├─ 화면 인터페이스
  ├─ DOM 구조
  └─ 시각적 스타일링
</code></pre></div></div>

<h3 id="viewmodel">ViewModel</h3>

<ul>
  <li>View와 Model 사이를 연결해주는 중간 다리. Vue의 반응형 상태·템플릿 연결 로직과 런타임이 ViewModel에 가까운 역할을 수행</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[View] ⇄ [반응형 상태·템플릿 로직] ⇄ [Model]
</code></pre></div></div>

<h3 id="그러면-어떻게-생긴-걸까">그러면 어떻게 생긴 걸까?</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Model]
   ⇅
[ViewModel]
   ⇅
[View]
</code></pre></div></div>

<h3 id="mvvm-구조는-왜-나온-걸까">MVVM 구조는 왜 나온 걸까?</h3>

<p>근데 대충 어떻게 생겼는지 다들 알았을 것이라 생각한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Model] → [ViewModel] → [View]
</code></pre></div></div>

<p>그러면 이게 왜 만들어진 것일까?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[분리된 역할]
      ↓
[변경 관리가 쉬운 구조]
</code></pre></div></div>

<ol>
  <li>기존 브라우저의 Real DOM을 자주 직접 조작하면 렌더링 비용이 커질 수 있다.
    <ul>
      <li>DOM: Browser가 HTML을 인식하여 Tree 구조로 만든 Object Model</li>
    </ul>
  </li>
</ol>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[HTML]
  ↓ 파싱
[DOM Tree]
  ↓ 렌더링 계산
[화면]
</code></pre></div></div>

<p>기존 브라우저에서는 실제 DOM이 수정되면 Browser는 Layout을 다시 계산(Reflow)하고 화면을 다시 색칠하는 Repaint 연산을 수행할 수 있다. 이러면 연속적인 상태 변경으로 인해 노드를 재구성하는 작업이 반복되고, Layout/Paint 연산 부하로 화면이 느려질 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[DOM 수정]
    ↓
[Layout/Reflow]
    ↓
[Paint/Repaint]
    ↓
[화면 갱신]
</code></pre></div></div>

<p>뭐 예시를 간단하게 들어보자면..</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[추상적인 설명]
        ↓
   [생활 속 예시]
</code></pre></div></div>

<p>브라우저의 상태가 변경될 때마다 직접 Real DOM을 연속해서 조작하므로, 만약 10번의 데이터 변화가 일어나면 브라우저가 매번 DOM 변경을 처리하고 그 결과에 따라 여러 번의 스타일 계산·Layout·Paint가 발생할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>데이터 변화 1 ─┐
데이터 변화 2 ─┤
데이터 변화 … ─┼→ DOM 처리 → Layout/Paint 가능
데이터 변화 10 ─┘
</code></pre></div></div>

<p>실생활에서의 예를 들자면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[데이터 변경]
      ↓ 비유
[마트를 여러 번 방문]
</code></pre></div></div>

<p>감자 10개를 환불하고 고구마 5개를 사야하는데, 감자 환불 하러 10번 마트가고 고구마 사러 마트 5번 가는 느낌이랄까? 그렇게 예를 들 수 있을 거 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[감자 환불 10번] + [고구마 구매 5번]
                 ↓
          [마트 방문 반복]
</code></pre></div></div>

<ol>
  <li>Vue.js는 Virtual DOM과 렌더러를 통해 필요한 DOM 변경만 적용한다.</li>
</ol>

<ul>
  <li>Virtual DOM: 실제 브라우저의 DOM과 동기화되는 메모리상의 UI 표현</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[상태 변화]
    ↓
[Virtual DOM 비교]
    ↓
[필요한 Real DOM만 업데이트]
</code></pre></div></div>

<p>아무튼 이런 문제를 줄이기 위해서 Vue.js에서는 Virtual DOM과 렌더러로 변경 사항을 비교한 뒤 필요한 DOM 업데이트를 적용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[이전 Virtual DOM] ⇄ [새 Virtual DOM]
          ↓ 차이 계산
    [필요한 DOM 업데이트]
</code></pre></div></div>

<p>10번 변경할 것을 여러 번의 상태 변경으로 모아서 한 번의 업데이트 사이클에 처리할 수 있게 해준다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[상태 변경 1]
[상태 변경 2] ─┐
[상태 변경 …] ─┼→ [업데이트 사이클 1회]
[상태 변경 10] ┘
</code></pre></div></div>

<p>개발자가 직접 DOM을 일일이 제어하는 번거로움 없이, 상태 변화만 선언해도 Vue가 반응형 의존성을 추적하고 변경된 Virtual DOM을 이전 것과 비교해 필요한 DOM 업데이트를 계산한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[상태 선언]
    ↓
[반응형 의존성 추적]
    ↓
[Virtual DOM 비교]
    ↓
[필요한 DOM 업데이트 계산]
</code></pre></div></div>

<p>Batch 처리를 통한 렌더링 횟수 최적화를 통해 Vue는 한 업데이트 사이클 안에 여러 번 데이터가 바뀌어도 변경 사항을 모아서 실제 DOM에 한 번 반영할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[여러 상태 변경]
        ↓ Batch
[한 번의 업데이트 사이클]
        ↓
   [Real DOM 반영]
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[상태 변경]
    ↓
[Vue 반응성 + Virtual DOM]
    ↓
[필요한 화면만 갱신]
</code></pre></div></div>

<h2 id="vuejs-특징-2---two-way-data-binding">Vue.js 특징 2 - Two-Way Data Binding</h2>

<p>Vue.js에서 <code class="language-plaintext highlighter-rouge">v-model</code>을 사용하면 양방향 데이터 바인딩 (Two-Way Data Binding)을 구현할 수 있다. “Model(JavaScript Data)이 바뀌면 View(화면)가 바뀌고, 반대로 View(화면 입력)가 바뀌면 Model도 동시에 바뀐다”는 것을 의미한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Model: JavaScript Data]
          ⇅
 [v-model 양방향 연결]
          ⇅
      [View: 입력 화면]
</code></pre></div></div>

<h2 id="vuejs-특징-3---component-based-architecture">Vue.js 특징 3 - Component Based Architecture</h2>

<p>웹페이지를 통째로 만들지 않는다!</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[큰 웹페이지 하나]
        ↓ 분리
[여러 개의 컴포넌트]
</code></pre></div></div>

<p>이제는 조각조각 하나씩 만들고 각각을 조립하는 시스템을 채택한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Header] + [Content] + [Footer]
              ↓ 조립
          [웹페이지]
</code></pre></div></div>

<ul>
  <li>이 방식을 Component(독립 요소) Based Architecture라고 한다.</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Component Based Architecture]
              ↓
 [독립적인 요소를 조립해 화면 구성]
</code></pre></div></div>

<p>세 가지 특징을 가진다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Component]
 ├─ Encapsulation
 ├─ Reusability
 └─ Tree Structure
</code></pre></div></div>

<ul>
  <li>Encapsulation : Single-File Component(SFC)로 작성하면 컴포넌트의 HTML(구조), JavaScript(로직), CSS(스타일)를 하나의 파일(.vue) 안에 응집시켜 놓는다.</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Component.vue]
 ├─ &lt;template&gt;  구조
 ├─ &lt;script&gt;    로직
 └─ &lt;style&gt;     스타일
</code></pre></div></div>

<ul>
  <li>Reusability: 잘 만든 Component는 여러 곳에서 다시 가져다 쓸 수 있어 중복 코드가 제거된다. (재탕이 최고!)</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>       [재사용]
          ↑
[Component] ─┬→ [화면 A]
             └→ [화면 B]
</code></pre></div></div>

<ul>
  <li>Tree Structure: Component들은 부모-자식 관계를 가지며 부모는 자식에게 데이터를 내려주고(Props Down), 자식은 부모에게 상태 변경을 알려준다(Event Up = Emit).</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>        [Parent]
        ↙      ↘
 [Child A]   [Child B]
    ↑ Props Down / Event Up ↑
</code></pre></div></div>

<h2 id="vuejs-특징-4---rendering">Vue.js 특징 4 - Rendering</h2>

<p>근데 이러면 화면은 어떻게 표시되는 걸까?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Vue 상태·템플릿]
        ↓ 렌더링
      [화면]
</code></pre></div></div>

<p>HTML은 조상님이 렌더해주시는 걸까?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[HTML]
  ?
[조상님 렌더링?]
</code></pre></div></div>

<p>그럴 리는 없겠지…?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[조상님]
   ✕
[렌더링 담당 아님]
</code></pre></div></div>

<p>아쉽게도 조상님 대신 콤퓨타가 렌더해준다..</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Vue + Browser + JavaScript]
             ↓
          [렌더링]
</code></pre></div></div>

<p>화면을 구성하는 대표적인 방식은 2가지이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>          [렌더링 방식]
             ↙   ↘
          [CSR] [SSR]
</code></pre></div></div>

<ol>
  <li>CSR(Client-Side-Rendering)
    <ul>
      <li>브라우저(Client)가 JavaScript를 실행하여 화면을 직접 그린다(rendering).</li>
    </ul>
  </li>
  <li>SSR(Server-Side-Rendering)
    <ul>
      <li>서버(Server)에서 Vue 컴포넌트를 HTML로 렌더링해 브라우저에 내려주는 방식이다. 데이터는 서버에서 미리 주입할 수도 있고, Vue의 SSR API나 Nuxt.js 같은 프레임워크를 이용해서 구현한다.</li>
    </ul>
  </li>
</ol>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[CSR] 브라우저에서 JavaScript 실행 → 화면 렌더링
[SSR] 서버에서 HTML 렌더링       → 브라우저 전달
</code></pre></div></div>

<h2 id="vuejs로-구현하는-spa-single-page-application">Vue.js로 구현하는 SPA (Single Page Application)</h2>

<p>자자 아직 할 것이 많으니 다음으로 넘어가자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Vue 특징 1~4]
        ↓
[SPA 방식 살펴보기]
</code></pre></div></div>

<p>Vue.js로 구현할 수 있는 SPA(Single Page Application)에 대해서 알아보자?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Vue.js]
    ↓ 구현
[SPA]
</code></pre></div></div>

<h3 id="기존--mpa">기존 : MPA</h3>

<p>기존에는 사용자가 메뉴를 클릭할 때마다 새 HTML을 새로 요청해서 렌더했다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[메뉴 클릭]
    ↓
[새 HTML 요청]
    ↓
[전체 페이지 렌더링]
</code></pre></div></div>

<p>그래서 페이지 전환마다 HTML과 리소스를 다시 요청할 수 있어 네트워크 비용과 처리 시간이 늘어날 수 있었다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[페이지 전환]
      ↓
[HTML·리소스 재요청 가능]
      ↓
[네트워크·처리 비용 증가 가능]
</code></pre></div></div>

<h3 id="신규--spa">신규 : SPA</h3>

<p>다행히도! Vue.js로 SPA (Single Page Application)를 구현하면 HTML 문서를 처음에 불러온 뒤, 클라이언트 측 JavaScript가 필요한 데이터와 화면만 업데이트하는 방식으로 변경을 처리할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[HTML 문서 최초 로드]
          ↓
[클라이언트 JavaScript]
          ↓
 [변경된 화면만 업데이트]
</code></pre></div></div>

<h3 id="어떻게-구현-그리고-뭐가-장단점일까">어떻게 구현? 그리고 뭐가 장단점일까?</h3>

<p>구현은 초기 로딩 시 하나의 HTML과 초기 화면에 필요한 JavaScript를 로딩하는 방식으로 이루어진다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[초기 로딩]
 ├─ HTML
 └─ 초기 화면에 필요한 JavaScript
</code></pre></div></div>

<p>이제 뭔가 변경이 필요하다면 클라이언트에서 JS로 필요한 부분만 변경(부분 렌더링)하는 방식으로 이루어진다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[변경 요청]
    ↓
[클라이언트 JS]
    ↓
[필요한 부분만 변경]
</code></pre></div></div>

<p>데이터 전송도 필요한 데이터만 주고받고, 클라이언트의 JS로 필요한 부분만 변경(부분 렌더링)한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[서버] ⇄ [필요한 데이터]
              ↓
        [클라이언트 JS]
              ↓
       [부분 렌더링]
</code></pre></div></div>

<p>그래서 여러 가지 장점을 제공한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[SPA]
 ├─ 빠른 페이지 전환
 ├─ 네트워크 효율성
 └─ 역할 분리
</code></pre></div></div>

<ol>
  <li>페이지 전환 시 전체 문서를 다시 로드하지 않아 빠른 사용자 경험을 제공할 수 있다.</li>
  <li>네트워크 효율성 : 한 번 로딩된 리소스 (CSS, JS 등)를 재사용하고 필요한 데이터만 요청할 수 있어 전체적인 네트워크 트래픽을 줄일 수 있다. (엄마가 끓여주신 사골국으로 온갖 요리를 해먹는 것을 생각해보자, 본인은 일주일동안 사골로 사골라면, 사골된장찌개…부터 시작해서 약 10가지 요리를 해봤다..OTL)</li>
  <li>프론트/백엔드 분리: 프론트엔드는 Vue 화면과 로직을 담당하고, 백엔드는 API로 데이터를 제공하는 식으로 역할을 나눌 수 있어 개발 팀 간 협업과 서버 구성이 명확해질 수 있다.</li>
</ol>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Vue 프론트엔드] ⇄ [API 서버]
   화면·로직          데이터 제공
</code></pre></div></div>

<p>근데 이 방식 장점만 있는 것은 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[SPA 장점]
    +
[SPA 단점]
    ↓
[상황에 맞는 선택]
</code></pre></div></div>

<p>여러 가지 문제가 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[주의할 점]
 ├─ 초기 로딩
 ├─ 새로고침
 └─ SEO
</code></pre></div></div>

<ol>
  <li>초기 로딩이 느려질 수 있다.
    <ul>
      <li>첫 로딩 시 초기 화면에 필요한 JavaScript를 내려받고 실행해야 해서 초기에 느리게 로딩될 수 있다.</li>
    </ul>
  </li>
  <li>새로고침 시 전체 앱이 다시 로딩된다.
    <ul>
      <li>이로 인해 상태가 초기화될 수 있다.</li>
    </ul>
  </li>
  <li>JS 기반 렌더링만 사용하는 경우 SEO에 불리할 수 있다.
    <ul>
      <li>로봇이 들어와도 JavaScript 실행 전에는 알맹이를 못 볼 수 있다.</li>
      <li>그래서 검색 노출에 불리할 수 있다.</li>
    </ul>
  </li>
  <li>별도 기술 조합이 필요할 수 있다. 예를 들어 Vue.js + REST API (ex: Spring Boot)를 함께 구성해야 한다.</li>
</ol>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[SPA 단점]
 ├─ 초기 JavaScript 로딩·실행
 ├─ 새로고침 시 앱 재로딩
 └─ CSR 환경에서 SEO 불리할 수 있음
</code></pre></div></div>

<h3 id="vuejs로-spa를-만들-때-자주-쓰는-추가-기술-요소">Vue.js로 SPA를 만들 때 자주 쓰는 추가 기술 요소</h3>

<ul>
  <li>Vite: Frontend Build Tool로 개발자가 작성한 수많은 .vue, .js, .css 파일들을 개발 중에는 빠르게 제공하고, 배포 시에는 브라우저가 읽을 수 있는 최적화된 정적 파일로 변환·번들링하며 개발 서버도 띄워주는 빌드 도구이다.</li>
  <li>Vue Router: 브라우저의 URL 주소와 Vue 컴포넌트를 연결해 주는 공식 라우팅 라이브러리</li>
  <li>Pinia: 전역 상태 관리 라이브러리로 필요한 컴포넌트가 접근할 수 있는 중앙 집중식 데이터 저장소(Store)를 메모리 상에 개설한다.</li>
  <li>Axios: 백엔드 서버(API 서버)와 JSON 등을 주고받는 HTTP 통신 창구.</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Vite]
   ↓ 빌드
[Vue 앱 정적 파일]
   ↓ 라우팅·상태·통신
[Vue Router] + [Pinia] + [Axios]
</code></pre></div></div>

<h3 id="spa의-장점">SPA의 장점</h3>
<ul>
  <li>압도적인 사용자 경험(UX)</li>
  <li>네트워크 효율성</li>
  <li>프론트 / 백엔드 분리</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[SPA 장점]
 ├─ 사용자 경험
 ├─ 네트워크 효율성
 └─ 프론트 / 백엔드 분리
</code></pre></div></div>

<h3 id="spa-단점">SPA 단점</h3>
<ul>
  <li>초기 로딩 속도: 초기 화면에 필요한 JavaScript가 한꺼번에 다운로드·실행되면 첫 접속 시 대기 시간이 발생할 수 있다. 코드 분할을 적용하면 필요한 코드만 먼저 불러올 수도 있다.</li>
  <li>SEO 취약: 순수 CSR에서는 검색 로봇이 JavaScript를 실행하기 전 알맹이가 적은 <code class="language-plaintext highlighter-rouge">index.html</code>을 먼저 보게 될 수 있어 검색 노출에 불리할 수 있다.</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[초기 JS 다운로드·실행]
          ↓
   [첫 화면 대기 시간]

[SSR/SSG 또는 코드 분할]
          ↓
   [초기 부담 완화 가능]
</code></pre></div></div>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Frontend" /><category term="Vue.js" /><summary type="html"><![CDATA[Vue.js]]></summary></entry><entry><title type="html">Deep Learning 테스트</title><link href="https://minnong511.github.io/deep_learning/2026/07/30/deep-learning-test/" rel="alternate" type="text/html" title="Deep Learning 테스트" /><published>2026-07-30T09:01:00+09:00</published><updated>2026-07-30T09:01:00+09:00</updated><id>https://minnong511.github.io/deep_learning/2026/07/30/deep-learning-test</id><content type="html" xml:base="https://minnong511.github.io/deep_learning/2026/07/30/deep-learning-test/"><![CDATA[<p>가</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Deep_Learning" /><summary type="html"><![CDATA[가]]></summary></entry><entry><title type="html">[번외] Vue 개념 점검</title><link href="https://minnong511.github.io/frontend/vue/2026/07/30/vue_intro2/" rel="alternate" type="text/html" title="[번외] Vue 개념 점검" /><published>2026-07-30T09:01:00+09:00</published><updated>2026-07-30T09:01:00+09:00</updated><id>https://minnong511.github.io/frontend/vue/2026/07/30/vue_intro2</id><content type="html" xml:base="https://minnong511.github.io/frontend/vue/2026/07/30/vue_intro2/"><![CDATA[<h2 id="vue에서-반드시-알아야-하는-javascript">Vue에서 반드시 알아야 하는 JavaScript</h2>

<p>Vue를 시작하기 전에 진짜~ 기본적인 JavaScript를 알아보고 가자.</p>

<p>전부 외울 필요는 없고, 아래 문법을 읽고 간단하게 사용할 수 있으면 된다.</p>

<ul>
  <li>객체와 배열</li>
  <li>함수와 화살표 함수</li>
  <li>구조 분해와 스프레드 문법</li>
  <li><code class="language-plaintext highlighter-rouge">map</code>, <code class="language-plaintext highlighter-rouge">filter</code>, <code class="language-plaintext highlighter-rouge">find</code>, <code class="language-plaintext highlighter-rouge">reduce</code></li>
  <li><code class="language-plaintext highlighter-rouge">import</code>와 <code class="language-plaintext highlighter-rouge">export</code></li>
  <li><code class="language-plaintext highlighter-rouge">async</code>와 <code class="language-plaintext highlighter-rouge">await</code></li>
</ul>

<h3 id="변수-객체-배열">변수, 객체, 배열</h3>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">name</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">Lee</span><span class="dl">'</span>
<span class="kd">let</span> <span class="nx">count</span> <span class="o">=</span> <span class="mi">0</span>

<span class="kd">const</span> <span class="nx">user</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Lee</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">active</span><span class="p">:</span> <span class="kc">true</span>
<span class="p">}</span>

<span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nx">user</span><span class="p">.</span><span class="nx">name</span><span class="p">)</span>
<span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nx">user</span><span class="p">.</span><span class="nx">active</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">users</span> <span class="o">=</span> <span class="p">[</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Lee</span><span class="dl">'</span><span class="p">,</span> <span class="na">active</span><span class="p">:</span> <span class="kc">true</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Kim</span><span class="dl">'</span><span class="p">,</span> <span class="na">active</span><span class="p">:</span> <span class="kc">false</span> <span class="p">}</span>
<span class="p">]</span>
</code></pre></div></div>

<h3 id="함수와-구조-분해">함수와 구조 분해</h3>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">add</span> <span class="o">=</span> <span class="p">(</span><span class="nx">a</span><span class="p">,</span> <span class="nx">b</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">a</span> <span class="o">+</span> <span class="nx">b</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="p">{</span> <span class="nx">name</span><span class="p">,</span> <span class="nx">age</span> <span class="p">}</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Lee</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">age</span><span class="p">:</span> <span class="mi">25</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="배열-함수와-자주-쓰는-문법">배열 함수와 자주 쓰는 문법</h3>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">names</span> <span class="o">=</span> <span class="nx">users</span><span class="p">.</span><span class="nx">map</span><span class="p">((</span><span class="nx">user</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">user</span><span class="p">.</span><span class="nx">name</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">activeUsers</span> <span class="o">=</span> <span class="nx">users</span><span class="p">.</span><span class="nx">filter</span><span class="p">((</span><span class="nx">user</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">user</span><span class="p">.</span><span class="nx">active</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">firstUser</span> <span class="o">=</span> <span class="nx">users</span><span class="p">.</span><span class="nx">find</span><span class="p">((</span><span class="nx">user</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">user</span><span class="p">.</span><span class="nx">id</span> <span class="o">===</span> <span class="mi">1</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">newUser</span> <span class="o">=</span> <span class="p">{</span>
  <span class="p">...</span><span class="nx">users</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span>
  <span class="na">active</span><span class="p">:</span> <span class="kc">false</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">message</span> <span class="o">=</span> <span class="nx">isLoggedIn</span> <span class="p">?</span> <span class="dl">'</span><span class="s1">환영합니다</span><span class="dl">'</span> <span class="p">:</span> <span class="dl">'</span><span class="s1">로그인하세요</span><span class="dl">'</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">map</code>은 배열을 변환하고, <code class="language-plaintext highlighter-rouge">filter</code>는 조건에 맞는 항목만 남기며, <code class="language-plaintext highlighter-rouge">find</code>는 조건에 맞는 첫 번째 항목을 찾는다. 상품 총합처럼 여러 값을 하나로 합칠 때는 <code class="language-plaintext highlighter-rouge">reduce</code>를 사용한다.</p>

<h3 id="비동기-처리">비동기 처리</h3>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">loadUsers</span> <span class="o">=</span> <span class="k">async</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">response</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">fetch</span><span class="p">(</span><span class="dl">'</span><span class="s1">/api/users</span><span class="dl">'</span><span class="p">)</span>
  <span class="kd">const</span> <span class="nx">data</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">response</span><span class="p">.</span><span class="nx">json</span><span class="p">()</span>

  <span class="k">return</span> <span class="nx">data</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="vue-코드를-읽는-방법">Vue 코드를 읽는 방법</h2>

<p>Vue 코드는 먼저 상태를 찾고, 그 상태를 바꾸는 함수와 템플릿의 연결을 확인하면 된다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">computed</span><span class="p">,</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">products</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">키보드</span><span class="dl">'</span><span class="p">,</span> <span class="na">price</span><span class="p">:</span> <span class="mi">50000</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">마우스</span><span class="dl">'</span><span class="p">,</span> <span class="na">price</span><span class="p">:</span> <span class="mi">30000</span> <span class="p">}</span>
<span class="p">])</span>

<span class="kd">const</span> <span class="nx">selectedProduct</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="kc">null</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">totalPrice</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">products</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">reduce</span><span class="p">((</span><span class="nx">sum</span><span class="p">,</span> <span class="nx">product</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="k">return</span> <span class="nx">sum</span> <span class="o">+</span> <span class="nx">product</span><span class="p">.</span><span class="nx">price</span>
  <span class="p">},</span> <span class="mi">0</span><span class="p">)</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">selectProduct</span> <span class="o">=</span> <span class="p">(</span><span class="nx">product</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">selectedProduct</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="nx">product</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;div</span> <span class="na">v-for=</span><span class="s">"product in products"</span> <span class="na">:key=</span><span class="s">"product.id"</span><span class="nt">&gt;</span>
    <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"selectProduct(product)"</span><span class="nt">&gt;</span>
      <span class="si">{{</span> <span class="nx">product</span><span class="p">.</span><span class="nx">name</span> <span class="si">}}</span>
    <span class="nt">&lt;/button&gt;</span>
  <span class="nt">&lt;/div&gt;</span>

  <span class="nt">&lt;p&gt;</span>총가격: <span class="si">{{</span> <span class="nx">totalPrice</span> <span class="si">}}</span>원<span class="nt">&lt;/p&gt;</span>

  <span class="nt">&lt;p</span> <span class="na">v-if=</span><span class="s">"selectedProduct"</span><span class="nt">&gt;</span>
    선택 상품: <span class="si">{{</span> <span class="nx">selectedProduct</span><span class="p">.</span><span class="nx">name</span> <span class="si">}}</span>
  <span class="nt">&lt;/p&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>읽는 순서는 다음과 같다.</p>

<ol>
  <li>어떤 반응형 상태가 있는가? → <code class="language-plaintext highlighter-rouge">products</code>, <code class="language-plaintext highlighter-rouge">selectedProduct</code></li>
  <li>어떤 계산된 값이 있는가? → <code class="language-plaintext highlighter-rouge">totalPrice</code></li>
  <li>어떤 함수가 상태를 바꾸는가? → <code class="language-plaintext highlighter-rouge">selectProduct</code></li>
  <li>템플릿에서 무엇을 반복하는가? → <code class="language-plaintext highlighter-rouge">products</code></li>
  <li>어떤 이벤트가 함수를 호출하는가? → <code class="language-plaintext highlighter-rouge">@click</code></li>
  <li>어떤 조건에서 화면이 나타나는가? → <code class="language-plaintext highlighter-rouge">v-if="selectedProduct"</code></li>
</ol>

<p>결국 Vue 코드는 <strong>상태 → 함수 → 템플릿 연결</strong> 순서로 분석하면 된다.</p>

<h2 id="기본-개념">기본 개념</h2>

<h3 id="1-일반-변수와-ref">1. 일반 변수와 <code class="language-plaintext highlighter-rouge">ref</code></h3>

<p>일반 변수도 값을 변경할 수 있지만, Vue가 그 변경을 반응형 상태로 추적하지는 않는다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">let</span> <span class="nx">count</span> <span class="o">=</span> <span class="mi">0</span>
<span class="nx">count</span><span class="o">++</span>
</code></pre></div></div>

<p>Vue가 값을 추적하고 화면까지 갱신하게 하려면 <code class="language-plaintext highlighter-rouge">ref</code>를 사용한다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
<span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
</code></pre></div></div>

<p>정리하면 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>값 변경</th>
      <th>Vue의 변경 추적</th>
      <th>화면 갱신</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>일반 변수</td>
      <td>가능</td>
      <td>하지 않음</td>
      <td>보장하지 않음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ref</code></td>
      <td>가능</td>
      <td>함</td>
      <td>반응형으로 갱신</td>
    </tr>
  </tbody>
</table>

<h3 id="2-javascript에서는-value-템플릿에서는-생략">2. JavaScript에서는 <code class="language-plaintext highlighter-rouge">.value</code>, 템플릿에서는 생략</h3>

<p><code class="language-plaintext highlighter-rouge">ref(0)</code>은 내부적으로 값을 감싸는 반응형 객체를 만든다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="c1">// JavaScript에서는</span>
<span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="p">)</span>
<span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
</code></pre></div></div>

<p>템플릿에서는 Vue가 <code class="language-plaintext highlighter-rouge">.value</code>를 자동으로 처리하므로 생략할 수 있다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;p&gt;</span>{{ count }}<span class="nt">&lt;/p&gt;</span>
</code></pre></div></div>

<p>이것을 <strong>자동 ref 언래핑</strong>이라고 한다.</p>

<h3 id="3-v-bindsrc와-src">3. <code class="language-plaintext highlighter-rouge">v-bind:src</code>와 <code class="language-plaintext highlighter-rouge">:src</code></h3>

<p>둘은 같은 문법이다. <code class="language-plaintext highlighter-rouge">:</code>는 <code class="language-plaintext highlighter-rouge">v-bind:</code>의 줄임말이다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;img</span> <span class="na">v-bind:src=</span><span class="s">"imageUrl"</span> <span class="na">alt=</span><span class="s">"프로필"</span> <span class="nt">/&gt;</span>
<span class="nt">&lt;img</span> <span class="na">:src=</span><span class="s">"imageUrl"</span> <span class="na">alt=</span><span class="s">"프로필"</span> <span class="nt">/&gt;</span>
</code></pre></div></div>

<h3 id="4-clickincrease와-clickincrease">4. <code class="language-plaintext highlighter-rouge">@click="increase"</code>와 <code class="language-plaintext highlighter-rouge">@click="increase()"</code></h3>

<p>인자가 없는 단순한 이벤트라면 함수 이름을 바로 전달하는 방식이 읽기 좋다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increase"</span><span class="nt">&gt;</span>증가<span class="nt">&lt;/button&gt;</span>
</code></pre></div></div>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>
</code></pre></div></div>

<p>인자를 전달해야 할 때는 괄호를 사용한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"selectCity(city)"</span><span class="nt">&gt;</span>선택<span class="nt">&lt;/button&gt;</span>
</code></pre></div></div>

<p>함수 이름만 전달하면 Vue가 이벤트 객체를 함수에 전달할 수도 있다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"handleClick"</span><span class="nt">&gt;</span>클릭<span class="nt">&lt;/button&gt;</span>
</code></pre></div></div>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">handleClick</span> <span class="o">=</span> <span class="p">(</span><span class="nx">event</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="nx">event</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="5-v-if와-v-show">5. <code class="language-plaintext highlighter-rouge">v-if</code>와 <code class="language-plaintext highlighter-rouge">v-show</code></h3>

<p>둘 다 조건에 따라 화면을 보이거나 숨기지만 동작이 다르다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;p</span> <span class="na">v-if=</span><span class="s">"isVisible"</span><span class="nt">&gt;</span>내용<span class="nt">&lt;/p&gt;</span>
<span class="nt">&lt;p</span> <span class="na">v-show=</span><span class="s">"isVisible"</span><span class="nt">&gt;</span>내용<span class="nt">&lt;/p&gt;</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">v-if</code>: 조건에 따라 DOM 요소를 생성하거나 제거한다.</li>
  <li><code class="language-plaintext highlighter-rouge">v-show</code>: DOM 요소를 유지하고 CSS의 <code class="language-plaintext highlighter-rouge">display</code>로 보이거나 숨긴다.</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">v-if</code>는 조건 변경 비용이 있지만 처음부터 렌더링하지 않을 수 있다. <code class="language-plaintext highlighter-rouge">v-show</code>는 자주 보였다 숨겨지는 요소에 적합하다.</p>

<h3 id="6-v-for와-key">6. <code class="language-plaintext highlighter-rouge">v-for</code>와 <code class="language-plaintext highlighter-rouge">key</code></h3>

<p><code class="language-plaintext highlighter-rouge">v-for</code>가 만든 반복 변수는 해당 요소와 자식 요소 안에서만 사용할 수 있다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;div</span> <span class="na">v-for=</span><span class="s">"city in cities"</span> <span class="na">:key=</span><span class="s">"city.id"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;span&gt;</span>{{ city.name }}: {{ city.temp }}℃<span class="nt">&lt;/span&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"selectCity(city)"</span><span class="nt">&gt;</span>선택<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/div&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">city</code>는 반복문 밖에서는 존재하지 않는다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;div</span> <span class="na">v-for=</span><span class="s">"city in cities"</span> <span class="na">:key=</span><span class="s">"city.id"</span><span class="nt">&gt;</span>
  {{ city.name }}
<span class="nt">&lt;/div&gt;</span>

<span class="c">&lt;!-- 오류: city는 v-for 바깥에서 사용할 수 없음 --&gt;</span>
<span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"selectCity(city)"</span><span class="nt">&gt;</span>선택<span class="nt">&lt;/button&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">key</code>는 각 항목의 고유한 정체성을 Vue에 알려준다. 목록의 순서가 바뀌거나 항목이 삭제될 때 Vue가 어떤 DOM을 재사용하고 제거할지 정확하게 판단할 수 있다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;div</span> <span class="na">v-for=</span><span class="s">"user in users"</span> <span class="na">:key=</span><span class="s">"user.id"</span><span class="nt">&gt;</span>
  {{ user.name }}
<span class="nt">&lt;/div&gt;</span>
</code></pre></div></div>

<h3 id="7-computed와-일반-함수">7. <code class="language-plaintext highlighter-rouge">computed</code>와 일반 함수</h3>

<p>상태에서 새로운 값을 계산하면 <code class="language-plaintext highlighter-rouge">computed</code>, 사용자 행동을 처리하면 일반 함수를 사용한다.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">totalPrice</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">products</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">reduce</span><span class="p">((</span><span class="nx">sum</span><span class="p">,</span> <span class="nx">product</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="k">return</span> <span class="nx">sum</span> <span class="o">+</span> <span class="nx">product</span><span class="p">.</span><span class="nx">price</span>
  <span class="p">},</span> <span class="mi">0</span><span class="p">)</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">save</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">alert</span><span class="p">(</span><span class="dl">'</span><span class="s1">저장되었습니다.</span><span class="dl">'</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;p&gt;</span>총가격: {{ totalPrice }}원<span class="nt">&lt;/p&gt;</span>
<span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"save"</span><span class="nt">&gt;</span>저장<span class="nt">&lt;/button&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">computed</code>는 계산된 값이므로 템플릿에서 함수처럼 호출하지 않는다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- 잘못된 사용 --&gt;</span>
<span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"totalPrice(products)"</span><span class="nt">&gt;</span>총가격<span class="nt">&lt;/button&gt;</span>
</code></pre></div></div>

<p>기억법은 간단하다.</p>

<ul>
  <li>증가, 삭제, 선택, 저장 → 일반 함수</li>
  <li>총합, 개수, 평균, 필터 결과 → <code class="language-plaintext highlighter-rouge">computed</code></li>
</ul>

<h3 id="8-v-model은-입력-요소에-사용">8. <code class="language-plaintext highlighter-rouge">v-model</code>은 입력 요소에 사용</h3>

<p><code class="language-plaintext highlighter-rouge">v-model</code>은 사용자가 값을 입력하거나 선택하는 요소에 연결한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;input</span> <span class="na">v-model=</span><span class="s">"name"</span> <span class="nt">/&gt;</span>
<span class="nt">&lt;textarea</span> <span class="na">v-model=</span><span class="s">"content"</span><span class="nt">&gt;&lt;/textarea&gt;</span>

<span class="nt">&lt;select</span> <span class="na">v-model=</span><span class="s">"selectedCity"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;option</span> <span class="na">value=</span><span class="s">"서울"</span><span class="nt">&gt;</span>서울<span class="nt">&lt;/option&gt;</span>
<span class="nt">&lt;/select&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">p</code>는 값을 입력받는 요소가 아니라 결과를 보여주는 요소다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- 잘못된 사용 --&gt;</span>
<span class="nt">&lt;p</span> <span class="na">input</span> <span class="na">v-model=</span><span class="s">"name"</span><span class="nt">&gt;</span>{{ name }}<span class="nt">&lt;/p&gt;</span>

<span class="c">&lt;!-- 올바른 사용 --&gt;</span>
<span class="nt">&lt;input</span> <span class="na">v-model=</span><span class="s">"name"</span> <span class="nt">/&gt;</span>
<span class="nt">&lt;p&gt;</span>{{ name }}<span class="nt">&lt;/p&gt;</span>
</code></pre></div></div>

<p>입력과 출력의 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자가 input에 입력
        ↓
v-model이 name 상태를 변경
        ↓
실제로는 name.value가 변경
        ↓
{{ name }}을 사용하는 화면이 갱신
</code></pre></div></div>

<h3 id="9-부모와-자식-컴포넌트-통신">9. 부모와 자식 컴포넌트 통신</h3>

<p>부모에서 자식으로는 <code class="language-plaintext highlighter-rouge">props</code>, 자식에서 부모로는 <code class="language-plaintext highlighter-rouge">emit</code>을 사용한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- 부모 컴포넌트 --&gt;</span>
<span class="nt">&lt;UserCard</span> <span class="na">:name=</span><span class="s">"userName"</span> <span class="err">@</span><span class="na">select=</span><span class="s">"handleSelect"</span> <span class="nt">/&gt;</span>
</code></pre></div></div>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- 자식 컴포넌트 --&gt;</span>
<span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="nx">defineProps</span><span class="p">({</span>
  <span class="na">name</span><span class="p">:</span> <span class="nb">String</span>
<span class="p">})</span>

<span class="kd">const</span> <span class="nx">emit</span> <span class="o">=</span> <span class="nx">defineEmits</span><span class="p">([</span><span class="dl">'</span><span class="s1">select</span><span class="dl">'</span><span class="p">])</span>

<span class="kd">const</span> <span class="nx">handleClick</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">emit</span><span class="p">(</span><span class="dl">'</span><span class="s1">select</span><span class="dl">'</span><span class="p">,</span> <span class="mi">10</span><span class="p">)</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span><span class="si">{{</span> <span class="nx">name</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"handleClick"</span><span class="nt">&gt;</span>선택<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>부모 → 자식: props
자식 → 부모: emit
</code></pre></div></div>

<h3 id="10-버튼-클릭부터-화면-변경까지">10. 버튼 클릭부터 화면 변경까지</h3>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;p&gt;</span><span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increase"</span><span class="nt">&gt;</span>증가<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자가 버튼을 클릭
        ↓
@click이 increase 실행
        ↓
count.value 변경
        ↓
Vue가 변경을 감지
        ↓
{{ count }}를 사용하는 화면 갱신
</code></pre></div></div>

<h2 id="코드-따라치기-작은-관리-화면-만들기">코드 따라치기: 작은 관리 화면 만들기</h2>

<p>앞에서 배운 <code class="language-plaintext highlighter-rouge">ref</code>, <code class="language-plaintext highlighter-rouge">computed</code>, <code class="language-plaintext highlighter-rouge">v-model</code>, <code class="language-plaintext highlighter-rouge">v-if</code>, <code class="language-plaintext highlighter-rouge">v-for</code>, 이벤트를 한 번에 연습해보자.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">computed</span><span class="p">,</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="c1">// 1. 카운터</span>
<span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">decrease</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">--</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">reset</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="mi">0</span>
<span class="p">}</span>

<span class="c1">// 2. 이름 입력</span>
<span class="kd">const</span> <span class="nx">name</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">''</span><span class="p">)</span>

<span class="c1">// 3. 도시 목록</span>
<span class="kd">const</span> <span class="nx">cities</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span><span class="p">,</span> <span class="na">temp</span><span class="p">:</span> <span class="mi">28</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">수원</span><span class="dl">'</span><span class="p">,</span> <span class="na">temp</span><span class="p">:</span> <span class="mi">27</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">3</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">부산</span><span class="dl">'</span><span class="p">,</span> <span class="na">temp</span><span class="p">:</span> <span class="mi">26</span> <span class="p">}</span>
<span class="p">])</span>

<span class="kd">const</span> <span class="nx">selectedCity</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="kc">null</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">selectCity</span> <span class="o">=</span> <span class="p">(</span><span class="nx">city</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">selectedCity</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="nx">city</span>
<span class="p">}</span>

<span class="c1">// 4. 상품 목록</span>
<span class="kd">const</span> <span class="nx">products</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">키보드</span><span class="dl">'</span><span class="p">,</span> <span class="na">price</span><span class="p">:</span> <span class="mi">50000</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">마우스</span><span class="dl">'</span><span class="p">,</span> <span class="na">price</span><span class="p">:</span> <span class="mi">30000</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">3</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">모니터</span><span class="dl">'</span><span class="p">,</span> <span class="na">price</span><span class="p">:</span> <span class="mi">250000</span> <span class="p">}</span>
<span class="p">])</span>

<span class="kd">const</span> <span class="nx">totalPrice</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">products</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">reduce</span><span class="p">((</span><span class="nx">sum</span><span class="p">,</span> <span class="nx">product</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="k">return</span> <span class="nx">sum</span> <span class="o">+</span> <span class="nx">product</span><span class="p">.</span><span class="nx">price</span>
  <span class="p">},</span> <span class="mi">0</span><span class="p">)</span>
<span class="p">})</span>

<span class="c1">// 5. 할 일 목록</span>
<span class="kd">const</span> <span class="nx">newTodo</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="dl">''</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">todos</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="na">text</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Vue 공부하기</span><span class="dl">'</span> <span class="p">}</span>
<span class="p">])</span>

<span class="kd">const</span> <span class="nx">addTodo</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">text</span> <span class="o">=</span> <span class="nx">newTodo</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">trim</span><span class="p">()</span>

  <span class="k">if</span> <span class="p">(</span><span class="nx">text</span> <span class="o">===</span> <span class="dl">''</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span>
  <span class="p">}</span>

  <span class="nx">todos</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">push</span><span class="p">({</span>
    <span class="na">id</span><span class="p">:</span> <span class="nb">Date</span><span class="p">.</span><span class="nx">now</span><span class="p">(),</span>
    <span class="nx">text</span>
  <span class="p">})</span>

  <span class="nx">newTodo</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="dl">''</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">deleteTodo</span> <span class="o">=</span> <span class="p">(</span><span class="nx">todoId</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">todos</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="nx">todos</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">filter</span><span class="p">((</span><span class="nx">todo</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="k">return</span> <span class="nx">todo</span><span class="p">.</span><span class="nx">id</span> <span class="o">!==</span> <span class="nx">todoId</span>
  <span class="p">})</span>
<span class="p">}</span>

<span class="kd">const</span> <span class="nx">todoCount</span> <span class="o">=</span> <span class="nx">computed</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="k">return</span> <span class="nx">todos</span><span class="p">.</span><span class="nx">value</span><span class="p">.</span><span class="nx">length</span>
<span class="p">})</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>

<span class="nt">&lt;</span><span class="k">template</span><span class="nt">&gt;</span>
  <span class="nt">&lt;main&gt;</span>
    <span class="nt">&lt;section&gt;</span>
      <span class="nt">&lt;h2&gt;</span>1. 카운터<span class="nt">&lt;/h2&gt;</span>

      <span class="nt">&lt;p&gt;</span>현재 숫자: <span class="si">{{</span> <span class="nx">count</span> <span class="si">}}</span><span class="nt">&lt;/p&gt;</span>
      <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"increase"</span><span class="nt">&gt;</span>증가<span class="nt">&lt;/button&gt;</span>
      <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"decrease"</span><span class="nt">&gt;</span>감소<span class="nt">&lt;/button&gt;</span>
      <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"reset"</span><span class="nt">&gt;</span>초기화<span class="nt">&lt;/button&gt;</span>

      <span class="nt">&lt;p</span> <span class="na">v-if=</span><span class="s">"count &gt; 0"</span><span class="nt">&gt;</span>양수입니다.<span class="nt">&lt;/p&gt;</span>
      <span class="nt">&lt;p</span> <span class="na">v-else-if=</span><span class="s">"count === 0"</span><span class="nt">&gt;</span>0입니다.<span class="nt">&lt;/p&gt;</span>
      <span class="nt">&lt;p</span> <span class="na">v-else</span><span class="nt">&gt;</span>음수입니다.<span class="nt">&lt;/p&gt;</span>
    <span class="nt">&lt;/section&gt;</span>

    <span class="nt">&lt;section&gt;</span>
      <span class="nt">&lt;h2&gt;</span>2. 이름 입력<span class="nt">&lt;/h2&gt;</span>

      <span class="nt">&lt;input</span> <span class="na">v-model=</span><span class="s">"name"</span> <span class="na">placeholder=</span><span class="s">"이름을 입력하세요"</span> <span class="nt">/&gt;</span>
      <span class="nt">&lt;p</span> <span class="na">v-if=</span><span class="s">"name.trim() === ''"</span><span class="nt">&gt;</span>이름을 입력하세요.<span class="nt">&lt;/p&gt;</span>
      <span class="nt">&lt;p</span> <span class="na">v-else</span><span class="nt">&gt;</span>안녕하세요, <span class="si">{{</span> <span class="nx">name</span> <span class="si">}}</span>님!<span class="nt">&lt;/p&gt;</span>
    <span class="nt">&lt;/section&gt;</span>

    <span class="nt">&lt;section&gt;</span>
      <span class="nt">&lt;h2&gt;</span>3. 도시 목록<span class="nt">&lt;/h2&gt;</span>

      <span class="nt">&lt;div</span> <span class="na">v-for=</span><span class="s">"city in cities"</span> <span class="na">:key=</span><span class="s">"city.id"</span><span class="nt">&gt;</span>
        <span class="nt">&lt;span&gt;</span><span class="si">{{</span> <span class="nx">city</span><span class="p">.</span><span class="nx">name</span> <span class="si">}}</span>: <span class="si">{{</span> <span class="nx">city</span><span class="p">.</span><span class="nx">temp</span> <span class="si">}}</span>℃<span class="nt">&lt;/span&gt;</span>
        <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"selectCity(city)"</span><span class="nt">&gt;</span>선택<span class="nt">&lt;/button&gt;</span>
      <span class="nt">&lt;/div&gt;</span>

      <span class="nt">&lt;p</span> <span class="na">v-if=</span><span class="s">"selectedCity"</span><span class="nt">&gt;</span>
        선택된 도시: <span class="si">{{</span> <span class="nx">selectedCity</span><span class="p">.</span><span class="nx">name</span> <span class="si">}}</span>
      <span class="nt">&lt;/p&gt;</span>
      <span class="nt">&lt;p</span> <span class="na">v-else</span><span class="nt">&gt;</span>선택된 도시가 없습니다.<span class="nt">&lt;/p&gt;</span>
    <span class="nt">&lt;/section&gt;</span>

    <span class="nt">&lt;section&gt;</span>
      <span class="nt">&lt;h2&gt;</span>4. 상품 목록<span class="nt">&lt;/h2&gt;</span>

      <span class="nt">&lt;div</span> <span class="na">v-for=</span><span class="s">"product in products"</span> <span class="na">:key=</span><span class="s">"product.id"</span><span class="nt">&gt;</span>
        <span class="si">{{</span> <span class="nx">product</span><span class="p">.</span><span class="nx">name</span> <span class="si">}}</span>: <span class="si">{{</span> <span class="nx">product</span><span class="p">.</span><span class="nx">price</span> <span class="si">}}</span>원
      <span class="nt">&lt;/div&gt;</span>

      <span class="nt">&lt;p&gt;</span>총가격: <span class="si">{{</span> <span class="nx">totalPrice</span> <span class="si">}}</span>원<span class="nt">&lt;/p&gt;</span>
    <span class="nt">&lt;/section&gt;</span>

    <span class="nt">&lt;section&gt;</span>
      <span class="nt">&lt;h2&gt;</span>5. 할 일 목록<span class="nt">&lt;/h2&gt;</span>

      <span class="nt">&lt;input</span>
        <span class="na">v-model=</span><span class="s">"newTodo"</span>
        <span class="na">placeholder=</span><span class="s">"할 일을 입력하세요"</span>
        <span class="err">@</span><span class="na">keyup.enter=</span><span class="s">"addTodo"</span>
      <span class="nt">/&gt;</span>
      <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"addTodo"</span><span class="nt">&gt;</span>추가<span class="nt">&lt;/button&gt;</span>

      <span class="nt">&lt;p&gt;</span>현재 할 일: <span class="si">{{</span> <span class="nx">todoCount</span> <span class="si">}}</span>개<span class="nt">&lt;/p&gt;</span>

      <span class="nt">&lt;p</span> <span class="na">v-if=</span><span class="s">"todos.length === 0"</span><span class="nt">&gt;</span>등록된 할 일이 없습니다.<span class="nt">&lt;/p&gt;</span>
      <span class="nt">&lt;ul</span> <span class="na">v-else</span><span class="nt">&gt;</span>
        <span class="nt">&lt;li</span> <span class="na">v-for=</span><span class="s">"todo in todos"</span> <span class="na">:key=</span><span class="s">"todo.id"</span><span class="nt">&gt;</span>
          <span class="si">{{</span> <span class="nx">todo</span><span class="p">.</span><span class="nx">text</span> <span class="si">}}</span>
          <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"deleteTodo(todo.id)"</span><span class="nt">&gt;</span>삭제<span class="nt">&lt;/button&gt;</span>
        <span class="nt">&lt;/li&gt;</span>
      <span class="nt">&lt;/ul&gt;</span>
    <span class="nt">&lt;/section&gt;</span>
  <span class="nt">&lt;/main&gt;</span>
<span class="nt">&lt;/</span><span class="k">template</span><span class="nt">&gt;</span>
</code></pre></div></div>

<h2 id="자주-발생하는-문법-오류">자주 발생하는 문법 오류</h2>

<h3 id="1-ref-값을-value-없이-변경">1. <code class="language-plaintext highlighter-rouge">ref</code> 값을 <code class="language-plaintext highlighter-rouge">.value</code> 없이 변경</h3>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">count</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

<span class="c1">// 잘못된 사용</span>
<span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="o">++</span>
<span class="p">}</span>

<span class="c1">// 올바른 사용</span>
<span class="kd">const</span> <span class="nx">increase</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">count</span><span class="p">.</span><span class="nx">value</span><span class="o">++</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="2-화살표-함수-문법-오류">2. 화살표 함수 문법 오류</h3>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 잘못된 사용</span>
<span class="kd">const</span> <span class="nx">handleClick</span><span class="p">()</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">alert</span><span class="p">(</span><span class="dl">'</span><span class="s1">클릭</span><span class="dl">'</span><span class="p">)</span>
<span class="p">}</span>

<span class="c1">// 올바른 사용 1</span>
<span class="kd">const</span> <span class="nx">handleClick</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">alert</span><span class="p">(</span><span class="dl">'</span><span class="s1">클릭</span><span class="dl">'</span><span class="p">)</span>
<span class="p">}</span>

<span class="c1">// 올바른 사용 2</span>
<span class="kd">function</span> <span class="nx">handleClick</span><span class="p">()</span> <span class="p">{</span>
  <span class="nx">alert</span><span class="p">(</span><span class="dl">'</span><span class="s1">클릭</span><span class="dl">'</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>화살표 함수와 일반 함수 문법을 섞으면 안 된다.</p>

<h3 id="3-배열-객체-사이의-쉼표-누락">3. 배열 객체 사이의 쉼표 누락</h3>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 잘못된 사용</span>
<span class="kd">const</span> <span class="nx">cities</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span> <span class="p">}</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">부산</span><span class="dl">'</span> <span class="p">}</span>
<span class="p">])</span>

<span class="c1">// 올바른 사용</span>
<span class="kd">const</span> <span class="nx">cities</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">([</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">서울</span><span class="dl">'</span> <span class="p">},</span>
  <span class="p">{</span> <span class="na">id</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">부산</span><span class="dl">'</span> <span class="p">}</span>
<span class="p">])</span>
</code></pre></div></div>

<h3 id="4-v-for-변수의-범위-착각">4. <code class="language-plaintext highlighter-rouge">v-for</code> 변수의 범위 착각</h3>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;div</span> <span class="na">v-for=</span><span class="s">"city in cities"</span> <span class="na">:key=</span><span class="s">"city.id"</span><span class="nt">&gt;</span>
  {{ city.name }}
<span class="nt">&lt;/div&gt;</span>

<span class="c">&lt;!-- city는 이 위치에 없음 --&gt;</span>
<span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"selectCity(city)"</span><span class="nt">&gt;</span>선택<span class="nt">&lt;/button&gt;</span>
</code></pre></div></div>

<p>반복 중인 항목을 사용하려면 버튼도 반복 범위 안에 넣어야 한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;div</span> <span class="na">v-for=</span><span class="s">"city in cities"</span> <span class="na">:key=</span><span class="s">"city.id"</span><span class="nt">&gt;</span>
  {{ city.name }}
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"selectCity(city)"</span><span class="nt">&gt;</span>선택<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/div&gt;</span>
</code></pre></div></div>

<h3 id="5-ref-import와-value-누락">5. <code class="language-plaintext highlighter-rouge">ref</code> import와 <code class="language-plaintext highlighter-rouge">.value</code> 누락</h3>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;</span><span class="k">script</span> <span class="na">setup</span><span class="nt">&gt;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">ref</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">vue</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">selectedCity</span> <span class="o">=</span> <span class="nx">ref</span><span class="p">(</span><span class="kc">null</span><span class="p">)</span>

<span class="kd">const</span> <span class="nx">selectCity</span> <span class="o">=</span> <span class="p">(</span><span class="nx">city</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">selectedCity</span><span class="p">.</span><span class="nx">value</span> <span class="o">=</span> <span class="nx">city</span>
<span class="p">}</span>
<span class="nt">&lt;/</span><span class="k">script</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ref</code>를 사용하면 import가 필요하고, JavaScript에서 값을 변경할 때는 <code class="language-plaintext highlighter-rouge">.value</code>를 사용한다.</p>

<h3 id="6-html-부모자식-구조-오류">6. HTML 부모·자식 구조 오류</h3>

<p>목록 전체를 버튼 안에 넣기보다, 입력·버튼·목록을 자연스러운 형제로 구성한다.</p>

<div class="language-vue highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- 잘못된 구조 --&gt;</span>
<span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"addTodo"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;ul&gt;</span>
    <span class="nt">&lt;li&gt;</span>Vue 공부<span class="nt">&lt;/li&gt;</span>
  <span class="nt">&lt;/ul&gt;</span>
<span class="nt">&lt;/button&gt;</span>

<span class="c">&lt;!-- 올바른 구조 --&gt;</span>
<span class="nt">&lt;section&gt;</span>
  <span class="nt">&lt;input</span> <span class="na">v-model=</span><span class="s">"newTodo"</span> <span class="nt">/&gt;</span>
  <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"addTodo"</span><span class="nt">&gt;</span>추가<span class="nt">&lt;/button&gt;</span>

  <span class="nt">&lt;ul&gt;</span>
    <span class="nt">&lt;li</span> <span class="na">v-for=</span><span class="s">"todo in todos"</span> <span class="na">:key=</span><span class="s">"todo.id"</span><span class="nt">&gt;</span>
      {{ todo.text }}
      <span class="nt">&lt;button</span> <span class="err">@</span><span class="na">click=</span><span class="s">"deleteTodo(todo.id)"</span><span class="nt">&gt;</span>삭제<span class="nt">&lt;/button&gt;</span>
    <span class="nt">&lt;/li&gt;</span>
  <span class="nt">&lt;/ul&gt;</span>
<span class="nt">&lt;/section&gt;</span>
</code></pre></div></div>

<h2 id="최종-점검">최종 점검</h2>

<p>아래 질문에 답할 수 있으면 Vue 기초 흐름은 잘 잡힌 것이다.</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">ref</code> 값은 JavaScript에서 어떻게 읽고 변경하는가? → <code class="language-plaintext highlighter-rouge">.value</code></li>
  <li>템플릿에서 <code class="language-plaintext highlighter-rouge">ref</code>의 <code class="language-plaintext highlighter-rouge">.value</code>를 생략할 수 있는 이유는? → 자동 ref 언래핑</li>
  <li>이벤트가 함수를 실행하고 함수가 상태를 변경하는 흐름을 설명할 수 있는가?</li>
  <li><code class="language-plaintext highlighter-rouge">v-for</code>로 만든 변수는 어느 범위에서 사용할 수 있는가? → 반복 요소와 자식 요소 안</li>
  <li><code class="language-plaintext highlighter-rouge">computed</code>와 일반 함수의 차이는 무엇인가? → 계산된 값과 사용자 행동 처리</li>
  <li><code class="language-plaintext highlighter-rouge">v-model</code>은 어떤 요소에 사용하는가? → 값을 입력하거나 선택하는 요소</li>
  <li>부모에서 자식으로, 자식에서 부모로 데이터를 전달할 때 무엇을 사용하는가? → <code class="language-plaintext highlighter-rouge">props</code>, <code class="language-plaintext highlighter-rouge">emit</code></li>
</ol>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ref 선언
  ↓
템플릿과 v-model·이벤트 연결
  ↓
함수가 상태 변경
  ↓
computed가 필요한 값을 계산
  ↓
Vue가 화면을 갱신
</code></pre></div></div>

<p>여기까지 이해했으면 이제부터는 문법 자체보다, 어떤 상태를 만들고 어떤 흐름으로 화면에 연결할지 생각하면 된다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Frontend" /><category term="Vue" /><category term="Vue" /><summary type="html"><![CDATA[Vue에서 반드시 알아야 하는 JavaScript Vue를 시작하기 전에 진짜~ 기본적인 JavaScript를 알아보고 가자. 전부 외울 필요는 없고, 아래 문법을 읽고 간단하게 사용할 수 있으면 된다. 객체와 배열 함수와 화살표 함수 구조 분해와 스프레드 문법 map, filter, find, reduce import와 export async와 await 변수, 객체, 배열 const name = 'Lee' let count = 0 const user = { id: 1, name: 'Lee', active: true } console.log(user.name) console.log(user.active) const users = [ { id: 1, name: 'Lee', active: true }, { id: 2, name: 'Kim', active: false } ] 함수와 구조 분해 const add = (a, b) =&gt; { return a + b } const { name, age } = { name: 'Lee', age: 25 } 배열 함수와 자주 쓰는 문법 const names = users.map((user) =&gt; user.name) const activeUsers = users.filter((user) =&gt; user.active) const firstUser = users.find((user) =&gt; user.id === 1) const newUser = { ...users[0], active: false } const message = isLoggedIn ? '환영합니다' : '로그인하세요' map은 배열을 변환하고, filter는 조건에 맞는 항목만 남기며, find는 조건에 맞는 첫 번째 항목을 찾는다. 상품 총합처럼 여러 값을 하나로 합칠 때는 reduce를 사용한다. 비동기 처리 const loadUsers = async () =&gt; { const response = await fetch('/api/users') const data = await response.json() return data } Vue 코드를 읽는 방법 Vue 코드는 먼저 상태를 찾고, 그 상태를 바꾸는 함수와 템플릿의 연결을 확인하면 된다. &lt;script setup&gt; import { computed, ref } from 'vue' const products = ref([ { id: 1, name: '키보드', price: 50000 }, { id: 2, name: '마우스', price: 30000 } ]) const selectedProduct = ref(null) const totalPrice = computed(() =&gt; { return products.value.reduce((sum, product) =&gt; { return sum + product.price }, 0) }) const selectProduct = (product) =&gt; { selectedProduct.value = product } &lt;/script&gt; &lt;template&gt; &lt;div v-for="product in products" :key="product.id"&gt; &lt;button @click="selectProduct(product)"&gt; {{ product.name }} &lt;/button&gt; &lt;/div&gt; &lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt; &lt;p v-if="selectedProduct"&gt; 선택 상품: {{ selectedProduct.name }} &lt;/p&gt; &lt;/template&gt; 읽는 순서는 다음과 같다. 어떤 반응형 상태가 있는가? → products, selectedProduct 어떤 계산된 값이 있는가? → totalPrice 어떤 함수가 상태를 바꾸는가? → selectProduct 템플릿에서 무엇을 반복하는가? → products 어떤 이벤트가 함수를 호출하는가? → @click 어떤 조건에서 화면이 나타나는가? → v-if="selectedProduct" 결국 Vue 코드는 상태 → 함수 → 템플릿 연결 순서로 분석하면 된다. 기본 개념 1. 일반 변수와 ref 일반 변수도 값을 변경할 수 있지만, Vue가 그 변경을 반응형 상태로 추적하지는 않는다. let count = 0 count++ Vue가 값을 추적하고 화면까지 갱신하게 하려면 ref를 사용한다. import { ref } from 'vue' const count = ref(0) count.value++ 정리하면 다음과 같다. 구분 값 변경 Vue의 변경 추적 화면 갱신 일반 변수 가능 하지 않음 보장하지 않음 ref 가능 함 반응형으로 갱신 2. JavaScript에서는 .value, 템플릿에서는 생략 ref(0)은 내부적으로 값을 감싸는 반응형 객체를 만든다. const count = ref(0) // JavaScript에서는 console.log(count.value) count.value++ 템플릿에서는 Vue가 .value를 자동으로 처리하므로 생략할 수 있다. &lt;p&gt;{{ count }}&lt;/p&gt; 이것을 자동 ref 언래핑이라고 한다. 3. v-bind:src와 :src 둘은 같은 문법이다. :는 v-bind:의 줄임말이다. &lt;img v-bind:src="imageUrl" alt="프로필" /&gt; &lt;img :src="imageUrl" alt="프로필" /&gt; 4. @click="increase"와 @click="increase()" 인자가 없는 단순한 이벤트라면 함수 이름을 바로 전달하는 방식이 읽기 좋다. &lt;button @click="increase"&gt;증가&lt;/button&gt; const increase = () =&gt; { count.value++ } 인자를 전달해야 할 때는 괄호를 사용한다. &lt;button @click="selectCity(city)"&gt;선택&lt;/button&gt; 함수 이름만 전달하면 Vue가 이벤트 객체를 함수에 전달할 수도 있다. &lt;button @click="handleClick"&gt;클릭&lt;/button&gt; const handleClick = (event) =&gt; { console.log(event) } 5. v-if와 v-show 둘 다 조건에 따라 화면을 보이거나 숨기지만 동작이 다르다. &lt;p v-if="isVisible"&gt;내용&lt;/p&gt; &lt;p v-show="isVisible"&gt;내용&lt;/p&gt; v-if: 조건에 따라 DOM 요소를 생성하거나 제거한다. v-show: DOM 요소를 유지하고 CSS의 display로 보이거나 숨긴다. v-if는 조건 변경 비용이 있지만 처음부터 렌더링하지 않을 수 있다. v-show는 자주 보였다 숨겨지는 요소에 적합하다. 6. v-for와 key v-for가 만든 반복 변수는 해당 요소와 자식 요소 안에서만 사용할 수 있다. &lt;div v-for="city in cities" :key="city.id"&gt; &lt;span&gt;{{ city.name }}: {{ city.temp }}℃&lt;/span&gt; &lt;button @click="selectCity(city)"&gt;선택&lt;/button&gt; &lt;/div&gt; city는 반복문 밖에서는 존재하지 않는다. &lt;div v-for="city in cities" :key="city.id"&gt; {{ city.name }} &lt;/div&gt; &lt;!-- 오류: city는 v-for 바깥에서 사용할 수 없음 --&gt; &lt;button @click="selectCity(city)"&gt;선택&lt;/button&gt; key는 각 항목의 고유한 정체성을 Vue에 알려준다. 목록의 순서가 바뀌거나 항목이 삭제될 때 Vue가 어떤 DOM을 재사용하고 제거할지 정확하게 판단할 수 있다. &lt;div v-for="user in users" :key="user.id"&gt; {{ user.name }} &lt;/div&gt; 7. computed와 일반 함수 상태에서 새로운 값을 계산하면 computed, 사용자 행동을 처리하면 일반 함수를 사용한다. const totalPrice = computed(() =&gt; { return products.value.reduce((sum, product) =&gt; { return sum + product.price }, 0) }) const save = () =&gt; { alert('저장되었습니다.') } &lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt; &lt;button @click="save"&gt;저장&lt;/button&gt; computed는 계산된 값이므로 템플릿에서 함수처럼 호출하지 않는다. &lt;!-- 잘못된 사용 --&gt; &lt;button @click="totalPrice(products)"&gt;총가격&lt;/button&gt; 기억법은 간단하다. 증가, 삭제, 선택, 저장 → 일반 함수 총합, 개수, 평균, 필터 결과 → computed 8. v-model은 입력 요소에 사용 v-model은 사용자가 값을 입력하거나 선택하는 요소에 연결한다. &lt;input v-model="name" /&gt; &lt;textarea v-model="content"&gt;&lt;/textarea&gt; &lt;select v-model="selectedCity"&gt; &lt;option value="서울"&gt;서울&lt;/option&gt; &lt;/select&gt; p는 값을 입력받는 요소가 아니라 결과를 보여주는 요소다. &lt;!-- 잘못된 사용 --&gt; &lt;p input v-model="name"&gt;{{ name }}&lt;/p&gt; &lt;!-- 올바른 사용 --&gt; &lt;input v-model="name" /&gt; &lt;p&gt;{{ name }}&lt;/p&gt; 입력과 출력의 흐름은 다음과 같다. 사용자가 input에 입력 ↓ v-model이 name 상태를 변경 ↓ 실제로는 name.value가 변경 ↓ {{ name }}을 사용하는 화면이 갱신 9. 부모와 자식 컴포넌트 통신 부모에서 자식으로는 props, 자식에서 부모로는 emit을 사용한다. &lt;!-- 부모 컴포넌트 --&gt; &lt;UserCard :name="userName" @select="handleSelect" /&gt; &lt;!-- 자식 컴포넌트 --&gt; &lt;script setup&gt; defineProps({ name: String }) const emit = defineEmits(['select']) const handleClick = () =&gt; { emit('select', 10) } &lt;/script&gt; &lt;template&gt; &lt;p&gt;{{ name }}&lt;/p&gt; &lt;button @click="handleClick"&gt;선택&lt;/button&gt; &lt;/template&gt; 흐름은 다음과 같다. 부모 → 자식: props 자식 → 부모: emit 10. 버튼 클릭부터 화면 변경까지 &lt;script setup&gt; import { ref } from 'vue' const count = ref(0) const increase = () =&gt; { count.value++ } &lt;/script&gt; &lt;template&gt; &lt;p&gt;{{ count }}&lt;/p&gt; &lt;button @click="increase"&gt;증가&lt;/button&gt; &lt;/template&gt; 사용자가 버튼을 클릭 ↓ @click이 increase 실행 ↓ count.value 변경 ↓ Vue가 변경을 감지 ↓ {{ count }}를 사용하는 화면 갱신 코드 따라치기: 작은 관리 화면 만들기 앞에서 배운 ref, computed, v-model, v-if, v-for, 이벤트를 한 번에 연습해보자. &lt;script setup&gt; import { computed, ref } from 'vue' // 1. 카운터 const count = ref(0) const increase = () =&gt; { count.value++ } const decrease = () =&gt; { count.value-- } const reset = () =&gt; { count.value = 0 } // 2. 이름 입력 const name = ref('') // 3. 도시 목록 const cities = ref([ { id: 1, name: '서울', temp: 28 }, { id: 2, name: '수원', temp: 27 }, { id: 3, name: '부산', temp: 26 } ]) const selectedCity = ref(null) const selectCity = (city) =&gt; { selectedCity.value = city } // 4. 상품 목록 const products = ref([ { id: 1, name: '키보드', price: 50000 }, { id: 2, name: '마우스', price: 30000 }, { id: 3, name: '모니터', price: 250000 } ]) const totalPrice = computed(() =&gt; { return products.value.reduce((sum, product) =&gt; { return sum + product.price }, 0) }) // 5. 할 일 목록 const newTodo = ref('') const todos = ref([ { id: 1, text: 'Vue 공부하기' } ]) const addTodo = () =&gt; { const text = newTodo.value.trim() if (text === '') { return } todos.value.push({ id: Date.now(), text }) newTodo.value = '' } const deleteTodo = (todoId) =&gt; { todos.value = todos.value.filter((todo) =&gt; { return todo.id !== todoId }) } const todoCount = computed(() =&gt; { return todos.value.length }) &lt;/script&gt; &lt;template&gt; &lt;main&gt; &lt;section&gt; &lt;h2&gt;1. 카운터&lt;/h2&gt; &lt;p&gt;현재 숫자: {{ count }}&lt;/p&gt; &lt;button @click="increase"&gt;증가&lt;/button&gt; &lt;button @click="decrease"&gt;감소&lt;/button&gt; &lt;button @click="reset"&gt;초기화&lt;/button&gt; &lt;p v-if="count &gt; 0"&gt;양수입니다.&lt;/p&gt; &lt;p v-else-if="count === 0"&gt;0입니다.&lt;/p&gt; &lt;p v-else&gt;음수입니다.&lt;/p&gt; &lt;/section&gt; &lt;section&gt; &lt;h2&gt;2. 이름 입력&lt;/h2&gt; &lt;input v-model="name" placeholder="이름을 입력하세요" /&gt; &lt;p v-if="name.trim() === ''"&gt;이름을 입력하세요.&lt;/p&gt; &lt;p v-else&gt;안녕하세요, {{ name }}님!&lt;/p&gt; &lt;/section&gt; &lt;section&gt; &lt;h2&gt;3. 도시 목록&lt;/h2&gt; &lt;div v-for="city in cities" :key="city.id"&gt; &lt;span&gt;{{ city.name }}: {{ city.temp }}℃&lt;/span&gt; &lt;button @click="selectCity(city)"&gt;선택&lt;/button&gt; &lt;/div&gt; &lt;p v-if="selectedCity"&gt; 선택된 도시: {{ selectedCity.name }} &lt;/p&gt; &lt;p v-else&gt;선택된 도시가 없습니다.&lt;/p&gt; &lt;/section&gt; &lt;section&gt; &lt;h2&gt;4. 상품 목록&lt;/h2&gt; &lt;div v-for="product in products" :key="product.id"&gt; {{ product.name }}: {{ product.price }}원 &lt;/div&gt; &lt;p&gt;총가격: {{ totalPrice }}원&lt;/p&gt; &lt;/section&gt; &lt;section&gt; &lt;h2&gt;5. 할 일 목록&lt;/h2&gt; &lt;input v-model="newTodo" placeholder="할 일을 입력하세요" @keyup.enter="addTodo" /&gt; &lt;button @click="addTodo"&gt;추가&lt;/button&gt; &lt;p&gt;현재 할 일: {{ todoCount }}개&lt;/p&gt; &lt;p v-if="todos.length === 0"&gt;등록된 할 일이 없습니다.&lt;/p&gt; &lt;ul v-else&gt; &lt;li v-for="todo in todos" :key="todo.id"&gt; {{ todo.text }} &lt;button @click="deleteTodo(todo.id)"&gt;삭제&lt;/button&gt; &lt;/li&gt; &lt;/ul&gt; &lt;/section&gt; &lt;/main&gt; &lt;/template&gt; 자주 발생하는 문법 오류 1. ref 값을 .value 없이 변경 const count = ref(0) // 잘못된 사용 const increase = () =&gt; { count++ } // 올바른 사용 const increase = () =&gt; { count.value++ } 2. 화살표 함수 문법 오류 // 잘못된 사용 const handleClick() = () =&gt; { alert('클릭') } // 올바른 사용 1 const handleClick = () =&gt; { alert('클릭') } // 올바른 사용 2 function handleClick() { alert('클릭') } 화살표 함수와 일반 함수 문법을 섞으면 안 된다. 3. 배열 객체 사이의 쉼표 누락 // 잘못된 사용 const cities = ref([ { id: 1, name: '서울' } { id: 2, name: '부산' } ]) // 올바른 사용 const cities = ref([ { id: 1, name: '서울' }, { id: 2, name: '부산' } ]) 4. v-for 변수의 범위 착각 &lt;div v-for="city in cities" :key="city.id"&gt; {{ city.name }} &lt;/div&gt; &lt;!-- city는 이 위치에 없음 --&gt; &lt;button @click="selectCity(city)"&gt;선택&lt;/button&gt; 반복 중인 항목을 사용하려면 버튼도 반복 범위 안에 넣어야 한다. &lt;div v-for="city in cities" :key="city.id"&gt; {{ city.name }} &lt;button @click="selectCity(city)"&gt;선택&lt;/button&gt; &lt;/div&gt; 5. ref import와 .value 누락 &lt;script setup&gt; import { ref } from 'vue' const selectedCity = ref(null) const selectCity = (city) =&gt; { selectedCity.value = city } &lt;/script&gt; ref를 사용하면 import가 필요하고, JavaScript에서 값을 변경할 때는 .value를 사용한다. 6. HTML 부모·자식 구조 오류 목록 전체를 버튼 안에 넣기보다, 입력·버튼·목록을 자연스러운 형제로 구성한다. &lt;!-- 잘못된 구조 --&gt; &lt;button @click="addTodo"&gt; &lt;ul&gt; &lt;li&gt;Vue 공부&lt;/li&gt; &lt;/ul&gt; &lt;/button&gt; &lt;!-- 올바른 구조 --&gt; &lt;section&gt; &lt;input v-model="newTodo" /&gt; &lt;button @click="addTodo"&gt;추가&lt;/button&gt; &lt;ul&gt; &lt;li v-for="todo in todos" :key="todo.id"&gt; {{ todo.text }} &lt;button @click="deleteTodo(todo.id)"&gt;삭제&lt;/button&gt; &lt;/li&gt; &lt;/ul&gt; &lt;/section&gt; 최종 점검 아래 질문에 답할 수 있으면 Vue 기초 흐름은 잘 잡힌 것이다. ref 값은 JavaScript에서 어떻게 읽고 변경하는가? → .value 템플릿에서 ref의 .value를 생략할 수 있는 이유는? → 자동 ref 언래핑 이벤트가 함수를 실행하고 함수가 상태를 변경하는 흐름을 설명할 수 있는가? v-for로 만든 변수는 어느 범위에서 사용할 수 있는가? → 반복 요소와 자식 요소 안 computed와 일반 함수의 차이는 무엇인가? → 계산된 값과 사용자 행동 처리 v-model은 어떤 요소에 사용하는가? → 값을 입력하거나 선택하는 요소 부모에서 자식으로, 자식에서 부모로 데이터를 전달할 때 무엇을 사용하는가? → props, emit ref 선언 ↓ 템플릿과 v-model·이벤트 연결 ↓ 함수가 상태 변경 ↓ computed가 필요한 값을 계산 ↓ Vue가 화면을 갱신 여기까지 이해했으면 이제부터는 문법 자체보다, 어떤 상태를 만들고 어떤 흐름으로 화면에 연결할지 생각하면 된다.]]></summary></entry><entry><title type="html">CS 테스트</title><link href="https://minnong511.github.io/cs/2026/07/30/cs-test/" rel="alternate" type="text/html" title="CS 테스트" /><published>2026-07-30T09:00:00+09:00</published><updated>2026-07-30T09:00:00+09:00</updated><id>https://minnong511.github.io/cs/2026/07/30/cs-test</id><content type="html" xml:base="https://minnong511.github.io/cs/2026/07/30/cs-test/"><![CDATA[<p>가</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="CS" /><summary type="html"><![CDATA[가]]></summary></entry></feed>