<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-08-10T18:43:29+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Minnong’s Study Log</title><subtitle>개발과 AI를 배우고, 실험하고, 이해한 내용을 연결하는 공부 기록</subtitle><author><name>Min Hyeong Lee</name></author><entry><title type="html">Java OOP 핵심 개념 정리</title><link href="http://localhost:4000/backend/java/2026/08/10/java-oop-concepts/" rel="alternate" type="text/html" title="Java OOP 핵심 개념 정리" /><published>2026-08-10T12:00:00+09:00</published><updated>2026-08-10T12:00:00+09:00</updated><id>http://localhost:4000/backend/java/2026/08/10/java-oop-concepts</id><content type="html" xml:base="http://localhost:4000/backend/java/2026/08/10/java-oop-concepts/"><![CDATA[<h1 id="java-oop-핵심-개념">Java OOP 핵심 개념</h1>

<h2 id="1-oop란">1. OOP란?</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OOP = Object-Oriented Programming
    = 객체지향 프로그래밍
</code></pre></div></div>

<p>객체지향 프로그래밍은 프로그램을 <code class="language-plaintext highlighter-rouge">객체</code> 단위로 나누어 설계하는 방법이다. 객체 안에 데이터와 동작을 함께 묶고, 객체끼리 메시지를 주고받으며 프로그램을 실행한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">name</span><span class="o">;</span> <span class="c1">// 데이터: 필드</span>
    <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>

    <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">()</span> <span class="o">{</span> <span class="c1">// 동작: 메서드</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">name</span> <span class="o">+</span> <span class="s">" : "</span> <span class="o">+</span> <span class="n">price</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="nc">Stock</span> <span class="n">stock</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">();</span> <span class="c1">// 객체 생성</span>
<span class="n">stock</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="s">"SKALA"</span><span class="o">;</span>
<span class="n">stock</span><span class="o">.</span><span class="na">printInfo</span><span class="o">();</span>         <span class="c1">// 객체의 동작 호출</span>
</code></pre></div></div>

<p>Java OOP를 이해할 때 가장 먼저 구분할 용어는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>클래스(<code class="language-plaintext highlighter-rouge">Class</code>)</td>
      <td>객체를 만들기 위한 설계도. 필드와 메서드를 정의한다.</td>
    </tr>
    <tr>
      <td>객체(<code class="language-plaintext highlighter-rouge">Object</code>)</td>
      <td>클래스에 따라 실행 중 메모리에 실제로 만들어진 실체다.</td>
    </tr>
    <tr>
      <td>인스턴스(<code class="language-plaintext highlighter-rouge">Instance</code>)</td>
      <td>특정 클래스에서 만들어진 객체라는 관계를 강조하는 표현이다.</td>
    </tr>
    <tr>
      <td>필드(<code class="language-plaintext highlighter-rouge">Field</code>)</td>
      <td>객체의 상태나 데이터를 저장하는 클래스 내부 변수다.</td>
    </tr>
    <tr>
      <td>메서드(<code class="language-plaintext highlighter-rouge">Method</code>)</td>
      <td>객체가 수행할 동작을 정의한 클래스 내부 코드 블록이다.</td>
    </tr>
    <tr>
      <td>생성자(<code class="language-plaintext highlighter-rouge">Constructor</code>)</td>
      <td>객체가 만들어질 때 초기 상태를 설정하는 특별한 코드 블록이다.</td>
    </tr>
  </tbody>
</table>

<h2 id="2-클래스-객체-인스턴스-참조-변수">2. 클래스, 객체, 인스턴스, 참조 변수</h2>

<h3 id="클래스와-객체">클래스와 객체</h3>

<p>클래스는 설계도이고 객체는 설계도로 만든 실제 결과물이다. 클래스 선언만으로는 객체가 생성되지 않으며, <code class="language-plaintext highlighter-rouge">new</code>를 사용해야 객체가 만들어진다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kt">int</span> <span class="n">age</span><span class="o">;</span>
<span class="o">}</span>

<span class="nc">User</span> <span class="n">user1</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
<span class="nc">User</span> <span class="n">user2</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">user1</code>과 <code class="language-plaintext highlighter-rouge">user2</code>는 같은 <code class="language-plaintext highlighter-rouge">User</code> 클래스에서 만들어졌지만 서로 다른 객체다. 따라서 각각의 <code class="language-plaintext highlighter-rouge">name</code>과 <code class="language-plaintext highlighter-rouge">age</code> 값을 독립적으로 가질 수 있다.</p>

<h3 id="인스턴스">인스턴스</h3>

<p><code class="language-plaintext highlighter-rouge">User</code> 클래스에서 만들어진 객체를 <code class="language-plaintext highlighter-rouge">User의 인스턴스</code>라고 한다. 즉, 객체는 실체에 초점을 둔 표현이고 인스턴스는 “어떤 클래스에서 만들어졌는가”에 초점을 둔 표현이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User 클래스
   │       │
   ▼       ▼
user1   user2
   │       │
User 인스턴스  User 인스턴스
</code></pre></div></div>

<h3 id="참조-변수">참조 변수</h3>

<p>참조 변수는 객체 자체가 아니라 객체를 가리키는 참조값을 저장한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">a</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
<span class="nc">User</span> <span class="n">b</span> <span class="o">=</span> <span class="n">a</span><span class="o">;</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a ─────┐
       ▼
  [ Heap의 User 객체 ]
       ▲
b ─────┘
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">a</code>와 <code class="language-plaintext highlighter-rouge">b</code>는 같은 객체를 가리킨다. <code class="language-plaintext highlighter-rouge">a</code>와 <code class="language-plaintext highlighter-rouge">b</code>가 별도의 변수가 아닌 것은 아니지만, 두 변수가 가리키는 객체는 하나다.</p>

<p>참고로 Java의 모든 클래스는 기본적으로 <code class="language-plaintext highlighter-rouge">java.lang.Object</code>를 직접 또는 간접적으로 상속한다. <code class="language-plaintext highlighter-rouge">int</code>, <code class="language-plaintext highlighter-rouge">double</code>, <code class="language-plaintext highlighter-rouge">boolean</code> 같은 기본형은 객체가 아니다.</p>

<h2 id="3-필드-메서드-생성자">3. 필드, 메서드, 생성자</h2>

<p>다음 <code class="language-plaintext highlighter-rouge">Stock</code> 클래스를 기준으로 세 개념을 함께 살펴보자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span> <span class="c1">// 필드</span>
    <span class="kd">private</span> <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>       <span class="c1">// 필드</span>

    <span class="nc">Stock</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="kt">double</span> <span class="n">price</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 생성자</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">updatePrice</span><span class="o">(</span><span class="kt">double</span> <span class="n">newPrice</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// 메서드</span>
        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">newPrice</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">()</span> <span class="o">{</span> <span class="c1">// 메서드</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">name</span> <span class="o">+</span> <span class="s">" : "</span> <span class="o">+</span> <span class="n">price</span> <span class="o">+</span> <span class="s">"원"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="필드field">필드(Field)</h3>

<p>필드는 클래스 안에 선언된 변수다. 객체가 가지고 있는 상태를 저장한다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">name</code>, <code class="language-plaintext highlighter-rouge">price</code>: Stock 객체의 상태</li>
  <li>인스턴스 필드: 객체마다 독립적으로 존재</li>
  <li><code class="language-plaintext highlighter-rouge">static</code> 필드: 클래스에 하나만 존재하며 객체들이 공유</li>
</ul>

<h3 id="메서드method">메서드(Method)</h3>

<p>메서드는 클래스 안에 선언된 코드 블록이다. 객체의 동작을 정의하며, 입력을 받을 수도 있고 결과를 반환할 수도 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span><span class="n">접근</span> <span class="n">제어자</span><span class="o">]</span> <span class="o">[</span><span class="n">반환형</span><span class="o">]</span> <span class="o">[</span><span class="n">메서드</span> <span class="n">이름</span><span class="o">](</span><span class="n">매개변수</span> <span class="n">목록</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// 실행할 코드</span>
    <span class="k">return</span> <span class="n">반환값</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">void</code>는 호출한 곳에 반환할 값이 없다는 뜻이다. <code class="language-plaintext highlighter-rouge">0</code>이나 <code class="language-plaintext highlighter-rouge">null</code>을 반환한다는 뜻이 아니다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">printWelcome</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"환영합니다."</span><span class="o">);</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kt">double</span> <span class="nf">getPrice</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">price</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="생성자constructor">생성자(Constructor)</h3>

<p>생성자는 <code class="language-plaintext highlighter-rouge">new</code>로 객체를 만들 때 자동으로 실행되어 초기값을 설정하는 특별한 코드 블록이다.</p>

<ul>
  <li>이름이 클래스 이름과 같다.</li>
  <li>반환형을 작성하지 않는다. <code class="language-plaintext highlighter-rouge">void</code>도 작성하지 않는다.</li>
  <li>생성자를 직접 작성하지 않으면 컴파일러가 기본 생성자를 제공한다. 단, 다른 생성자를 직접 작성하면 기본 생성자가 자동으로 생기지 않는다.</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Stock</span> <span class="n">stock</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">(</span><span class="s">"SKALA"</span><span class="o">,</span> <span class="mi">17000</span><span class="o">);</span>
</code></pre></div></div>

<p>위 코드가 실행되면 Heap에 Stock 객체가 만들어지고, 생성자가 <code class="language-plaintext highlighter-rouge">name</code>과 <code class="language-plaintext highlighter-rouge">price</code>를 초기화한다.</p>

<p>생성자는 메서드처럼 보이지만 Java 언어에서는 반환형이 없고 호출 방식도 다르므로 일반 메서드와 구분한다.</p>

<h2 id="4-this-매개변수-인자">4. <code class="language-plaintext highlighter-rouge">this</code>, 매개변수, 인자</h2>

<h3 id="this"><code class="language-plaintext highlighter-rouge">this</code></h3>

<p><code class="language-plaintext highlighter-rouge">this</code>는 인스턴스 메서드나 생성자에서 현재 객체 자신을 가리키는 참조다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="nc">User</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>this.name = name;
│           │
│           └─ 생성자에 전달된 매개변수
└─ 현재 객체의 필드
</code></pre></div></div>

<p>따라서 <code class="language-plaintext highlighter-rouge">this.name = name;</code>은 현재 객체의 <code class="language-plaintext highlighter-rouge">name</code> 필드에 매개변수 <code class="language-plaintext highlighter-rouge">name</code>의 값을 저장한다는 뜻이다.</p>

<h3 id="매개변수와-인자">매개변수와 인자</h3>

<ul>
  <li>매개변수(<code class="language-plaintext highlighter-rouge">Parameter</code>): 메서드 선언부에서 입력을 받기 위해 선언한 변수</li>
  <li>인자(<code class="language-plaintext highlighter-rouge">Argument</code>): 메서드를 호출할 때 실제로 전달하는 값</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">sayHello</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// name: 매개변수</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">name</span><span class="o">);</span>
<span class="o">}</span>

<span class="n">sayHello</span><span class="o">(</span><span class="s">"민형"</span><span class="o">);</span>                   <span class="c1">// "민형": 인자</span>
</code></pre></div></div>

<h2 id="5-멤버member">5. 멤버(Member)</h2>

<p>멤버는 클래스 내부에 속한 구성 요소를 통칭하는 말이다. 대표적으로 필드와 메서드가 있으며, 정적 멤버와 중첩 타입도 멤버에 포함될 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>        <span class="c1">// 필드 멤버</span>

    <span class="kt">void</span> <span class="nf">introduce</span><span class="o">()</span> <span class="o">{</span>  <span class="c1">// 메서드 멤버</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">name</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>생성자는 클래스 내부에 작성되지만 일반 메서드와는 구분되는 특별한 초기화 문법이다.</p>

<h2 id="6-static-클래스에-소속된-멤버">6. <code class="language-plaintext highlighter-rouge">static</code>: 클래스에 소속된 멤버</h2>

<p><code class="language-plaintext highlighter-rouge">static</code>은 필드나 메서드를 특정 객체가 아니라 클래스 자체에 소속시키는 키워드다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">static</span> <span class="kt">int</span> <span class="n">count</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>

    <span class="nc">User</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">count</span><span class="o">++;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">int</span> <span class="nf">getCount</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">count</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="nc">User</span> <span class="n">user1</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
<span class="nc">User</span> <span class="n">user2</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="nc">User</span><span class="o">.</span><span class="na">getCount</span><span class="o">());</span> <span class="c1">// 2</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">count</code>는 User 객체마다 따로 생기는 값이 아니라 User 클래스가 하나만 가지고 모든 객체가 공유하는 값이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User 클래스
    │
  count = 2  ← user1, user2가 함께 사용
</code></pre></div></div>

<p>static 메서드는 특정 객체 없이 실행될 수 있으므로 인스턴스 필드에 직접 접근할 수 없다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Example</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">instanceValue</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">static</span> <span class="kt">int</span> <span class="n">classValue</span><span class="o">;</span>

    <span class="kd">static</span> <span class="kt">void</span> <span class="nf">print</span><span class="o">()</span> <span class="o">{</span>
        <span class="c1">// System.out.println(instanceValue); // 오류</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">classValue</span><span class="o">);</span>       <span class="c1">// 가능</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="7-final-재대입과-변경-제한">7. <code class="language-plaintext highlighter-rouge">final</code>: 재대입과 변경 제한</h2>

<p><code class="language-plaintext highlighter-rouge">final</code>은 대상에 따라 의미가 조금 다르다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">final</span> <span class="kt">int</span> <span class="n">age</span> <span class="o">=</span> <span class="mi">25</span><span class="o">;</span>
<span class="c1">// age = 30; // 컴파일 오류</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>적용 대상</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>변수, 필드</td>
      <td>값을 다시 대입할 수 없음</td>
    </tr>
    <tr>
      <td>클래스</td>
      <td>다른 클래스가 상속할 수 없음</td>
    </tr>
    <tr>
      <td>메서드</td>
      <td>자식 클래스가 오버라이드할 수 없음</td>
    </tr>
  </tbody>
</table>

<p>참조 변수에 <code class="language-plaintext highlighter-rouge">final</code>을 사용하면 참조 대상을 바꿀 수 없다는 뜻이다. 객체 내부 필드까지 모두 변경할 수 없다는 뜻은 아니다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">final</span> <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
<span class="c1">// user = new User(); // 다른 객체를 다시 대입할 수 없음</span>
<span class="n">user</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="s">"민형"</span><span class="o">;</span>    <span class="c1">// name이 final이 아니면 변경 가능</span>
</code></pre></div></div>

<h2 id="8-접근-제어자와-캡슐화">8. 접근 제어자와 캡슐화</h2>

<h3 id="접근-제어자access-modifier">접근 제어자(Access Modifier)</h3>

<p>접근 제어자는 클래스, 필드, 메서드 등에 외부 코드가 접근할 수 있는 범위를 결정한다.</p>

<table>
  <thead>
    <tr>
      <th>접근 제어자</th>
      <th>접근 범위</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">public</code></td>
      <td>어디서든 접근 가능</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">protected</code></td>
      <td>같은 패키지와 자식 클래스에서 접근 가능</td>
    </tr>
    <tr>
      <td>기본 접근 제어자</td>
      <td>같은 패키지에서만 접근 가능</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">private</code></td>
      <td>선언된 클래스 내부에서만 접근 가능</td>
    </tr>
  </tbody>
</table>

<h3 id="캡슐화encapsulation">캡슐화(Encapsulation)</h3>

<p>캡슐화는 객체 내부의 데이터를 외부에서 직접 변경하지 못하게 숨기고, 필요한 기능만 메서드로 공개하는 설계 방식이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Account</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">balance</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">deposit</span><span class="o">(</span><span class="kt">int</span> <span class="n">money</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">money</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">balance</span> <span class="o">+=</span> <span class="n">money</span><span class="o">;</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">getBalance</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">balance</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="nc">Account</span> <span class="n">account</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Account</span><span class="o">();</span>
<span class="n">account</span><span class="o">.</span><span class="na">deposit</span><span class="o">(</span><span class="mi">10000</span><span class="o">);</span>
<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">account</span><span class="o">.</span><span class="na">getBalance</span><span class="o">());</span>
</code></pre></div></div>

<p>외부 코드는 <code class="language-plaintext highlighter-rouge">balance</code>를 직접 바꾸지 않고, 유효성 검사가 포함된 <code class="language-plaintext highlighter-rouge">deposit()</code>을 통해서만 잔액을 변경한다.</p>

<h3 id="getter와-setter">Getter와 Setter</h3>

<ul>
  <li>Getter: private 필드의 값을 반환하는 메서드</li>
  <li>Setter: private 필드에 값을 대입하는 메서드</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getName</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">name</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setName</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Getter와 Setter를 모든 필드에 무조건 만들 필요는 없다. 객체 설계에 따라 필요한 기능만 외부에 공개하는 것이 중요하다.</p>

<h2 id="9-상속과-오버라이드">9. 상속과 오버라이드</h2>

<h3 id="상속inheritance">상속(Inheritance)</h3>

<p>상속은 기존 클래스의 필드와 메서드를 자식 클래스가 물려받는 기능이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">eat</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"먹는다"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">Dog</span> <span class="kd">extends</span> <span class="nc">Animal</span> <span class="o">{</span>
<span class="o">}</span>

<span class="nc">Dog</span> <span class="n">dog</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Dog</span><span class="o">();</span>
<span class="n">dog</span><span class="o">.</span><span class="na">eat</span><span class="o">();</span> <span class="c1">// Animal에게서 물려받은 메서드</span>
</code></pre></div></div>

<p>Java 클래스는 부모 클래스를 하나만 직접 상속할 수 있다. 대신 하나의 클래스가 여러 인터페이스를 구현하는 것은 가능하다.</p>

<h3 id="오버라이드override">오버라이드(Override)</h3>

<p>오버라이드는 부모 클래스에서 물려받은 메서드를 자식 클래스에서 같은 형태로 다시 정의하는 것이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">sound</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"동물 소리"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">Dog</span> <span class="kd">extends</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">sound</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"멍멍"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="nc">Dog</span> <span class="n">dog</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Dog</span><span class="o">();</span>
<span class="n">dog</span><span class="o">.</span><span class="na">sound</span><span class="o">();</span> <span class="c1">// 멍멍</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Override</code>는 부모 메서드를 재정의한다는 것을 컴파일러에 알려 주는 애너테이션이다. 메서드 이름이나 매개변수를 잘못 작성했을 때 오류를 확인하는 데 도움이 된다.</p>

<h3 id="오버로딩과-오버라이드-비교">오버로딩과 오버라이드 비교</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>오버로딩(Overloading)</th>
      <th>오버라이드(Override)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>발생 위치</td>
      <td>같은 클래스 안</td>
      <td>부모와 자식 클래스 사이</td>
    </tr>
    <tr>
      <td>목적</td>
      <td>같은 이름으로 다른 입력 처리</td>
      <td>부모 동작을 자식에 맞게 변경</td>
    </tr>
    <tr>
      <td>매개변수</td>
      <td>달라야 함</td>
      <td>부모 메서드와 같아야 함</td>
    </tr>
    <tr>
      <td>표시</td>
      <td>별도 키워드 없음</td>
      <td><code class="language-plaintext highlighter-rouge">@Override</code> 사용 권장</td>
    </tr>
  </tbody>
</table>

<h2 id="10-핵심-용어-요약">10. 핵심 용어 요약</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>한 줄 정의</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>OOP</td>
      <td>객체를 중심으로 데이터와 동작을 묶어 설계하는 프로그래밍 방식</td>
    </tr>
    <tr>
      <td>클래스</td>
      <td>객체를 만들기 위한 설계도</td>
    </tr>
    <tr>
      <td>객체</td>
      <td>실행 중 메모리에 만들어진 실체</td>
    </tr>
    <tr>
      <td>인스턴스</td>
      <td>특정 클래스에서 만들어진 객체</td>
    </tr>
    <tr>
      <td>참조 변수</td>
      <td>객체를 가리키는 참조값을 저장하는 변수</td>
    </tr>
    <tr>
      <td>필드</td>
      <td>객체의 상태를 저장하는 변수</td>
    </tr>
    <tr>
      <td>메서드</td>
      <td>객체의 동작을 정의하는 코드 블록</td>
    </tr>
    <tr>
      <td>생성자</td>
      <td>객체 생성 시 초기값을 설정하는 코드 블록</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">this</code></td>
      <td>현재 객체를 가리키는 참조</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">static</code></td>
      <td>클래스에 소속되어 객체들이 공유하는 멤버를 만드는 키워드</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">final</code></td>
      <td>재대입, 상속, 오버라이드를 제한하는 키워드</td>
    </tr>
    <tr>
      <td>캡슐화</td>
      <td>내부 데이터를 숨기고 필요한 기능만 공개하는 설계 방식</td>
    </tr>
    <tr>
      <td>상속</td>
      <td>부모 클래스의 기능을 자식 클래스가 물려받는 기능</td>
    </tr>
    <tr>
      <td>오버라이드</td>
      <td>부모 메서드를 자식 클래스에서 다시 정의하는 것</td>
    </tr>
  </tbody>
</table>

<h2 id="마지막-한-번에-정리">마지막 한 번에 정리</h2>

<table>
  <thead>
    <tr>
      <th>체크 포인트</th>
      <th>핵심 문장</th>
      <th>외우기 포인트</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>클래스와 객체</td>
      <td>클래스는 설계도, 객체는 실행 중 만든 실체</td>
      <td><code class="language-plaintext highlighter-rouge">new</code> 뒤에만 객체가 생성됨</td>
    </tr>
    <tr>
      <td>인스턴스와 참조</td>
      <td>변수는 참조값을 저장</td>
      <td><code class="language-plaintext highlighter-rouge">a = b</code>는 객체 복사가 아니라 주소 공유</td>
    </tr>
    <tr>
      <td>필드와 메서드</td>
      <td>필드는 상태, 메서드는 동작</td>
      <td>필드는 <code class="language-plaintext highlighter-rouge">static</code>이면 공유, 아니면 객체별로 별도 생성</td>
    </tr>
    <tr>
      <td>생성자</td>
      <td>객체 생성 시점의 초기화 구문</td>
      <td>반환형이 없고 클래스명과 동일</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">this</code></td>
      <td>현재 객체 자신을 가리킴</td>
      <td><code class="language-plaintext highlighter-rouge">this.name = name</code>은 필드/매개변수 구분용</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">static</code></td>
      <td>클래스 소유 멤버</td>
      <td><code class="language-plaintext highlighter-rouge">ClassName.메서드()</code>로 호출 가능</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">final</code></td>
      <td>변경 제한 키워드</td>
      <td>변수는 재할당 금지, 클래스/메서드는 상속·오버라이드 제한</td>
    </tr>
    <tr>
      <td>접근 제어자</td>
      <td>외부 접근 범위 제어</td>
      <td><code class="language-plaintext highlighter-rouge">private</code> + getter/setter 조합으로 캡슐화</td>
    </tr>
    <tr>
      <td>상속/오버라이드</td>
      <td>기능 재사용과 동작 변경</td>
      <td>오버라이드는 이름·매개변수 동일 + <code class="language-plaintext highlighter-rouge">@Override</code></td>
    </tr>
    <tr>
      <td>오버로딩/오버라이드</td>
      <td>이름은 같아도 다른 용도</td>
      <td>오버로딩은 매개변수/시그니처 다름</td>
    </tr>
  </tbody>
</table>]]></content><author><name>Min Hyeong Lee</name></author><category term="Backend" /><category term="JAVA" /><category term="Java" /><category term="OOP" /><category term="Class" /><category term="Object" /><category term="Instance" /><category term="Field" /><category term="Method" /><category term="Constructor" /><category term="Static" /><category term="Encapsulation" /><category term="Inheritance" /><category term="Override" /><summary type="html"><![CDATA[Java OOP 핵심 개념]]></summary></entry><entry><title type="html">Java 클래스와 객체 정리: 필드, 메서드, 생성자, static</title><link href="http://localhost:4000/backend/java/2026/08/10/java-class-object/" rel="alternate" type="text/html" title="Java 클래스와 객체 정리: 필드, 메서드, 생성자, static" /><published>2026-08-10T11:00:00+09:00</published><updated>2026-08-10T11:00:00+09:00</updated><id>http://localhost:4000/backend/java/2026/08/10/java-class-object</id><content type="html" xml:base="http://localhost:4000/backend/java/2026/08/10/java-class-object/"><![CDATA[<h1 id="java-클래스와-객체">Java 클래스와 객체</h1>

<p>Java를 이해하려면 먼저 <code class="language-plaintext highlighter-rouge">클래스</code>, <code class="language-plaintext highlighter-rouge">객체</code>, <code class="language-plaintext highlighter-rouge">인스턴스</code>, <code class="language-plaintext highlighter-rouge">필드</code>, <code class="language-plaintext highlighter-rouge">메서드</code>, <code class="language-plaintext highlighter-rouge">생성자</code>를 구분해야 한다. 이 글에서는 각 용어를 다음 순서로 설명한다.</p>

<ol>
  <li>용어의 정확한 정의</li>
  <li>서로 어떤 관계인지</li>
  <li>코드에서 어떻게 사용하는지</li>
</ol>

<h2 id="1-핵심-용어-한눈에-보기">1. 핵심 용어 한눈에 보기</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정확한 정의</th>
      <th>쉬운 비유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>클래스(<code class="language-plaintext highlighter-rouge">Class</code>)</td>
      <td>객체를 만들기 위한 설계도이며, 객체가 가질 필드와 메서드를 정의한 코드 단위</td>
      <td>자동차 설계도</td>
    </tr>
    <tr>
      <td>객체(<code class="language-plaintext highlighter-rouge">Object</code>)</td>
      <td>프로그램 실행 중 클래스에 따라 실제로 만들어진 데이터</td>
      <td>실제 자동차</td>
    </tr>
    <tr>
      <td>인스턴스(<code class="language-plaintext highlighter-rouge">Instance</code>)</td>
      <td>특정 클래스에서 생성된 객체라는 관계를 강조하는 표현</td>
      <td>자동차 설계도로 만든 한 대의 자동차</td>
    </tr>
    <tr>
      <td>필드(<code class="language-plaintext highlighter-rouge">Field</code>)</td>
      <td>클래스 안에 선언되어 객체의 상태를 저장하는 변수</td>
      <td>자동차의 색상, 속도</td>
    </tr>
    <tr>
      <td>메서드(<code class="language-plaintext highlighter-rouge">Method</code>)</td>
      <td>클래스 안에 선언되어 객체의 동작을 정의하는 코드 블록</td>
      <td>자동차의 가속, 정지</td>
    </tr>
    <tr>
      <td>생성자(<code class="language-plaintext highlighter-rouge">Constructor</code>)</td>
      <td>객체가 생성될 때 필드를 초기화하는 특별한 코드 블록</td>
      <td>자동차를 출고할 때 기본 설정</td>
    </tr>
    <tr>
      <td>참조값(<code class="language-plaintext highlighter-rouge">Reference</code>)</td>
      <td>객체가 있는 곳을 가리키는 값</td>
      <td>자동차 주차 위치</td>
    </tr>
  </tbody>
</table>

<h3 id="객체와-인스턴스의-차이">객체와 인스턴스의 차이</h3>

<p>두 단어는 실제 대화에서 비슷하게 사용하지만 강조점이 다르다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">객체</code>: 실행 중 존재하는 실체라는 점을 강조한다.</li>
  <li><code class="language-plaintext highlighter-rouge">인스턴스</code>: 어떤 클래스에서 만들어졌는지를 강조한다.</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Stock</span> <span class="n">stock</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">(</span><span class="s">"SKALA"</span><span class="o">,</span> <span class="mi">17000</span><span class="o">);</span>
</code></pre></div></div>

<p>위 코드에서 <code class="language-plaintext highlighter-rouge">new Stock(...)</code>으로 Heap에 만들어진 실체는 <code class="language-plaintext highlighter-rouge">객체</code>다. 동시에 <code class="language-plaintext highlighter-rouge">Stock</code> 클래스에서 만들어졌으므로 <code class="language-plaintext highlighter-rouge">Stock의 인스턴스</code>라고도 부른다.</p>

<p>참고로 Java의 모든 클래스는 기본적으로 <code class="language-plaintext highlighter-rouge">java.lang.Object</code>를 직접 또는 간접적으로 상속한다. 다만 <code class="language-plaintext highlighter-rouge">int</code>, <code class="language-plaintext highlighter-rouge">double</code>, <code class="language-plaintext highlighter-rouge">boolean</code> 같은 기본형은 객체가 아니다.</p>

<h2 id="2-클래스는-설계도다">2. 클래스는 설계도다</h2>

<p>클래스는 객체를 만들기 전에 객체의 구조와 기능을 정의한다. 클래스 선언만 했다고 객체가 바로 만들어지는 것은 아니다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>    <span class="c1">// 필드</span>
    <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>   <span class="c1">// 필드</span>

    <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">()</span> <span class="o">{</span>  <span class="c1">// 메서드</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">name</span> <span class="o">+</span> <span class="s">" : "</span> <span class="o">+</span> <span class="n">price</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이 클래스는 다음을 정의한다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">name</code>, <code class="language-plaintext highlighter-rouge">price</code>: Stock 객체가 가질 상태</li>
  <li><code class="language-plaintext highlighter-rouge">printInfo()</code>: Stock 객체가 수행할 동작</li>
</ul>

<p>하지만 아직 <code class="language-plaintext highlighter-rouge">Stock</code> 객체가 만들어진 것은 아니다. 객체를 만들려면 <code class="language-plaintext highlighter-rouge">new</code>를 사용한다.</p>

<h2 id="3-객체와-인스턴스-만들기">3. 객체와 인스턴스 만들기</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Stock</span> <span class="n">scala</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">();</span>
</code></pre></div></div>

<p>실행 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Stock 클래스의 설계 정보 확인
          │
          ▼
Heap에 Stock 객체 생성
          │
          ▼
scala 변수에는 그 객체를 가리키는 참조값 저장
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Stock</code>: 어떤 클래스의 객체를 만들지 나타내는 타입</li>
  <li><code class="language-plaintext highlighter-rouge">scala</code>: 객체를 가리키는 참조형 변수</li>
  <li><code class="language-plaintext highlighter-rouge">new Stock()</code>: Heap에 새로운 Stock 객체를 생성하는 표현</li>
  <li><code class="language-plaintext highlighter-rouge">=</code>: 생성된 객체의 참조값을 <code class="language-plaintext highlighter-rouge">scala</code>에 대입</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Stock</span> <span class="n">scala</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">();</span>
<span class="nc">Stock</span> <span class="n">ai</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">();</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">scala</code>와 <code class="language-plaintext highlighter-rouge">ai</code>는 서로 다른 Stock 객체를 가리킨다. 같은 클래스에서 만들어졌지만 객체는 각각 독립적으로 존재한다.</p>

<h2 id="4-필드field-객체의-상태를-저장하는-변수">4. 필드(Field): 객체의 상태를 저장하는 변수</h2>

<h3 id="정의">정의</h3>

<p>필드란 클래스 내부에 선언된 변수다. 필드는 객체의 상태나 클래스 전체가 공유하는 값을 저장한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">name</code>과 <code class="language-plaintext highlighter-rouge">price</code>가 필드다.</p>

<h3 id="인스턴스-필드">인스턴스 필드</h3>

<p>인스턴스 필드는 객체마다 독립적으로 존재하는 필드다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Stock</span> <span class="n">scala</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">();</span>
<span class="nc">Stock</span> <span class="n">ai</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">();</span>

<span class="n">scala</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="s">"SKALA"</span><span class="o">;</span>
<span class="n">ai</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="s">"SKALA AI"</span><span class="o">;</span>
</code></pre></div></div>

<p>두 객체는 같은 <code class="language-plaintext highlighter-rouge">name</code> 필드를 가지고 있지만, 실제 값은 서로 다르게 저장된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>scala ──▶ Stock 객체 1: name = "SKALA"
ai    ──▶ Stock 객체 2: name = "SKALA AI"
</code></pre></div></div>

<h3 id="정적-필드static-field">정적 필드(<code class="language-plaintext highlighter-rouge">static</code> field)</h3>

<p>정적 필드는 특정 객체가 아니라 클래스에 소속된 필드다. 해당 클래스의 모든 객체가 하나의 값을 공유한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="kd">static</span> <span class="kt">int</span> <span class="n">count</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">count</code>는 Stock 객체마다 따로 생기는 값이 아니다. Stock 클래스에 하나만 존재하며 모든 Stock 객체가 함께 사용한다.</p>

<h3 id="상수constant">상수(<code class="language-plaintext highlighter-rouge">constant</code>)</h3>

<p>상수는 한 번 정해진 뒤 변경하지 않는 값이다. Java에서는 보통 <code class="language-plaintext highlighter-rouge">static final</code>로 선언한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">String</span> <span class="no">MARKET</span> <span class="o">=</span> <span class="s">"KOSPI"</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">static</code>: 클래스에 하나만 만든다.</li>
  <li><code class="language-plaintext highlighter-rouge">final</code>: 값을 다시 대입할 수 없다.</li>
  <li><code class="language-plaintext highlighter-rouge">static final</code>: 클래스 전체가 공유하며 변경할 수 없는 값이다.</li>
</ul>

<p>단, <code class="language-plaintext highlighter-rouge">final</code> 필드라고 해서 항상 컴파일 시점의 상수인 것은 아니다. 생성자에서 한 번만 값을 대입하는 <code class="language-plaintext highlighter-rouge">final</code> 인스턴스 필드도 만들 수 있다.</p>

<h2 id="5-생성자constructor-객체-초기화-담당">5. 생성자(Constructor): 객체 초기화 담당</h2>

<h3 id="정의-1">정의</h3>

<p>생성자는 <code class="language-plaintext highlighter-rouge">new</code>로 객체를 만들 때 자동으로 실행되는 특별한 코드 블록이다. 객체의 초기 상태를 설정하는 데 사용한다.</p>

<p>생성자의 규칙은 다음과 같다.</p>

<ul>
  <li>이름이 클래스 이름과 같아야 한다.</li>
  <li>반환형을 작성하지 않는다. <code class="language-plaintext highlighter-rouge">void</code>도 작성하지 않는다.</li>
  <li><code class="language-plaintext highlighter-rouge">new</code>를 사용해 객체를 만들 때 호출된다.</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">Stock</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="kt">double</span> <span class="n">price</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Stock</span> <span class="n">stock</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">(</span><span class="s">"SKALA"</span><span class="o">,</span> <span class="mi">17000</span><span class="o">);</span>
</code></pre></div></div>

<p>위 코드는 다음 순서로 동작한다.</p>

<ol>
  <li>Heap에 Stock 객체를 만든다.</li>
  <li><code class="language-plaintext highlighter-rouge">Stock(String name, double price)</code> 생성자를 호출한다.</li>
  <li>전달받은 값을 객체의 <code class="language-plaintext highlighter-rouge">name</code>, <code class="language-plaintext highlighter-rouge">price</code> 필드에 저장한다.</li>
  <li>생성된 객체의 참조값을 <code class="language-plaintext highlighter-rouge">stock</code> 변수에 저장한다.</li>
</ol>

<h2 id="6-this-현재-객체를-가리키는-참조">6. <code class="language-plaintext highlighter-rouge">this</code>: 현재 객체를 가리키는 참조</h2>

<h3 id="정의-2">정의</h3>

<p><code class="language-plaintext highlighter-rouge">this</code>는 현재 실행 중인 인스턴스 메서드나 생성자에서 <strong>현재 객체 자신을 가리키는 참조</strong>다. 흔히 “현재 객체의 주소”라고 설명하지만, Java에서는 주소 자체보다 객체를 가리키는 참조라고 표현하는 것이 정확하다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">User</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>여기서 왼쪽 <code class="language-plaintext highlighter-rouge">this.name</code>은 현재 객체의 필드이고, 오른쪽 <code class="language-plaintext highlighter-rouge">name</code>은 생성자의 매개변수다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>this.name = name;
│           │
│           └─ 생성자에 전달된 매개변수
└─ 현재 객체의 필드
</code></pre></div></div>

<p>매개변수 이름을 다르게 쓰면 <code class="language-plaintext highlighter-rouge">this</code>가 없어도 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="nf">User</span><span class="o">(</span><span class="nc">String</span> <span class="n">userName</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">name</span> <span class="o">=</span> <span class="n">userName</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>하지만 필드와 매개변수 이름이 같을 때는 <code class="language-plaintext highlighter-rouge">this</code>를 사용해야 둘을 구분할 수 있다.</p>

<h2 id="7-super-부모-클래스에-접근하는-키워드">7. <code class="language-plaintext highlighter-rouge">super</code>: 부모 클래스에 접근하는 키워드</h2>

<h3 id="정의-3">정의</h3>

<p><code class="language-plaintext highlighter-rouge">super</code>는 현재 클래스의 부모 클래스 부분을 가리키는 키워드다. 부모의 생성자를 호출하거나 부모의 필드와 메서드에 접근할 때 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Product</span> <span class="o">{</span>
    <span class="kd">protected</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="nc">Product</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">Stock</span> <span class="kd">extends</span> <span class="nc">Product</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>

    <span class="nc">Stock</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="kt">double</span> <span class="n">price</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">(</span><span class="n">name</span><span class="o">);</span> <span class="c1">// 부모 Product 생성자 호출</span>
        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">extends</code>: 자식 클래스가 부모 클래스를 상속한다는 뜻</li>
  <li><code class="language-plaintext highlighter-rouge">super(name)</code>: 부모 생성자 <code class="language-plaintext highlighter-rouge">Product(String name)</code> 호출</li>
  <li>부모 생성자 호출은 자식 생성자의 첫 번째 문장에 작성한다.</li>
</ul>

<h2 id="8-메서드method-객체의-동작을-정의하는-코드">8. 메서드(Method): 객체의 동작을 정의하는 코드</h2>

<h3 id="정의-4">정의</h3>

<p>메서드는 클래스 내부에 선언된 코드 블록으로, 특정 작업을 수행한다. 입력을 받을 수도 있고, 결과를 반환할 수도 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span><span class="n">접근</span> <span class="n">제어자</span><span class="o">]</span> <span class="o">[</span><span class="n">반환형</span><span class="o">]</span> <span class="o">[</span><span class="n">메서드</span> <span class="n">이름</span><span class="o">](</span><span class="n">매개변수</span> <span class="n">목록</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// 실행할 코드</span>
    <span class="k">return</span> <span class="n">반환값</span><span class="o">;</span> <span class="c1">// 반환형이 void가 아닐 때</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="메서드-구성-요소">메서드 구성 요소</h3>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>정확한 정의</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>접근 제어자</td>
      <td>어디에서 호출할 수 있는지 결정하는 키워드</td>
      <td><code class="language-plaintext highlighter-rouge">public</code>, <code class="language-plaintext highlighter-rouge">private</code></td>
    </tr>
    <tr>
      <td>반환형</td>
      <td>메서드가 호출한 곳에 돌려주는 값의 타입</td>
      <td><code class="language-plaintext highlighter-rouge">int</code>, <code class="language-plaintext highlighter-rouge">String</code>, <code class="language-plaintext highlighter-rouge">void</code></td>
    </tr>
    <tr>
      <td>메서드 이름</td>
      <td>메서드의 동작을 나타내는 이름</td>
      <td><code class="language-plaintext highlighter-rouge">getPrice()</code></td>
    </tr>
    <tr>
      <td>매개변수(Parameter)</td>
      <td>메서드가 실행될 때 받을 입력을 저장하는 변수</td>
      <td><code class="language-plaintext highlighter-rouge">double price</code></td>
    </tr>
    <tr>
      <td>본문</td>
      <td>실제 작업을 작성하는 중괄호 안의 코드</td>
      <td><code class="language-plaintext highlighter-rouge">{ ... }</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">return</code></td>
      <td>결과를 호출한 곳에 돌려주고 메서드를 종료하는 문장</td>
      <td><code class="language-plaintext highlighter-rouge">return price;</code></td>
    </tr>
  </tbody>
</table>

<h3 id="void란"><code class="language-plaintext highlighter-rouge">void</code>란?</h3>

<p><code class="language-plaintext highlighter-rouge">void</code>는 메서드가 호출한 곳에 <strong>반환할 값이 없다는 뜻의 반환형</strong>이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"주식 정보"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이 메서드는 화면에 출력하는 작업은 하지만 결과값을 돌려주지는 않는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">double</span> <span class="nf">calculatePriceChange</span><span class="o">(</span><span class="kt">double</span> <span class="n">before</span><span class="o">,</span> <span class="kt">double</span> <span class="n">after</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">after</span> <span class="o">-</span> <span class="n">before</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이 메서드는 <code class="language-plaintext highlighter-rouge">double</code> 값을 반환하므로 호출한 곳에서 결과를 받을 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">double</span> <span class="n">change</span> <span class="o">=</span> <span class="n">calculatePriceChange</span><span class="o">(</span><span class="mi">100</span><span class="o">,</span> <span class="mi">120</span><span class="o">);</span> <span class="c1">// change는 20.0</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">void</code>는 <code class="language-plaintext highlighter-rouge">0</code>이나 <code class="language-plaintext highlighter-rouge">null</code>을 반환한다는 뜻이 아니다. 애초에 반환값이 없는 것이다.</p>

<h3 id="메서드-호출">메서드 호출</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Stock</span> <span class="n">stock</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">(</span><span class="s">"SKALA"</span><span class="o">,</span> <span class="mi">17000</span><span class="o">);</span>
<span class="n">stock</span><span class="o">.</span><span class="na">updatePrice</span><span class="o">(</span><span class="mi">18000</span><span class="o">);</span>
<span class="n">stock</span><span class="o">.</span><span class="na">printInfo</span><span class="o">();</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">stock.updatePrice(...)</code>처럼 객체 뒤에 점(<code class="language-plaintext highlighter-rouge">.</code>)을 붙여 인스턴스 메서드를 호출한다.</p>

<h2 id="9-매개변수parameter와-인자argument">9. 매개변수(Parameter)와 인자(Argument)</h2>

<p>두 용어는 비슷하지만 위치가 다르다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">int</span> <span class="nf">add</span><span class="o">(</span><span class="kt">int</span> <span class="n">a</span><span class="o">,</span> <span class="kt">int</span> <span class="n">b</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// a, b는 매개변수</span>
    <span class="k">return</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="o">;</span>
<span class="o">}</span>

<span class="kt">int</span> <span class="n">result</span> <span class="o">=</span> <span class="n">add</span><span class="o">(</span><span class="mi">10</span><span class="o">,</span> <span class="mi">20</span><span class="o">);</span>      <span class="c1">// 10, 20은 인자</span>
</code></pre></div></div>

<ul>
  <li>매개변수(<code class="language-plaintext highlighter-rouge">Parameter</code>): 메서드 선언부에서 입력을 받기 위해 선언한 변수</li>
  <li>인자 또는 전달인자(<code class="language-plaintext highlighter-rouge">Argument</code>): 메서드를 호출할 때 실제로 전달하는 값</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>메서드 선언: add(int a, int b)
                    ▲       ▲
                 매개변수  매개변수

메서드 호출: add(10, 20)
                 ▲   ▲
                인자 인자
</code></pre></div></div>

<h2 id="10-오버로딩overloading">10. 오버로딩(Overloading)</h2>

<p>오버로딩은 같은 클래스 안에서 <strong>같은 이름의 메서드를 매개변수 목록을 다르게 하여 여러 개 정의하는 것</strong>이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">StockUtils</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printStockPrice</span><span class="o">(</span><span class="nc">String</span> <span class="n">stockName</span><span class="o">,</span> <span class="kt">double</span> <span class="n">price</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">stockName</span> <span class="o">+</span> <span class="s">" : "</span> <span class="o">+</span> <span class="n">price</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printStockPrice</span><span class="o">(</span><span class="nc">String</span> <span class="n">stockName</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"종목명: "</span> <span class="o">+</span> <span class="n">stockName</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">StockUtils</span> <span class="n">utils</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">StockUtils</span><span class="o">();</span>
<span class="n">utils</span><span class="o">.</span><span class="na">printStockPrice</span><span class="o">(</span><span class="s">"SKALA"</span><span class="o">,</span> <span class="mi">17000</span><span class="o">);</span> <span class="c1">// 두 매개변수 메서드 호출</span>
<span class="n">utils</span><span class="o">.</span><span class="na">printStockPrice</span><span class="o">(</span><span class="s">"SKALA"</span><span class="o">);</span>        <span class="c1">// 한 매개변수 메서드 호출</span>
</code></pre></div></div>

<p>반환형만 다르게 해서는 오버로딩할 수 없다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 매개변수 목록이 같으므로 오버로딩 불가</span>
<span class="kt">int</span> <span class="nf">getValue</span><span class="o">()</span> <span class="o">{</span> <span class="k">return</span> <span class="mi">1</span><span class="o">;</span> <span class="o">}</span>
<span class="kt">double</span> <span class="nf">getValue</span><span class="o">()</span> <span class="o">{</span> <span class="k">return</span> <span class="mf">1.0</span><span class="o">;</span> <span class="o">}</span>
</code></pre></div></div>

<h2 id="11-static-클래스에-소속된-멤버">11. <code class="language-plaintext highlighter-rouge">static</code>: 클래스에 소속된 멤버</h2>

<h3 id="정의-5">정의</h3>

<p><code class="language-plaintext highlighter-rouge">static</code>은 필드나 메서드를 특정 객체가 아니라 <strong>클래스에 소속시키는 키워드</strong>다. 객체를 여러 개 만들어도 static 필드는 클래스에 하나만 존재하고 공유된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Example</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">static</span> <span class="kt">int</span> <span class="n">count</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">instanceId</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">Example</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">count</span><span class="o">++;</span>
        <span class="n">instanceId</span> <span class="o">=</span> <span class="n">count</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">int</span> <span class="nf">getCount</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">count</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">getInstanceId</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">instanceId</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Example</span> <span class="n">e1</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Example</span><span class="o">();</span>
<span class="nc">Example</span> <span class="n">e2</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Example</span><span class="o">();</span>
<span class="nc">Example</span> <span class="n">e3</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Example</span><span class="o">();</span>

<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="nc">Example</span><span class="o">.</span><span class="na">getCount</span><span class="o">());</span>  <span class="c1">// 3</span>
<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">e1</span><span class="o">.</span><span class="na">getInstanceId</span><span class="o">());</span> <span class="c1">// 1</span>
<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">e2</span><span class="o">.</span><span class="na">getInstanceId</span><span class="o">());</span> <span class="c1">// 2</span>
<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">e3</span><span class="o">.</span><span class="na">getInstanceId</span><span class="o">());</span> <span class="c1">// 3</span>
</code></pre></div></div>

<h3 id="static과-인스턴스-멤버-비교">static과 인스턴스 멤버 비교</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>static 멤버</th>
      <th>인스턴스 멤버</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>소속</td>
      <td>클래스</td>
      <td>각각의 객체</td>
    </tr>
    <tr>
      <td>생성 개수</td>
      <td>클래스에 하나</td>
      <td>객체마다 하나</td>
    </tr>
    <tr>
      <td>호출 방법</td>
      <td><code class="language-plaintext highlighter-rouge">Example.getCount()</code></td>
      <td><code class="language-plaintext highlighter-rouge">e1.getInstanceId()</code></td>
    </tr>
    <tr>
      <td>객체 생성 필요</td>
      <td>static 메서드는 보통 필요 없음</td>
      <td>필요함</td>
    </tr>
    <tr>
      <td>직접 접근 가능한 값</td>
      <td>static 멤버</td>
      <td>static, 인스턴스 멤버</td>
    </tr>
  </tbody>
</table>

<p>static 메서드는 특정 객체가 없을 수 있으므로 인스턴스 필드에 직접 접근할 수 없다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Example</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">instanceValue</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">static</span> <span class="kt">int</span> <span class="n">classValue</span><span class="o">;</span>

    <span class="kd">static</span> <span class="kt">void</span> <span class="nf">print</span><span class="o">()</span> <span class="o">{</span>
        <span class="c1">// System.out.println(instanceValue); // 오류</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">classValue</span><span class="o">);</span>       <span class="c1">// 가능</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="12-전체-예제">12. 전체 예제</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">static</span> <span class="kt">int</span> <span class="n">count</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">Stock</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="kt">double</span> <span class="n">price</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">price</span><span class="o">;</span>
        <span class="n">count</span><span class="o">++;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">updatePrice</span><span class="o">(</span><span class="kt">double</span> <span class="n">newPrice</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">newPrice</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">name</span> <span class="o">+</span> <span class="s">" : "</span> <span class="o">+</span> <span class="n">price</span> <span class="o">+</span> <span class="s">"원"</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">int</span> <span class="nf">getCount</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">count</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Main</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">Stock</span> <span class="n">scalaEdu</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">(</span><span class="s">"스칼라 에듀"</span><span class="o">,</span> <span class="mi">15000</span><span class="o">);</span>
        <span class="nc">Stock</span> <span class="n">scalaAi</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">(</span><span class="s">"스칼라 AI"</span><span class="o">,</span> <span class="mi">17500</span><span class="o">);</span>

        <span class="n">scalaEdu</span><span class="o">.</span><span class="na">updatePrice</span><span class="o">(</span><span class="mi">15800</span><span class="o">);</span>
        <span class="n">scalaEdu</span><span class="o">.</span><span class="na">printInfo</span><span class="o">();</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"생성된 Stock 객체 수: "</span> <span class="o">+</span> <span class="nc">Stock</span><span class="o">.</span><span class="na">getCount</span><span class="o">());</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이 예제에서 확인할 수 있는 내용은 다음과 같다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Stock</code>: 객체를 만들기 위한 클래스</li>
  <li><code class="language-plaintext highlighter-rouge">scalaEdu</code>, <code class="language-plaintext highlighter-rouge">scalaAi</code>: Stock 객체를 가리키는 참조 변수</li>
  <li><code class="language-plaintext highlighter-rouge">new Stock(...)</code>: Stock 인스턴스 생성</li>
  <li><code class="language-plaintext highlighter-rouge">name</code>, <code class="language-plaintext highlighter-rouge">price</code>: 객체의 상태를 저장하는 필드</li>
  <li><code class="language-plaintext highlighter-rouge">Stock(...)</code>: 객체를 초기화하는 생성자</li>
  <li><code class="language-plaintext highlighter-rouge">updatePrice()</code>, <code class="language-plaintext highlighter-rouge">printInfo()</code>: 객체의 동작을 정의한 인스턴스 메서드</li>
  <li><code class="language-plaintext highlighter-rouge">count</code>, <code class="language-plaintext highlighter-rouge">getCount()</code>: Stock 클래스 전체가 공유하는 static 멤버</li>
  <li><code class="language-plaintext highlighter-rouge">this.name</code>, <code class="language-plaintext highlighter-rouge">this.price</code>: 현재 생성 중인 객체의 필드</li>
</ul>

<h2 id="13-최종-정리">13. 최종 정리</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>클래스(Class)
  └─ 객체를 만들기 위한 설계도
       ├─ 필드(Field): 상태를 저장하는 변수
       ├─ 메서드(Method): 동작을 정의하는 코드
       └─ 생성자(Constructor): 객체 생성 시 초기화

new Stock(...)
  └─ Stock 클래스의 인스턴스인 객체 생성
       ├─ 인스턴스 필드: 객체마다 별도 보유
       ├─ static 필드: 클래스 전체가 공유
       └─ 참조 변수: 객체를 가리키는 참조값 보유
</code></pre></div></div>

<ul>
  <li>클래스는 설계도이고, 객체는 설계도로 만든 실행 중인 실체다.</li>
  <li>인스턴스는 “특정 클래스에서 만들어진 객체”라는 관계를 나타낸다.</li>
  <li>필드는 상태, 메서드는 동작, 생성자는 초기화를 담당한다.</li>
  <li>매개변수는 메서드 선언부의 변수이고, 인자는 호출할 때 전달하는 실제 값이다.</li>
  <li><code class="language-plaintext highlighter-rouge">void</code>는 반환값이 없다는 뜻이다.</li>
  <li><code class="language-plaintext highlighter-rouge">this</code>는 현재 객체를 가리키고, <code class="language-plaintext highlighter-rouge">super</code>는 부모 클래스 부분에 접근한다.</li>
  <li><code class="language-plaintext highlighter-rouge">static</code> 멤버는 객체가 아니라 클래스에 소속되어 공유된다.</li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="Backend" /><category term="JAVA" /><category term="Java" /><category term="Class" /><category term="Object" /><category term="Instance" /><category term="Field" /><category term="Method" /><category term="Constructor" /><category term="This" /><category term="Super" /><category term="Static" /><summary type="html"><![CDATA[Java 클래스와 객체]]></summary></entry><entry><title type="html">Java 기초 정리: 실행 구조와 메모리</title><link href="http://localhost:4000/backend/java/2026/08/10/java-execution-memory/" rel="alternate" type="text/html" title="Java 기초 정리: 실행 구조와 메모리" /><published>2026-08-10T10:00:00+09:00</published><updated>2026-08-10T10:00:00+09:00</updated><id>http://localhost:4000/backend/java/2026/08/10/java-execution-memory</id><content type="html" xml:base="http://localhost:4000/backend/java/2026/08/10/java-execution-memory/"><![CDATA[<h1 id="java-기초-정리">Java 기초 정리</h1>

<p>Java 프로그램이 어떻게 실행되는지, 변수와 객체가 메모리에 어떻게 연결되는지, 그리고 문자열과 멀티스레드를 어떻게 다루는지 한 번에 정리한다.</p>

<h2 id="1-java-개발-환경">1. Java 개발 환경</h2>

<h3 id="필요한-도구-설치">필요한 도구 설치</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>jq curl maven gradle node git kubectl
</code></pre></div></div>

<h3 id="java-21-설치와-환경-변수-설정">Java 21 설치와 환경 변수 설정</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>openjdk@21
java <span class="nt">--version</span>
<span class="nb">export </span><span class="nv">JAVA_HOME</span><span class="o">=</span>/opt/homebrew/opt/openjdk@21
</code></pre></div></div>

<p>매번 설정하지 않으려면 <code class="language-plaintext highlighter-rouge">~/.zshrc</code>에 추가한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s1">'export JAVA_HOME=/opt/homebrew/opt/openjdk@21'</span> <span class="o">&gt;&gt;</span> ~/.zshrc
<span class="nb">source</span> ~/.zshrc
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">JAVA_HOME</code>은 Java가 설치된 위치를 알려 주는 환경 변수다. 실제 경로는 설치 환경에 따라 다를 수 있으므로 <code class="language-plaintext highlighter-rouge">brew --prefix openjdk@21</code>로 확인할 수 있다.</p>

<h2 id="2-java-코드가-실행되는-과정">2. Java 코드가 실행되는 과정</h2>

<p>Java는 소스 코드를 바로 실행하지 않는다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>App.java
   │  javac 컴파일
   ▼
App.class  ← JVM이 이해하는 바이트코드
   │  java 실행
   ▼
JVM이 main() 메서드를 찾아 실행
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> bin
javac <span class="nt">-d</span> bin src/App.java
java <span class="nt">-cp</span> bin App
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">javac</code>: <code class="language-plaintext highlighter-rouge">.java</code>를 <code class="language-plaintext highlighter-rouge">.class</code>로 컴파일한다.</li>
  <li><code class="language-plaintext highlighter-rouge">-d bin</code>: 컴파일 결과를 <code class="language-plaintext highlighter-rouge">bin</code> 폴더에 저장한다.</li>
  <li><code class="language-plaintext highlighter-rouge">java</code>: <code class="language-plaintext highlighter-rouge">.class</code> 파일을 JVM에서 실행한다.</li>
  <li><code class="language-plaintext highlighter-rouge">-cp bin</code>: 클래스를 찾을 경로를 지정한다.</li>
  <li><code class="language-plaintext highlighter-rouge">main()</code>: Java 애플리케이션의 시작 메서드다.</li>
</ul>

<p>즉, JVM이 <code class="language-plaintext highlighter-rouge">javac</code>를 실행하는 것이 아니라 <code class="language-plaintext highlighter-rouge">javac</code>가 만든 바이트코드를 JVM이 실행한다.</p>

<h2 id="3-java-프로그램의-기본-구조">3. Java 프로그램의 기본 구조</h2>

<p>Java 코드는 보통 클래스 안에 작성한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">HelloSkala</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Hello, Skala!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">class</code>: 데이터와 기능을 묶는 기본 단위다.</li>
  <li><code class="language-plaintext highlighter-rouge">main</code>: 프로그램이 시작되는 메서드다.</li>
  <li><code class="language-plaintext highlighter-rouge">System.out.println</code>: 콘솔에 값을 출력한다.</li>
  <li><code class="language-plaintext highlighter-rouge">static</code>: 객체를 만들지 않아도 클래스에 소속된 형태로 사용할 수 있다는 뜻이다.</li>
</ul>

<h2 id="4-기본형과-참조형">4. 기본형과 참조형</h2>

<h3 id="기본형">기본형</h3>

<p>기본형은 숫자, 문자, 참·거짓처럼 값 자체를 저장하는 타입이다.</p>

<table>
  <thead>
    <tr>
      <th>타입</th>
      <th>의미</th>
      <th>크기</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">byte</code></td>
      <td>작은 정수</td>
      <td>1 byte</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">short</code></td>
      <td>정수</td>
      <td>2 bytes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">int</code></td>
      <td>일반적인 정수</td>
      <td>4 bytes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">long</code></td>
      <td>큰 정수</td>
      <td>8 bytes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">float</code></td>
      <td>실수</td>
      <td>4 bytes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">double</code></td>
      <td>정밀한 실수</td>
      <td>8 bytes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">char</code></td>
      <td>문자 하나</td>
      <td>2 bytes</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">boolean</code></td>
      <td><code class="language-plaintext highlighter-rouge">true</code> 또는 <code class="language-plaintext highlighter-rouge">false</code></td>
      <td>JVM 구현에 따라 다름</td>
    </tr>
  </tbody>
</table>

<h3 id="참조형">참조형</h3>

<p>참조형 변수는 객체 본체를 직접 담는 것이 아니라, 객체를 가리키는 참조값을 저장한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">age</span> <span class="o">=</span> <span class="mi">20</span><span class="o">;</span>              <span class="c1">// age에 값 20 저장</span>
<span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="s">"Skala"</span><span class="o">;</span>     <span class="c1">// name에는 String 객체를 가리키는 참조값 저장</span>
<span class="kt">int</span><span class="o">[]</span> <span class="n">scores</span> <span class="o">=</span> <span class="o">{</span><span class="mi">90</span><span class="o">,</span> <span class="mi">80</span><span class="o">};</span>   <span class="c1">// scores에는 배열 객체를 가리키는 참조값 저장</span>
</code></pre></div></div>

<p>쉽게 말하면 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>name ── 참조값 ──▶ "Skala" String 객체
age  ── 값 20
</code></pre></div></div>

<h3 id="주의-기본값과-지역-변수">주의: 기본값과 지역 변수</h3>

<ul>
  <li>클래스의 필드에는 선언하지 않아도 기본값이 들어간다. <code class="language-plaintext highlighter-rouge">int</code>는 <code class="language-plaintext highlighter-rouge">0</code>, <code class="language-plaintext highlighter-rouge">boolean</code>은 <code class="language-plaintext highlighter-rouge">false</code>, 참조형은 <code class="language-plaintext highlighter-rouge">null</code>이다.</li>
  <li>메서드 안의 지역 변수는 자동으로 기본값이 들어가지 않는다. 사용하기 전에 반드시 값을 대입해야 한다.</li>
</ul>

<h2 id="5-jvm-메모리-구조">5. JVM 메모리 구조</h2>

<p>메모리를 설명할 때 자주 등장하는 영역은 Stack, Heap, Metaspace다.</p>

<h3 id="stack">Stack</h3>

<p>메서드가 호출될 때마다 해당 메서드의 Stack Frame이 만들어진다. 메서드가 끝나면 Frame도 사라진다.</p>

<p>Stack Frame에는 다음과 같은 정보가 들어간다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Local Variables</code>: 지역 변수와 매개변수</li>
  <li><code class="language-plaintext highlighter-rouge">Operand Stack</code>: 바이트코드 연산에 잠시 사용하는 값</li>
  <li>반환 주소 등 메서드 실행에 필요한 정보</li>
</ul>

<h3 id="heap">Heap</h3>

<p><code class="language-plaintext highlighter-rouge">new</code>로 만든 객체와 배열의 본체가 저장되는 영역이다. 객체는 메서드가 끝난 뒤에도 다른 곳에서 참조하고 있다면 계속 살아 있을 수 있다. 더 이상 참조되지 않는 객체는 Garbage Collector가 정리한다.</p>

<h3 id="metaspace">Metaspace</h3>

<p>클래스의 이름, 메서드 정보, 필드 정보, 바이트코드 등 클래스 구조에 대한 메타데이터가 저장되는 영역이다.</p>

<p><code class="language-plaintext highlighter-rouge">java.lang.Class</code> 객체도 Heap에 존재하며, Metaspace의 클래스 메타데이터와 연결되어 있다고 이해하면 된다.</p>

<h2 id="6-코드로-보는-데이터-흐름">6. 코드로 보는 데이터 흐름</h2>

<p>다음 코드를 기준으로 각 값의 위치를 살펴보자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Person</span> <span class="o">{</span>
    <span class="kd">static</span> <span class="kt">int</span> <span class="n">count</span><span class="o">;</span>
    <span class="kt">int</span> <span class="n">age</span> <span class="o">=</span> <span class="mi">20</span><span class="o">;</span>
<span class="o">}</span>

<span class="kt">void</span> <span class="nf">foo</span><span class="o">()</span> <span class="o">{</span>
    <span class="kt">int</span> <span class="n">x</span> <span class="o">=</span> <span class="mi">10</span><span class="o">;</span>
    <span class="nc">Person</span> <span class="n">person</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Person</span><span class="o">();</span>
    <span class="kt">int</span> <span class="n">result</span> <span class="o">=</span> <span class="n">person</span><span class="o">.</span><span class="na">age</span> <span class="o">+</span> <span class="n">x</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="실행-순서">실행 순서</h3>

<ol>
  <li><code class="language-plaintext highlighter-rouge">foo()</code>가 호출되면 <code class="language-plaintext highlighter-rouge">foo()</code> 전용 Stack Frame이 만들어진다.</li>
  <li><code class="language-plaintext highlighter-rouge">int x = 10</code>의 <code class="language-plaintext highlighter-rouge">x</code>는 Local Variables 영역에 값 <code class="language-plaintext highlighter-rouge">10</code>으로 저장된다.</li>
  <li><code class="language-plaintext highlighter-rouge">new Person()</code>이 Heap에 <code class="language-plaintext highlighter-rouge">Person</code> 객체를 만든다.</li>
  <li><code class="language-plaintext highlighter-rouge">person</code>은 Stack에 저장되고, Heap 객체를 가리키는 참조값을 가진다.</li>
  <li><code class="language-plaintext highlighter-rouge">person.age</code>의 <code class="language-plaintext highlighter-rouge">20</code>과 <code class="language-plaintext highlighter-rouge">x</code>의 <code class="language-plaintext highlighter-rouge">10</code>이 Operand Stack으로 전달되어 계산된다.</li>
  <li>계산 결과 <code class="language-plaintext highlighter-rouge">30</code>이 지역 변수 <code class="language-plaintext highlighter-rouge">result</code>에 저장된다.</li>
  <li><code class="language-plaintext highlighter-rouge">foo()</code>가 끝나면 <code class="language-plaintext highlighter-rouge">x</code>, <code class="language-plaintext highlighter-rouge">person</code>, <code class="language-plaintext highlighter-rouge">result</code>가 들어 있던 Stack Frame은 사라진다.</li>
</ol>

<pre><code class="language-mermaid">flowchart LR
    Call["foo() 호출"] --&gt; Frame["foo() Stack Frame"]
    Frame --&gt; X["Local Variables\nx = 10"]
    Frame --&gt; P["Local Variables\nperson = 참조값"]
    Frame --&gt; R["Local Variables\nresult = 30"]
    P --&gt;|"참조"| Object["Heap\nPerson 객체"]
    Object --&gt; Age["age = 20"]
    Age --&gt; Operand["Operand Stack\n20 + 10"]
    X --&gt; Operand
    Operand --&gt; R
</code></pre>

<h3 id="핵심-정리">핵심 정리</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>person ─────────────▶ Heap의 Person 객체
  Stack                  age = 20

x = 10 ──┐
age = 20 ─┴─▶ Operand Stack에서 20 + 10 계산
                    │
                    ▼
              result = 30
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">person</code>: Stack의 지역 변수 슬롯에 참조값이 저장된다.</li>
  <li><code class="language-plaintext highlighter-rouge">new Person()</code>: Heap에 객체 본체가 만들어진다.</li>
  <li><code class="language-plaintext highlighter-rouge">age</code>: Person 객체의 인스턴스 필드이므로 Heap 객체 안에 저장된다.</li>
  <li><code class="language-plaintext highlighter-rouge">x</code>, <code class="language-plaintext highlighter-rouge">result</code>: 메서드의 지역 변수이므로 해당 Stack Frame의 Local Variables에 저장된다.</li>
  <li>연산 중간값: Operand Stack을 사용한다.</li>
</ul>

<h3 id="static-필드는-어디에-있을까"><code class="language-plaintext highlighter-rouge">static</code> 필드는 어디에 있을까?</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Person</span> <span class="o">{</span>
    <span class="kd">static</span> <span class="kt">int</span> <span class="n">count</span><span class="o">;</span>
    <span class="kt">int</span> <span class="n">age</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">age</code>는 각 객체마다 따로 존재하지만, <code class="language-plaintext highlighter-rouge">count</code>는 Person 클래스 전체가 하나만 공유한다.</p>

<pre><code class="language-mermaid">flowchart TD
    Loader["ClassLoader"] --&gt; Meta["Metaspace\nPerson 클래스 메타데이터"]
    Loader --&gt; ClassObject["Heap\njava.lang.Class 객체"]
    ClassObject --&gt; Static["Person.count\n클래스 공용 데이터"]
    ClassObject -. "클래스 구조와 연결" .-&gt; Meta
    Instance1["Heap\nPerson 객체 1: age"]
    Instance2["Heap\nPerson 객체 2: age"]
</code></pre>

<ul>
  <li>인스턴스 필드 <code class="language-plaintext highlighter-rouge">age</code>는 객체마다 Heap에 하나씩 생긴다.</li>
  <li><code class="language-plaintext highlighter-rouge">static</code> 필드 <code class="language-plaintext highlighter-rouge">count</code>는 객체가 아니라 클래스에 소속되며 모든 인스턴스가 공유한다.</li>
  <li>정확한 물리적 위치와 구현 방식은 JVM에 따라 다를 수 있다. 따라서 <code class="language-plaintext highlighter-rouge">static</code> 필드를 무조건 “Heap의 <code class="language-plaintext highlighter-rouge">Class</code> 객체 안에 있다”고 단정하기보다, “클래스 단위로 관리되고 클래스 메타데이터와 연결된다”고 이해하는 편이 안전하다.</li>
</ul>

<h2 id="7-변수-저장-위치를-판단하는-방법">7. 변수 저장 위치를 판단하는 방법</h2>

<p>“기본형은 Stack, 참조형은 Heap”이라고 외우면 틀릴 수 있다. 변수의 종류와 실행 위치를 함께 봐야 한다.</p>

<table>
  <thead>
    <tr>
      <th>코드</th>
      <th>무엇인가?</th>
      <th>쉽게 이해하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">int x = 10</code> in method</td>
      <td>지역 기본형 변수</td>
      <td>Stack Frame에 값 저장</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Person person</code> in method</td>
      <td>지역 참조형 변수</td>
      <td>Stack Frame에 참조값 저장</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">new Person()</code></td>
      <td>객체 본체</td>
      <td>Heap에 저장</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">int age</code> in <code class="language-plaintext highlighter-rouge">Person</code></td>
      <td>인스턴스 필드</td>
      <td>Heap 객체 안에 저장</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">static int count</code></td>
      <td>클래스 필드</td>
      <td>클래스 단위로 공유</td>
    </tr>
  </tbody>
</table>

<p>예를 들어 다음 코드에서 <code class="language-plaintext highlighter-rouge">score</code>는 지역 변수가 아니라 인스턴스 필드다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Student</span> <span class="o">{</span>
    <span class="kt">int</span> <span class="n">score</span> <span class="o">=</span> <span class="mi">100</span><span class="o">;</span>
    <span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="s">"Min"</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Student student = new Student()</code>로 객체를 만들면 <code class="language-plaintext highlighter-rouge">score</code>와 <code class="language-plaintext highlighter-rouge">name</code>은 Student Heap 객체의 일부가 된다. 반대로 <code class="language-plaintext highlighter-rouge">void study() { int minutes = 30; }</code>의 <code class="language-plaintext highlighter-rouge">minutes</code>는 메서드의 지역 변수이므로 Stack Frame에서 관리된다.</p>

<h2 id="8-불변-객체와-wrapper-클래스">8. 불변 객체와 Wrapper 클래스</h2>

<h3 id="불변-객체">불변 객체</h3>

<p>불변 객체는 생성된 뒤 내부 값을 바꿀 수 없는 객체다. 대표적으로 <code class="language-plaintext highlighter-rouge">String</code>, <code class="language-plaintext highlighter-rouge">Integer</code>, <code class="language-plaintext highlighter-rouge">Long</code>, <code class="language-plaintext highlighter-rouge">Double</code>, <code class="language-plaintext highlighter-rouge">Character</code>, <code class="language-plaintext highlighter-rouge">Boolean</code>이 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">String</span> <span class="n">text</span> <span class="o">=</span> <span class="s">"Java"</span><span class="o">;</span>
<span class="n">text</span> <span class="o">=</span> <span class="n">text</span> <span class="o">+</span> <span class="s">"!"</span><span class="o">;</span>
</code></pre></div></div>

<p>위 코드는 기존 String 객체에 <code class="language-plaintext highlighter-rouge">!</code>를 붙이는 것이 아니다. <code class="language-plaintext highlighter-rouge">"Java!"</code>라는 새로운 String 객체를 만들고 <code class="language-plaintext highlighter-rouge">text</code>가 새 객체를 가리키게 된다.</p>

<h3 id="wrapper-클래스와-오토박싱">Wrapper 클래스와 오토박싱</h3>

<p>Wrapper 클래스는 기본형을 객체처럼 사용할 수 있게 감싼 클래스다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Integer</span> <span class="n">number</span> <span class="o">=</span> <span class="mi">10</span><span class="o">;</span> <span class="c1">// int 10이 Integer 객체로 자동 변환</span>
<span class="n">number</span> <span class="o">=</span> <span class="n">number</span> <span class="o">+</span> <span class="mi">1</span><span class="o">;</span>  <span class="c1">// 값을 바꾸는 대신 새 Integer 객체를 가리킴</span>
</code></pre></div></div>

<p>컬렉션은 객체만 저장할 수 있으므로 다음처럼 Wrapper 클래스를 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">scores</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
<span class="n">scores</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span>
</code></pre></div></div>

<h2 id="9-stringbuilder와-stringbuffer">9. StringBuilder와 StringBuffer</h2>

<p>String은 불변이므로 문자열을 반복해서 더하면 중간 객체가 많이 만들어질 수 있다. 이때 문자열을 수정할 수 있는 버퍼를 사용한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th><code class="language-plaintext highlighter-rouge">StringBuilder</code></th>
      <th><code class="language-plaintext highlighter-rouge">StringBuffer</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>문자열 수정</td>
      <td>가능</td>
      <td>가능</td>
    </tr>
    <tr>
      <td>동기화</td>
      <td>하지 않음</td>
      <td>메서드에 동기화 적용</td>
    </tr>
    <tr>
      <td>일반적인 속도</td>
      <td>빠름</td>
      <td>상대적으로 느림</td>
    </tr>
    <tr>
      <td>사용 상황</td>
      <td>대부분의 단일 스레드 코드</td>
      <td>여러 스레드가 같은 버퍼를 공유하는 경우</td>
    </tr>
  </tbody>
</table>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">StringBuilder</span> <span class="n">builder</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">StringBuilder</span><span class="o">();</span>
<span class="n">builder</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"Hello"</span><span class="o">).</span><span class="na">append</span><span class="o">(</span><span class="s">" "</span><span class="o">).</span><span class="na">append</span><span class="o">(</span><span class="s">"Java"</span><span class="o">);</span>
<span class="nc">String</span> <span class="n">result</span> <span class="o">=</span> <span class="n">builder</span><span class="o">.</span><span class="na">toString</span><span class="o">();</span>
</code></pre></div></div>

<p>실무에서는 공유하지 않는 문자열 조립에는 보통 <code class="language-plaintext highlighter-rouge">StringBuilder</code>를 사용한다. 여러 스레드가 하나의 버퍼를 직접 공유해야 하고 동기화가 필요하다면 <code class="language-plaintext highlighter-rouge">StringBuffer</code>를 고려한다.</p>

<h2 id="10-synchronized와-공유-자원">10. <code class="language-plaintext highlighter-rouge">synchronized</code>와 공유 자원</h2>

<p>여러 스레드가 같은 값을 동시에 수정하면 실행 순서에 따라 결과가 달라질 수 있다. 이런 문제가 발생하는 코드를 임계 영역이라고 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">BankAccount</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">balance</span> <span class="o">=</span> <span class="mi">1000</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kd">synchronized</span> <span class="kt">void</span> <span class="nf">withdraw</span><span class="o">(</span><span class="kt">int</span> <span class="n">amount</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">balance</span> <span class="o">&gt;=</span> <span class="n">amount</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">balance</span> <span class="o">-=</span> <span class="n">amount</span><span class="o">;</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"출금 성공: "</span> <span class="o">+</span> <span class="n">amount</span><span class="o">);</span>
        <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"잔액 부족"</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">withdraw()</code>에 <code class="language-plaintext highlighter-rouge">synchronized</code>를 붙이면 같은 계좌 객체를 대상으로 한 출금 메서드 실행에 한 번에 한 스레드만 들어간다. 따라서 잔액 확인과 차감이 중간에 끊기지 않도록 보호할 수 있다.</p>

<p>다만 <code class="language-plaintext highlighter-rouge">synchronized</code>는 모든 문제를 자동으로 해결하는 기능은 아니다. 어떤 객체를 잠그는지, 여러 메서드가 같은 공유 자원을 다루는지까지 함께 확인해야 한다.</p>

<h2 id="11-마지막-요약">11. 마지막 요약</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>소스 코드(.java)
      │ javac
      ▼
바이트코드(.class)
      │ JVM 실행
      ▼
┌────────────────────────────────────┐
│ Stack      : 메서드 실행, 지역 변수   │
│ Heap       : 객체와 배열의 본체       │
│ Metaspace  : 클래스 구조 정보         │
└────────────────────────────────────┘
</code></pre></div></div>

<ul>
  <li>기본형과 참조형은 “무조건 Stack과 Heap”으로 나누지 말고, 지역 변수인지 필드인지 먼저 확인한다.</li>
  <li>지역 변수는 Stack Frame에서 관리되는 경우가 많다.</li>
  <li>객체와 배열의 본체는 Heap에 저장된다.</li>
  <li>참조형 지역 변수에는 객체 자체가 아니라 객체를 가리키는 참조값이 저장된다.</li>
  <li>인스턴스 필드는 객체마다 존재하고, <code class="language-plaintext highlighter-rouge">static</code> 필드는 클래스 전체가 공유한다.</li>
  <li>메서드 내부 계산에는 Operand Stack이 사용된다.</li>
  <li>더 이상 참조되지 않는 Heap 객체는 Garbage Collector가 회수한다.</li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="Backend" /><category term="JAVA" /><category term="Java" /><category term="JDK" /><category term="JVM" /><category term="Stack" /><category term="Heap" /><category term="Thread" /><category term="StringBuilder" /><category term="Synchronized" /><summary type="html"><![CDATA[Java 기초 정리]]></summary></entry><entry><title type="html">Java 기초: 실행 구조와 JVM 메모리</title><link href="http://localhost:4000/backend/java/2026/08/10/java-basics/" rel="alternate" type="text/html" title="Java 기초: 실행 구조와 JVM 메모리" /><published>2026-08-10T09:00:00+09:00</published><updated>2026-08-10T09:00:00+09:00</updated><id>http://localhost:4000/backend/java/2026/08/10/java-basics</id><content type="html" xml:base="http://localhost:4000/backend/java/2026/08/10/java-basics/"><![CDATA[<h2 id="ip와-port">IP와 Port</h2>

<p>통신을 하려면 IP와 포트 정보가 모두 필요합니다.<br />
네트워크에서 프로세스 통신을 식별하는 기본 식별자는 <code class="language-plaintext highlighter-rouge">IP + PORT</code>입니다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>IP 주소</td>
      <td>인터넷에서 특정 호스트를 식별하는 논리적 주소</td>
    </tr>
    <tr>
      <td>Port</td>
      <td>해당 Host에서 실행 중인 서비스 또는 프로세스를 구분하는 번호</td>
    </tr>
    <tr>
      <td>예시</td>
      <td><code class="language-plaintext highlighter-rouge">http://168.126.63.42:8080</code> (<code class="language-plaintext highlighter-rouge">168.126.63.42</code> = IP, <code class="language-plaintext highlighter-rouge">8080</code> = Port)</td>
    </tr>
  </tbody>
</table>

<h3 id="localhost-vs-127001">Localhost vs 127.0.0.1</h3>

<p>동일 호스트 내에서 프로세스 간 통신할 때 사용하는 이름:
<code class="language-plaintext highlighter-rouge">localhost</code> (도메인 이름), <code class="language-plaintext highlighter-rouge">127.0.0.1</code> (IP 주소)</p>

<h3 id="process와-port">Process와 Port</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>값</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>현재 메모리에 로드되어 CPU를 할당받고 실행 중인 예시</td>
      <td>예시 실행</td>
    </tr>
    <tr>
      <td>내부 실행 명령</td>
      <td><code class="language-plaintext highlighter-rouge">python webserver.py</code>, <code class="language-plaintext highlighter-rouge">npm run dev</code>, <code class="language-plaintext highlighter-rouge">java -jar ./target/ideoperators-0.0.1-SNAPSHOT.jar</code></td>
    </tr>
    <tr>
      <td>외부 프로세스 간 통신</td>
      <td><code class="language-plaintext highlighter-rouge">172.8.32.9:8080</code></td>
    </tr>
    <tr>
      <td>내부 프로세스 간 통신</td>
      <td><code class="language-plaintext highlighter-rouge">127.0.0.1:8080</code>, <code class="language-plaintext highlighter-rouge">localhost:8080</code></td>
    </tr>
  </tbody>
</table>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python webserver.py
npm run dev
java <span class="nt">-jar</span> ./target/ideoperators-0.0.1-SNAPSHOT.jar
</code></pre></div></div>

<h2 id="process-vs-thread">Process vs Thread</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>프로세스 (Process)</th>
      <th>스레드 (Thread)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>정의</td>
      <td>실행 중인 프로그램의 인스턴스</td>
      <td>프로세스 내에서 실행되는 작업 단위</td>
    </tr>
    <tr>
      <td>메모리</td>
      <td>공간 독립적인 메모리 사용</td>
      <td>프로세스의 메모리 공간 공유</td>
    </tr>
    <tr>
      <td>생성 비용</td>
      <td>상대적으로 크고 무겁다</td>
      <td>낮고 가볍다</td>
    </tr>
    <tr>
      <td>통신 방법</td>
      <td>IPC(파이프, 소켓 등) 필요</td>
      <td>공유 메모리로 비교적 쉽게 통신</td>
    </tr>
    <tr>
      <td>충돌 영향</td>
      <td>다른 프로세스에 영향이 적음</td>
      <td>다른 스레드에 영향 가능</td>
    </tr>
    <tr>
      <td>예시</td>
      <td>브라우저, 게임, IDE 등 독립 실행 환경</td>
      <td>웹 페이지 렌더링, 백그라운드 다운로드</td>
    </tr>
  </tbody>
</table>

<p>한 프로세스 내부에서 여러 스레드를 실행할 수 있습니다.</p>

<h2 id="public-ip와-private-ip">Public IP와 Private IP</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Public IP</td>
      <td>인터넷 전역에서 접근 가능한 IP 주소</td>
    </tr>
    <tr>
      <td>Private IP</td>
      <td>특정 private network 내부에서만 유일하게 사용되는 IP (보통 내부용)</td>
    </tr>
  </tbody>
</table>

<h2 id="gateway-router-load-balancer">Gateway, Router, Load Balancer</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Router</td>
      <td>네트워크를 연결·확장하여 경로를 만드는 장비. 인터넷은 라우터들의 집합</td>
    </tr>
    <tr>
      <td>Gateway</td>
      <td>서비스/애플리케이션 진입점 역할, 외부 IP를 내부 IP로 연결/변환, 프로토콜 변환, 인증/필터/헤더 처리 등 계층 처리</td>
    </tr>
    <tr>
      <td>Load Balancer</td>
      <td>트래픽을 여러 노드에 분산/배분. 대표적으로 L4, L7 라우팅 역할</td>
    </tr>
  </tbody>
</table>

<h2 id="frontend--backend">Frontend / Backend</h2>

<h3 id="frontend">Frontend</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>역할</td>
      <td>사용자가 직접 보고 조작하는 UI/UX 영역을 설계·구현</td>
    </tr>
    <tr>
      <td>구조 작성</td>
      <td>HTML을 통해 페이지 뼈대 구성(제목, 단락, 버튼, 이미지 배치)</td>
    </tr>
    <tr>
      <td>디자인</td>
      <td>CSS로 색상, 글꼴, 여백, 애니메이션 처리</td>
    </tr>
    <tr>
      <td>동작 처리</td>
      <td>JavaScript로 클릭/입력/드래그 등 사용자 반응 처리</td>
    </tr>
    <tr>
      <td>데이터 처리</td>
      <td>API 연동 후 백엔드 응답을 화면에 노출</td>
    </tr>
    <tr>
      <td>UX 개선</td>
      <td>빠른 조작 경험을 위한 화면 흐름 최적화</td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>기본 언어</td>
      <td>HTML, CSS, JavaScript</td>
    </tr>
    <tr>
      <td>프레임워크/라이브러리</td>
      <td>React, Vue.js</td>
    </tr>
    <tr>
      <td>패키지 관리자</td>
      <td>npm, yarn</td>
    </tr>
    <tr>
      <td>빌드 도구</td>
      <td>Webpack, Vite</td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Endpoint</td>
      <td>서비스의 종단점, 경계 지점</td>
    </tr>
  </tbody>
</table>

<h3 id="back-end">Back end</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>역할</td>
      <td>프론트엔드 요청 처리, 비즈니스 로직 수행, 응답 반환</td>
    </tr>
    <tr>
      <td>핵심 기능</td>
      <td>서버 운영, DB 연동, API 제공, 보안/권한 관리, 성능 최적화 및 로깅</td>
    </tr>
    <tr>
      <td>프로그래밍 언어</td>
      <td>Java, Python, Node.js, Go, Kotlin</td>
    </tr>
    <tr>
      <td>프레임워크</td>
      <td>Spring Boot, Django, Express, FastAPI</td>
    </tr>
    <tr>
      <td>데이터베이스</td>
      <td>MySQL, PostgreSQL, MongoDB, Redis</td>
    </tr>
    <tr>
      <td>API 형식</td>
      <td>REST API, GraphQL</td>
    </tr>
    <tr>
      <td>배포 환경</td>
      <td>AWS, Docker, Nginx, GitHub Actions</td>
    </tr>
  </tbody>
</table>

<h2 id="현대적-frontendbackend-구조">현대적 Frontend/Backend 구조</h2>

<p>최초 1회 HTML을 로드한 뒤에는 페이지 전체 새로고침 없이 JavaScript가 동적으로 화면을 바꿉니다(SPA 구조).<br />
Client side에서 상태와 변수 데이터를 생성·처리하여 화면을 갱신합니다.</p>

<p><img src="image.png" alt="alt text" /></p>

<h2 id="json-format">JSON Format</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>정의</td>
      <td>키-값 쌍을 사용하는 경량 데이터 교환 형식</td>
    </tr>
    <tr>
      <td>특징</td>
      <td>언어에 구애받지 않음, 읽기/파싱이 쉬움</td>
    </tr>
    <tr>
      <td>주요 사용</td>
      <td>API 응답, 데이터 저장 및 전송</td>
    </tr>
  </tbody>
</table>

<h3 id="json-javascript-object-notation">JSON (JavaScript Object Notation)</h3>

<table>
  <thead>
    <tr>
      <th>요소</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>객체(Object)</td>
      <td><code class="language-plaintext highlighter-rouge">{}</code>로 묶인 키-값 쌍</td>
    </tr>
    <tr>
      <td>배열(Array)</td>
      <td><code class="language-plaintext highlighter-rouge">[]</code>로 묶인 값 목록</td>
    </tr>
    <tr>
      <td>값(Value)</td>
      <td>문자열, 숫자, 객체, 배열, true, false, null</td>
    </tr>
  </tbody>
</table>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"John Doe"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"age"</span><span class="p">:</span><span class="w"> </span><span class="mi">30</span><span class="p">,</span><span class="w">
  </span><span class="nl">"isMarried"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
  </span><span class="nl">"children"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"Anna"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Ben"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"address"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"city"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Seoul"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"zipCode"</span><span class="p">:</span><span class="w"> </span><span class="s2">"12345"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h3 id="yaml-format">YAML Format</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>정의</td>
      <td>YAML은 “YAML Ain’t Markup Language”의 약자로, 사람이 읽기 쉽게 설계된 직렬화 형식</td>
    </tr>
    <tr>
      <td>표현 방식</td>
      <td>들여쓰기를 통해 계층 구조 표현</td>
    </tr>
    <tr>
      <td>특징</td>
      <td>JSON보다 간결하고 가독성이 높은 편</td>
    </tr>
    <tr>
      <td>주석</td>
      <td><code class="language-plaintext highlighter-rouge">#</code>으로 작성 가능</td>
    </tr>
  </tbody>
</table>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">John Doe</span>
<span class="na">age</span><span class="pi">:</span> <span class="m">30</span>
<span class="na">isMarried</span><span class="pi">:</span> <span class="no">false</span>
<span class="na">children</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">Anna</span>
  <span class="pi">-</span> <span class="s">Ben</span>
<span class="na">address</span><span class="pi">:</span>
  <span class="na">city</span><span class="pi">:</span> <span class="s">Seoul</span>
  <span class="na">zipCode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">12345"</span>
</code></pre></div></div>

<h3 id="json-vs-yaml">JSON vs YAML</h3>

<table>
  <thead>
    <tr>
      <th>특징</th>
      <th>JSON</th>
      <th>YAML</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>표현 방식</td>
      <td>중괄호 기반의 구조화</td>
      <td>들여쓰기 기반의 구조화</td>
    </tr>
    <tr>
      <td>가독성</td>
      <td>딱딱한 편</td>
      <td>사람이 읽기 매우 쉬움</td>
    </tr>
    <tr>
      <td>주석 지원</td>
      <td>지원 안 함</td>
      <td><code class="language-plaintext highlighter-rouge">#</code> 주석 지원</td>
    </tr>
    <tr>
      <td>사용 사례</td>
      <td>API 응답, 데이터 교환</td>
      <td>구성/설정 파일</td>
    </tr>
    <tr>
      <td>파싱 속도</td>
      <td>보통 빠름</td>
      <td>상대적으로 느릴 수 있음</td>
    </tr>
    <tr>
      <td>유연성</td>
      <td>형식이 엄격함</td>
      <td>덜 엄격하고 유연함</td>
    </tr>
  </tbody>
</table>

<h1 id="좋은-소프트웨어란">좋은 소프트웨어란?</h1>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>응집도 (Cohesion)</td>
      <td>모듈/서비스가 하나의 책임에 집중하는 정도가 높을수록 변경·이해·재사용이 쉬움</td>
    </tr>
    <tr>
      <td>복잡도 (Complexity)</td>
      <td>로직과 아키텍처의 복잡도를 의미, 낮을수록 유지보수와 확장이 용이</td>
    </tr>
    <tr>
      <td>단독 실행 (Standalone Execution)</td>
      <td>시스템의 일부를 독립 실행/테스트할 수 있는 능력. 분산 개발·배포·복구에 유리</td>
    </tr>
    <tr>
      <td>결합도 (Coupling)</td>
      <td>요소 간 의존성. 낮을수록 개별 변경의 영향이 작고 확장 용이</td>
    </tr>
  </tbody>
</table>

<h2 id="소프트웨어-아키텍처-비교">소프트웨어 아키텍처 비교</h2>

<table>
  <thead>
    <tr>
      <th>기준</th>
      <th>Monolith</th>
      <th>MSA / Microservice Architecture</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>응집도</td>
      <td>역할과 책임이 혼재하기 쉬움</td>
      <td>기능별 책임 분리가 쉬워 응집도 개선</td>
    </tr>
    <tr>
      <td>복잡도</td>
      <td>로직/아키텍처 복잡도가 높고 통합 부담 큼</td>
      <td>서비스 단위 분리로 관리 범위가 명확해짐</td>
    </tr>
    <tr>
      <td>단독 실행</td>
      <td>전체 실행 의존도가 커 테스트가 어려울 수 있음</td>
      <td>개별 서비스 단위로 실행/테스트가 상대적으로 용이</td>
    </tr>
    <tr>
      <td>결합도</td>
      <td>변경 시 전체 영향이 큼 (Tightly Coupled)</td>
      <td>독립 배포·확장 가능, 변경 영향 범위 제한 (Loosely Coupled)</td>
    </tr>
  </tbody>
</table>

<p>—- 여기부터 다듬어야 한다.</p>

<h1 id="java의-역사">Java의 역사</h1>

<h2 id="java-등장-이전의-주요-어려움-1990년대-초반">Java 등장 이전의 주요 어려움 (1990년대 초반)</h2>

<ul>
  <li>플랫폼 종속성 묹제 (OS 하드웨어 의존)</li>
  <li>메모리 관리의 어려움</li>
  <li>메모리 직접 접근 및 OS 권한을 가지는 프로그램으로 인한 보안 취약</li>
  <li>네트워크 / 분산 프로그램 개발의 어려움</li>
</ul>

<h2 id="java의-등장">Java의 등장</h2>

<p>하드웨어와 OS 중심 개발에서 “플랫폼 독립적, 네트워크 중심, 엔터프라이즈 중심 개발”로 패러다임 전환을 위해 등장</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1. 플랫폼 독립성 해결</td>
      <td>write once, run anywhere, 소스코드 -&gt; 바이트코드, JVM, 윈도우, Linux, Unix</td>
    </tr>
    <tr>
      <td>2. 메모리 관리 자동화</td>
      <td>Garbage Collection 도입, 개발자가 메모리 해제 신경 쓸 필요 없음, 메모리 누수 감소, 서버 장기 실행에 안정적</td>
    </tr>
    <tr>
      <td>3. 강력한 보안 모델</td>
      <td>포인트 없음, 샌드 박스 실행 모델, 클래스 로딩 시 검증</td>
    </tr>
    <tr>
      <td>4. 네트워크를 기본 전제로 설계</td>
      <td>java.net 패키지 제공, socket, http, URL 추상화, 예외 기반 오류 처리, 웹서버, WAS, 분산 시스템으로 확장</td>
    </tr>
    <tr>
      <td>5. 대규모 엔터프라이즈 개발에 적합</td>
      <td>강제적 OOP, 명확한 타입 시스템, 패키지 구조, 멀티스레딩 표준화, 비동기 처리 기반의 대용량 처리 지원</td>
    </tr>
  </tbody>
</table>

<pre><code class="language-mermaid">flowchart LR
  S0["Java 등장 이전 (1990년대 초반)"]
  S1["플랫폼 종속성 묹제"]
  S2["메모리 관리의 어려움"]
  S3["보안 취약성 (직접 메모리 접근/OS 권한)"]
  S4["네트워크/분산 개발의 어려움"]
  S5["Java의 등장"]
  S6["write once, run anywhere"]
  S7["Garbage Collection"]
  S8["샌드박스 + 클래스 로딩 검증"]
  S9["네트워크 중심 설계 (java.net, socket/http/URL)"]
  S10["대규모 엔터프라이즈에 적합"]

  S0 --&gt; S1 --&gt; S2 --&gt; S3 --&gt; S4 --&gt; S5
  S5 --&gt; S6 --&gt; S7 --&gt; S8 --&gt; S9 --&gt; S10
</code></pre>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>이력</td>
      <td>Sun Microsystems의 제임스 고슬링James Gosling과 팀이 가전 제품용 언어 개발 시작</td>
    </tr>
    <tr>
      <td>인수</td>
      <td>2010년 Oracle이 SUN 인수하여, Java의 상품권, 저작권, 브랜드가 Oracle로 이전</td>
    </tr>
  </tbody>
</table>

<h3 id="java-버전시스템-정리">Java 버전/시스템 정리</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1995</td>
      <td>Java 1.0 공개 , Write Once, Run Anywhere</td>
    </tr>
    <tr>
      <td>1998</td>
      <td>Java 2 J2SE 1.2 Swing UI 도입, JVM 안정성 강화</td>
    </tr>
    <tr>
      <td>2018</td>
      <td>Java 11 Oracle이 LTS Long Term Support 정책 발표: 6개월마다 새 버전, 3년마다 LTS</td>
    </tr>
    <tr>
      <td>2025 기준 최신 버전</td>
      <td>GA: Java SE 25</td>
    </tr>
    <tr>
      <td>2025 기준 LTS</td>
      <td>Java SE 21</td>
    </tr>
    <tr>
      <td>SE</td>
      <td>standard Edition : 자바 언어의 기준이 되는 표준 플랫폼</td>
    </tr>
    <tr>
      <td>EE</td>
      <td>Enterprise Edition: WAS 같은 기업용 서버의 표준 사양 Jakarta EE로 이전</td>
    </tr>
    <tr>
      <td>GA</td>
      <td>General Availability: 정식 릴리즈. 검증된 안정 버전으로 새로운 기능이나 변경 사항을 포함. 최신 기술을 빠르게 경험하고 싶은 개발자용</td>
    </tr>
    <tr>
      <td>기타</td>
      <td>일반적으로 다음버전까지 6개월 단기 지원 신규 기능이라도 deprecated 될 수 있음</td>
    </tr>
    <tr>
      <td>LTS</td>
      <td>Long Term Support: 장기지원 버전. 몇년간 안정적이고 지속적인 보안/버그 패치, 호환성 검증된 버전이며, 기업, 프로덕션 환경용</td>
    </tr>
  </tbody>
</table>

<h2 id="interpreter-와-compiler">Interpreter 와 Compiler</h2>

<p>소스 코드를 기계가 이해할 수 있는 기계어로 변환 파일을 여기 저기서 바로 실행<br />
소스 코드를 한 줄(또는 일정 단위)씩 읽어서 해석(interpret) -&gt; 기계어 실행 -&gt; 실행</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Compiler</td>
      <td>프로그램 전체를 먼저 변환 실행하면서 조금씩 변환</td>
    </tr>
    <tr>
      <td>Interpreter</td>
      <td>실행 전에 완료 실행 중 계속 수행</td>
    </tr>
    <tr>
      <td>공통점</td>
      <td>한 번만 컴파일 실행할 때마다 해석 및 변환</td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ByteCode</td>
      <td><code class="language-plaintext highlighter-rouge">.pyc</code>, <code class="language-plaintext highlighter-rouge">.class</code></td>
    </tr>
    <tr>
      <td>JIT</td>
      <td>Just In Time</td>
    </tr>
    <tr>
      <td>의미</td>
      <td>Compiler의 기기 의존 관계 à 컨테이너로 해결</td>
    </tr>
  </tbody>
</table>

<pre><code class="language-mermaid">flowchart TB
  P["소스 코드"] --&gt; A1["한 줄/구간 해석 (Interpreter)"]
  P --&gt; B1["전체 변환 (Compiler)"]
  A1 --&gt; A2["기계어 실행"]
  B1 --&gt; C1["바이트코드 생성/변환"]
  C1 --&gt; C2["실행 중 계속 해석/변환"]
  B1 --&gt; C3["실행 전 변환 완료"]
  A2 --&gt; D["실행 결과"]
  C2 --&gt; D
  C3 --&gt; D
</code></pre>

<h3 id="언어-방식실행-방식-정리">언어 방식/실행 방식 정리</h3>

<table>
  <thead>
    <tr>
      <th>언어</th>
      <th>바이트코드?</th>
      <th>JIT?</th>
      <th>실행 방식</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>C</td>
      <td>컴파일</td>
      <td>X</td>
      <td>X Native 실행</td>
    </tr>
    <tr>
      <td>Rust</td>
      <td>컴파일</td>
      <td>X</td>
      <td>X Native 실행</td>
    </tr>
    <tr>
      <td>Go</td>
      <td>컴파일</td>
      <td>X</td>
      <td>X Native 실행</td>
    </tr>
    <tr>
      <td>Java</td>
      <td>.class</td>
      <td>O</td>
      <td>O Bytecode JIT Native</td>
    </tr>
    <tr>
      <td>Python</td>
      <td>.pyc</td>
      <td>O</td>
      <td>X</td>
    </tr>
    <tr>
      <td>외부 C/C Lib</td>
      <td>호출</td>
      <td>Bytecode</td>
      <td>Python VM</td>
    </tr>
    <tr>
      <td>JavaScript</td>
      <td>혼합</td>
      <td>O</td>
      <td>O Bytecode JIT Native</td>
    </tr>
  </tbody>
</table>

<pre><code class="language-mermaid">flowchart TD
  L["언어별 방식 정리"]
  L1["C / Rust / Go: 컴파일 -&gt; Native 실행"]
  L2["Java: .class 바이트코드 + JIT -&gt; Native"]
  L3["Python: .pyc 바이트코드, VM 기반 실행"]
  L4["JavaScript: 실행 시 바이트코드 + JIT"]
  L --&gt; L1 --&gt; L2 --&gt; L3 --&gt; L4
</code></pre>

<h2 id="언어별-position">언어별 Position</h2>

<table>
  <thead>
    <tr>
      <th>역할</th>
      <th>언어</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>엔터프라이즈 비즈니스 로직</td>
      <td>Java, kotlin</td>
    </tr>
    <tr>
      <td>클라우드 인프라 / 플랫폼</td>
      <td>Go</td>
    </tr>
    <tr>
      <td>시스템 핵심 / 고성능</td>
      <td>Rust</td>
    </tr>
    <tr>
      <td>AI / 데이터</td>
      <td>Python</td>
    </tr>
  </tbody>
</table>

<h2 id="언어별-position-중복-정리">언어별 Position (중복 정리)</h2>

<table>
  <thead>
    <tr>
      <th>역할</th>
      <th>언어</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>엔터프라이즈 비즈니스 로직</td>
      <td>Java, kotlin</td>
    </tr>
    <tr>
      <td>클라우드 인프라 / 플랫폼</td>
      <td>Go</td>
    </tr>
    <tr>
      <td>시스템 핵심 / 고성능</td>
      <td>Rust</td>
    </tr>
    <tr>
      <td>AI / 데이터</td>
      <td>Python</td>
    </tr>
  </tbody>
</table>

<h2 id="java-프로그램의-구조-자바-둘러보기">Java 프로그램의 구조 자바 둘러보기</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>클래스 구성</td>
      <td>Class를 기본 단위로 구성</td>
    </tr>
    <tr>
      <td>최소 단위</td>
      <td>가장 간단한 자바 프로그램도 최소 하나 이상의 클래스를 포함</td>
    </tr>
  </tbody>
</table>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 패키지 선언 (선택 사항)</span>
<span class="kn">package</span> <span class="nn">edu.skala</span><span class="o">;</span>
<span class="c1">// 클래스 선언</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">HelloSkala</span> <span class="o">{</span>
<span class="c1">// main 메서드 (프로그램의 시작점)</span>
<span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
<span class="c1">// 실행할 코드 작성</span>
<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Hello, Skala!"</span><span class="o">);</span>
<span class="c1">// 다른 메서드들 (선택 사항)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">anotherMethod</span><span class="o">()</span> <span class="o">{</span>
<span class="c1">// ...</span>
<span class="o">}</span>
<span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="java-메모리-구조">Java 메모리 구조</h2>

<p>Java 프로그램이 실행될 때 메모리는 여러 영역으로 나뉘어 사용됩니다. 그중 초보자가 가장 먼저 이해할 영역은 <code class="language-plaintext highlighter-rouge">Stack</code>과 <code class="language-plaintext highlighter-rouge">Heap</code>입니다.</p>

<p>쉽게 비유하면 다음과 같습니다.</p>

<table>
  <thead>
    <tr>
      <th>영역</th>
      <th>쉬운 비유</th>
      <th>주로 저장되는 것</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Stack</td>
      <td>현재 작업 중인 사람의 책상</td>
      <td>메서드의 호출 정보, 지역 변수, 기본형 값, 객체를 가리키는 참조값</td>
      <td>메서드가 끝나면 해당 메서드의 공간이 자동으로 정리됨. 스레드마다 별도의 Stack을 가짐</td>
    </tr>
    <tr>
      <td>Heap</td>
      <td>여러 사람이 함께 사용하는 창고</td>
      <td><code class="language-plaintext highlighter-rouge">new</code>로 만든 객체와 배열</td>
      <td>여러 스레드가 공유함. 더 이상 사용하지 않는 객체는 Garbage Collector가 정리함</td>
    </tr>
  </tbody>
</table>

<h3 id="아주-간단한-예시">아주 간단한 예시</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Person</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">MemoryExample</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="kt">int</span> <span class="n">age</span> <span class="o">=</span> <span class="mi">20</span><span class="o">;</span>
        <span class="nc">Person</span> <span class="n">person</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Person</span><span class="o">();</span>
        <span class="n">person</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="s">"민형"</span><span class="o">;</span>

        <span class="n">printPerson</span><span class="o">(</span><span class="n">person</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">static</span> <span class="kt">void</span> <span class="nf">printPerson</span><span class="o">(</span><span class="nc">Person</span> <span class="n">target</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">target</span><span class="o">.</span><span class="na">name</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>위 코드가 실행되는 과정을 순서대로 보면 다음과 같습니다.</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">main()</code>이 시작되면 <code class="language-plaintext highlighter-rouge">main</code>용 Stack 프레임이 만들어집니다.</li>
  <li><code class="language-plaintext highlighter-rouge">int age = 20</code>에서 <code class="language-plaintext highlighter-rouge">age</code>와 숫자 <code class="language-plaintext highlighter-rouge">20</code>은 <code class="language-plaintext highlighter-rouge">main</code> 프레임 안에 저장됩니다.</li>
  <li><code class="language-plaintext highlighter-rouge">new Person()</code>은 Heap에 실제 <code class="language-plaintext highlighter-rouge">Person</code> 객체를 만듭니다.</li>
  <li><code class="language-plaintext highlighter-rouge">person</code> 변수에는 객체 자체가 아니라 Heap에 있는 객체를 찾아가기 위한 참조값이 저장됩니다.</li>
  <li><code class="language-plaintext highlighter-rouge">printPerson(person)</code>을 호출하면 새로운 <code class="language-plaintext highlighter-rouge">printPerson</code>용 Stack 프레임이 쌓입니다. 매개변수 <code class="language-plaintext highlighter-rouge">target</code>도 이 프레임에 저장됩니다.</li>
  <li><code class="language-plaintext highlighter-rouge">printPerson()</code>이 끝나면 <code class="language-plaintext highlighter-rouge">target</code>이 들어 있던 프레임은 제거됩니다.</li>
  <li><code class="language-plaintext highlighter-rouge">main()</code>까지 끝나고 더 이상 <code class="language-plaintext highlighter-rouge">Person</code> 객체를 가리키는 변수가 없으면, 해당 객체는 나중에 Garbage Collector의 정리 대상이 됩니다.</li>
</ol>

<p>즉, <code class="language-plaintext highlighter-rouge">person</code>은 Stack에 있는 “창고 위치 메모”이고, <code class="language-plaintext highlighter-rouge">new Person()</code>으로 만들어진 실제 객체는 Heap에 있습니다.</p>

<pre><code class="language-mermaid">flowchart TB
  Main["main Stack 프레임&lt;br/&gt;age = 20&lt;br/&gt;person = 참조값"]
  Print["printPerson Stack 프레임&lt;br/&gt;target = 참조값"]
  Person["Heap&lt;br/&gt;Person 객체&lt;br/&gt;name = 민형"]
  GC["Garbage Collector"]

  Main --&gt;|person이 가리킴| Person
  Print --&gt;|target이 가리킴| Person
  Main -. 메서드 호출 .-&gt; Print
  Person -. 더 이상 참조되지 않으면 .-&gt; GC
</code></pre>

<h3 id="stack과-heap의-차이-기억하기">Stack과 Heap의 차이 기억하기</h3>

<ul>
  <li>메서드가 호출되면 Stack에 작업 공간이 생기고, 메서드가 끝나면 사라집니다.</li>
  <li><code class="language-plaintext highlighter-rouge">int</code>, <code class="language-plaintext highlighter-rouge">double</code>, <code class="language-plaintext highlighter-rouge">boolean</code> 같은 기본형 지역 변수는 보통 Stack 프레임에서 바로 관리됩니다.</li>
  <li><code class="language-plaintext highlighter-rouge">new Person()</code>, <code class="language-plaintext highlighter-rouge">new int[3]</code>처럼 만든 객체와 배열은 Heap에 생성됩니다.</li>
  <li>객체 변수는 객체 그 자체가 아니라 Heap의 객체를 가리키는 참조값을 담습니다.</li>
  <li>Heap의 객체를 더 이상 아무도 사용하지 않으면 Garbage Collector가 정리합니다.</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">static</span> <span class="kt">void</span> <span class="nf">example</span><span class="o">()</span> <span class="o">{</span>
    <span class="kt">int</span> <span class="n">number</span> <span class="o">=</span> <span class="mi">10</span><span class="o">;</span>          <span class="c1">// Stack 프레임에 값 저장</span>
    <span class="nc">Person</span> <span class="n">p</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Person</span><span class="o">();</span>  <span class="c1">// p는 Stack, 실제 Person 객체는 Heap</span>
<span class="o">}</span> <span class="c1">// example()의 Stack 프레임은 제거됨</span>
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">example()</code>이 끝나면 <code class="language-plaintext highlighter-rouge">number</code>와 <code class="language-plaintext highlighter-rouge">p</code>는 사라집니다. <code class="language-plaintext highlighter-rouge">p</code>가 가리키던 <code class="language-plaintext highlighter-rouge">Person</code> 객체도 다른 곳에서 사용하지 않는다면 Garbage Collector가 나중에 Heap에서 정리할 수 있습니다.</p>

<h2 id="자바와-jvm">자바와 JVM</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JVM Java Virtual Machine 자바를 실행하기 위한 가상 머신
Java 어플리케이션은 JVM 위에서 동작하기 때문에 OS에 종속적이지 않음
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Java 어플리케이션
   ↓
JVM
   ↓
OS Windows, MacOS
   ↓
컴퓨터
</code></pre></div></div>

<h2 id="java-파일과-컴파일">Java 파일과 컴파일</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>.java 파일</td>
      <td>소스 코드 파일 개발자가 작성하는 텍스트 기반의 Java 프로그램 소스 코드</td>
    </tr>
    <tr>
      <td>.class 파일</td>
      <td>바이트코드 파일 컴파일 결과물 JVM이 읽고 실행할 수 있는 중간 언어로 변환 플랫폼 독립적</td>
    </tr>
    <tr>
      <td>JVM</td>
      <td>Java Virtual Machine: 실행 엔진 바이트코드class 파일 을 실행하는 가상 머신</td>
    </tr>
  </tbody>
</table>

<pre><code class="language-mermaid">flowchart LR
  JavaFile[".java 파일"] --&gt; Compile["컴파일"]
  Compile --&gt; Bytecode[".class (바이트코드)"]
  Bytecode --&gt; JVM["JVM 실행 엔진"]
  JVM --&gt; OS["OS 독립 실행"]
</code></pre>]]></content><author><name>Min Hyeong Lee</name></author><category term="Backend" /><category term="JAVA" /><category term="Java" /><category term="JVM" /><category term="Stack" /><category term="Heap" /><category term="Garbage Collection" /><category term="Backend" /><summary type="html"><![CDATA[Java의 등장 배경부터 컴파일, JVM, Stack과 Heap의 동작 원리까지 기초 개념을 예시로 정리한다.]]></summary></entry><entry><title type="html">AI 시대, 개발자는 사라지는가?</title><link href="http://localhost:4000/ai/development/2026/08/09/ai-era-developers-disappear/" rel="alternate" type="text/html" title="AI 시대, 개발자는 사라지는가?" /><published>2026-08-09T22:00:00+09:00</published><updated>2026-08-09T22:00:00+09:00</updated><id>http://localhost:4000/ai/development/2026/08/09/ai-era-developers-disappear</id><content type="html" xml:base="http://localhost:4000/ai/development/2026/08/09/ai-era-developers-disappear/"><![CDATA[<blockquote>
  <p>⭐ <strong>개발자는 사라지는가? 엔진이 등장한 시대의 마부와 같은 운명을 맞이하게 되는가?</strong></p>
</blockquote>

<p>작년만 하더라도 AI를 이용해 프로젝트 전체를 진행한다고 하면 꽤 많은 비판을 받았을 것이다. (ㄹㅇ AI가 그냥 오류 발사기였기에.. ㅋㅋ)</p>

<p>당시에는 AI의 할루시네이션 문제가 컸고, 복잡한 프로젝트를 하나의 긴 컨텍스트 안에서 이해하고 해결하는 능력도 충분하지 않았다.</p>

<p>뭐… 코드를 일부 생성하거나 간단한 오류를 찾는 정도에서는 쓸 만했지만, 프로젝트 전체를 맡기기에는 신뢰하기 어려운 도구에 가까웠다.</p>

<p>그런데 2026년부터는 아예 상황이 많이 달라졌다.</p>
<ul>
  <li>(CODEX, CLAUDE CODE, ANTIGRAVITY LET’S GO)</li>
</ul>

<p>지난 1년 동안 수많은 프론티어 모델이 출시됐고, 코딩 에이전트의 성능 역시 빠르게 상승했다.(중국에서도 QWEN,KIMI,DeepSeek 같은 저비용 고효율 AI도 출시되었다.)</p>

<p>이제는 단순한 함수 하나를 작성하는 수준을 넘어 여러 파일을 동시에 분석하고, 기존 프로젝트의 구조를 이해하고, 테스트를 실행하고, 오류를 수정하고, 목적에 맞게 코드를 수정하는 작업까지 수행한다.</p>

<p>실제로 프로젝트를 진행하다 보면 직접 코드를 작성하는 시간보다 AI가 작성한 코드를 읽고, 방향을 결정하고, 결과를 검증하는 시간이 점점 더 많아지고 있다는 것을 느낄 수 있을 것이다.</p>

<p>이런 상황을 보면서 한 가지 의문이 들었다.</p>

<blockquote>
  <p>기존처럼 사람이 직접 코드를 작성하고, 사람이 직접 리뷰하며 개발하는 방식은 앞으로도 의미가 있을까? 사람이 코드 읽는 시간 때문에 작업 속도가 지연되는데?</p>
</blockquote>

<p>오늘은 최근 AI 코딩 도구와 개발 환경의 변화를 살펴보고, 앞으로 개발 관련 일을 하려는 사람은 어떤 관점을 가지고 준비해야 하는지 생각해보려고 한다.</p>

<hr />

<h1 id="1-ai-코드-리뷰에-관하여">1. AI 코드 리뷰에 관하여</h1>

<p>2025년 Stack Overflow Developer Survey에서는 응답자의 84%가 개발 과정에서 AI를 사용하거나 사용할 계획이라고 답했고, 전문 개발자의 51%는 매일 AI를 사용한다고 답했다.</p>

<p>그런데 AI 출력의 정확성을 신뢰한다고 답한 비율은 33%였던 반면, 신뢰하지 않는다고 답한 비율은 46%였다.</p>

<p>즉, AI 사용 자체는 이미 개발 과정의 일반적인 도구가 됐지만, <strong>AI가 만든 결과를 어떻게 검증할 것인가에 대한 문제는 여전히 해결되지 않았다는 의미다.</strong></p>

<p>현재 시점에서는 당시보다 훨씬 많은 개발자가 AI를 이용해 코드를 작성하고 있을 것이라고 생각한다.</p>

<p>(2026에는 작년보다 더 사람들이 AI 개발도구를 신뢰하고 있을 것이라 생각한다. )</p>

<table>
  <tbody>
    <tr>
      <td>[AI</td>
      <td>2025 Stack Overflow Developer Survey](https://survey.stackoverflow.co/2025/ai)</td>
    </tr>
  </tbody>
</table>

<p>코드 리뷰 역시 이미 AI가 상당 부분 수행하고 있다.</p>

<p>GitHub는 2026년 3월까지 Copilot을 이용한 코드 리뷰가 6천만 건 이상 수행됐다고 발표했고, 2026년 5월에는 GitHub에서 이루어지는 코드 리뷰 5건 중 1건 이상에 에이전트가 관여하고 있다고 설명했다.</p>

<p><a href="https://github.blog/ai-and-ml/github-copilot/60-million-copilot-code-reviews-and-counting/">60 million Copilot code reviews and counting</a></p>

<p>그만큼 이제는 AI로 코딩하는 일은 당연하게 되었다.</p>

<p>분명히 AI를 이용해 코드를 작성하는 것은 분명 빠르고 편리하다.</p>

<p>반복적인 코드를 작성하거나, 여러 파일을 수정하거나, 기존 코드의 구조를 분석하는 작업에서는 사람이 직접 작업하는 것보다 훨씬 빠른 경우도 많다.</p>

<h4 id="하지만-이러한-방식에-장점만-있는-것은-아니다">하지만 이러한 방식에 장점만 있는 것은 아니다.</h4>

<p>문제는 <strong>AI가 작성한 코드가 겉으로는 상당히 그럴듯하고 테스트까지 통과하지만, 장기적으로는 중복 코드와 기술 부채를 남길 수 있다는 점이다.</strong> 쉽게 말하자면, 기술은 이해하지 못한 채로 불필요한 구조 및 코드를 만들어내고 있다는 것이다.</p>

<p>2026년 공개된 연구에서는 AI 에이전트가 사람보다 기존 코드의 재사용 기회를 더 자주 놓치는 경향이 관찰됐다.</p>

<p>더 흥미로운 점은 리뷰어가 이러한 코드를 오히려 긍정적으로 평가하는 경우도 있었다는 것이다.</p>

<p>결국 앞으로는 단순히 오류가 있는 코드뿐만 아니라,</p>

<blockquote>
  <p><strong>깔끔하게 보이지만 구조적으로 잘못된 코드</strong></p>
</blockquote>

<p>를 찾아내는 능력이 더욱 중요해질 수 있다.</p>

<p><a href="https://arxiv.org/abs/2601.21276">More Code, Less Reuse: Investigating Code Quality and Reviewer Perception</a></p>

<hr />

<h1 id="2-그러면-사람은-무엇을-하게-되는가">2. 그러면 사람은 무엇을 하게 되는가?</h1>

<blockquote>
  <p><strong>기존에 일하던 방식을 그대로 유지하기보다 새로운 개발 방식을 받아들여야 한다.</strong> 이미 변화는 시작되었고, 우리는 다시는 뒤로 돌아갈 수는 없다.</p>
</blockquote>

<p>Anthropic이 약 40만 건의 Claude Code 세션을 분석한 결과를 보면 흥미로운 변화가 나타난다.</p>

<p>사람은 주로 <strong>무엇을 만들 것인지</strong> 결정하고, 에이전트는 주로 <strong>어떻게 구현할 것인지</strong> 결정하는 형태의 역할 분담이 나타났다고 한다.</p>

<p>또한 단순한 코딩 숙련도보다 문제 영역에 대한 도메인 지식이 작업 성공률과 오류 복구 능력에 더 큰 영향을 주는 경향도 나타났다.</p>

<p><a href="https://www.anthropic.com/research/claude-code-expertise">How Claude Code is used in practice</a></p>

<p>앞으로 개발 과정에서 사람과 AI의 역할은 다음과 같이 나뉠 가능성이 높다고 생각한다.</p>

<table>
  <thead>
    <tr>
      <th>개발 단계</th>
      <th>AI가 잘하는 영역</th>
      <th>사람이 책임질 영역</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>요구사항</td>
      <td>요구사항 분해, 초안 작성</td>
      <td>진짜 문제가 무엇인지 판단</td>
    </tr>
    <tr>
      <td>설계</td>
      <td>익숙한 아키텍처 제안</td>
      <td>비용, 보안, 확장성, 조직 상황의 절충</td>
    </tr>
    <tr>
      <td>구현</td>
      <td>코드 생성, 리팩터링, 문서화</td>
      <td>시스템 규칙과 일치하는지 확인</td>
    </tr>
    <tr>
      <td>테스트</td>
      <td>단위 테스트 생성, 일반적인 엣지 케이스 탐색</td>
      <td>어떤 실패를 반드시 검증해야 하는지 결정</td>
    </tr>
    <tr>
      <td>코드 리뷰</td>
      <td>문법 오류, 반복 패턴, 일반적인 버그 탐지</td>
      <td>요구사항 불일치, 기술 부채, 운영 위험 판단</td>
    </tr>
    <tr>
      <td>배포</td>
      <td>CI/CD 설정, 명령 실행</td>
      <td>권한, 롤백, 장애 영향 범위 결정</td>
    </tr>
    <tr>
      <td>운영</td>
      <td>로그 요약, 원인 후보 분석</td>
      <td>장애 발생 시 최종 판단과 책임</td>
    </tr>
    <tr>
      <td>제품 결정</td>
      <td>여러 가지 대안 생성</td>
      <td>고객, 사업, 현장의 우선순위 결정</td>
    </tr>
  </tbody>
</table>

<p>결국 개발자의 역할은 단순히 코드를 작성하는 사람에서 다음과 같은 역할로 이동할 가능성이 높다.</p>

<blockquote>
  <p><strong>문제 정의자 + 시스템 설계자 + 검증 책임자 + 운영 책임자</strong></p>
</blockquote>

<p>코드를 직접 몇 줄 작성했는지가 중요했던 시대에서, 전체 시스템을 어떤 방향으로 움직이게 했는지가 중요해지는 시대가 오고 있다고 생각한다.</p>

<hr />

<h1 id="3-주니어는-시니어처럼-일해야-하는가">3. 주니어는 시니어처럼 일해야 하는가?</h1>

<h2 id="31-생산량은-시니어와-비슷해질-수-있다">3.1 생산량은 시니어와 비슷해질 수 있다</h2>

<p>AI를 잘 사용하는 주니어 개발자는 과거보다 훨씬 빠르게 많은 작업을 수행할 수 있다.</p>

<p>예를 들면 다음과 같은 작업이다.</p>

<ul>
  <li>여러 파일에 걸친 기능 구현</li>
  <li>테스트 코드 구현</li>
  <li>API 연결</li>
  <li>Docker 설정</li>
  <li>CI 환경 구성</li>
  <li>문서와 README 작성</li>
  <li>기존 코드 분석</li>
  <li>리팩터링</li>
  <li>오류 분석과 수정</li>
</ul>

<p>과거에는 여러 개발자가 밤을 새워가며 만들던 프로토타입을 이제는 몇 시간 안에 만들어낼 수도 있다.</p>

<p>따라서 단순한 코드 생산량만 본다면 주니어와 시니어의 차이는 이전보다 훨씬 작아질 가능성이 있다.</p>

<p>하지만 여기서 중요한 문제가 하나 있다.</p>

<blockquote>
  <p><strong>생산량이 비슷해졌다고 해서 판단 능력까지 같아진 것은 아니다.</strong></p>
</blockquote>

<hr />

<h2 id="32-개발자의-경험은-단기간에-생기지-않는다">3.2 개발자의 경험은 단기간에 생기지 않는다</h2>

<p>코드를 빠르게 작성하는 것이 시니어 개발자의 가장 중요한 역할일까?</p>

<p>나는 그렇지 않다고 생각한다.</p>

<p>시니어 개발자는 직접 많은 코드를 작성하는 것보다 팀이 올바른 문제를 풀고 있는지 판단하고, 프로젝트가 잘못된 방향으로 흘러가지 않도록 관리하는 역할이 더 중요하다.</p>

<p>다음과 같은 문제를 판단해야 한다.</p>

<ul>
  <li>애초에 문제 정의가 잘못된 것은 아닌가?</li>
  <li>이 변경이 다른 시스템에 영향을 주는 것은 아닌가?</li>
  <li>테스트는 통과했지만 데이터 누수가 발생한 것은 아닌가?</li>
  <li>동시성 문제나 보안 취약점이 존재하는 것은 아닌가?</li>
  <li>장애가 발생하면 어디까지 롤백해야 하는가?</li>
  <li>지금 빠르게 만드는 것과 장기적인 유지보수 중 무엇을 우선해야 하는가?</li>
  <li>이 시스템을 최종적으로 누가 운영하고 책임질 것인가?</li>
</ul>

<p>예를 들어 AI를 이용해 풍력발전량 예측 모델을 만든다고 가정해보자.</p>

<p>AI에게 데이터를 전달하고 모델을 만들어달라고 요청하면 모델 자체는 상당히 빠르게 만들 수 있을 것이다.</p>

<p>하지만 다음과 같은 문제는 여전히 사람이 판단해야 한다.</p>

<ul>
  <li>미래 정보가 학습 데이터에 포함되지 않았는가?</li>
  <li>시간 순서를 무시한 랜덤 분할을 사용하지 않았는가?</li>
  <li>기상 예보 시점과 실제 발전량 시점이 정확하게 정렬됐는가?</li>
  <li>결측 예보가 발생했을 때 모델이 어떻게 동작하는가?</li>
  <li>평균 성능은 좋지만 특정 기상 조건에서 크게 실패하지 않는가?</li>
  <li>모델 성능의 향상이 실제 운영 가치로 연결되는가?</li>
</ul>

<p>이러한 문제를 판단하는 능력은 단순히 코드를 빠르게 작성한다고 해서 얻어지는 것이 아니다.</p>

<p>그래서 나는 다음과 같이 생각한다.</p>

<blockquote>
  <p><strong>AI는 주니어에게 시니어처럼 보이는 산출물을 만들게 해주지만, 시니어 수준의 판단과 책임까지 만들어주지는 않는다.</strong></p>
</blockquote>

<p>오히려 AI 시대에는 잘못된 판단을 이전보다 훨씬 빠르고 크게 실행할 수 있다.</p>

<p>기초가 부족한 사람이 AI를 사용하면 잘못된 방향으로 매우 빠르게 갈 수도 있다는 의미다.</p>

<p>DORA 연구에서도 AI가 조직과 개발자가 기존에 가지고 있던 강점과 약점을 증폭시키는 역할을 한다고 설명한다.</p>

<table>
  <tbody>
    <tr>
      <td>[DORA</td>
      <td>State of AI-assisted Software Development 2025](https://dora.dev/dora-report-2025/)</td>
    </tr>
  </tbody>
</table>

<p>METR 연구에서는 2025년 초 숙련된 오픈소스 개발자가 AI를 사용했을 때 오히려 작업 시간이 19% 증가하는 결과도 나타났다.</p>

<p>이후 더 발전된 모델에서는 생산성 향상의 신호가 나타났지만, 측정 과정의 편향 때문에 정확한 증가폭은 확정하기 어렵다고 설명했다.</p>

<p>중요한 것은 단순하다.</p>

<blockquote>
  <p><strong>AI를 사용한다고 해서 무조건 생산성이 높아지는 것은 아니다.</strong></p>
</blockquote>

<p>오히려 AI를 제대로 활용하지 못하면 사용하지 않는 것보다 못할 수도 있다.</p>

<hr />

<h1 id="4-취업-시장은-어떻게-바뀔까">4. 취업 시장은 어떻게 바뀔까?</h1>

<p>앞으로 가장 먼저 압박받는 사람은 다음과 같은 개발자일 가능성이 높다고 생각한다.</p>

<blockquote>
  <p><strong>정해진 요구사항을 받아서 일반적인 코드를 작성하는 사람</strong></p>
</blockquote>

<p>CRUD 구현, 단순한 UI 조립, 보일러플레이트 작성, 반복적인 테스트 생성처럼 요구사항이 명확한 작업은 AI 에이전트가 상당히 잘 수행한다.</p>

<p>이러한 업무만 수행하는 개발자의 가치는 앞으로 빠르게 낮아질 가능성이 있다.</p>

<p>반대로 다음과 같은 사람의 가치는 더욱 높아질 것이다.</p>

<blockquote>
  <p><strong>불명확한 현실 문제를 구조화하고, 여러 기술을 연결해서 실제로 운영 가능한 결과를 만드는 사람</strong></p>
</blockquote>

<p>미국 노동통계국 전망에서도 단순히 코드를 작성하는 <code class="language-plaintext highlighter-rouge">Computer Programmer</code> 직군은 2024년부터 2034년까지 6% 감소할 것으로 예상하는 반면, 소프트웨어 개발자, QA, 테스터 직군은 15% 성장할 것으로 전망한다.</p>

<p>AI, IoT, 로봇, 자동화 시스템 확대가 이러한 개발자 수요를 뒷받침하는 주요 원인으로 제시되고 있다.</p>

<p>미국 기준이기 때문에 한국 시장과 완전히 동일하게 적용할 수는 없지만, <strong>코드를 단순히 작성하는 사람과 시스템 전체를 만드는 사람의 차이가 커지고 있다는 방향은 중요하다.</strong></p>

<p><a href="https://www.bls.gov/ooh/computer-and-information-technology/software-developers.htm">Bureau of Labor Statistics</a></p>

<p>세계경제포럼 역시 AI, 머신러닝 전문가뿐만 아니라 소프트웨어와 애플리케이션 개발자를 앞으로 빠르게 성장할 직군으로 분류하고 있다.</p>

<p><a href="https://www.weforum.org/publications/the-future-of-jobs-report-2025/in-full/2-jobs-outlook/">World Economic Forum</a></p>

<p>따라서 개발자 취업 자체가 완전히 사라진다기보다 개발자 시장 내부에서 양극화가 더 심해질 가능성이 높다고 생각한다.</p>

<table>
  <thead>
    <tr>
      <th>약해지는 포지션</th>
      <th>강해지는 포지션</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>단순 코드 구현자</td>
      <td>시스템 전체를 이해하는 개발자</td>
    </tr>
    <tr>
      <td>프롬프트만 입력하는 사람</td>
      <td>결과를 검증하고 수정하는 사람</td>
    </tr>
    <tr>
      <td>모델 학습만 해본 사람</td>
      <td>데이터부터 운영까지 연결하는 사람</td>
    </tr>
    <tr>
      <td>기술 이름을 많이 아는 사람</td>
      <td>실패 원인과 기술적 절충을 설명하는 사람</td>
    </tr>
    <tr>
      <td>범용적인 주니어 개발자</td>
      <td>특정 산업과 도메인에 강한 엔지니어</td>
    </tr>
    <tr>
      <td>코드 양으로 평가받는 사람</td>
      <td>사업과 운영 결과를 책임지는 사람</td>
    </tr>
  </tbody>
</table>

<hr />

<h1 id="5-앞으로-준비해야-할-역량은-무엇인가">5. 앞으로 준비해야 할 역량은 무엇인가?</h1>

<h2 id="51-코드를-작성하는-능력보다-코드를-판단하는-능력">5.1 코드를 작성하는 능력보다 코드를 판단하는 능력</h2>

<p>AI 시대라고 해서 개발 기초가 필요 없어지는 것은 아니다.</p>

<p>오히려 AI가 코드를 작성하기 때문에 사람이 코드를 판단하는 능력은 더욱 중요해질 수 있다.</p>

<p>AI가 작성한 코드를 보고 최소한 다음 질문에는 답할 수 있어야 한다.</p>

<ul>
  <li>이 코드의 입력과 출력은 무엇인가?</li>
  <li>데이터가 어떤 경로로 이동하는가?</li>
  <li>어디에서 예외가 발생할 수 있는가?</li>
  <li>시간복잡도와 메모리 사용량은 어느 정도인가?</li>
  <li>동시성, 트랜잭션, 보안 문제가 발생할 가능성은 없는가?</li>
  <li>테스트가 무엇을 보장하고 무엇을 보장하지 않는가?</li>
  <li>다른 구현과 비교했을 때 왜 이 구조를 선택했는가?</li>
</ul>

<p>이를 위해서는 기본적인 개발 지식이 여전히 중요하다.</p>

<p><strong>Python, SQL, Linux, Git, Docker, HTTP, REST API, 데이터베이스, 프로세스, 스레드, 비동기 처리, 테스트, 로깅, 패키지 구조</strong> 정도는 AI 없이도 전체적인 구조와 작동 원리를 설명할 수 있어야 한다.</p>

<p>모든 문법을 외울 필요는 없다.
(애초에 외워본 적도 없다 ㅋㅋ)</p>

<p>대신 다음 능력을 가져야 한다.</p>

<blockquote>
  <p><strong>코드를 읽고 → 실행 흐름을 예측하고 → 오류 원인을 좁히고 → 수정 결과를 검증하는 능력</strong></p>
</blockquote>

<p>앞으로 개발자의 중요한 역량은 코드를 얼마나 빨리 타이핑하는지가 아니라, 코드가 올바르게 작동하고 있는지를 얼마나 정확하게 판단할 수 있는지가 될 가능성이 높다.</p>

<hr />

<h1 id="6-ai에게-코드를-요청하는-방식도-바뀌어야-한다">6. AI에게 코드를 요청하는 방식도 바뀌어야 한다</h1>

<p>결론적으로 AI 사용을 피해갈 수는 없다고 생각한다.</p>

<p>그렇다면 AI를 사용하지 않는 사람이 되려고 하기보다, 다른 사람보다 AI를 더 잘 활용하는 사람이 되는 것이 현실적인 선택이다. (남들도 쓰고 있다면, 더 잘 쓰는 사람이 될 수 밖에 없는 것이다.)</p>

<p>그런데 AI를 잘 사용한다는 것은 단순히 다음처럼 요청하는 것이 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>이 기능 만들어줘.
</code></pre></div></div>

<p>앞으로는 AI가 실패하기 어려운 구조를 사람이 먼저 설계해야 한다.</p>

<p>예를 들면 다음과 같은 내용을 먼저 정의하는 방식이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>목표

입력 데이터 형식

출력 형식

수용 조건

예외 처리

성능 요구사항

보안과 권한 조건

테스트 케이스

수정 가능한 파일 범위

수정하면 안 되는 기존 동작
</code></pre></div></div>

<p>즉, 앞으로 중요해지는 능력은 단순한 프롬프트 작성 능력보다 <strong>문제를 명확한 Specification으로 만드는 능력</strong>이라고 생각한다.</p>

<p>사람이 먼저 문제를 구조화한다.</p>

<p>그 구조를 AI에게 전달한다.</p>

<p>AI가 코드를 작성한다.</p>

<p>그 이후 사람이 결과가 처음 정의한 목표에 도달했는지 검증한다.</p>

<p>개발 과정이 점점 다음과 같은 구조로 변화할 가능성이 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>문제 정의
    ↓
요구사항 구조화
    ↓
AI에게 작업 전달
    ↓
코드 생성
    ↓
테스트
    ↓
결과 검증
    ↓
수정
    ↓
운영
</code></pre></div></div>

<p>결국 중요한 것은 AI에게 무엇을 입력하느냐가 아니라, <strong>AI가 만든 결과를 어떤 기준으로 판단하느냐</strong>다.</p>

<hr />

<h1 id="7-구현보다-운영이-중요해질-것이다">7. 구현보다 운영이 중요해질 것이다</h1>

<p>AI가 코드를 대량으로 생산하기 시작하면 자연스럽게 개발 과정의 병목도 바뀌게 된다.</p>

<p>과거에는 구현 자체에 많은 시간이 필요했다.</p>

<p>하지만 구현 속도가 매우 빨라지면 그다음 문제는 시스템을 얼마나 안정적으로 운영할 수 있는지가 된다.</p>

<p>예를 들어 머신러닝 프로젝트라면 앞으로는 단순히 모델 하나를 만드는 것보다 다음과 같은 전체 파이프라인을 이해하는 능력이 중요해질 것이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>데이터 수집
    ↓
Schema Validation
    ↓
Data Quality Check
    ↓
Feature Pipeline
    ↓
Model Training
    ↓
Evaluation
    ↓
Model Registry
    ↓
Batch/API Serving
    ↓
Logging &amp; Monitoring
    ↓
Drift Detection
    ↓
Rollback
</code></pre></div></div>

<p>모델 코드는 AI가 몇 분 만에 작성할 수 있다.</p>

<p>하지만 잘못된 데이터가 들어왔을 때 어떻게 처리할 것인지, 모델 성능이 떨어졌다는 것을 어떻게 감지할 것인지, 장애가 발생했을 때 어느 버전으로 돌아갈 것인지 결정하는 문제는 여전히 남는다.</p>

<p>그래서 앞으로는 코드 작성 자체에는 조금 힘을 빼고, <strong>판단 과정과 결과를 보여주는 데 더 많은 힘을 써야 한다고 생각한다.</strong></p>

<hr />

<h1 id="8-포트폴리오를-보여주는-방식도-달라져야-한다">8. 포트폴리오를 보여주는 방식도 달라져야 한다</h1>

<p>앞으로는 단순히 어떤 기술을 사용했다는 설명만으로는 경쟁력이 부족할 수 있다.</p>

<p>예를 들어 다음과 같은 설명이 있다고 하자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PyTorch를 사용하여 YOLO 모델을 구현했다.
</code></pre></div></div>

<p>이 문장은 사용한 기술은 알려주지만, 어떤 문제를 해결했고 어떤 판단을 했는지는 보여주지 못한다.</p>

<p>대신 다음과 같이 표현할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>456장의 제한된 데이터 환경에서 라벨 품질을 분석하고
Mask Refinement Pipeline을 설계했다.

기존 YOLO 결과 대비 IoU를 0.82에서 0.94로 개선했고,
모델이 실패하는 사례를 유형별로 분류해 후속 개선 방향을 도출했다.
</code></pre></div></div>

<p>두 설명은 같은 프로젝트를 이야기하고 있지만 전달되는 정보의 수준은 완전히 다르다.</p>

<p>앞으로는 단순히 다음과 같은 이야기를 하는 것보다,</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>어떤 기술을 사용했다.
</code></pre></div></div>

<p>다음과 같은 내용을 설명할 수 있어야 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>어떤 문제가 있었는가?

왜 이 방법을 선택했는가?

다른 방법은 무엇이 있었는가?

어떤 기준으로 결과를 검증했는가?

어떤 실패가 발생했는가?

어떻게 개선했는가?

최종적으로 어떤 결과가 나왔는가?
</code></pre></div></div>

<p>AI 시대에는 코드 자체가 점점 저렴해질 것이다.</p>

<p>반대로 <strong>올바른 판단을 내린 과정과 그 결과를 설명할 수 있는 능력은 더욱 비싸질 가능성이 높다.</strong></p>

<hr />

<h1 id="최종-판단">최종 판단</h1>

<p>2027년에는 AI가 코드 작성뿐만 아니라 테스트, 문서화, 리팩터링, 1차 코드 리뷰까지 상당 부분 수행할 가능성이 높다고 생각한다.</p>

<p>하지만 그 결과로 개발자가 사라지는 것은 아닐 것이라 생각한다.</p>

<p>대신 <strong>사람에게 요구되는 책임 수준이 올라갈 것이다.</strong></p>

<p>과거의 주니어 개발자는 정해진 기능을 구현하면서 경험을 쌓는 경우가 많았다.</p>

<p>앞으로의 주니어 개발자는 AI를 이용해 훨씬 빠르게 많은 결과물을 만들 수 있는 대신, 그 결과물이 올바른지 판단해야 하는 책임까지 빠르게 요구받을 수 있다.</p>

<p>결국 앞으로 필요한 사람은 다음과 같은 사람이라고 생각한다.</p>

<blockquote>
  <p>기초를 이해하고,</p>

  <p>도메인을 알고,</p>

  <p>AI에게 일을 정확하게 지시하고,</p>

  <p>결과를 검증하고,</p>

  <p>시스템 전체를 설명할 수 있는 사람이다.</p>
</blockquote>

<p>그렇다면 실제로 경쟁해야 할 상대는 누구일까?</p>

<p>나는 AI 자체가 경쟁 상대는 아니라고 생각한다.</p>

<blockquote>
  <p><strong>AI가 만든 코드를 그대로 제출하는 다른 지원자가 경쟁 상대다.</strong></p>
</blockquote>

<p>앞으로 중요한 것은 AI를 사용했는지 여부가 아니다.</p>

<p>대부분의 개발자가 AI를 사용할 것이기 때문이다.</p>

<p>중요한 것은 <strong>AI를 사용해서 어떤 문제를 해결했고, 그 결과를 얼마나 정확하게 검증했으며, 최종적으로 어떤 가치를 만들어냈는가</strong>다.</p>

<p>그래서 이제는 자신을 단순히 코드를 많이 작성하는 개발자로 정의하기보다 다음과 같은 엔지니어로 만드는 것이 중요하다고 생각한다.</p>

<blockquote>
  <p><strong>AI와 여러 기술을 이용해 실제 문제를 끝까지 해결하고, 그 결과에 책임질 수 있는 엔지니어다.</strong></p>
</blockquote>

<p>AI 시대에는 코드를 작성하는 능력이 사라지는 것이 아니다.</p>

<p>다만 코드 작성 능력만으로 개발자의 가치를 설명하기는 점점 어려워질 것이다.</p>

<p>앞으로 개발자의 경쟁력은 <strong>무엇을 만들었는가보다 왜 그렇게 만들었는지 설명할 수 있는 능력</strong>에서 결정될 가능성이 높다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="AI" /><category term="Development" /><category term="AI" /><category term="Software Engineering" /><category term="Coding Agent" /><category term="Developer" /><category term="Career" /><summary type="html"><![CDATA[AI 시대에 개발자의 역할은?]]></summary></entry><entry><title type="html">MLops</title><link href="http://localhost:4000/mlops/2026/08/07/Devops_1/" rel="alternate" type="text/html" title="MLops" /><published>2026-08-07T09:00:00+09:00</published><updated>2026-08-07T09:00:00+09:00</updated><id>http://localhost:4000/mlops/2026/08/07/Devops_1</id><content type="html" xml:base="http://localhost:4000/mlops/2026/08/07/Devops_1/"><![CDATA[]]></content><author><name>Min Hyeong Lee</name></author><category term="MlOps" /><category term="MlOps" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Devops_기초_2편</title><link href="http://localhost:4000/devops/2026/08/07/Devops_2/" rel="alternate" type="text/html" title="Devops_기초_2편" /><published>2026-08-07T09:00:00+09:00</published><updated>2026-08-07T09:00:00+09:00</updated><id>http://localhost:4000/devops/2026/08/07/Devops_2</id><content type="html" xml:base="http://localhost:4000/devops/2026/08/07/Devops_2/"><![CDATA[<p>자 이어서! 다시 DevOps에 관해서 공부해보자</p>

<h1 id="docker">Docker</h1>

<p>일반적으로 프로그램은 아래와 같이 작동한다</p>

<p>Frontend
Backend
Database
Redis</p>

<h1 id="docker-compose">Docker Compose</h1>

<p>Docker Compose를 사용하면 이를 한 번에 실행할 수 있다.</p>

<p>docker compose up</p>

<p>예</p>

<p>Vue</p>

<p>-&gt; FastAPI 
-&gt; PostgerSQL</p>

<p>를 하나의 개발환경으로 묶을 수있다</p>

<p>예:</p>

<p>Vue
  ↓
FastAPI
  ↓
PostgreSQL</p>

<h1 id="cd---continuous-delivery--deployment">CD - Continuous Delivery / Deployment</h1>

<p>git push
   ↓
CI
   ↓
Ruff
pytest
build
   ↓
CD
   ↓
Server Deployment</p>

<p>즉, 테스트가 성공하면 자동으로 배포한다.</p>

<p>main branch push
       ↓
GitHub Actions
       ↓
pytest PASS
       ↓
Docker Image Build
       ↓
Server Deploy</p>

<p>사람이 서버에 접속해서</p>

<p>git pull
pyhthon app.py</p>

<p>할 필요가 줄어든다.</p>

<h1 id="cloud">Cloud</h1>

<p>Docker까지 이해했다면 Cloud를 배우면 된다.</p>

<p>처음부터 AWS 다 외울 필요는 없다 <del>나중에는 외워야 해?</del></p>

<p>Compute</p>

<p>EC2</p>

<p>Storage</p>

<p>S3</p>

<p>Database</p>

<p>RDS</p>

<p>관리형 Database</p>

<p>Network</p>

<p>VPC
Security Group
Load Balancer</p>

<p>DNS</p>

<p>Route 53</p>

<h1 id="reverse-proxy---nginx">Reverse Proxy - Nginx</h1>

<p>실제 서버에서는 Python 애플리케이션을 인터넷에 바로 공개하지 않는다.</p>

<p>보통</p>

<p>Internet
   ↓
Nginx
   ↓
FastAPI</p>

<p>형태로 사용한다.</p>

<p>Nginx는 대표적으로</p>

<p>Reverse Proxy
HTTPS
Load Balancing
Static File</p>

<h1 id="로그--모니터링">로그 &amp; 모니터링</h1>

<p>운영 중 문제가 발생할 수 있다.. 이를 해결하려면 특정 지표들을 확인해야 한다.</p>

<p>CPU 사용량
Memory 사용량
Request 수
Response Time
Error Rate
Logs</p>

<p>이를 Observability 라고 한다.</p>

<p>대표 개념</p>

<p>Logs 
Metrics 
Traces</p>

<p>도구 예</p>

<p>Prometheus
Grafana
OpenTelemetry</p>

<p>처음에는 Logging부터 시작하면 된다.</p>

<h1 id="infrastructure-as-code--terraform">Infrastructure as Code — Terraform</h1>

<p>서버를 사림이 직접 클릭해서 만드는 것도 자동화 할 수 있다.</p>

<p>EC2 생성
↓
Security Group 생성
↓
Network 설정
↓
Database 생성</p>

<p>이것도 코드로 자동화 가능하다</p>

<p>Terraform</p>

<p><strong>IaC(Infrastructure as Code)</strong> 라고 한다.</p>

<p>재현 가능
버전 관리 가능
자동 생성 가능</p>

<h1 id="kubernetes">Kubernetes</h1>

<p>Kubernetes는 가장 나중에 공부해도 된다.</p>

<p>Docker Container가 수십 개, 수백 개로 늘어나면 관리가 어려워진다.</p>

<p>Backend Container × 20
Worker × 30
Redis
Database
AI Server</p>

<p>대표 개념</p>

<p>Pod
Deployment
Service
Ingress
ConfigMap
Secret
Namespace</p>

<h1 id="정리--devops-전체-흐름">정리 : DevOps 전체 흐름</h1>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>       Developer
           │
           ▼
          Git
           │
           ▼
        GitHub
           │
           ▼
    GitHub Actions CI
      │          │
    Ruff       pytest
      │          │
      └────┬─────┘
           ▼
      Docker Build
           │
           ▼
         CD
           │
           ▼
    Cloud / Server
           │
           ▼
         Nginx
           │
           ▼
      Application
           │
           ▼
    Logs / Monitoring
</code></pre></div></div>

<h1 id="팁">팁</h1>

<p>DevOps를 배우는 가장 좋은 방법은 API 프로젝트를 하나 배포해보는 것</p>

<p>FastAPI
   │
   ├── Pydantic
   ├── pytest
   └── Ruff
        ↓
      GitHub
        ↓
 GitHub Actions
        ↓
      Docker
        ↓
     AWS EC2
        ↓
      Nginx
        ↓
     HTTPS API
        ↓
Logging / Monitoring</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="DevOps" /><summary type="html"><![CDATA[자 이어서! 다시 DevOps에 관해서 공부해보자]]></summary></entry><entry><title type="html">Devops_기초_1편</title><link href="http://localhost:4000/devops/2026/08/07/Devops_1/" rel="alternate" type="text/html" title="Devops_기초_1편" /><published>2026-08-07T09:00:00+09:00</published><updated>2026-08-07T09:00:00+09:00</updated><id>http://localhost:4000/devops/2026/08/07/Devops_1</id><content type="html" xml:base="http://localhost:4000/devops/2026/08/07/Devops_1/"><![CDATA[<hr />
<p>layout: post
title: “DevOps 기초 1편”
date: 2026-08-07 09:00:00 +0900
categories: [DevOps]
tags: [DevOps]
—</p>

<h1 id="devops-기초">DevOps 기초</h1>

<h2 id="1-devops란">1. DevOps란?</h2>

<p>DevOps는 단순히 Docker나 Kubernetes 같은 기술을 사용하는 것을 의미하지 않는다.</p>

<p><strong>Development + Operations</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>코드 작성
   ↓
테스트
   ↓
빌드
   ↓
배포
   ↓
운영
   ↓
모니터링
   ↓
문제 수정
   ↓
다시 배포
</code></pre></div></div>

<p>이 전체 과정을 <strong>빠르고 안정적으로 만드는 개발 및 운영 방식</strong>이라고 볼 수 있다.</p>

<p>DevOps의 핵심은 <strong>자동화와 재현성</strong>이다.</p>

<p>예를 들어,</p>

<blockquote>
  <p>“내 컴퓨터에서는 실행되는데 서버에서는 실행되지 않는다.”</p>
</blockquote>

<p>라는 상황이 반복된다면 안정적인 개발 및 운영 환경이라고 보기 어렵다.</p>

<p>DevOps에서는 가능한 한</p>

<ul>
  <li>같은 실행 환경</li>
  <li>같은 테스트 과정</li>
  <li>같은 배포 방법</li>
  <li>같은 설정 관리 방식</li>
</ul>

<p>을 사용하도록 만들어 이러한 문제를 줄인다.</p>

<hr />

<h2 id="2-가장-먼저-알아야-할-것-linux">2. 가장 먼저 알아야 할 것: Linux</h2>

<p>DevOps에서 사용하는 서버 환경은 Linux 기반인 경우가 많다.</p>

<p>따라서 Docker나 AWS를 배우기 전에 기본적인 Linux 명령어와 터미널 사용법을 익혀두는 것이 좋다.</p>

<p><del>참 쉽죠?</del></p>

<p>기본적으로 다음 정도는 알아두자.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">pwd
ls
cd
mkdir
cp
mv
rm

cat
</span>less
<span class="nb">grep

</span>ps
top
<span class="nb">kill

chmod
chown

</span>curl

ssh
</code></pre></div></div>

<p>예를 들어 현재 실행 중인 프로세스를 확인하려면 다음과 같이 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ps aux
</code></pre></div></div>

<p>PID가 <code class="language-plaintext highlighter-rouge">1234</code>인 프로세스를 종료하려면 다음과 같이 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">kill </span>1234
</code></pre></div></div>

<p>서버가 정상적으로 HTTP 응답을 보내는지 확인할 때는 <code class="language-plaintext highlighter-rouge">curl</code>을 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:8000
</code></pre></div></div>

<h3 id="왜-필요한가">왜 필요한가?</h3>

<p>실제 서버에서 문제가 발생했을 때 GUI 환경이 없는 경우가 많다.</p>

<p>결국 터미널에서 직접</p>

<ul>
  <li>프로세스가 실행되고 있는가?</li>
  <li>포트가 열려 있는가?</li>
  <li>파일 권한은 정상인가?</li>
  <li>서버가 요청에 응답하는가?</li>
  <li>로그에는 어떤 오류가 기록되었는가?</li>
</ul>

<p>등을 확인해야 한다.</p>

<hr />

<h2 id="3-shell과-환경변수">3. Shell과 환경변수</h2>

<p>서버에서는 명령어를 반복해서 실행해야 하는 일이 많다.</p>

<p>이때 Bash와 같은 <strong>Shell</strong>을 사용한다.</p>

<p>예를 들어 Python 프로그램을 실행하려면 다음과 같이 입력한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python app.py
</code></pre></div></div>

<p>환경변수는 다음과 같이 설정할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">API_KEY</span><span class="o">=</span><span class="s2">"secret"</span>
</code></pre></div></div>

<p>Python에서는 <code class="language-plaintext highlighter-rouge">os.getenv()</code>를 이용해 환경변수를 가져올 수 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">os</span>

<span class="n">api_key</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">getenv</span><span class="p">(</span><span class="s">"API_KEY"</span><span class="p">)</span>
</code></pre></div></div>

<h3 id="왜-필요한가-1">왜 필요한가?</h3>

<p>API Key나 DB Password 같은 민감한 정보를 코드에 직접 작성하면 안 된다.</p>

<p>예를 들어 다음과 같이 작성하는 것은 좋지 않다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">API_KEY</span> <span class="o">=</span> <span class="s">"abcdef123456"</span>
</code></pre></div></div>

<p>대신</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>코드
+
환경변수
</code></pre></div></div>

<p>를 분리해서 관리한다.</p>

<p>이 개념은 이후 배우게 될</p>

<ul>
  <li>Docker</li>
  <li>GitHub Actions</li>
  <li>AWS</li>
  <li>Kubernetes</li>
</ul>

<p>에서도 계속 등장한다.</p>

<hr />

<h2 id="4-git과-github">4. Git과 GitHub</h2>

<p>DevOps 자동화는 대부분 Git을 기반으로 시작한다.</p>

<p>기본적인 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>코드 수정
   ↓
git add
   ↓
git commit
   ↓
git push
   ↓
GitHub
</code></pre></div></div>

<p>필수적으로 알아두어야 할 개념은 다음과 같다.</p>

<ul>
  <li>Repository</li>
  <li>Commit</li>
  <li>Branch</li>
  <li>Merge</li>
  <li>Pull Request</li>
  <li>Conflict</li>
  <li><code class="language-plaintext highlighter-rouge">.gitignore</code></li>
</ul>

<p>특히 <code class="language-plaintext highlighter-rouge">.gitignore</code>는 중요하다.</p>

<p>예를 들어 Python 프로젝트에서는 다음과 같은 파일을 Git에서 제외할 수 있다.</p>

<pre><code class="language-gitignore">.env
.venv/
__pycache__/
</code></pre>

<p>API Key가 저장된 <code class="language-plaintext highlighter-rouge">.env</code> 파일이나 로컬 가상환경 같은 파일은 Git 저장소에 올리지 않는 것이 기본이다.</p>

<hr />

<h2 id="5-네트워크-기초">5. 네트워크 기초</h2>

<p>DevOps를 배우면서 의외로 자주 막히는 부분이 네트워크다.</p>

<p>기본적으로 다음 개념은 알아둘 필요가 있다.</p>

<ul>
  <li>IP</li>
  <li>Port</li>
  <li>DNS</li>
  <li>HTTP / HTTPS</li>
  <li>TCP</li>
  <li>localhost</li>
  <li><code class="language-plaintext highlighter-rouge">127.0.0.1</code></li>
  <li><code class="language-plaintext highlighter-rouge">0.0.0.0</code></li>
  <li>Firewall</li>
</ul>

<p>예를 들어 FastAPI 서버를 다음과 같이 실행한다고 하자.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uvicorn main:app <span class="nt">--host</span> 127.0.0.1 <span class="nt">--port</span> 8000
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">127.0.0.1</code>에 바인딩하면 기본적으로 <strong>현재 컴퓨터 내부에서만 접근할 수 있다.</strong></p>

<p>반면</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uvicorn main:app <span class="nt">--host</span> 0.0.0.0 <span class="nt">--port</span> 8000
</code></pre></div></div>

<p>으로 실행하면 서버가 <strong>모든 네트워크 인터페이스에서 들어오는 요청을 받을 수 있도록 바인딩</strong>된다.</p>

<p>다만 실제 외부 접근 가능 여부는 방화벽, 공유기, 클라우드 보안 설정 등에 따라서도 달라진다.</p>

<p>이런 차이를 이해해야 Docker나 Cloud 환경에서 서버를 배포할 때 발생하는 문제를 쉽게 파악할 수 있다.</p>

<hr />

<h2 id="6-패키지와-실행-환경-관리">6. 패키지와 실행 환경 관리</h2>

<p>DevOps에서 중요한 원칙 중 하나는 다음과 같다.</p>

<blockquote>
  <p>내가 아니더라도 다른 사람이 같은 실행 환경을 재현할 수 있어야 한다.</p>
</blockquote>

<p>개발하면서 자주 발생하는 문제가 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"내 컴퓨터에서는 되는데?"
</code></pre></div></div>

<p>실제로는 다음과 같은 차이가 원인일 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Python 3.11
Python 3.12

Pydantic 1.x
Pydantic 2.x

NumPy 버전 차이
</code></pre></div></div>

<p>따라서 프로젝트에서 사용하는 의존성을 기록한다.</p>

<p>대표적인 방법이 <code class="language-plaintext highlighter-rouge">requirements.txt</code>이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fastapi
uvicorn
pydantic
httpx
pytest
ruff
</code></pre></div></div>

<p>예를 들어 다음 명령어로 설치할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
</code></pre></div></div>

<p>최근 Python 프로젝트에서는 <code class="language-plaintext highlighter-rouge">pyproject.toml</code>을 중심으로 프로젝트 설정과 의존성을 관리하는 방식도 많이 사용한다.</p>

<p>결국 중요한 것은</p>

<blockquote>
  <p><strong>누가 실행하더라도 동일한 환경을 최대한 재현할 수 있도록 만드는 것</strong></p>
</blockquote>

<p>이다.</p>

<hr />

<h2 id="7-코드-품질-검사">7. 코드 품질 검사</h2>

<p>여기서 앞에서 배운 <strong>Ruff</strong>가 연결된다.</p>

<p>코드 문제를 검사하려면 다음과 같이 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruff check <span class="nb">.</span>
</code></pre></div></div>

<p>자동으로 수정 가능한 문제를 고치려면 다음과 같이 사용할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruff check <span class="nb">.</span> <span class="nt">--fix</span>
</code></pre></div></div>

<p>코드 포맷을 정리하려면 다음과 같이 실행한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruff format <span class="nb">.</span>
</code></pre></div></div>

<p>DevOps에서는 이러한 코드 품질 검사를 사람이 매번 직접 수행하는 것이 아니라 <strong>자동화된 파이프라인에 포함시키는 것</strong>이 중요하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Developer
   ↓
Ruff
   ↓
문제 없음
   ↓
다음 단계
</code></pre></div></div>

<p>문제가 발견되면 다음 단계로 넘어가지 않도록 만들 수도 있다.</p>

<hr />

<h2 id="8-자동-테스트">8. 자동 테스트</h2>

<p>그다음 연결되는 것이 <code class="language-plaintext highlighter-rouge">pytest</code>이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pytest
</code></pre></div></div>

<p>예를 들어 다음과 같은 함수가 있다고 하자.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">add</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span>
</code></pre></div></div>

<p>테스트 코드는 다음과 같이 작성할 수 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">test_add</span><span class="p">():</span>
    <span class="k">assert</span> <span class="n">add</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="o">==</span> <span class="mi">3</span>
</code></pre></div></div>

<p>코드를 수정할 때마다 사람이 모든 기능을 직접 확인하는 대신 테스트 프로그램이 코드의 동작을 자동으로 검사한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>코드 변경
   ↓
pytest
   ↓
PASS / FAIL
</code></pre></div></div>

<p>기능이 많아질수록 자동 테스트의 중요성은 더욱 커진다.</p>

<hr />

<h2 id="9-ci---continuous-integration">9. CI - Continuous Integration</h2>

<p>여기서 <strong>GitHub Actions</strong>가 등장한다.</p>

<p>CI(Continuous Integration)는 간단하게 말하면</p>

<blockquote>
  <p><strong>코드가 저장소에 올라왔을 때 자동으로 검사하고 검증하는 과정</strong></p>
</blockquote>

<p>이라고 이해할 수 있다.</p>

<p>예를 들어 다음과 같은 Pipeline을 구성할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push
   ↓
GitHub Actions
   ↓
Ruff
   ↓
pytest
   ↓
Build
</code></pre></div></div>

<p>즉 개발자가 코드를 Push하거나 Pull Request를 생성하면 GitHub Actions가 자동으로 코드 품질과 테스트 결과를 검사한다.</p>

<p><code class="language-plaintext highlighter-rouge">.github/workflows/ci.yml</code></p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">CI</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
  <span class="na">pull_request</span><span class="pi">:</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">test</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>

    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v4</span>

      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-python@v5</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">python-version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.11"</span>

      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">pip install -r requirements.txt</span>

      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">ruff check .</span>

      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">pytest</span>
</code></pre></div></div>

<p>이렇게 설정해두면 코드가 GitHub에 올라올 때마다 자동으로</p>

<ol>
  <li>Repository의 코드를 가져오고</li>
  <li>Python 3.11 환경을 구성하고</li>
  <li>필요한 패키지를 설치하고</li>
  <li>Ruff로 코드 품질을 검사하고</li>
  <li>pytest로 테스트를 수행한다.</li>
</ol>

<p>하나라도 실패하면 CI 역시 실패한다.</p>

<p><del>신기한데?</del></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발자 코드 작성
       ↓
    git push
       ↓
 GitHub Actions
       ↓
 ┌───────────────┐
 │               │
Ruff            pytest
 │               │
 └───────┬───────┘
         ↓
     PASS / FAIL
</code></pre></div></div>

<p>결국 DevOps의 시작은 Docker, Kubernetes가 아니라,</p>

<blockquote>
  <p><strong>코드를 작성하고 → 검사하고 → 테스트하고 → 동일한 방법으로 실행하는 과정을 자동화하는 것</strong></p>
</blockquote>

<p>부터 시작한다</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="DevOps" /><summary type="html"><![CDATA[layout: post title: “DevOps 기초 1편” date: 2026-08-07 09:00:00 +0900 categories: [DevOps] tags: [DevOps] —]]></summary></entry><entry><title type="html">딥러닝 데이터셋 엔지니어링</title><link href="http://localhost:4000/deep_learning/data/2026/08/06/dataset-engineering/" rel="alternate" type="text/html" title="딥러닝 데이터셋 엔지니어링" /><published>2026-08-06T00:00:00+09:00</published><updated>2026-08-06T00:00:00+09:00</updated><id>http://localhost:4000/deep_learning/data/2026/08/06/dataset-engineering</id><content type="html" xml:base="http://localhost:4000/deep_learning/data/2026/08/06/dataset-engineering/"><![CDATA[<p>좋은 데이터셋의 조건
Class Imbalance란 무엇인가
Train과 Validation 데이터 누수를 방지하는 방법
이미지 크기를 통일해야 하는 이유
Normalization과 Standardization
Data Augmentation 기본
데이터 증강을 많이 하면 항상 좋은가
Label Noise가 성능에 미치는 영향
잘못된 라벨을 찾는 방법
Duplicate Image가 평가 결과를 왜곡하는 이유
Stratified Split과 Group Split
실제 환경을 반영한 데이터 분리 전략
Hard Example Mining
Synthetic Data 활용 방법
데이터셋 버전 관리</p>

<p>모델 성능이 안 나올 때 모델부터 바꾸면 안 되는 이유
mAP가 높은데 실제 영상에서는 탐지가 안 되는 이유
데이터 증강이 오히려 성능을 떨어뜨리는 경우
Validation 성능은 좋은데 Test 성능이 낮은 이유</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Deep_Learning" /><category term="Data" /><category term="Deep Learning" /><category term="Dataset Engineering" /><category term="Data Augmentation" /><summary type="html"><![CDATA[데이터 분리, 불균형, 증강, 라벨 노이즈, 중복 데이터와 데이터셋 버전 관리 방법을 정리한다.]]></summary></entry><entry><title type="html">딥러닝 학습 문제 진단과 디버깅</title><link href="http://localhost:4000/deep_learning/debugging/2026/08/06/deep-learning-debugging/" rel="alternate" type="text/html" title="딥러닝 학습 문제 진단과 디버깅" /><published>2026-08-06T00:00:00+09:00</published><updated>2026-08-06T00:00:00+09:00</updated><id>http://localhost:4000/deep_learning/debugging/2026/08/06/deep-learning-debugging</id><content type="html" xml:base="http://localhost:4000/deep_learning/debugging/2026/08/06/deep-learning-debugging/"><![CDATA[<p>Loss가 줄지 않는 원인
Loss가 NaN이 되는 원인
Training Loss만 낮아지는 경우
Validation Loss가 계속 상승하는 경우
Overfitting 진단 방법
Underfitting 진단 방법
Accuracy가 높은데 모델이 쓸모없는 이유
Confusion Matrix 읽는 방법
False Positive와 False Negative 분석
Precision과 Recall 중 무엇을 봐야 하는가
Threshold 조정 방법
Class별 성능 분석
데이터 분포 변화 감지
Gradient가 사라지거나 폭발하는 경우
GPU 메모리 부족 원인
학습 속도가 느린 원인
DataLoader가 병목인 경우
모델이 배경만 학습하는 경우
Shortcut Learning
Domain Shift</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Deep_Learning" /><category term="Debugging" /><category term="Deep Learning" /><category term="Model Debugging" /><category term="Training" /><summary type="html"><![CDATA[Loss, 과적합, 평가 지표, 데이터 분포, Gradient, GPU 메모리 등 딥러닝 학습 문제의 진단 항목을 정리한다.]]></summary></entry></feed>