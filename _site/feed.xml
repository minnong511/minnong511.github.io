<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-08-25T08:08:48+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Minnong’s Study Log</title><subtitle>개발과 AI를 배우고, 실험하고, 이해한 내용을 연결하는 공부 기록</subtitle><author><name>Min Hyeong Lee</name></author><entry><title type="html">OCI: 컨테이너 이미지와 런타임의 공통 표준</title><link href="http://localhost:4000/devops/docker/2026/08/24/oci-container-standard/" rel="alternate" type="text/html" title="OCI: 컨테이너 이미지와 런타임의 공통 표준" /><published>2026-08-24T00:50:00+09:00</published><updated>2026-08-24T00:50:00+09:00</updated><id>http://localhost:4000/devops/docker/2026/08/24/oci-container-standard</id><content type="html" xml:base="http://localhost:4000/devops/docker/2026/08/24/oci-container-standard/"><![CDATA[<h2 id="ociopen-container-initiative란">OCI(Open Container Initiative)란?</h2>

<blockquote>
  <p>OCI는 “컨테이너 이미지와 컨테이너 실행 방식에 대한 국제 공통 규격”</p>
</blockquote>

<p>Docker가 만든 이미지를 Docker에서만 실행하는 게 아니라, containerd, CRI-O, Kubernetes, Podman 같은 다른 생태계에서도 동일하게 사용할 수 있게 해주는 표준</p>

<h1 id="1-oci는-왜-등장했는가">1. OCI는 왜 등장했는가?</h1>

<p>초창기 컨테이너 시장 -&gt; Docker가 거의 표준처럼 쓰임</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Image
    ↓
Docker Engine
    ↓
Container
</code></pre></div></div>

<p>문제는 이러면 Docker라는 특정 회사와 구현체에 생태계가 강하게 종속될 수 있다는 것</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Image
    ↓
Docker에서만 실행 가능

A회사 Image
    ↓
A회사 Runtime에서만 실행 가능

B회사 Image
    ↓
B회사 Runtime에서만 실행 가능
</code></pre></div></div>

<p>따라서 Docker, Google, Red Hat, IBM, Microsoft 등 여러 기업이 참여해서 만든 표준화 프로젝트가 OCI(Open Container Initiative)</p>

<p>이와 같이 모인 목적은</p>

<blockquote>
  <p>컨테이너를 어떤 형식으로 만들고, 어떻게 실행하면 좋을까하는 공통 규격을 만들자.</p>
</blockquote>

<p>라는 일념하에 힘을 합치게 된다.</p>

<h1 id="2-oci는-프로그램이-아니다">2. OCI는 프로그램이 아니다.</h1>

<blockquote>
  <p>OCI는 설계도 / 규격이고 runc 같은 프로그램이 그 규격을 구현</p>
</blockquote>

<p>OCI 자체는 Docker나 containerd처럼 실행되는 프로그램이 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker      → 프로그램
containerd  → 프로그램
runc        → 프로그램

OCI         → 표준 Specification
</code></pre></div></div>

<p>비유를 해보자면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTTP   = 웹 통신 규칙
Chrome = HTTP를 사용하는 프로그램

OCI    = 컨테이너 규칙
runc   = OCI 규칙을 구현한 프로그램
</code></pre></div></div>

<h1 id="3-oci의-핵심-3가지-표준">3. OCI의 핵심 3가지 표준</h1>

<p>OCI에서는 크게 세 가지를 정의</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OCI
├── Image Specification
├── Runtime Specification
└── Distribution Specification
</code></pre></div></div>

<p>각각이 약간씩 다르므로 아래서 좀 설명을 해보려 한다.</p>

<h1 id="4-oci-image-specification">4. OCI Image Specification</h1>

<p>컨테이너 이미지를 어떤 구조로 저장할 것인가를 정의</p>

<p>Docker 이미지를 생각하면 된다.</p>

<p>예를 들어</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nginx image
├── Layer 4  nginx 설정
├── Layer 3  nginx 설치
├── Layer 2  라이브러리
└── Layer 1  Ubuntu filesystem
</code></pre></div></div>

<p>OCI Image Spec은 다음과 같은 것을 정의</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image
├── Manifest
├── Config
└── Layers
</code></pre></div></div>

<p>예를 들면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Manifest
    ↓
어떤 Config와 Layer를 사용하는지 기록

Config
    ├── 환경변수
    ├── 실행 명령어
    ├── Working Directory
    └── Architecture

Layers
    ↓
실제 파일 시스템 데이터
</code></pre></div></div>

<p>이런 식으로 규격을 표준화했기 때문에, 덕분에 Docker에서 만든 이미지도 다른 OCI 호환 프로그램에서 사용할 수 있다. (오우? 굉장히 좋다고 할 수 있다.)</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker build
    ↓
OCI Image
    ├── Docker
    ├── Podman
    └── containerd
</code></pre></div></div>

<h1 id="5-oci-runtime-specification">5. OCI Runtime Specification</h1>

<p>Runtime Spec은:</p>

<blockquote>
  <p>“컨테이너를 실제 Linux 프로세스로 어떻게 실행할 것인가?”</p>
</blockquote>

<p>를 정의</p>

<p>예를 들어 컨테이너 실행하려면 이런 정보가 필요하다</p>

<p>어떤 프로그램을 실행할 것인가?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nginx
</code></pre></div></div>

<p>어떤 환경변수를 사용할 것인가?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PORT=80
</code></pre></div></div>

<p>어떤 namespace를 만들 것인가?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PID Namespace
Network Namespace
Mount Namespace
</code></pre></div></div>

<p>어떤 resource 제한을 적용할 것인가?</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CPU
Memory
</code></pre></div></div>

<p>이런 실행 정보를 OCI Runtime Spec에서 규정</p>

<p>대표적인 설정 파일이</p>

<p><code class="language-plaintext highlighter-rouge">config.json</code></p>

<p>개념적으로</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"process"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"args"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"nginx"</span><span class="p">]</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"linux"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"namespaces"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="s2">"..."</span><span class="w">
    </span><span class="p">]</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>이런 정보를 Runtime이 읽는다.</p>

<p>대표적인 OCI Runtime 구현체가</p>

<p><code class="language-plaintext highlighter-rouge">runc</code></p>

<h2 id="runc가-중요한-이유">runc가 중요한 이유</h2>

<p>Docker에서</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run nginx
</code></pre></div></div>

<p>를 실행한다고, Docker가 직접 Linux Namespace와 cgroup을 전부 만드는 구조는 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자
    │
    │ docker run nginx
    ▼
Docker CLI
    │
    ▼
dockerd
    │
    ▼
containerd
    │
    ▼
runc
    │
    ▼
Linux Kernel
    ├── Namespace
    ├── cgroup
    ├── capabilities
    └── filesystem
    │
    ▼
nginx process
</code></pre></div></div>

<p>여기서</p>

<p><code class="language-plaintext highlighter-rouge">runc</code></p>

<p>가 OCI Runtime Specification을 구현한 프로그램이다.</p>

<blockquote>
  <p>Docker가 최종적으로 컨테이너라는 특별한 VM을 만드는 것이 아니라, runc를 통해 Linux Kernel 기능을 이용해서 격리된 프로세스를 만들어낸다.</p>
</blockquote>

<p>결국은</p>

<blockquote>
  <p>“컨테이너의 실체는 결국 Linux에서 실행되는 프로세스다”</p>
</blockquote>

<p>라는 개념을 구현하는 것은 OCI Runtime</p>

<h1 id="6-kubernetes에서도-oci가-사용된다">6. Kubernetes에서도 OCI가 사용된다</h1>

<p>OCI가 중요한 이유는 Docker만의 이야기가 아니기 때문.</p>

<p>Kubernetes에서 사용되기 때문이다.</p>

<p>에를 들어 Kubernetes에서 Pod를 생성</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> nginx.yaml
</code></pre></div></div>

<p>라고 하면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl
    │
    ▼
Kubernetes API Server
    │
    ▼
kubelet
    │
    ▼
CRI
    │
    ▼
containerd
    │
    ▼
OCI Runtime
    │
    ▼
runc
    │
    ▼
Linux Kernel
    │
    ▼
nginx process
</code></pre></div></div>

<p>라고 하면서,</p>

<p>OCI Runtime을 사용하게 된다.</p>

<h1 id="7-cri와-oci는-다른-것">7. CRI와 OCI는 다른 것</h1>

<p>CRI
OCI</p>

<h3 id="cri">CRI</h3>
<ul>
  <li>Container Runtime Interface</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kubernetes
    ↕
containerd / CRI-O
</code></pre></div></div>

<blockquote>
  <p>Kubernetes와 Container Runtime 사이의 API 규격</p>
</blockquote>

<h3 id="oci">OCI</h3>
<ul>
  <li>Open Container Initiative</li>
</ul>

<blockquote>
  <p>컨테이너 이미지와 실제 실행 규격</p>
</blockquote>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kubernetes
    │
    ▼
kubelet
    │ CRI
    ▼
containerd
    │ OCI
    ▼
runc
    │
    ▼
Linux Kernel
</code></pre></div></div>

<h1 id="8-oci가-실제로-어디에-응용">8. OCI가 실제로 어디에 응용?</h1>

<p>OCI 표준 덕분에 다양한 컨테이너 기술이 서로 호환</p>

<p>Docker 
Podman 
containerd 
CRI-0 
Kubernetes 
AWS ECS / EKS 
Google GKE 
Azure AKS 
Github Actions 
CI/CD 시스템</p>

<p>등이 컨테이너 이미지를 다룰 수 있음</p>

<p>예를 들어</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> myapp <span class="nb">.</span>
</code></pre></div></div>

<p>해서 이미지를 만들고 Registry에 올리면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Developer Laptop
    │ docker build
    ▼
OCI Image
    │
    ▼
Container Registry
    ├── Docker
    ├── Kubernetes
    ├── AWS ECS
    ├── GKE
    └── Azure AKS
</code></pre></div></div>

<p>어디에서 실행하든 OCI 규격을 이해하기 때문에 동일한 이미지를 사용할 수 있다.</p>

<h1 id="9-docker-registry에서도-oci가-응용">9. Docker Registry에서도 OCI가 응용</h1>

<p>OCI에는 Distribution Specification도 있다.</p>

<p>이건</p>

<blockquote>
  <p>“컨테이너 이미지를 Registry와 어떻게 주고받을 것인가”</p>
</blockquote>

<p>를 정의</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker pull nginx
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker
    │ Registry API
    ▼
Docker Hub
    ├── Manifest
    ├── Config
    └── Layers
</code></pre></div></div>

<p>그래서 다음과 같은 Registry들이 서로 비슷한 방식으로 OCI 이미지를 저장</p>

<p>Docker Hub</p>

<p>GitHub Container Registry</p>

<p>Amazon ECR</p>

<p>Google Artifact Registry</p>

<p>Azure Container Registry</p>

<p>Harbor</p>

<h1 id="10-oci-때문에-docker가-없어도-된다">10. OCI 때문에 Docker가 없어도 된다.</h1>

<p>OCI 표준이 있기 때문에</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Build
    ↓
OCI Image
    ├── Podman
    ├── containerd
    └── Kubernetes
</code></pre></div></div>

<p>처럼 사용할 수 있다.</p>

<p>이는 Vendor Lock-in을 줄이는 역할을 한다.</p>

<h1 id="결론">결론</h1>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker CLI
    │
    ▼
dockerd
    │
    ▼
containerd
    │ OCI Runtime Specification
    ▼
runc
    │
    ▼
Linux Kernel
    ├── Namespace
    ├── cgroup
    └── capability
    │
    ▼
Container
(= isolated process)
</code></pre></div></div>

<p>이미지는</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile
    │
    ▼
docker build
    │
    ▼
OCI Image
    ├── Manifest
    ├── Config
    └── Layers
    │
    ▼
Container Registry
    │
    ▼
containerd
    │
    ▼
runc
    │
    ▼
Linux Process
</code></pre></div></div>

<p>면접에서 나온다면 …</p>

<p>OCI는 Open Container Initiative로, 컨테이너 이미지 형식과 컨테이너 실행 방식 등을 표준화한 규격입니다. 대표적으로 OCI Image Spec, Runtime Spec, Distribution Spec이 있습니다. Docker나 Kubernetes 생태계에서 서로 다른 Runtime과 Registry가 동일한 컨테이너 이미지를 호환해서 사용할 수 있도록 해줍니다. 대표적인 OCI Runtime 구현체가 runc입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker       = 컨테이너를 사용하는 플랫폼
containerd   = 컨테이너 생명주기 관리자
OCI          = 컨테이너 표준 규격
runc         = OCI Runtime 규격 구현체
Linux Kernel = 실제 격리를 수행하는 주체
Container    = 결국 격리된 Linux Process
</code></pre></div></div>

<p>여기까지 연결하면</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kubernetes
    ↓
CRI
    ↓
containerd
    ↓
OCI
    ↓
runc
    ↓
Linux Kernel
    ↓
Process
</code></pre></div></div>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="Docker" /><category term="OCI" /><category term="Docker" /><category term="Container" /><category term="containerd" /><category term="runc" /><category term="Kubernetes" /><summary type="html"><![CDATA[OCI가 등장한 배경과 Image, Runtime, Distribution Specification의 역할, Docker와 Kubernetes에서 OCI가 사용되는 흐름을 정리한다.]]></summary></entry><entry><title type="html">Nginx 로드 밸런싱과 HTTPS</title><link href="http://localhost:4000/devops/nginx/2026/08/24/nginx-load-balancing-https/" rel="alternate" type="text/html" title="Nginx 로드 밸런싱과 HTTPS" /><published>2026-08-24T00:40:00+09:00</published><updated>2026-08-24T00:40:00+09:00</updated><id>http://localhost:4000/devops/nginx/2026/08/24/nginx-load-balancing-https</id><content type="html" xml:base="http://localhost:4000/devops/nginx/2026/08/24/nginx-load-balancing-https/"><![CDATA[<h2 id="1-로드-밸런싱이란">1. 로드 밸런싱이란?</h2>

<p>로드 밸런싱은 하나의 서버에 요청이 몰리지 않도록 여러 서버에 요청을 나누어 전달하는 방식입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                   ┌─ Spring Boot 1 :8081
Client → Nginx ────┼─ Spring Boot 2 :8082
                   └─ Spring Boot 3 :8083
</code></pre></div></div>

<h2 id="2-upstream-설정">2. <code class="language-plaintext highlighter-rouge">upstream</code> 설정</h2>

<div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">upstream</span> <span class="s">backend_servers</span> <span class="p">{</span>
    <span class="kn">server</span> <span class="nf">127.0.0.1</span><span class="p">:</span><span class="mi">8081</span><span class="p">;</span>
    <span class="kn">server</span> <span class="nf">127.0.0.1</span><span class="p">:</span><span class="mi">8082</span><span class="p">;</span>
    <span class="kn">server</span> <span class="nf">127.0.0.1</span><span class="p">:</span><span class="mi">8083</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">80</span><span class="p">;</span>

    <span class="kn">location</span> <span class="n">/</span> <span class="p">{</span>
        <span class="kn">proxy_pass</span> <span class="s">http://backend_servers</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="3-https와-tls-인증서">3. HTTPS와 TLS 인증서</h2>

<p>HTTPS를 사용하면 클라이언트와 Nginx 사이에서 주고받는 데이터를 암호화할 수 있습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  ↓ HTTPS
Nginx
  ↓ HTTP 또는 HTTPS
Backend Server
</code></pre></div></div>

<h2 id="4-https-설정-구조">4. HTTPS 설정 구조</h2>

<!-- 인증서 경로와 80 포트에서 443 포트로 리다이렉트하는 설정을 정리합니다. -->

<h2 id="5-핵심-정리">5. 핵심 정리</h2>

<ul>
  <li>Nginx는 여러 Backend Server에 요청을 분산할 수 있습니다.</li>
  <li>HTTPS 인증서를 Nginx에서 관리할 수 있습니다.</li>
  <li>Backend Server는 비즈니스 로직에 집중할 수 있습니다.</li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="Nginx" /><category term="Nginx" /><category term="LoadBalancing" /><category term="HTTPS" /><category term="TLS" /><category term="DevOps" /><summary type="html"><![CDATA[Nginx가 요청을 여러 서버로 분산하는 방법과 HTTPS 인증서를 처리하는 구조를 정리한다.]]></summary></entry><entry><title type="html">Nginx 리버스 프록시와 Spring Boot 연결</title><link href="http://localhost:4000/devops/nginx/2026/08/24/nginx-reverse-proxy/" rel="alternate" type="text/html" title="Nginx 리버스 프록시와 Spring Boot 연결" /><published>2026-08-24T00:30:00+09:00</published><updated>2026-08-24T00:30:00+09:00</updated><id>http://localhost:4000/devops/nginx/2026/08/24/nginx-reverse-proxy</id><content type="html" xml:base="http://localhost:4000/devops/nginx/2026/08/24/nginx-reverse-proxy/"><![CDATA[<h2 id="1-리버스-프록시란">1. 리버스 프록시란?</h2>

<p>리버스 프록시는 클라이언트의 요청을 대신 받은 뒤 내부 Backend Server로 전달하는 서버입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  ↓ example.com
Nginx :80
  ↓ proxy_pass
Spring Boot :8080
</code></pre></div></div>

<p>클라이언트는 Spring Boot의 주소와 포트를 직접 알지 않아도 Nginx를 통해 API를 호출할 수 있습니다.</p>

<h2 id="2-nginx와-spring-boot의-역할">2. Nginx와 Spring Boot의 역할</h2>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Nginx</td>
      <td>외부 요청 수신, 정적 파일 제공, 요청 전달</td>
    </tr>
    <tr>
      <td>Spring Boot</td>
      <td>비즈니스 로직 실행, API 응답 생성</td>
    </tr>
  </tbody>
</table>

<h2 id="3-proxy_pass-설정">3. <code class="language-plaintext highlighter-rouge">proxy_pass</code> 설정</h2>

<div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">80</span><span class="p">;</span>
    <span class="kn">server_name</span> <span class="s">example.com</span><span class="p">;</span>

    <span class="kn">location</span> <span class="n">/api/</span> <span class="p">{</span>
        <span class="kn">proxy_pass</span> <span class="s">http://127.0.0.1:8080</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">Host</span> <span class="nv">$host</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Real-IP</span> <span class="nv">$remote_addr</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Forwarded-For</span> <span class="nv">$proxy_add_x_forwarded_for</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Forwarded-Proto</span> <span class="nv">$scheme</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="4-요청-전달-흐름">4. 요청 전달 흐름</h2>

<!-- 실제 요청 URL과 Spring Controller 예제를 사용해서 흐름을 정리합니다. -->

<h2 id="5-핵심-정리">5. 핵심 정리</h2>

<ul>
  <li>사용자는 Nginx의 주소로 요청합니다.</li>
  <li>Nginx는 설정된 Backend Server로 요청을 전달합니다.</li>
  <li>Spring Boot가 생성한 응답은 Nginx를 거쳐 사용자에게 돌아갑니다.</li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="Nginx" /><category term="Nginx" /><category term="ReverseProxy" /><category term="SpringBoot" /><category term="DevOps" /><summary type="html"><![CDATA[리버스 프록시의 의미와 Nginx가 클라이언트 요청을 Spring Boot 서버로 전달하는 과정을 정리한다.]]></summary></entry><entry><title type="html">Nginx 기초와 동작 구조</title><link href="http://localhost:4000/devops/nginx/2026/08/24/nginx-basics/" rel="alternate" type="text/html" title="Nginx 기초와 동작 구조" /><published>2026-08-24T00:20:00+09:00</published><updated>2026-08-24T00:20:00+09:00</updated><id>http://localhost:4000/devops/nginx/2026/08/24/nginx-basics</id><content type="html" xml:base="http://localhost:4000/devops/nginx/2026/08/24/nginx-basics/"><![CDATA[<h2 id="1-nginx란">1. Nginx란?</h2>

<p>Nginx는 클라이언트의 HTTP 요청을 받아 정적 파일을 제공하거나 요청을 다른 서버로 전달하는 Web Server입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  ↓ HTTP Request
Nginx
  ├─ HTML, CSS, JavaScript 제공
  └─ Backend Server로 요청 전달
</code></pre></div></div>

<h2 id="2-nginx를-사용하는-이유">2. Nginx를 사용하는 이유</h2>

<!-- 정적 파일 제공, 동시 요청 처리, 리버스 프록시 등의 내용을 정리합니다. -->

<h2 id="3-nginx의-요청-처리-흐름">3. Nginx의 요청 처리 흐름</h2>

<!-- Client부터 Nginx를 거쳐 응답이 돌아오는 과정을 정리합니다. -->

<h2 id="4-기본-설정-파일-구조">4. 기본 설정 파일 구조</h2>

<div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">80</span><span class="p">;</span>
    <span class="kn">server_name</span> <span class="s">example.com</span><span class="p">;</span>

    <span class="kn">location</span> <span class="n">/</span> <span class="p">{</span>
        <span class="kn">root</span> <span class="n">/usr/share/nginx/html</span><span class="p">;</span>
        <span class="kn">index</span> <span class="s">index.html</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="5-핵심-정리">5. 핵심 정리</h2>

<ul>
  <li>Nginx는 클라이언트의 HTTP 요청을 가장 먼저 받을 수 있습니다.</li>
  <li>정적 파일을 직접 제공할 수 있습니다.</li>
  <li>요청을 Spring Boot와 같은 Backend Server로 전달할 수 있습니다.</li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="DevOps" /><category term="Nginx" /><category term="Nginx" /><category term="WebServer" /><category term="DevOps" /><category term="Network" /><summary type="html"><![CDATA[Nginx가 무엇인지 알아보고 Web Server, 정적 파일 제공, 요청 처리 구조를 정리한다.]]></summary></entry><entry><title type="html">Docker를 이해하기 위한 운영체제 기초</title><link href="http://localhost:4000/basic/os/2026/08/24/docker-os-basics/" rel="alternate" type="text/html" title="Docker를 이해하기 위한 운영체제 기초" /><published>2026-08-24T00:10:00+09:00</published><updated>2026-08-24T00:10:00+09:00</updated><id>http://localhost:4000/basic/os/2026/08/24/docker-os-basics</id><content type="html" xml:base="http://localhost:4000/basic/os/2026/08/24/docker-os-basics/"><![CDATA[<p>Docker를 이해하려면 먼저 <code class="language-plaintext highlighter-rouge">Process</code>, <code class="language-plaintext highlighter-rouge">Kernel</code>, <code class="language-plaintext highlighter-rouge">Memory</code>, <code class="language-plaintext highlighter-rouge">CPU</code>, <code class="language-plaintext highlighter-rouge">Thread</code>, <code class="language-plaintext highlighter-rouge">Filesystem</code>, <code class="language-plaintext highlighter-rouge">Network</code>가 무엇인지 알아야 합니다.</p>

<p>이 글에서는 Docker를 공부할 때 자주 만나는 개념을 <strong>운영체제 기초 단어장</strong>처럼 정리합니다. 처음부터 모든 내용을 외우기보다는 각 개념이 어떻게 연결되는지 이해하는 것이 중요합니다.</p>

<h2 id="1-program">1. Program</h2>

<p><strong>프로그램 = 실행 가능한 코드가 저장된 파일</strong></p>

<p>예:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nginx
java
python
chrome
</code></pre></div></div>

<p>아직 실행되지 않았다면 디스크에 저장된 파일일 뿐입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SSD

/usr/bin/python
/usr/bin/nginx
/app/my-server.jar

→ 아직 실행되지 않음
</code></pre></div></div>

<p>프로그램을 실행하면 <strong>Process</strong>가 됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Program
   ↓ 실행
Process
</code></pre></div></div>

<hr />

<h2 id="2-process">2. Process</h2>

<p><strong>프로세스 = 실행 중인 프로그램</strong></p>

<p>예를 들어:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python server.py
</code></pre></div></div>

<p>를 실행하면 Linux Kernel이 Python 프로세스를 만듭니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Program
server.py
   ↓
Python 실행
   ↓
Process 생성
</code></pre></div></div>

<p>프로세스에는 보통 다음과 같은 정보와 자원이 포함됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process

PID
CPU 실행 상태
Memory
File Descriptor
Network Socket
환경 변수
권한
</code></pre></div></div>

<p>예를 들어 Chrome을 3개 실행했다고 생각해보면:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Chrome 프로그램 파일
        ↓
 ┌──────┼──────┐
 ↓      ↓      ↓
PID 101 PID 102 PID 103
Process Process Process
</code></pre></div></div>

<p>같은 프로그램이라도 <strong>여러 프로세스가 존재할 수 있습니다.</strong></p>

<p>Docker에서 가장 중요한 연결점:</p>

<blockquote>
  <p><strong>컨테이너도 결국 Linux에서 실행되는 프로세스입니다.</strong></p>
</blockquote>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker Container
       ↓
격리된 Process
       ↓
Linux Kernel
</code></pre></div></div>

<hr />

<h2 id="3-pid">3. PID</h2>

<p><strong>PID = Process ID</strong></p>

<p>Linux가 프로세스를 구분하기 위해 붙이는 번호입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PID 1    systemd
PID 421  nginx
PID 532  java
PID 991  postgres
</code></pre></div></div>

<p>Linux에서:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ps
</code></pre></div></div>

<p>를 입력하면 실행 중인 프로세스를 확인할 수 있습니다.</p>

<p>Docker Container 내부에서는 PID Namespace 때문에 Host와 다른 PID 목록을 볼 수 있습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host

PID 1000 nginx

        ↓ PID Namespace

Container 내부에서는

PID 1 nginx
</code></pre></div></div>

<p>실제 프로세스는 같지만 <strong>보이는 프로세스 공간을 격리</strong>한 것입니다.</p>

<hr />

<h2 id="4-cpu">4. CPU</h2>

<p><strong>CPU = 명령어를 실제로 실행하는 장치</strong></p>

<p>프로그램 코드가:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">a</span> <span class="o">=</span> <span class="mi">10</span>
<span class="n">b</span> <span class="o">=</span> <span class="mi">20</span>
<span class="n">c</span> <span class="o">=</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span>
</code></pre></div></div>

<p>라면 CPU가 실제 계산을 수행합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Program Code
     ↓
Process
     ↓
CPU
     ↓
명령어 실행
</code></pre></div></div>

<p>CPU에는 Core가 있습니다.</p>

<p>예:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>8 Core CPU

Core 1
Core 2
Core 3
Core 4
Core 5
Core 6
Core 7
Core 8
</code></pre></div></div>

<p>여러 프로세스가 CPU를 번갈아 사용합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CPU Core

시간 →
────────────────────────

Chrome
       Java
             Python
                    Chrome
</code></pre></div></div>

<p>이렇게 실행 대상을 바꾸는 것을 <strong>Context Switching</strong>이라고 합니다.</p>

<hr />

<h2 id="5-memory--ram">5. Memory / RAM</h2>

<p><strong>Memory = 실행 중인 프로그램이 데이터를 임시로 저장하는 공간</strong></p>

<p>디스크보다 훨씬 빠르지만 전원을 끄면 내용이 사라집니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SSD
프로그램 저장

        ↓ 실행

RAM
Process 데이터 저장

        ↓

CPU
계산
</code></pre></div></div>

<p>전체 구조:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SSD
 │
 │ 프로그램 읽기
 ↓
RAM
 │
 │ 데이터 전달
 ↓
CPU
</code></pre></div></div>

<p>예:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>RAM 16GB

Chrome       3GB
IntelliJ     2GB
Docker       4GB
PostgreSQL   1GB
OS           3GB
기타         3GB
</code></pre></div></div>

<p>Docker에서:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">--memory</span><span class="o">=</span>512m nginx
</code></pre></div></div>

<p>처럼 메모리 사용량을 제한할 수 있습니다.</p>

<p>이 제한은 <strong>cgroup</strong>을 통해 Kernel이 수행합니다.</p>

<hr />

<h2 id="6-kernel">6. Kernel</h2>

<p>Docker를 이해할 때 특히 중요한 개념입니다.</p>

<p><strong>Kernel = 운영체제의 핵심 부분으로, 하드웨어와 프로그램 사이를 관리하는 소프트웨어</strong></p>

<p>쉽게 말하면 다음과 같습니다.</p>

<blockquote>
  <p>Kernel은 모든 프로세스와 하드웨어 자원을 관리하는 관리자입니다.</p>
</blockquote>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application

Chrome
Spring
PostgreSQL
Docker Container
      ↓
────────────────
Linux Kernel
────────────────
CPU
RAM
Disk
Network
</code></pre></div></div>

<p>프로그램이 직접 RAM이나 SSD를 마음대로 제어하면 위험합니다.</p>

<p>그래서 Kernel이 중간에서 관리합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process

"파일 읽어줘"
"메모리 줘"
"네트워크 보내줘"
"새 Process 만들어줘"

        ↓

Kernel

        ↓

CPU / RAM / SSD / Network
</code></pre></div></div>

<hr />

<h2 id="7-user-space와-kernel-space">7. User Space와 Kernel Space</h2>

<p>운영체제는 크게 두 영역으로 생각할 수 있습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User Space

Chrome
Java
Python
nginx
PostgreSQL

──────────────────

Kernel Space

Linux Kernel

──────────────────

Hardware

CPU
RAM
SSD
NIC
</code></pre></div></div>

<p>일반 프로그램은 <strong>User Space</strong>에서 실행됩니다.</p>

<p>Kernel만 <strong>Kernel Space</strong>에서 실행됩니다.</p>

<p>두 공간을 분리하는 이유는 일반 프로그램의 문제가 운영체제 전체에 영향을 주는 것을 막기 위해서입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Chrome 버그 발생
→ Chrome만 죽음

Chrome이 Kernel 메모리에 직접 접근 가능
→ OS 전체가 죽을 수도 있음
</code></pre></div></div>

<p>그래서 두 공간의 권한을 분리합니다.</p>

<hr />

<h2 id="8-system-call">8. System Call</h2>

<p>그렇다면 프로그램은 Kernel의 기능을 어떻게 사용할까요?</p>

<p>프로그램은 <strong>System Call</strong>을 사용합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application
    ↓
System Call
    ↓
Kernel
    ↓
Hardware
</code></pre></div></div>

<p>대표적인 System Call:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>open()   → 파일 열기
read()   → 데이터 읽기
write()  → 데이터 쓰기
fork()   → 프로세스 생성
exec()   → 프로그램 실행
socket() → 네트워크 Socket 생성
</code></pre></div></div>

<p>예:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">f</span> <span class="o">=</span> <span class="nb">open</span><span class="p">(</span><span class="s">"hello.txt"</span><span class="p">)</span>
</code></pre></div></div>

<p>Python 코드에서는 <code class="language-plaintext highlighter-rouge">open()</code>으로 보이지만 내부적으로는 Kernel에 파일을 열어달라고 요청합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Python
   ↓
open()
   ↓
System Call
   ↓
Linux Kernel
   ↓
Filesystem
   ↓
SSD
</code></pre></div></div>

<hr />

<h2 id="9-thread">9. Thread</h2>

<p><strong>Thread = 프로세스 내부의 실행 흐름</strong></p>

<p>프로세스 하나 안에 여러 Thread가 존재할 수 있습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process

Memory
Heap
Files

 ├── Thread 1
 ├── Thread 2
 ├── Thread 3
 └── Thread 4
</code></pre></div></div>

<p>예를 들어 Spring 서버:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Process

Thread 1 → 사용자 A 요청
Thread 2 → 사용자 B 요청
Thread 3 → 사용자 C 요청
Thread 4 → 사용자 D 요청
</code></pre></div></div>

<p>Process와 Thread 차이:</p>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Process</td>
      <td>실행 중인 프로그램 단위</td>
    </tr>
    <tr>
      <td>Thread</td>
      <td>Process 내부의 실행 단위</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="10-stack">10. Stack</h2>

<p><strong>Stack = 함수 호출과 지역 변수를 저장하는 메모리 영역</strong></p>

<p>예:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">hello</span><span class="o">()</span> <span class="o">{</span>
    <span class="kt">int</span> <span class="n">x</span> <span class="o">=</span> <span class="mi">10</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>실행하면 대략:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Stack

hello()
 ├── x = 10
 └── return address
</code></pre></div></div>

<p>함수가 끝나면 자동으로 제거됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>함수 호출

Stack
↓
생성

함수 종료
↓
제거
</code></pre></div></div>

<p>Thread마다 자신의 Stack을 가지고 있습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process

Heap

Thread 1 Stack
Thread 2 Stack
Thread 3 Stack
</code></pre></div></div>

<hr />

<h2 id="11-heap">11. Heap</h2>

<p><strong>Heap = 동적으로 생성된 객체를 저장하는 메모리 영역</strong></p>

<p>Java 예:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
</code></pre></div></div>

<p>대략:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Stack

user
 │
 │ reference
 ↓

Heap

User Object
</code></pre></div></div>

<p>Spring에서:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="nc">UserService</span>
</code></pre></div></div>

<p>같은 객체들도 JVM Heap에 존재합니다.</p>

<p>Java에서는 Garbage Collector가 Heap을 관리합니다.</p>

<hr />

<h2 id="12-virtual-memory">12. Virtual Memory</h2>

<p>Virtual Memory는 프로세스가 실제 RAM 주소를 직접 사용하지 않고 <strong>자신만의 메모리 공간이 있다고 생각하게 만드는 기술</strong>입니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process A

0x0000
0x0001
0x0002
...

Process B

0x0000
0x0001
0x0002
...
</code></pre></div></div>

<p>둘 다 같은 주소를 사용하는 것처럼 보이지만 Kernel이 실제 RAM 주소로 변환합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process Virtual Address

       ↓

Kernel

Page Table

       ↓

Physical RAM
</code></pre></div></div>

<p>덕분에:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process A
</code></pre></div></div>

<p>가</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process B
</code></pre></div></div>

<p>의 메모리를 마음대로 읽지 못합니다.</p>

<p>즉 <strong>프로세스 격리의 핵심 기술 중 하나</strong>입니다.</p>

<hr />

<h2 id="13-file-system">13. File System</h2>

<p><strong>File System = 파일과 디렉터리를 저장하고 관리하는 방식</strong></p>

<p>Linux:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/
├── bin
├── etc
├── home
├── usr
├── var
└── tmp
</code></pre></div></div>

<p>Docker 컨테이너도 자기만의 파일시스템이 있는 것처럼 보인다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container A

/
├── bin
├── app
└── etc

Container B

/
├── bin
├── app
└── etc
</code></pre></div></div>

<p>하지만 실제로는 Linux Kernel의 Filesystem 기능과 Namespace를 이용합니다.</p>

<hr />

<h2 id="14-file-descriptor">14. File Descriptor</h2>

<p>File Descriptor는 Linux에서 중요한 개념입니다.</p>

<p><strong>File Descriptor = 프로세스가 열어놓은 파일이나 I/O 자원을 나타내는 번호</strong></p>

<p>기본적으로:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0 = stdin
1 = stdout
2 = stderr
</code></pre></div></div>

<p>그래서:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo </span>hello
</code></pre></div></div>

<p>를 실행하면:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process
   ↓
stdout
FD 1
   ↓
Terminal
</code></pre></div></div>

<p>Docker가 로그를 받을 수 있는 것도 이 구조와 관련이 있습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container nginx

stdout
stderr
   ↓
containerd-shim
   ↓
Docker logs
</code></pre></div></div>

<hr />

<h2 id="15-socket">15. Socket</h2>

<p><strong>Socket = 네트워크 통신을 위한 프로세스의 통신 endpoint</strong></p>

<p>Spring 서버가:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>localhost:8080
</code></pre></div></div>

<p>에서 기다린다고 하면:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Process

Socket
IP : 0.0.0.0
Port : 8080
</code></pre></div></div>

<p>클라이언트가 연결하면:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Browser
   ↓
TCP
   ↓
Socket :8080
   ↓
Spring Process
</code></pre></div></div>

<p>Docker에서는 Network Namespace를 이용해서 컨테이너마다 네트워크 환경을 분리합니다.</p>

<hr />

<h2 id="16-port">16. Port</h2>

<p><strong>Port = 한 컴퓨터에서 어떤 프로세스로 네트워크 데이터를 전달할지 구분하는 번호</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Computer
IP 192.168.0.10

22   → SSH
80   → nginx
3306 → MySQL
8080 → Spring
</code></pre></div></div>

<p>Docker:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-p</span> 8080:80 nginx
</code></pre></div></div>

<p>이면:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host

8080
 ↓
Docker Network
 ↓
Container
80
 ↓
nginx
</code></pre></div></div>

<hr />

<h2 id="17-namespace">17. Namespace</h2>

<p>Namespace는 Docker의 격리 구조를 이해할 때 매우 중요합니다.</p>

<p><strong>Namespace = 프로세스가 볼 수 있는 시스템 자원을 격리하는 Linux Kernel 기능</strong></p>

<p>예:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PID Namespace
→ 다른 프로세스 안 보이게

Network Namespace
→ 네트워크 따로

Mount Namespace
→ 파일시스템 따로

UTS Namespace
→ hostname 따로

IPC Namespace
→ IPC 따로
</code></pre></div></div>

<p>이러한 격리를 통해 컨테이너는 다음과 같이 독립된 환경을 사용하는 것처럼 느낍니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"나 혼자 컴퓨터를 사용하는 것 같은데?"
</code></pre></div></div>

<p>라고 느끼게 만듭니다.</p>

<hr />

<h2 id="18-cgroup">18. cgroup</h2>

<p><strong>cgroup = 프로세스가 사용할 수 있는 자원의 양을 제한하고 측정하는 Linux Kernel 기능</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container A

CPU ≤ 2 Core
Memory ≤ 2GB

Container B

CPU ≤ 4 Core
Memory ≤ 8GB
</code></pre></div></div>

<p>Kernel이 실제로 자원 사용량을 제한합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process
   ↓
cgroup
   ↓
CPU / RAM 제한
</code></pre></div></div>

<p>그래서:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Namespace
= 무엇을 볼 수 있는가

cgroup
= 얼마나 사용할 수 있는가
</code></pre></div></div>

<p>이렇게 구분하면 두 개념을 쉽게 이해할 수 있습니다.</p>

<hr />

<h2 id="19-context-switch">19. Context Switch</h2>

<p>CPU 하나가 여러 프로세스를 실행할 때 아주 빠르게 실행 대상을 바꾼다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CPU

시간 →

Process A
        Process B
                  Process C
                            Process A
</code></pre></div></div>

<p>전환할 때 CPU 상태를 저장하고 다시 불러와야 합니다.</p>

<p>이 과정을 <strong>Context Switch</strong>라고 합니다.</p>

<p>Context Switch가 너무 많으면 성능 비용, 즉 <strong>Overhead</strong>가 발생합니다.</p>

<hr />

<h2 id="20-전체-연결">20. 전체 연결</h2>

<p>지금까지 살펴본 개념을 하나로 연결하면 다음과 같습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                   Application
              Spring / nginx / DB
                       │
                       │ Process
                       ▼
              ┌─────────────────┐
              │   User Space    │
              │                 │
              │ Process         │
              │ ├─ Thread       │
              │ ├─ Stack        │
              │ ├─ Heap         │
              │ ├─ Socket       │
              │ └─ File         │
              └────────┬────────┘
                       │
                   System Call
                       │
───────────────────────┼────────────────────
                       ▼
              ┌─────────────────┐
              │  Linux Kernel   │
              │                 │
              │ Process 관리    │
              │ Memory 관리     │
              │ Filesystem      │
              │ Network         │
              │ Namespace       │
              │ cgroup          │
              └────────┬────────┘
                       │
───────────────────────┼────────────────────
                       ▼
              ┌─────────────────┐
              │    Hardware     │
              │                 │
              │ CPU             │
              │ RAM             │
              │ SSD             │
              │ Network Card    │
              └─────────────────┘
</code></pre></div></div>

<p>그리고 Docker를 여기다 올리면:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Docker

docker run nginx
        ↓
dockerd
        ↓
containerd
        ↓
runc
        ↓

Linux Kernel

Namespace 설정
cgroup 설정
rootfs 설정
        ↓

nginx Process
        ↓

CPU / RAM / Network / Disk 사용
</code></pre></div></div>

<h2 id="학습-우선순위">학습 우선순위</h2>

<p>처음부터 전부 외울 필요는 없습니다. <strong>아래 순서대로 개념의 연결 관계를 익히는 것을 추천합니다.</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Program
   ↓
2. Process
   ↓
3. Thread
   ↓
4. CPU
   ↓
5. Memory
   ↓
6. Kernel
   ↓
7. User Space / Kernel Space
   ↓
8. System Call
   ↓
9. Filesystem
   ↓
10. Network / Socket / Port
    ↓
11. Namespace
    ↓
12. cgroup
</code></pre></div></div>

<p>특히 Docker를 공부하고 있다면 다음 관계를 먼저 기억하면 됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Program
→ 실행하면 Process

Process
→ CPU에서 실행되고 Memory를 사용함

Kernel
→ Process와 CPU/Memory/Network를 관리함

Docker Container
→ Kernel이 실행하는 Process를
   Namespace로 격리하고
   cgroup으로 자원을 제한한 것
</code></pre></div></div>

<p>이 <strong>4줄이 Docker를 이해하기 위한 운영체제 기초의 핵심</strong>입니다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Basic" /><category term="OS" /><category term="OS" /><category term="Linux" /><category term="Docker" /><category term="Process" /><category term="Kernel" /><category term="Namespace" /><category term="cgroup" /><summary type="html"><![CDATA[Program, Process, CPU, Memory, Kernel부터 Namespace와 cgroup까지 Docker 학습에 필요한 운영체제 핵심 개념을 흐름에 따라 정리한다.]]></summary></entry><entry><title type="html">개발 문서를 읽기 위한 핵심 기술 용어</title><link href="http://localhost:4000/basic/cs/2026/08/24/essential-development-terms/" rel="alternate" type="text/html" title="개발 문서를 읽기 위한 핵심 기술 용어" /><published>2026-08-24T00:05:00+09:00</published><updated>2026-08-24T00:05:00+09:00</updated><id>http://localhost:4000/basic/cs/2026/08/24/essential-development-terms</id><content type="html" xml:base="http://localhost:4000/basic/cs/2026/08/24/essential-development-terms/"><![CDATA[<p>Docker, Linux, 백엔드, 네트워크를 배우는 단계에서는 개별 기술뿐 아니라 여러 분야에서 공통으로 사용하는 기술 용어를 알아두는 것이 중요합니다. 같은 단어가 여러 문서에서 반복되기 때문에 용어를 이해하면 새로운 설명도 훨씬 빠르게 읽을 수 있습니다.</p>

<p>중요한 것은 모든 단어를 한 번에 외우는 것이 아닙니다. <strong>개발 문서에서 반복해서 나오는 표현을 문장 속에서 익히는 것</strong>이 목표입니다.</p>

<h2 id="1-자주-나오는-핵심-기술-용어">1. 자주 나오는 핵심 기술 용어</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>쉬운 뜻</th>
      <th>자주 나오는 곳</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Overhead</td>
      <td>본 작업 외에 추가로 드는 비용</td>
      <td>성능, 네트워크, Docker</td>
    </tr>
    <tr>
      <td>Latency</td>
      <td>요청한 뒤 응답을 받을 때까지 걸리는 지연 시간</td>
      <td>네트워크, API, DB</td>
    </tr>
    <tr>
      <td>Throughput</td>
      <td>일정 시간 동안 처리할 수 있는 작업량</td>
      <td>서버, 네트워크, DB</td>
    </tr>
    <tr>
      <td>Bottleneck</td>
      <td>전체 성능을 제한하는 가장 느린 지점</td>
      <td>성능 분석</td>
    </tr>
    <tr>
      <td>I/O</td>
      <td>파일, 디스크, 네트워크와 데이터를 주고받는 작업</td>
      <td>DB, OS, Docker</td>
    </tr>
    <tr>
      <td>Resource</td>
      <td>CPU, 메모리, 디스크 같은 자원</td>
      <td>OS, Cloud</td>
    </tr>
    <tr>
      <td>Allocation</td>
      <td>필요한 대상에 자원을 할당하는 것</td>
      <td>메모리, CPU</td>
    </tr>
    <tr>
      <td>Utilization</td>
      <td>할당된 자원을 실제로 얼마나 사용하는지 나타내는 정도</td>
      <td>모니터링</td>
    </tr>
    <tr>
      <td>Isolation</td>
      <td>여러 대상이 서로 영향을 덜 주도록 격리하는 것</td>
      <td>Docker, VM</td>
    </tr>
    <tr>
      <td>Abstraction</td>
      <td>복잡한 내부 구현을 감추고 쉽게 사용할 수 있게 만든 구조</td>
      <td>거의 모든 CS 분야</td>
    </tr>
    <tr>
      <td>Layer</td>
      <td>기능이나 구조를 역할에 따라 나눈 계층</td>
      <td>Docker, Network, Software</td>
    </tr>
    <tr>
      <td>Runtime</td>
      <td>프로그램이 실제로 실행되는 환경 또는 실행 시점</td>
      <td>Java, Docker</td>
    </tr>
    <tr>
      <td>Dependency</td>
      <td>어떤 구성 요소가 실행되기 위해 필요로 하는 다른 요소</td>
      <td>Spring, 패키지 관리</td>
    </tr>
    <tr>
      <td>Lifecycle</td>
      <td>생성부터 실행과 종료까지 이어지는 과정</td>
      <td>Spring, Docker</td>
    </tr>
    <tr>
      <td>Persistent</td>
      <td>프로세스나 컨테이너가 종료돼도 데이터가 유지되는 성질</td>
      <td>DB, Volume</td>
    </tr>
    <tr>
      <td>Ephemeral</td>
      <td>일시적이며 종료나 교체 과정에서 사라질 수 있는 성질</td>
      <td>Container, Cloud</td>
    </tr>
    <tr>
      <td>Immutable</td>
      <td>생성한 뒤 직접 변경하지 않고 새 버전으로 교체하는 성질</td>
      <td>Docker Image, Infrastructure</td>
    </tr>
    <tr>
      <td>State</td>
      <td>현재 상태나 이전 작업을 이어가는 데 필요한 저장 정보</td>
      <td>서버, DB</td>
    </tr>
    <tr>
      <td>Stateless</td>
      <td>요청 사이의 상태를 서버 내부에 유지하지 않는 구조</td>
      <td>API, Cloud</td>
    </tr>
    <tr>
      <td>Stateful</td>
      <td>이전 상태를 저장하고 다음 작업에서도 사용하는 구조</td>
      <td>DB, Session Server</td>
    </tr>
    <tr>
      <td>Scalability</td>
      <td>부하가 커졌을 때 자원을 늘려 대응할 수 있는 정도</td>
      <td>Cloud, Architecture</td>
    </tr>
    <tr>
      <td>Availability</td>
      <td>사용자가 서비스를 정상적으로 사용할 수 있는 정도</td>
      <td>서버 운영</td>
    </tr>
    <tr>
      <td>Fault Tolerance</td>
      <td>일부 구성 요소가 고장 나도 서비스를 계속 제공하는 능력</td>
      <td>분산 시스템</td>
    </tr>
    <tr>
      <td>Redundancy</td>
      <td>장애에 대비해 서버나 경로 등을 중복으로 구성하는 것</td>
      <td>서버, 네트워크</td>
    </tr>
    <tr>
      <td>Concurrency</td>
      <td>여러 작업의 실행 시간이 겹치도록 번갈아 처리하는 성질</td>
      <td>Java, 서버</td>
    </tr>
    <tr>
      <td>Parallelism</td>
      <td>여러 작업을 여러 실행 자원에서 실제로 동시에 처리하는 성질</td>
      <td>CPU, Thread</td>
    </tr>
    <tr>
      <td>Blocking</td>
      <td>진행 중인 작업이 끝날 때까지 호출한 쪽이 기다리는 방식</td>
      <td>I/O, Java</td>
    </tr>
    <tr>
      <td>Non-blocking</td>
      <td>작업 완료를 기다리지 않고 즉시 제어권을 돌려주는 방식</td>
      <td>서버, Async I/O</td>
    </tr>
    <tr>
      <td>Synchronous</td>
      <td>작업 완료를 현재 흐름에서 확인한 뒤 다음 단계로 진행하는 방식</td>
      <td>API, Java</td>
    </tr>
    <tr>
      <td>Asynchronous</td>
      <td>작업 완료를 나중에 알림, 이벤트, 콜백 등으로 전달받는 방식</td>
      <td>Message Queue, Event 처리</td>
    </tr>
    <tr>
      <td>Cache</td>
      <td>자주 쓰는 데이터를 더 빠른 저장 공간에 임시로 보관하는 것</td>
      <td>Redis, CPU, Web</td>
    </tr>
    <tr>
      <td>Serialization</td>
      <td>객체나 데이터를 저장하거나 전송할 수 있는 형태로 변환하는 것</td>
      <td>JSON, API</td>
    </tr>
    <tr>
      <td>Deserialization</td>
      <td>저장하거나 전송한 데이터를 프로그램이 사용할 형태로 복원하는 것</td>
      <td>JSON, API</td>
    </tr>
    <tr>
      <td>Idempotent</td>
      <td>같은 요청을 여러 번 실행해도 시스템의 최종 상태가 같아지는 성질</td>
      <td>HTTP, API</td>
    </tr>
    <tr>
      <td>Atomic</td>
      <td>작업 전체가 성공하거나 실패하며 일부만 반영된 중간 결과를 남기지 않는 성질</td>
      <td>DB, Thread</td>
    </tr>
    <tr>
      <td>Consistency</td>
      <td>데이터가 정해진 규칙과 제약 조건을 만족하는 상태</td>
      <td>DB, 분산 시스템</td>
    </tr>
  </tbody>
</table>

<h2 id="2-먼저-익힐-10개">2. 먼저 익힐 10개</h2>

<p>처음부터 모든 단어를 외우기보다 다음 10개를 먼저 익히는 것이 좋습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Overhead
Latency
Throughput
Bottleneck
I/O
Isolation
Abstraction
Runtime
Dependency
Lifecycle
</code></pre></div></div>

<p>이 단어들은 Docker와 Linux뿐 아니라 Spring, Kubernetes, AWS 문서에서도 반복해서 등장합니다.</p>

<h2 id="3-문장을-용어-단위로-해석하기">3. 문장을 용어 단위로 해석하기</h2>

<p>Docker 문서에서 다음과 같은 문장을 만났다고 가정해 봅니다.</p>

<blockquote>
  <p>Container isolation introduces relatively low runtime overhead.</p>
</blockquote>

<p>문장을 용어 단위로 나누면 이해하기 쉽습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Container isolation
→ 컨테이너 격리

runtime overhead
→ 실행 중 발생하는 추가 비용

relatively low
→ 비교적 적다
</code></pre></div></div>

<p>따라서 전체 문장은 다음과 같이 해석할 수 있습니다.</p>

<blockquote>
  <p>컨테이너 격리는 실행 시 발생하는 추가 비용이 비교적 적다.</p>
</blockquote>

<p>영어 문장을 처음부터 완벽하게 번역하려 하기보다 익숙한 기술 용어를 먼저 찾으면 문서의 핵심을 더 빠르게 파악할 수 있습니다.</p>

<h2 id="4-백엔드에서-자주-보는-표현">4. 백엔드에서 자주 보는 표현</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>High latency
→ 응답이 느리다

High throughput
→ 일정 시간 동안 많은 요청을 처리할 수 있다

I/O bottleneck
→ 디스크 또는 네트워크 입출력이 전체 성능을 제한하고 있다

Memory overhead
→ 핵심 작업 외에 메모리가 추가로 사용된다

Runtime dependency
→ 프로그램을 실행할 때 필요한 의존 요소

Persistent storage
→ 프로세스나 컨테이너가 종료돼도 데이터가 유지되는 저장소
</code></pre></div></div>

<h2 id="5-단어보다-표현으로-기억하기">5. 단어보다 표현으로 기억하기</h2>

<p>기술 용어는 영어사전처럼 단어 하나만 외우기보다 자주 함께 사용하는 동사와 묶어서 기억하는 것이 좋습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reduce overhead
→ 오버헤드를 줄인다

improve throughput
→ 처리량을 높인다

reduce latency
→ 지연 시간을 줄인다

allocate resources
→ 자원을 할당한다

isolate processes
→ 프로세스를 격리한다

persist data
→ 데이터를 영속적으로 저장한다

resolve dependencies
→ 의존성을 해결한다

manage lifecycle
→ 생명주기를 관리한다
</code></pre></div></div>

<p>이런 표현은 개발 문서, 오류 메시지, 기술 발표에서 반복적으로 등장합니다.</p>

<h2 id="6-단계별-학습-순서">6. 단계별 학습 순서</h2>

<p>Docker 공부 흐름을 기준으로 다음 순서로 익히면 개념을 연결하기 쉽습니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1단계: 프로그램 실행의 기본
Process → Thread → Daemon → Runtime → Lifecycle

2단계: 실행 환경과 비용
Resource → Allocation → Isolation → Overhead → I/O

3단계: 성능
Latency → Throughput → Bottleneck → Cache

4단계: 상태와 데이터
Stateful → Stateless → Persistent → Ephemeral → Immutable

5단계: 여러 작업 처리
Concurrency → Parallelism → Blocking → Non-blocking → Sync → Async
</code></pre></div></div>

<p>이 단어들이 익숙해지면 Docker뿐 아니라 Spring, Kubernetes, AWS, 시스템 설계 문서를 읽을 때 느끼는 난도가 크게 낮아집니다.</p>

<h2 id="7-반드시-구분할-네-쌍">7. 반드시 구분할 네 쌍</h2>

<h3 id="71-latency와-throughput">7.1 Latency와 Throughput</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Latency    = 요청 하나가 얼마나 빨리 끝나는가?
Throughput = 일정 시간 동안 요청을 몇 개 처리하는가?
</code></pre></div></div>

<p>처리량이 높아도 요청 하나의 지연 시간은 길 수 있습니다. 반대로 요청 하나는 빨리 처리하지만 동시에 처리할 수 있는 요청 수가 적을 수도 있습니다.</p>

<h3 id="72-concurrency와-parallelism">7.2 Concurrency와 Parallelism</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Concurrency = 여러 작업을 겹치는 시간 동안 다루는 구조
Parallelism = 여러 작업을 실제로 같은 순간에 실행하는 구조
</code></pre></div></div>

<p>한 명의 요리사가 여러 요리를 번갈아 만드는 것은 Concurrency이고, 여러 요리사가 각 요리를 동시에 만드는 것은 Parallelism에 가깝습니다.</p>

<h3 id="73-blocking과-non-blocking">7.3 Blocking과 Non-blocking</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Blocking     = 작업이 끝날 때까지 제어권을 돌려받지 못함
Non-blocking = 작업 완료 여부와 관계없이 제어권을 바로 돌려받음
</code></pre></div></div>

<p>이 구분은 호출한 작업이 제어권을 언제 돌려주는지에 초점을 맞춥니다.</p>

<h3 id="74-stateful과-stateless">7.4 Stateful과 Stateless</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Stateful  = 이전 요청의 상태를 저장하고 사용함
Stateless = 각 요청을 독립적으로 처리함
</code></pre></div></div>

<p>DB처럼 데이터를 유지해야 하는 서비스는 Stateful한 경우가 많습니다. 요청마다 필요한 정보를 모두 전달받는 일반적인 REST API 서버는 Stateless하게 설계하기 좋습니다.</p>

<hr />

<p>핵심은 용어의 번역만 암기하는 것이 아니라 실제 문장과 상황 속에서 의미를 연결하는 것입니다. 모르는 기술을 만나더라도 공통 용어가 익숙하면 설명의 구조부터 파악할 수 있습니다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="Basic" /><category term="CS" /><category term="CS" /><category term="Backend" /><category term="Docker" /><category term="Linux" /><category term="Network" /><category term="Terminology" /><summary type="html"><![CDATA[Docker, Linux, 백엔드, 네트워크 문서에서 반복해서 등장하는 공통 기술 용어와 자주 쓰는 표현을 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 11: Actuator와 애플리케이션 모니터링</title><link href="http://localhost:4000/java/spring/2026/08/23/spring-boot-actuator/" rel="alternate" type="text/html" title="Spring 기초 Part 11: Actuator와 애플리케이션 모니터링" /><published>2026-08-23T09:00:00+09:00</published><updated>2026-08-23T09:00:00+09:00</updated><id>http://localhost:4000/java/spring/2026/08/23/spring-boot-actuator</id><content type="html" xml:base="http://localhost:4000/java/spring/2026/08/23/spring-boot-actuator/"><![CDATA[<h2 id="spring-기초-part-11-actuator와-애플리케이션-모니터링">Spring 기초 Part 11: Actuator와 애플리케이션 모니터링</h2>

<blockquote>
  <p>Spring Boot Actuator는 실행 중인 애플리케이션의 상태와 성능 지표를 확인하고 관리할 수 있도록 운영용 엔드포인트를 제공한다.</p>
</blockquote>

<p>애플리케이션이 실행된다고 운영 준비가 끝난 것은 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>서버가 살아 있는가?
DB 연결은 정상인가?
메모리를 얼마나 사용하는가?
요청이 얼마나 들어오는가?
오류가 증가하고 있는가?
</code></pre></div></div>

<p>Actuator와 Metrics는 이런 질문에 답할 수 있는 정보를 제공한다.</p>

<hr />

<h3 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>모니터링</td>
      <td>애플리케이션 상태와 성능을 지속적으로 관찰하는 작업</td>
      <td>서버가 정상인지 확인</td>
    </tr>
    <tr>
      <td>Endpoint</td>
      <td>운영 정보를 조회하거나 관리하는 접근 지점</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/health</code></td>
    </tr>
    <tr>
      <td>Health Check</td>
      <td>애플리케이션과 의존 시스템의 정상 여부 확인</td>
      <td>DB 연결 상태 확인</td>
    </tr>
    <tr>
      <td>Metric</td>
      <td>시간에 따라 측정되는 수치 데이터</td>
      <td>CPU 사용량, 요청 수</td>
    </tr>
    <tr>
      <td>Micrometer</td>
      <td>여러 모니터링 시스템에 Metrics를 전달하는 계측 라이브러리</td>
      <td>Metrics 공통 인터페이스</td>
    </tr>
    <tr>
      <td>Prometheus</td>
      <td>Metrics를 주기적으로 수집하고 저장하는 모니터링 시스템</td>
      <td>Actuator Metrics 수집기</td>
    </tr>
    <tr>
      <td>Scrape</td>
      <td>Prometheus가 애플리케이션의 Metrics Endpoint를 주기적으로 조회</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/prometheus</code> 호출</td>
    </tr>
    <tr>
      <td>Metadata</td>
      <td>애플리케이션을 설명하는 부가 정보</td>
      <td>버전, 빌드 정보</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="2-의존성-추가">2. 의존성 추가</h3>

<p>Maven 프로젝트에 Actuator Starter를 추가한다.</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;dependency&gt;</span>
    <span class="nt">&lt;groupId&gt;</span>org.springframework.boot<span class="nt">&lt;/groupId&gt;</span>
    <span class="nt">&lt;artifactId&gt;</span>spring-boot-starter-actuator<span class="nt">&lt;/artifactId&gt;</span>
<span class="nt">&lt;/dependency&gt;</span>
</code></pre></div></div>

<p>Prometheus 형식으로 Metrics를 제공하려면 Registry도 추가한다.</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;dependency&gt;</span>
    <span class="nt">&lt;groupId&gt;</span>io.micrometer<span class="nt">&lt;/groupId&gt;</span>
    <span class="nt">&lt;artifactId&gt;</span>micrometer-registry-prometheus<span class="nt">&lt;/artifactId&gt;</span>
<span class="nt">&lt;/dependency&gt;</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Boot Actuator
        ↓
Micrometer가 Metrics 수집
        ↓
Prometheus Registry가 형식 변환
        ↓
/actuator/prometheus로 노출
        ↓
Prometheus가 주기적으로 수집
</code></pre></div></div>

<hr />

<h3 id="3-주요-엔드포인트">3. 주요 엔드포인트</h3>

<table>
  <thead>
    <tr>
      <th>기능</th>
      <th>설명</th>
      <th>엔드포인트</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Health</td>
      <td>애플리케이션과 의존 시스템의 상태 확인</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/health</code></td>
    </tr>
    <tr>
      <td>Metrics</td>
      <td>JVM, CPU, 메모리, GC, 요청 수 등의 지표 조회</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/metrics</code></td>
    </tr>
    <tr>
      <td>Prometheus</td>
      <td>Metrics를 Prometheus Scrape 형식으로 제공</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/prometheus</code></td>
    </tr>
    <tr>
      <td>Info</td>
      <td>버전과 빌드 정보 등의 Metadata 제공</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/info</code></td>
    </tr>
    <tr>
      <td>Env</td>
      <td>환경 변수와 설정 속성 정보 제공</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/env</code></td>
    </tr>
    <tr>
      <td>Loggers</td>
      <td>Logger 목록과 Log Level 조회 및 변경</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/loggers</code></td>
    </tr>
    <tr>
      <td>Beans</td>
      <td>Spring Container에 등록된 Bean 정보 제공</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/beans</code></td>
    </tr>
    <tr>
      <td>Mappings</td>
      <td>Controller의 URL Mapping 정보 제공</td>
      <td><code class="language-plaintext highlighter-rouge">/actuator/mappings</code></td>
    </tr>
  </tbody>
</table>

<p>엔드포인트가 Actuator에 존재하는 것과 HTTP로 외부에 노출되는 것은 다른 문제다.</p>

<blockquote>
  <p>기본 설정에서는 일반적으로 <code class="language-plaintext highlighter-rouge">health</code>만 HTTP와 JMX에 노출된다. 다른 엔드포인트는 필요한 항목을 명시적으로 노출해야 한다.</p>
</blockquote>

<hr />

<h3 id="4-health-check">4. Health Check</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="s1">'http://localhost:8080/actuator/health'</span>
</code></pre></div></div>

<p>정상 상태라면 다음과 같은 응답을 확인할 수 있다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"status"</span><span class="p">:</span><span class="w"> </span><span class="s2">"UP"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">UP</code></td>
      <td>정상적으로 동작 중</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DOWN</code></td>
      <td>구성 요소가 동작하지 않음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OUT_OF_SERVICE</code></td>
      <td>서비스 대상에서 제외된 상태</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">UNKNOWN</code></td>
      <td>상태를 판단할 수 없음</td>
    </tr>
  </tbody>
</table>

<p>Health 정보의 상세 내용을 얼마나 공개할지는 별도로 설정한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">management</span><span class="pi">:</span>
  <span class="na">endpoint</span><span class="pi">:</span>
    <span class="na">health</span><span class="pi">:</span>
      <span class="na">show-details</span><span class="pi">:</span> <span class="s">when_authorized</span>
</code></pre></div></div>

<p>운영 환경에서 DB 주소나 내부 구성 정보가 불필요하게 공개되지 않도록 주의한다.</p>

<hr />

<h3 id="5-metrics">5. Metrics</h3>

<p>Metrics Endpoint를 노출한 뒤 사용 가능한 지표 이름을 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="s1">'http://localhost:8080/actuator/metrics'</span>
</code></pre></div></div>

<p>특정 지표를 조회하려면 이름을 URL 뒤에 붙인다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="s1">'http://localhost:8080/actuator/metrics/jvm.memory.used'</span>
</code></pre></div></div>

<p>대표적인 지표는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>지표</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">jvm.memory.used</code></td>
      <td>JVM이 사용 중인 메모리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">jvm.gc.pause</code></td>
      <td>Garbage Collection 정지 시간</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">jvm.threads.live</code></td>
      <td>현재 활성 Thread 수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">process.cpu.usage</code></td>
      <td>애플리케이션 프로세스 CPU 사용률</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">system.cpu.usage</code></td>
      <td>시스템 전체 CPU 사용률</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">http.server.requests</code></td>
      <td>HTTP 요청 수와 처리 시간</td>
    </tr>
  </tbody>
</table>

<p>Metrics는 한 번 확인하는 값보다 시간에 따른 변화와 평소 기준을 함께 보는 것이 중요하다.</p>

<hr />

<h3 id="6-prometheus-연동">6. Prometheus 연동</h3>

<p>Prometheus Registry 의존성을 추가하고 Endpoint를 노출하면 다음 주소에서 Metrics를 확인할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="s1">'http://localhost:8080/actuator/prometheus'</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code># HELP jvm_memory_used_bytes ...
# TYPE jvm_memory_used_bytes gauge
jvm_memory_used_bytes{area="heap",...} 1.2345678E8
</code></pre></div></div>

<p>Prometheus는 이 Endpoint를 주기적으로 Scrape한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">scrape_configs</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">job_name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">spring-app"</span>
    <span class="na">metrics_path</span><span class="pi">:</span> <span class="s2">"</span><span class="s">/actuator/prometheus"</span>
    <span class="na">static_configs</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">targets</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">localhost:8080"</span><span class="pi">]</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Boot
  → /actuator/prometheus
  → Prometheus 수집
  → 시계열 데이터 저장
  → Grafana Dashboard와 Alert
</code></pre></div></div>

<hr />

<h3 id="7-필요한-엔드포인트만-노출하기">7. 필요한 엔드포인트만 노출하기</h3>

<p>학습 환경에서 <code class="language-plaintext highlighter-rouge">health</code>, <code class="language-plaintext highlighter-rouge">info</code>, <code class="language-plaintext highlighter-rouge">metrics</code>, <code class="language-plaintext highlighter-rouge">prometheus</code>만 노출하는 예다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">management</span><span class="pi">:</span>
  <span class="na">endpoints</span><span class="pi">:</span>
    <span class="na">web</span><span class="pi">:</span>
      <span class="na">exposure</span><span class="pi">:</span>
        <span class="na">include</span><span class="pi">:</span> <span class="s">health, info, metrics, prometheus</span>

  <span class="na">endpoint</span><span class="pi">:</span>
    <span class="na">health</span><span class="pi">:</span>
      <span class="na">show-details</span><span class="pi">:</span> <span class="s">when_authorized</span>
</code></pre></div></div>

<p>다음 설정은 모든 Endpoint를 노출한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">management</span><span class="pi">:</span>
  <span class="na">endpoints</span><span class="pi">:</span>
    <span class="na">web</span><span class="pi">:</span>
      <span class="na">exposure</span><span class="pi">:</span>
        <span class="na">include</span><span class="pi">:</span> <span class="s2">"</span><span class="s">*"</span>
</code></pre></div></div>

<p>학습용 로컬 환경에서는 편할 수 있지만 운영 환경에서는 권장하지 않는다.</p>

<p><code class="language-plaintext highlighter-rouge">env</code>, <code class="language-plaintext highlighter-rouge">beans</code>, <code class="language-plaintext highlighter-rouge">mappings</code>, <code class="language-plaintext highlighter-rouge">loggers</code> 등은 내부 구조와 설정 정보를 보여주거나 동작을 변경할 수 있다. 필요한 Endpoint만 선택하고 인증, 방화벽, 내부 네트워크 등의 보호 수단을 적용한다.</p>

<hr />

<h3 id="8-관리-포트-분리">8. 관리 포트 분리</h3>

<p>Actuator를 애플리케이션과 다른 포트에서 실행할 수도 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">management</span><span class="pi">:</span>
  <span class="na">server</span><span class="pi">:</span>
    <span class="na">port</span><span class="pi">:</span> <span class="m">8081</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자 API
→ 8080

Actuator 관리 Endpoint
→ 8081
</code></pre></div></div>

<p>포트를 분리했다고 자동으로 안전해지는 것은 아니다. 외부 Network에서 관리 포트에 접근할 수 없도록 구성해야 한다.</p>

<p>로컬 주소에서만 수신하려면 관리 포트를 분리한 뒤 다음과 같은 설정을 고려할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">management</span><span class="pi">:</span>
  <span class="na">server</span><span class="pi">:</span>
    <span class="na">port</span><span class="pi">:</span> <span class="m">8081</span>
    <span class="na">address</span><span class="pi">:</span> <span class="s">127.0.0.1</span>
</code></pre></div></div>

<hr />

<h3 id="9-운영-환경-체크리스트">9. 운영 환경 체크리스트</h3>

<table>
  <thead>
    <tr>
      <th>확인 항목</th>
      <th>이유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>필요한 Endpoint만 노출했는가?</td>
      <td>불필요한 내부 정보 공개 방지</td>
    </tr>
    <tr>
      <td>인증 또는 Network 접근 제어가 있는가?</td>
      <td>외부 사용자의 관리 기능 접근 차단</td>
    </tr>
    <tr>
      <td>Health 상세 정보 공개 범위가 적절한가?</td>
      <td>DB와 내부 시스템 정보 보호</td>
    </tr>
    <tr>
      <td>Prometheus Endpoint를 내부에서만 수집하는가?</td>
      <td>Metrics 정보 노출 방지</td>
    </tr>
    <tr>
      <td>비밀번호와 Token이 설정 파일이나 응답에 노출되지 않는가?</td>
      <td>Credential 유출 방지</td>
    </tr>
    <tr>
      <td>Alert 기준을 설정했는가?</td>
      <td>장애를 사람이 계속 화면을 보지 않아도 감지</td>
    </tr>
  </tbody>
</table>

<p>Actuator는 정보를 제공하는 도구다. 장애 대응을 자동화하려면 Prometheus Alertmanager, Grafana Alerting 같은 별도의 알림 체계도 필요하다.</p>

<hr />

<h3 id="핵심-정리">핵심 정리</h3>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 줄 정리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Actuator</td>
      <td>애플리케이션의 운영 상태를 제공하는 Spring Boot 기능</td>
    </tr>
    <tr>
      <td>Health</td>
      <td>애플리케이션과 의존 시스템의 정상 여부 확인</td>
    </tr>
    <tr>
      <td>Metrics</td>
      <td>JVM, CPU, 요청 수 등의 측정값</td>
    </tr>
    <tr>
      <td>Micrometer</td>
      <td>Metrics 수집과 Registry 연결을 위한 계측 Facade</td>
    </tr>
    <tr>
      <td>Prometheus</td>
      <td>Metrics Endpoint를 주기적으로 Scrape하고 저장</td>
    </tr>
    <tr>
      <td>Endpoint 노출</td>
      <td>존재하는 Endpoint 중 HTTP로 공개할 범위를 설정</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Actuator의 핵심은 모든 Endpoint를 여는 것이 아니라, 운영에 필요한 정보를 안전하게 노출하고 지속적으로 관찰하는 것이다.</p>
</blockquote>

<h3 id="참고-자료">참고 자료</h3>

<ul>
  <li><a href="https://docs.spring.io/spring-boot/reference/actuator/endpoints.html">Spring Boot Actuator Endpoint 공식 문서</a></li>
  <li><a href="https://docs.spring.io/spring-boot/reference/actuator/metrics.html">Spring Boot Actuator Metrics 공식 문서</a></li>
  <li><a href="https://docs.spring.io/spring-boot/reference/actuator/monitoring.html">Spring Boot HTTP Monitoring 공식 문서</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring Boot" /><category term="Actuator" /><category term="Monitoring" /><category term="Metrics" /><category term="Micrometer" /><category term="Prometheus" /><summary type="html"><![CDATA[Spring Boot Actuator의 Health, Metrics, Prometheus 엔드포인트와 노출 설정, 운영 환경 보안 주의사항을 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 10: 비동기 처리와 @Async</title><link href="http://localhost:4000/java/spring/2026/08/22/spring-async/" rel="alternate" type="text/html" title="Spring 기초 Part 10: 비동기 처리와 @Async" /><published>2026-08-22T09:00:00+09:00</published><updated>2026-08-22T09:00:00+09:00</updated><id>http://localhost:4000/java/spring/2026/08/22/spring-async</id><content type="html" xml:base="http://localhost:4000/java/spring/2026/08/22/spring-async/"><![CDATA[<h2 id="spring-기초-part-10-비동기-처리와-async">Spring 기초 Part 10: 비동기 처리와 <code class="language-plaintext highlighter-rouge">@Async</code></h2>

<blockquote>
  <p>비동기 처리는 작업의 완료를 기다리는 대신 다른 실행 흐름에 작업을 맡기고, 호출한 쪽은 다음 작업을 진행하는 방식이다.</p>
</blockquote>

<p>이메일 발송에 5초가 걸린다고 해보자.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>동기 처리

회원 저장 0.1초
   ↓
이메일 전송 5초
   ↓
응답
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>비동기 처리

회원 저장
   ↓
이메일 작업 제출 ─────→ 다른 Thread에서 전송
   ↓
바로 응답
</code></pre></div></div>

<hr />

<h3 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>동기(Synchronous)</td>
      <td>이전 작업이 끝날 때까지 기다린 뒤 다음 작업 실행</td>
      <td>순서대로 하나씩 처리</td>
    </tr>
    <tr>
      <td>비동기(Asynchronous)</td>
      <td>작업을 맡긴 뒤 완료를 기다리지 않고 다음 작업 진행</td>
      <td>다른 실행 흐름에 맡기기</td>
    </tr>
    <tr>
      <td>Thread</td>
      <td>코드를 실행하는 하나의 흐름</td>
      <td>실제 작업자</td>
    </tr>
    <tr>
      <td>Thread Pool</td>
      <td>미리 준비한 여러 Thread를 재사용하는 구조</td>
      <td>작업자 대기실</td>
    </tr>
    <tr>
      <td>Task</td>
      <td>Executor에 맡길 실행 단위</td>
      <td>이메일 한 건 발송</td>
    </tr>
    <tr>
      <td>Executor</td>
      <td>Task를 어떤 Thread에서 실행할지 관리하는 객체</td>
      <td>작업 배정 관리자</td>
    </tr>
    <tr>
      <td>Future</td>
      <td>나중에 완료될 결과를 표현하는 객체</td>
      <td>결과 수령표</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">CompletableFuture</code></td>
      <td>비동기 결과를 연결하고 조합할 수 있는 Future</td>
      <td>완료 후 다음 작업 연결</td>
    </tr>
  </tbody>
</table>

<p>비동기라고 해서 작업 자체의 실행 시간이 무조건 줄어드는 것은 아니다. 호출자가 기다리지 않고 다른 일을 할 수 있도록 실행 흐름을 분리하는 것이 핵심이다.</p>

<hr />

<h3 id="2-async-활성화">2. <code class="language-plaintext highlighter-rouge">@Async</code> 활성화</h3>

<p>Spring에서 <code class="language-plaintext highlighter-rouge">@Async</code>를 사용하려면 비동기 실행 기능을 활성화한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Configuration</span>
<span class="nd">@EnableAsync</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">AsyncConfig</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>어노테이션</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Configuration</code></td>
      <td>설정 클래스를 Spring Bean으로 등록</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@EnableAsync</code></td>
      <td><code class="language-plaintext highlighter-rouge">@Async</code> 어노테이션 처리 활성화</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Async</code></td>
      <td>해당 메서드 호출을 TaskExecutor에 제출</td>
    </tr>
  </tbody>
</table>

<p>Spring의 기본 <code class="language-plaintext highlighter-rouge">@Async</code> 처리 방식은 Proxy 기반이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>호출자
  → Async Proxy
  → TaskExecutor에 작업 제출
  → 호출자는 다음 작업 진행

TaskExecutor
  → 다른 Thread에서 실제 메서드 실행
</code></pre></div></div>

<hr />

<h3 id="3-가장-간단한-async">3. 가장 간단한 <code class="language-plaintext highlighter-rouge">@Async</code></h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">EmailService</span> <span class="o">{</span>

    <span class="nd">@Async</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">sendWelcomeEmail</span><span class="o">(</span><span class="nc">String</span> <span class="n">email</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="nc">Thread</span><span class="o">.</span><span class="na">currentThread</span><span class="o">().</span><span class="na">getName</span><span class="o">());</span>
        <span class="c1">// 이메일 전송</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>다른 Bean에서 호출한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">EmailService</span> <span class="n">emailService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserService</span><span class="o">(</span><span class="nc">EmailService</span> <span class="n">emailService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">emailService</span> <span class="o">=</span> <span class="n">emailService</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">register</span><span class="o">(</span><span class="nc">String</span> <span class="n">email</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// 회원 저장</span>
        <span class="n">emailService</span><span class="o">.</span><span class="na">sendWelcomeEmail</span><span class="o">(</span><span class="n">email</span><span class="o">);</span>

        <span class="c1">// 이메일 완료를 기다리지 않고 다음 코드 실행</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>@Async 호출 흐름</span>
  </header>
  <div class="mermaid js-mermaid">
sequenceDiagram
    participant U as UserService
    participant P as Async Proxy
    participant E as TaskExecutor
    participant T as EmailService

    U-&gt;&gt;P: sendWelcomeEmail 호출
    P-&gt;&gt;E: Task 제출
    P--&gt;&gt;U: 즉시 반환
    E-&gt;&gt;T: 별도 Thread에서 메서드 실행
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<hr />

<h3 id="4-결과가-필요한-경우">4. 결과가 필요한 경우</h3>

<p>결과가 필요하다면 <code class="language-plaintext highlighter-rouge">CompletableFuture</code>를 반환할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Async</span>
<span class="kd">public</span> <span class="nc">CompletableFuture</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="nf">createReport</span><span class="o">(</span><span class="nc">Long</span> <span class="n">userId</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">result</span> <span class="o">=</span> <span class="n">reportGenerator</span><span class="o">.</span><span class="na">generate</span><span class="o">(</span><span class="n">userId</span><span class="o">);</span>
    <span class="k">return</span> <span class="nc">CompletableFuture</span><span class="o">.</span><span class="na">completedFuture</span><span class="o">(</span><span class="n">result</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>호출한 쪽은 완료 후 실행할 작업을 연결할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">CompletableFuture</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">future</span> <span class="o">=</span> <span class="n">reportService</span><span class="o">.</span><span class="na">createReport</span><span class="o">(</span><span class="n">userId</span><span class="o">);</span>

<span class="n">future</span><span class="o">.</span><span class="na">thenAccept</span><span class="o">(</span><span class="n">result</span> <span class="o">-&gt;</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"보고서 생성 완료: "</span> <span class="o">+</span> <span class="n">result</span><span class="o">);</span>
<span class="o">});</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">join()</code>이나 <code class="language-plaintext highlighter-rouge">get()</code>을 바로 호출하면 결과가 나올 때까지 현재 Thread가 기다리므로 비동기 효과가 줄어들 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">String</span> <span class="n">result</span> <span class="o">=</span> <span class="n">future</span><span class="o">.</span><span class="na">join</span><span class="o">();</span> <span class="c1">// 이 지점에서 완료까지 대기</span>
</code></pre></div></div>

<hr />

<h3 id="5-thread-pool-설정">5. Thread Pool 설정</h3>

<p>운영 환경에서는 작업량에 맞는 Executor를 명시적으로 설정하는 것이 좋다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Configuration</span>
<span class="nd">@EnableAsync</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">AsyncConfig</span> <span class="o">{</span>

    <span class="nd">@Bean</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"emailExecutor"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">Executor</span> <span class="nf">emailExecutor</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">ThreadPoolTaskExecutor</span> <span class="n">executor</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ThreadPoolTaskExecutor</span><span class="o">();</span>

        <span class="n">executor</span><span class="o">.</span><span class="na">setCorePoolSize</span><span class="o">(</span><span class="mi">2</span><span class="o">);</span>
        <span class="n">executor</span><span class="o">.</span><span class="na">setMaxPoolSize</span><span class="o">(</span><span class="mi">5</span><span class="o">);</span>
        <span class="n">executor</span><span class="o">.</span><span class="na">setQueueCapacity</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span>
        <span class="n">executor</span><span class="o">.</span><span class="na">setThreadNamePrefix</span><span class="o">(</span><span class="s">"email-"</span><span class="o">);</span>
        <span class="n">executor</span><span class="o">.</span><span class="na">initialize</span><span class="o">();</span>

        <span class="k">return</span> <span class="n">executor</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Async</span><span class="o">(</span><span class="s">"emailExecutor"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">sendWelcomeEmail</span><span class="o">(</span><span class="nc">String</span> <span class="n">email</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// emailExecutor에서 실행</span>
<span class="o">}</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">corePoolSize</code></td>
      <td>기본적으로 유지할 Thread 수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">maxPoolSize</code></td>
      <td>최대로 늘어날 수 있는 Thread 수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">queueCapacity</code></td>
      <td>실행을 기다리는 Task의 Queue 크기</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">threadNamePrefix</code></td>
      <td>로그에서 Thread를 구분할 이름</td>
    </tr>
  </tbody>
</table>

<p>Thread를 많이 만들면 무조건 빨라지는 것이 아니다. CPU, 메모리, 외부 시스템 처리량을 고려해 설정해야 한다.</p>

<hr />

<h3 id="6-자기-호출-문제">6. 자기 호출 문제</h3>

<p><code class="language-plaintext highlighter-rouge">@Async</code>도 기본적으로 Proxy를 통해 동작한다.</p>

<p>같은 객체 안에서 비동기 메서드를 직접 호출하면 Proxy를 거치지 않아 현재 Thread에서 실행될 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">EmailService</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">registerAndSend</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">sendEmail</span><span class="o">();</span> <span class="c1">// 자기 호출</span>
    <span class="o">}</span>

    <span class="nd">@Async</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">sendEmail</span><span class="o">()</span> <span class="o">{</span>
        <span class="c1">// 비동기로 실행되지 않을 수 있음</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>비동기 메서드를 별도 Bean으로 분리하고 외부에서 호출한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UserService
  → EmailService Proxy
  → TaskExecutor
  → EmailService.sendEmail()
</code></pre></div></div>

<hr />

<h3 id="7-예외-처리">7. 예외 처리</h3>

<h4 id="completablefuture-반환"><code class="language-plaintext highlighter-rouge">CompletableFuture</code> 반환</h4>

<p>비동기 결과를 통해 예외를 처리할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reportService</span><span class="o">.</span><span class="na">createReport</span><span class="o">(</span><span class="n">userId</span><span class="o">)</span>
        <span class="o">.</span><span class="na">exceptionally</span><span class="o">(</span><span class="n">exception</span> <span class="o">-&gt;</span> <span class="o">{</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">exception</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
            <span class="k">return</span> <span class="s">"보고서 생성 실패"</span><span class="o">;</span>
        <span class="o">});</span>
</code></pre></div></div>

<h4 id="void-반환"><code class="language-plaintext highlighter-rouge">void</code> 반환</h4>

<p><code class="language-plaintext highlighter-rouge">void</code> 메서드에서 발생한 예외는 호출자에게 직접 전달할 수 없다. 기본적으로 로그에 남을 수 있으며, 필요하면 <code class="language-plaintext highlighter-rouge">AsyncUncaughtExceptionHandler</code>를 설정한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Override</span>
<span class="kd">public</span> <span class="nc">AsyncUncaughtExceptionHandler</span> <span class="nf">getAsyncUncaughtExceptionHandler</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">return</span> <span class="o">(</span><span class="n">exception</span><span class="o">,</span> <span class="n">method</span><span class="o">,</span> <span class="n">params</span><span class="o">)</span> <span class="o">-&gt;</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">method</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">exception</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
    <span class="o">};</span>
<span class="o">}</span>
</code></pre></div></div>

<p>중요한 비동기 작업은 실패 여부를 저장하고 재시도하거나 메시지 Queue를 사용하는 방식도 고려해야 한다.</p>

<hr />

<h3 id="8-트랜잭션과-async">8. 트랜잭션과 <code class="language-plaintext highlighter-rouge">@Async</code></h3>

<p>일반적인 Spring 트랜잭션은 현재 Thread에 연결된다. 따라서 호출자 Thread의 트랜잭션이 새 비동기 Thread로 자동 전달되지 않는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">register</span><span class="o">(</span><span class="nc">User</span> <span class="n">user</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">userRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
    <span class="n">emailService</span><span class="o">.</span><span class="na">sendWelcomeEmail</span><span class="o">(</span><span class="n">user</span><span class="o">.</span><span class="na">getEmail</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>요청 Thread
→ register()의 트랜잭션

비동기 Thread
→ 기존 트랜잭션을 자동으로 이어받지 않음
</code></pre></div></div>

<p>비동기 메서드에서 별도의 DB 트랜잭션이 필요하면 해당 비동기 Bean의 메서드에 트랜잭션 경계를 따로 설정한다.</p>

<p>또한 <code class="language-plaintext highlighter-rouge">register()</code>가 커밋되기 전에 비동기 작업이 시작될 수 있다. 반드시 커밋 후 실행해야 하는 작업이라면 트랜잭션 이벤트와 메시징 같은 방법을 고려해야 한다.</p>

<hr />

<h3 id="9-언제-사용하면-좋은가">9. 언제 사용하면 좋은가</h3>

<table>
  <thead>
    <tr>
      <th>적합한 작업</th>
      <th>주의할 작업</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>이메일과 알림 발송</td>
      <td>결과가 즉시 필요한 작업</td>
    </tr>
    <tr>
      <td>독립적인 외부 API 호출</td>
      <td>하나의 트랜잭션으로 묶여야 하는 DB 작업</td>
    </tr>
    <tr>
      <td>이미지 변환과 보고서 생성</td>
      <td>실패하면 반드시 즉시 사용자에게 알려야 하는 작업</td>
    </tr>
    <tr>
      <td>부가적인 로그 처리</td>
      <td>순서 보장이 중요한 작업</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">@Async</code>는 단일 서버의 메모리와 Thread Pool에 작업을 맡긴다. 서버가 종료되면 아직 처리하지 못한 작업을 잃을 수 있다. 반드시 처리해야 하는 작업은 Kafka, RabbitMQ 같은 메시지 시스템이나 별도의 작업 Queue가 더 적합할 수 있다.</p>

<hr />

<h3 id="핵심-정리">핵심 정리</h3>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 줄 정리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>비동기</td>
      <td>완료를 기다리지 않고 다른 실행 흐름에 작업을 맡김</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@EnableAsync</code></td>
      <td>Spring의 비동기 어노테이션 처리 활성화</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Async</code></td>
      <td>메서드 작업을 TaskExecutor에 제출</td>
    </tr>
    <tr>
      <td>TaskExecutor</td>
      <td>비동기 Task와 Thread를 관리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">CompletableFuture</code></td>
      <td>나중에 완료될 결과와 후속 작업 표현</td>
    </tr>
    <tr>
      <td>자기 호출</td>
      <td>같은 객체 내부 호출은 Async Proxy를 거치지 않음</td>
    </tr>
    <tr>
      <td>트랜잭션</td>
      <td>호출자 Thread의 트랜잭션이 비동기 Thread로 자동 전달되지 않음</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">@Async</code>의 핵심은 작업을 무조건 빠르게 만드는 것이 아니라, 기다리는 작업을 별도 실행 흐름에 맡기는 것이다.</p>
</blockquote>

<h3 id="참고-자료">참고 자료</h3>

<ul>
  <li><a href="https://docs.spring.io/spring-framework/reference/integration/scheduling.html">Spring Framework Task Execution and Scheduling</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="Async" /><category term="EnableAsync" /><category term="CompletableFuture" /><category term="TaskExecutor" /><category term="Thread Pool" /><summary type="html"><![CDATA[동기와 비동기, Thread와 Executor의 차이를 이해하고 Spring @Async의 설정, 반환값, 예외와 트랜잭션 경계를 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 9: JPA 트랜잭션과 동시성 제어</title><link href="http://localhost:4000/java/spring/2026/08/21/jpa-transaction-concurrency/" rel="alternate" type="text/html" title="Spring 기초 Part 9: JPA 트랜잭션과 동시성 제어" /><published>2026-08-21T09:00:00+09:00</published><updated>2026-08-21T09:00:00+09:00</updated><id>http://localhost:4000/java/spring/2026/08/21/jpa-transaction-concurrency</id><content type="html" xml:base="http://localhost:4000/java/spring/2026/08/21/jpa-transaction-concurrency/"><![CDATA[<h2 id="spring-기초-part-9-jpa-트랜잭션과-동시성-제어">Spring 기초 Part 9: JPA 트랜잭션과 동시성 제어</h2>

<blockquote>
  <p>트랜잭션은 여러 데이터베이스 작업을 하나의 성공 또는 실패 단위로 묶는 것이다.</p>
</blockquote>

<p>모든 작업이 성공하면 커밋하고, 중간에 실패하면 전체 작업을 롤백한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>주문 생성
   ↓
재고 감소
   ↓
결제 정보 저장
   ↓
모두 성공 → COMMIT
하나라도 실패 → ROLLBACK
</code></pre></div></div>

<hr />

<h3 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>트랜잭션</td>
      <td>여러 DB 작업을 묶은 논리적인 작업 단위</td>
      <td>주문 저장과 재고 감소를 함께 처리</td>
    </tr>
    <tr>
      <td>커밋</td>
      <td>트랜잭션의 변경 내용을 최종 확정</td>
      <td>성공한 작업을 DB에 반영</td>
    </tr>
    <tr>
      <td>롤백</td>
      <td>트랜잭션의 변경 내용을 취소</td>
      <td>실패 전 상태로 복구</td>
    </tr>
    <tr>
      <td>트랜잭션 경계</td>
      <td>트랜잭션이 시작되고 끝나는 범위</td>
      <td><code class="language-plaintext highlighter-rouge">@Transactional</code> 메서드</td>
    </tr>
    <tr>
      <td>전파</td>
      <td>이미 트랜잭션이 있을 때 참여 방식을 정하는 규칙</td>
      <td>기존 트랜잭션 참여 또는 새로 생성</td>
    </tr>
    <tr>
      <td>격리 수준</td>
      <td>다른 트랜잭션의 변경을 어디까지 볼지 정하는 수준</td>
      <td>커밋된 값만 읽기</td>
    </tr>
    <tr>
      <td>동시성</td>
      <td>여러 작업이 같은 시간에 실행되는 성질</td>
      <td>두 요청이 동시에 재고 차감</td>
    </tr>
    <tr>
      <td>락</td>
      <td>같은 데이터의 동시 접근을 제어하는 장치</td>
      <td>수정이 끝날 때까지 다른 작업 대기</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="2-acid-원칙">2. ACID 원칙</h3>

<table>
  <thead>
    <tr>
      <th>원칙</th>
      <th>의미</th>
      <th>쉬운 예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>원자성(Atomicity)</td>
      <td>작업이 모두 성공하거나 모두 실패해야 한다.</td>
      <td>출금과 입금이 함께 처리되어야 한다.</td>
    </tr>
    <tr>
      <td>일관성(Consistency)</td>
      <td>실행 전후에도 데이터 규칙을 만족해야 한다.</td>
      <td>재고가 음수가 되지 않아야 한다.</td>
    </tr>
    <tr>
      <td>격리성(Isolation)</td>
      <td>동시에 실행되는 트랜잭션의 중간 상태가 서로에게 영향을 주지 않아야 한다.</td>
      <td>다른 사용자의 미완료 결제를 읽지 않는다.</td>
    </tr>
    <tr>
      <td>지속성(Durability)</td>
      <td>커밋한 결과는 장애가 발생해도 보존돼야 한다.</td>
      <td>완료된 주문이 DB에 남는다.</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="3-transactional-동작-원리">3. <code class="language-plaintext highlighter-rouge">@Transactional</code> 동작 원리</h3>

<p><code class="language-plaintext highlighter-rouge">@Transactional</code>은 Spring이 트랜잭션 경계를 관리하도록 지정하는 어노테이션이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">OrderService</span> <span class="o">{</span>

    <span class="nd">@Transactional</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">createOrder</span><span class="o">(</span><span class="nc">Order</span> <span class="n">order</span><span class="o">,</span> <span class="nc">Product</span> <span class="n">product</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">orderRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">order</span><span class="o">);</span>
        <span class="n">product</span><span class="o">.</span><span class="na">decreaseStock</span><span class="o">(</span><span class="mi">1</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Spring의 선언적 트랜잭션은 일반적으로 AOP Proxy를 통해 동작한다.</p>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>Transactional Proxy 실행 흐름</span>
  </header>
  <div class="mermaid js-mermaid">
flowchart TD
    C[호출자] --&gt; P[Transactional Proxy]
    P --&gt; B[트랜잭션 시작]
    B --&gt; S[실제 Service 메서드]
    S --&gt; Q{정상 종료?}
    Q --&gt;|예| F[flush]
    F --&gt; COMMIT[commit]
    Q --&gt;|롤백 대상 예외| ROLLBACK[rollback]
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Proxy가 메서드 호출을 받음
          ↓
트랜잭션 시작
          ↓
실제 Service 메서드 실행
          ↓
정상 종료          롤백 대상 예외
   ↓                    ↓
COMMIT               ROLLBACK
</code></pre></div></div>

<p>같은 객체 안에서 <code class="language-plaintext highlighter-rouge">this.method()</code> 형태로 호출하면 Proxy를 거치지 않아 새 <code class="language-plaintext highlighter-rouge">@Transactional</code> 설정이 적용되지 않을 수 있다.</p>

<hr />

<h3 id="4-service에-트랜잭션을-적용하는-이유">4. Service에 트랜잭션을 적용하는 이유</h3>

<p><code class="language-plaintext highlighter-rouge">JpaRepository</code>의 기본 저장, 수정, 삭제 메서드에도 트랜잭션이 적용되어 있다. <code class="language-plaintext highlighter-rouge">save()</code> 하나만 호출하는 작업은 자체 트랜잭션으로 실행될 수 있다.</p>

<p>하지만 하나의 비즈니스 작업은 여러 Repository 호출로 구성될 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">transfer</span><span class="o">(</span><span class="nc">Long</span> <span class="n">fromId</span><span class="o">,</span> <span class="nc">Long</span> <span class="n">toId</span><span class="o">,</span> <span class="kt">int</span> <span class="n">amount</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Account</span> <span class="n">from</span> <span class="o">=</span> <span class="n">accountRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">fromId</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>
    <span class="nc">Account</span> <span class="n">to</span> <span class="o">=</span> <span class="n">accountRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">toId</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>

    <span class="n">from</span><span class="o">.</span><span class="na">withdraw</span><span class="o">(</span><span class="n">amount</span><span class="o">);</span>
    <span class="n">to</span><span class="o">.</span><span class="na">deposit</span><span class="o">(</span><span class="n">amount</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>출금과 입금은 함께 성공하거나 함께 실패해야 한다. 따라서 Repository 메서드 하나가 아니라 Service의 비즈니스 작업 전체를 트랜잭션으로 묶는다.</p>

<blockquote>
  <p>트랜잭션 경계는 데이터 접근 메서드 한 번이 아니라 하나의 완전한 업무 단위를 기준으로 잡는다.</p>
</blockquote>

<hr />

<h3 id="5-트랜잭션-범위는-짧게-유지한다">5. 트랜잭션 범위는 짧게 유지한다</h3>

<p>트랜잭션 안에서 외부 API나 파일 작업을 오래 수행하면 DB Connection과 Lock을 오랫동안 점유할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">processOrder</span><span class="o">(</span><span class="nc">Order</span> <span class="n">order</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">orderRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">order</span><span class="o">);</span>

    <span class="n">externalPaymentApi</span><span class="o">.</span><span class="na">call</span><span class="o">();</span> <span class="c1">// 응답이 오래 걸릴 수 있음</span>
    <span class="n">uploadReceiptFile</span><span class="o">();</span>       <span class="c1">// 파일 I/O</span>
<span class="o">}</span>
</code></pre></div></div>

<p>가능하면 DB 작업의 경계를 명확하게 분리한다.</p>

<table>
  <thead>
    <tr>
      <th>작업</th>
      <th>처리 방향</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DB 조회와 변경</td>
      <td>필요한 범위에 <code class="language-plaintext highlighter-rouge">@Transactional</code> 적용</td>
    </tr>
    <tr>
      <td>파일 저장</td>
      <td>DB 트랜잭션 밖에서 실행 고려</td>
    </tr>
    <tr>
      <td>외부 API 호출</td>
      <td>DB 트랜잭션 밖에서 실행하고 실패 복구 설계</td>
    </tr>
  </tbody>
</table>

<p>DB 커밋과 외부 시스템 호출은 하나의 로컬 트랜잭션으로 원자적으로 묶이지 않는다. 실제 시스템에서는 재시도, 보상 처리, 이벤트, Outbox 패턴 등을 추가로 고려할 수 있다.</p>

<hr />

<h3 id="6-readonly--true">6. <code class="language-plaintext highlighter-rouge">readOnly = true</code></h3>

<p>클래스에 조회 전용 설정을 두고 쓰기 메서드에서 덮어쓸 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="nd">@Transactional</span><span class="o">(</span><span class="n">readOnly</span> <span class="o">=</span> <span class="kc">true</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">getUser</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>
        <span class="k">return</span> <span class="nc">UserResponse</span><span class="o">.</span><span class="na">from</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Transactional</span>
    <span class="kd">public</span> <span class="nc">Long</span> <span class="nf">createUser</span><span class="o">(</span><span class="nc">User</span> <span class="n">user</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">user</span><span class="o">).</span><span class="na">getId</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">readOnly = true</code>는 조회 전용 의도를 표현하고 JPA 구현체나 DB 설정에 따라 flush와 변경 감지 비용을 줄이는 최적화에 활용될 수 있다.</p>

<blockquote>
  <p>모든 환경에서 쓰기 SQL을 완전히 차단하는 보안 장치는 아니다.</p>
</blockquote>

<hr />

<h3 id="7-flush와-commit">7. flush와 commit</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>flush</td>
      <td>영속성 컨텍스트의 변경 내용을 SQL로 DB에 전달</td>
    </tr>
    <tr>
      <td>commit</td>
      <td>트랜잭션 변경 내용을 최종 확정</td>
    </tr>
  </tbody>
</table>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Entity 변경
   ↓
flush
   ↓
UPDATE SQL 전달
   ↓
commit
</code></pre></div></div>

<p>flush가 실행된 뒤에도 commit 전에 오류가 발생하면 rollback할 수 있다.</p>

<hr />

<h3 id="8-예외와-롤백-규칙">8. 예외와 롤백 규칙</h3>

<p>Spring의 <code class="language-plaintext highlighter-rouge">@Transactional</code>은 기본적으로 <code class="language-plaintext highlighter-rouge">RuntimeException</code>과 <code class="language-plaintext highlighter-rouge">Error</code>가 메서드 밖으로 전달될 때 롤백한다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>예시</th>
      <th style="text-align: center">기본 동작</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Error</code></td>
      <td><code class="language-plaintext highlighter-rouge">OutOfMemoryError</code></td>
      <td style="text-align: center">롤백</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">RuntimeException</code></td>
      <td><code class="language-plaintext highlighter-rouge">IllegalArgumentException</code></td>
      <td style="text-align: center">롤백</td>
    </tr>
    <tr>
      <td>Checked Exception</td>
      <td><code class="language-plaintext highlighter-rouge">IOException</code>, <code class="language-plaintext highlighter-rouge">SQLException</code></td>
      <td style="text-align: center">커밋</td>
    </tr>
  </tbody>
</table>

<p>Checked Exception도 롤백하려면 <code class="language-plaintext highlighter-rouge">rollbackFor</code>를 지정한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span><span class="o">(</span><span class="n">rollbackFor</span> <span class="o">=</span> <span class="nc">Exception</span><span class="o">.</span><span class="na">class</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">process</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">MyCustomException</span> <span class="o">{</span>
    <span class="c1">// DB 작업</span>
<span class="o">}</span>
</code></pre></div></div>

<p>특정 예외를 롤백 대상에서 제외할 수도 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span><span class="o">(</span><span class="n">noRollbackFor</span> <span class="o">=</span> <span class="nc">NotificationException</span><span class="o">.</span><span class="na">class</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">process</span><span class="o">()</span> <span class="o">{</span>
    <span class="c1">// DB 작업</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="예외를-메서드-안에서-잡으면">예외를 메서드 안에서 잡으면</h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">process</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="n">orderRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">order</span><span class="o">);</span>
        <span class="n">paymentService</span><span class="o">.</span><span class="na">pay</span><span class="o">();</span>
    <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">RuntimeException</span> <span class="n">exception</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// 예외를 다시 던지지 않음</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>메서드가 정상 반환하면 Proxy는 예외가 발생했다는 사실을 알 수 없다. 롤백이 필요하다면 예외를 다시 던지거나 트랜잭션을 rollback-only 상태로 표시해야 한다.</p>

<hr />

<h3 id="9-트랜잭션-전파">9. 트랜잭션 전파</h3>

<p>전파(Propagation)는 이미 트랜잭션이 있을 때 새로 호출된 메서드가 그 트랜잭션을 어떻게 사용할지 정하는 규칙이다.</p>

<table>
  <thead>
    <tr>
      <th>옵션</th>
      <th>설명</th>
      <th>사용 예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">REQUIRED</code></td>
      <td>기존 트랜잭션이 있으면 참여하고, 없으면 새로 만든다.</td>
      <td>대부분의 Service 로직</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">REQUIRES_NEW</code></td>
      <td>기존 트랜잭션을 일시 중단하고 독립된 새 트랜잭션을 만든다.</td>
      <td>독립적으로 저장할 감사 로그</td>
    </tr>
  </tbody>
</table>

<h4 id="required"><code class="language-plaintext highlighter-rouge">REQUIRED</code></h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">createOrder</span><span class="o">()</span> <span class="o">{</span>
    <span class="n">orderRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">order</span><span class="o">);</span>
    <span class="n">paymentService</span><span class="o">.</span><span class="na">pay</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">paymentService.pay()</code>도 <code class="language-plaintext highlighter-rouge">REQUIRED</code>라면 바깥 트랜잭션에 참여한다. 내부 작업이 rollback-only로 표시되면 최종 커밋 시 <code class="language-plaintext highlighter-rouge">UnexpectedRollbackException</code>이 발생할 수 있다.</p>

<h4 id="requires_new"><code class="language-plaintext highlighter-rouge">REQUIRES_NEW</code></h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span><span class="o">(</span><span class="n">propagation</span> <span class="o">=</span> <span class="nc">Propagation</span><span class="o">.</span><span class="na">REQUIRES_NEW</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">saveAuditLog</span><span class="o">(</span><span class="nc">AuditLog</span> <span class="n">log</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">auditLogRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">log</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>새로운 물리 트랜잭션을 사용하므로 외부 트랜잭션과 독립적으로 커밋하거나 롤백할 수 있다.</p>

<p>다만 외부 트랜잭션의 자원은 유지한 채 새 DB Connection을 추가로 사용할 수 있다. 남발하면 Connection Pool 부족이나 Lock 대기가 발생할 수 있다.</p>

<p>또한 같은 객체의 내부 호출로는 전파 설정이 적용되지 않을 수 있으므로 별도 Bean을 통해 호출해야 한다.</p>

<hr />

<h3 id="10-동시성-문제">10. 동시성 문제</h3>

<p><code class="language-plaintext highlighter-rouge">@Transactional</code>은 작업 범위를 묶어주지만 모든 동시성 문제를 자동으로 해결하지는 않는다.</p>

<table>
  <thead>
    <tr>
      <th>문제</th>
      <th>설명</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>갱신 손실(Lost Update)</td>
      <td>나중에 저장한 값이 먼저 저장한 값을 덮어씀</td>
      <td>두 요청이 동시에 포인트 증가</td>
    </tr>
    <tr>
      <td>더티 리드(Dirty Read)</td>
      <td>커밋되지 않은 값을 다른 트랜잭션이 읽음</td>
      <td>곧 롤백될 값을 조회</td>
    </tr>
    <tr>
      <td>반복 읽기 불일치</td>
      <td>같은 행을 다시 읽었을 때 값이 달라짐</td>
      <td>포인트가 100에서 200으로 변경</td>
    </tr>
    <tr>
      <td>유령 읽기(Phantom Read)</td>
      <td>같은 조건의 재조회에서 행 개수가 달라짐</td>
      <td>회원이 10명에서 11명으로 증가</td>
    </tr>
    <tr>
      <td>경쟁 상태(Race Condition)</td>
      <td>실행 순서에 따라 업무 결과가 달라짐</td>
      <td>동시 재고 감소로 음수 발생</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="11-격리-수준">11. 격리 수준</h3>

<p>격리 수준은 다른 트랜잭션의 변경 내용을 어디까지 볼 수 있는지 정한다.</p>

<table>
  <thead>
    <tr>
      <th>격리 수준</th>
      <th>특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">READ_UNCOMMITTED</code></td>
      <td>커밋하지 않은 데이터도 읽을 수 있다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">READ_COMMITTED</code></td>
      <td>커밋된 데이터만 읽어 Dirty Read를 방지한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">REPEATABLE_READ</code></td>
      <td>같은 행을 반복 조회할 때 같은 결과를 보장한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">SERIALIZABLE</code></td>
      <td>트랜잭션을 순차 실행한 것처럼 강하게 격리한다.</td>
    </tr>
  </tbody>
</table>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span><span class="o">(</span><span class="n">isolation</span> <span class="o">=</span> <span class="nc">Isolation</span><span class="o">.</span><span class="na">READ_COMMITTED</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">process</span><span class="o">()</span> <span class="o">{</span>
    <span class="c1">// DB 작업</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Spring의 <code class="language-plaintext highlighter-rouge">Isolation.DEFAULT</code>는 사용하는 DB의 기본 격리 수준을 따른다. 격리 수준을 높이면 정합성은 강해지지만 대기와 성능 비용이 증가할 수 있다.</p>

<hr />

<h3 id="12-낙관적-락">12. 낙관적 락</h3>

<p>낙관적 락은 충돌이 드물다고 가정하고 버전 값으로 수정 충돌을 감지한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Entity</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Product</span> <span class="o">{</span>

    <span class="nd">@Id</span>
    <span class="nd">@GeneratedValue</span><span class="o">(</span><span class="n">strategy</span> <span class="o">=</span> <span class="nc">GenerationType</span><span class="o">.</span><span class="na">IDENTITY</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">;</span>

    <span class="kd">private</span> <span class="kt">int</span> <span class="n">stock</span><span class="o">;</span>

    <span class="nd">@Version</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">version</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>JPA는 수정할 때 조회 당시 버전을 조건에 포함한다.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">UPDATE</span> <span class="n">product</span>
<span class="k">SET</span> <span class="n">stock</span> <span class="o">=</span> <span class="mi">9</span><span class="p">,</span>
    <span class="k">version</span> <span class="o">=</span> <span class="mi">2</span>
<span class="k">WHERE</span> <span class="n">id</span> <span class="o">=</span> <span class="mi">1</span>
  <span class="k">AND</span> <span class="k">version</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div>

<p>다른 트랜잭션이 먼저 수정해 버전이 달라졌다면 충돌 예외가 발생한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A와 B가 version = 1 조회
        ↓
A가 수정하고 version = 2로 커밋
        ↓
B가 version = 1 조건으로 수정
        ↓
수정 실패와 낙관적 락 예외
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DB Lock을 오래 잡지 않아 읽기가 많은 환경에 유리</td>
      <td>충돌 시 예외 처리와 제한된 재시도 필요</td>
    </tr>
  </tbody>
</table>

<p>재시도할 때는 최신 데이터를 새 트랜잭션에서 다시 조회하고 업무 조건을 다시 검사해야 한다.</p>

<hr />

<h3 id="13-비관적-락">13. 비관적 락</h3>

<p>비관적 락은 충돌이 자주 발생한다고 가정하고 조회 시점에 DB Lock을 획득한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">ProductRepository</span> <span class="kd">extends</span> <span class="nc">JpaRepository</span><span class="o">&lt;</span><span class="nc">Product</span><span class="o">,</span> <span class="nc">Long</span><span class="o">&gt;</span> <span class="o">{</span>

    <span class="nd">@Lock</span><span class="o">(</span><span class="nc">LockModeType</span><span class="o">.</span><span class="na">PESSIMISTIC_WRITE</span><span class="o">)</span>
    <span class="nd">@Query</span><span class="o">(</span><span class="s">"select p from Product p where p.id = :id"</span><span class="o">)</span>
    <span class="nc">Optional</span><span class="o">&lt;</span><span class="nc">Product</span><span class="o">&gt;</span> <span class="nf">findByIdWithWriteLock</span><span class="o">(</span><span class="nd">@Param</span><span class="o">(</span><span class="s">"id"</span><span class="o">)</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">decreaseStock</span><span class="o">(</span><span class="nc">Long</span> <span class="n">productId</span><span class="o">,</span> <span class="kt">int</span> <span class="n">quantity</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Product</span> <span class="n">product</span> <span class="o">=</span> <span class="n">productRepository</span>
            <span class="o">.</span><span class="na">findByIdWithWriteLock</span><span class="o">(</span><span class="n">productId</span><span class="o">)</span>
            <span class="o">.</span><span class="na">orElseThrow</span><span class="o">();</span>

    <span class="n">product</span><span class="o">.</span><span class="na">decreaseStock</span><span class="o">(</span><span class="n">quantity</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>장점</th>
      <th>단점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>충돌이 잦은 상황에서 동시 수정을 순서대로 처리하기 쉬움</td>
      <td>대기 시간, 성능 저하, Timeout과 Deadlock 가능</td>
    </tr>
  </tbody>
</table>

<p>Lock의 실제 동작은 DB 종류와 격리 수준에 따라 달라질 수 있다.</p>

<hr />

<h3 id="14-해결-방법-선택">14. 해결 방법 선택</h3>

<table>
  <thead>
    <tr>
      <th>해결 방법</th>
      <th>적합한 상황</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>낙관적 락</td>
      <td>충돌은 드물고 읽기가 많은 수정</td>
    </tr>
    <tr>
      <td>비관적 락</td>
      <td>좌석 예약처럼 같은 데이터 충돌이 잦은 작업</td>
    </tr>
    <tr>
      <td>원자적 쿼리</td>
      <td>단순한 증가나 감소를 한 SQL로 처리할 수 있는 작업</td>
    </tr>
    <tr>
      <td>Unique 제약조건</td>
      <td>이메일, 주문 번호 등의 중복 방지</td>
    </tr>
    <tr>
      <td>격리 수준 조정</td>
      <td>트랜잭션 사이의 조회 일관성이 중요한 작업</td>
    </tr>
  </tbody>
</table>

<p>단순 재고 감소는 조건을 포함한 원자적 SQL로 처리할 수도 있다.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">UPDATE</span> <span class="n">product</span>
<span class="k">SET</span> <span class="n">stock</span> <span class="o">=</span> <span class="n">stock</span> <span class="o">-</span> <span class="mi">1</span>
<span class="k">WHERE</span> <span class="n">id</span> <span class="o">=</span> <span class="mi">1</span>
  <span class="k">AND</span> <span class="n">stock</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span>
</code></pre></div></div>

<p>수정된 행이 <code class="language-plaintext highlighter-rouge">0</code>개라면 재고가 없거나 상품이 없는 경우로 처리한다.</p>

<hr />

<h3 id="핵심-정리">핵심 정리</h3>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 줄 정리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>트랜잭션</td>
      <td>여러 DB 작업을 하나의 성공 또는 실패 단위로 묶음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Transactional</code></td>
      <td>Proxy가 트랜잭션의 시작, 커밋, 롤백을 관리</td>
    </tr>
    <tr>
      <td>롤백</td>
      <td>기본적으로 unchecked exception과 <code class="language-plaintext highlighter-rouge">Error</code>가 전달될 때 실행</td>
    </tr>
    <tr>
      <td>전파</td>
      <td>기존 트랜잭션 참여 여부와 새 트랜잭션 생성을 결정</td>
    </tr>
    <tr>
      <td>격리 수준</td>
      <td>다른 트랜잭션의 변경을 어디까지 볼지 결정</td>
    </tr>
    <tr>
      <td>낙관적 락</td>
      <td>버전으로 수정 충돌 감지</td>
    </tr>
    <tr>
      <td>비관적 락</td>
      <td>DB Lock으로 동시 수정을 제한</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">@Transactional</code>만 붙인다고 모든 문제가 해결되지는 않는다. 업무 단위, 롤백 규칙, 전파, 격리 수준과 동시성 제어 방법을 함께 선택해야 한다.</p>
</blockquote>

<h3 id="참고-자료">참고 자료</h3>

<ul>
  <li><a href="https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative.html">Spring Framework 선언적 트랜잭션 공식 문서</a></li>
  <li><a href="https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/tx-propagation.html">Spring Framework 트랜잭션 전파 공식 문서</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="JPA" /><category term="Transaction" /><category term="Transactional" /><category term="Concurrency" /><category term="Lock" /><summary type="html"><![CDATA[Transactional의 동작, 커밋과 롤백, 전파, 격리 수준, 낙관적 락과 비관적 락을 하나의 흐름으로 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 8: AOP와 공통 관심사 분리</title><link href="http://localhost:4000/java/spring/2026/08/20/spring-aop/" rel="alternate" type="text/html" title="Spring 기초 Part 8: AOP와 공통 관심사 분리" /><published>2026-08-20T09:00:00+09:00</published><updated>2026-08-20T09:00:00+09:00</updated><id>http://localhost:4000/java/spring/2026/08/20/spring-aop</id><content type="html" xml:base="http://localhost:4000/java/spring/2026/08/20/spring-aop/"><![CDATA[<h2 id="spring-기초-part-8-aop와-공통-관심사-분리">Spring 기초 Part 8: AOP와 공통 관심사 분리</h2>

<blockquote>
  <p>AOP는 여러 기능에서 반복되는 공통 관심사를 핵심 비즈니스 로직과 분리하는 프로그래밍 방식이다.</p>
</blockquote>

<p>Service 메서드의 핵심 목적은 주문, 결제, 회원 조회 같은 업무를 처리하는 것이다.</p>

<p>하지만 로그, 권한 검사, 실행 시간 측정 같은 코드를 모든 메서드에 반복하면 핵심 로직이 흐려진다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>핵심 관심사
→ 주문, 결제, 회원 조회

공통 관심사
→ 로그, 보안, 트랜잭션, 실행 시간 측정
</code></pre></div></div>

<hr />

<h3 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h3>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>핵심 질문</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Aspect</td>
      <td>공통 기능과 적용 규칙을 모아놓은 모듈</td>
      <td>어떤 공통 기능인가?</td>
    </tr>
    <tr>
      <td>Target</td>
      <td>공통 기능이 적용될 실제 객체</td>
      <td>누구를 감쌀 것인가?</td>
    </tr>
    <tr>
      <td>Join Point</td>
      <td>공통 기능을 적용할 수 있는 실행 지점</td>
      <td>어느 실행 지점인가?</td>
    </tr>
    <tr>
      <td>Pointcut</td>
      <td>적용할 메서드를 선택하는 조건</td>
      <td>어디에 적용할 것인가?</td>
    </tr>
    <tr>
      <td>Advice</td>
      <td>선택된 지점에서 실행할 공통 기능</td>
      <td>언제 무엇을 실행할 것인가?</td>
    </tr>
    <tr>
      <td>Weaving</td>
      <td>공통 기능을 실제 코드 실행에 연결하는 과정</td>
      <td>어떻게 결합하는가?</td>
    </tr>
    <tr>
      <td>Proxy</td>
      <td>Target 호출을 가로채 Advice를 실행하는 대리 객체</td>
      <td>실제로 누가 가로채는가?</td>
    </tr>
  </tbody>
</table>

<p>Spring AOP에서는 주로 메서드 실행이 Join Point가 된다.</p>

<hr />

<h3 id="2-전체-동작-구조">2. 전체 동작 구조</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Controller
    ↓
Spring Proxy
    ↓ Before Advice
실제 Service 메서드
    ↓ After Advice
Spring Proxy
    ↓
Controller
</code></pre></div></div>

<p>Spring은 AOP 설정을 읽고 적용 대상 Bean을 Proxy로 감싼다.</p>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>Spring AOP 실행 흐름</span>
  </header>
  <div class="mermaid js-mermaid">
flowchart TD
    C[Controller] --&gt; P[Spring AOP Proxy]
    P --&gt; B[Before Advice]
    B --&gt; T[Target Service]
    T --&gt; A[After Advice]
    A --&gt; P
    P --&gt; C
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<blockquote>
  <p>AOP는 무엇을 어디에 적용할지 정의하고, Spring은 그 설정을 바탕으로 Proxy를 만들며, Proxy가 실제 호출을 가로챈다.</p>
</blockquote>

<hr />

<h3 id="3-aspect-만들기">3. Aspect 만들기</h3>

<p>AOP 기능을 사용하려면 일반적으로 AOP Starter를 추가한다.</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;dependency&gt;</span>
    <span class="nt">&lt;groupId&gt;</span>org.springframework.boot<span class="nt">&lt;/groupId&gt;</span>
    <span class="nt">&lt;artifactId&gt;</span>spring-boot-starter-aop<span class="nt">&lt;/artifactId&gt;</span>
<span class="nt">&lt;/dependency&gt;</span>
</code></pre></div></div>

<p>Aspect 클래스는 다음처럼 시작한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Aspect</span>
<span class="nd">@Component</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">LoggingAspect</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>어노테이션</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Aspect</code></td>
      <td>이 클래스가 AOP 설정과 Advice를 담는 Aspect임을 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Component</code></td>
      <td>Spring이 객체를 생성하고 Bean으로 관리하도록 등록</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">@Aspect</code>만 붙이고 Bean으로 등록하지 않으면 Spring AOP가 해당 객체를 사용하지 못한다.</p>

<hr />

<h3 id="4-pointcut">4. Pointcut</h3>

<p>Pointcut은 AOP를 적용할 메서드를 고르는 조건이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Pointcut</span><span class="o">(</span><span class="s">"execution(* com.example.service.*.*(..))"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">serviceMethods</span><span class="o">()</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">serviceMethods()</code>를 실제 업무 메서드로 호출하는 것이 아니다. 긴 Pointcut 표현식에 이름을 붙인 것이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>execution(
    *                         반환 타입은 무엇이든 가능
    com.example.service.*    service 패키지의 모든 클래스
    .*                        모든 메서드
    (..)                      매개변수 개수와 타입은 무엇이든 가능
)
</code></pre></div></div>

<p>이 이름을 여러 Advice에서 재사용할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Before</span><span class="o">(</span><span class="s">"serviceMethods()"</span><span class="o">)</span>
<span class="nd">@After</span><span class="o">(</span><span class="s">"serviceMethods()"</span><span class="o">)</span>
</code></pre></div></div>

<p>Pointcut 범위를 지나치게 넓게 지정하면 원하지 않는 메서드까지 Proxy 대상이 될 수 있다. 실제 패키지와 클래스 범위를 정확히 확인해야 한다.</p>

<hr />

<h3 id="5-advice-종류">5. Advice 종류</h3>

<table>
  <thead>
    <tr>
      <th>Advice</th>
      <th>실행 시점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Before</code></td>
      <td>Target 메서드 실행 전</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@After</code></td>
      <td>정상 종료와 예외 여부와 관계없이 실행 후</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@AfterReturning</code></td>
      <td>Target 메서드가 정상 반환한 후</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@AfterThrowing</code></td>
      <td>Target 메서드에서 예외가 발생한 후</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Around</code></td>
      <td>Target 메서드 전체를 감싸고 전후를 직접 제어</td>
    </tr>
  </tbody>
</table>

<h4 id="before"><code class="language-plaintext highlighter-rouge">@Before</code></h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Before</span><span class="o">(</span><span class="s">"serviceMethods()"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">beforeAdvice</span><span class="o">(</span><span class="nc">JoinPoint</span> <span class="n">joinPoint</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"[Before] "</span> <span class="o">+</span> <span class="n">joinPoint</span><span class="o">.</span><span class="na">getSignature</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>beforeAdvice()
      ↓
실제 메서드
</code></pre></div></div>

<h4 id="afterreturning"><code class="language-plaintext highlighter-rouge">@AfterReturning</code></h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@AfterReturning</span><span class="o">(</span>
        <span class="n">pointcut</span> <span class="o">=</span> <span class="s">"serviceMethods()"</span><span class="o">,</span>
        <span class="n">returning</span> <span class="o">=</span> <span class="s">"result"</span>
<span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">afterReturning</span><span class="o">(</span><span class="nc">JoinPoint</span> <span class="n">joinPoint</span><span class="o">,</span> <span class="nc">Object</span> <span class="n">result</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"[Success] "</span> <span class="o">+</span> <span class="n">joinPoint</span><span class="o">.</span><span class="na">getSignature</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<p>정상적으로 값을 반환했을 때만 실행된다.</p>

<h4 id="afterthrowing"><code class="language-plaintext highlighter-rouge">@AfterThrowing</code></h4>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@AfterThrowing</span><span class="o">(</span>
        <span class="n">pointcut</span> <span class="o">=</span> <span class="s">"serviceMethods()"</span><span class="o">,</span>
        <span class="n">throwing</span> <span class="o">=</span> <span class="s">"exception"</span>
<span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">afterThrowing</span><span class="o">(</span>
        <span class="nc">JoinPoint</span> <span class="n">joinPoint</span><span class="o">,</span>
        <span class="nc">Throwable</span> <span class="n">exception</span><span class="o">)</span> <span class="o">{</span>

    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"[Failure] "</span> <span class="o">+</span> <span class="n">joinPoint</span><span class="o">.</span><span class="na">getSignature</span><span class="o">());</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">exception</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<p>예외가 발생했을 때 로그를 남기는 용도로 사용할 수 있다.</p>

<hr />

<h3 id="6-around">6. <code class="language-plaintext highlighter-rouge">@Around</code></h3>

<p><code class="language-plaintext highlighter-rouge">@Around</code>는 실제 메서드 실행 전체를 감싼다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Around</span><span class="o">(</span><span class="s">"serviceMethods()"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">Object</span> <span class="nf">measureExecutionTime</span><span class="o">(</span>
        <span class="nc">ProceedingJoinPoint</span> <span class="n">joinPoint</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Throwable</span> <span class="o">{</span>

    <span class="kt">long</span> <span class="n">start</span> <span class="o">=</span> <span class="nc">System</span><span class="o">.</span><span class="na">nanoTime</span><span class="o">();</span>

    <span class="k">try</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">joinPoint</span><span class="o">.</span><span class="na">proceed</span><span class="o">();</span>
    <span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
        <span class="kt">long</span> <span class="n">end</span> <span class="o">=</span> <span class="nc">System</span><span class="o">.</span><span class="na">nanoTime</span><span class="o">();</span>
        <span class="kt">long</span> <span class="n">elapsed</span> <span class="o">=</span> <span class="n">end</span> <span class="o">-</span> <span class="n">start</span><span class="o">;</span>

        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span>
                <span class="n">joinPoint</span><span class="o">.</span><span class="na">getSignature</span><span class="o">()</span> <span class="o">+</span> <span class="s">" : "</span> <span class="o">+</span> <span class="n">elapsed</span> <span class="o">+</span> <span class="s">"ns"</span>
        <span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>실행 순서는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Around Advice 시작
       ↓
실행 전 코드
       ↓
joinPoint.proceed()
       ↓
실제 Target 메서드
       ↓
실행 후 코드
       ↓
결과 반환
</code></pre></div></div>

<p>다음 호출이 가장 중요하다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">joinPoint</span><span class="o">.</span><span class="na">proceed</span><span class="o">();</span>
</code></pre></div></div>

<p>이 코드를 실행하지 않으면 실제 Target 메서드가 실행되지 않는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Around</span><span class="o">(</span><span class="s">"serviceMethods()"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">Object</span> <span class="nf">wrongAdvice</span><span class="o">(</span><span class="nc">ProceedingJoinPoint</span> <span class="n">joinPoint</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"호출을 가로챔"</span><span class="o">);</span>
    <span class="k">return</span> <span class="kc">null</span><span class="o">;</span> <span class="c1">// Target을 호출하지 않음</span>
<span class="o">}</span>
</code></pre></div></div>

<p>반환값을 임의로 없애거나 예외를 삼키면 기존 메서드의 동작을 깨뜨릴 수 있다.</p>

<hr />

<h3 id="7-실제-로그-aspect-예제">7. 실제 로그 Aspect 예제</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Aspect</span>
<span class="nd">@Component</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ServiceLoggingAspect</span> <span class="o">{</span>

    <span class="nd">@Pointcut</span><span class="o">(</span><span class="s">"execution(* com.example.service..*(..))"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">serviceMethods</span><span class="o">()</span> <span class="o">{</span>
    <span class="o">}</span>

    <span class="nd">@Around</span><span class="o">(</span><span class="s">"serviceMethods()"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">Object</span> <span class="nf">log</span><span class="o">(</span><span class="nc">ProceedingJoinPoint</span> <span class="n">joinPoint</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Throwable</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">method</span> <span class="o">=</span> <span class="n">joinPoint</span><span class="o">.</span><span class="na">getSignature</span><span class="o">().</span><span class="na">toShortString</span><span class="o">();</span>

        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"[START] "</span> <span class="o">+</span> <span class="n">method</span><span class="o">);</span>

        <span class="k">try</span> <span class="o">{</span>
            <span class="nc">Object</span> <span class="n">result</span> <span class="o">=</span> <span class="n">joinPoint</span><span class="o">.</span><span class="na">proceed</span><span class="o">();</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"[SUCCESS] "</span> <span class="o">+</span> <span class="n">method</span><span class="o">);</span>
            <span class="k">return</span> <span class="n">result</span><span class="o">;</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Throwable</span> <span class="n">exception</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"[FAILURE] "</span> <span class="o">+</span> <span class="n">method</span><span class="o">);</span>
            <span class="k">throw</span> <span class="n">exception</span><span class="o">;</span>
        <span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"[END] "</span> <span class="o">+</span> <span class="n">method</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>예외를 기록한 뒤 다시 던져야 Controller의 예외 처리와 트랜잭션 롤백 흐름이 유지된다.</p>

<hr />

<h3 id="8-aop와-proxy의-역할-구분">8. AOP와 Proxy의 역할 구분</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>AOP
→ 로그 기능을 Service 패키지의 메서드에 적용하겠다고 정의

Spring
→ 설정을 읽고 적용 대상 Bean을 확인

Proxy
→ 실제 메서드 호출을 가로채 Advice 실행

Target
→ 원래 비즈니스 로직 실행
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>핵심 역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>AOP</td>
      <td>공통 기능과 적용 범위를 정의</td>
    </tr>
    <tr>
      <td>Spring</td>
      <td>Aspect를 읽고 Proxy를 구성</td>
    </tr>
    <tr>
      <td>Proxy</td>
      <td>호출을 가로채 Advice 실행</td>
    </tr>
    <tr>
      <td>Target</td>
      <td>원래 업무 로직 실행</td>
    </tr>
  </tbody>
</table>

<p>Spring AOP가 Proxy를 사용한다고 해서 모든 AOP 구현이 Proxy만 사용하는 것은 아니다. AspectJ처럼 컴파일 시점이나 로딩 시점에 코드를 결합하는 방식도 있다.</p>

<hr />

<h3 id="9-주의할-점">9. 주의할 점</h3>

<h4 id="자기-호출">자기 호출</h4>

<p>같은 객체의 내부 호출은 Spring Proxy를 다시 거치지 않으므로 Advice가 적용되지 않을 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">outer</span><span class="o">()</span> <span class="o">{</span>
    <span class="n">inner</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="중복-proxy">중복 Proxy</h4>

<p>같은 기능에 수동 Proxy와 Spring AOP를 동시에 적용하면 로그나 실행 시간 측정이 두 번 실행될 수 있다.</p>

<h4 id="민감-정보-로그">민감 정보 로그</h4>

<p>메서드 인자를 전부 기록하면 비밀번호, 토큰, 개인정보가 로그에 남을 수 있다. 기록 대상을 제한해야 한다.</p>

<h4 id="aop에-업무-로직-넣지-않기">AOP에 업무 로직 넣지 않기</h4>

<p>AOP는 반복되는 기반 기능에 적합하다. 주문 금액 계산 같은 핵심 업무 규칙은 Service나 도메인 객체에 둔다.</p>

<hr />

<h3 id="핵심-정리">핵심 정리</h3>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 줄 정리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Aspect</td>
      <td>공통 기능과 적용 규칙을 모은 모듈</td>
    </tr>
    <tr>
      <td>Pointcut</td>
      <td>적용할 메서드를 선택하는 조건</td>
    </tr>
    <tr>
      <td>Advice</td>
      <td>선택된 메서드에서 실행할 공통 기능</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Around</code></td>
      <td>실제 메서드 전체를 감싸는 Advice</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">proceed()</code></td>
      <td>실제 Target 메서드를 실행</td>
    </tr>
    <tr>
      <td>Proxy</td>
      <td>Advice와 Target 호출을 실제로 연결하는 대리 객체</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>AOP는 공통 기능을 핵심 로직에서 분리하고, Spring AOP는 Proxy를 이용해 선택한 메서드의 앞뒤에서 그 기능을 실행한다.</p>
</blockquote>

<h3 id="참고-자료">참고 자료</h3>

<ul>
  <li><a href="https://docs.spring.io/spring-framework/reference/core/aop.html">Spring Framework AOP 공식 문서</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="AOP" /><category term="Aspect" /><category term="Pointcut" /><category term="Advice" /><category term="Proxy" /><summary type="html"><![CDATA[Aspect, Pointcut, Advice, JoinPoint의 의미와 Before, After, Around Advice가 Proxy를 통해 동작하는 흐름을 정리한다.]]></summary></entry></feed>