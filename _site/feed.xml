<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-08-11T17:46:41+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Minnong’s Study Log</title><subtitle>개발과 AI를 배우고, 실험하고, 이해한 내용을 연결하는 공부 기록</subtitle><author><name>Min Hyeong Lee</name></author><entry><title type="html">Java 심화 Part 3: 함수형 프로그래밍과 Lambda</title><link href="http://localhost:4000/java/advanced/2026/08/11/java-functional-programming-lambda/" rel="alternate" type="text/html" title="Java 심화 Part 3: 함수형 프로그래밍과 Lambda" /><published>2026-08-11T11:40:00+09:00</published><updated>2026-08-11T11:40:00+09:00</updated><id>http://localhost:4000/java/advanced/2026/08/11/java-functional-programming-lambda</id><content type="html" xml:base="http://localhost:4000/java/advanced/2026/08/11/java-functional-programming-lambda/"><![CDATA[<h1 id="java-심화-part-3-함수형-프로그래밍과-lambda">Java 심화 Part 3: 함수형 프로그래밍과 Lambda</h1>

<ul>
  <li>Java의 함수형 프로그래밍(Functional Programming)은 함수를 일급 객체(First-Class Citizen)로 취급, 불변성(immunability)과 순수 함수(Pure function)를 기반으로 프로그램을 작성하는 방식</li>
</ul>

<h3 id="first-class-citizen">First-Class Citizen</h3>
<ul>
  <li>함수를 변수에 저장, 전달, 반환할 수 있음</li>
  <li>함수 자체를 데이터처럼 다루는 방식</li>
</ul>

<h3 id="pure-function">Pure Function</h3>
<ul>
  <li>같은 입력 -&gt; 같은 출력</li>
  <li>외부 상태를 변경하지 않음 (Side Effect 없음)</li>
</ul>

<h3 id="immmunability">Immmunability</h3>
<ul>
  <li>함수 내부에서 외부 변수를 변경하지 않는 구조</li>
  <li>병렬 처리에서 안정성과 예측 가능성이 높아진다.</li>
</ul>

<h3 id="declarative-style">Declarative Style</h3>
<ul>
  <li>어떻게가 아니라 무엇을 할지에 집중</li>
  <li>시스템이 실행 흐름을 알아서 처리</li>
  <li>Stream, Optional 등이 대표적인 예</li>
</ul>

<h2 id="일급-함수">일급 함수</h2>
<blockquote>
  <p>함수를 값처럼 취급할 수 있는 언어 특성</p>
</blockquote>

<ol>
  <li>함수를 변수에 할당</li>
</ol>

<p>def say_hello():
print(“Hello”)</p>
<h1 id="함수를-변수에-할당">함수를 변수에 할당</h1>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>say_hello_var = say_hello # 변수를 통해 함수 실행 say_hello_var()
</code></pre></div></div>

<ol>
  <li>함수를 인자로 전달</li>
</ol>

<p>def execute(fn):
    fn()</p>

<p>execute(lambda : print(“Executed”))</p>

<ol>
  <li>함수를 반환(return)</li>
</ol>

<p>def make_adder(x); 
    def adder(y):
        return x + y 
    return adder</p>

<p>add5 = make_adder(5)
print(add(3))</p>

<ol>
  <li>객체의 속성properties 로 사용</li>
</ol>

<h4 id="딕셔너리의-key-value-쌍으로-함수를-정의">딕셔너리의 key-value 쌍으로 함수를 정의</h4>
<p>calculator = {
“add”: lambda a, b: a + b
}</p>

<h4 id="딕셔너리의-key로-함수에-접근하여-실행">딕셔너리의 Key로 함수에 접근하여 실행</h4>
<p>print(calculator<a href="2, 3">“add”</a>) # 출력: 5</p>

<h2 id="순수-함수-pure-function-lambda-expression">순수 함수 Pure Function Lambda Expression</h2>
<p>동일한 입력에는 항상 동일한 출력을 반환, 함수 외부 변수의 샅애를 변경하지 않는 함수</p>

<ul>
  <li>
    <p>비순수 함수</p>
  </li>
  <li>
    <p>외부 상태 (Global 변수)
count = 0
def increase_and_get():
global count
count += 1 # 외부 상태를 변경 (Side Effect)
return count</p>
  </li>
</ul>

<p>검증 및 특징 확인
print(increase_and_get()) # 출력: 1
print(increase_and_get()) # 출력: 2 (다른 출력</p>

<p>순수 함수</p>

<p>ef square(x):
return x * x # 같은 입력, 같은 출력
print(square(4)) # 출력: 16
print(square(4)) # 출력: 16</p>

<p>순수 함수 특징</p>

<p>예측 가능성 같은 입력 같은 출력, 디버깅이 쉬움
테스트 용이 외부 의존 없이 독립적으로 테스트 가능
병렬 처리 안전 외부 상태를 건드리지 않으니 race condition 없음
캐싱 가능 결과를 재활용 Memoization 가능
리팩토링 안정성 의존성 적어서 코드 변경 영향 최소화</p>

<p>익명 클래스 Anonymous Class</p>

<ul>
  <li>클래스 기반 언어인 Java 환경에서 함수형 프로그램을 지원하기 위한 방식</li>
</ul>

<h2 id="익명-클래스anonymus-class란">익명 클래스(Anonymus Class)란?</h2>
<ul>
  <li>이름이 없는 클래스</li>
  <li>한번만 사용할 목적으로 정의</li>
  <li>클래스를 선언하면서 동시에 인스터스 생성</li>
</ul>

<p>문제점</p>
<ul>
  <li>너무 장황함</li>
  <li>가독성 감소</li>
  <li>함수 전달을 위한 최소 표현이 아님</li>
</ul>

<h3 id="람다-표현식-lamda-expression">람다 표현식 Lamda Expression</h3>
<ul>
  <li>익명 클래스를 간결하게 표현하기 위한 문법, 함수형 인터페이스를 인스턴스로 생성하는 가장 짧은 방식</li>
</ul>

<p>배경</p>
<ul>
  <li>Java 82014 이전: Java는 순수 객체지향 언어로, 메서드도 반드시 클래스의 일부여야 함.</li>
  <li>Java 8: 람다, Stream API, java.util.function 패키지 도입
코드를 값처럼 전달 가능
선언적, 함수형 스타일 코딩 가능</li>
  <li>간결성, 병렬 처리 용이성, 컬렉션 처리의 함수형 스타일 지원</li>
</ul>

<p>목적 설명 예시
코드 간결화 익명 클래스 작성의 반복 코드 제거 list.forEachitem System.out.printlnitem;
함수형 프로그래밍 지원 함수를 값처럼 전달 stream.filterx x 10
병렬/선언형 처리 Stream API와 결합하여 병렬 연산 지원 list.parallelStream.map….reduce…</p>

<p>public class Main{
    public static void main(String[] args){
        Runnable lambdaRunnable = () -&gt; System.out.println(“별도 스레드에서 실행”);</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    Thread thread2 = new Thread(lambdaRunnable); 
    thread2.start(); 
} }
</code></pre></div></div>

<p>익명 함수를 간결하게 표현하기 위한 문법, 함수형 인터페이스를 인스턴스로 생성하는 가장 짧은 방식</p>

<p>Lambda</p>

<p>Runnable r = () -&gt; System.out.println(“Hello”);</p>

<p>항목 9
익명 클래스 람다식
목적 객체 클래스 생성 함수 전달 행위 전달
코드 길이 길고 반복적 매우 간결
의미 객체지향적 해결 함수형 프로그래밍 해결
실행 구조 내부적으로 클래스 생성 함수형 인터페이스 구현체 생성
사용성 가독성 낮음 가독성, 유지보수 높음</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="advanced" /><category term="Java" /><category term="Functional Programming" /><category term="Lambda" /><category term="Anonymous Class" /><category term="Stream" /><category term="Pure Function" /><summary type="html"><![CDATA[Java의 함수형 프로그래밍, 순수 함수, 익명 클래스, 람다식과 함수형 인터페이스를 정리한다.]]></summary></entry><entry><title type="html">Java 객체지향 Part 5: static 메서드와 중첩 클래스</title><link href="http://localhost:4000/java/oop/2026/08/11/java-static-nested-class/" rel="alternate" type="text/html" title="Java 객체지향 Part 5: static 메서드와 중첩 클래스" /><published>2026-08-11T11:20:00+09:00</published><updated>2026-08-11T11:20:00+09:00</updated><id>http://localhost:4000/java/oop/2026/08/11/java-static-nested-class</id><content type="html" xml:base="http://localhost:4000/java/oop/2026/08/11/java-static-nested-class/"><![CDATA[<h1 id="java-객체지향-part-5-static-메서드와-중첩-클래스">Java 객체지향 Part 5: static 메서드와 중첩 클래스</h1>

<p>중첩 클래스에서 Static을 붙이는 이유는 바깥 객체와 연결되지 않은 클래스를 내부에 묶어두기 위해서이다.</p>

<h2 id="일반-내부-클래스">일반 내부 클래스</h2>

<p>tatic이 없는 내부 클래스는 바깥 객체에 소속</p>

<p>class User {
    String name = “민형”;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Profile {
    void printName() {
        System.out.println(name); 
    }
} }
</code></pre></div></div>

<p>따라서 Profile 객체를 만들려면 먼저 User 객체가 필요하다.</p>

<p>User user = new User(); 
User.profile profile = user.new Profile();</p>

<p>구조를 개념적으로 보면 다음과 같다.</p>

<p>User 객체
    └── Profile 객체
            └── 바깥 User 객체를 참조</p>

<p>profile이 특정 User의 name을 사용하므로 바깥 객체와 연결되는 것이 자연스럽다</p>

<h2 id="static-중첩-클래스">Static 중첩 클래스</h2>

<p>static 중첩 클래스는 바깥 객체와 연결되지 않는다.</p>

<p>class User {
    static class Profile{
        void printMessage(){
            System.out.println(“프로필입니다.”)
        }
    }
}</p>

<p>User.Profile profile = new User.Profile();</p>

<p>profile.printMessage();</p>

<p>여기서 User.Profile 이라고 쓰지만, 특정 User 객체에 소속된 것은 아니다. 
단지 User와 관련된 클래스라서 User 내부에 정리한 것</p>

<h1 id="왜-static을-사용하는가">왜 Static을 사용하는가?</h1>

<h2 id="1-바깥-객체가-필요없기-때문에">1. 바깥 객체가 필요없기 때문에</h2>

<p>다음 Address는 특정 User 객체의 필드에 직접 접근할 필요가 없다.</p>

<p>class User{
    static class Address{
        String city;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    Address(String city){
        this.city = city; 
    }
} }
</code></pre></div></div>

<p>Address에 접근하고 싶다면</p>

<p>User.Address address = new User.Address(“판교”)</p>

<p>바깥 객체가 필요 없는데 일반 내부 클래스로 만들면 불필요하게 User 객체와 연결된다.</p>

<h2 id="2-관련-클래스를-한-곳에-묶을-수-있기-때문에">2. 관련 클래스를 한 곳에 묶을 수 있기 때문에</h2>

<p>class HttpResponse{
    static class Header{</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>}
static class Body{

} }
</code></pre></div></div>

<p>Header 와 Body가 HttpsResponse와 관련있다는 것을 구조적으로 표현할 수 있다.</p>

<p>HttpResponse.Header header  = new HttpResponse.Header(); 
HttpResponse.Body body = new HttpResponse.Body();</p>

<h1 id="3-불필요한-바깥-객체-참조를-막기-때문에">3. 불필요한 바깥 객체 참조를 막기 때문에</h1>

<p>일반 내부 클래스 객체는 내부적으로 바깥 객체를 참조</p>

<p>내부 클래스 객체 ──참조──→ 바깥 클래스 객체</p>

<p>하지만 static 중첩 클래스에은 그 참조가 없다.</p>

<p>static 중첩 클래스 객체     바깥 클래스 객체</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>      서로 독립적
</code></pre></div></div>

<p>바깥 객체가 필요하지 않다면 static 중첩 클래스를 사용해야 구조가 단순하고 
불필요한 객체 참조도 생기지 않는다.</p>

<h3 id="개념-확인-문제">개념 확인 문제</h3>

<p>Receipt 클래스는 특정 Order 객체의 orderNumber를 직접 사용해야 한다.</p>

<p>Receipt는 특정 Order 객체의 orderNumber를 사용해야 하므로 일반 내부 클래스로 선언해야 한다. 일반 내부 클래스는 자신을 생성한 바깥 Order 객체를 참조할 수 있기 때문에</p>

<h1 id="static-메서드">Static 메서드</h1>

<p>판단 기준</p>

<blockquote>
  <p>클래스의 필드를 사용하느냐? 
더 자세하게는, 이 기능이 특정 객체의 행동인가, 아니면 객체와 상관없는 공통 기능인가?</p>
</blockquote>

<p>특정 객체의 데이터가 필요하지 않을 때 사용한다.</p>

<p>판단할 떄 이렇게 생각하면 된다.</p>

<blockquote>
  <p>이 기능을 실행하려면 특정 객체가 꼭 필요한가?</p>
</blockquote>

<p>필요하면 일반 인스턴스 메서드</p>

<p>class User{
    string name;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>void printName() {}
    System.out.println(name);  }
</code></pre></div></div>

<p>printName()은 특정 User 객체의 name이 필요하다.</p>

<p>User user = new User(); 
user.name = “민형”; 
user.printName();</p>

<hr />

<p>반대로 특정 객체의 데이터가 필요하지 않으면 static 메서드를 사용할 수 있다.</p>

<p>class Calculator {
    static int add(int a, int b,){
        return a + b; 
    }
}</p>

<p>add()는 어떤 calculator 객체의 상태도 사용하지 않으므로…</p>

<p>int result = Calculator.add(10,20);</p>

<p>정리하면</p>

<p>특정 객체의 필드가 필요한가?
    ├── 필요함 → 일반 인스턴스 메서드
    └── 필요 없음 → static 메서드 고려</p>

<p>예를 들어 Order에서 특정 주문 번호를 출력하다면 일반 메서드가 자연스러움</p>

<p>class Order{
    String orderNumber;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>void printOrderNumber() {
    System.out.println(orderNumber); 
} }
</code></pre></div></div>

<p>하지만 주문 번호의 형식만 검사한다면 특정 Order 객체가 없어도 된다.</p>

<p>class Order {
    static boolean isValidOrderNumber(String orderNumber) {
        return orderNumber.startsWith(“ORDER-“);
    }
}</p>

<p>boolean valid = Order.isValidOrderNumber(“ORDER-100”);</p>

<h3 id="근데-그냥-필드-선언-안하고-쓰면-되는-거-아닌가">근데 그냥 필드 선언 안하고 쓰면 되는 거 아닌가?</h3>

<p>class Calculator {
    int add(int a, int b) {
        return a + b;
    }
}</p>

<p>class Calculator {
    static int add(int a, int b) {
        return a + b;
    }
}</p>

<p>두 메서드는 모두 전달받은 값만 계산 -&gt; 차이는 호출 방식</p>

<p>// 일반 메서드 
Calculator calculator = new Calculator(); 
calculator.add(10,20):</p>

<p>// static 메서드 
Calculator.add(10, 20);</p>

<p>일반 메서드는 필드를 사용하지 않더라도 객체를 먼저 만들어야 합니다. 반면 static 메서드는 객체 없이 실행할 수 있다.
-&gt; 이거는 객체를 만들지 않아서 상당히 편리하다!</p>

<h3 id="그러면-static-method는-언제-안써야-하는가">그러면 Static method는 언제 안써야 하는가?</h3>

<p>나중에 구현을 객체마다 다르게 바꿔야 한다면 일반 베서드로 구현해야 한다.</p>

<p>class Calculator {
    int calculate(int a, int b) {
        return a + b;
    }
}</p>

<p>class MultiplyCalculator extends Calculator {
    @Override
    int calculate(int a, int b) {
        return a * b;
    }
}</p>

<p>여기서는 객체에 따라 calculate() 동작이 달라진다.</p>

<p>Calculator calculator1 = new Calculator();
Calculator calculator2 = new MultiplyCalculator();</p>

<p>calculator1.calculate(10, 20); // 30
calculator2.calculate(10, 20); // 200</p>

<p>반대로 동작이 항상 같고, 특정 객체의 상태도 필요하지 않다면 static이 자연스럽다</p>

<p>class CalculatorUtils {
    static int add(int a, int b) {
        return a + b;
    }
}</p>

<p>정리하면</p>

<p>필드가 필요하다 -&gt; 일반 메소드 
필드가 필요 없지만 객체마다 동작이 달라질 수 있다 -&gt; 일반 메서드 
필드가 필요 업고 동작도 항상 같다 -&gt; static 메서드가 자연스럽다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="oop" /><category term="Java" /><category term="Static" /><category term="Nested Class" /><category term="Inner Class" /><category term="Method" /><category term="Polymorphism" /><summary type="html"><![CDATA[Java static 메서드, 일반 내부 클래스, static 중첩 클래스의 차이와 사용 기준을 정리한다.]]></summary></entry><entry><title type="html">Java 심화 Part 2: Annotation</title><link href="http://localhost:4000/java/advanced/2026/08/11/java-annotations/" rel="alternate" type="text/html" title="Java 심화 Part 2: Annotation" /><published>2026-08-11T11:00:00+09:00</published><updated>2026-08-11T11:00:00+09:00</updated><id>http://localhost:4000/java/advanced/2026/08/11/java-annotations</id><content type="html" xml:base="http://localhost:4000/java/advanced/2026/08/11/java-annotations/"><![CDATA[<h1 id="java-심화-part-2-annotation">Java 심화 Part 2: Annotation</h1>

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>단어</th>
      <th>정의</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Annotation</td>
      <td>클래스, 메서드, 필드 등에 추가 정보를 붙이는 Java 문법</td>
    </tr>
    <tr>
      <td>Metadata</td>
      <td>코드의 실행 로직이 아니라 코드의 역할과 설정을 설명하는 정보</td>
    </tr>
    <tr>
      <td>컴파일러</td>
      <td>Java 코드를 검사하고 바이트코드로 변환하는 도구</td>
    </tr>
    <tr>
      <td>Reflection</td>
      <td>실행 중에 클래스, 메서드, 필드, 애너테이션 정보를 조회하는 기능</td>
    </tr>
    <tr>
      <td>Bean</td>
      <td>Spring 컨테이너가 생성하고 관리하는 객체</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Annotation은 클래스, 메서드, 필드 등에 추가적인 메타데이터를 붙이는 기능이다. 애너테이션 자체가 로직을 실행하는 것은 아니며, 컴파일러나 프레임워크가 이 정보를 읽고 처리한다.</p>
</blockquote>

<h2 id="2-override-컴파일러에게-재정의를-알리기">2. <code class="language-plaintext highlighter-rouge">@Override</code>: 컴파일러에게 재정의를 알리기</h2>

<p>Java에서 <code class="language-plaintext highlighter-rouge">@</code>로 시작하는 것이 Annotation이다. 가장 익숙한 예시는 <code class="language-plaintext highlighter-rouge">@Override</code>다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">interface</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">Dog</span> <span class="kd">implements</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"멍멍!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Override</code>는 <code class="language-plaintext highlighter-rouge">makeSound()</code>가 부모 클래스나 인터페이스의 메서드를 재정의했다는 사실을 컴파일러에 알려준다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Dog</span> <span class="kd">implements</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">makeSond</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"멍멍!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>위 코드의 <code class="language-plaintext highlighter-rouge">makeSond()</code>는 <code class="language-plaintext highlighter-rouge">Animal</code>의 <code class="language-plaintext highlighter-rouge">makeSound()</code>와 이름이 다르다. <code class="language-plaintext highlighter-rouge">@Override</code>가 있기 때문에 컴파일러가 재정의가 아니라는 오류를 바로 알려준다.</p>

<h2 id="3-애너테이션은-혼자-실행되지-않는다">3. 애너테이션은 혼자 실행되지 않는다</h2>

<p>다음 코드에서 <code class="language-plaintext highlighter-rouge">@Override</code>가 직접 <code class="language-plaintext highlighter-rouge">makeSound()</code>를 실행하는 것은 아니다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Override</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"멍멍!"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>역할은 다음처럼 나뉜다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@Override
    ↓
재정의라는 정보를 표시한다
    ↓
컴파일러가 이 정보를 검사한다
    ↓
부모 또는 인터페이스 메서드가 없으면 오류를 보여 준다
</code></pre></div></div>

<p>즉, 애너테이션은 표시이고 그 표시를 해석하는 컴파일러나 프레임워크가 실제 처리를 담당한다.</p>

<h2 id="4-자주-사용하는-java-애너테이션">4. 자주 사용하는 Java 애너테이션</h2>

<table>
  <thead>
    <tr>
      <th>애너테이션</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Override</code></td>
      <td>부모 클래스 또는 인터페이스 메서드를 재정의했음을 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Deprecated</code></td>
      <td>더 이상 사용을 권장하지 않는 코드임을 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@SuppressWarnings</code></td>
      <td>특정 컴파일 경고를 억제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@FunctionalInterface</code></td>
      <td>함수형 인터페이스 규칙을 지키는지 검사</td>
    </tr>
  </tbody>
</table>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Deprecated</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">oldLogin</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"이 메서드는 더 이상 권장하지 않습니다."</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="5-spring에서의-annotation">5. Spring에서의 Annotation</h2>

<p>Spring에서는 애너테이션이 클래스의 역할과 처리 방식을 알려 주는 중요한 설정 정보가 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Service</code>는 Spring에 이 클래스가 서비스 역할을 하며 관리할 객체 후보라는 정보를 전달한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserController</span><span class="o">(</span><span class="nc">UserService</span> <span class="n">userService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userService</span> <span class="o">=</span> <span class="n">userService</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/users"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="nf">getUsers</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>위 코드에서:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@RestController</code>: HTTP 요청을 처리하는 Controller 클래스임을 표시한다.</li>
  <li><code class="language-plaintext highlighter-rouge">@GetMapping("/users")</code>: <code class="language-plaintext highlighter-rouge">/users</code>로 들어오는 GET 요청을 이 메서드와 연결한다.</li>
  <li>생성자: Spring이 필요한 <code class="language-plaintext highlighter-rouge">UserService</code>를 주입할 수 있는 위치다. 생성자가 하나라면 보통 <code class="language-plaintext highlighter-rouge">@Autowired</code>를 생략한다.</li>
</ul>

<h2 id="6-reflection과-spring의-연결">6. Reflection과 Spring의 연결</h2>

<p>Spring은 실행 중 Reflection을 이용해 클래스와 애너테이션을 검사한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>클래스 탐색
    ↓
@Service, @Controller 같은 애너테이션 발견
    ↓
Spring이 관리할 클래스인지 판단
    ↓
객체 생성
    ↓
Bean으로 등록
    ↓
필요한 객체에 의존성 주입
</code></pre></div></div>

<p>정리하면 역할은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Annotation → 코드에 의미와 설정을 표시
Reflection → 실행 중 그 표시를 읽음
Spring     → 읽은 정보를 기준으로 객체를 생성하고 관리
</code></pre></div></div>

<h2 id="7-애너테이션에-값-넣기">7. 애너테이션에 값 넣기</h2>

<p>애너테이션은 설정값도 받을 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="nf">getUsers</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">"/users"</code>는 <code class="language-plaintext highlighter-rouge">@GetMapping</code>에 전달한 요청 경로 설정값이다.</p>

<p>직접 애너테이션을 만들 수도 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">java.lang.annotation.ElementType</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.lang.annotation.Retention</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.lang.annotation.RetentionPolicy</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.lang.annotation.Target</span><span class="o">;</span>

<span class="nd">@Retention</span><span class="o">(</span><span class="nc">RetentionPolicy</span><span class="o">.</span><span class="na">RUNTIME</span><span class="o">)</span>
<span class="nd">@Target</span><span class="o">(</span><span class="nc">ElementType</span><span class="o">.</span><span class="na">TYPE</span><span class="o">)</span>
<span class="nd">@interface</span> <span class="nc">Role</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="nf">value</span><span class="o">();</span>
<span class="o">}</span>

<span class="nd">@Role</span><span class="o">(</span><span class="s">"ADMIN"</span><span class="o">)</span>
<span class="kd">class</span> <span class="nc">AdminService</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Retention(RetentionPolicy.RUNTIME)</code>은 실행 중 Reflection으로 이 애너테이션을 읽을 수 있게 한다. <code class="language-plaintext highlighter-rouge">@Target(ElementType.TYPE)</code>은 이 애너테이션을 클래스나 인터페이스에 붙일 수 있다는 뜻이다.</p>

<h2 id="8-요약">8. 요약</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Annotation
    ↓
코드에 역할과 설정 정보를 표시한다
    ↓
컴파일러 또는 프레임워크가 정보를 읽는다
    ↓
검사, 요청 매핑, Bean 등록, 의존성 주입 같은 처리를 수행한다
</code></pre></div></div>

<blockquote>
  <p>Annotation은 코드에 메타데이터를 붙이는 기능이다. 컴파일러는 코드 검증에 사용하고, Spring 같은 프레임워크는 Reflection으로 읽어 객체 관리와 요청 처리에 활용한다.</p>
</blockquote>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="advanced" /><category term="Java" /><category term="Annotation" /><category term="Reflection" /><category term="Spring" /><category term="Override" /><category term="Metadata" /><summary type="html"><![CDATA[Java Annotation의 메타데이터 역할과 Override, Spring 컴포넌트, 요청 매핑, Reflection의 연결을 정리한다.]]></summary></entry><entry><title type="html">Java 심화 Part 1: Reflection</title><link href="http://localhost:4000/java/advanced/2026/08/11/java-reflection/" rel="alternate" type="text/html" title="Java 심화 Part 1: Reflection" /><published>2026-08-11T10:40:00+09:00</published><updated>2026-08-11T10:40:00+09:00</updated><id>http://localhost:4000/java/advanced/2026/08/11/java-reflection</id><content type="html" xml:base="http://localhost:4000/java/advanced/2026/08/11/java-reflection/"><![CDATA[<h1 id="java-심화-part-1-reflection">Java 심화 Part 1: Reflection</h1>

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>단어</th>
      <th>정의</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Reflection</td>
      <td>실행 중인 클래스나 객체의 구조를 프로그램이 직접 조회하고 조작하는 기능</td>
    </tr>
    <tr>
      <td>Metadata</td>
      <td>클래스 이름, 필드, 메서드, 생성자, 애너테이션처럼 클래스 자체에 관한 정보</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Class&lt;?&gt;</code></td>
      <td>특정 클래스의 메타데이터를 다루는 객체</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Field</code></td>
      <td>클래스에 선언된 필드 정보를 나타내는 객체</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Method</code></td>
      <td>클래스에 선언된 메서드 정보를 나타내는 객체</td>
    </tr>
    <tr>
      <td>Annotation</td>
      <td>클래스나 메서드 등에 붙여 추가 의미를 전달하는 표식</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Reflection은 프로그램이 실행 중에 클래스의 구조를 직접 조사하고, 필요하면 메서드를 호출하거나 필드 값을 읽고 수정할 수 있게 해주는 기능이다.</p>
</blockquote>

<h2 id="2-reflection으로-확인할-수-있는-것">2. Reflection으로 확인할 수 있는 것</h2>

<p>Reflection을 사용하면 실행 중에 다음 정보를 확인할 수 있다.</p>

<ul>
  <li>이 클래스 이름이 무엇인지</li>
  <li>어떤 필드가 있는지</li>
  <li>어떤 메서드와 생성자가 있는지</li>
  <li>어떤 애너테이션이 붙어 있는지</li>
</ul>

<p>예제를 위한 <code class="language-plaintext highlighter-rouge">User</code> 클래스를 먼저 보자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">hello</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"hello"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="3-클래스-정보-가져오기">3. 클래스 정보 가져오기</h2>

<p><code class="language-plaintext highlighter-rouge">User.class</code>로 <code class="language-plaintext highlighter-rouge">User</code> 클래스의 정보를 담은 <code class="language-plaintext highlighter-rouge">Class</code> 객체를 가져올 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Class</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="n">clazz</span> <span class="o">=</span> <span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">;</span>

<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">clazz</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: User
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">clazz</code>에는 <code class="language-plaintext highlighter-rouge">User</code> 객체 한 개의 데이터가 아니라 <code class="language-plaintext highlighter-rouge">User</code> 클래스 자체의 구조 정보가 들어 있다.</p>

<h2 id="4-메서드와-필드-조회하기">4. 메서드와 필드 조회하기</h2>

<h3 id="메서드-조회">메서드 조회</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Method</span><span class="o">[]</span> <span class="n">methods</span> <span class="o">=</span> <span class="n">clazz</span><span class="o">.</span><span class="na">getDeclaredMethods</span><span class="o">();</span>

<span class="k">for</span> <span class="o">(</span><span class="nc">Method</span> <span class="n">method</span> <span class="o">:</span> <span class="n">methods</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">method</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: hello
</code></pre></div></div>

<h3 id="필드-조회">필드 조회</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Field</span><span class="o">[]</span> <span class="n">fields</span> <span class="o">=</span> <span class="n">clazz</span><span class="o">.</span><span class="na">getDeclaredFields</span><span class="o">();</span>

<span class="k">for</span> <span class="o">(</span><span class="nc">Field</span> <span class="n">field</span> <span class="o">:</span> <span class="n">fields</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">field</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: name
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">getDeclaredMethods()</code>와 <code class="language-plaintext highlighter-rouge">getDeclaredFields()</code>는 해당 클래스에 직접 선언된 메서드와 필드를 가져온다.</p>

<h2 id="5-실행-중에-메서드-호출하기">5. 실행 중에 메서드 호출하기</h2>

<p>Reflection을 사용하면 메서드 이름을 문자열로 찾아 실행할 수도 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">java.lang.reflect.Method</span><span class="o">;</span>

<span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">hello</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"hello"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ReflectionExample</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">ReflectiveOperationException</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>

        <span class="nc">Method</span> <span class="n">method</span> <span class="o">=</span> <span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">.</span><span class="na">getDeclaredMethod</span><span class="o">(</span><span class="s">"hello"</span><span class="o">);</span>
        <span class="n">method</span><span class="o">.</span><span class="na">invoke</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: hello
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">getDeclaredMethod("hello")</code>로 메서드를 찾고, <code class="language-plaintext highlighter-rouge">invoke(user)</code>로 <code class="language-plaintext highlighter-rouge">user</code> 객체에서 그 메서드를 실행한다.</p>

<h2 id="6-private-필드-접근">6. private 필드 접근</h2>

<p>Reflection을 사용하면 <code class="language-plaintext highlighter-rouge">private</code> 필드에도 접근을 시도할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">java.lang.reflect.Field</span><span class="o">;</span>

<span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="s">"민형"</span><span class="o">;</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ReflectionExample</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">ReflectiveOperationException</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>

        <span class="nc">Field</span> <span class="n">field</span> <span class="o">=</span> <span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">.</span><span class="na">getDeclaredField</span><span class="o">(</span><span class="s">"name"</span><span class="o">);</span>
        <span class="n">field</span><span class="o">.</span><span class="na">setAccessible</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>

        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">field</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="n">user</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: 민형
</code></pre></div></div>

<p>다만 <code class="language-plaintext highlighter-rouge">setAccessible(true)</code>는 캡슐화를 우회하는 기능이다. Java 모듈 환경에서는 접근이 제한될 수도 있으므로, 일반 애플리케이션 코드에서 무분별하게 사용하면 안 된다.</p>

<h2 id="7-spring에서-reflection을-사용하는-이유">7. Spring에서 Reflection을 사용하는 이유</h2>

<p>Spring 같은 프레임워크는 실행 중에 클래스를 조사해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Spring은 대략 다음 흐름으로 동작한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>클래스들을 조사한다
    ↓
@Service가 붙은 클래스를 찾는다
    ↓
관리할 객체를 생성한다
    ↓
Bean으로 등록한다
    ↓
필요한 곳에 의존성을 주입한다
</code></pre></div></div>

<p>예를 들어 <code class="language-plaintext highlighter-rouge">@Autowired</code>가 붙은 필드를 확인해 해당 타입의 객체를 넣어 주는 과정에도 Reflection이 활용될 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">OrderService</span> <span class="o">{</span>
    <span class="nd">@Autowired</span>
    <span class="kd">private</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="8-사용할-때-주의할-점">8. 사용할 때 주의할 점</h2>

<ul>
  <li>일반 코드보다 느릴 수 있다.</li>
  <li>컴파일러가 확인해 주는 타입 안정성이 약해질 수 있다.</li>
  <li><code class="language-plaintext highlighter-rouge">private</code> 접근 제한을 우회할 수 있어 코드 추적이 어려워진다.</li>
  <li>프레임워크 내부 기능이나 정말 동적으로 처리해야 하는 경우에 사용하는 편이 좋다.</li>
</ul>

<p>Spring, Hibernate, Jackson, 테스트 프레임워크처럼 클래스 구조를 자동으로 분석해야 하는 라이브러리와 프레임워크에서 Reflection을 많이 사용한다.</p>

<h2 id="9-요약">9. 요약</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Reflection
    ↓
실행 중에 클래스 구조를 조사한다
    ↓
필드, 메서드, 생성자, 애너테이션 정보를 확인한다
    ↓
필요하면 메서드 호출과 필드 접근도 가능하다
</code></pre></div></div>

<blockquote>
  <p>Reflection은 런타임에 클래스의 메타데이터를 조회하고 조작하는 기능이다. 강력하지만 타입 안정성과 캡슐화를 약화시킬 수 있으므로, 주로 프레임워크 내부나 동적 처리가 꼭 필요한 상황에서 사용한다.</p>
</blockquote>

<ul>
  <li>런타임 동적 로딩 및 조작
    <ul>
      <li>컴파일 시점이 아닌 실행 중에 객체 정보를 확인하거나 수정 가능</li>
      <li>클래스 이름이 동적으로 결정되거나, 조건에 따라 특정 메서드를 실행해야 할 때 사용</li>
    </ul>
  </li>
  <li>어노테이션 기반 동작 제어
    <ul>
      <li>런타임에 Annotation을 읽어서 특정 메서드나 필드에 대해 자동 동작을 수행</li>
      <li>예: Spring에서 Autowired로 의존성 주입, RequestMapping으로 URL 메서드 매핑</li>
    </ul>
  </li>
  <li>플러그인 시스템 및 동적 확장
    <ul>
      <li>실행 중에 플러그인 추가, 또는 인터페이스를 구현하지 않은 클래스도 동적으로 실행 가능</li>
      <li>외부 패키지의 클래스를 런타임에 로드하여 확장 기능 구현 가능</li>
    </ul>
  </li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="advanced" /><category term="Java" /><category term="Reflection" /><category term="Metadata" /><category term="Annotation" /><category term="Spring" /><category term="DI" /><summary type="html"><![CDATA[Java Reflection으로 런타임에 클래스, 필드, 메서드, 애너테이션 정보를 조회하고 호출하는 방법을 정리한다.]]></summary></entry><entry><title type="html">Java 기초 Part 5: Stream API</title><link href="http://localhost:4000/java/basics/2026/08/11/java-stream-api/" rel="alternate" type="text/html" title="Java 기초 Part 5: Stream API" /><published>2026-08-11T10:20:00+09:00</published><updated>2026-08-11T10:20:00+09:00</updated><id>http://localhost:4000/java/basics/2026/08/11/java-stream-api</id><content type="html" xml:base="http://localhost:4000/java/basics/2026/08/11/java-stream-api/"><![CDATA[<h1 id="java-기초-part-5-stream-api">Java 기초 Part 5: Stream API</h1>

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>단어</th>
      <th>정의</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Stream</td>
      <td>컬렉션의 원본 데이터를 바꾸지 않고 요소를 순서대로 처리하는 흐름</td>
      <td><code class="language-plaintext highlighter-rouge">numbers.stream()</code></td>
    </tr>
    <tr>
      <td>파이프라인(Pipeline)</td>
      <td>여러 연산을 연결한 데이터 처리 과정</td>
      <td><code class="language-plaintext highlighter-rouge">stream().filter().map().toList()</code></td>
    </tr>
    <tr>
      <td>중간 연산</td>
      <td>Stream을 받아 새로운 Stream을 반환하는 연산</td>
      <td><code class="language-plaintext highlighter-rouge">filter()</code>, <code class="language-plaintext highlighter-rouge">map()</code>, <code class="language-plaintext highlighter-rouge">sorted()</code></td>
    </tr>
    <tr>
      <td>최종 연산</td>
      <td>Stream 처리를 실행하고 결과를 만드는 연산</td>
      <td><code class="language-plaintext highlighter-rouge">toList()</code>, <code class="language-plaintext highlighter-rouge">forEach()</code></td>
    </tr>
    <tr>
      <td>람다식(Lambda)</td>
      <td>짧게 표현한 익명 함수</td>
      <td><code class="language-plaintext highlighter-rouge">n -&gt; n * 2</code></td>
    </tr>
    <tr>
      <td>메서드 참조</td>
      <td>이미 존재하는 메서드를 간결하게 전달하는 문법</td>
      <td><code class="language-plaintext highlighter-rouge">System.out::println</code></td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Stream API는 리스트 같은 데이터 묶음을 하나씩 꺼내서 조건을 걸고, 바꾸고, 다시 모으는 데이터 처리 기능이다.</p>
</blockquote>

<p>Java 8에서 도입됐으며, 반복문이나 <code class="language-plaintext highlighter-rouge">Iterator</code>로 작성하던 코드를 선언적이고 함수형인 스타일로 표현할 수 있게 해준다.</p>

<h2 id="2-stream-api의-핵심">2. Stream API의 핵심</h2>

<p>Stream API의 핵심은 데이터 처리 과정을 위에서 아래로 읽을 수 있다는 점이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">names</span> <span class="o">=</span> <span class="n">users</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">user</span> <span class="o">-&gt;</span> <span class="n">user</span><span class="o">.</span><span class="na">getAge</span><span class="o">()</span> <span class="o">&gt;=</span> <span class="mi">20</span><span class="o">)</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="nl">User:</span><span class="o">:</span><span class="n">getName</span><span class="o">)</span>
        <span class="o">.</span><span class="na">sorted</span><span class="o">()</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<p>위 코드는 다음 순서로 읽을 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>users를 Stream으로 만든다
    ↓
20세 이상인 사용자만 남긴다
    ↓
User 객체를 이름으로 바꾼다
    ↓
이름을 정렬한다
    ↓
List로 모은다
</code></pre></div></div>

<h2 id="3-반복문과-stream-비교">3. 반복문과 Stream 비교</h2>

<p>예를 들어 숫자 리스트에서 짝수만 뽑는다고 하자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numbers</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> <span class="mi">3</span><span class="o">,</span> <span class="mi">4</span><span class="o">,</span> <span class="mi">5</span><span class="o">);</span>
</code></pre></div></div>

<p>반복문을 사용하면 결과 리스트를 직접 만들고 값을 추가해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>

<span class="k">for</span> <span class="o">(</span><span class="kt">int</span> <span class="n">number</span> <span class="o">:</span> <span class="n">numbers</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">number</span> <span class="o">%</span> <span class="mi">2</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">result</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">number</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Stream을 사용하면 데이터 처리 과정에 집중해서 작성할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">%</span> <span class="mi">2</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<p>다음과 같이 읽으면 된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>numbers를 Stream으로 만든다
    ↓
짝수만 남긴다
    ↓
List로 모은다
</code></pre></div></div>

<h2 id="4-자주-사용하는-메서드">4. 자주 사용하는 메서드</h2>

<table>
  <thead>
    <tr>
      <th>메서드</th>
      <th>구분</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">stream()</code></td>
      <td>Stream 생성</td>
      <td>컬렉션에서 데이터 처리 흐름을 시작한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">filter()</code></td>
      <td>중간 연산</td>
      <td>조건에 맞는 요소만 남긴다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">map()</code></td>
      <td>중간 연산</td>
      <td>요소를 다른 값이나 형태로 바꾼다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">sorted()</code></td>
      <td>중간 연산</td>
      <td>요소를 정렬한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">distinct()</code></td>
      <td>중간 연산</td>
      <td>중복 요소를 제거한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">toList()</code></td>
      <td>최종 연산</td>
      <td>처리 결과를 <code class="language-plaintext highlighter-rouge">List</code>로 모은다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">forEach()</code></td>
      <td>최종 연산</td>
      <td>각 요소에 지정한 동작을 실행한다.</td>
    </tr>
  </tbody>
</table>

<h2 id="5-filter-조건에-맞는-값만-남기기">5. <code class="language-plaintext highlighter-rouge">filter()</code>: 조건에 맞는 값만 남기기</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">names</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"민형"</span><span class="o">,</span> <span class="s">"철수"</span><span class="o">,</span> <span class="s">"영희"</span><span class="o">,</span> <span class="s">"김민형"</span><span class="o">);</span>

<span class="n">names</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">name</span> <span class="o">-&gt;</span> <span class="n">name</span><span class="o">.</span><span class="na">length</span><span class="o">()</span> <span class="o">&gt;</span> <span class="mi">2</span><span class="o">)</span>
        <span class="o">.</span><span class="na">forEach</span><span class="o">(</span><span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">::</span><span class="n">println</span><span class="o">);</span>
</code></pre></div></div>

<p>결과는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>김민형
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">filter()</code>는 조건을 만족하는 요소만 통과시킨다. 요소의 값 자체를 다른 값으로 바꾸지는 않는다.</p>

<h2 id="6-map-값을-다른-형태로-바꾸기">6. <code class="language-plaintext highlighter-rouge">map()</code>: 값을 다른 형태로 바꾸기</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numbers</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> <span class="mi">3</span><span class="o">);</span>

<span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">*</span> <span class="mi">2</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>결과: [2, 4, 6]
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">map()</code>은 각 요소를 전달받아 새로운 값으로 바꾼다.</p>

<h2 id="7-filter와-map-함께-사용하기">7. <code class="language-plaintext highlighter-rouge">filter()</code>와 <code class="language-plaintext highlighter-rouge">map()</code> 함께 사용하기</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numbers</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> <span class="mi">3</span><span class="o">,</span> <span class="mi">4</span><span class="o">,</span> <span class="mi">5</span><span class="o">);</span>

<span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">&gt;</span> <span class="mi">2</span><span class="o">)</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">*</span> <span class="mi">10</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<p>처리 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1, 2, 3, 4, 5]
    ↓ filter(number -&gt; number &gt; 2)
[3, 4, 5]
    ↓ map(number -&gt; number * 10)
[30, 40, 50]
</code></pre></div></div>

<p>기억할 내용은 간단하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>filter → 골라낸다
map    → 바꾼다
</code></pre></div></div>

<h2 id="8-중간-연산과-최종-연산">8. 중간 연산과 최종 연산</h2>

<p><code class="language-plaintext highlighter-rouge">filter()</code>, <code class="language-plaintext highlighter-rouge">map()</code>, <code class="language-plaintext highlighter-rouge">sorted()</code> 같은 중간 연산만 작성하면 실제 데이터 처리가 시작되지 않는다. <code class="language-plaintext highlighter-rouge">toList()</code>나 <code class="language-plaintext highlighter-rouge">forEach()</code> 같은 최종 연산이 호출되어야 전체 파이프라인이 실행된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="o">{</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"확인: "</span> <span class="o">+</span> <span class="n">number</span><span class="o">);</span>
            <span class="k">return</span> <span class="n">number</span> <span class="o">%</span> <span class="mi">2</span> <span class="o">==</span> <span class="mi">0</span><span class="o">;</span>
        <span class="o">});</span>
</code></pre></div></div>

<p>위 코드는 최종 연산이 없기 때문에 <code class="language-plaintext highlighter-rouge">filter()</code> 내부의 출력도 실행되지 않는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">%</span> <span class="mi">2</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<p>이처럼 필요한 시점까지 실행을 미루는 방식을 지연 연산(Lazy Evaluation)이라고 한다.</p>

<h2 id="9-사용할-때-주의할-점">9. 사용할 때 주의할 점</h2>

<h3 id="원본-컬렉션은-바뀌지-않는다">원본 컬렉션은 바뀌지 않는다</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numbers</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> <span class="mi">3</span><span class="o">);</span>

<span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">*</span> <span class="mi">10</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>numbers → [1, 2, 3]
result  → [10, 20, 30]
</code></pre></div></div>

<p>Stream은 원본 컬렉션을 직접 변경하는 기능이 아니다. 처리 결과가 필요하면 새로운 리스트 등으로 받아야 한다.</p>

<h3 id="stream은-한-번만-사용할-수-있다">Stream은 한 번만 사용할 수 있다</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Stream</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">stream</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">();</span>

<span class="n">stream</span><span class="o">.</span><span class="na">toList</span><span class="o">();</span>
<span class="n">stream</span><span class="o">.</span><span class="na">toList</span><span class="o">();</span> <span class="c1">// IllegalStateException</span>
</code></pre></div></div>

<p>최종 연산이 끝난 Stream은 다시 사용할 수 없다. 다시 처리하려면 <code class="language-plaintext highlighter-rouge">numbers.stream()</code>으로 새로운 Stream을 만들어야 한다.</p>

<h2 id="10-최종-정리">10. 최종 정리</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>핵심 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Stream</td>
      <td>데이터 자체가 아니라 데이터 처리 흐름이다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">filter()</code></td>
      <td>조건에 맞는 요소만 남긴다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">map()</code></td>
      <td>요소를 다른 값이나 형태로 바꾼다.</td>
    </tr>
    <tr>
      <td>중간 연산</td>
      <td>Stream을 반환하며, 최종 연산 전까지 실행을 미룬다.</td>
    </tr>
    <tr>
      <td>최종 연산</td>
      <td>파이프라인을 실행하고 결과를 만든다.</td>
    </tr>
    <tr>
      <td>원본 데이터</td>
      <td>Stream으로 처리해도 원본 컬렉션은 그대로 유지된다.</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Stream API는 데이터를 어떻게 반복할지보다 어떤 조건으로 골라내고, 어떻게 바꾸고, 어떤 결과로 모을지에 집중하게 해주는 기능이다.</p>
</blockquote>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="basics" /><category term="Java" /><category term="Stream API" /><category term="Collection" /><category term="Lambda" /><category term="Filter" /><category term="Map" /><summary type="html"><![CDATA[Java Stream API의 파이프라인, 중간 연산과 최종 연산을 filter, map, sorted 예제로 정리한다.]]></summary></entry><entry><title type="html">Java 기초 Part 4: 제네릭</title><link href="http://localhost:4000/java/basics/2026/08/11/java-generics/" rel="alternate" type="text/html" title="Java 기초 Part 4: 제네릭" /><published>2026-08-11T10:00:00+09:00</published><updated>2026-08-11T10:00:00+09:00</updated><id>http://localhost:4000/java/basics/2026/08/11/java-generics</id><content type="html" xml:base="http://localhost:4000/java/basics/2026/08/11/java-generics/"><![CDATA[<h1 id="java-기초-part-4-제네릭">Java 기초 Part 4: 제네릭</h1>

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>단어</th>
      <th>정의</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>제네릭(Generic)</td>
      <td>클래스나 메서드가 사용할 데이터 타입을 외부에서 지정할 수 있게 하는 기능</td>
      <td><code class="language-plaintext highlighter-rouge">Box&lt;T&gt;</code></td>
    </tr>
    <tr>
      <td>타입 매개변수(Type Parameter)</td>
      <td>아직 정해지지 않은 타입을 나타내는 이름</td>
      <td><code class="language-plaintext highlighter-rouge">T</code></td>
    </tr>
    <tr>
      <td>타입 인자(Type Argument)</td>
      <td>제네릭을 사용할 때 실제로 지정하는 타입</td>
      <td><code class="language-plaintext highlighter-rouge">Box&lt;String&gt;</code>의 <code class="language-plaintext highlighter-rouge">String</code></td>
    </tr>
    <tr>
      <td>타입 안정성(Type Safety)</td>
      <td>잘못된 타입의 값을 컴파일 단계에서 막는 성질</td>
      <td><code class="language-plaintext highlighter-rouge">List&lt;String&gt;</code>에는 문자열만 저장</td>
    </tr>
    <tr>
      <td>Raw Type</td>
      <td>타입 인자를 생략한 제네릭 타입</td>
      <td><code class="language-plaintext highlighter-rouge">ArrayList list</code></td>
    </tr>
    <tr>
      <td>Wrapper Class</td>
      <td>기본형을 객체처럼 다루기 위한 참조 타입</td>
      <td><code class="language-plaintext highlighter-rouge">int</code>의 <code class="language-plaintext highlighter-rouge">Integer</code></td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>제네릭은 클래스나 메서드가 다룰 데이터 타입을 나중에 지정할 수 있게 해주는 기능이다.</p>
</blockquote>

<h2 id="2-제네릭의-기본-사용법">2. 제네릭의 기본 사용법</h2>

<p><code class="language-plaintext highlighter-rouge">ArrayList&lt;String&gt;</code>은 이 리스트에 <code class="language-plaintext highlighter-rouge">String</code> 타입만 저장하겠다는 뜻이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ArrayList</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">history</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>

<span class="n">history</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"10 + 20 = 30"</span><span class="o">);</span> <span class="c1">// 가능</span>
<span class="n">history</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span>            <span class="c1">// 컴파일 오류</span>
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">String</code>은 타입 인자다. <code class="language-plaintext highlighter-rouge">history.add(100)</code>은 <code class="language-plaintext highlighter-rouge">String</code> 전용 리스트에 <code class="language-plaintext highlighter-rouge">Integer</code>를 넣으려고 했기 때문에 컴파일 오류가 발생한다.</p>

<h2 id="3-제네릭이-없던-방식">3. 제네릭이 없던 방식</h2>

<p>타입 인자를 지정하지 않은 <code class="language-plaintext highlighter-rouge">ArrayList</code>를 Raw Type이라고 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ArrayList</span> <span class="n">list</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">();</span>

<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"민형"</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>
</code></pre></div></div>

<p>Raw Type에는 여러 타입의 값을 넣을 수 있지만, 값을 꺼낼 때 문제가 생긴다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Object</span> <span class="n">value</span> <span class="o">=</span> <span class="n">list</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="mi">0</span><span class="o">);</span>
<span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="o">(</span><span class="nc">String</span><span class="o">)</span> <span class="n">list</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="mi">0</span><span class="o">);</span>
</code></pre></div></div>

<p>컴파일러는 꺼낸 값이 <code class="language-plaintext highlighter-rouge">String</code>인지 <code class="language-plaintext highlighter-rouge">Integer</code>인지 확신할 수 없으므로 직접 형변환해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="o">(</span><span class="nc">String</span><span class="o">)</span> <span class="n">list</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="mi">1</span><span class="o">);</span>
</code></pre></div></div>

<p>하지만 인덱스 <code class="language-plaintext highlighter-rouge">1</code>에는 정수 <code class="language-plaintext highlighter-rouge">100</code>이 들어 있으므로 실행 중에 <code class="language-plaintext highlighter-rouge">ClassCastException</code>이 발생할 수 있다.</p>

<p>이러한 문제를 방지하기 위해 제네릭을 사용한다.</p>

<h2 id="4-제네릭의-장점">4. 제네릭의 장점</h2>

<h3 id="타입-안정성">타입 안정성</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ArrayList</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">list</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>

<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"민형"</span><span class="o">);</span> <span class="c1">// 가능</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span>    <span class="c1">// 컴파일 오류</span>
</code></pre></div></div>

<p>컴파일러는 처음부터 이 리스트가 <code class="language-plaintext highlighter-rouge">String</code>만 저장하는 곳이라는 사실을 알고 있다. 따라서 잘못된 타입을 실행 전에 차단한다. 이것이 타입 안정성이다.</p>

<h3 id="형변환-불필요">형변환 불필요</h3>

<p>제네릭이 없다면 값을 꺼낸 뒤 직접 형변환해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Object</span> <span class="n">value</span> <span class="o">=</span> <span class="n">list</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="mi">0</span><span class="o">);</span> 
<span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="o">(</span><span class="nc">String</span><span class="o">)</span> <span class="n">value</span><span class="o">;</span> 
</code></pre></div></div>

<p>제네릭을 사용하면 꺼낸 값의 타입을 컴파일러가 알고 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ArrayList</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">list</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span> 
<span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="n">list</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="mi">0</span><span class="o">);</span>
</code></pre></div></div>

<p>따라서 바로 <code class="language-plaintext highlighter-rouge">String</code> 변수로 받을 수 있다.</p>

<h3 id="여러-타입에-재사용">여러 타입에 재사용</h3>

<p>다음 <code class="language-plaintext highlighter-rouge">Box</code> 클래스는 <code class="language-plaintext highlighter-rouge">String</code>만 저장할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Box</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">value</span><span class="o">;</span> 

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setValue</span><span class="o">(</span><span class="nc">String</span> <span class="n">value</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">value</span> <span class="o">=</span> <span class="n">value</span><span class="o">;</span> 
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getValue</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">value</span><span class="o">;</span> 
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>제네릭을 사용하면 저장할 타입을 나중에 정할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Box</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="no">T</span> <span class="n">value</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setValue</span><span class="o">(</span><span class="no">T</span> <span class="n">value</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">value</span> <span class="o">=</span> <span class="n">value</span><span class="o">;</span> 
    <span class="o">}</span>

    <span class="kd">public</span> <span class="no">T</span> <span class="nf">getValue</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">value</span><span class="o">;</span> 
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">T</code>는 타입 매개변수이며, 실제 타입은 <code class="language-plaintext highlighter-rouge">Box</code> 객체를 사용할 때 결정한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Box</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">stringBox</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Box</span><span class="o">&lt;&gt;();</span>
<span class="nc">Box</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numberBox</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Box</span><span class="o">&lt;&gt;();</span>

<span class="n">stringBox</span><span class="o">.</span><span class="na">setValue</span><span class="o">(</span><span class="s">"민형"</span><span class="o">);</span>
<span class="n">numberBox</span><span class="o">.</span><span class="na">setValue</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span>

<span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="n">stringBox</span><span class="o">.</span><span class="na">getValue</span><span class="o">();</span>
<span class="nc">Integer</span> <span class="n">number</span> <span class="o">=</span> <span class="n">numberBox</span><span class="o">.</span><span class="na">getValue</span><span class="o">();</span>
</code></pre></div></div>

<p>같은 <code class="language-plaintext highlighter-rouge">Box&lt;T&gt;</code> 코드를 <code class="language-plaintext highlighter-rouge">String</code>과 <code class="language-plaintext highlighter-rouge">Integer</code> 타입에 모두 재사용할 수 있다.</p>

<h2 id="5-iterator와-제네릭">5. Iterator와 제네릭</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Iterator</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">iterator</span> <span class="o">=</span> <span class="n">history</span><span class="o">.</span><span class="na">iterator</span><span class="o">();</span>
<span class="nc">String</span> <span class="n">record</span> <span class="o">=</span> <span class="n">iterator</span><span class="o">.</span><span class="na">next</span><span class="o">();</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Iterator&lt;String&gt;</code>은 순회하면서 꺼내는 데이터가 <code class="language-plaintext highlighter-rouge">String</code>이라는 뜻이다. 따라서 <code class="language-plaintext highlighter-rouge">iterator.next()</code>의 반환값도 바로 <code class="language-plaintext highlighter-rouge">String</code>으로 받을 수 있다.</p>

<h2 id="6-자주-사용하는-제네릭-타입">6. 자주 사용하는 제네릭 타입</h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">List&lt;String&gt;</code></li>
  <li><code class="language-plaintext highlighter-rouge">ArrayList&lt;Integer&gt;</code></li>
  <li><code class="language-plaintext highlighter-rouge">Iterator&lt;String&gt;</code></li>
  <li><code class="language-plaintext highlighter-rouge">Map&lt;String, Integer&gt;</code></li>
  <li><code class="language-plaintext highlighter-rouge">Optional&lt;User&gt;</code></li>
</ul>

<p><code class="language-plaintext highlighter-rouge">Map&lt;String, Integer&gt;</code>는 키가 <code class="language-plaintext highlighter-rouge">String</code>, 값이 <code class="language-plaintext highlighter-rouge">Integer</code>라는 뜻이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">scores</span><span class="o">;</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Key   → String
Value → Integer
</code></pre></div></div>

<h2 id="7-왜-기본형-대신-wrapper-class를-사용할까">7. 왜 기본형 대신 Wrapper Class를 사용할까?</h2>

<p>제네릭의 타입 인자에는 기본형을 직접 사용할 수 없다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 허용되지 않음</span>
<span class="nc">ArrayList</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">numbers</span><span class="o">;</span> 
</code></pre></div></div>

<p>제네릭은 참조 타입을 대상으로 하기 때문에 Wrapper Class를 사용해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 허용됨</span>
<span class="nc">ArrayList</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numbers</span><span class="o">;</span> 
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int     → Integer
double  → Double
boolean → Boolean
char    → Character
</code></pre></div></div>

<h2 id="8-결론">8. 결론</h2>

<ol>
  <li>
    <p>타입 안정성</p>

    <p>잘못된 타입을 컴파일 단계에서 차단한다.</p>
  </li>
  <li>
    <p>형변환 감소</p>

    <p>값을 꺼낼 때 불필요한 타입 캐스팅을 줄인다.</p>
  </li>
  <li>
    <p>코드 재사용</p>

    <p>하나의 클래스나 메서드를 여러 타입에 활용할 수 있다.</p>
  </li>
</ol>

<blockquote>
  <p>제네릭은 클래스나 메서드가 사용할 데이터 타입을 외부에서 지정할 수 있게 한다. 이를 통해 타입 안정성을 확보하고, 불필요한 형변환을 줄이며, 같은 코드를 여러 타입에서 재사용할 수 있다.</p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">ArrayList&lt;String&gt;</code>을 사용할 때 이미 제네릭을 사용하고 있었던 것이다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="basics" /><category term="Java" /><category term="Generic" /><category term="ArrayList" /><category term="Collection" /><category term="Type Safety" /><summary type="html"><![CDATA[Java 제네릭으로 컬렉션이 다룰 데이터 타입을 지정하는 방법을 ArrayList 예제로 정리한다.]]></summary></entry><entry><title type="html">Java 디버깅 Part 1: 자주 헷갈리는 핵심 개념</title><link href="http://localhost:4000/java/debugging/2026/08/11/java-confusing-concepts-debugging/" rel="alternate" type="text/html" title="Java 디버깅 Part 1: 자주 헷갈리는 핵심 개념" /><published>2026-08-11T09:00:00+09:00</published><updated>2026-08-11T09:00:00+09:00</updated><id>http://localhost:4000/java/debugging/2026/08/11/java-confusing-concepts-debugging</id><content type="html" xml:base="http://localhost:4000/java/debugging/2026/08/11/java-confusing-concepts-debugging/"><![CDATA[<h1 id="java-디버깅-part-1-자주-헷갈리는-핵심-개념">Java 디버깅 Part 1: 자주 헷갈리는 핵심 개념</h1>

<h2 id="1-클래스와-객체">1. 클래스와 객체</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
</code></pre></div></div>

<ul>
  <li>User -&gt; 클래스 타입</li>
  <li>user -&gt; 참조 변수</li>
  <li>new - &gt; 새로오 객체를 생성하는 키워드</li>
  <li>User() -&gt; 생성자 호출</li>
</ul>

<p>클래스는 이미 존재하고, 그 클래스를 기반으로 객체를 새로 만드는 것</p>

<blockquote>
  <p>User 클래스를 기반으로 새로운 객체를 생성ㅅ, 그 객체의 참조를 user 변수에 저장한다.</p>
</blockquote>

<h2 id="2-생성자와-this">2. 생성자와 This</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="nc">User</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li>String name; -&gt; 인자(argument)가 아니라 field
    <ul>
      <li>User 객체가 가지는 필드</li>
    </ul>
  </li>
</ul>

<p><code class="language-plaintext highlighter-rouge">User(String name)</code>
    - 여기있는 name은 Parameter(매개변수)</p>

<p><code class="language-plaintext highlighter-rouge">new User("민형")</code>
    - 여기에 들어가는 “민형”이 argument(인자)</p>

<p><code class="language-plaintext highlighter-rouge">String name;</code> -&gt; field</p>

<p><code class="language-plaintext highlighter-rouge">User(String name)</code> -&gt; parameter</p>

<p><code class="language-plaintext highlighter-rouge">new User("민형")</code> -&gt; “민형”은 argument</p>

<p>this.name은 생성자로 전달받은 name parameter 값을 현재 객체의 name field에 저장한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"민형"
    ↓ argument
String name
    ↓ parameter
this.name = name
    ↓
현재 객체의 name field = "민형"
</code></pre></div></div>

<p>User 객체를 생성하면 “민형”을 생성자의 argument로 전달하고, 생성자는 그 값을 현재 객체의 name 필드에 저장한다.</p>

<h2 id="3-참조변수">3. 참조변수</h2>

<p>JAVA 언어 수준에서 메모리 직접 다루는 것은 아니기 때문에,</p>

<blockquote>
  <p>같은 객체를 참조한다. 라고 서술하는 것이 더 안전한 편이다.</p>
</blockquote>

<ul>
  <li>b에는 새로운 객체가 생성되는 것이 아니라 a가 가진 참조값이 복사되므로, a 와 b는 같은 User 객체를 가리킨다.</li>
</ul>

<h2 id="4-static">4. Static</h2>

<blockquote>
  <p>static은 특정 객체가 아니라, 클래스 자체에 속하는 멤버를 정의하고, 모든 인스턴스가 하나의 값을 공유하도록 한다.</p>
</blockquote>

<h2 id="5-static-final">5. Static final</h2>

<blockquote>
  <p>final 참조 변수는 다른 객체로 재할당할 수 없다.</p>
</blockquote>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">static</span> <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">(</span><span class="s">"민형"</span><span class="o">);</span>

<span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">(</span><span class="s">"철수"</span><span class="o">);</span> <span class="o">-&gt;</span> <span class="n">가능하다</span><span class="o">.</span> 
</code></pre></div></div>

<p>다만</p>

<p><code class="language-plaintext highlighter-rouge">static final User user = new User("민형");</code> 같은 경우에는 
<code class="language-plaintext highlighter-rouge">user = new User("철수");</code> 로 재할당이 불가능하다.</p>

<p>user가 가리키는 객체를 다른 객체로 바꾸지 못하게 하는 것</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>final user ─X──→ [다른 User 객체]
</code></pre></div></div>

<p>다만 객체 내부는 이와 같은 변경이 가능하다</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[User 객체]
name = "민형"
      ↓
name = "철수"
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>코드</th>
      <th style="text-align: right">static</th>
      <th style="text-align: right">static final</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">user = new User("철수")</code></td>
      <td style="text-align: right">가능</td>
      <td style="text-align: right">불가능</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">user.name = "철수"</code></td>
      <td style="text-align: right">가능</td>
      <td style="text-align: right">가능</td>
    </tr>
  </tbody>
</table>

<p>단, name 자체도 final이거나 setter가 막혀 있으면 별개야.</p>

<blockquote>
  <p>Static은 클래스 단위의 공유 여부를 결정하고, final은 변수에 저장된 값을 다시 할당할 수 있는지를 제한한다.</p>
</blockquote>

<h2 id="6-package--import">6. Package / import</h2>

<blockquote>
  <p>package는 클래스의 소속과 이름 공간(namespace)를 정의하는 역할</p>
</blockquote>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">package</span> <span class="nn">com.practice.model</span><span class="o">;</span>
</code></pre></div></div>

<p>의 의미는 이 클래는 com.practice.model 패키지에 속한다.</p>

<p>그리고서는</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">com.practice.model.User</span><span class="o">;</span>
</code></pre></div></div>

<p>는:</p>

<blockquote>
  <p>다른 패키지에 있는 User 클래스를 이 소스 코드에서 짧은 이름 User로 사용할 수 있게 된다.</p>
</blockquote>

<blockquote>
  <p>어떤 클래스를 말하는지 컴파일러가 알 수 있도록 이름을 연결해주는 것에 가깝다.</p>
</blockquote>

<ul>
  <li>왜 Main 안에서 임포트하냐..?</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">package</span> <span class="nn">com.practice</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">com.practice.model.User</span><span class="o">;</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Main</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>import는 Main 클래스 밖, 파일 위쪽에 있는 이유</p>

<p>Main은 com.practice에 있고, 
User는 com.practice.model에 있기 때문</p>

<p>꼭 import할 필요는 없는데</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">com</span><span class="o">.</span><span class="na">practice</span><span class="o">.</span><span class="na">model</span><span class="o">.</span><span class="na">User</span> <span class="n">user</span> <span class="o">=</span>
    <span class="k">new</span> <span class="n">com</span><span class="o">.</span><span class="na">practice</span><span class="o">.</span><span class="na">model</span><span class="o">.</span><span class="na">User</span><span class="o">();</span>
</code></pre></div></div>

<p>라고 풀네임으로 적어도 되긴하다.</p>

<p>하지만 귀찮으니까</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">com.practice.model.User</span><span class="o">;</span>
</code></pre></div></div>

<p>를 하고 나서</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>
</code></pre></div></div>

<h2 id="7-추상화">7. 추상화</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Animal</span> <span class="n">animal</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Animal</span><span class="o">();</span>
</code></pre></div></div>

<p>부모 클래스에 sound()가 없는 게 아니라 존재한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">abstract</span> <span class="kt">void</span> <span class="nf">sound</span><span class="o">();</span>
</code></pre></div></div>

<p>하지만 구현 내용이 없는 추상 메서드여서 구현을 반드시 구현을 해야한다.</p>

<p>eat() 사용 가능하다는 것도 맞긴 하다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Void</span> <span class="nf">eat</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"먹는다"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>부모 클래스에서 구현됐으니까 Dog가 상속받아 사용할 수 있다.</p>

<blockquote>
  <p>정리하자면</p>
</blockquote>

<p>Animal은 모든 동물이 sound() 기능을 가져야 한다는 공통 규칙만 추상적으로 정의하고, 실제 소리를 내는 방법은 자식 클래스가 구현하도록 해야한다.</p>

<h2 id="가장-많이-헷갈리는-용어">가장 많이 헷갈리는 용어</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>

    <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="nc">User</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">(</span><span class="s">"민형"</span><span class="o">);</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">User</code> -&gt; 클래스 / 타입</p>

<p><code class="language-plaintext highlighter-rouge">String name;</code> -&gt; field</p>

<p><code class="language-plaintext highlighter-rouge">User(String name)</code> -&gt; 생성자</p>

<p><code class="language-plaintext highlighter-rouge">"민형"</code> -&gt; argument</p>

<p><code class="language-plaintext highlighter-rouge">this</code> -&gt; 현재 객체</p>

<p><code class="language-plaintext highlighter-rouge">this.name</code> -&gt; 현재 객체의 field</p>

<p><code class="language-plaintext highlighter-rouge">user</code> -&gt; 참조 변수</p>

<p><code class="language-plaintext highlighter-rouge">new User("민형")</code>
-&gt; 새 User 객체 생성 + 생성 호출</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="debugging" /><category term="Java" /><category term="Debugging" /><category term="OOP" /><category term="Class" /><category term="Constructor" /><category term="Static" /><category term="Package" /><category term="Import" /><category term="Abstract" /><summary type="html"><![CDATA[Java에서 자주 헷갈리는 클래스와 객체, 생성자와 this, static final, package import, 추상화 개념을 디버깅 관점에서 정리한다.]]></summary></entry><entry><title type="html">Java 디버깅 Part 2: VS Code 자동 컴파일과 프로젝트 구조</title><link href="http://localhost:4000/java/debugging/2026/08/11/java-vscode-auto-compile-debugging/" rel="alternate" type="text/html" title="Java 디버깅 Part 2: VS Code 자동 컴파일과 프로젝트 구조" /><published>2026-08-11T09:00:00+09:00</published><updated>2026-08-11T09:00:00+09:00</updated><id>http://localhost:4000/java/debugging/2026/08/11/java-vscode-auto-compile-debugging</id><content type="html" xml:base="http://localhost:4000/java/debugging/2026/08/11/java-vscode-auto-compile-debugging/"><![CDATA[<h1 id="java-디버깅-part-2-vs-code-자동-컴파일과-프로젝트-구조">Java 디버깅 Part 2: VS Code 자동 컴파일과 프로젝트 구조</h1>

<p>VS Code에서는 왜 자동 컴파일이 바로 안 보이는 걸까? 내가 굳이 그 디렉토리로 이동해서 컴파일해야 하는 걸까? 코드가 바뀌면 알아서 다시 컴파일해 주는 도구는 없는 걸까?</p>

<p>일단 <code class="language-plaintext highlighter-rouge">자동 컴파일</code>과 내가 보고 있는 폴더에 <code class="language-plaintext highlighter-rouge">.class</code> 파일이 생기는 것은 같은 얘기가 아니다.</p>

<h2 id="vs-code에서는-어떻게-실행되는가">VS Code에서는 어떻게 실행되는가</h2>

<p>예를 들어 <code class="language-plaintext highlighter-rouge">Calculator.java</code>가 아래처럼 있다고 하자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Calculator</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="kt">int</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">10</span> <span class="o">+</span> <span class="mi">20</span><span class="o">;</span>

        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">result</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>VS Code에서 Java 프로젝트가 정상적으로 잡혀 있다면, 저장할 때 내부적으로 최신 클래스를 만들고 <code class="language-plaintext highlighter-rouge">Run Java</code>로 실행할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Calculator.java 수정
    ↓
Ctrl+S / Cmd+S
    ↓
Java Language Server
    ↓
자동 빌드
    ↓
내부 bin에 최신 class 생성
    ↓
Run Java
    ↓
최신 코드 실행
</code></pre></div></div>

<p>그래서 매번 아래처럼 직접 컴파일할 필요는 없다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>6.exception
javac Calculator.java
java Calculator
</code></pre></div></div>

<p>다만 VS Code가 지금 열어 둔 폴더를 Java 프로젝트로 제대로 인식했을 때 이야기다.</p>

<h2 id="프로젝트-구조가-애매한-경우">프로젝트 구조가 애매한 경우</h2>

<p>아래처럼 챕터별 폴더만 있는 단순 실습 구조라면 VS Code가 이 폴더를 일반적인 Java 프로젝트로 정확히 잡지 못할 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>java-springboot/
├── 1.basic/
├── 2.object/
├── 3.inheritance/
├── 6.exception/
│   └── Calculator.java
└── ...
</code></pre></div></div>

<p>이런 경우 VS Code는 폴더를 <code class="language-plaintext highlighter-rouge">unmanaged folder</code> 형태로 처리할 수 있다. 그래서 내가 원하는 위치에 <code class="language-plaintext highlighter-rouge">.class</code>가 안 보이거나, 직접 <code class="language-plaintext highlighter-rouge">javac</code>를 실행해야 하는 것처럼 느껴질 수 있다.</p>

<p>보통 Java에서는 Maven이나 Gradle 프로젝트 구조를 사용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>project/
├── pom.xml
└── src/
    └── main/
        └── java/
            └── ...
</code></pre></div></div>

<p>이 구조면 Maven이나 Gradle이 소스 폴더, 빌드 결과 폴더, 필요한 라이브러리를 알고 있어서 훨씬 안정적으로 동작한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Maven / Gradle
    ↓
소스 탐색
    ↓
의존성 해결
    ↓
컴파일
    ↓
테스트
    ↓
패키징
</code></pre></div></div>

<p>VS Code도 Maven과 Gradle을 Java 빌드 도구 워크플로로 지원한다.</p>

<h2 id="코드-변경-시-자동-재실행까지">코드 변경 시 자동 재실행까지</h2>

<p>자동 컴파일은 저장한 코드를 다시 빌드하는 것까지다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>코드 저장
    ↓
.class 갱신
</code></pre></div></div>

<p>실행 중인 애플리케이션까지 자동으로 다시 시작하려면 별도 도구가 필요하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>코드 저장
    ↓
컴파일
    ↓
실행 중인 애플리케이션 재시작
</code></pre></div></div>

<p>Spring Boot를 쓰는 단계가 되면 <code class="language-plaintext highlighter-rouge">spring-boot-devtools</code>를 추가해서 개발 중 재시작을 더 편하게 할 수 있다.</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;dependency&gt;</span>
    <span class="nt">&lt;groupId&gt;</span>org.springframework.boot<span class="nt">&lt;/groupId&gt;</span>
    <span class="nt">&lt;artifactId&gt;</span>spring-boot-devtools<span class="nt">&lt;/artifactId&gt;</span>
    <span class="nt">&lt;scope&gt;</span>runtime<span class="nt">&lt;/scope&gt;</span>
<span class="nt">&lt;/dependency&gt;</span>
</code></pre></div></div>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="debugging" /><category term="Java" /><category term="Debugging" /><category term="VS Code" /><category term="Compiler" /><category term="Maven" /><category term="Gradle" /><category term="Spring Boot" /><category term="DevTools" /><summary type="html"><![CDATA[VS Code의 Java 자동 빌드 동작, .class 파일 위치, Maven과 Gradle 기반 프로젝트 구조, 자동 재시작 도구를 정리한다.]]></summary></entry><entry><title type="html">Java 객체지향 Part 4: 상속과 인터페이스</title><link href="http://localhost:4000/java/oop/2026/08/10/java-inheritance-interface/" rel="alternate" type="text/html" title="Java 객체지향 Part 4: 상속과 인터페이스" /><published>2026-08-10T13:20:00+09:00</published><updated>2026-08-10T13:20:00+09:00</updated><id>http://localhost:4000/java/oop/2026/08/10/java-inheritance-interface</id><content type="html" xml:base="http://localhost:4000/java/oop/2026/08/10/java-inheritance-interface/"><![CDATA[<h1 id="java-객체지향-part-4-상속과-인터페이스">Java 객체지향 Part 4: 상속과 인터페이스</h1>

<h2 id="1-상속">1. 상속</h2>

<p>상속은 <code class="language-plaintext highlighter-rouge">Superclass</code>의 상태와 동작을 <code class="language-plaintext highlighter-rouge">Subclass</code>가 물려받아 재사용하고 확장하는 방법이다.</p>

<p>일반적으로 기능을 물려받는 쪽을 자식 클래스, 물려주는 쪽을 부모 클래스라고 한다.</p>

<h2 id="2-인터페이스">2. 인터페이스</h2>

<blockquote>
  <p>공통 규칙을 정의하고, 서로 다른 구현 객체를 같은 타입으로 처리해 다형성을 활용할 수 있다.</p>
</blockquote>

<ul>
  <li>클래스가 구현해야 하는 메서드의 규약(Contract)를 정의하는 참조 타입</li>
  <li>메서드 시그니쳐 (이름, 매개변수, 반환형)만 선언하고 실제 구현은 인터페이스를 구현한 클래스가 담당</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li>class 키워드 대신 interface 키워드 활용</li>
  <li>메소드의 정의만 해두고 구현체는 존재하지 않음</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 1. 인터페이스 선언</span>
<span class="kd">interface</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">();</span>
<span class="o">}</span>

<span class="c1">// 2. Dog 구현체</span>
<span class="kd">class</span> <span class="nc">Dog</span> <span class="kd">implements</span> <span class="nc">Animal</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"멍멍!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="c1">// 3. Cat 구현체</span>
<span class="kd">class</span> <span class="nc">Cat</span> <span class="kd">implements</span> <span class="nc">Animal</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"야옹!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="c1">// 4. Main 클래스</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">AnimalSound</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// Animal 타입으로 각각의 구현 객체를 참조</span>
        <span class="nc">Animal</span> <span class="n">dog</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Dog</span><span class="o">();</span>
        <span class="nc">Animal</span> <span class="n">cat</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Cat</span><span class="o">();</span>

        <span class="n">dog</span><span class="o">.</span><span class="na">makeSound</span><span class="o">();</span> <span class="c1">// 멍멍!</span>
        <span class="n">cat</span><span class="o">.</span><span class="na">makeSound</span><span class="o">();</span> <span class="c1">// 야옹!</span>

        <span class="c1">// Animal 타입을 받는 메서드에 Cat 객체 전달</span>
        <span class="n">handleAnimalSound</span><span class="o">(</span><span class="n">cat</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="c1">// Animal을 구현한 객체라면 전달 가능</span>
    <span class="kd">static</span> <span class="kt">void</span> <span class="nf">handleAnimalSound</span><span class="o">(</span><span class="nc">Animal</span> <span class="n">animal</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">animal</span><span class="o">.</span><span class="na">makeSound</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="override-annotation-사용-이유"><code class="language-plaintext highlighter-rouge">@Override</code> Annotation 사용 이유</h3>

<p>다음과 같은 이유로 사용 권고</p>

<ol>
  <li>컴파일러 체크 기능</li>
  <li>가독성향상</li>
  <li>리팩토링 안정성</li>
</ol>

<h3 id="인터페이스-메서드-접근자">인터페이스 메서드 접근자</h3>

<p>인터페이스의 추상 메서드는 기본적으로 <code class="language-plaintext highlighter-rouge">public abstract</code>이며, 명시하지 않아도 자동으로 적용된다.</p>

<h2 id="3-다중-상속">3. 다중 상속</h2>

<p>Java 클래스는 다중 상속을 지원하지 않지만, 여러 인터페이스를 동시에 구현할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">interface</span> <span class="nc">Flyable</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">fly</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">interface</span> <span class="nc">Swimmable</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">swim</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">abstract</span> <span class="kd">class</span> <span class="nc">Bird</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">moving</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"움직입니다!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="c1">// 하나의 클래스가 두 인터페이스를 모두 구현</span>
<span class="kd">class</span> <span class="nc">Duck</span> <span class="kd">extends</span> <span class="nc">Bird</span> <span class="kd">implements</span> <span class="nc">Flyable</span><span class="o">,</span> <span class="nc">Swimmable</span> <span class="o">{</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">fly</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"오리가 하늘을 납니다!"</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">swim</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"오리가 물 위를 헤엄칩니다!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="nc">Duck</span> <span class="n">duck</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Duck</span><span class="o">();</span>
<span class="n">duck</span><span class="o">.</span><span class="na">fly</span><span class="o">();</span>
<span class="n">duck</span><span class="o">.</span><span class="na">swim</span><span class="o">();</span>
<span class="n">duck</span><span class="o">.</span><span class="na">moving</span><span class="o">();</span>
</code></pre></div></div>

<p>클래스는 다중 상속을 지원하지 않는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">A</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">print</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"A"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">B</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">print</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"B"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="c1">// 컴파일 오류: Java 클래스는 두 클래스를 동시에 상속할 수 없다.</span>
<span class="kd">class</span> <span class="nc">C</span> <span class="kd">extends</span> <span class="no">A</span><span class="o">,</span> <span class="no">B</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">A</code>와 <code class="language-plaintext highlighter-rouge">B</code>에 같은 <code class="language-plaintext highlighter-rouge">print()</code> 메서드가 있으면 어느 메서드를 물려받아야 하는지 모호해지기 때문에 클래스 다중 상속을 허용하지 않는다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="oop" /><category term="Java" /><category term="OOP" /><category term="Inheritance" /><category term="Interface" /><category term="Override" /><category term="Polymorphism" /><summary type="html"><![CDATA[Java의 상속, 인터페이스, Override 애너테이션과 다중 구현 방식을 예제로 정리한다.]]></summary></entry><entry><title type="html">Java 객체지향 Part 3: 좋은 설계와 OOP 4대 특성</title><link href="http://localhost:4000/java/oop/2026/08/10/java-oop-design-four-pillars/" rel="alternate" type="text/html" title="Java 객체지향 Part 3: 좋은 설계와 OOP 4대 특성" /><published>2026-08-10T13:00:00+09:00</published><updated>2026-08-10T13:00:00+09:00</updated><id>http://localhost:4000/java/oop/2026/08/10/java-oop-design-four-pillars</id><content type="html" xml:base="http://localhost:4000/java/oop/2026/08/10/java-oop-design-four-pillars/"><![CDATA[<h1 id="java-객체지향-part-3-좋은-설계와-oop-4대-특성">Java 객체지향 Part 3: 좋은 설계와 OOP 4대 특성</h1>

<h2 id="1-oop란">1. OOP란?</h2>

<p>OOP(Object-Oriented Programming)는 프로그램을 여러 <strong>객체(Object)</strong>로 나누고, 객체들이 서로 요청하고 응답하면서 문제를 해결하도록 설계하는 방식이다.</p>

<p>예를 들어 사람이 상품을 주문하는 상황은 다음과 같이 표현할 수 있다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Customer</code> 객체가 <code class="language-plaintext highlighter-rouge">Product</code> 객체에 주문을 요청한다.</li>
  <li><code class="language-plaintext highlighter-rouge">Product</code> 객체는 자신의 가격과 재고를 확인한다.</li>
  <li>각 객체는 맡은 역할과 책임에 따라 동작한다.</li>
</ul>

<p>즉, 객체는 자신의 상태를 관리하고 필요한 동작을 수행하는 프로그램의 구성 단위다.</p>

<h2 id="2-핵심-용어">2. 핵심 용어</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉬운 예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>클래스(Class)</td>
      <td>객체를 만들기 위한 설계도</td>
      <td><code class="language-plaintext highlighter-rouge">Stock</code>이라는 주식 설계도</td>
    </tr>
    <tr>
      <td>객체(Object)</td>
      <td>클래스를 바탕으로 만들어진 대상</td>
      <td>삼성전자 주식 객체</td>
    </tr>
    <tr>
      <td>상태(State)</td>
      <td>객체가 가지고 있는 데이터</td>
      <td>종목명, 가격</td>
    </tr>
    <tr>
      <td>행동(Behavior)</td>
      <td>객체가 수행할 수 있는 기능</td>
      <td>가격 변경, 정보 출력</td>
    </tr>
    <tr>
      <td>책임(Responsibility)</td>
      <td>객체가 맡아서 처리해야 하는 일</td>
      <td>주식 객체가 올바른 가격을 관리하는 일</td>
    </tr>
    <tr>
      <td>메시지(Message)</td>
      <td>다른 객체의 메서드를 호출해 작업을 요청하는 것</td>
      <td><code class="language-plaintext highlighter-rouge">stock.setPrice(80000)</code></td>
    </tr>
  </tbody>
</table>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>   <span class="c1">// 상태</span>
    <span class="kd">private</span> <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>  <span class="c1">// 상태</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">()</span> <span class="o">{</span> <span class="c1">// 행동</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">name</span> <span class="o">+</span> <span class="s">": "</span> <span class="o">+</span> <span class="n">price</span> <span class="o">+</span> <span class="s">"원"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="3-좋은-객체지향-설계">3. 좋은 객체지향 설계</h2>

<p>좋은 객체지향 설계는 일반적으로 <strong>높은 응집도</strong>와 <strong>낮은 결합도</strong>를 지향한다.</p>

<h3 id="응집도cohesion">응집도(Cohesion)</h3>

<p>하나의 클래스 안에 서로 밀접한 역할과 기능이 모여 있는 정도다. 한 클래스가 하나의 명확한 책임에 집중할수록 응집도가 높다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Stock 클래스 → 종목명과 가격 관리
Order 클래스 → 주문 생성과 주문 상태 관리
</code></pre></div></div>

<h3 id="결합도coupling">결합도(Coupling)</h3>

<p>클래스가 다른 클래스에 의존하는 정도다. 의존성이 낮을수록 한 클래스의 변경이 다른 클래스에 미치는 영향이 작다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>높은 응집도 + 낮은 결합도
→ 이해하기 쉽고, 수정과 확장이 쉬운 코드
</code></pre></div></div>

<h2 id="4-oop의-네-가지-핵심-특징">4. OOP의 네 가지 핵심 특징</h2>

<h3 id="41-캡슐화encapsulation">4.1 캡슐화(Encapsulation)</h3>

<p>캡슐화는 객체의 데이터와 기능을 하나로 묶고, 내부 데이터를 외부에서 함부로 변경하지 못하도록 숨기는 것이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">double</span> <span class="nf">getPrice</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setPrice</span><span class="o">(</span><span class="kt">double</span> <span class="n">price</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">price</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"가격은 0보다 커야 합니다."</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">private</code> 필드는 외부에서 직접 접근할 수 없다.</li>
  <li>공개된 메서드를 통해서만 값을 조회하거나 변경한다.</li>
  <li>메서드 안에서 유효성 검사를 수행해 객체의 상태를 안전하게 보호할 수 있다.</li>
</ul>

<h3 id="42-추상화abstraction">4.2 추상화(Abstraction)</h3>

<p>추상화는 복잡한 내부 구현은 숨기고, 사용하는 사람이 알아야 할 기능만 보여 주는 것이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">interface</span> <span class="nc">Printable</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<p>사용자는 <code class="language-plaintext highlighter-rouge">printInfo()</code>가 내부에서 어떻게 동작하는지 몰라도 “정보를 출력하는 기능”이라는 사실만 알면 사용할 수 있다.</p>

<h3 id="43-상속inheritance">4.3 상속(Inheritance)</h3>

<p>상속은 기존 클래스의 필드와 메서드를 새로운 클래스가 물려받아 확장하는 기능이다. Java에서는 <code class="language-plaintext highlighter-rouge">extends</code>를 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="kd">protected</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kd">protected</span> <span class="kt">double</span> <span class="n">price</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">Stock</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="kt">double</span> <span class="n">price</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">price</span> <span class="o">=</span> <span class="n">price</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"[일반주] "</span> <span class="o">+</span> <span class="n">name</span> <span class="o">+</span> <span class="s">", "</span> <span class="o">+</span> <span class="n">price</span> <span class="o">+</span> <span class="s">"원"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">PreferredStock</span> <span class="kd">extends</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kt">double</span> <span class="n">dividendRate</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">PreferredStock</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="kt">double</span> <span class="n">price</span><span class="o">,</span> <span class="kt">double</span> <span class="n">dividendRate</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">(</span><span class="n">name</span><span class="o">,</span> <span class="n">price</span><span class="o">);</span>
        <span class="k">this</span><span class="o">.</span><span class="na">dividendRate</span> <span class="o">=</span> <span class="n">dividendRate</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">PreferredStock</code>은 <code class="language-plaintext highlighter-rouge">Stock</code>의 필드와 메서드를 물려받는다.</li>
  <li><code class="language-plaintext highlighter-rouge">super(...)</code>는 부모 클래스의 생성자를 호출한다.</li>
  <li>상속은 두 클래스가 명확한 <strong>is-a 관계</strong>일 때 사용하는 것이 좋다.</li>
</ul>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PreferredStock is a Stock.
우선주는 주식이다.
</code></pre></div></div>

<h3 id="44-다형성polymorphism">4.4 다형성(Polymorphism)</h3>

<p>다형성은 같은 타입의 메서드를 호출하더라도 실제 객체에 따라 서로 다르게 동작하는 성질이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"일반주 정보"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">PreferredStock</span> <span class="kd">extends</span> <span class="nc">Stock</span> <span class="o">{</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printInfo</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"우선주 정보"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Main</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">Stock</span> <span class="n">stock1</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Stock</span><span class="o">();</span>
        <span class="nc">Stock</span> <span class="n">stock2</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">PreferredStock</span><span class="o">();</span>

        <span class="n">stock1</span><span class="o">.</span><span class="na">printInfo</span><span class="o">();</span> <span class="c1">// 일반주 정보</span>
        <span class="n">stock2</span><span class="o">.</span><span class="na">printInfo</span><span class="o">();</span> <span class="c1">// 우선주 정보</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">stock2</code>의 변수 타입은 <code class="language-plaintext highlighter-rouge">Stock</code>이지만 실제 객체는 <code class="language-plaintext highlighter-rouge">PreferredStock</code>이다. 따라서 실행할 때는 자식 클래스가 재정의한 <code class="language-plaintext highlighter-rouge">printInfo()</code>가 호출된다.</p>

<h2 id="5-오버로딩과-오버라이딩">5. 오버로딩과 오버라이딩</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>오버로딩(Overloading)</th>
      <th>오버라이딩(Overriding)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>의미</td>
      <td>같은 이름의 메서드를 매개변수에 따라 여러 개 정의</td>
      <td>부모 메서드를 자식 클래스에서 다시 정의</td>
    </tr>
    <tr>
      <td>조건</td>
      <td>매개변수의 개수 또는 타입이 달라야 함</td>
      <td>메서드 이름과 매개변수가 같아야 함</td>
    </tr>
    <tr>
      <td>목적</td>
      <td>다양한 입력 방식 제공</td>
      <td>객체에 따라 다른 동작 제공</td>
    </tr>
    <tr>
      <td>관련 개념</td>
      <td>컴파일 시점 다형성</td>
      <td>실행 시점 다형성</td>
    </tr>
  </tbody>
</table>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Calculator</span> <span class="o">{</span>
    <span class="kt">int</span> <span class="nf">add</span><span class="o">(</span><span class="kt">int</span> <span class="n">a</span><span class="o">,</span> <span class="kt">int</span> <span class="n">b</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kt">double</span> <span class="nf">add</span><span class="o">(</span><span class="kt">double</span> <span class="n">a</span><span class="o">,</span> <span class="kt">double</span> <span class="n">b</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>위 코드는 매개변수 타입이 다른 <code class="language-plaintext highlighter-rouge">add()</code> 메서드를 여러 개 정의한 오버로딩 예시다.</p>

<h2 id="6-절차적-프로그래밍과-oop-비교">6. 절차적 프로그래밍과 OOP 비교</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>절차적 프로그래밍</th>
      <th>객체지향 프로그래밍</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>중심 단위</td>
      <td>함수와 실행 순서</td>
      <td>클래스와 객체</td>
    </tr>
    <tr>
      <td>데이터 처리</td>
      <td>데이터와 함수를 주로 분리</td>
      <td>데이터와 메서드를 객체 안에 묶음</td>
    </tr>
    <tr>
      <td>실행 흐름</td>
      <td>정해진 순서에 따라 함수 호출</td>
      <td>객체끼리 메시지를 주고받으며 동작</td>
    </tr>
    <tr>
      <td>재사용</td>
      <td>함수 단위 재사용</td>
      <td>클래스, 객체, 상속, 조합을 통한 재사용</td>
    </tr>
    <tr>
      <td>유지보수</td>
      <td>규모가 커지면 데이터 흐름 추적이 어려울 수 있음</td>
      <td>역할과 책임이 잘 분리되면 변경 범위를 줄이기 쉬움</td>
    </tr>
    <tr>
      <td>대표 개념</td>
      <td>순차, 조건, 반복, 함수</td>
      <td>캡슐화, 추상화, 상속, 다형성</td>
    </tr>
    <tr>
      <td>대표 언어</td>
      <td>C, Pascal, Fortran</td>
      <td>Java, C++, C#, Python</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>C++가 객체지향 프로그래밍을 지원하며, C는 대표적인 절차적 프로그래밍 언어다.</p>
</blockquote>

<h2 id="7-핵심-정리">7. 핵심 정리</h2>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 문장 정의</th>
      <th>Java의 주요 구현 방법</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>객체</td>
      <td>상태와 행동을 함께 가지는 프로그램 구성 단위</td>
      <td><code class="language-plaintext highlighter-rouge">new</code>로 인스턴스 생성</td>
    </tr>
    <tr>
      <td>캡슐화</td>
      <td>데이터를 숨기고 공개된 메서드로 접근을 제어</td>
      <td><code class="language-plaintext highlighter-rouge">private</code>, getter, setter</td>
    </tr>
    <tr>
      <td>추상화</td>
      <td>복잡한 구현을 숨기고 필요한 기능만 노출</td>
      <td><code class="language-plaintext highlighter-rouge">interface</code>, <code class="language-plaintext highlighter-rouge">abstract class</code></td>
    </tr>
    <tr>
      <td>상속</td>
      <td>기존 클래스의 기능을 물려받아 확장</td>
      <td><code class="language-plaintext highlighter-rouge">extends</code>, <code class="language-plaintext highlighter-rouge">super</code></td>
    </tr>
    <tr>
      <td>다형성</td>
      <td>같은 호출이 실제 객체에 따라 다르게 동작</td>
      <td>오버라이딩, 부모 타입 참조</td>
    </tr>
    <tr>
      <td>응집도</td>
      <td>한 클래스의 기능이 하나의 책임에 집중된 정도</td>
      <td>단일 책임을 가진 클래스 설계</td>
    </tr>
    <tr>
      <td>결합도</td>
      <td>클래스끼리 서로 의존하는 정도</td>
      <td>인터페이스를 통한 의존성 분리</td>
    </tr>
  </tbody>
</table>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>객체지향 프로그래밍의 핵심
= 객체마다 역할과 책임을 나누고
+ 내부 상태는 안전하게 보호하며
+ 객체들이 협력하도록 설계하는 것
</code></pre></div></div>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="oop" /><category term="Java" /><category term="OOP" /><category term="Object" /><category term="Encapsulation" /><category term="Abstraction" /><category term="Inheritance" /><category term="Polymorphism" /><summary type="html"><![CDATA[응집도와 결합도, 캡슐화, 추상화, 상속, 다형성을 중심으로 객체지향 설계 원칙을 정리한다.]]></summary></entry></feed>