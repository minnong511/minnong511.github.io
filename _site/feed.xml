<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://minnong511.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://minnong511.github.io/" rel="alternate" type="text/html" /><updated>2026-08-16T17:57:30+09:00</updated><id>https://minnong511.github.io/feed.xml</id><title type="html">Minnong’s Study Log</title><subtitle>개발과 AI를 배우고, 실험하고, 이해한 내용을 연결하는 공부 기록</subtitle><author><name>Min Hyeong Lee</name></author><entry><title type="html">Spring 기초 Part 4: JPA, Entity와 Repository</title><link href="https://minnong511.github.io/java/spring/2026/08/16/spring-data-jpa-basics/" rel="alternate" type="text/html" title="Spring 기초 Part 4: JPA, Entity와 Repository" /><published>2026-08-16T09:00:00+09:00</published><updated>2026-08-16T09:00:00+09:00</updated><id>https://minnong511.github.io/java/spring/2026/08/16/spring-data-jpa-basics</id><content type="html" xml:base="https://minnong511.github.io/java/spring/2026/08/16/spring-data-jpa-basics/"><![CDATA[<h1 id="spring-기초-part-4-jpa-entity와-repository">Spring 기초 Part 4: JPA, Entity와 Repository</h1>

<blockquote>
  <p>JPA는 자바 객체와 데이터베이스 테이블을 매핑하여 객체 중심으로 데이터를 다룰 수 있게 해주는 자바 표준이다.</p>
</blockquote>

<p>개발자는 Entity와 Repository를 작성하고, JPA 구현체는 객체 정보를 바탕으로 SQL을 만든다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Java 객체
   ↓
Spring Data JPA
   ↓
JPA
   ↓
Hibernate
   ↓
JDBC
   ↓
Database
</code></pre></div></div>

<hr />

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>관계형 데이터베이스</td>
      <td>데이터를 테이블 사이의 관계로 저장하는 시스템</td>
      <td>H2, MySQL, PostgreSQL</td>
    </tr>
    <tr>
      <td>테이블</td>
      <td>데이터를 행과 열로 저장하는 구조</td>
      <td>사용자 목록을 저장하는 표</td>
    </tr>
    <tr>
      <td>행(Row)</td>
      <td>테이블에 저장된 데이터 한 건</td>
      <td>사용자 한 명</td>
    </tr>
    <tr>
      <td>컬럼(Column)</td>
      <td>데이터가 가지는 속성</td>
      <td>이름, 이메일, 나이</td>
    </tr>
    <tr>
      <td>기본키(PK)</td>
      <td>각 행을 유일하게 구분하는 값</td>
      <td>사용자 <code class="language-plaintext highlighter-rouge">id</code></td>
    </tr>
    <tr>
      <td>ORM</td>
      <td>객체와 관계형 테이블을 연결하는 기술</td>
      <td><code class="language-plaintext highlighter-rouge">User</code>와 <code class="language-plaintext highlighter-rouge">users</code>를 연결</td>
    </tr>
    <tr>
      <td>JPA</td>
      <td>자바 ORM 사용법을 정의한 표준</td>
      <td>Entity와 관련 어노테이션 규칙</td>
    </tr>
    <tr>
      <td>Hibernate</td>
      <td>JPA 표준을 실제로 구현한 라이브러리</td>
      <td>객체를 보고 SQL을 생성</td>
    </tr>
    <tr>
      <td>JDBC</td>
      <td>자바가 DB에 SQL을 전달하는 API</td>
      <td>Hibernate가 생성한 SQL 전달</td>
    </tr>
    <tr>
      <td>Entity</td>
      <td>JPA가 관리하고 테이블과 매핑하는 객체</td>
      <td><code class="language-plaintext highlighter-rouge">User</code> 클래스</td>
    </tr>
    <tr>
      <td>Repository</td>
      <td>데이터 저장과 조회를 담당하는 계층</td>
      <td><code class="language-plaintext highlighter-rouge">UserRepository</code></td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="2-orm은-왜-필요한가">2. ORM은 왜 필요한가</h2>

<p>자바는 객체를 사용하고 관계형 데이터베이스는 테이블을 사용한다.</p>

<table>
  <thead>
    <tr>
      <th>자바 객체</th>
      <th>데이터베이스</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>클래스</td>
      <td>테이블</td>
    </tr>
    <tr>
      <td>객체</td>
      <td>행</td>
    </tr>
    <tr>
      <td>필드</td>
      <td>컬럼</td>
    </tr>
    <tr>
      <td>객체 참조</td>
      <td>외래키</td>
    </tr>
  </tbody>
</table>

<p>두 구조가 다르기 때문에 JDBC만 사용하면 SQL 작성과 결과 변환을 직접 처리해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">String</span> <span class="n">sql</span> <span class="o">=</span> <span class="s">"SELECT id, name, email FROM users WHERE id = ?"</span><span class="o">;</span>

<span class="c1">// Connection, PreparedStatement, ResultSet 처리</span>
<span class="c1">// ResultSet 값을 꺼내 User 객체로 직접 변환</span>
</code></pre></div></div>

<p>ORM을 사용하면 매핑 정보를 기준으로 반복 코드를 줄일 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>
</code></pre></div></div>

<p>ORM이 SQL 지식을 없애주는 것은 아니다. 생성되는 SQL과 데이터베이스 구조를 이해해야 성능 문제를 찾을 수 있다.</p>

<hr />

<h2 id="3-jpa-hibernate-spring-data-jpa의-차이">3. JPA, Hibernate, Spring Data JPA의 차이</h2>

<p>세 단어는 같은 뜻이 아니다.</p>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>JPA</td>
      <td>ORM을 사용하는 인터페이스와 어노테이션을 정의한 표준</td>
    </tr>
    <tr>
      <td>Hibernate</td>
      <td>JPA 표준을 구현하여 실제 SQL을 생성하고 Entity를 관리</td>
    </tr>
    <tr>
      <td>Spring Data JPA</td>
      <td>Repository 인터페이스를 쉽게 작성하도록 도와주는 Spring 프로젝트</td>
    </tr>
  </tbody>
</table>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발자가 UserRepository 호출
          ↓
Spring Data JPA가 Repository 구현 제공
          ↓
JPA 표준 방식으로 Entity 관리 요청
          ↓
Hibernate가 SQL 생성
          ↓
JDBC가 DB에 SQL 전달
</code></pre></div></div>

<blockquote>
  <p>JPA는 규칙이고, Hibernate는 그 규칙의 구현체이며, Spring Data JPA는 JPA를 편하게 사용하도록 도와준다.</p>
</blockquote>

<hr />

<h2 id="4-entity-만들기">4. Entity 만들기</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">jakarta.persistence.Column</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.Entity</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.GeneratedValue</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.GenerationType</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.Id</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.persistence.Table</span><span class="o">;</span>

<span class="nd">@Entity</span>
<span class="nd">@Table</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>

    <span class="nd">@Id</span>
    <span class="nd">@GeneratedValue</span><span class="o">(</span><span class="n">strategy</span> <span class="o">=</span> <span class="nc">GenerationType</span><span class="o">.</span><span class="na">IDENTITY</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">;</span>

    <span class="nd">@Column</span><span class="o">(</span><span class="n">nullable</span> <span class="o">=</span> <span class="kc">false</span><span class="o">,</span> <span class="n">length</span> <span class="o">=</span> <span class="mi">50</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="nd">@Column</span><span class="o">(</span><span class="n">nullable</span> <span class="o">=</span> <span class="kc">false</span><span class="o">,</span> <span class="n">unique</span> <span class="o">=</span> <span class="kc">true</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">email</span><span class="o">;</span>

    <span class="kd">protected</span> <span class="nf">User</span><span class="o">()</span> <span class="o">{</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nf">User</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="nc">String</span> <span class="n">email</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">name</span> <span class="o">=</span> <span class="n">name</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">email</span> <span class="o">=</span> <span class="n">email</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Entity 클래스에는 다음 조건이 필요하다.</p>

<ul>
  <li>클래스에 <code class="language-plaintext highlighter-rouge">@Entity</code>를 붙인다.</li>
  <li><code class="language-plaintext highlighter-rouge">@Id</code>로 기본키를 지정한다.</li>
  <li>JPA가 사용할 <code class="language-plaintext highlighter-rouge">public</code> 또는 <code class="language-plaintext highlighter-rouge">protected</code> 기본 생성자가 필요하다.</li>
  <li>Entity 클래스 자체를 <code class="language-plaintext highlighter-rouge">final</code>로 선언하지 않는다.</li>
</ul>

<h3 id="주요-어노테이션">주요 어노테이션</h3>

<table>
  <thead>
    <tr>
      <th>어노테이션</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Entity</code></td>
      <td>클래스를 JPA가 관리할 Entity로 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Table</code></td>
      <td>매핑할 테이블 이름 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Id</code></td>
      <td>기본키 필드 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@GeneratedValue</code></td>
      <td>기본키 자동 생성 전략 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Column</code></td>
      <td>컬럼 이름과 제약조건 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Enumerated</code></td>
      <td>Enum을 저장할 방식 지정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Transient</code></td>
      <td>DB 컬럼과 매핑하지 않을 필드 지정</td>
    </tr>
  </tbody>
</table>

<h3 id="generatedvalue"><code class="language-plaintext highlighter-rouge">@GeneratedValue</code></h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Id</span>
<span class="nd">@GeneratedValue</span><span class="o">(</span><span class="n">strategy</span> <span class="o">=</span> <span class="nc">GenerationType</span><span class="o">.</span><span class="na">IDENTITY</span><span class="o">)</span>
<span class="kd">private</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">IDENTITY</code> 전략은 기본키 생성을 데이터베이스에 맡긴다. 새로운 사용자를 만들 때 개발자가 <code class="language-plaintext highlighter-rouge">id</code>를 직접 지정하지 않는다.</p>

<h3 id="column"><code class="language-plaintext highlighter-rouge">@Column</code></h3>

<table>
  <thead>
    <tr>
      <th>속성</th>
      <th>의미</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">name</code></td>
      <td>매핑할 컬럼명</td>
      <td><code class="language-plaintext highlighter-rouge">@Column(name = "user_name")</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">nullable</code></td>
      <td><code class="language-plaintext highlighter-rouge">NULL</code> 허용 여부</td>
      <td><code class="language-plaintext highlighter-rouge">@Column(nullable = false)</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">length</code></td>
      <td>문자열 컬럼 길이</td>
      <td><code class="language-plaintext highlighter-rouge">@Column(length = 50)</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">unique</code></td>
      <td>중복 허용 여부</td>
      <td><code class="language-plaintext highlighter-rouge">@Column(unique = true)</code></td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">@Column</code> 설정만 믿기보다 운영 DB에는 마이그레이션 도구 등을 이용해 실제 제약조건도 함께 관리해야 한다.</p>

<hr />

<h2 id="5-repository-만들기">5. Repository 만들기</h2>

<p>Spring Data JPA의 <code class="language-plaintext highlighter-rouge">JpaRepository</code>를 상속하면 기본 CRUD 기능을 사용할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">UserRepository</span> <span class="kd">extends</span> <span class="nc">JpaRepository</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">,</span> <span class="nc">Long</span><span class="o">&gt;</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p>두 타입의 의미는 다음과 같다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">User</code>: Repository가 관리할 Entity 타입</li>
  <li><code class="language-plaintext highlighter-rouge">Long</code>: <code class="language-plaintext highlighter-rouge">User</code>의 기본키 타입</li>
</ul>

<p>기본 제공 메서드는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>메서드</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">save(entity)</code></td>
      <td>새 Entity 저장 또는 기존 Entity 반영</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">findById(id)</code></td>
      <td>기본키로 한 건 조회</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">findAll()</code></td>
      <td>전체 조회</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">existsById(id)</code></td>
      <td>기본키에 해당하는 데이터 존재 여부 확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">delete(entity)</code></td>
      <td>Entity 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">deleteById(id)</code></td>
      <td>기본키로 삭제</td>
    </tr>
  </tbody>
</table>

<p>메서드 이름을 분석해 조회 조건을 만들 수도 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Optional</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="nf">findByEmail</span><span class="o">(</span><span class="nc">String</span> <span class="n">email</span><span class="o">);</span>

<span class="kt">boolean</span> <span class="nf">existsByEmail</span><span class="o">(</span><span class="nc">String</span> <span class="n">email</span><span class="o">);</span>

<span class="nc">List</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="nf">findByNameContaining</span><span class="o">(</span><span class="nc">String</span> <span class="n">keyword</span><span class="o">);</span>
</code></pre></div></div>

<hr />

<h2 id="6-사용자-저장-흐름">6. 사용자 저장 흐름</h2>

<h3 id="service">Service</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserRepository</span> <span class="n">userRepository</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserService</span><span class="o">(</span><span class="nc">UserRepository</span> <span class="n">userRepository</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userRepository</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@Transactional</span>
    <span class="kd">public</span> <span class="nc">Long</span> <span class="nf">createUser</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="nc">String</span> <span class="n">email</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">(</span><span class="n">name</span><span class="o">,</span> <span class="n">email</span><span class="o">);</span>
        <span class="nc">User</span> <span class="n">savedUser</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
        <span class="k">return</span> <span class="n">savedUser</span><span class="o">.</span><span class="na">getId</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>실행 흐름은 다음과 같다.</p>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>Spring Data JPA 저장 흐름</span>
  </header>
  <div class="mermaid js-mermaid">
flowchart TD
    S[Service가 User 생성] --&gt; R[UserRepository.save 호출]
    R --&gt; SD[Spring Data JPA]
    SD --&gt; H[Hibernate가 Entity 관리]
    H --&gt; SQL[INSERT SQL 생성]
    SQL --&gt; J[JDBC가 SQL 전달]
    J --&gt; DB[(Database 저장)]
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<p>Hibernate는 Entity의 매핑 정보를 보고 대략 다음과 같은 SQL을 실행한다.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">users</span> <span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="n">email</span><span class="p">)</span>
<span class="k">VALUES</span> <span class="p">(</span><span class="s1">'민형'</span><span class="p">,</span> <span class="s1">'min@example.com'</span><span class="p">);</span>
</code></pre></div></div>

<hr />

<h2 id="7-jpa와-jdbc-비교">7. JPA와 JDBC 비교</h2>

<table>
  <thead>
    <tr>
      <th>JPA</th>
      <th>JDBC</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>객체와 테이블을 매핑한다.</td>
      <td>SQL을 직접 작성한다.</td>
    </tr>
    <tr>
      <td>반복적인 CRUD 코드를 줄인다.</td>
      <td>결과를 객체로 직접 변환한다.</td>
    </tr>
    <tr>
      <td>변경 감지와 연관관계 기능을 제공한다.</td>
      <td>실행 흐름이 SQL 중심으로 명확하다.</td>
    </tr>
    <tr>
      <td>잘못 사용하면 예상하지 못한 SQL이 발생할 수 있다.</td>
      <td>반복 코드가 많아질 수 있다.</td>
    </tr>
  </tbody>
</table>

<p>JPA도 내부적으로 JDBC를 사용해 SQL을 전달한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JPA와 JDBC 중 하나만 선택
</code></pre></div></div>

<p>이 아니라 다음 구조에 가깝다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JPA / Hibernate
      ↓
    JDBC
      ↓
  Database
</code></pre></div></div>

<hr />

<h2 id="8-h2-연결-설정">8. H2 연결 설정</h2>

<p>H2는 학습과 테스트에 많이 사용하는 가벼운 관계형 데이터베이스다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">spring</span><span class="pi">:</span>
  <span class="na">datasource</span><span class="pi">:</span>
    <span class="na">url</span><span class="pi">:</span> <span class="s">jdbc:h2:mem:testdb</span>
    <span class="na">driver-class-name</span><span class="pi">:</span> <span class="s">org.h2.Driver</span>
    <span class="na">username</span><span class="pi">:</span> <span class="s">sa</span>
    <span class="na">password</span><span class="pi">:</span>

  <span class="na">h2</span><span class="pi">:</span>
    <span class="na">console</span><span class="pi">:</span>
      <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>
      <span class="na">path</span><span class="pi">:</span> <span class="s">/h2-console</span>

  <span class="na">jpa</span><span class="pi">:</span>
    <span class="na">hibernate</span><span class="pi">:</span>
      <span class="na">ddl-auto</span><span class="pi">:</span> <span class="s">create-drop</span>
    <span class="na">show-sql</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>설정</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">datasource</code></td>
      <td>연결할 DB 주소와 계정 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ddl-auto</code></td>
      <td>Entity를 기준으로 테이블을 관리하는 방식 설정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">create-drop</code></td>
      <td>시작할 때 생성하고 종료할 때 삭제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">show-sql</code></td>
      <td>Hibernate가 실행하는 SQL 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">h2.console.enabled</code></td>
      <td>H2 웹 콘솔 활성화</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">create-drop</code>은 애플리케이션 종료 시 테이블을 삭제하므로 운영 환경에서 사용하면 안 된다.</p>

<hr />

<h2 id="핵심-정리">핵심 정리</h2>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>한 줄 정리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ORM</td>
      <td>자바 객체와 관계형 테이블을 연결하는 기술</td>
    </tr>
    <tr>
      <td>JPA</td>
      <td>자바 ORM 사용법을 정의한 표준</td>
    </tr>
    <tr>
      <td>Hibernate</td>
      <td>JPA를 실제로 구현한 라이브러리</td>
    </tr>
    <tr>
      <td>Spring Data JPA</td>
      <td>Repository 작성을 편하게 도와주는 Spring 프로젝트</td>
    </tr>
    <tr>
      <td>Entity</td>
      <td>JPA가 관리하며 테이블과 매핑되는 객체</td>
    </tr>
    <tr>
      <td>Repository</td>
      <td>Entity의 저장과 조회를 담당하는 데이터 접근 계층</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>JPA를 사용한다는 것은 SQL을 몰라도 된다는 뜻이 아니라, 객체와 테이블의 매핑을 이용해 반복적인 데이터 접근 코드를 줄인다는 뜻이다.</p>
</blockquote>

<h2 id="참고-자료">참고 자료</h2>

<ul>
  <li><a href="https://docs.spring.io/spring-data/jpa/reference/">Spring Data JPA 공식 문서</a></li>
  <li><a href="https://jakarta.ee/specifications/persistence/">Jakarta Persistence 공식 명세</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="Spring Boot" /><category term="JPA" /><category term="Hibernate" /><category term="Spring Data JPA" /><category term="Entity" /><summary type="html"><![CDATA[ORM, JPA, Hibernate, Entity, Repository의 역할과 Spring Data JPA의 기본 CRUD 흐름을 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 3: REST API 요청값과 입력값 검증</title><link href="https://minnong511.github.io/java/spring/2026/08/15/spring-rest-api-validation/" rel="alternate" type="text/html" title="Spring 기초 Part 3: REST API 요청값과 입력값 검증" /><published>2026-08-15T09:00:00+09:00</published><updated>2026-08-15T09:00:00+09:00</updated><id>https://minnong511.github.io/java/spring/2026/08/15/spring-rest-api-validation</id><content type="html" xml:base="https://minnong511.github.io/java/spring/2026/08/15/spring-rest-api-validation/"><![CDATA[<h1 id="spring-기초-part-3-rest-api-요청값과-입력값-검증">Spring 기초 Part 3: REST API 요청값과 입력값 검증</h1>

<blockquote>
  <p>Controller는 HTTP 요청값을 자바 값이나 객체로 변환하고, 규칙에 맞는지 검증한 뒤 Service에 전달한다.</p>
</blockquote>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTTP Request
    ↓
요청값 바인딩
    ↓
입력값 검증
    ↓
Controller
    ↓
Service
</code></pre></div></div>

<hr />

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Endpoint</td>
      <td>Client가 호출할 수 있는 API 주소와 HTTP Method의 조합</td>
      <td><code class="language-plaintext highlighter-rouge">POST /api/users</code></td>
    </tr>
    <tr>
      <td>Parameter</td>
      <td>HTTP 요청에 포함된 입력값</td>
      <td>사용자 ID, 검색어, 페이지 번호</td>
    </tr>
    <tr>
      <td>Request Body</td>
      <td>HTTP 요청 본문에 담긴 데이터</td>
      <td>JSON 회원가입 정보</td>
    </tr>
    <tr>
      <td>Binding</td>
      <td>HTTP 요청값을 자바 값이나 객체로 변환하는 과정</td>
      <td>JSON을 <code class="language-plaintext highlighter-rouge">UserCreateRequest</code>로 변환</td>
    </tr>
    <tr>
      <td>DTO</td>
      <td>계층 사이에서 데이터를 전달하는 객체</td>
      <td>API 요청 전용 객체</td>
    </tr>
    <tr>
      <td>Validation</td>
      <td>값이 정해진 규칙을 만족하는지 검사하는 과정</td>
      <td>이메일 형식과 나이 확인</td>
    </tr>
    <tr>
      <td>Constraint</td>
      <td>입력값이 지켜야 하는 검증 규칙</td>
      <td><code class="language-plaintext highlighter-rouge">@NotBlank</code>, <code class="language-plaintext highlighter-rouge">@Email</code></td>
    </tr>
    <tr>
      <td>직렬화</td>
      <td>자바 객체를 JSON 같은 전송 형식으로 변환</td>
      <td><code class="language-plaintext highlighter-rouge">UserResponse</code> → JSON</td>
    </tr>
    <tr>
      <td>역직렬화</td>
      <td>JSON을 자바 객체로 변환</td>
      <td>JSON → <code class="language-plaintext highlighter-rouge">UserCreateRequest</code></td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="2-http-요청값을-받는-세-가지-방법">2. HTTP 요청값을 받는 세 가지 방법</h2>

<table>
  <thead>
    <tr>
      <th>어노테이션</th>
      <th>값을 가져오는 위치</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PathVariable</code></td>
      <td>URL 경로</td>
      <td><code class="language-plaintext highlighter-rouge">/api/users/1</code>의 <code class="language-plaintext highlighter-rouge">1</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestParam</code></td>
      <td>Query String</td>
      <td><code class="language-plaintext highlighter-rouge">/api/users?name=민형</code>의 <code class="language-plaintext highlighter-rouge">name</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestBody</code></td>
      <td>HTTP 요청 본문</td>
      <td>JSON 회원가입 정보</td>
    </tr>
  </tbody>
</table>

<h3 id="pathvariable"><code class="language-plaintext highlighter-rouge">@PathVariable</code></h3>

<p>특정 자원을 식별할 때 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/api/users/{id}"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">getUser</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">getUser</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">GET /api/users/1
</span></code></pre></div></div>

<h3 id="requestparam"><code class="language-plaintext highlighter-rouge">@RequestParam</code></h3>

<p>검색, 필터, 정렬처럼 선택적인 조건을 전달할 때 자주 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/api/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">UserResponse</span><span class="o">&gt;</span> <span class="nf">searchUsers</span><span class="o">(</span>
        <span class="nd">@RequestParam</span><span class="o">(</span><span class="n">required</span> <span class="o">=</span> <span class="kc">false</span><span class="o">)</span> <span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">searchUsers</span><span class="o">(</span><span class="n">name</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">GET /api/users?name=민형
</span></code></pre></div></div>

<h3 id="requestbody"><code class="language-plaintext highlighter-rouge">@RequestBody</code></h3>

<p>JSON 같은 요청 본문을 자바 객체로 변환할 때 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@PostMapping</span><span class="o">(</span><span class="s">"/api/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">createUser</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">UserCreateRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">createUser</span><span class="o">(</span><span class="n">request</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"민형"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"min@example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"age"</span><span class="p">:</span><span class="w"> </span><span class="mi">25</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Spring MVC와 Jackson이 JSON을 <code class="language-plaintext highlighter-rouge">UserCreateRequest</code> 객체로 변환한다.</p>

<hr />

<h2 id="3-entity-대신-요청-dto를-사용한다">3. Entity 대신 요청 DTO를 사용한다</h2>

<p>요청 JSON을 Entity로 바로 받으면 API 입력 구조와 DB 구조가 강하게 연결된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 권장하지 않는 형태</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">createUser</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">User</span> <span class="n">user</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// 요청이 Entity 구조에 직접 의존</span>
<span class="o">}</span>
</code></pre></div></div>

<p>요청 전용 DTO를 만들면 API가 허용할 값과 검증 규칙을 명확하게 표현할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="n">record</span> <span class="nf">UserCreateRequest</span><span class="o">(</span>
        <span class="nc">String</span> <span class="n">name</span><span class="o">,</span>
        <span class="nc">String</span> <span class="n">email</span><span class="o">,</span>
        <span class="kt">int</span> <span class="n">age</span>
<span class="o">)</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>객체</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Request DTO</td>
      <td>Client가 보낸 입력값 표현</td>
    </tr>
    <tr>
      <td>Entity</td>
      <td>데이터베이스에 저장하고 JPA가 관리</td>
    </tr>
    <tr>
      <td>Response DTO</td>
      <td>Client에 반환할 데이터 표현</td>
    </tr>
  </tbody>
</table>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JSON
  → Request DTO
  → Service
  → Entity
  → Repository
  → Database
</code></pre></div></div>

<hr />

<h2 id="4-bean-validation">4. Bean Validation</h2>

<p>Bean Validation은 어노테이션으로 입력값 규칙을 선언하는 표준이다. Spring Boot 3 계열에서는 일반적으로 <code class="language-plaintext highlighter-rouge">jakarta.validation</code> 패키지를 사용한다.</p>

<p>검증 기능을 사용하려면 Validation Starter를 추가한다.</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;dependency&gt;</span>
    <span class="nt">&lt;groupId&gt;</span>org.springframework.boot<span class="nt">&lt;/groupId&gt;</span>
    <span class="nt">&lt;artifactId&gt;</span>spring-boot-starter-validation<span class="nt">&lt;/artifactId&gt;</span>
<span class="nt">&lt;/dependency&gt;</span>
</code></pre></div></div>

<h3 id="자주-사용하는-검증-어노테이션">자주 사용하는 검증 어노테이션</h3>

<table>
  <thead>
    <tr>
      <th>어노테이션</th>
      <th>검증 내용</th>
      <th>적용 예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@NotNull</code></td>
      <td>값이 <code class="language-plaintext highlighter-rouge">null</code>이면 실패</td>
      <td>객체, 숫자 필수값</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@NotBlank</code></td>
      <td><code class="language-plaintext highlighter-rouge">null</code>, 빈 문자열, 공백 문자열이면 실패</td>
      <td>이름, 제목</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@NotEmpty</code></td>
      <td><code class="language-plaintext highlighter-rouge">null</code>이거나 길이가 0이면 실패</td>
      <td>문자열, 컬렉션</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Email</code></td>
      <td>이메일 형식 검사</td>
      <td>이메일 주소</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Positive</code></td>
      <td>0보다 큰 값인지 검사</td>
      <td>가격, 수량</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Min</code></td>
      <td>지정한 최솟값 이상인지 검사</td>
      <td>나이</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Max</code></td>
      <td>지정한 최댓값 이하인지 검사</td>
      <td>점수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Size</code></td>
      <td>문자열이나 컬렉션의 길이 검사</td>
      <td>비밀번호 길이</td>
    </tr>
  </tbody>
</table>

<p>요청 DTO에 규칙을 선언한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">jakarta.validation.constraints.Email</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.validation.constraints.Max</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.validation.constraints.Min</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">jakarta.validation.constraints.NotBlank</span><span class="o">;</span>

<span class="kd">public</span> <span class="n">record</span> <span class="nf">UserCreateRequest</span><span class="o">(</span>
        <span class="nd">@NotBlank</span><span class="o">(</span><span class="n">message</span> <span class="o">=</span> <span class="s">"이름은 필수입니다."</span><span class="o">)</span>
        <span class="nc">String</span> <span class="n">name</span><span class="o">,</span>

        <span class="nd">@NotBlank</span><span class="o">(</span><span class="n">message</span> <span class="o">=</span> <span class="s">"이메일은 필수입니다."</span><span class="o">)</span>
        <span class="nd">@Email</span><span class="o">(</span><span class="n">message</span> <span class="o">=</span> <span class="s">"이메일 형식이 올바르지 않습니다."</span><span class="o">)</span>
        <span class="nc">String</span> <span class="n">email</span><span class="o">,</span>

        <span class="nd">@Min</span><span class="o">(</span><span class="n">value</span> <span class="o">=</span> <span class="mi">1</span><span class="o">,</span> <span class="n">message</span> <span class="o">=</span> <span class="s">"나이는 1 이상이어야 합니다."</span><span class="o">)</span>
        <span class="nd">@Max</span><span class="o">(</span><span class="n">value</span> <span class="o">=</span> <span class="mi">150</span><span class="o">,</span> <span class="n">message</span> <span class="o">=</span> <span class="s">"나이는 150 이하여야 합니다."</span><span class="o">)</span>
        <span class="kt">int</span> <span class="n">age</span>
<span class="o">)</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<hr />

<h2 id="5-valid로-검증-실행하기">5. <code class="language-plaintext highlighter-rouge">@Valid</code>로 검증 실행하기</h2>

<p>DTO에 검증 어노테이션만 붙이면 끝이 아니다. Controller 매개변수에 <code class="language-plaintext highlighter-rouge">@Valid</code>를 붙여 검증을 실행한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/api/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserController</span><span class="o">(</span><span class="nc">UserService</span> <span class="n">userService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userService</span> <span class="o">=</span> <span class="n">userService</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">UserResponse</span><span class="o">&gt;</span> <span class="nf">createUser</span><span class="o">(</span>
            <span class="nd">@Valid</span> <span class="nd">@RequestBody</span> <span class="nc">UserCreateRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>

        <span class="nc">UserResponse</span> <span class="n">response</span> <span class="o">=</span> <span class="n">userService</span><span class="o">.</span><span class="na">createUser</span><span class="o">(</span><span class="n">request</span><span class="o">);</span>

        <span class="k">return</span> <span class="nc">ResponseEntity</span>
                <span class="o">.</span><span class="na">status</span><span class="o">(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">CREATED</span><span class="o">)</span>
                <span class="o">.</span><span class="na">body</span><span class="o">(</span><span class="n">response</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>검증 흐름은 다음과 같다.</p>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>Request Body 검증 흐름</span>
  </header>
  <div class="mermaid js-mermaid">
flowchart TD
    C[Client JSON 요청] --&gt; J[Jackson이 DTO로 변환]
    J --&gt; V[@Valid 검증]
    V --&gt;|성공| CT[Controller 실행]
    CT --&gt; S[Service 실행]
    V --&gt;|실패| E[MethodArgumentNotValidException]
    E --&gt; R[400 Bad Request 응답]
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<p>검증에 실패하면 보통 <code class="language-plaintext highlighter-rouge">MethodArgumentNotValidException</code>이 발생하고 HTTP 400 응답으로 처리된다.</p>

<hr />

<h2 id="6-검증-오류-응답-통일하기">6. 검증 오류 응답 통일하기</h2>

<p><code class="language-plaintext highlighter-rouge">@RestControllerAdvice</code>를 사용하면 여러 Controller의 예외를 한곳에서 처리할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestControllerAdvice</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GlobalExceptionHandler</span> <span class="o">{</span>

    <span class="nd">@ExceptionHandler</span><span class="o">(</span><span class="nc">MethodArgumentNotValidException</span><span class="o">.</span><span class="na">class</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;&gt;</span> <span class="nf">handleValidation</span><span class="o">(</span>
            <span class="nc">MethodArgumentNotValidException</span> <span class="n">exception</span><span class="o">)</span> <span class="o">{</span>

        <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">String</span><span class="o">&gt;</span> <span class="n">errors</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">LinkedHashMap</span><span class="o">&lt;&gt;();</span>

        <span class="n">exception</span><span class="o">.</span><span class="na">getBindingResult</span><span class="o">()</span>
                <span class="o">.</span><span class="na">getFieldErrors</span><span class="o">()</span>
                <span class="o">.</span><span class="na">forEach</span><span class="o">(</span><span class="n">error</span> <span class="o">-&gt;</span> <span class="n">errors</span><span class="o">.</span><span class="na">put</span><span class="o">(</span>
                        <span class="n">error</span><span class="o">.</span><span class="na">getField</span><span class="o">(),</span>
                        <span class="n">error</span><span class="o">.</span><span class="na">getDefaultMessage</span><span class="o">()</span>
                <span class="o">));</span>

        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">.</span><span class="na">badRequest</span><span class="o">().</span><span class="na">body</span><span class="o">(</span><span class="n">errors</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>잘못된 요청을 보내면 다음과 같은 응답을 만들 수 있다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"이름은 필수입니다."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"이메일 형식이 올바르지 않습니다."</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<hr />

<h2 id="7-직접-요청해보기">7. 직접 요청해보기</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-i</span> <span class="nt">-X</span> POST <span class="s1">'http://localhost:8080/api/users'</span> <span class="se">\</span>
  <span class="nt">-H</span> <span class="s1">'Content-Type: application/json'</span> <span class="se">\</span>
  <span class="nt">-d</span> <span class="s1">'{
    "name": "민형",
    "email": "min@example.com",
    "age": 25
  }'</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>부분</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-i</code></td>
      <td>응답 Header까지 출력</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-X POST</code></td>
      <td>POST Method 사용</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Content-Type</code></td>
      <td>요청 본문이 JSON임을 알림</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-d</code></td>
      <td>서버로 보낼 요청 본문</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="8-형식-검증과-비즈니스-검증">8. 형식 검증과 비즈니스 검증</h2>

<p><code class="language-plaintext highlighter-rouge">@Valid</code>는 값의 기본 형식을 검사하는 데 적합하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@NotBlank → 이름이 비어 있는가?
@Email    → 이메일 형태가 맞는가?
@Positive → 수량이 양수인가?
</code></pre></div></div>

<p>하지만 DB 조회가 필요한 업무 규칙은 Service에서 검사하는 것이 자연스럽다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">createUser</span><span class="o">(</span><span class="nc">UserCreateRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">userRepository</span><span class="o">.</span><span class="na">existsByEmail</span><span class="o">(</span><span class="n">request</span><span class="o">.</span><span class="na">email</span><span class="o">()))</span> <span class="o">{</span>
        <span class="k">throw</span> <span class="k">new</span> <span class="nf">DuplicateEmailException</span><span class="o">(</span><span class="n">request</span><span class="o">.</span><span class="na">email</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="nc">User</span><span class="o">.</span><span class="na">create</span><span class="o">(</span>
            <span class="n">request</span><span class="o">.</span><span class="na">name</span><span class="o">(),</span>
            <span class="n">request</span><span class="o">.</span><span class="na">email</span><span class="o">(),</span>
            <span class="n">request</span><span class="o">.</span><span class="na">age</span><span class="o">()</span>
    <span class="o">);</span>

    <span class="k">return</span> <span class="nc">UserResponse</span><span class="o">.</span><span class="na">from</span><span class="o">(</span><span class="n">userRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">user</span><span class="o">));</span>
<span class="o">}</span>
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>검증 위치</th>
      <th>담당할 규칙</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Request DTO</td>
      <td>빈 값, 길이, 숫자 범위, 이메일 형식</td>
    </tr>
    <tr>
      <td>Service</td>
      <td>이메일 중복, 재고 부족, 권한 등 업무 규칙</td>
    </tr>
    <tr>
      <td>Database</td>
      <td><code class="language-plaintext highlighter-rouge">NOT NULL</code>, <code class="language-plaintext highlighter-rouge">UNIQUE</code>, 외래키 등 최종 무결성</td>
    </tr>
  </tbody>
</table>

<p>검증을 한 계층에만 의존하지 않고 각 계층이 책임져야 할 규칙을 나누는 것이 중요하다.</p>

<hr />

<h2 id="핵심-정리">핵심 정리</h2>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>핵심 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PathVariable</code></td>
      <td>URL 경로의 식별자를 받는다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestParam</code></td>
      <td>Query String의 검색 조건을 받는다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestBody</code></td>
      <td>JSON 요청 본문을 자바 객체로 변환한다.</td>
    </tr>
    <tr>
      <td>DTO</td>
      <td>API와 Entity의 구조를 분리한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Valid</code></td>
      <td>DTO에 선언한 Bean Validation을 실행한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RestControllerAdvice</code></td>
      <td>여러 Controller의 오류 응답을 통일한다.</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Controller는 요청값을 받고 검증하는 입구이며, 실제 비즈니스 규칙은 Service에 맡긴다.</p>
</blockquote>

<h2 id="참고-자료">참고 자료</h2>

<ul>
  <li><a href="https://docs.spring.io/spring-framework/reference/core/validation/beanvalidation.html">Spring Framework Validation 공식 문서</a></li>
  <li><a href="https://docs.spring.io/spring-framework/reference/web/webmvc.html">Spring Framework Web MVC 공식 문서</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="Spring Boot" /><category term="REST API" /><category term="Validation" /><category term="DTO" /><category term="RequestBody" /><summary type="html"><![CDATA[PathVariable, RequestParam, RequestBody로 요청값을 받고 DTO와 Bean Validation으로 검증하는 흐름을 정리한다.]]></summary></entry><entry><title type="html">Python Counter, 빈도수를 쉽게 세는 방법</title><link href="https://minnong511.github.io/algorithm/2026/08/14/counter/" rel="alternate" type="text/html" title="Python Counter, 빈도수를 쉽게 세는 방법" /><published>2026-08-14T09:00:00+09:00</published><updated>2026-08-14T09:00:00+09:00</updated><id>https://minnong511.github.io/algorithm/2026/08/14/counter</id><content type="html" xml:base="https://minnong511.github.io/algorithm/2026/08/14/counter/"><![CDATA[<h1 id="counter">Counter</h1>

<blockquote>
  <p>빈도수를 세는 문제에서 코드를 확 줄여주는 기능이다.</p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">Counter</code>는 <code class="language-plaintext highlighter-rouge">collections</code> 모듈에서 가져와 사용한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">collections</span> <span class="kn">import</span> <span class="n">Counter</span>
</code></pre></div></div>

<p>기본 형태는 다음과 같다.</p>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">Counter(이터러블)</code> → <code class="language-plaintext highlighter-rouge">{원소: 등장 횟수}</code> 형태로 세어준다.</p>
</blockquote>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">collections</span> <span class="kn">import</span> <span class="n">Counter</span>

<span class="n">nums</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">3</span><span class="p">]</span>
<span class="n">count</span> <span class="o">=</span> <span class="n">Counter</span><span class="p">(</span><span class="n">nums</span><span class="p">)</span>

<span class="k">print</span><span class="p">(</span><span class="n">count</span><span class="p">[</span><span class="mi">2</span><span class="p">])</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>2
</code></pre></div></div>

<p>문자열도 문자 하나씩 순회할 수 있는 이터러블이므로 카운팅할 수 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">count</span> <span class="o">=</span> <span class="n">Counter</span><span class="p">(</span><span class="s">"banana"</span><span class="p">)</span>

<span class="k">print</span><span class="p">(</span><span class="n">count</span><span class="p">)</span>
<span class="c1"># Counter({'a': 3, 'n': 2, 'b': 1})
</span></code></pre></div></div>

<hr />

<h1 id="알고리즘-문제에서-자주-쓰는-기능">알고리즘 문제에서 자주 쓰는 기능</h1>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">count</span> <span class="o">=</span> <span class="n">Counter</span><span class="p">(</span><span class="n">nums</span><span class="p">)</span>

<span class="n">count</span><span class="p">[</span><span class="n">x</span><span class="p">]</span>              <span class="c1"># x의 개수, 없으면 0
</span><span class="n">count</span><span class="p">.</span><span class="n">most_common</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># 가장 많이 나온 값 1개
</span><span class="n">count</span><span class="p">.</span><span class="n">keys</span><span class="p">()</span>          <span class="c1"># 등장한 값들
</span><span class="n">count</span><span class="p">.</span><span class="n">values</span><span class="p">()</span>        <span class="c1"># 각 값의 등장 횟수
</span><span class="n">count</span><span class="p">.</span><span class="n">items</span><span class="p">()</span>         <span class="c1"># (값, 등장 횟수)
</span></code></pre></div></div>

<p>예를 들어 각 귤 크기의 개수를 센 뒤, 등장 횟수만 가져오고 싶다면 다음처럼 사용할 수 있다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">tangerine</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">]</span>
<span class="n">counts</span> <span class="o">=</span> <span class="nb">list</span><span class="p">(</span><span class="n">Counter</span><span class="p">(</span><span class="n">tangerine</span><span class="p">).</span><span class="n">values</span><span class="p">())</span>

<span class="k">print</span><span class="p">(</span><span class="n">counts</span><span class="p">)</span>
<span class="c1"># [1, 2, 2, 2, 1]
</span></code></pre></div></div>

<h2 id="특히-이런-문제에서-유용하다">특히 이런 문제에서 유용하다</h2>

<ul>
  <li>문자열의 문자 개수 세기</li>
  <li>숫자별 등장 횟수 세기</li>
  <li>애너그램 판별</li>
  <li>최빈값 찾기</li>
  <li>두 배열의 원소 구성 비교</li>
  <li>중복 원소 찾기</li>
</ul>

<p>애너그램은 문자의 순서는 다르지만, 사용된 문자와 각 문자의 개수가 같은 단어를 말한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Counter</span><span class="p">(</span><span class="s">"listen"</span><span class="p">)</span> <span class="o">==</span> <span class="n">Counter</span><span class="p">(</span><span class="s">"silent"</span><span class="p">)</span>  <span class="c1"># True
</span></code></pre></div></div>

<hr />

<h1 id="개인적인-고민">개인적인 고민</h1>

<p>모든 기능을 내가 구현해야 하는 게 아닌가?</p>

<p>나는 약간 내가 직접 구현해야 할 것 같은 강박이 있다.</p>

<p>하지만 이런 강박을 가질 필요는 없다.</p>

<p>알고리즘 문제에서는 단순히 코드를 길게 짜는 능력만 보는 것이 아니다.</p>

<ul>
  <li>이 문제가 빈도수를 활용해서 해결할 수 있는 문제인지 알아보는가?</li>
  <li>어떤 자료구조를 사용해야 하는지 판단할 수 있는가?</li>
  <li>시간 복잡도를 고려할 수 있는가?</li>
</ul>

<p>빈도수를 딕셔너리에 저장하는 반복문은 이미 검증된 도구로 대체할 수 있는 구현 세부 사항이다.</p>

<p>굳이 검증된 도구가 있다면 매번 다시 만들어서 쓸 필요는 없다.</p>

<h2 id="직접-구현한다면">직접 구현한다면</h2>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">count</span> <span class="o">=</span> <span class="p">{}</span>

<span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">nums</span><span class="p">:</span>
    <span class="n">count</span><span class="p">[</span><span class="n">x</span><span class="p">]</span> <span class="o">=</span> <span class="n">count</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span>
</code></pre></div></div>

<h2 id="counter를-사용한다면">Counter를 사용한다면</h2>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">count</span> <span class="o">=</span> <span class="n">Counter</span><span class="p">(</span><span class="n">nums</span><span class="p">)</span>
</code></pre></div></div>

<p>둘 다 핵심 원리는 똑같고, 평균 시간 복잡도도 보통 <code class="language-plaintext highlighter-rouge">O(N)</code>이다.</p>

<p>따라서 <code class="language-plaintext highlighter-rouge">Counter</code>를 사용했다고 해서 알고리즘을 건너뛴 것은 아니다.</p>

<hr />

<h1 id="추천-학습-방식">추천 학습 방식</h1>

<ol>
  <li>처음에는 빈도수 계산을 직접 구현해보며 원리를 이해한다.</li>
  <li>이후 문제에서는 <code class="language-plaintext highlighter-rouge">Counter</code>를 사용한다.</li>
  <li>라이브러리 사용이 금지된 문제나 코딩 테스트 환경에서 지원하지 않는 경우에만 직접 구현한다.</li>
  <li><code class="language-plaintext highlighter-rouge">Counter</code> 없이도 구현 방법을 설명할 수 있으면 충분하다.</li>
</ol>

<p>다만 아래 알고리즘과 자료구조는 동작 원리를 이해하기 위해 직접 구현해보는 것이 좋다.</p>

<ul>
  <li>정렬, 이분 탐색</li>
  <li>스택, 큐</li>
  <li>BFS, DFS</li>
  <li>힙</li>
  <li>유니온 파인드</li>
  <li>다익스트라</li>
</ul>

<hr />

<h1 id="결론">결론</h1>

<blockquote>
  <p>문제가 빈도수 문제라는 것을 알아보는 것도 알고리즘 실력이다.</p>

  <p>원리는 직접 이해하고, 실전에서는 표준 라이브러리를 적극적으로 사용하자.</p>
</blockquote>]]></content><author><name>Min Hyeong Lee</name></author><category term="Algorithm" /><category term="Python" /><category term="Algorithm" /><category term="Counter" /><category term="Collections" /><summary type="html"><![CDATA[Counter]]></summary></entry><entry><title type="html">Spring 기초 Part 2: Spring MVC 요청 처리 흐름</title><link href="https://minnong511.github.io/java/spring/2026/08/14/spring-mvc-request-flow/" rel="alternate" type="text/html" title="Spring 기초 Part 2: Spring MVC 요청 처리 흐름" /><published>2026-08-14T09:00:00+09:00</published><updated>2026-08-14T09:00:00+09:00</updated><id>https://minnong511.github.io/java/spring/2026/08/14/spring-mvc-request-flow</id><content type="html" xml:base="https://minnong511.github.io/java/spring/2026/08/14/spring-mvc-request-flow/"><![CDATA[<h1 id="spring-기초-part-2-spring-mvc-요청-처리-흐름">Spring 기초 Part 2: Spring MVC 요청 처리 흐름</h1>

<blockquote>
  <p>Spring MVC는 HTTP 요청을 적절한 Controller에 연결하고, 처리 결과를 View 또는 JSON으로 반환하는 웹 프레임워크다.</p>
</blockquote>

<p>처음에는 다음 흐름부터 기억하면 된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  → DispatcherServlet
  → Controller
  → Service
  → Repository
  → Database
</code></pre></div></div>

<p>응답은 반대 방향으로 돌아온다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Database
  → Repository
  → Service
  → Controller
  → JSON 또는 HTML
  → Client
</code></pre></div></div>

<hr />

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
      <th>쉽게 말하면</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Client</td>
      <td>서버에 HTTP 요청을 보내는 프로그램</td>
      <td>브라우저, Vue, React, 앱, Postman</td>
    </tr>
    <tr>
      <td>HTTP Request</td>
      <td>Client가 서버에 보내는 요청</td>
      <td><code class="language-plaintext highlighter-rouge">GET /api/users/1</code></td>
    </tr>
    <tr>
      <td>HTTP Response</td>
      <td>서버가 Client에 돌려주는 결과</td>
      <td>상태 코드와 JSON 데이터</td>
    </tr>
    <tr>
      <td>MVC</td>
      <td>Model, View, Controller로 역할을 나누는 설계 방식</td>
      <td>데이터, 화면, 요청 처리를 분리</td>
    </tr>
    <tr>
      <td>Servlet</td>
      <td>Java 웹 서버에서 HTTP 요청과 응답을 처리하는 객체</td>
      <td>Spring MVC의 입구를 만드는 기반 기술</td>
    </tr>
    <tr>
      <td>DispatcherServlet</td>
      <td>Spring MVC의 모든 요청을 먼저 받는 Front Controller</td>
      <td>요청을 담당 Controller에 배분하는 중앙 안내자</td>
    </tr>
    <tr>
      <td>HandlerMapping</td>
      <td>요청을 처리할 Controller 메서드를 찾는 객체</td>
      <td><code class="language-plaintext highlighter-rouge">/users/1</code>을 누가 처리하는지 검색</td>
    </tr>
    <tr>
      <td>HandlerAdapter</td>
      <td>찾은 Controller 메서드를 실제로 호출하는 객체</td>
      <td>매개변수를 준비하고 메서드를 실행</td>
    </tr>
    <tr>
      <td>Jackson</td>
      <td>자바 객체와 JSON을 변환하는 라이브러리</td>
      <td><code class="language-plaintext highlighter-rouge">UserDto</code>를 JSON으로 변환</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="2-mvc와-계층형-구조는-같은-말이-아니다">2. MVC와 계층형 구조는 같은 말이 아니다</h2>

<p>기본 MVC는 역할을 다음처럼 구분한다.</p>

<table>
  <thead>
    <tr>
      <th>구성</th>
      <th>역할</th>
      <th>Spring에서의 예</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Model</td>
      <td>애플리케이션의 데이터와 상태</td>
      <td>DTO, Entity, Service의 처리 결과</td>
    </tr>
    <tr>
      <td>View</td>
      <td>사용자에게 보여줄 결과</td>
      <td>HTML, Thymeleaf 화면</td>
    </tr>
    <tr>
      <td>Controller</td>
      <td>요청을 받고 처리 흐름을 연결</td>
      <td><code class="language-plaintext highlighter-rouge">@Controller</code>, <code class="language-plaintext highlighter-rouge">@RestController</code></td>
    </tr>
  </tbody>
</table>

<p>실무의 Spring 애플리케이션에서는 유지보수를 위해 역할을 더 세분화한다.</p>

<table>
  <thead>
    <tr>
      <th>계층</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Controller</td>
      <td>HTTP 요청과 응답 처리</td>
    </tr>
    <tr>
      <td>Service</td>
      <td>비즈니스 규칙과 트랜잭션 처리</td>
    </tr>
    <tr>
      <td>Repository</td>
      <td>데이터베이스 접근</td>
    </tr>
  </tbody>
</table>

<p>즉 Spring MVC와 계층형 구조를 함께 사용하는 것이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring MVC
→ HTTP 요청을 Controller에 연결하고 응답을 만드는 구조

계층형 구조
→ Controller, Service, Repository의 책임을 나누는 구조
</code></pre></div></div>

<hr />

<h2 id="3-dispatcherservlet">3. DispatcherServlet</h2>

<p>Client의 요청이 Controller로 바로 전달되는 것은 아니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client
  → DispatcherServlet
  → Controller
</code></pre></div></div>

<p>DispatcherServlet은 모든 요청을 먼저 받고 적절한 Controller로 전달한다. 이런 구조를 <strong>Front Controller Pattern</strong>이라고 한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                       ┌→ UserController
Client → DispatcherServlet → ProductController
                       └→ OrderController
</code></pre></div></div>

<p>각 Controller가 공통 처리를 직접 반복하지 않아도 DispatcherServlet을 중심으로 일관된 요청 처리가 가능하다.</p>

<hr />

<h2 id="4-handlermapping과-handleradapter">4. HandlerMapping과 HandlerAdapter</h2>

<p>다음 Controller가 있다고 해보자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/api/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/{id}"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">getUser</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// 사용자 조회</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Client가 다음 요청을 보낸다.</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">GET /api/users/1
</span></code></pre></div></div>

<p>Spring은 애플리케이션을 시작할 때 어노테이션 정보를 읽고 URL과 메서드의 연결 정보를 준비한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET /api/users/{id}
        ↓
UserController.getUser()
</code></pre></div></div>

<p>실행 시 각 객체의 역할은 다음과 같다.</p>

<ol>
  <li>DispatcherServlet이 요청을 받는다.</li>
  <li>HandlerMapping이 요청을 처리할 Controller 메서드를 찾는다.</li>
  <li>HandlerAdapter가 필요한 매개변수를 준비한다.</li>
  <li>HandlerAdapter가 Controller 메서드를 호출한다.</li>
  <li>반환 결과를 DispatcherServlet이 응답으로 처리한다.</li>
</ol>

<section class="mermaid-card">
  <header class="mermaid-head">
    <span>Spring MVC REST API 요청 흐름</span>
  </header>
  <div class="mermaid js-mermaid">
flowchart LR
    C[Client] --&gt;|HTTP Request| D[DispatcherServlet]
    D --&gt;|처리 대상 검색| HM[HandlerMapping]
    HM --&gt;|Controller 정보| D
    D --&gt;|실행 요청| HA[HandlerAdapter]
    HA --&gt; CT[Controller]
    CT --&gt; S[Service]
    S --&gt; R[Repository]
    R --&gt; DB[(Database)]
    DB --&gt; R
    R --&gt; S
    S --&gt; CT
    CT --&gt;|Java Object| HA
    HA --&gt; D
    D --&gt;|JSON Response| C
</div>
</section>

<script>
  (function () {
    function render() {
      if (window.mermaid && window.mermaid.run) {
        window.mermaid.run({ querySelector: '.js-mermaid' });
      }
    }

    if (!window.__mermaidLibInit) {
      window.__mermaidLibInit = true;
      var script = document.createElement('script');
      script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
      script.async = true;
      script.onload = function () {
        if (window.mermaid && window.mermaid.initialize) {
          window.mermaid.initialize({ startOnLoad: false, theme: 'neutral' });
        }
        render();
      };
      document.head.appendChild(script);
    } else {
      render();
    }
  })();
</script>

<hr />

<h2 id="5-controller-service-repository">5. Controller, Service, Repository</h2>

<h3 id="controller">Controller</h3>

<p>Controller는 HTTP 요청과 응답에 집중한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/api/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserController</span><span class="o">(</span><span class="nc">UserService</span> <span class="n">userService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userService</span> <span class="o">=</span> <span class="n">userService</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/{id}"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">UserResponse</span><span class="o">&gt;</span> <span class="nf">getUser</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">UserResponse</span> <span class="n">response</span> <span class="o">=</span> <span class="n">userService</span><span class="o">.</span><span class="na">getUser</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>
        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">.</span><span class="na">ok</span><span class="o">(</span><span class="n">response</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Controller에는 복잡한 업무 규칙을 넣지 않는다.</p>

<h3 id="service">Service</h3>

<p>Service는 실제 업무 규칙을 처리한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserRepository</span> <span class="n">userRepository</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserService</span><span class="o">(</span><span class="nc">UserRepository</span> <span class="n">userRepository</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userRepository</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">getUser</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>
        <span class="k">return</span> <span class="nc">UserResponse</span><span class="o">.</span><span class="na">from</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="repository">Repository</h3>

<p>Repository는 데이터베이스 접근을 담당한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">UserRepository</span> <span class="kd">extends</span> <span class="nc">JpaRepository</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">,</span> <span class="nc">Long</span><span class="o">&gt;</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p>역할을 나누면 HTTP 처리, 업무 규칙, DB 접근을 각각 독립적으로 수정하고 테스트하기 쉬워진다.</p>

<hr />

<h2 id="6-controller와-restcontroller">6. <code class="language-plaintext highlighter-rouge">@Controller</code>와 <code class="language-plaintext highlighter-rouge">@RestController</code></h2>

<h3 id="controller-1"><code class="language-plaintext highlighter-rouge">@Controller</code></h3>

<p>일반적인 <code class="language-plaintext highlighter-rouge">@Controller</code>가 문자열을 반환하면 View 이름으로 해석할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Controller</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">HomeController</span> <span class="o">{</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">home</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"home"</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>return "home"
   → ViewResolver
   → home.html 검색
   → HTML 응답
</code></pre></div></div>

<h3 id="restcontroller"><code class="language-plaintext highlighter-rouge">@RestController</code></h3>

<p><code class="language-plaintext highlighter-rouge">@RestController</code>의 반환값은 기본적으로 HTTP 응답 본문으로 전달된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">HelloController</span> <span class="o">{</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/api/hello"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">MessageResponse</span> <span class="nf">hello</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nf">MessageResponse</span><span class="o">(</span><span class="s">"hello"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Jackson이 자바 객체를 JSON으로 변환한다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="s2">"hello"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th><code class="language-plaintext highlighter-rouge">@Controller</code></th>
      <th><code class="language-plaintext highlighter-rouge">@RestController</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>주요 목적</td>
      <td>HTML View 반환</td>
      <td>JSON 데이터 반환</td>
    </tr>
    <tr>
      <td>반환값 처리</td>
      <td>View 이름으로 해석 가능</td>
      <td>Response Body로 변환</td>
    </tr>
    <tr>
      <td>주 사용처</td>
      <td>서버 렌더링 웹 페이지</td>
      <td>REST API</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="7-전체-실행-흐름">7. 전체 실행 흐름</h2>

<p><code class="language-plaintext highlighter-rouge">GET /api/users/1</code> 요청을 다시 연결하면 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Client가 GET /api/users/1 요청
2. DispatcherServlet이 요청 수신
3. HandlerMapping이 UserController.getUser() 검색
4. HandlerAdapter가 id 값을 준비하고 메서드 호출
5. Controller가 UserService 호출
6. Service가 UserRepository 호출
7. Repository가 DB 조회
8. 조회 결과가 Repository → Service → Controller로 반환
9. Jackson이 UserResponse를 JSON으로 변환
10. Client에 HTTP 응답 반환
</code></pre></div></div>

<hr />

<h2 id="핵심-정리">핵심 정리</h2>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>핵심 역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DispatcherServlet</td>
      <td>모든 요청을 먼저 받는 Front Controller</td>
    </tr>
    <tr>
      <td>HandlerMapping</td>
      <td>요청을 처리할 Controller 메서드 검색</td>
    </tr>
    <tr>
      <td>HandlerAdapter</td>
      <td>Controller 메서드 실행</td>
    </tr>
    <tr>
      <td>Controller</td>
      <td>HTTP 요청과 응답 처리</td>
    </tr>
    <tr>
      <td>Service</td>
      <td>비즈니스 규칙 처리</td>
    </tr>
    <tr>
      <td>Repository</td>
      <td>데이터베이스 접근</td>
    </tr>
    <tr>
      <td>Jackson</td>
      <td>자바 객체를 JSON으로 변환</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Spring MVC의 핵심은 요청을 Controller에 연결하는 흐름과 Controller, Service, Repository의 책임을 분리하는 데 있다.</p>
</blockquote>

<h2 id="참고-자료">참고 자료</h2>

<ul>
  <li><a href="https://docs.spring.io/spring-framework/reference/web/webmvc.html">Spring Framework Web MVC 공식 문서</a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="Spring Boot" /><category term="Spring MVC" /><category term="DispatcherServlet" /><category term="REST API" /><summary type="html"><![CDATA[DispatcherServlet부터 Controller, Service, Repository, JSON 응답까지 Spring MVC의 전체 요청 흐름을 정리한다.]]></summary></entry><entry><title type="html">Spring 기초 Part 1: IoC, Bean, DI와 주요 Annotation</title><link href="https://minnong511.github.io/java/spring/2026/08/11/spring-ioc-bean-di/" rel="alternate" type="text/html" title="Spring 기초 Part 1: IoC, Bean, DI와 주요 Annotation" /><published>2026-08-11T19:20:00+09:00</published><updated>2026-08-11T19:20:00+09:00</updated><id>https://minnong511.github.io/java/spring/2026/08/11/spring-ioc-bean-di</id><content type="html" xml:base="https://minnong511.github.io/java/spring/2026/08/11/spring-ioc-bean-di/"><![CDATA[<h1 id="spring-기초-part-1-ioc-bean-di와-주요-annotation">Spring 기초 Part 1: IoC, Bean, DI와 주요 Annotation</h1>

<h2 id="1-spring이란-무엇인가">1. Spring이란 무엇인가</h2>

<p>Spring이 뭔지 일단 한 문장으로 말하면 다음과 같다.</p>

<blockquote>
  <p>Spring은 Java 객체를 대신 생성하고 관리하며, 서로 연결해 주고, 웹 요청 처리나 DB Transaction 같은 애플리케이션 기능까지 제공하는 Framework다.</p>
</blockquote>

<p>여기서 <strong>Framework</strong>란 애플리케이션의 전체 구조와 실행 흐름을 제공하고, 정해진 시점에 개발자가 작성한 코드를 호출하는 기반을 말한다. 개발자가 필요할 때 직접 호출하는 Library와 비교하면, Framework는 애플리케이션의 흐름을 주도한다는 특징이 있다.</p>

<p>Spring을 처음 배우면 Annotation부터 정말 많이 보게 된다. 그런데 일단 다음 세 개념의 관계부터 이해해야 한다.</p>

<ol>
  <li><strong>IoC Container</strong>: 객체를 생성하고 관리하는 공간</li>
  <li><strong>Bean</strong>: Spring Container가 관리하는 객체</li>
  <li><strong>DI</strong>: Bean이 필요로 하는 다른 Bean을 넣어 주는 방식</li>
</ol>

<p>관계를 한 줄로 나타내면 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Container가 Bean을 생성하고, Bean 사이의 의존 관계를 DI로 연결한다.
</code></pre></div></div>

<h3 id="spring을-왜-사용하는가">Spring을 왜 사용하는가</h3>

<p>그냥 Java에서 직접 <code class="language-plaintext highlighter-rouge">new</code>를 사용해도 애플리케이션을 만들 수 있다. 그런데 규모가 커지면 객체 생성과 연결, 설정, Transaction, 웹 요청 처리 같은 반복 코드가 빠르게 늘어난다. Spring은 이러한 문제를 다음과 같이 줄여 준다.</p>

<ul>
  <li><strong>객체 관리 자동화</strong>: 객체 생성, 의존 관계 설정, 생명주기 관리를 Container에 맡길 수 있다.</li>
  <li><strong>낮은 결합도</strong>: 클래스가 구체적인 구현 객체를 직접 생성하지 않으므로 구현 교체와 테스트가 쉬워진다.</li>
  <li><strong>관심사 분리</strong>: Controller, Service, Repository처럼 역할을 나누어 코드를 관리할 수 있다.</li>
  <li><strong>선언적 기능 사용</strong>: <code class="language-plaintext highlighter-rouge">@Transactional</code> 같은 애너테이션으로 반복적인 기반 코드를 직접 작성하지 않고 기능을 적용할 수 있다.</li>
  <li><strong>일관된 생태계</strong>: Spring MVC, Spring Data 등 여러 모듈을 같은 객체 관리 방식과 설정 모델로 사용할 수 있다.</li>
</ul>

<p>그렇다고 모든 객체를 무조건 Bean으로 만들어야 하는 것은 아니다. 값만 담는 단순 객체나 한 메서드 안에서 잠깐 사용하는 객체처럼 Container의 관리가 필요 없는 객체는 직접 <code class="language-plaintext highlighter-rouge">new</code>로 생성해도 된다.</p>

<h2 id="2-먼저-알아둘-용어">2. 먼저 알아둘 용어</h2>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>정의</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>객체(Object)</td>
      <td>클래스를 바탕으로 메모리에 생성된 실제 값</td>
    </tr>
    <tr>
      <td>컴포넌트(Component)</td>
      <td>애플리케이션을 구성하는 하나의 기능 단위. Spring에서는 보통 Container가 관리하는 클래스 또는 Bean 후보를 가리킨다.</td>
    </tr>
    <tr>
      <td>의존성(Dependency)</td>
      <td>어떤 객체가 자신의 기능을 수행하기 위해 필요로 하는 다른 객체</td>
    </tr>
    <tr>
      <td>메타데이터(Metadata)</td>
      <td>코드 자체의 실행 내용이 아니라 코드의 역할과 처리 방법을 설명하는 부가 정보</td>
    </tr>
    <tr>
      <td>스테레오타입(Stereotype)</td>
      <td><code class="language-plaintext highlighter-rouge">@Service</code>, <code class="language-plaintext highlighter-rouge">@Repository</code>처럼 클래스의 계층별 역할을 나타내는 애너테이션</td>
    </tr>
    <tr>
      <td>프록시(Proxy)</td>
      <td>원본 객체를 감싸서 메서드 호출 전후에 트랜잭션, 로깅 같은 부가 기능을 수행하는 대리 객체</td>
    </tr>
    <tr>
      <td>트랜잭션(Transaction)</td>
      <td>여러 데이터 작업을 하나의 논리적 작업 단위로 묶어 모두 성공시키거나 모두 취소하는 처리 단위</td>
    </tr>
  </tbody>
</table>

<h2 id="3-spring-container">3. Spring Container</h2>

<p><strong>Spring Container</strong>는 애플리케이션에 필요한 객체를 생성하고, 설정하고, 서로 연결하고, 생명주기를 관리하는 실행 환경이다. 일반적으로 <code class="language-plaintext highlighter-rouge">ApplicationContext</code>가 Spring IoC Container의 역할을 담당한다.</p>

<p>Container는 설정 정보를 읽고 다음 작업을 수행한다.</p>

<ol>
  <li>관리할 클래스를 찾거나 <code class="language-plaintext highlighter-rouge">@Bean</code> 설정을 읽는다.</li>
  <li>해당 클래스의 객체를 생성한다.</li>
  <li>객체가 필요로 하는 의존성을 찾는다.</li>
  <li>생성자 등을 통해 의존성을 주입한다.</li>
  <li>완성된 객체를 Bean으로 보관하고 필요한 곳에 제공한다.</li>
</ol>

<p>예를 들어 Spring Boot 애플리케이션에서는 보통 <code class="language-plaintext highlighter-rouge">@SpringBootApplication</code>이 붙은 시작 클래스의 패키지와 그 하위 패키지를 대상으로 Component Scan이 이루어진다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>애플리케이션 실행
    ↓
설정 정보와 컴포넌트 탐색
    ↓
Bean 생성
    ↓
Bean 사이의 의존성 주입
    ↓
요청을 처리할 준비 완료
</code></pre></div></div>

<h3 id="container를-왜-사용하는가">Container를 왜 사용하는가</h3>

<p>개발자가 객체 생성 순서와 공유 방법을 매번 직접 관리하면 객체가 많아질수록 코드가 복잡해진다. Container에 관리를 맡기면 애플리케이션 코드는 객체를 만드는 방법보다 각 객체가 맡은 비즈니스 역할에 집중할 수 있다.</p>

<h2 id="4-bean">4. Bean</h2>

<p><strong>Bean</strong>은 Spring Container가 생성하고 관리하는 객체다. 쉽게 말하면 Spring이 관리하고 있는 Java 객체다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Component Scan이 <code class="language-plaintext highlighter-rouge">UserService</code>를 발견해 객체를 생성하고 Container에 등록했다면 그 객체는 Spring Bean이다.</p>

<p>반면 다음 객체는 개발자가 직접 생성했으므로 그 자체로는 Bean이 아니다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">UserService</span> <span class="n">userService</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">UserService</span><span class="o">();</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발자가 직접 new로 생성
→ 일반 Java 객체

Spring Container가 생성하거나 등록하여 관리
→ Spring Bean
</code></pre></div></div>

<p>Bean은 기본적으로 하나의 Container 안에서 하나만 생성되는 <strong>singleton scope</strong>를 사용한다. 여기서 singleton은 JVM 전체에 무조건 객체가 하나라는 뜻이 아니라, 해당 Spring Container가 같은 Bean 정의에 대해 하나의 인스턴스를 관리한다는 뜻이다. 필요하면 prototype, request, session 같은 다른 scope도 사용할 수 있다.</p>

<h3 id="bean으로-관리하면-좋은-이유">Bean으로 관리하면 좋은 이유</h3>

<ul>
  <li>동일한 객체를 여러 곳에서 일관되게 공유할 수 있다.</li>
  <li>객체 생성과 종료 같은 생명주기를 Spring에 맡길 수 있다.</li>
  <li>DI, 트랜잭션, AOP 같은 Spring 기능을 적용할 수 있다.</li>
  <li>구현 교체와 테스트용 대체 객체 사용이 쉬워진다.</li>
</ul>

<h2 id="5-ioc-제어의-역전">5. IoC: 제어의 역전</h2>

<p><strong>IoC(Inversion of Control)</strong>는 객체의 생성과 연결에 대한 제어권이 애플리케이션 코드에서 외부 Container로 이동하는 설계 원칙이다.</p>

<h3 id="spring을-사용하지-않을-때">Spring을 사용하지 않을 때</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">UserService</span> <span class="n">userService</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">UserService</span><span class="o">();</span>
<span class="nc">UserController</span> <span class="n">userController</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">UserController</span><span class="o">(</span><span class="n">userService</span><span class="o">);</span>
</code></pre></div></div>

<p>개발자가 객체의 생성 순서와 연결 방법을 직접 결정한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발자 코드
    ↓ 생성
UserService
    ↓ 전달
UserController
</code></pre></div></div>

<h3 id="spring을-사용할-때">Spring을 사용할 때</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>
<span class="o">}</span>

<span class="nd">@RestController</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserController</span><span class="o">(</span><span class="nc">UserService</span> <span class="n">userService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userService</span> <span class="o">=</span> <span class="n">userService</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>개발자는 필요한 의존성을 생성자에 선언하고, 실제 객체 생성과 연결은 Spring Container가 담당한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Spring Container
    ├─ UserService Bean 생성
    ├─ UserController Bean 생성
    └─ UserService Bean을 UserController에 주입
</code></pre></div></div>

<p>여기서 중요한 점은 IoC와 DI가 완전히 같은 단어는 아니라는 것이다. <strong>IoC는 제어권이 뒤집힌 상태를 설명하는 원칙</strong>이고, <strong>DI는 그 원칙을 구현하는 대표적인 방법</strong>이다.</p>

<h3 id="ioc가-필요한-이유">IoC가 필요한 이유</h3>

<p>클래스가 사용할 구현체를 직접 생성하면 두 클래스가 강하게 결합된다. 생성 책임을 Container로 옮기면 각 클래스는 자신의 역할에 집중할 수 있고, 객체 구성 방법은 별도의 설정으로 관리할 수 있다.</p>

<h2 id="6-di-의존성-주입">6. DI: 의존성 주입</h2>

<p><strong>DI(Dependency Injection)</strong>는 객체가 필요로 하는 의존성을 객체 외부에서 전달하는 방식이다. 말 그대로 Dependency를 외부에서 Injection한다는 뜻이다.</p>

<p>여기서 의존성이라는 말이 조금 어렵게 느껴질 수 있다. <code class="language-plaintext highlighter-rouge">UserController</code>가 회원 관련 기능을 수행하기 위해 <code class="language-plaintext highlighter-rouge">UserService</code>를 사용한다면, <code class="language-plaintext highlighter-rouge">UserService</code>가 <code class="language-plaintext highlighter-rouge">UserController</code>의 의존성이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserController</span><span class="o">(</span><span class="nc">UserService</span> <span class="n">userService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userService</span> <span class="o">=</span> <span class="n">userService</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">UserController</code> 내부에는 <code class="language-plaintext highlighter-rouge">new UserService()</code>가 없다. Spring이 이미 관리 중인 <code class="language-plaintext highlighter-rouge">UserService</code> Bean을 생성자 매개변수로 전달한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[UserService Bean]
         │
         │ DI
         ▼
[UserController Bean]
</code></pre></div></div>

<h3 id="생성자-주입을-권장하는-이유">생성자 주입을 권장하는 이유</h3>

<p>Spring에는 생성자, setter, field를 이용한 여러 주입 방식이 있지만, 필수 의존성에는 생성자 주입을 주로 사용한다.</p>

<ul>
  <li>객체를 만들 때 필수 의존성이 반드시 전달된다.</li>
  <li>필드를 <code class="language-plaintext highlighter-rouge">final</code>로 선언해 생성 후 변경을 막을 수 있다.</li>
  <li>필요한 의존성이 생성자에 명확하게 드러난다.</li>
  <li>Spring 없이도 테스트에서 가짜 객체를 직접 전달하기 쉽다.</li>
</ul>

<p>생성자가 하나뿐이라면 보통 <code class="language-plaintext highlighter-rouge">@Autowired</code>를 생략할 수 있다.</p>

<h3 id="인터페이스와-함께-사용하면-더-유연해진다">인터페이스와 함께 사용하면 더 유연해진다</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">MessageSender</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">send</span><span class="o">(</span><span class="nc">String</span> <span class="n">message</span><span class="o">);</span>
<span class="o">}</span>

<span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">EmailSender</span> <span class="kd">implements</span> <span class="nc">MessageSender</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">send</span><span class="o">(</span><span class="nc">String</span> <span class="n">message</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// 이메일 발송</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">NotificationService</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">MessageSender</span> <span class="n">messageSender</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">NotificationService</span><span class="o">(</span><span class="nc">MessageSender</span> <span class="n">messageSender</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">messageSender</span> <span class="o">=</span> <span class="n">messageSender</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">NotificationService</code>는 <code class="language-plaintext highlighter-rouge">EmailSender</code>라는 구체 클래스가 아니라 <code class="language-plaintext highlighter-rouge">MessageSender</code>라는 역할에 의존한다. 따라서 구현 교체와 테스트가 쉬워진다. 단, 같은 인터페이스를 구현한 Bean이 여러 개라면 Spring이 어느 Bean을 주입할지 결정할 수 있도록 <code class="language-plaintext highlighter-rouge">@Primary</code>나 <code class="language-plaintext highlighter-rouge">@Qualifier</code> 같은 추가 정보가 필요하다.</p>

<h2 id="7-bean을-등록하는-두-가지-대표-방법">7. Bean을 등록하는 두 가지 대표 방법</h2>

<h3 id="71-component-scan과-스테레오타입-애너테이션">7.1 Component Scan과 스테레오타입 애너테이션</h3>

<p>Component Scan은 지정된 패키지를 탐색해 Bean 후보 클래스를 자동으로 찾고 등록하는 기능이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">EmailSender</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Component</code>는 해당 클래스를 Spring이 관리할 컴포넌트 후보로 표시한다. 다음 애너테이션들은 모두 <code class="language-plaintext highlighter-rouge">@Component</code>를 기반으로 하며 계층별 의미를 추가한 스테레오타입 애너테이션이다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@Component
    ├─ @Service
    ├─ @Repository
    └─ @Controller
         └─ @RestController의 기반
</code></pre></div></div>

<h4 id="component"><code class="language-plaintext highlighter-rouge">@Component</code></h4>

<p>특정 계층으로 분류하기 어려운 일반 컴포넌트에 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">PasswordEncoder</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="service"><code class="language-plaintext highlighter-rouge">@Service</code></h4>

<p>회원가입, 주문, 결제, 가격 계산 같은 <strong>비즈니스 로직</strong>을 담당하는 클래스에 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">register</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"회원가입"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Component</code>를 사용해도 Bean 등록은 가능하지만, <code class="language-plaintext highlighter-rouge">@Service</code>를 사용하면 클래스의 역할이 명확해져 계층 구조를 이해하기 쉽다.</p>

<h4 id="repository"><code class="language-plaintext highlighter-rouge">@Repository</code></h4>

<p>데이터베이스 조회, 저장, 수정, 삭제 같은 <strong>데이터 접근 책임</strong>을 맡는 클래스에 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Repository</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserRepository</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">save</span><span class="o">(</span><span class="nc">User</span> <span class="n">user</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// 데이터 저장</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>역할을 명확히 나타낼 뿐 아니라, 지원되는 데이터 접근 기술에서 발생한 예외를 Spring의 일관된 데이터 접근 예외 계층으로 변환하는 데에도 사용된다.</p>

<h4 id="controller"><code class="language-plaintext highlighter-rouge">@Controller</code></h4>

<p>Spring MVC에서 HTTP 요청을 받고, 처리 결과를 화면인 <strong>View</strong>로 연결하는 웹 계층 클래스에 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Controller</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">PageController</span> <span class="o">{</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/hello"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">hello</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"hello"</span><span class="o">;</span> <span class="c1">// View 이름</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="restcontroller"><code class="language-plaintext highlighter-rouge">@RestController</code></h4>

<p>REST API처럼 메서드의 반환값을 HTTP 응답 본문에 직접 작성하는 Controller에 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/users"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getUsers</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"users"</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@RestController</code>는 <code class="language-plaintext highlighter-rouge">@Controller</code>와 <code class="language-plaintext highlighter-rouge">@ResponseBody</code>의 의미를 합친 <strong>조합 애너테이션(composed annotation)</strong>이다. 객체를 반환하면 일반적으로 등록된 메시지 변환기가 객체를 JSON 같은 응답 형식으로 직렬화한다.</p>

<h3 id="72-configuration과-bean">7.2 <code class="language-plaintext highlighter-rouge">@Configuration</code>과 <code class="language-plaintext highlighter-rouge">@Bean</code></h3>

<p>외부 라이브러리의 클래스처럼 소스에 <code class="language-plaintext highlighter-rouge">@Component</code>를 붙일 수 없거나, 객체 생성 과정을 직접 설정해야 할 때 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Configuration</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">AppConfig</span> <span class="o">{</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">PasswordEncoder</span> <span class="nf">passwordEncoder</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nf">PasswordEncoder</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@Configuration</code>: Bean 설정을 정의하는 클래스임을 나타낸다.</li>
  <li><code class="language-plaintext highlighter-rouge">@Bean</code>: 메서드가 반환한 객체를 Spring Bean으로 등록한다.</li>
</ul>

<p>기본 Bean 이름은 메서드 이름인 <code class="language-plaintext highlighter-rouge">passwordEncoder</code>가 된다.</p>

<p>두 등록 방법의 선택 기준은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>상황</th>
      <th>적합한 방법</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>직접 작성한 Controller, Service, Repository</td>
      <td><code class="language-plaintext highlighter-rouge">@Component</code> 계열 + Component Scan</td>
    </tr>
    <tr>
      <td>외부 라이브러리 객체 등록</td>
      <td><code class="language-plaintext highlighter-rouge">@Configuration</code> + <code class="language-plaintext highlighter-rouge">@Bean</code></td>
    </tr>
    <tr>
      <td>생성 과정에 세부 설정이 필요한 객체</td>
      <td><code class="language-plaintext highlighter-rouge">@Configuration</code> + <code class="language-plaintext highlighter-rouge">@Bean</code></td>
    </tr>
  </tbody>
</table>

<h2 id="8-애너테이션은-무엇이며-왜-사용하는가">8. 애너테이션은 무엇이며 왜 사용하는가</h2>

<p>Java <strong>애너테이션(Annotation)</strong>은 클래스, 메서드, 필드, 매개변수 등에 부가적인 의미나 처리 정보를 붙이는 메타데이터다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">@Service</code>를 붙였다고 Annotation이 알아서 <code class="language-plaintext highlighter-rouge">new UserService()</code>를 실행하는 것은 아니다. Spring의 기반 기능이 Annotation 정보를 읽고, 그 의미에 따라 Bean을 등록하거나 기능을 적용한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>애너테이션으로 역할이나 설정 표시
    ↓
Spring이 메타데이터 확인
    ↓
Bean 등록, 요청 매핑, 트랜잭션 등의 기능 적용
</code></pre></div></div>

<h3 id="애너테이션을-사용하는-이유">애너테이션을 사용하는 이유</h3>

<ul>
  <li>설정 의도가 코드 가까이에 드러난다.</li>
  <li>반복적인 설정 코드를 줄일 수 있다.</li>
  <li>“무엇을 적용할지”만 선언하고 세부 실행은 프레임워크에 맡길 수 있다.</li>
  <li>클래스와 메서드의 역할을 빠르게 파악할 수 있다.</li>
</ul>

<p>Spring은 기능에 따라 클래스 메타데이터 읽기, Reflection, Bean 후처리기, AOP Proxy 등 여러 기술을 함께 사용한다. 따라서 “모든 Spring 애너테이션은 Reflection만으로 동작한다”고 이해하면 정확하지 않다. 예를 들어 <code class="language-plaintext highlighter-rouge">@Transactional</code>은 애너테이션이 트랜잭션 정보를 제공하고, 기본적으로 AOP Proxy가 메서드 호출을 가로채 실제 트랜잭션을 처리한다.</p>

<h2 id="9-spring-mvc와-rest-api">9. Spring MVC와 REST API</h2>

<p><strong>Spring MVC</strong>는 HTTP 요청을 Controller 메서드에 연결하고, 입력값 변환과 응답 생성을 지원하는 Spring의 웹 프레임워크다.</p>

<ul>
  <li><strong>HTTP</strong>: 클라이언트와 서버가 요청과 응답을 주고받는 통신 규칙</li>
  <li><strong>API</strong>: 프로그램이 다른 프로그램의 기능이나 데이터를 사용하기 위한 접점</li>
  <li><strong>REST API</strong>: 자원을 URL로 표현하고 HTTP 메서드로 조회, 생성, 수정, 삭제 의도를 나타내는 API 방식</li>
  <li><strong>Mapping</strong>: 특정 HTTP 요청을 실행할 Java 메서드와 연결하는 것</li>
</ul>

<p>일반적인 요청 처리 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>클라이언트
    ↓ HTTP 요청
DispatcherServlet
    ↓ 요청에 맞는 메서드 탐색
Controller
    ↓ 비즈니스 로직 호출
Service
    ↓ 데이터 접근
Repository
    ↓
Database
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">DispatcherServlet</code>은 Spring MVC의 앞단에서 요청을 받아 알맞은 Controller로 전달하는 <strong>Front Controller</strong>다.</p>

<h3 id="요청-mapping-애너테이션">요청 Mapping 애너테이션</h3>

<p><code class="language-plaintext highlighter-rouge">@RequestMapping</code>은 URL, HTTP 메서드, 요청 매개변수, 헤더, 미디어 타입 등을 기준으로 요청을 Controller 메서드에 연결한다. 다음 애너테이션들은 HTTP 메서드별 축약형이다.</p>

<table>
  <thead>
    <tr>
      <th>애너테이션</th>
      <th>HTTP 메서드</th>
      <th>주된 의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@GetMapping</code></td>
      <td>GET</td>
      <td>자원 조회</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PostMapping</code></td>
      <td>POST</td>
      <td>자원 생성 또는 처리 요청</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PutMapping</code></td>
      <td>PUT</td>
      <td>자원 전체 수정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PatchMapping</code></td>
      <td>PATCH</td>
      <td>자원 일부 수정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@DeleteMapping</code></td>
      <td>DELETE</td>
      <td>자원 삭제</td>
    </tr>
  </tbody>
</table>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>

    <span class="nd">@GetMapping</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getUsers</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"users"</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">createUser</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">CreateUserRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="o">}</span>

    <span class="nd">@DeleteMapping</span><span class="o">(</span><span class="s">"/{id}"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">deleteUser</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>클래스의 <code class="language-plaintext highlighter-rouge">@RequestMapping("/users")</code>는 공통 경로를 지정한다. 따라서 각 메서드는 다음 요청을 처리한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET    /users
POST   /users
DELETE /users/10
</code></pre></div></div>

<h2 id="10-http-요청값을-받는-방법">10. HTTP 요청값을 받는 방법</h2>

<h3 id="pathvariable"><code class="language-plaintext highlighter-rouge">@PathVariable</code></h3>

<p>URL 경로 자체에 포함된 값을 받는다. 특정 자원을 식별할 때 주로 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/users/{id}"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">getUser</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// GET /users/10 요청이면 id는 10</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="requestparam"><code class="language-plaintext highlighter-rouge">@RequestParam</code></h3>

<p>URL의 Query Parameter를 받는다. 검색, 필터, 정렬, 페이지 번호처럼 선택 조건을 전달할 때 주로 사용한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">UserResponse</span> <span class="nf">getUser</span><span class="o">(</span><span class="nd">@RequestParam</span> <span class="nc">String</span> <span class="n">name</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// GET /users?name=min 요청이면 name은 "min"</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="requestbody"><code class="language-plaintext highlighter-rouge">@RequestBody</code></h3>

<p>HTTP 요청 본문의 JSON 같은 데이터를 Java 객체로 변환해 받는다. 자원을 생성하거나 수정할 때 주로 사용한다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"민형"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"age"</span><span class="p">:</span><span class="w"> </span><span class="mi">25</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">CreateUserRequest</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">int</span> <span class="n">age</span><span class="o">;</span>

    <span class="c1">// getter, setter 또는 생성자</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@PostMapping</span><span class="o">(</span><span class="s">"/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">createUser</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">CreateUserRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Spring MVC는 등록된 메시지 변환기를 이용해 요청 본문을 <code class="language-plaintext highlighter-rouge">CreateUserRequest</code> 객체로 역직렬화한다.</p>

<p>그래서 세 방식의 차이를 정리하면 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>요청 예시</th>
      <th>용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PathVariable</code></td>
      <td><code class="language-plaintext highlighter-rouge">/users/10</code></td>
      <td>특정 자원의 식별자</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestParam</code></td>
      <td><code class="language-plaintext highlighter-rouge">/users?name=min</code></td>
      <td>검색, 필터 같은 부가 조건</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestBody</code></td>
      <td>JSON 요청 본문</td>
      <td>생성, 수정할 데이터</td>
    </tr>
  </tbody>
</table>

<h2 id="11-controller-service-repository를-나누는-이유">11. Controller, Service, Repository를 나누는 이유</h2>

<p>각 계층이 하나의 책임에 집중하도록 나누면 변경 이유가 다른 코드가 뒤섞이는 것을 막을 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Controller
→ HTTP 요청과 응답을 담당

Service
→ 비즈니스 규칙과 작업 순서를 담당

Repository
→ 데이터 저장소 접근을 담당
</code></pre></div></div>

<p>예를 들어 회원가입 흐름은 다음처럼 구성할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserController</span><span class="o">(</span><span class="nc">UserService</span> <span class="n">userService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userService</span> <span class="o">=</span> <span class="n">userService</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">createUser</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">CreateUserRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">userService</span><span class="o">.</span><span class="na">register</span><span class="o">(</span><span class="n">request</span><span class="o">.</span><span class="na">getName</span><span class="o">(),</span> <span class="n">request</span><span class="o">.</span><span class="na">getAge</span><span class="o">());</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserRepository</span> <span class="n">userRepository</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserService</span><span class="o">(</span><span class="nc">UserRepository</span> <span class="n">userRepository</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userRepository</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@Transactional</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">register</span><span class="o">(</span><span class="nc">String</span> <span class="n">name</span><span class="o">,</span> <span class="kt">int</span> <span class="n">age</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">(</span><span class="n">name</span><span class="o">,</span> <span class="n">age</span><span class="o">);</span>
        <span class="n">userRepository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Repository</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserRepository</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">save</span><span class="o">(</span><span class="nc">User</span> <span class="n">user</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// DB 저장 로직</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이 구조에서는 웹 통신 방식이 바뀌어도 Service의 비즈니스 규칙에 미치는 영향을 줄일 수 있고, 데이터 저장 기술이 바뀌어도 Controller까지 수정할 가능성이 작아진다.</p>

<h2 id="12-transactional">12. <code class="language-plaintext highlighter-rouge">@Transactional</code></h2>

<p><strong>트랜잭션</strong>은 여러 데이터 변경을 하나의 작업 단위로 묶는 기능이다. 모든 작업이 성공하면 <strong>commit</strong>하여 결과를 확정하고, 중간에 실패하면 <strong>rollback</strong>하여 이전 상태로 되돌린다.</p>

<p>왜 필요한지는 계좌 이체를 생각하면 바로 이해할 수 있다. 출금만 성공하고 입금이 실패한 상태가 남아서는 안 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">transfer</span><span class="o">()</span> <span class="o">{</span>
    <span class="n">accountA</span><span class="o">.</span><span class="na">withdraw</span><span class="o">(</span><span class="mi">10_000</span><span class="o">);</span>
    <span class="n">accountB</span><span class="o">.</span><span class="na">deposit</span><span class="o">(</span><span class="mi">10_000</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>트랜잭션 시작
    ↓
A 계좌에서 10,000원 출금
    ↓
B 계좌에 10,000원 입금
    ↓
모두 성공 → COMMIT
하나라도 실패 → ROLLBACK
</code></pre></div></div>

<h3 id="왜-transactional을-사용하는가">왜 <code class="language-plaintext highlighter-rouge">@Transactional</code>을 사용하는가</h3>

<p>개발자가 메서드마다 트랜잭션 시작, commit, rollback, 자원 정리 코드를 반복하지 않아도 된다. 비즈니스 코드는 실제 작업에 집중하고, 트랜잭션 경계는 선언적으로 표현할 수 있다.</p>

<h3 id="동작-원리">동작 원리</h3>

<p>기본적인 Proxy 방식에서는 Spring이 원본 Bean을 감싸는 Proxy를 만들고 메서드 호출 전후에 트랜잭션 처리를 추가한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>호출자
  ↓
Transaction Proxy
  ├─ 트랜잭션 시작
  ├─ 실제 메서드 호출
  └─ 성공 시 commit / 실패 시 rollback
</code></pre></div></div>

<h3 id="주의할-점">주의할 점</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@Transactional</code>이 실제로 적용되려면 대상 객체가 Spring Bean으로 관리되어야 한다.</li>
  <li>기본 Proxy 방식에서는 외부에서 Proxy를 거쳐 들어오는 호출이 대상이다. 같은 클래스의 메서드가 내부에서 다른 <code class="language-plaintext highlighter-rouge">@Transactional</code> 메서드를 직접 호출하는 <strong>self-invocation</strong>에는 트랜잭션이 적용되지 않을 수 있다.</li>
  <li>기본 설정에서는 <code class="language-plaintext highlighter-rouge">RuntimeException</code>과 <code class="language-plaintext highlighter-rouge">Error</code>가 rollback 대상이고, checked exception은 자동 rollback 대상이 아니다. 필요하면 <code class="language-plaintext highlighter-rouge">rollbackFor</code> 등을 명시한다.</li>
</ul>

<h2 id="13-전체-흐름-다시-보기">13. 전체 흐름 다시 보기</h2>

<p>애플리케이션 시작 과정은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Spring 애플리케이션 실행
2. Container 생성
3. 설정 정보와 Component Scan으로 Bean 후보 탐색
4. Controller, Service, Repository Bean 생성
5. 생성자 주입으로 Bean 사이의 의존 관계 연결
6. 웹 요청을 받을 준비 완료
</code></pre></div></div>

<p>요청 처리 과정은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 클라이언트가 POST /users와 JSON을 전송
2. Spring MVC가 UserController.createUser()에 요청을 Mapping
3. @RequestBody가 JSON을 CreateUserRequest로 변환
4. Controller가 UserService 호출
5. @Transactional Proxy가 트랜잭션 시작
6. Service가 비즈니스 로직 수행
7. Repository가 DB에 데이터 저장
8. 성공 시 commit 후 HTTP 응답 반환
</code></pre></div></div>

<h2 id="14-핵심-애너테이션-요약">14. 핵심 애너테이션 요약</h2>

<table>
  <thead>
    <tr>
      <th>애너테이션</th>
      <th>정의</th>
      <th>사용하는 이유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Component</code></td>
      <td>일반적인 Spring 관리 컴포넌트 후보</td>
      <td>Component Scan으로 Bean을 자동 등록하기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Service</code></td>
      <td>비즈니스 로직 계층의 컴포넌트</td>
      <td>역할을 명확히 하고 Service를 Bean으로 관리하기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Repository</code></td>
      <td>데이터 접근 계층의 컴포넌트</td>
      <td>데이터 접근 책임을 표시하고 예외 변환 같은 지원을 받기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Controller</code></td>
      <td>View 기반 MVC Controller</td>
      <td>HTTP 요청을 처리하고 View로 연결하기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RestController</code></td>
      <td>응답 본문을 직접 반환하는 Controller</td>
      <td>REST API 응답을 JSON 등으로 반환하기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestMapping</code></td>
      <td>공통적인 요청 Mapping</td>
      <td>경로, HTTP 메서드, 헤더 등 다양한 조건으로 요청을 연결하기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@GetMapping</code></td>
      <td>GET 요청 Mapping</td>
      <td>조회 요청을 메서드와 연결하기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PostMapping</code></td>
      <td>POST 요청 Mapping</td>
      <td>생성, 처리 요청을 메서드와 연결하기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestBody</code></td>
      <td>요청 본문을 객체로 변환</td>
      <td>JSON 등의 본문 데이터를 Java 객체로 받기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@PathVariable</code></td>
      <td>URL 경로 값을 매개변수에 연결</td>
      <td>특정 자원의 식별자를 받기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@RequestParam</code></td>
      <td>Query Parameter를 매개변수에 연결</td>
      <td>검색, 필터, 정렬 조건을 받기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Configuration</code></td>
      <td>Java 기반 Bean 설정 클래스</td>
      <td>객체 생성과 설정 방법을 코드로 정의하기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Bean</code></td>
      <td>메서드 반환 객체를 Bean으로 등록</td>
      <td>직접 생성하거나 외부 라이브러리 객체를 등록하기 위해</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Transactional</code></td>
      <td>트랜잭션 경계를 나타내는 메타데이터</td>
      <td>여러 데이터 작업의 원자성을 선언적으로 보장하기 위해</td>
    </tr>
  </tbody>
</table>

<h2 id="15-최종-정리">15. 최종 정리</h2>

<blockquote>
  <p>Spring은 Java 객체의 생성, 설정, 생명주기와 의존 관계를 IoC Container가 관리하도록 하고, 웹 요청 처리와 데이터 접근, 트랜잭션 같은 애플리케이션 공통 기능을 제공하는 프레임워크다.</p>
</blockquote>

<blockquote>
  <p>Bean은 Spring Container가 관리하는 객체이고, IoC는 객체 관리의 제어권을 Container로 옮기는 원칙이며, DI는 객체가 필요로 하는 의존성을 외부에서 주입하는 구현 방식이다.</p>
</blockquote>

<blockquote>
  <p>Spring 애너테이션은 클래스와 메서드의 역할 또는 처리 방법을 표시하는 메타데이터다. Spring은 이 정보를 해석해 Bean 등록, 의존성 주입, HTTP 요청 Mapping, 트랜잭션 같은 실제 기능을 적용한다.</p>
</blockquote>

<p>결국 핵심 연결 순서만 다시 보면 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Annotation과 Configuration
        ↓
Bean 등록
        ↓
IoC Container가 관리
        ↓
DI로 객체 연결
        ↓
Controller → Service → Repository
        ↓
웹 요청과 비즈니스 로직, 데이터 처리 수행
</code></pre></div></div>

<h2 id="참고-자료">참고 자료</h2>

<ul>
  <li><a href="https://docs.spring.io/spring-framework/reference/core/beans.html">Spring Framework: IoC Container</a></li>
  <li><a href="https://docs.spring.io/spring-framework/reference/core/beans/basics.html">Spring Framework: Container Overview</a></li>
  <li><a href="https://docs.spring.io/spring-framework/reference/core/beans/classpath-scanning.html">Spring Framework: Classpath Scanning and Managed Components</a></li>
  <li><a href="https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-controller.html">Spring Framework: Annotated Controllers</a></li>
  <li><a href="https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-controller/ann-requestmapping.html">Spring Framework: Mapping Requests</a></li>
  <li><a href="https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/annotations.html">Spring Framework: Using <code class="language-plaintext highlighter-rouge">@Transactional</code></a></li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="spring" /><category term="Java" /><category term="Spring" /><category term="Spring Framework" /><category term="IoC" /><category term="DI" /><category term="Bean" /><category term="Annotation" /><category term="Spring MVC" /><category term="Transaction" /><summary type="html"><![CDATA[Spring을 사용하는 이유와 IoC Container, Bean, DI, Component Scan, Spring MVC, Transaction의 관계를 정리한다.]]></summary></entry><entry><title type="html">Java 심화 Part 3: 함수형 프로그래밍과 Lambda</title><link href="https://minnong511.github.io/java/advanced/2026/08/11/java-functional-programming-lambda/" rel="alternate" type="text/html" title="Java 심화 Part 3: 함수형 프로그래밍과 Lambda" /><published>2026-08-11T11:40:00+09:00</published><updated>2026-08-11T11:40:00+09:00</updated><id>https://minnong511.github.io/java/advanced/2026/08/11/java-functional-programming-lambda</id><content type="html" xml:base="https://minnong511.github.io/java/advanced/2026/08/11/java-functional-programming-lambda/"><![CDATA[<h1 id="java-심화-part-3-함수형-프로그래밍과-lambda">Java 심화 Part 3: 함수형 프로그래밍과 Lambda</h1>

<ul>
  <li>Java의 함수형 프로그래밍(Functional Programming)은 함수를 일급 객체(First-Class Citizen)로 취급, 불변성(immunability)과 순수 함수(Pure function)를 기반으로 프로그램을 작성하는 방식</li>
</ul>

<h3 id="first-class-citizen">First-Class Citizen</h3>
<ul>
  <li>함수를 변수에 저장, 전달, 반환할 수 있음</li>
  <li>함수 자체를 데이터처럼 다루는 방식</li>
</ul>

<h3 id="pure-function">Pure Function</h3>
<ul>
  <li>같은 입력 -&gt; 같은 출력</li>
  <li>외부 상태를 변경하지 않음 (Side Effect 없음)</li>
</ul>

<h3 id="immmunability">Immmunability</h3>
<ul>
  <li>함수 내부에서 외부 변수를 변경하지 않는 구조</li>
  <li>병렬 처리에서 안정성과 예측 가능성이 높아진다.</li>
</ul>

<h3 id="declarative-style">Declarative Style</h3>
<ul>
  <li>어떻게가 아니라 무엇을 할지에 집중</li>
  <li>시스템이 실행 흐름을 알아서 처리</li>
  <li>Stream, Optional 등이 대표적인 예</li>
</ul>

<h2 id="일급-함수">일급 함수</h2>
<blockquote>
  <p>함수를 값처럼 취급할 수 있는 언어 특성</p>
</blockquote>

<ol>
  <li>함수를 변수에 할당</li>
</ol>

<p>def say_hello():
print(“Hello”)</p>
<h1 id="함수를-변수에-할당">함수를 변수에 할당</h1>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>say_hello_var = say_hello # 변수를 통해 함수 실행 say_hello_var()
</code></pre></div></div>

<ol>
  <li>함수를 인자로 전달</li>
</ol>

<p>def execute(fn):
    fn()</p>

<p>execute(lambda : print(“Executed”))</p>

<ol>
  <li>함수를 반환(return)</li>
</ol>

<p>def make_adder(x); 
    def adder(y):
        return x + y 
    return adder</p>

<p>add5 = make_adder(5)
print(add(3))</p>

<ol>
  <li>객체의 속성properties 로 사용</li>
</ol>

<h4 id="딕셔너리의-key-value-쌍으로-함수를-정의">딕셔너리의 key-value 쌍으로 함수를 정의</h4>
<p>calculator = {
“add”: lambda a, b: a + b
}</p>

<h4 id="딕셔너리의-key로-함수에-접근하여-실행">딕셔너리의 Key로 함수에 접근하여 실행</h4>
<p>print(calculator<a href="2, 3">“add”</a>) # 출력: 5</p>

<h2 id="순수-함수-pure-function-lambda-expression">순수 함수 Pure Function Lambda Expression</h2>
<p>동일한 입력에는 항상 동일한 출력을 반환, 함수 외부 변수의 샅애를 변경하지 않는 함수</p>

<ul>
  <li>
    <p>비순수 함수</p>
  </li>
  <li>
    <p>외부 상태 (Global 변수)
count = 0
def increase_and_get():
global count
count += 1 # 외부 상태를 변경 (Side Effect)
return count</p>
  </li>
</ul>

<p>검증 및 특징 확인
print(increase_and_get()) # 출력: 1
print(increase_and_get()) # 출력: 2 (다른 출력</p>

<p>순수 함수</p>

<p>ef square(x):
return x * x # 같은 입력, 같은 출력
print(square(4)) # 출력: 16
print(square(4)) # 출력: 16</p>

<p>순수 함수 특징</p>

<p>예측 가능성 같은 입력 같은 출력, 디버깅이 쉬움
테스트 용이 외부 의존 없이 독립적으로 테스트 가능
병렬 처리 안전 외부 상태를 건드리지 않으니 race condition 없음
캐싱 가능 결과를 재활용 Memoization 가능
리팩토링 안정성 의존성 적어서 코드 변경 영향 최소화</p>

<p>익명 클래스 Anonymous Class</p>

<ul>
  <li>클래스 기반 언어인 Java 환경에서 함수형 프로그램을 지원하기 위한 방식</li>
</ul>

<h2 id="익명-클래스anonymus-class란">익명 클래스(Anonymus Class)란?</h2>
<ul>
  <li>이름이 없는 클래스</li>
  <li>한번만 사용할 목적으로 정의</li>
  <li>클래스를 선언하면서 동시에 인스터스 생성</li>
</ul>

<p>문제점</p>
<ul>
  <li>너무 장황함</li>
  <li>가독성 감소</li>
  <li>함수 전달을 위한 최소 표현이 아님</li>
</ul>

<h3 id="람다-표현식-lamda-expression">람다 표현식 Lamda Expression</h3>
<ul>
  <li>익명 클래스를 간결하게 표현하기 위한 문법, 함수형 인터페이스를 인스턴스로 생성하는 가장 짧은 방식</li>
</ul>

<p>배경</p>
<ul>
  <li>Java 82014 이전: Java는 순수 객체지향 언어로, 메서드도 반드시 클래스의 일부여야 함.</li>
  <li>Java 8: 람다, Stream API, java.util.function 패키지 도입
코드를 값처럼 전달 가능
선언적, 함수형 스타일 코딩 가능</li>
  <li>간결성, 병렬 처리 용이성, 컬렉션 처리의 함수형 스타일 지원</li>
</ul>

<p>목적 설명 예시
코드 간결화 익명 클래스 작성의 반복 코드 제거 list.forEachitem System.out.printlnitem;
함수형 프로그래밍 지원 함수를 값처럼 전달 stream.filterx x 10
병렬/선언형 처리 Stream API와 결합하여 병렬 연산 지원 list.parallelStream.map….reduce…</p>

<p>public class Main{
    public static void main(String[] args){
        Runnable lambdaRunnable = () -&gt; System.out.println(“별도 스레드에서 실행”);</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    Thread thread2 = new Thread(lambdaRunnable); 
    thread2.start(); 
} }
</code></pre></div></div>

<p>익명 함수를 간결하게 표현하기 위한 문법, 함수형 인터페이스를 인스턴스로 생성하는 가장 짧은 방식</p>

<p>Lambda</p>

<p>Runnable r = () -&gt; System.out.println(“Hello”);</p>

<p>항목 9
익명 클래스 람다식
목적 객체 클래스 생성 함수 전달 행위 전달
코드 길이 길고 반복적 매우 간결
의미 객체지향적 해결 함수형 프로그래밍 해결
실행 구조 내부적으로 클래스 생성 함수형 인터페이스 구현체 생성
사용성 가독성 낮음 가독성, 유지보수 높음</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="advanced" /><category term="Java" /><category term="Functional Programming" /><category term="Lambda" /><category term="Anonymous Class" /><category term="Stream" /><category term="Pure Function" /><summary type="html"><![CDATA[Java의 함수형 프로그래밍, 순수 함수, 익명 클래스, 람다식과 함수형 인터페이스를 정리한다.]]></summary></entry><entry><title type="html">Java 객체지향 Part 5: static 메서드와 중첩 클래스</title><link href="https://minnong511.github.io/java/oop/2026/08/11/java-static-nested-class/" rel="alternate" type="text/html" title="Java 객체지향 Part 5: static 메서드와 중첩 클래스" /><published>2026-08-11T11:20:00+09:00</published><updated>2026-08-11T11:20:00+09:00</updated><id>https://minnong511.github.io/java/oop/2026/08/11/java-static-nested-class</id><content type="html" xml:base="https://minnong511.github.io/java/oop/2026/08/11/java-static-nested-class/"><![CDATA[<h1 id="java-객체지향-part-5-static-메서드와-중첩-클래스">Java 객체지향 Part 5: static 메서드와 중첩 클래스</h1>

<p>중첩 클래스에서 Static을 붙이는 이유는 바깥 객체와 연결되지 않은 클래스를 내부에 묶어두기 위해서이다.</p>

<h2 id="일반-내부-클래스">일반 내부 클래스</h2>

<p>tatic이 없는 내부 클래스는 바깥 객체에 소속</p>

<p>class User {
    String name = “민형”;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>class Profile {
    void printName() {
        System.out.println(name); 
    }
} }
</code></pre></div></div>

<p>따라서 Profile 객체를 만들려면 먼저 User 객체가 필요하다.</p>

<p>User user = new User(); 
User.profile profile = user.new Profile();</p>

<p>구조를 개념적으로 보면 다음과 같다.</p>

<p>User 객체
    └── Profile 객체
            └── 바깥 User 객체를 참조</p>

<p>profile이 특정 User의 name을 사용하므로 바깥 객체와 연결되는 것이 자연스럽다</p>

<h2 id="static-중첩-클래스">Static 중첩 클래스</h2>

<p>static 중첩 클래스는 바깥 객체와 연결되지 않는다.</p>

<p>class User {
    static class Profile{
        void printMessage(){
            System.out.println(“프로필입니다.”)
        }
    }
}</p>

<p>User.Profile profile = new User.Profile();</p>

<p>profile.printMessage();</p>

<p>여기서 User.Profile 이라고 쓰지만, 특정 User 객체에 소속된 것은 아니다. 
단지 User와 관련된 클래스라서 User 내부에 정리한 것</p>

<h1 id="왜-static을-사용하는가">왜 Static을 사용하는가?</h1>

<h2 id="1-바깥-객체가-필요없기-때문에">1. 바깥 객체가 필요없기 때문에</h2>

<p>다음 Address는 특정 User 객체의 필드에 직접 접근할 필요가 없다.</p>

<p>class User{
    static class Address{
        String city;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    Address(String city){
        this.city = city; 
    }
} }
</code></pre></div></div>

<p>Address에 접근하고 싶다면</p>

<p>User.Address address = new User.Address(“판교”)</p>

<p>바깥 객체가 필요 없는데 일반 내부 클래스로 만들면 불필요하게 User 객체와 연결된다.</p>

<h2 id="2-관련-클래스를-한-곳에-묶을-수-있기-때문에">2. 관련 클래스를 한 곳에 묶을 수 있기 때문에</h2>

<p>class HttpResponse{
    static class Header{</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>}
static class Body{

} }
</code></pre></div></div>

<p>Header 와 Body가 HttpsResponse와 관련있다는 것을 구조적으로 표현할 수 있다.</p>

<p>HttpResponse.Header header  = new HttpResponse.Header(); 
HttpResponse.Body body = new HttpResponse.Body();</p>

<h1 id="3-불필요한-바깥-객체-참조를-막기-때문에">3. 불필요한 바깥 객체 참조를 막기 때문에</h1>

<p>일반 내부 클래스 객체는 내부적으로 바깥 객체를 참조</p>

<p>내부 클래스 객체 ──참조──→ 바깥 클래스 객체</p>

<p>하지만 static 중첩 클래스에은 그 참조가 없다.</p>

<p>static 중첩 클래스 객체     바깥 클래스 객체</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>      서로 독립적
</code></pre></div></div>

<p>바깥 객체가 필요하지 않다면 static 중첩 클래스를 사용해야 구조가 단순하고 
불필요한 객체 참조도 생기지 않는다.</p>

<h3 id="개념-확인-문제">개념 확인 문제</h3>

<p>Receipt 클래스는 특정 Order 객체의 orderNumber를 직접 사용해야 한다.</p>

<p>Receipt는 특정 Order 객체의 orderNumber를 사용해야 하므로 일반 내부 클래스로 선언해야 한다. 일반 내부 클래스는 자신을 생성한 바깥 Order 객체를 참조할 수 있기 때문에</p>

<h1 id="static-메서드">Static 메서드</h1>

<p>판단 기준</p>

<blockquote>
  <p>클래스의 필드를 사용하느냐? 
더 자세하게는, 이 기능이 특정 객체의 행동인가, 아니면 객체와 상관없는 공통 기능인가?</p>
</blockquote>

<p>특정 객체의 데이터가 필요하지 않을 때 사용한다.</p>

<p>판단할 떄 이렇게 생각하면 된다.</p>

<blockquote>
  <p>이 기능을 실행하려면 특정 객체가 꼭 필요한가?</p>
</blockquote>

<p>필요하면 일반 인스턴스 메서드</p>

<p>class User{
    string name;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>void printName() {}
    System.out.println(name);  }
</code></pre></div></div>

<p>printName()은 특정 User 객체의 name이 필요하다.</p>

<p>User user = new User(); 
user.name = “민형”; 
user.printName();</p>

<hr />

<p>반대로 특정 객체의 데이터가 필요하지 않으면 static 메서드를 사용할 수 있다.</p>

<p>class Calculator {
    static int add(int a, int b,){
        return a + b; 
    }
}</p>

<p>add()는 어떤 calculator 객체의 상태도 사용하지 않으므로…</p>

<p>int result = Calculator.add(10,20);</p>

<p>정리하면</p>

<p>특정 객체의 필드가 필요한가?
    ├── 필요함 → 일반 인스턴스 메서드
    └── 필요 없음 → static 메서드 고려</p>

<p>예를 들어 Order에서 특정 주문 번호를 출력하다면 일반 메서드가 자연스러움</p>

<p>class Order{
    String orderNumber;</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>void printOrderNumber() {
    System.out.println(orderNumber); 
} }
</code></pre></div></div>

<p>하지만 주문 번호의 형식만 검사한다면 특정 Order 객체가 없어도 된다.</p>

<p>class Order {
    static boolean isValidOrderNumber(String orderNumber) {
        return orderNumber.startsWith(“ORDER-“);
    }
}</p>

<p>boolean valid = Order.isValidOrderNumber(“ORDER-100”);</p>

<h3 id="근데-그냥-필드-선언-안하고-쓰면-되는-거-아닌가">근데 그냥 필드 선언 안하고 쓰면 되는 거 아닌가?</h3>

<p>class Calculator {
    int add(int a, int b) {
        return a + b;
    }
}</p>

<p>class Calculator {
    static int add(int a, int b) {
        return a + b;
    }
}</p>

<p>두 메서드는 모두 전달받은 값만 계산 -&gt; 차이는 호출 방식</p>

<p>// 일반 메서드 
Calculator calculator = new Calculator(); 
calculator.add(10,20):</p>

<p>// static 메서드 
Calculator.add(10, 20);</p>

<p>일반 메서드는 필드를 사용하지 않더라도 객체를 먼저 만들어야 합니다. 반면 static 메서드는 객체 없이 실행할 수 있다.
-&gt; 이거는 객체를 만들지 않아서 상당히 편리하다!</p>

<h3 id="그러면-static-method는-언제-안써야-하는가">그러면 Static method는 언제 안써야 하는가?</h3>

<p>나중에 구현을 객체마다 다르게 바꿔야 한다면 일반 베서드로 구현해야 한다.</p>

<p>class Calculator {
    int calculate(int a, int b) {
        return a + b;
    }
}</p>

<p>class MultiplyCalculator extends Calculator {
    @Override
    int calculate(int a, int b) {
        return a * b;
    }
}</p>

<p>여기서는 객체에 따라 calculate() 동작이 달라진다.</p>

<p>Calculator calculator1 = new Calculator();
Calculator calculator2 = new MultiplyCalculator();</p>

<p>calculator1.calculate(10, 20); // 30
calculator2.calculate(10, 20); // 200</p>

<p>반대로 동작이 항상 같고, 특정 객체의 상태도 필요하지 않다면 static이 자연스럽다</p>

<p>class CalculatorUtils {
    static int add(int a, int b) {
        return a + b;
    }
}</p>

<p>정리하면</p>

<p>필드가 필요하다 -&gt; 일반 메소드 
필드가 필요 없지만 객체마다 동작이 달라질 수 있다 -&gt; 일반 메서드 
필드가 필요 업고 동작도 항상 같다 -&gt; static 메서드가 자연스럽다.</p>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="oop" /><category term="Java" /><category term="Static" /><category term="Nested Class" /><category term="Inner Class" /><category term="Method" /><category term="Polymorphism" /><summary type="html"><![CDATA[Java static 메서드, 일반 내부 클래스, static 중첩 클래스의 차이와 사용 기준을 정리한다.]]></summary></entry><entry><title type="html">Java 심화 Part 2: Annotation</title><link href="https://minnong511.github.io/java/advanced/2026/08/11/java-annotations/" rel="alternate" type="text/html" title="Java 심화 Part 2: Annotation" /><published>2026-08-11T11:00:00+09:00</published><updated>2026-08-11T11:00:00+09:00</updated><id>https://minnong511.github.io/java/advanced/2026/08/11/java-annotations</id><content type="html" xml:base="https://minnong511.github.io/java/advanced/2026/08/11/java-annotations/"><![CDATA[<h1 id="java-심화-part-2-annotation">Java 심화 Part 2: Annotation</h1>

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>단어</th>
      <th>정의</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Annotation</td>
      <td>클래스, 메서드, 필드 등에 추가 정보를 붙이는 Java 문법</td>
    </tr>
    <tr>
      <td>Metadata</td>
      <td>코드의 실행 로직이 아니라 코드의 역할과 설정을 설명하는 정보</td>
    </tr>
    <tr>
      <td>컴파일러</td>
      <td>Java 코드를 검사하고 바이트코드로 변환하는 도구</td>
    </tr>
    <tr>
      <td>Reflection</td>
      <td>실행 중에 클래스, 메서드, 필드, 애너테이션 정보를 조회하는 기능</td>
    </tr>
    <tr>
      <td>Bean</td>
      <td>Spring 컨테이너가 생성하고 관리하는 객체</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Annotation은 클래스, 메서드, 필드 등에 추가적인 메타데이터를 붙이는 기능이다. 애너테이션 자체가 로직을 실행하는 것은 아니며, 컴파일러나 프레임워크가 이 정보를 읽고 처리한다.</p>
</blockquote>

<h2 id="2-override-컴파일러에게-재정의를-알리기">2. <code class="language-plaintext highlighter-rouge">@Override</code>: 컴파일러에게 재정의를 알리기</h2>

<p>Java에서 <code class="language-plaintext highlighter-rouge">@</code>로 시작하는 것이 Annotation이다. 가장 익숙한 예시는 <code class="language-plaintext highlighter-rouge">@Override</code>다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">interface</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">Dog</span> <span class="kd">implements</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"멍멍!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Override</code>는 <code class="language-plaintext highlighter-rouge">makeSound()</code>가 부모 클래스나 인터페이스의 메서드를 재정의했다는 사실을 컴파일러에 알려준다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">Dog</span> <span class="kd">implements</span> <span class="nc">Animal</span> <span class="o">{</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">makeSond</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"멍멍!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>위 코드의 <code class="language-plaintext highlighter-rouge">makeSond()</code>는 <code class="language-plaintext highlighter-rouge">Animal</code>의 <code class="language-plaintext highlighter-rouge">makeSound()</code>와 이름이 다르다. <code class="language-plaintext highlighter-rouge">@Override</code>가 있기 때문에 컴파일러가 재정의가 아니라는 오류를 바로 알려준다.</p>

<h2 id="3-애너테이션은-혼자-실행되지-않는다">3. 애너테이션은 혼자 실행되지 않는다</h2>

<p>다음 코드에서 <code class="language-plaintext highlighter-rouge">@Override</code>가 직접 <code class="language-plaintext highlighter-rouge">makeSound()</code>를 실행하는 것은 아니다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Override</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">makeSound</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"멍멍!"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>역할은 다음처럼 나뉜다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@Override
    ↓
재정의라는 정보를 표시한다
    ↓
컴파일러가 이 정보를 검사한다
    ↓
부모 또는 인터페이스 메서드가 없으면 오류를 보여 준다
</code></pre></div></div>

<p>즉, 애너테이션은 표시이고 그 표시를 해석하는 컴파일러나 프레임워크가 실제 처리를 담당한다.</p>

<h2 id="4-자주-사용하는-java-애너테이션">4. 자주 사용하는 Java 애너테이션</h2>

<table>
  <thead>
    <tr>
      <th>애너테이션</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Override</code></td>
      <td>부모 클래스 또는 인터페이스 메서드를 재정의했음을 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@Deprecated</code></td>
      <td>더 이상 사용을 권장하지 않는 코드임을 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@SuppressWarnings</code></td>
      <td>특정 컴파일 경고를 억제</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@FunctionalInterface</code></td>
      <td>함수형 인터페이스 규칙을 지키는지 검사</td>
    </tr>
  </tbody>
</table>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Deprecated</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">oldLogin</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"이 메서드는 더 이상 권장하지 않습니다."</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="5-spring에서의-annotation">5. Spring에서의 Annotation</h2>

<p>Spring에서는 애너테이션이 클래스의 역할과 처리 방식을 알려 주는 중요한 설정 정보가 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Service</code>는 Spring에 이 클래스가 서비스 역할을 하며 관리할 객체 후보라는 정보를 전달한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserController</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">UserController</span><span class="o">(</span><span class="nc">UserService</span> <span class="n">userService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">userService</span> <span class="o">=</span> <span class="n">userService</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/users"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="nf">getUsers</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>위 코드에서:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@RestController</code>: HTTP 요청을 처리하는 Controller 클래스임을 표시한다.</li>
  <li><code class="language-plaintext highlighter-rouge">@GetMapping("/users")</code>: <code class="language-plaintext highlighter-rouge">/users</code>로 들어오는 GET 요청을 이 메서드와 연결한다.</li>
  <li>생성자: Spring이 필요한 <code class="language-plaintext highlighter-rouge">UserService</code>를 주입할 수 있는 위치다. 생성자가 하나라면 보통 <code class="language-plaintext highlighter-rouge">@Autowired</code>를 생략한다.</li>
</ul>

<h2 id="6-reflection과-spring의-연결">6. Reflection과 Spring의 연결</h2>

<p>Spring은 실행 중 Reflection을 이용해 클래스와 애너테이션을 검사한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>클래스 탐색
    ↓
@Service, @Controller 같은 애너테이션 발견
    ↓
Spring이 관리할 클래스인지 판단
    ↓
객체 생성
    ↓
Bean으로 등록
    ↓
필요한 객체에 의존성 주입
</code></pre></div></div>

<p>정리하면 역할은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Annotation → 코드에 의미와 설정을 표시
Reflection → 실행 중 그 표시를 읽음
Spring     → 읽은 정보를 기준으로 객체를 생성하고 관리
</code></pre></div></div>

<h2 id="7-애너테이션에-값-넣기">7. 애너테이션에 값 넣기</h2>

<p>애너테이션은 설정값도 받을 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/users"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="nf">getUsers</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">"/users"</code>는 <code class="language-plaintext highlighter-rouge">@GetMapping</code>에 전달한 요청 경로 설정값이다.</p>

<p>직접 애너테이션을 만들 수도 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">java.lang.annotation.ElementType</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.lang.annotation.Retention</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.lang.annotation.RetentionPolicy</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.lang.annotation.Target</span><span class="o">;</span>

<span class="nd">@Retention</span><span class="o">(</span><span class="nc">RetentionPolicy</span><span class="o">.</span><span class="na">RUNTIME</span><span class="o">)</span>
<span class="nd">@Target</span><span class="o">(</span><span class="nc">ElementType</span><span class="o">.</span><span class="na">TYPE</span><span class="o">)</span>
<span class="nd">@interface</span> <span class="nc">Role</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="nf">value</span><span class="o">();</span>
<span class="o">}</span>

<span class="nd">@Role</span><span class="o">(</span><span class="s">"ADMIN"</span><span class="o">)</span>
<span class="kd">class</span> <span class="nc">AdminService</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">@Retention(RetentionPolicy.RUNTIME)</code>은 실행 중 Reflection으로 이 애너테이션을 읽을 수 있게 한다. <code class="language-plaintext highlighter-rouge">@Target(ElementType.TYPE)</code>은 이 애너테이션을 클래스나 인터페이스에 붙일 수 있다는 뜻이다.</p>

<h2 id="8-요약">8. 요약</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Annotation
    ↓
코드에 역할과 설정 정보를 표시한다
    ↓
컴파일러 또는 프레임워크가 정보를 읽는다
    ↓
검사, 요청 매핑, Bean 등록, 의존성 주입 같은 처리를 수행한다
</code></pre></div></div>

<blockquote>
  <p>Annotation은 코드에 메타데이터를 붙이는 기능이다. 컴파일러는 코드 검증에 사용하고, Spring 같은 프레임워크는 Reflection으로 읽어 객체 관리와 요청 처리에 활용한다.</p>
</blockquote>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="advanced" /><category term="Java" /><category term="Annotation" /><category term="Reflection" /><category term="Spring" /><category term="Override" /><category term="Metadata" /><summary type="html"><![CDATA[Java Annotation의 메타데이터 역할과 Override, Spring 컴포넌트, 요청 매핑, Reflection의 연결을 정리한다.]]></summary></entry><entry><title type="html">Java 심화 Part 1: Reflection</title><link href="https://minnong511.github.io/java/advanced/2026/08/11/java-reflection/" rel="alternate" type="text/html" title="Java 심화 Part 1: Reflection" /><published>2026-08-11T10:40:00+09:00</published><updated>2026-08-11T10:40:00+09:00</updated><id>https://minnong511.github.io/java/advanced/2026/08/11/java-reflection</id><content type="html" xml:base="https://minnong511.github.io/java/advanced/2026/08/11/java-reflection/"><![CDATA[<h1 id="java-심화-part-1-reflection">Java 심화 Part 1: Reflection</h1>

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>단어</th>
      <th>정의</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Reflection</td>
      <td>실행 중인 클래스나 객체의 구조를 프로그램이 직접 조회하고 조작하는 기능</td>
    </tr>
    <tr>
      <td>Metadata</td>
      <td>클래스 이름, 필드, 메서드, 생성자, 애너테이션처럼 클래스 자체에 관한 정보</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Class&lt;?&gt;</code></td>
      <td>특정 클래스의 메타데이터를 다루는 객체</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Field</code></td>
      <td>클래스에 선언된 필드 정보를 나타내는 객체</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Method</code></td>
      <td>클래스에 선언된 메서드 정보를 나타내는 객체</td>
    </tr>
    <tr>
      <td>Annotation</td>
      <td>클래스나 메서드 등에 붙여 추가 의미를 전달하는 표식</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Reflection은 프로그램이 실행 중에 클래스의 구조를 직접 조사하고, 필요하면 메서드를 호출하거나 필드 값을 읽고 수정할 수 있게 해주는 기능이다.</p>
</blockquote>

<h2 id="2-reflection으로-확인할-수-있는-것">2. Reflection으로 확인할 수 있는 것</h2>

<p>Reflection을 사용하면 실행 중에 다음 정보를 확인할 수 있다.</p>

<ul>
  <li>이 클래스 이름이 무엇인지</li>
  <li>어떤 필드가 있는지</li>
  <li>어떤 메서드와 생성자가 있는지</li>
  <li>어떤 애너테이션이 붙어 있는지</li>
</ul>

<p>예제를 위한 <code class="language-plaintext highlighter-rouge">User</code> 클래스를 먼저 보자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">hello</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"hello"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="3-클래스-정보-가져오기">3. 클래스 정보 가져오기</h2>

<p><code class="language-plaintext highlighter-rouge">User.class</code>로 <code class="language-plaintext highlighter-rouge">User</code> 클래스의 정보를 담은 <code class="language-plaintext highlighter-rouge">Class</code> 객체를 가져올 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Class</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="n">clazz</span> <span class="o">=</span> <span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">;</span>

<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">clazz</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: User
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">clazz</code>에는 <code class="language-plaintext highlighter-rouge">User</code> 객체 한 개의 데이터가 아니라 <code class="language-plaintext highlighter-rouge">User</code> 클래스 자체의 구조 정보가 들어 있다.</p>

<h2 id="4-메서드와-필드-조회하기">4. 메서드와 필드 조회하기</h2>

<h3 id="메서드-조회">메서드 조회</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Method</span><span class="o">[]</span> <span class="n">methods</span> <span class="o">=</span> <span class="n">clazz</span><span class="o">.</span><span class="na">getDeclaredMethods</span><span class="o">();</span>

<span class="k">for</span> <span class="o">(</span><span class="nc">Method</span> <span class="n">method</span> <span class="o">:</span> <span class="n">methods</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">method</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: hello
</code></pre></div></div>

<h3 id="필드-조회">필드 조회</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Field</span><span class="o">[]</span> <span class="n">fields</span> <span class="o">=</span> <span class="n">clazz</span><span class="o">.</span><span class="na">getDeclaredFields</span><span class="o">();</span>

<span class="k">for</span> <span class="o">(</span><span class="nc">Field</span> <span class="n">field</span> <span class="o">:</span> <span class="n">fields</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">field</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: name
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">getDeclaredMethods()</code>와 <code class="language-plaintext highlighter-rouge">getDeclaredFields()</code>는 해당 클래스에 직접 선언된 메서드와 필드를 가져온다.</p>

<h2 id="5-실행-중에-메서드-호출하기">5. 실행 중에 메서드 호출하기</h2>

<p>Reflection을 사용하면 메서드 이름을 문자열로 찾아 실행할 수도 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">java.lang.reflect.Method</span><span class="o">;</span>

<span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">hello</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"hello"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ReflectionExample</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">ReflectiveOperationException</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>

        <span class="nc">Method</span> <span class="n">method</span> <span class="o">=</span> <span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">.</span><span class="na">getDeclaredMethod</span><span class="o">(</span><span class="s">"hello"</span><span class="o">);</span>
        <span class="n">method</span><span class="o">.</span><span class="na">invoke</span><span class="o">(</span><span class="n">user</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: hello
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">getDeclaredMethod("hello")</code>로 메서드를 찾고, <code class="language-plaintext highlighter-rouge">invoke(user)</code>로 <code class="language-plaintext highlighter-rouge">user</code> 객체에서 그 메서드를 실행한다.</p>

<h2 id="6-private-필드-접근">6. private 필드 접근</h2>

<p>Reflection을 사용하면 <code class="language-plaintext highlighter-rouge">private</code> 필드에도 접근을 시도할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">java.lang.reflect.Field</span><span class="o">;</span>

<span class="kd">class</span> <span class="nc">User</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">name</span> <span class="o">=</span> <span class="s">"민형"</span><span class="o">;</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ReflectionExample</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">ReflectiveOperationException</span> <span class="o">{</span>
        <span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">User</span><span class="o">();</span>

        <span class="nc">Field</span> <span class="n">field</span> <span class="o">=</span> <span class="nc">User</span><span class="o">.</span><span class="na">class</span><span class="o">.</span><span class="na">getDeclaredField</span><span class="o">(</span><span class="s">"name"</span><span class="o">);</span>
        <span class="n">field</span><span class="o">.</span><span class="na">setAccessible</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>

        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">field</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="n">user</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>출력: 민형
</code></pre></div></div>

<p>다만 <code class="language-plaintext highlighter-rouge">setAccessible(true)</code>는 캡슐화를 우회하는 기능이다. Java 모듈 환경에서는 접근이 제한될 수도 있으므로, 일반 애플리케이션 코드에서 무분별하게 사용하면 안 된다.</p>

<h2 id="7-spring에서-reflection을-사용하는-이유">7. Spring에서 Reflection을 사용하는 이유</h2>

<p>Spring 같은 프레임워크는 실행 중에 클래스를 조사해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserService</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Spring은 대략 다음 흐름으로 동작한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>클래스들을 조사한다
    ↓
@Service가 붙은 클래스를 찾는다
    ↓
관리할 객체를 생성한다
    ↓
Bean으로 등록한다
    ↓
필요한 곳에 의존성을 주입한다
</code></pre></div></div>

<p>예를 들어 <code class="language-plaintext highlighter-rouge">@Autowired</code>가 붙은 필드를 확인해 해당 타입의 객체를 넣어 주는 과정에도 Reflection이 활용될 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">OrderService</span> <span class="o">{</span>
    <span class="nd">@Autowired</span>
    <span class="kd">private</span> <span class="nc">UserService</span> <span class="n">userService</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="8-사용할-때-주의할-점">8. 사용할 때 주의할 점</h2>

<ul>
  <li>일반 코드보다 느릴 수 있다.</li>
  <li>컴파일러가 확인해 주는 타입 안정성이 약해질 수 있다.</li>
  <li><code class="language-plaintext highlighter-rouge">private</code> 접근 제한을 우회할 수 있어 코드 추적이 어려워진다.</li>
  <li>프레임워크 내부 기능이나 정말 동적으로 처리해야 하는 경우에 사용하는 편이 좋다.</li>
</ul>

<p>Spring, Hibernate, Jackson, 테스트 프레임워크처럼 클래스 구조를 자동으로 분석해야 하는 라이브러리와 프레임워크에서 Reflection을 많이 사용한다.</p>

<h2 id="9-요약">9. 요약</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Reflection
    ↓
실행 중에 클래스 구조를 조사한다
    ↓
필드, 메서드, 생성자, 애너테이션 정보를 확인한다
    ↓
필요하면 메서드 호출과 필드 접근도 가능하다
</code></pre></div></div>

<blockquote>
  <p>Reflection은 런타임에 클래스의 메타데이터를 조회하고 조작하는 기능이다. 강력하지만 타입 안정성과 캡슐화를 약화시킬 수 있으므로, 주로 프레임워크 내부나 동적 처리가 꼭 필요한 상황에서 사용한다.</p>
</blockquote>

<ul>
  <li>런타임 동적 로딩 및 조작
    <ul>
      <li>컴파일 시점이 아닌 실행 중에 객체 정보를 확인하거나 수정 가능</li>
      <li>클래스 이름이 동적으로 결정되거나, 조건에 따라 특정 메서드를 실행해야 할 때 사용</li>
    </ul>
  </li>
  <li>어노테이션 기반 동작 제어
    <ul>
      <li>런타임에 Annotation을 읽어서 특정 메서드나 필드에 대해 자동 동작을 수행</li>
      <li>예: Spring에서 Autowired로 의존성 주입, RequestMapping으로 URL 메서드 매핑</li>
    </ul>
  </li>
  <li>플러그인 시스템 및 동적 확장
    <ul>
      <li>실행 중에 플러그인 추가, 또는 인터페이스를 구현하지 않은 클래스도 동적으로 실행 가능</li>
      <li>외부 패키지의 클래스를 런타임에 로드하여 확장 기능 구현 가능</li>
    </ul>
  </li>
</ul>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="advanced" /><category term="Java" /><category term="Reflection" /><category term="Metadata" /><category term="Annotation" /><category term="Spring" /><category term="DI" /><summary type="html"><![CDATA[Java Reflection으로 런타임에 클래스, 필드, 메서드, 애너테이션 정보를 조회하고 호출하는 방법을 정리한다.]]></summary></entry><entry><title type="html">Java 기초 Part 5: Stream API</title><link href="https://minnong511.github.io/java/basics/2026/08/11/java-stream-api/" rel="alternate" type="text/html" title="Java 기초 Part 5: Stream API" /><published>2026-08-11T10:20:00+09:00</published><updated>2026-08-11T10:20:00+09:00</updated><id>https://minnong511.github.io/java/basics/2026/08/11/java-stream-api</id><content type="html" xml:base="https://minnong511.github.io/java/basics/2026/08/11/java-stream-api/"><![CDATA[<h1 id="java-기초-part-5-stream-api">Java 기초 Part 5: Stream API</h1>

<h2 id="1-먼저-알아둘-단어">1. 먼저 알아둘 단어</h2>

<table>
  <thead>
    <tr>
      <th>단어</th>
      <th>정의</th>
      <th>예시</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Stream</td>
      <td>컬렉션의 원본 데이터를 바꾸지 않고 요소를 순서대로 처리하는 흐름</td>
      <td><code class="language-plaintext highlighter-rouge">numbers.stream()</code></td>
    </tr>
    <tr>
      <td>파이프라인(Pipeline)</td>
      <td>여러 연산을 연결한 데이터 처리 과정</td>
      <td><code class="language-plaintext highlighter-rouge">stream().filter().map().toList()</code></td>
    </tr>
    <tr>
      <td>중간 연산</td>
      <td>Stream을 받아 새로운 Stream을 반환하는 연산</td>
      <td><code class="language-plaintext highlighter-rouge">filter()</code>, <code class="language-plaintext highlighter-rouge">map()</code>, <code class="language-plaintext highlighter-rouge">sorted()</code></td>
    </tr>
    <tr>
      <td>최종 연산</td>
      <td>Stream 처리를 실행하고 결과를 만드는 연산</td>
      <td><code class="language-plaintext highlighter-rouge">toList()</code>, <code class="language-plaintext highlighter-rouge">forEach()</code></td>
    </tr>
    <tr>
      <td>람다식(Lambda)</td>
      <td>짧게 표현한 익명 함수</td>
      <td><code class="language-plaintext highlighter-rouge">n -&gt; n * 2</code></td>
    </tr>
    <tr>
      <td>메서드 참조</td>
      <td>이미 존재하는 메서드를 간결하게 전달하는 문법</td>
      <td><code class="language-plaintext highlighter-rouge">System.out::println</code></td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Stream API는 리스트 같은 데이터 묶음을 하나씩 꺼내서 조건을 걸고, 바꾸고, 다시 모으는 데이터 처리 기능이다.</p>
</blockquote>

<p>Java 8에서 도입됐으며, 반복문이나 <code class="language-plaintext highlighter-rouge">Iterator</code>로 작성하던 코드를 선언적이고 함수형인 스타일로 표현할 수 있게 해준다.</p>

<h2 id="2-stream-api의-핵심">2. Stream API의 핵심</h2>

<p>Stream API의 핵심은 데이터 처리 과정을 위에서 아래로 읽을 수 있다는 점이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">names</span> <span class="o">=</span> <span class="n">users</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">user</span> <span class="o">-&gt;</span> <span class="n">user</span><span class="o">.</span><span class="na">getAge</span><span class="o">()</span> <span class="o">&gt;=</span> <span class="mi">20</span><span class="o">)</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="nl">User:</span><span class="o">:</span><span class="n">getName</span><span class="o">)</span>
        <span class="o">.</span><span class="na">sorted</span><span class="o">()</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<p>위 코드는 다음 순서로 읽을 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>users를 Stream으로 만든다
    ↓
20세 이상인 사용자만 남긴다
    ↓
User 객체를 이름으로 바꾼다
    ↓
이름을 정렬한다
    ↓
List로 모은다
</code></pre></div></div>

<h2 id="3-반복문과-stream-비교">3. 반복문과 Stream 비교</h2>

<p>예를 들어 숫자 리스트에서 짝수만 뽑는다고 하자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numbers</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> <span class="mi">3</span><span class="o">,</span> <span class="mi">4</span><span class="o">,</span> <span class="mi">5</span><span class="o">);</span>
</code></pre></div></div>

<p>반복문을 사용하면 결과 리스트를 직접 만들고 값을 추가해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>

<span class="k">for</span> <span class="o">(</span><span class="kt">int</span> <span class="n">number</span> <span class="o">:</span> <span class="n">numbers</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">number</span> <span class="o">%</span> <span class="mi">2</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">result</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">number</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Stream을 사용하면 데이터 처리 과정에 집중해서 작성할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">%</span> <span class="mi">2</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<p>다음과 같이 읽으면 된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>numbers를 Stream으로 만든다
    ↓
짝수만 남긴다
    ↓
List로 모은다
</code></pre></div></div>

<h2 id="4-자주-사용하는-메서드">4. 자주 사용하는 메서드</h2>

<table>
  <thead>
    <tr>
      <th>메서드</th>
      <th>구분</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">stream()</code></td>
      <td>Stream 생성</td>
      <td>컬렉션에서 데이터 처리 흐름을 시작한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">filter()</code></td>
      <td>중간 연산</td>
      <td>조건에 맞는 요소만 남긴다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">map()</code></td>
      <td>중간 연산</td>
      <td>요소를 다른 값이나 형태로 바꾼다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">sorted()</code></td>
      <td>중간 연산</td>
      <td>요소를 정렬한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">distinct()</code></td>
      <td>중간 연산</td>
      <td>중복 요소를 제거한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">toList()</code></td>
      <td>최종 연산</td>
      <td>처리 결과를 <code class="language-plaintext highlighter-rouge">List</code>로 모은다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">forEach()</code></td>
      <td>최종 연산</td>
      <td>각 요소에 지정한 동작을 실행한다.</td>
    </tr>
  </tbody>
</table>

<h2 id="5-filter-조건에-맞는-값만-남기기">5. <code class="language-plaintext highlighter-rouge">filter()</code>: 조건에 맞는 값만 남기기</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">names</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"민형"</span><span class="o">,</span> <span class="s">"철수"</span><span class="o">,</span> <span class="s">"영희"</span><span class="o">,</span> <span class="s">"김민형"</span><span class="o">);</span>

<span class="n">names</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">name</span> <span class="o">-&gt;</span> <span class="n">name</span><span class="o">.</span><span class="na">length</span><span class="o">()</span> <span class="o">&gt;</span> <span class="mi">2</span><span class="o">)</span>
        <span class="o">.</span><span class="na">forEach</span><span class="o">(</span><span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">::</span><span class="n">println</span><span class="o">);</span>
</code></pre></div></div>

<p>결과는 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>김민형
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">filter()</code>는 조건을 만족하는 요소만 통과시킨다. 요소의 값 자체를 다른 값으로 바꾸지는 않는다.</p>

<h2 id="6-map-값을-다른-형태로-바꾸기">6. <code class="language-plaintext highlighter-rouge">map()</code>: 값을 다른 형태로 바꾸기</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numbers</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> <span class="mi">3</span><span class="o">);</span>

<span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">*</span> <span class="mi">2</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>결과: [2, 4, 6]
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">map()</code>은 각 요소를 전달받아 새로운 값으로 바꾼다.</p>

<h2 id="7-filter와-map-함께-사용하기">7. <code class="language-plaintext highlighter-rouge">filter()</code>와 <code class="language-plaintext highlighter-rouge">map()</code> 함께 사용하기</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numbers</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> <span class="mi">3</span><span class="o">,</span> <span class="mi">4</span><span class="o">,</span> <span class="mi">5</span><span class="o">);</span>

<span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">&gt;</span> <span class="mi">2</span><span class="o">)</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">*</span> <span class="mi">10</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<p>처리 흐름은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1, 2, 3, 4, 5]
    ↓ filter(number -&gt; number &gt; 2)
[3, 4, 5]
    ↓ map(number -&gt; number * 10)
[30, 40, 50]
</code></pre></div></div>

<p>기억할 내용은 간단하다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>filter → 골라낸다
map    → 바꾼다
</code></pre></div></div>

<h2 id="8-중간-연산과-최종-연산">8. 중간 연산과 최종 연산</h2>

<p><code class="language-plaintext highlighter-rouge">filter()</code>, <code class="language-plaintext highlighter-rouge">map()</code>, <code class="language-plaintext highlighter-rouge">sorted()</code> 같은 중간 연산만 작성하면 실제 데이터 처리가 시작되지 않는다. <code class="language-plaintext highlighter-rouge">toList()</code>나 <code class="language-plaintext highlighter-rouge">forEach()</code> 같은 최종 연산이 호출되어야 전체 파이프라인이 실행된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="o">{</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"확인: "</span> <span class="o">+</span> <span class="n">number</span><span class="o">);</span>
            <span class="k">return</span> <span class="n">number</span> <span class="o">%</span> <span class="mi">2</span> <span class="o">==</span> <span class="mi">0</span><span class="o">;</span>
        <span class="o">});</span>
</code></pre></div></div>

<p>위 코드는 최종 연산이 없기 때문에 <code class="language-plaintext highlighter-rouge">filter()</code> 내부의 출력도 실행되지 않는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">%</span> <span class="mi">2</span> <span class="o">==</span> <span class="mi">0</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<p>이처럼 필요한 시점까지 실행을 미루는 방식을 지연 연산(Lazy Evaluation)이라고 한다.</p>

<h2 id="9-사용할-때-주의할-점">9. 사용할 때 주의할 점</h2>

<h3 id="원본-컬렉션은-바뀌지-않는다">원본 컬렉션은 바뀌지 않는다</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">numbers</span> <span class="o">=</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> <span class="mi">3</span><span class="o">);</span>

<span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">result</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">number</span> <span class="o">-&gt;</span> <span class="n">number</span> <span class="o">*</span> <span class="mi">10</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>numbers → [1, 2, 3]
result  → [10, 20, 30]
</code></pre></div></div>

<p>Stream은 원본 컬렉션을 직접 변경하는 기능이 아니다. 처리 결과가 필요하면 새로운 리스트 등으로 받아야 한다.</p>

<h3 id="stream은-한-번만-사용할-수-있다">Stream은 한 번만 사용할 수 있다</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Stream</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">stream</span> <span class="o">=</span> <span class="n">numbers</span><span class="o">.</span><span class="na">stream</span><span class="o">();</span>

<span class="n">stream</span><span class="o">.</span><span class="na">toList</span><span class="o">();</span>
<span class="n">stream</span><span class="o">.</span><span class="na">toList</span><span class="o">();</span> <span class="c1">// IllegalStateException</span>
</code></pre></div></div>

<p>최종 연산이 끝난 Stream은 다시 사용할 수 없다. 다시 처리하려면 <code class="language-plaintext highlighter-rouge">numbers.stream()</code>으로 새로운 Stream을 만들어야 한다.</p>

<h2 id="10-최종-정리">10. 최종 정리</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>핵심 내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Stream</td>
      <td>데이터 자체가 아니라 데이터 처리 흐름이다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">filter()</code></td>
      <td>조건에 맞는 요소만 남긴다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">map()</code></td>
      <td>요소를 다른 값이나 형태로 바꾼다.</td>
    </tr>
    <tr>
      <td>중간 연산</td>
      <td>Stream을 반환하며, 최종 연산 전까지 실행을 미룬다.</td>
    </tr>
    <tr>
      <td>최종 연산</td>
      <td>파이프라인을 실행하고 결과를 만든다.</td>
    </tr>
    <tr>
      <td>원본 데이터</td>
      <td>Stream으로 처리해도 원본 컬렉션은 그대로 유지된다.</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>Stream API는 데이터를 어떻게 반복할지보다 어떤 조건으로 골라내고, 어떻게 바꾸고, 어떤 결과로 모을지에 집중하게 해주는 기능이다.</p>
</blockquote>]]></content><author><name>Min Hyeong Lee</name></author><category term="java" /><category term="basics" /><category term="Java" /><category term="Stream API" /><category term="Collection" /><category term="Lambda" /><category term="Filter" /><category term="Map" /><summary type="html"><![CDATA[Java Stream API의 파이프라인, 중간 연산과 최종 연산을 filter, map, sorted 예제로 정리한다.]]></summary></entry></feed>